# omf-mes 기획 문서

omf-mes(OhMyFactory MES — AI Agent 네이티브 MES) 프로젝트의 기획안을 작성·관리하는 저장소다.
코드 없이 한국어 Markdown 문서만 다룬다. 작성 주체는 CREFLE OMF 팀.

**모든 문서는 내부 대외비다.** 문서 내용을 외부 서비스에 업로드·공유·게시하지 않는다.

## 폴더 구조

| 폴더 | 용도 | 핵심 규칙 |
| --- | --- | --- |
| `planning/` | 작성한 기획안 정리 (버전 관리) | 아래 버전 관리 규칙을 따른다 |
| `research/` | 기획 과정에서 직접 만든 조사·정리 자료 | 출처를 반드시 남긴다 |
| `raw/` | 외부에서 가져온 참조용 원본 자료 | **읽기 전용 — 생성·수정·삭제 금지** |

공통 파일명 규칙: `YYYY-MM-DD-<제목>.md` (날짜 = 최초 작성일).

## raw/ — 읽기 전용 (절대 규칙)

- raw/ 안의 파일은 **읽기만 한다.** 어떤 파일도 만들거나, 고치거나, 지우거나, 옮기지 않는다. 다운로드 저장도 금지. 자료 추가는 사용자가 직접 한다.
- 이 규칙은 `.claude/settings.json` 권한 규칙과 `.claude/hooks/protect_readonly.py` 훅이 강제한다. 작업이 차단되면 우회하지 말고, 산출물을 research/ 또는 planning/에 만든다.
- raw/ 자료를 요약·발췌·번역·정리한 결과물은 research/에 새 문서로 작성한다.
- raw/ 자료 현황은 `research/raw-자료-목록.md`에서 관리한다. 갱신은 `/raw-index`.

## research/ — 조사·정리 자료

- 문서 머리에 출처를 남긴다 — raw/ 파일이면 경로, 웹이면 URL과 접근일, 조사 방법.
- 검증된 사실과 해석·추정을 구분해 표기한다 (기존 문서의 적대적 검증 표기 방식 유지).

## planning/ — 기획안과 버전 관리

```
planning/
├── YYYY-MM-DD-<제목>.md   # 작업본 — 항상 최신 내용, 수정은 여기에만
└── versions/
    └── <제목>-v1.1.md     # 확정 스냅샷 — 수정·삭제 금지(훅이 차단)
```

- 작업본을 수정해 가다가, 매듭 시점에 `/snapshot`으로 버전을 확정한다(작업본을 versions/에 복사).
- 스냅샷 파일명: 작업본 파일명에서 날짜 prefix를 떼고 `-v<버전>`을 붙인다.
- 버전 의미: `v0.x` 초안 단계, `v1.0` 최초 확정, 이후 변경 폭에 따라 증가.
- 작업본을 수정하면 문서 머리의 버전·날짜 표기를 갱신한다.

문서 머리 형식 (기존 문서와 동일한 blockquote 스타일):

```markdown
# <제목>

> 작성일: YYYY-MM-DD (v0.1 — 작성 중) · 작성 주체: CREFLE OMF 팀
> 근거 자료: research/·raw/ 경로 나열
> 표기 원칙: 검증된 사실과 **내부 가설**을 구분 표기한다. 가설은 `[가설]`로 표시하고 검증 방법을 병기한다.
```

문서 끝에 변경 이력 표를 둔다:

```markdown
## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
```

## 작성 지침

- 기획안의 사실·수치는 research/ 문서에 근거를 두고 해당 경로를 '근거 자료'에 남긴다. 근거가 없으면 `[가설]` 또는 `(확인 필요)`를 붙인다.
- 문서 간 링크는 저장소 루트 기준 상대 경로를 쓴다.
- 날짜는 항상 `YYYY-MM-DD` 형식.

## 하네스 구성 (.claude/)

- `settings.json` — raw/ 쓰기 거부 권한 규칙 + 보호 훅 등록.
- `hooks/protect_readonly.py` — raw/ 전체와 `planning/versions/` 기존 파일의 변경을 차단한다(파일 도구·Bash 모두). 훅이 작업을 막으면 의도된 동작이므로 우회하지 말고 사용자에게 알린다.
- 커맨드:
  - `/new-plan <주제>` — 새 기획안 작업본 스캐폴딩
  - `/snapshot [주제] [버전]` — 작업본을 버전 스냅샷으로 확정
  - `/raw-index` — raw/ 자료 목록 갱신

### 팀 하네스: FigJam 워크플로우 도식 제작

**목표:** 워크플로우 스펙(md) → FigJam 도식 작화를 설계자·작업자·검토자 3인 에이전트 팀으로 수행한다.

**트리거:** FigJam 도식의 신규 작화·수정·재작업 요청 시 `figjam-workflow-team` 스킬을 사용하라. 도식에 대한 단순 조회·질문·의견은 직접 응답 가능. (`figjam-workflow-diagram`은 공통 지식 베이스 — 팀 스킬과 에이전트들이 참조)

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-07-13 | 초기 구성 — 에이전트 3종(workflow-designer/drawer/reviewer) + 스킬 3종(figjam-workflow-team/workflow-plan-brief/workflow-review-checklist). 작업자 모델 Opus 4.8 고정 | 전체 | 사용자 하네스 구성 지시 |
| 2026-07-13 | 경량 경로 추가 — 소규모 수정(변경 요소 ≤5·스펙 해석 불요)은 리더 직접 수정 + 검토자 1회 검증(미니 지시서 필수), r2 FAIL 시 표준 경로 전환 | skills/figjam-workflow-team | 파일럿(01-S-I 재배선) 피드백: 에이전트 3왕복이 소규모 수정에 과잉, 검토자 검증은 실효 입증되어 유지 |
| 2026-07-13 | 작업 페이지 v2 고정 — 모든 도식 작업은 "system workflow v2"(219:4509), v1은 명시 지정 시에만(동결 구판). 매 콜 setCurrentPageAsync 필수 규칙 병기 | skills/figjam-workflow-team·figjam-workflow-diagram | 사용자 지시: 명시적으로 v1을 명명하지 않으면 v2만 작업 |
| 2026-07-14 | 검토 항목 확장 — 텍스트 렌더 클리핑 검사를 신규 도형뿐 아니라 텍스트 갱신된 기존 도형 전부로 확대(격리 스크린샷 필수) | skills/workflow-review-checklist | 02 마이그레이션에서 동형 결함 2회(태그 3건 r1 적발 + 재코딩 헥사곤 1건 검토 누락→리더 발견) |
| 2026-07-14 | 겹침 사전 방어 규칙 3종(사용자 승인) — ①축 단계별 정의+레인 점유표·동적 축 간격 ②간격 최소값(노드 ≥40·도형↔라벨 ≥16·라벨 직선 구간 ≥라벨 폭+40) ③헤어핀 금지(간격 확장 우선) | skills/figjam-workflow-diagram·workflow-plan-brief·workflow-review-checklist + agents/workflow-drawer | 02 회랑 겹침 사고 + 헤어핀 사례 — 사후 수정이 아닌 사전 방어로 전환 |

### 팀 하네스: 문서 작성·정합성 검증

**목표:** 기획 문서(research/·planning/)의 작성·수정을 검증 담당자→작성 담당자 2인으로 수행한다. 한 결정이 여러 문서로 파급되는 정합성 리스크를, 작성 전 검증 게이트로 방어한다.

**트리거:** 미결정 사항 정리·조회, 결정 사항 기록·반영, 정합성/충돌 검토, 확정기록 신설, 스펙·데이터모델·워크플로우 문서 갱신 요청 시 `doc-team` 스킬을 사용하라. 단순 문서 열람·조회는 직접 응답 가능. FigJam 캔버스 작화는 제외(→ `figjam-workflow-team`). (`doc-consistency-checklist`는 공통 지식 베이스 — 두 에이전트가 참조.)

**모델 고정 (사용자 지시):** 검증 담당자=Opus 4.8(`model: "opus"`) · 작성 담당자=Sonnet 5(`model: "sonnet"`). 검증은 추론 무게가 큰 교차 대조라 Opus, 작성은 승인된 지시서의 기계적 실행이라 Sonnet.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-07-15 | 초기 구성 — 에이전트 2종(doc-validator/doc-writer) + 스킬 2종(doc-team 오케스트레이터·doc-consistency-checklist 지식 베이스). 서브 에이전트 패턴(검증→사람 게이트→작성 순차·역할별 모델 상이), 검증 담당자 읽기 전용·작성 담당자 지시서 밖 무작성 | 전체 | 사용자 하네스 구성 지시 — 문서 파급 정합성 수작업의 오류 리스크 |
| 2026-07-16 | 경량 경로 추가 — 단순 정정·확정(단일 문서 1~2곳·신규 엔티티/규칙 없음·파급 없음·확정 뒤집기 아님)은 리더 직접 편집 + grep 자가 검증, 검증·작성 에이전트 스폰 생략. 스코프 확인 중 파급·충돌 발견 시 표준 Flow B로 에스컬레이션 | skills/doc-team | 첫 후속(용어 통일)이 단일 1곳 수정이라 2에이전트 스폰이 과잉 — FigJam 하네스 경량 경로와 동형 |
