# 02 작성 보고 — 자재LOT 재정의 §5 (명세서·개념모델·WF01)

> run: `2026-07-20-자재LOT재정의` · 작성 담당자(doc-writer, Sonnet)
> 지시서: `01_validation_자재LOT재정의.md` §5 초안 중 **G-명세서·G-개념모델·G-WF01만** 실행. G-도식스펙01·§2B 6문서는 손대지 않음(확인: 미접촉).
> 근거 마커 정본: `research/2026-07-20-자재LOT관리-재정의-확정기록.md`(v1.1)

## 결과 요약

| 문서 | 편집 수 | 버전 | 변경 이력 | 앵커 불일치/미실행 |
| --- | --- | --- | --- | --- |
| G-명세서 | 5 | v1.0 → v1.1 | 반영 완료 | 없음 |
| G-개념모델 | 18 | v2.7 → v2.8 | 반영 완료 | 없음 |
| G-WF01 | 20 (19 지시 + 1 추가, 아래 설명) | v1.4 → v1.5 | 반영 완료 | 없음 |

전 항목 앵커가 원문과 정확히 매칭되어 지시서 그대로 실행했다. 앵커 불일치로 멈춘 항목 없음.

---

## 1. G-명세서 — `research/2026-07-03-MES-요구사항-명세서-v1-정리.md`

편집 5건:
1. 헤더 정리일: `2026-07-03` → `2026-07-03 · 2026-07-20 (v1.1)` 병기.
2. L43 REQ-PR-0001 비고 셀(공란) → `(§5-8 재정의 주석 참조 — 공급사 번호 수용·등록)`.
3. L49 REQ-PR-0007 비고 셀(공란) → `(§5-8 재정의 주석 참조 — LOT 등록=입하 Hold·Release=IQC 합격)`.
4. §5에 관찰 항 8 신설(항목 7 다음, "## 정리 이력" 앞) — 재정의 요지 + 근거 경로.
5. 정리 이력 표에 v1.1 행 추가(v1.0 위).

**원문 As-Is/To-Be 셀은 전부 불변** — L43·L49의 "현행"·"개선요구" 열은 원문 그대로 두고 비고 열만 채웠다(전사 무결성 유지, 지시서 준수). 표 열 개수(8열) pipe count로 재검증 완료.

---

## 2. G-개념모델 — `research/2026-07-03-개념데이터모델-v2-요구사항통합.md`

편집 18건(지시서 §5 G-개념모델 항목 전부):
1. 헤더 버전: `v2.7` → `v2.8`(정리일 2026-07-07 고정 유지, 파라미터 텍스트만 갱신 — 이 문서의 기존 헤더 표기 관행).
2. **L206 자재LOT 엔티티**: 정의를 "입하 등록(Hold)→Release(IQC 합격)"로 교체 + 핵심 속성에 5개 항목(생성 시점·발번 주체·No 구성·수량·P/O 참조) 전부 반영, 지시서 마커 그대로 사용.
3. **L265 Lot Status**: 한도승인 열거에 "긴급 IQC 생략 승인" 확장 문구 삽입.
4. **L274 §4 품질 비고**: 정면 대체 — "Hold 규칙 불성립" 폐기 → "Hold 통제가 자재LOT에도 적용" 신문.
5. **L289 검사실적(IQC)—자재LOT 관계**: 정면 반전 — "생성 트리거" → "Release 트리거".
6. **마커 스왑 12곳**(L60·L100·L111·L126·L154·L190·L199-201·L234·L247·L446·L507·L578): 각 문맥 유지하며 "생성"→"등록(입하 Hold)·Release(합격)", 발번은 자재LOT 한정 경로 이원화로 교체 + `(기존 서술 폐기: …)` 흔적 부기. 생산LOT·제품LOT은 MES 단일 발번 그대로 보존(자재LOT만 이원화 — 지시서 "발번은 자재LOT 한정 이원화" 준수).
7. **변경 이력**: v2.8 행 삽입(v2.7 위), 지시서 문구 그대로.
8. **엔티티 총계(64) 불변**: 신규 엔티티 없음 확인 — 편집 없음(지시서상 "총계 불변 확인"이 요구한 것은 검증이지 숫자 수정이 아님).

전 편집 후 표 pipe count로 컬럼 붕괴 여부 재검증(L60·L206·L265·L289 모두 6 — 정상).

---

## 3. G-WF01 — `research/2026-07-03-워크플로우-요구사항반영-01-자재창고관리.md`

편집 20건(지시서 19건 + 추가 1건, 아래 설명):
1. 헤더 정리일: `2026-07-14` 다음에 `· 2026-07-20 (v1.5 — 자재LOT 재정의 반영)` 병기.
2. **개요 도식(L40-41)**: S2에 "자재LOT 등록=Hold" 부기, S3→S4 사이에 "Release" 삽입, S4 괄호에서 "자재LOT 생성=IQC 합격 시점" 제거 + 폐기 흔적.
3. S2 Action 말미: "입하→IQC→LOT 생성" 순서 서술 → "입하(등록=Hold·P/O 확인)→IQC(Release)"로 교체 + P/O 확인·발번 경로 이원화 반영.
4. S2 Result: "자재LOT도 아직 미생성" → "자재LOT 등록(Hold)" + 폐기 마커.
5. S3 Result: "합격 시점에 자재LOT 생성" → "IQC 합격=Release".
6. S3 Rule: "IQC 합격 시점에 생성"·"입하 대기(LOT 미생성)" → "입하 시 이미 등록(Hold)…Release" + IQC 대상 전량·긴급 생략=한도승인 반영.
7. S4 제목: "자재LOT 생성" → "자재LOT Release · 재고 가용화".
8. S4 Action: "자재LOT 생성" → "자재LOT Release" + 사전부착 경로를 "공급사 번호 수용·등록"으로 재서술.
9. S4 Result: "자재LOT 생성(생성 시점=IQC 합격)" → "자재LOT Release(Hold→가용)".
10. S4 Rule: "자재LOT 생성·바코드 발행·재고 반영" → "자재LOT Release·바코드 발행·재고 가용화"(LOT 수량 유연화 서술은 무관하여 불변 유지).
11. S4 Exception①: "합격 전 LOT 미생성이라 가입고 식별 체계 별도 필요" → "이미 등록(Hold)이므로 불요 — 긴급 시 한도승인 경로".
12. S4 Boundary: "자재LOT 발번·생성=MES 정본" → "발번·등록=MES 정본(경로 이원화)".
13. S4 Open-Issue(A1 절): "LOT 3종 전부 MES 발번, 사전발행+IQC 합격 시 생성 2단계" → "생산·제품LOT은 MES 유지, 자재LOT은 경로 이원화·등록(Hold)→Release".
14. **S4 Open-Issue(C4 절, 추가 1건)**: 지시서 미적시 항목이나 §5 아래 부연. 바로 위 13번과 **같은 줄, 같은 필드**(L109) 안에서 몇 단어 뒤에 "~~C4(합격→생성 vs Release)~~ → 확정(생성)"이 그대로 남아 13번 수정과 정면 모순되는 것을 발견 — "S4 (L97–110)" 지시 범위 내 동일 패턴이라 판단해 "확정(생성)" → "재정의: Release(기존 서술 폐기: 확정(생성))"로 정정. 새 위치 창작이 아니라 이미 지시된 줄 내부의 잔여 조각.
15. S4 Check: "발번 주체=MES, IQC 합격 시점 생성" → "경로 이원화, 등록=입하(Hold)·Release=IQC 합격".
16. S10 Result(L196): "IQC 불합격분은 LOT 미생성 상태에서 반품" → "Hold 유지 상태의 자재LOT을 반품".
17. 예외 카탈로그 E3(L286): "합격 전 LOT 미생성이라 가입고 식별 체계 별도 필요" → "이미 등록(Hold)이므로 불요 — 긴급 시 한도승인 경로".
18. 요약매핑 S3 행: Release 재정의 + 긴급 IQC 생략=한도승인 부기.
19. 요약매핑 S4 행: 경로 이원화·Release·재고 가용화 재정의 부기.
20. 변경 이력: v1.5 행 삽입(v1.4 위), 지시서 문구 그대로.

**S10 "L191" 관련**: 지시서가 "S10 (L196·L191 Trigger·Result)"로 두 줄을 명명했으나, 실측 결과 대상 문구("LOT 미생성 상태에서 반품")는 L196(Result)에만 존재하고 L191(Trigger)에는 해당 패턴이 없다(Trigger는 IQC 불합격·초과 입하 후속 반품 트리거를 서술할 뿐 LOT 생성/등록 시점 언급이 없음). L191은 억지로 손대지 않고 L196만 수정했다 — "지정 라인이 이미 다르게 서술돼 있으면 억지 적용 말고 보고" 원칙 적용.

전 편집 후 표 pipe count로 요약매핑·예외카탈로그 행 컬럼 붕괴 여부 재검증(정상).

---

## 4. 스코프 밖 발견 사항 (미실행 — 검증 담당자 확인 필요)

작업 중 지시서의 마커 스왑/필드 목록에 **포함되지 않은** 동일 패턴("IQC 합격 생성"/"LOT 미생성"류)의 잔존을 아래에서 발견했다. 지시서에 없는 위치라 **손대지 않았다**(창작 금지 원칙) — 검증 담당자가 스코프 판단 후 별도 지시서로 처리 필요:

- **개념모델**: L312(클러스터4 ERD 아스키아트 "합격 시 생성"), L345(클러스터4 미결 "IQC 합격이 자재LOT을 생성(create) — Release 아님" + "'입하 대기(LOT 미생성)' 상태" — L274·L289와 동일 클러스터 내 정면 모순), L544(§7 통합 ERD 백본 "사전발행→IQC 합격 생성"), L562(§7 횡단 핵심 관계 표 "합격 시 생성 ✓✅0007").
- **WF01**: L31(범위 IN 서술 "입고(자재LOT 생성·적치)"), L202(S10 Check "LOT 생성 확정(REQ-PR-0007)에 따르면 불합격분은 입고 전 반품이 정합적").

L345는 특히 주의 요망 — L274·L289(내가 수정)와 같은 클러스터4 안에 "생성(create)·Release 아님"이 그대로 남아 있어, 같은 섹션 내에서 신·구 모델이 병존하는 상태다.

## 5. 근거 자료 헤더 미변경 사항

WF01 헤더의 "근거 자료" 불릿 목록에 `research/2026-07-20-자재LOT관리-재정의-확정기록.md`를 새 불릿으로 추가하지 않았다 — 이 문서는 과거 버전(v1.1·v1.2·v1.4)마다 정본 확정기록을 불릿으로 추가해온 관행이 있으나, 지시서는 "버전: v1.4 → v1.5" + 구체 라인 편집 + "변경 이력 v1.5 행 + 헤더 갱신"만 명시했고 checklist §5도 근거 확정기록 경로 병기를 **변경 이력 행**에 요구할 뿐 근거 자료 불릿 추가를 명시하지 않는다. 변경 이력 행에는 확정기록 경로를 넣었으므로 추적성은 확보했다고 판단해 불릿 추가는 보류했다 — 필요시 후속 지시로 처리 가능.
