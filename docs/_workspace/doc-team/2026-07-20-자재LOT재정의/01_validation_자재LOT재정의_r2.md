# 01 검증 r2 — 자재LOT 재정의 §2B 6문서 정밀 작성 지시서 (게이트 해소)

> run: `2026-07-20-자재LOT재정의` · 회차 **r2** · 모드: **게이트(Flow B)** · 검증 담당자(doc-validator, Opus)
> 결정 정본: `research/2026-07-20-자재LOT관리-재정의-확정기록.md` (**v1.1** — §4 파급 4→10문서 확장·§3 재고 편입=입하 Hold/가용 Release·불합격=Hold 유지·반품 일괄 대체)
> 선행 보고: `_workspace/doc-team/2026-07-20-자재LOT재정의/01_validation_자재LOT재정의.md` (r1 — BLOCK)
> 참조: doc-consistency-checklist §5 G-개념모델 패턴(생성→등록(Hold)·Release·폐기 흔적) · §6 작성 지시서 포맷 · 6문서 앵커 read-only 실측 완료

## 판정: **CLEAR (5문서 즉시 실행) + 도식스펙03 figjam 선행 게이트 (지시서 준비완료·실행 보류)**

r1이 BLOCK한 3대 선결(Q1 스코프·Q2 불합격 표기·Q3 재고 반영 정합)은 **확정기록 v1.1 개정으로 사용자 승인·해소**됐다(§4 파급 10문서 명문화, §3 "불합격=Hold 유지·반품(≠미생성) 일괄 대체" + "재고 편입=입하 등록 시 Hold·가용=Release"). 따라서 §2B 6문서에 대해 **신규 충돌·추가 질문 없음** → 작성 지시서 발행.

단 **도식스펙03**은 4개 앵커 중 2개(L242·L295)가 **캔버스 텍스트 노드의 소급 전사물**이라, 스펙만 먼저 고치면 (a) 스펙≠캔버스, (b) 도식스펙03 내부 불일치(신모델 산문 vs 구모델 전사)가 생긴다. → 문서 전체를 **figjam-workflow-team 선행 후 일괄 실행**하도록 게이트한다(r1이 도식스펙01을 다룬 방식과 동형). 지시서 내용은 준비 완료.

---

## §0. 공통 재정의 매핑 (구모델 문구 → 신모델 문구 사전)

전 문서 공통. 확정기록 §2·§3 기준. 작성 담당자는 각 앵커에서 아래 축을 적용한다.

| 축 | 구모델 (폐기) | 신모델 (2026-07-20 확정) | 결정# |
| --- | --- | --- | --- |
| 생성 시점 | 자재LOT = **IQC 합격 시점 생성** | 자재LOT = **입하 시점 등록 (Lot Status=Hold)** | #1·#5 |
| 가용 전이 | 합격이 '생성'이며 **Release 아님**(C4) | IQC 합격 = **Release(가용 전이)**, 입하 시 이미 등록(Hold) — Release 모델 | #5·#7 |
| 합격 전 상태 | 합격 전 **LOT 미존재**('입하 대기·LOT 미생성') | 합격 전 = **등록된 Hold** 상태 | #5 |
| 불합격 | **불합격=LOT 미생성** | **불합격=Hold 유지·반품(재고 미가용)** | §3 |
| 발번 주체 | **MES 단일**(LOT 3종 전부 MES) | **경로 이원화**: 사전부착=공급사 번호 MES 수용·등록 / 직접부착=MES 발번 | #4 |
| 재고 편입 | (합격 시 입고 생성) | 입하 등록 시 재고 행 생성(**상태 Hold=가용 아님**), **가용=Release(합격)** | §3 |

**마커 규칙**:
- 확정 근거 마커: `(✓확정 2026-07-20 자재LOT재정의 #n)` — #n은 위 결정 번호. 근거 경로 `research/2026-07-20-자재LOT관리-재정의-확정기록.md`.
- 뒤집힌 서술: `(기존 서술 폐기: "…원문 요지…")` — **필수. 되돌아가지 않게 남긴다.**
- WF03 L160은 **역방향 특례**: v1.0에서 폐기했던 가설을 재정의가 되살리므로 `(기존 가설 부활·확정: …)`로 표기(재-폐기 방지).
- 원문 보존 대상(QA #1·#4 등 `✓확정 2026-07-04`)은 **고치지 않는다** — 재정의가 그 자재LOT 함의를 대체함을 문장 내 상호 참조로만 남긴다.

## §0-A. 변경이력·헤더 처리 원칙 (작성 담당자 필독 — 리더 앵커 지정 초과 판단)

**리더가 앵커로 지정한 WF03 L301 · WF06 L282 · WF04 L273은 전부 `v1.0`(또는 과거) 변경이력 행이다. 이들은 편집하지 않는다.**
- 사유: 변경이력 행은 각 문서가 그 버전에서 **무엇을 통합했는지의 사실 기록**이다. v1.0 당시 실제로 "IQC 합격 시점 생성"을 반영한 것은 참이므로, 이를 고치면 모델이 바뀐 궤적 자체가 지워진다(checklist §5 append-only·§4 원문 보존 정신).
- 대신 각 문서에 **신규 버전 행을 최상단에 추가**하고(§각 문서 지시), 재정의 반영을 기록한다. 구↔신 궤적이 변경이력 표에서 보이게 한다.

**헤더 stale 2건 동시 정정**(checklist §5 "헤더 버전 = 변경이력 최신 반드시 일치"):
- **WF03**: 헤더 L3 = `v1.2`이나 변경이력 최신 = `v1.3`(2026-07-15 PQC). 이번 bump 시 헤더를 **v1.4**로 정정(중간 누락분 해소).
- **도식스펙03**: 헤더 L3 = `v1.0`이나 변경이력 최신 = `v1.3`. 이번 bump 시 헤더를 **v1.4**로 정정.

**변경이력 신규 행 삽입 앵커(공통)**: 각 문서 끝 `## 변경 이력` 블록의 표 헤더 구분선 바로 아래(현재 최신 행 위)에 삽입. 권장 old_string:
```
## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
```
new_string = 위 블록 + 개행 + 신규 행. (블록이 정확히 매칭 안 되면 현재 최신 행을 old_string으로 잡아 그 위에 삽입.)

---

# G1. 통합분석 (판정 정본 · 최우선) — `research/2026-07-03-요구사항-기존기획-통합분석.md`

**버전**: v1.2 → **v1.3** · **성격**: 서술 교체 + 폐기 표기 · **본문 앵커 8** (리더 지정 4 + 확장 4). WF·도식스펙이 인용하는 상류 판정 정본이므로 **우선 실행**.

### G1-1 · L46 (§4 확정④ 헤더) — [확장]
OLD:
```
### 확정④ 자재LOT 발번·생성 2단계 (REQ-PR-0001 · 0007 — 충돌 아닌 핵심 미결 해소)
```
NEW:
```
### 확정④ 자재LOT 발번·등록/Release 모델 (REQ-PR-0001 · 0007 — 2026-07-20 재정의로 개정, 구 "생성 2단계" 폐기)
```

### G1-2 · L48 (핵심 단언) — [리더 지정]
OLD:
```
- **자재LOT 생성 시점 = IQC 합격** (0007, 미팅 확정 — '생성'이며 'Release'가 아님 → C4 해소). 발번 주체 = MES.
```
NEW:
```
- **자재LOT 등록 시점 = 입하 (Lot Status=Hold) · 재고 가용(Release) = IQC 합격** (0007 개정 — 2026-07-20 CREFLE 재정의: 기존 "IQC 합격 '생성'(Release 아님)·C4"를 뒤집어 정확히 Release 모델로 확정). 발번 주체 = **경로 이원화**(사전부착=공급사 번호 MES 수용·등록 / 직접부착=MES 발번). (✓확정 2026-07-20 자재LOT재정의 #4·#5·#7, 기존 서술 폐기: "IQC 합격이 '생성'이며 'Release' 아님 → C4 해소"·"발번 주체=MES 단일")
```

### G1-3 · L50 (라이프사이클) — [확장]
OLD:
```
- **판정: '번호 사전 발행' ≠ 'LOT 생성(활성)'의 2단계 모델.** 라이프사이클: 사전발행(선택) → 입하 → IQC → 합격 시 LOT 생성 → 분할(QR)·부분출고·잔량. A1 잔여 = 제품LOT 발번 주체.
```
NEW:
```
- **판정: 'LOT 번호 등록(존재)' 과 '재고 가용(Release)'의 분리 모델.** 라이프사이클: (사전부착=공급사 번호 수용 / 직접부착=MES 발번) → **입하 등록(Lot Status=Hold)** → IQC → **합격 시 Release(가용)** / 불합격 시 Hold 유지·반품 → 분할(QR)·부분출고·잔량. A1 잔여 = 제품LOT 발번 주체. (✓확정 2026-07-20 자재LOT재정의 #1·#5, 기존 서술 폐기: "'번호 사전 발행'≠'LOT 생성(활성)' 2단계·합격 시 LOT 생성")
```

### G1-4 · L51 (파생 보정 [의견]) — [리더 지정]
OLD (선두 `- ` 및 코드스팬 백틱 포함, 원문 그대로):
```
- `[의견]` 파생 보정: "IQC 미완 LOT=Hold" 규칙(WF03 S5)은 자재에는 성립하지 않는다 — 합격 전에는 LOT이 없으므로 '입하 대기(LOT 미생성)' 상태다. Hold 통제는 생산·제품LOT과 사전발행 번호 상태에 적용한다.
```
NEW:
```
- `[의견]` 파생 보정 (2026-07-20 재정의로 개정): "IQC 미완 LOT=Hold" 규칙(WF03 S5)은 **자재LOT에도 적용된다** — 자재LOT은 입하 시 등록되어 Lot Status=Hold이고 IQC 합격 시 Release된다. 불합격분은 Hold 유지 후 반품(재고 미가용). Hold 통제가 자재·생산·제품LOT 전반에 적용된다. (✓확정 2026-07-20 자재LOT재정의 #5, 기존 서술 폐기: "자재엔 Hold 미성립·합격 전 LOT 없음·'입하 대기(LOT 미생성)'")
```

### G1-5 · L67 (§4 판정표 A1 행) — [확장]
OLD:
```
| A1 | **부분 확정** — 자재LOT: MES 발번, 사전발행+IQC 합격 생성 2단계 | 0001·0007 | 제품LOT 발번 주체, ERP G/R 연계 시점 |
```
NEW:
```
| A1 | **부분 확정** — 자재LOT: 발번 경로 이원화(사전부착=공급사 수용/직접=MES), **입하 등록(Hold)·Release=IQC 합격** (2026-07-20 재정의 — 기존 "IQC 합격 생성 2단계" 폐기) | 0001·0007 개정 | 제품LOT 발번 주체, ERP G/R 연계 시점 |
```

### G1-6 · L74 (§4 판정표 C4 행) — [리더 지정]
OLD:
```
| C4 | **확정** — IQC 합격이 자재LOT을 '생성' (Release 아님) | 0007 | — |
```
NEW:
```
| C4 | **재확정(뒤집힘)** — IQC 합격 = 자재LOT **Release(가용 전이)**, 입하 시 이미 등록(Hold). 기존 "'생성'(Release 아님)" 폐기 (2026-07-20 재정의) | 0007 개정 | — |
```

### G1-7 · L95 (§5 매핑표 PR-0001 행 비고) — [확장]
OLD:
```
| PR-0001 | 자재LOT 번호 사전 발행(협력사 부착) | 미결해소(A1)+신규 | WF01 S2·S4, 모델 C3(사전발행 상태), 질문지 A1 | 0007과 2단계 정합 |
```
NEW:
```
| PR-0001 | 자재LOT 번호 사전 발행(협력사 부착) | 미결해소(A1)+신규 | WF01 S2·S4, 모델 C3(사전발행 상태), 질문지 A1 | 2026-07-20 재정의: 사전부착=공급사 번호 MES 수용·등록(입하 Hold) |
```

### G1-8 · L101 (§5 매핑표 PR-0007 행) — [리더 지정]
OLD:
```
| PR-0007 | **IQC 합격 시점 LOT 생성** (확정) | 미결해소(C4·A1) | WF01 S3·S4 ✅, WF03 S5 보정, 모델 C3·C4, 질문지 C4 | — |
```
NEW:
```
| PR-0007 | **개정(2026-07-20)**: "IQC 합격 시점 LOT 생성" → **"LOT 등록=입하(Hold)·Release=IQC 합격"** | 미결해소(C4·A1)+개정 | WF01 S3·S4 ✅, WF03 S5 보정, 모델 C3·C4, 질문지 C4 | 근거: research/2026-07-20-자재LOT관리-재정의-확정기록.md |
```

### G1-변경이력 (신규 v1.3 행 · 최상단 삽입)
NEW ROW:
```
| v1.3 | 2026-07-20 | **자재LOT 재정의 반영(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md).** §4 확정④·판정표(A1·C4)·§5 매핑표(PR-0001·0007): "IQC 합격 시점 생성·'생성'이며 Release 아님·C4 해소" → **"등록=입하(Lot Status Hold)·Release=IQC 합격, 발번 경로 이원화(사전부착=공급사 수용/직접=MES)"**로 개정. §4 파생 보정([의견]) "자재엔 Hold 미성립·LOT 미생성" → "자재LOT도 Hold 적용·불합격=Hold 유지·반품" 대체. |
```

### G1-헤더 (L3 버전 정정)
OLD:
```
> 정리일: 2026-07-03 (v1.2 — 2026-07-06 갱신) · 작성 주체: CREFLE OMF 팀
```
NEW:
```
> 정리일: 2026-07-03 (v1.3 — 2026-07-20 자재LOT 재정의 반영 / v1.2 2026-07-06 갱신) · 작성 주체: CREFLE OMF 팀
```

### G1 집계 재계수 확인
- §4 판정표·§5 매핑표는 **행 수·건수 불변**(내용 교체만). "47건 전수 매핑"·"국지 충돌 5건" 등 총계 표현 불변. 추가 재계수 없음.
- L146 "자재LOT 상태(+사전발행)"·L100 "`[의견]` IQC·LOT 생성과의 순서 확인 필요"(PR-0006 행 비고)는 **선택적**(no-change 권장) — '사전발행 상태'는 사전부착 번호 등록으로 여전히 성립, PR-0006은 거래명세서 입고 순서 [의견]으로 재정의 직접 대상 아님. 손대지 않는다.

---

# G2. WF02 생산실행 — `research/2026-07-03-워크플로우-요구사항반영-02-생산실행.md`

**버전**: v1.10 → **v1.11** · **성격**: 서술 교체 + 폐기 표기 · **본문 앵커 5** (리더 지정과 동일). 핵심: "존재하는 자재LOT=합격분" → **존재(Hold)≠가용, 투입 가능=Release분**.

### G2-1 · L150 (S6 Rule — 핵심)
OLD (부분 라인, 유일 매칭):
```
투입 자재LOT은 가용분이라야 투입 — **자재LOT은 IQC 합격 시점에 생성되므로 존재하는 자재LOT=합격분** — ✅ (REQ-PR-0007 요구).
```
NEW:
```
투입 자재LOT은 가용분(Lot Status=Release)이라야 투입 — **자재LOT은 입하 시점에 등록(Lot Status=Hold)되고 IQC 합격 시 Release되므로, 존재하는 자재LOT ≠ 합격분(Hold분 포함)이며 투입 가능분 = Release분(Lot Status 참조)** — ✅ (REQ-PR-0007 개정, 2026-07-20 재정의) (기존 서술 폐기: "IQC 합격 시점에 생성·존재하는 자재LOT=합격분").
```

### G2-2 · L153 (S6 Boundary)
OLD (부분 라인):
```
자재LOT **발번=MES·생성=IQC 합격** — ✅ (REQ-PR-0001·0007) — A1 완전 종결(LOT 3종 전부 MES 발번, ✓확정 2026-07-04 QA #1).
```
NEW:
```
자재LOT **발번=경로 이원화(사전부착=공급사 번호 MES 수용·등록 / 직접부착=MES 발번)·등록=입하(Lot Status=Hold)·Release=IQC 합격** — ✅ (REQ-PR-0001·0007, 2026-07-20 재정의) — 생산·제품LOT 발번=MES(A1, ✓확정 2026-07-04 QA #1 — 자재LOT 발번 함의는 재정의로 이원화) (기존 서술 폐기: "자재LOT 발번=MES·생성=IQC 합격·LOT 3종 전부 MES 발번").
```

### G2-3 · L154 (S6 Open-Issue A1)
OLD (부분 라인, "발번 주체=MES." 까지):
```
⚠ **A1 — 확정**: 자재LOT 번호 **사전 발행**(협력사 라벨 부착·납품, 입고 전 상태 관리 — ✅ REQ-PR-0001 요구) + **IQC 합격 시 LOT 생성(활성)** (✅ REQ-PR-0007 요구)의 2단계 구조, 발번 주체=MES.
```
NEW:
```
⚠ **A1 — 확정(2026-07-20 재정의)**: 자재LOT 번호 **등록**(사전부착=협력사 라벨 공급사 번호 MES 수용 / 직접부착=MES 발번, 입고 전 상태 관리 — ✅ REQ-PR-0001 요구) + **입하 등록=Lot Status Hold · IQC 합격 시 Release(가용)** (✅ REQ-PR-0007 개정)의 등록/Release 분리 구조, 발번 주체=경로 이원화. (기존 서술 폐기: "사전 발행 + IQC 합격 시 LOT 생성(활성) 2단계·발번 주체=MES 단일")
```

### G2-4 · L262 (요약매핑표 S6 행 — 우측 셀)
OLD (부분, 유일 매칭):
```
자재LOT=**IQC 합격 생성·MES 발번** ✅ REQ-PR-0001·0007 |
```
NEW:
```
자재LOT=**입하 등록(Hold)·Release=IQC 합격·발번 이원화(사전부착=공급사 수용/직접=MES)** ✅ REQ-PR-0001·0007(2026-07-20 재정의) |
```

### G2-5 · L270 (최우선 검증)
OLD (부분 라인):
```
→ **A1 완전 종결**(사전발행+IQC 합격 생성, LOT 3종 전부 MES 발번 — REQ-PR-0001·0007 + ✓확정 2026-07-04 QA #1).
```
NEW:
```
→ **A1 종결(2026-07-20 재정의)**(등록=입하 Hold·Release=IQC 합격·발번 이원화(사전부착=공급사 수용/직접=MES); 생산·제품LOT 발번=MES — REQ-PR-0001·0007 개정 + ✓확정 2026-07-04 QA #1) (기존 서술 폐기: "사전발행+IQC 합격 생성·LOT 3종 전부 MES 발번").
```

### G2-변경이력 (신규 v1.11 행 · 최상단 삽입)
NEW ROW:
```
| v1.11 | 2026-07-20 | **자재LOT 재정의 반영(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md).** S6 Rule: "IQC 합격 시점 생성·존재=합격분" → **입하 등록(Lot Status=Hold)·투입 가능=Release분(Lot Status 참조)**로 개정. S6 Boundary·Open-Issue(A1)·요약매핑(S6)·최우선 검증: 발번 경로 이원화(사전부착=공급사 수용/직접=MES)·등록=입하 Hold·Release=IQC 합격 반영. genealogy·투입 스캔 서술은 불변. |
```

### G2-헤더 (L3 버전 정정)
- 헤더 L3의 `v1.10`을 `v1.11`로 갱신. 형식: `최종 갱신: 2026-07-20 (v1.11 — 자재LOT 재정의 반영: S6 투입 자재LOT=입하 Hold·투입 가능=Release분 / v1.10 [4.포장] 결정 05 보정 …)`. 기존 v1.10 요지는 뒤에 유지.

### G2 집계 재계수 확인
- 요약매핑표 행 수 불변. S6 관련 서술만 교체. 추가 재계수 없음. L39·L149(genealogy·투입 스캔) 등은 모델 무관 — 손대지 않는다.

---

# G3. WF03 품질관리 — `research/2026-07-03-워크플로우-요구사항반영-03-품질관리.md`

**버전**: v1.3 → **v1.4** (헤더 v1.2 stale 동시 정정) · **성격**: 서술 교체 + 폐기 표기 + **폐기 가설 부활** · **본문 앵커 8** (리더 지정 7 + 확장 L242; 리더 지정 L301=변경이력 → 신규 행). 핵심: **L160 — v1.0에서 폐기했던 'IQC 미완 자재LOT이 Hold로 존재' 가설을 재정의가 부활·확정.**

### G3-1 · L33 (범위 OUT — IQC)
OLD:
```
- **수입검사(IQC)** — 자재 창고 관리 문서 **S3**에 작성(재정리본: `research/2026-07-03-워크플로우-요구사항반영-01-자재창고관리.md`). 본 문서는 *참조만* 하고 중복 작성하지 않는다(검사 3종 중 IQC 흐름·예외는 자재창고 S3 참조). **자재LOT은 IQC 합격 시점에 생성 — 미팅 확정** ✅ (REQ-PR-0007 요구). 경계로 맞닿는 지점은 🔗로 표시.
```
NEW:
```
- **수입검사(IQC)** — 자재 창고 관리 문서 **S3**에 작성(재정리본: `research/2026-07-03-워크플로우-요구사항반영-01-자재창고관리.md`). 본 문서는 *참조만* 하고 중복 작성하지 않는다(검사 3종 중 IQC 흐름·예외는 자재창고 S3 참조). **자재LOT은 입하 시점에 등록(Lot Status=Hold)되고 IQC 합격 시 Release — 2026-07-20 재정의** ✅ (REQ-PR-0007 개정) (기존 서술 폐기: "IQC 합격 시점에 생성"). 경계로 맞닿는 지점은 🔗로 표시.
```

### G3-2 · L41 (개요 도식 주석 — 코드블록 내, 선두 공백 1칸 포함)
OLD:
```
 (자재 입하분 IQC = 자재창고 S3, 본 문서 OUT·참조만 — 자재LOT은 IQC 합격 시점 생성 ✅ REQ-PR-0007 요구)
```
NEW:
```
 (자재 입하분 IQC = 자재창고 S3, 본 문서 OUT·참조만 — 자재LOT은 입하 등록(Hold)·IQC 합격 시 Release ✅ REQ-PR-0007 개정, 2026-07-20 재정의)
```

### G3-3 · L69 (요구사항 접점표 REQ-PR-0007 행)
OLD:
```
| REQ-PR-0007 | IQC 합격 시점 자재LOT 생성 확정 | S5 보정 · S8 |
```
NEW:
```
| REQ-PR-0007 | **개정(2026-07-20)**: 자재LOT 등록=입하(Hold)·Release=IQC 합격 (구 "IQC 합격 시점 생성" 폐기) | S5 보정 · S8 |
```

### G3-4 · L157 (S5 Input)
OLD:
```
- **Input(입력)**: 판정 결과(S4), 대상 LOT — **생산·제품 LOT** (자재LOT은 IQC 합격 시점에 생성되므로(✅ REQ-PR-0007 요구) 합격 전에는 통제 대상 LOT 자체가 없음 — 아래 Rule 보정 참조).
```
NEW:
```
- **Input(입력)**: 판정 결과(S4), 대상 LOT — **자재·생산·제품 LOT** (자재LOT도 입하 시 등록되어 Lot Status=Hold이고 IQC 합격 시 Release되므로 통제 대상 — 2026-07-20 재정의, 아래 Rule 참조) (기존 서술 폐기: "자재LOT은 IQC 합격 시점 생성·합격 전 통제 대상 LOT 없음").
```

### G3-5 · L160 (S5 Rule — 핵심 · 폐기 가설 부활)
OLD:
```
- **Rule(선행조건·규칙)**: 검사 미완 LOT = **Hold(기본)** — **적용 범위 보정** ✅: 이 규칙은 **생산·제품 LOT에 적용**된다. **자재는 IQC 합격 시점에 LOT이 생성되므로(REQ-PR-0007 요구 — 미팅 확정) 합격 전 자재는 'LOT이 존재하지 않는 입하 대기 상태'** — Hold 통제가 아니라 LOT 미생성으로 표현된다. `(기존 가설 폐기: 'IQC 검사 미완 자재LOT이 Hold 상태로 존재'한다는 전제)` **Release분만 출고/출하 가능**은 유지.
```
NEW:
```
- **Rule(선행조건·규칙)**: 검사 미완 LOT = **Hold(기본)** — **적용 범위 확정(2026-07-20 재정의)** ✅: 이 규칙은 **자재·생산·제품 LOT 전반에 적용**된다. **자재LOT은 입하 시점에 등록되어 Lot Status=Hold이고 IQC 합격 시 Release되므로, 합격 전 자재는 'LOT이 존재하지 않는 미생성'이 아니라 '등록된 Hold' 상태**다. 불합격분은 Hold 유지·반품(재고 미가용). `(기존 가설 부활·확정: 'IQC 검사 미완 자재LOT이 Hold 상태로 존재' — v1.0에서 폐기했던 이 전제를 2026-07-20 재정의가 정확히 되살려 확정, 재-폐기 금지)` **Release분만 출고/출하 가능**은 유지. (근거: research/2026-07-20-자재LOT관리-재정의-확정기록.md)
```

### G3-6 · L163 (S5 Boundary)
OLD (부분 라인):
```
자재LOT 생성 시점 경계는 자재창고 재정리본 S3·S4(IQC 합격 시 생성 — REQ-PR-0007).
```
NEW:
```
자재LOT 등록/Release 경계는 자재창고 재정리본 S2(입하 등록 Hold)·S3(IQC 합격 Release)(REQ-PR-0007 개정, 2026-07-20 재정의) (기존 서술 폐기: "IQC 합격 시 생성·S3·S4").
```

### G3-7 · L242 (요약매핑표 S5 행 — 우측 통제 셀) — [확장]
OLD (부분, 유일 매칭 — 셀 문구만 교체):
```
물류통제(Hold/Release/보류) — 자재LOT 비적용 보정
```
NEW:
```
물류통제(Hold/Release/보류) — 자재LOT도 적용(입하 등록 Hold·IQC 합격 Release, 2026-07-20 재정의)
```

### G3-8 · L250 (검사 3종 통합)
OLD (부분 라인):
```
자재LOT은 IQC 합격 시점 생성(✅ REQ-PR-0007 요구)이므로 IQC 검사 대상 식별(LOT 생성 전 입하 단위)은 자재창고 재정리본에서 다룬다.
```
NEW:
```
자재LOT은 입하 등록(Hold)·IQC 합격 시 Release(✅ REQ-PR-0007 개정, 2026-07-20 재정의)이므로 IQC 검사 대상 식별(입하 등록된 Hold LOT 단위)은 자재창고 재정리본에서 다룬다 (기존 서술 폐기: "IQC 합격 시점 생성·LOT 생성 전 입하 단위").
```

### G3-변경이력 (신규 v1.4 행 · 최상단 삽입 — L301 v1.0 행은 **불변**)
NEW ROW:
```
| v1.4 | 2026-07-20 | **자재LOT 재정의 반영(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md).** S5 Rule 개정: "검사 미완 Hold=생산·제품LOT 한정·자재는 LOT 미생성" → **자재LOT도 입하 시 등록(Lot Status=Hold)·IQC 합격 시 Release**로 확정 — **v1.0에서 폐기했던 'IQC 미완 자재LOT이 Hold로 존재' 전제를 재정의가 부활·확정**. S5 Input·Boundary·범위(OUT)·개요도 주석·접점표(REQ-PR-0007 개정)·요약매핑(S5 자재LOT 적용)·§검사3종통합 동기 갱신. 불합격=Hold 유지·반품(≠미생성) 일괄 반영. 헤더 버전 v1.2→v1.4 정정(v1.3 반영 누락분 포함). |
```

### G3-헤더 (L3 버전 정정 — stale 해소)
OLD:
```
> 정리일: 2026-07-03 · 갱신: 2026-07-06 (v1.2 — 내부 설계 결정서 확정분 6건(결정 01·06·09·10·11·12) 반영) · 작성 주체: CREFLE OMF 팀
```
NEW:
```
> 정리일: 2026-07-03 · 갱신: 2026-07-20 (v1.4 — 자재LOT 재정의: S5 자재LOT도 입하 Hold·IQC 합격 Release / v1.3 2026-07-15 PQC 검사방식 / v1.2 2026-07-06 설계결정서 6건) · 작성 주체: CREFLE OMF 팀
```

### G3 집계 재계수 확인
- 접점표·요약매핑표 행 수 불변. L301(v1.0 변경이력)·L298~L300(v1.1~v1.3)은 **불변**. 추가 재계수 없음.

---

# G4. WF06 기준정보연계 — `research/2026-07-03-워크플로우-요구사항반영-06-기준정보연계.md`

**버전**: v1.3 → **v1.4** · **성격**: 서술 교체 + 폐기 표기 · **본문 앵커 3** (리더 지정 L143·L91 + 확장 L85; 리더 지정 L282=변경이력 → 신규 행). 핵심: **L143 "입하 시점 선(先)생성이 아니다" → 정확히 입하 시점 선(先)등록**.

### G4-1 · L143 (S1 Boundary — 핵심)
OLD (부분 라인, 유일 매칭):
```
자재LOT 생성 vs Release(C4) → **확정: IQC 합격이 자재LOT '생성'의 선행조건 — 입하 시점 선(先)생성이 아니다** (REQ-PR-0007 요구, C4 해소. 상세 흐름은 자재창고·품질 문서 소관).
```
NEW:
```
자재LOT 등록 vs Release(C4) → **개정(2026-07-20 재정의): 자재LOT은 입하 시점에 선(先)등록(Lot Status=Hold)되고, IQC 합격은 '생성'이 아니라 Release(가용 전이)의 선행조건** (REQ-PR-0007 개정, C4 재확정. 상세 흐름은 자재창고·품질 문서 소관) (기존 서술 폐기: "IQC 합격이 자재LOT '생성'의 선행조건·입하 시점 선생성이 아니다").
```

### G4-2 · L91 (S1 Open-Issue A1)
OLD (부분 라인, "(기존 서술 폐기: 제품LOT 발번 주체·G/R 시점 잔여)." 까지):
```
⚠ **A1 완전 확정** — 자재LOT 2단계 모델('LOT 번호 사전 발행'(REQ-PR-0001 요구)+'LOT 생성=IQC 합격 시점'(REQ-PR-0007 요구))에 더해 **제품LOT 발번 주체=MES(LOT 3종 전부 MES 발번) · ERP G/R=IQC 합격 직후 건별 즉시 연계 확정**(✓확정 2026-07-04 QA #1 — §8.A.1 종결) (기존 서술 폐기: 제품LOT 발번 주체·G/R 시점 잔여).
```
NEW:
```
⚠ **A1 완전 확정(2026-07-20 재정의)** — 자재LOT 등록/Release 모델(사전부착=공급사 번호 MES 수용·등록 / 직접부착=MES 발번, **등록=입하 Lot Status Hold · Release=IQC 합격** — REQ-PR-0001·0007 개정)에 더해 **생산·제품LOT 발번 주체=MES · ERP G/R=IQC 합격 직후 건별 즉시 연계 확정**(✓확정 2026-07-04 QA #1 — §8.A.1 종결, 자재LOT 발번 함의는 재정의로 이원화) (기존 서술 폐기: "자재LOT 2단계 모델·LOT 생성=IQC 합격 시점·LOT 3종 전부 MES 발번").
```
※ ERP G/R="IQC 합격 직후"는 신모델의 Release(=합격) 시점과 정합 → 유지.

### G4-3 · L85 (S1 Medium 매체 — 괄호 주석) — [확장]
OLD (부분, 괄호 주석만 교체):
```
(자재LOT 발번은 MES로 확정됐으나 품목 채번 주체는 별개 — 아래 Open-Issue)
```
NEW:
```
(자재LOT 발번은 직접부착=MES·사전부착=공급사 번호 수용으로 이원화 확정(2026-07-20 재정의)이나 품목 채번 주체는 별개 — 아래 Open-Issue)
```

### G4-변경이력 (신규 v1.4 행 · 최상단 삽입 — L282 v1.0 행은 **불변**)
NEW ROW:
```
| v1.4 | 2026-07-20 | **자재LOT 재정의 반영(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md).** S1 Boundary 개정: "IQC 합격이 자재LOT '생성'의 선행조건·입하 시점 선(先)생성이 아니다" → **자재LOT은 입하 시점 선(先)등록(Lot Status=Hold)·IQC 합격=Release의 선행조건**(C4 재확정). S1 Open-Issue(A1)·Medium: 자재LOT 발번 경로 이원화(사전부착=공급사 수용/직접=MES)·등록=입하 Hold·Release=IQC 합격 반영. |
```

### G4-헤더 (L3 버전 정정)
OLD:
```
> 정리일: 2026-07-03 · 갱신 2026-07-07 (v1.3 — 러닝체인지 분할 구조 QA #37 반영) · 작성 주체: CREFLE OMF 팀
```
NEW:
```
> 정리일: 2026-07-03 · 갱신 2026-07-20 (v1.4 — 자재LOT 재정의: S1 자재LOT 등록=입하 Hold·Release=IQC 합격·발번 이원화 / v1.3 2026-07-07 러닝체인지 분할 QA #37) · 작성 주체: CREFLE OMF 팀
```

### G4 집계 재계수 확인
- 수신 연계대상·송신 스펙 목록 불변(품목·BOM·G/R 등). G/R="IQC 합격 직후" 서술은 정합 유지. 추가 재계수 없음.

---

# G5. WF04 제품출하 — `research/2026-07-03-워크플로우-요구사항반영-04-제품출하.md`

**버전**: v1.2 → **v1.3** · **성격**: 인용 baseline 갱신 (자재 측 원칙을 논거로 인용하는 stale 지점) · **본문 앵커 3** (리더 지정 L39·L67·L71; 리더 지정 L273=변경이력 → 신규 행). **주의: 외주 제품LOT 적용 여부 [확인]은 본 재정의로 해소되지 않음 — [확인] 마커 그대로 보존.**

### G5-1 · L39 (핵심 결정 반영 bullet — 자재 baseline)
OLD (부분, 유일 매칭):
```
자재LOT 측은 MES 발번·IQC 합격 시점 생성으로 기확정(REQ-PR-0001·0007 요구).
```
NEW:
```
자재LOT 측은 **입하 등록(Lot Status=Hold)·IQC 합격 시 Release·발번 이원화(사전부착=공급사 수용/직접=MES)**로 재확정(REQ-PR-0001·0007 개정, 2026-07-20 재정의) (기존 서술 폐기: "자재LOT=MES 발번·IQC 합격 시점 생성").
```
※ 같은 문장 뒤의 제품LOT 관련 `(기존 서술 폐기: 원본 p.57·아키텍처…)`는 불변.

### G5-2 · L67 (Exception ② 외주품 IQC — 자재 baseline 인용)
OLD (부분):
```
② **외주품 IQC 미완 입고**(긴급) → 검사 전 가입고? — ✅ 자재 측 결정: **IQC 합격 시점 LOT 생성**(REQ-PR-0007 요구, 미팅 확정) — 외주 *제품*입고·제품LOT에 동일 원칙이 적용되는지 [확인]
```
NEW:
```
② **외주품 IQC 미완 입고**(긴급) → 검사 전 가입고? — ✅ 자재 측 결정(2026-07-20 재정의): **자재LOT은 입하 등록(Lot Status=Hold)·IQC 합격 시 Release**(REQ-PR-0007 개정) — 외주 *제품*입고·제품LOT에 동일 원칙(입고 시 등록 Hold·검사 합격 시 Release)이 적용되는지 [확인] (기존 서술 폐기: 자재 측 "IQC 합격 시점 LOT 생성")
```
※ 말미 `[확인]` **보존** — 외주 제품LOT 적용 여부는 미해소 잔존.

### G5-3 · L71 ([Check] — 자재 baseline 인용)
OLD (부분):
```
외주 제품입고 IQC가 *언제* 걸리나(입하 vs 제품입고) — 자재 측 원칙(IQC 합격 시 LOT 생성, REQ-PR-0007 요구)의 외주 제품 적용 여부.
```
NEW:
```
외주 제품입고 IQC가 *언제* 걸리나(입하 vs 제품입고) — 자재 측 원칙(입하 등록 Hold·IQC 합격 시 Release, REQ-PR-0007 개정 2026-07-20 재정의)의 외주 제품 적용 여부.
```

### G5-변경이력 (신규 v1.3 행 · 최상단 삽입 — L273 v1.0 행은 **불변**)
NEW ROW:
```
| v1.3 | 2026-07-20 | **자재LOT 재정의 baseline 갱신(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md).** 외주 제품LOT 미결 논거로 인용한 자재 측 baseline을 "IQC 합격 시점 LOT 생성" → **"입하 등록(Lot Status=Hold)·IQC 합격 시 Release·발번 이원화"**로 갱신(§핵심결정 반영·S1 Exception②·[Check]). 외주 제품LOT 적용 여부 [확인]은 미해소 유지(본 재정의는 자재 baseline만 갱신). |
```

### G5-헤더 (L3 버전 정정)
OLD:
```
> 정리일: 2026-07-03 · 갱신 2026-07-06 (v1.2 — 내부 설계 결정서 확정 반영: 결정 05(+보정)·13·10) · 작성 주체: CREFLE OMF 팀
```
NEW:
```
> 정리일: 2026-07-03 · 갱신 2026-07-20 (v1.3 — 자재LOT 재정의 baseline 갱신: 외주 제품LOT 논거의 자재 측 원칙=입하 등록 Hold·IQC 합격 Release / v1.2 2026-07-06 설계결정서 05·13·10) · 작성 주체: CREFLE OMF 팀
```

### G5 집계 재계수 확인
- 예외 카탈로그 행 수 불변. 인용 baseline 3곳만 교체. 추가 재계수 없음.

---

# G6. 도식스펙03 품질관리 — `research/2026-07-06-도식스펙-03-품질관리.md`  ⚠ **figjam 선행 게이트**

**버전**: v1.3(변경이력)/v1.0(헤더 stale) → **v1.4** · **성격**: 캔버스 소급 전사물 갱신 · **앵커 4** (L37·L396=스펙층 / **L242·L295=캔버스 텍스트 노드**).

## ⚠ 실행 순서 (게이트 사유)
L242(좌하단 IQC 경계 스티키)·L295(사이드 B 규칙 텍스트)는 **캔버스 도형 텍스트의 소급 전사**다. 스펙만 신모델로 고치면 (a) 스펙≠캔버스(소급 전사 원칙 위반), (b) 도식스펙03 내부 불일치(L37 신모델 산문 vs L242/L295 구모델 전사)가 발생한다. 따라서 **문서 전체를 다음 순서로 일괄 실행**한다:

1. **figjam-workflow-team 선행** — 캔버스 v2 페이지의 아래 2개 텍스트 노드를 신모델로 갱신:
   - **좌하단 IQC 경계 스티키**(N3 회색 화살표 연결): `자재LOT은 IQC 합격 시점 생성` → `자재LOT은 입하 등록(Hold)·IQC 합격 시 Release`
   - **사이드 B 규칙 텍스트 노드**: `자재는 IQC 합격 전 LOT 미생성(01 도식)` → `자재는 입하 등록 시 Hold·IQC 합격 시 Release(01 도식)`(+ "검사 미완 생산·제품 LOT=Hold" → "검사 미완 LOT=Hold(자재·생산·제품)")
2. **리더 실측 맵 제공** — 갱신된 캔버스 스티키/규칙 노드의 실제 텍스트를 리더가 read-only 실측해 전달.
3. **doc-team 전사·정합 갱신** — 아래 G6-1~G6-4 일괄 실행 + 변경이력·헤더.

> 나머지 5문서는 이 게이트와 무관하게 즉시 실행 가능. 도식스펙03만 위 1~2 완료 후 실행.

### G6-1 · L37 (§1 IQC 제외 근거 — 스펙 산문, 캔버스 비전사)
OLD:
```
- **IQC 제외 근거**: WF03 v1.2 범위(OUT) — "수입검사(IQC)는 자재 창고 관리 문서 S3에 작성… 본 문서는 참조만 하고 중복 작성하지 않는다". 자재LOT은 IQC 합격 시점 생성이므로(REQ-PR-0007) IQC 검사 대상 식별도 01 도식 소관.
```
NEW:
```
- **IQC 제외 근거**: WF03 범위(OUT) — "수입검사(IQC)는 자재 창고 관리 문서 S3에 작성… 본 문서는 참조만 하고 중복 작성하지 않는다". 자재LOT은 입하 등록(Lot Status=Hold)·IQC 합격 시 Release이므로(REQ-PR-0007 개정, 2026-07-20 재정의) IQC 검사 대상 식별도 01 도식 소관 (기존 서술 폐기: "IQC 합격 시점 생성").
```

### G6-2 · L242 (좌하단 IQC 경계 스티키 전사) — ⚠ 캔버스 노드 (figjam 선행)
OLD (표 셀 내 백틱 스티키 텍스트 전사):
```
| 좌하단 | `IQC 상세 = 01.자재창고 도식 (S3) — 자재LOT은 IQC 합격 시점 생성. 검사 기록 구조(3계층)·Lot Status는 본 도식과 공통(단일 검사실적+검사유형 코드)` → N3로 회색 화살표 |
```
NEW (figjam이 캔버스 스티키를 갱신한 실측 텍스트로 확정 후 전사):
```
| 좌하단 | `IQC 상세 = 01.자재창고 도식 (S3) — 자재LOT은 입하 등록(Hold)·IQC 합격 시 Release. 검사 기록 구조(3계층)·Lot Status는 본 도식과 공통(단일 검사실적+검사유형 코드)` → N3로 회색 화살표 |
```

### G6-3 · L295 (사이드 B 규칙 텍스트 노드 전사) — ⚠ 캔버스 노드 (figjam 선행)
OLD:
```
- **규칙 텍스트**: "검사 미완 생산·제품 LOT=Hold 기본 / 자재는 IQC 합격 전 LOT 미생성(01 도식) / Release분만 출고·출하 가능".
```
NEW (figjam 캔버스 규칙 노드 갱신 실측 후 전사):
```
- **규칙 텍스트**: "검사 미완 LOT=Hold 기본(자재·생산·제품) / 자재는 입하 등록 시 Hold·IQC 합격 시 Release(01 도식) / Release분만 출고·출하 가능".
```

### G6-4 · L396 (§3 근거 매핑 행 — 스펙층, 캔버스 비전사; G6-3과 정합)
OLD:
```
| 사이드 B 규칙(검사 미완=Hold 기본·자재LOT 비적용) | WF03 S5 Rule 보정 · REQ-PR-0007(IQC 합격 시점 생성) |
```
NEW:
```
| 사이드 B 규칙(검사 미완=Hold 기본·자재LOT도 적용) | WF03 S5 Rule(2026-07-20 재정의) · REQ-PR-0007 개정(자재LOT 등록=입하 Hold·Release=IQC 합격) · research/2026-07-20-자재LOT관리-재정의-확정기록.md |
```

### G6-변경이력 (신규 v1.4 행 · 최상단 삽입)
NEW ROW:
```
| v1.4 | 2026-07-20 | **자재LOT 재정의 반영(✓확정 2026-07-20 — research/2026-07-20-자재LOT관리-재정의-확정기록.md, figjam 캔버스 선행 갱신 후 전사).** 사이드 B 규칙 텍스트·좌하단 IQC 경계 스티키(캔버스 노드)·§1 IQC 제외 근거·§3 근거 매핑을 "자재는 IQC 합격 전 LOT 미생성" → **"자재LOT은 입하 등록 시 Hold·IQC 합격 시 Release"**로 갱신. 헤더 버전 v1.0→v1.4 정정(기존 헤더/변경이력 불일치 해소). |
```

### G6-헤더 (L3 버전 정정 — v1.0 stale 해소)
OLD:
```
> 작성일: 2026-07-06 (v1.0) · 작성 주체: CREFLE OMF 팀
```
NEW:
```
> 작성일: 2026-07-06 (v1.4 — 2026-07-20 자재LOT 재정의 반영; v1.1~v1.3 변경이력 참조) · 작성 주체: CREFLE OMF 팀
```

### G6 집계 재계수 확인
- 미정의 마커 수·상태 박스 수 등 불변(자재 규칙 문구만 교체). §3 근거 매핑 행 수 불변. 추가 재계수 없음.

---

## §9. 파급 대상 전체 목록 (실측 · 확정기록 v1.1 §4 대조)

| # | 문서 | 이번 r2 | 상태 |
| --- | --- | --- | --- |
| 1 | **통합분석**(판정 정본) | G1 — 8앵커+헤더+이력 | **CLEAR — 즉시** |
| 2 | WF02 생산실행 | G2 — 5앵커+헤더+이력 | **CLEAR — 즉시** |
| 3 | WF03 품질관리 | G3 — 8앵커+헤더+이력(폐기 가설 부활·헤더 stale 정정) | **CLEAR — 즉시** |
| 4 | WF06 기준정보연계 | G4 — 3앵커+헤더+이력 | **CLEAR — 즉시** |
| 5 | WF04 제품출하 | G5 — 3앵커+헤더+이력(외주 [확인] 보존) | **CLEAR — 즉시** |
| 6 | 도식스펙03 품질 | G6 — 4앵커(2 캔버스)+헤더+이력 | **figjam 선행 게이트** — 지시서 준비완료 |
| — | 명세서·개념모델·WF01 (확정기록 §4 #1~3) | r1 §5 초안 별건 | **r2 범위 밖** — 리더 위임분 |
| — | 도식스펙01 (확정기록 §4 #10, A2 재동기화) | figjam 캔버스 선행 | **r2 범위 밖** — 별도 run |
| — | 파생 HTML 3종·gen_coderef.py | 원본 갱신 후 재생성 | 후속(손편집 아님) |
| — | QA확정기록(2026-07-04)·설계결정서(2026-07-06) | **원문 보존 — 수정 금지** | 재정의가 함의 대체함을 상호 참조만 |

**원문 보존 확인**: G2·G4에서 인용하는 `✓확정 2026-07-04 QA #1`(LOT 3종 MES 발번·G/R=IQC 직후)은 **고치지 않는다**. 재정의가 자재LOT 발번 함의만 이원화함을 각 문장 내 상호 참조로 남긴다(§0 마커 규칙).

## §10. figjam 선행 필요 문서

- **도식스펙03만 해당** (6문서 중 1). 캔버스 텍스트 노드 2개(좌하단 IQC 경계 스티키·사이드 B 규칙 텍스트)가 재정의로 거짓이 됨 → figjam-workflow-team이 캔버스 갱신 → 리더 실측 맵 → doc-team 전사(G6-1~4 일괄).
- 나머지 5문서(통합분석·WF02·WF03·WF06·WF04)는 순수 텍스트 정본 — **캔버스 의존 없음, 즉시 실행 가능**.

## §11. 최종 판정

- **CLEAR (즉시 작성 발행)**: G1 통합분석 → G2 WF02 → G3 WF03 → G4 WF06 → G5 WF04. **통합분석(판정 정본) 우선**. 각 문서 앵커·신규 텍스트·마커·버전 bump·변경이력 행·집계 재계수 지점 위에 확정.
- **figjam 선행 게이트**: G6 도식스펙03 — 지시서 내용 준비완료, figjam 캔버스 갱신 + 리더 실측 맵 확보 후 일괄 실행.
- **신규 사용자 결정 불요**: 재정의는 확정기록 v1.1로 전량 확정. 잔여 미결(공급사 성분 코드 자릿수 [확인필요]·외주 제품LOT 적용 [확인])은 본 6문서 편집이 **건드리지 않고 그대로 보존**(신규 창작 없음).

### 작성 담당자 주의 (재확인)
1. **변경이력 v1.0 행(WF03 L301·WF06 L282·WF04 L273) 편집 금지** — 신규 버전 행만 최상단 추가.
2. **헤더 stale 2건(WF03 v1.2·도식스펙03 v1.0)** 은 새 버전으로 정정하며 중간 버전 참조 병기.
3. **원문 보존 마커(✓확정 2026-07-04 QA #1 등) 미변경** — 상호 참조만.
4. 앵커 old_string이 **정확히 매칭 안 되면 멈추고 보고**(지시서 밖 창작 금지). 특히 부분-라인 앵커(G2-1·G2-4·G3-6·G3-7·G4-1·G4-3·G5 전체)는 인용 구간만 교체.
5. G6은 figjam 선행 확인 전 **실행 금지**.
