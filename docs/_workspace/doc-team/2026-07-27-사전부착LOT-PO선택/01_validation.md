# 01 정합성 검증 — 사전부착 LOT → 수신 P/O 후보 선택 (이슈 #16)

> 검증일: 2026-07-27 · 담당: doc-validator(Opus 4.8·읽기 전용) · 모드: **게이트 모드(Flow B)**
> 대상 결정: GitHub #16 [uiux→docs] — 협력사 사전부착 자재 LOT 스캔 → LOT No 파싱({제품코드→품목}·{공급사 성분→거래처}) → 수신 P/O(REQ-PR-0024) 중 "해당 품목×공급사×미마감" 후보 자동 필터 → 담당자 열람·선택. #6(담당자 P/O 확인) 유지·정제.
> 근거: 사용자 확정 2026-07-25~27 세션(고정폭 34자리 9/9/6/6/4·공급사 자체 번호=external_identifier optional·정제이지 뒤집기 아님). worktree docs/prebind-lot-po-select.
> **판정: BLOCK** — 정합성 충돌 0건이나 **추가 질문 1건(B 도식스펙-01 배치)** + 확인 권고 2건. (A)(C)(D)는 정합성 CLEAR·앵커 확보(§5 초안 준비). (B) 배치 결정 후 발행.

---

## §1. 정합성 5단계 판정 (충돌 판정)

**결론: 하드 충돌 0건.** 5개 축 전부 "정제/가산"이며 기존 확정을 뒤집지 않는다. file:line 근거 아래.

### (a) #16 P/O 후보 필터 ↔ #6(담당자 확정)·재정의 모델 — 정합 (뒤집기 아님)
- 재정의 #6(`research/2026-07-20-자재LOT관리-재정의-확정기록.md:27`): "번호만으론 어떤 P/O 물품인지 확정 불가 → 입하 등록 시 담당자가 P/O 확인."
- #16은 담당자 확인 **앞단에 자동 후보 필터(파싱 기반)를 신설**하고 **담당자 최종 선택은 그대로 유지**. 파싱은 품목·거래처로 후보를 **좁힐 뿐 P/O를 유일 확정하지 않음**(동일 품목×공급사 미마감 P/O 복수 가능) → #6의 "번호만으론 확정 불가"가 **여전히 성립**. → 정제(pre-step 추가), 뒤집기 아님. **정합.**

### (b) LOT No 파싱 기반 ↔ 데이터모델링(lot_no / external_identifier) 실측 — 정합
- `research/2026-07-23-데이터모델링/3-2 논리적 모델링.md:1267` `lot.lot_no` = **"내부 LOT 번호"**, UK1. 물리(`mes_postgresql_physical_model.sql:1043`) `lot_no app.business_no_t` = **varchar(100)**(도메인 정의 `:52`) → **34자리 수용 가능**. UNIQUE(plant_id, lot_no)(`:1283`).
- `3-2 논리적 모델링.md:1292` `lot_external_identifier` = **"공급사·ERP·고객·외주처 LOT를 연결"**, `external_identifier` varchar(150)·`partner_id` Nullable·`identifier_type_code`. → 공급사 **자유 번호 별도·optional** 수용처. task 매핑(lot_no=내부 구조화 / external_identifier=공급사 자유 번호 별도)과 **일치**.
- **실측 추가 발견(정합·정밀화 필요)**: 데이터모델은 공급사 번호를 **3층**으로 구분 — ① `lot.lot_no`(내부 구조화) ② `lot_external_identifier.external_identifier`(외부 식별자 연결) ③ **`supplier_lot_no`**(`asn_line:1074`·`inbound_receipt_line:1124`, varchar(100)) + **`supplier_lot_missing`**(`:1125` — optional 확증) + `substitute_lot_reason_code`. task의 (C) 계획은 ②만 언급 → **③을 누락하면 개념모델 주석이 "external_identifier가 유일 수용처"로 과대주장**될 수 있음(정밀화 권고 §4-3, 충돌 아님).
- 데이터모델링 전역에 **"사전부착" 언급 0건**(grep 실측) → #16 로직과 **충돌할 기존 서술 없음**. (C) 브릿지는 순수 가산.
- **정합** (사용자 #4 "이 분리는 정상 설계·정합"과 실측 일치).

### (c) 재정의 #4 문구 정제 ↔ 기존 확정 — 정합 (뒤집기 아님)
- 재정의 #4(`:25`): "사전부착 = 공급사 번호를 MES가 수용·등록." 이 문구는 번호가 **자유형식이라고 단언하지 않음**. 반면 #2(`:23`)·#3(`:24`)은 이미 **{제품코드}+{수량}+{날짜}+{번호} 구성 + 공급사 성분 포함 + 등록 시 유일성 검증**(=MES 양식 구조)을 요구.
- 즉 **원 재정의 내부에 잠재 긴장**(#4의 "공급사 번호"가 자유형식으로 읽힐 여지 vs #2/#3의 MES 양식 구성)이 존재. #16 정제("사전부착=공급사가 MES 양식대로 부여")는 **#4를 #2/#3와 정합하도록 명확화** → 정제이지 뒤집기 아님. `(기존 서술 폐기)` 대상 아님(단, 정제임을 밝히는 주석 권장 — §5-D).
- **인용 사슬 무모순 확인**: "공급사 번호 수용·등록" 문구는 약 10개 문서에 퍼져 있고(§2) **전부 §재정의 #4를 인용**. #4를 in-place 정제하면 인용처가 정제된 의미를 자동 승계. 하류 10문서 어디에도 "자유형식/MES 양식 무관/상이한 자릿수" 단언이 **없음**(grep 실측) → 4문서(A~D) 밖 추가 수정 불요. **정합.**

### (d) 고정폭 34자리(9/9/6/6/4) ↔ 기존 서술 — 정합
- 배치 = 제품코드9[1-9]·수량 스냅샷9[10-18]·날짜6 YYMMDD[19-24]·공급사 성분6[25-30]·번호4[31-34] = 34.
- 재정의 **§3**(`:36`) 5분절 순서 `{제품코드}+{수량 스냅샷}+{날짜}+{공급사 성분}+{번호}`와 **정확히 일치**. 개념모델 자재LOT 행(`개념데이터모델-v2:206`)의 구성 서술도 동일 순서.
- **[확인필요] 해소 위치 2곳뿐**: 재정의 §5(`:67` "공급사 성분 코드 체계·자릿수(고정폭)…[확인필요]") + 개념모델(`:206` "공급사 성분 코드 체계·자릿수는 [확인필요](상세 설계)"). 둘 다 (D)(C) 스코프 내 → **미해소 [확인필요] 잔존 없음**.
- 기존에 **자재 LOT No 자릿수/포맷을 다르게 못박은 서술 0건**(데이터모델링 2-5 LOT추적성·CHANGELOG·개념모델 grep 실측) → 충돌 없음.
- **주의(정밀)**: 재정의 **#2**(`:23`)는 4분절 `{제품코드}+{수량}+{날짜}+{번호}`(공급사 성분 미포함, #3이 별도 삽입). 34자리 확정은 **§3의 5분절 순서** 기준 — 작성 시 §5 기록도 §3 순서(공급사 성분 before 번호)로 통일할 것. **정합.**
- **주의(품목코드)**: `item_code`=`code_t`=varchar(50)(`:365`·`:46`)이며 영숫자 가능. LOT No 내 "제품코드 9자리 숫자"는 item_code 전체와 **동일 보장 아님** — 파싱→품목 매핑은 채번 설계(별도 후속)의 도출 규칙. 34자리는 **확정 입력값**이므로 이 매핑 상세는 미해결로 남겨도 무방(task 명시).

### (e) REQ-PR-0024(수신 P/O)·REQ-PR-0001/0007 재해석 — 정합
- REQ-PR-0024 문언(명세서 `:66`)은 "P/O 변경→W/O 변경"이나, **D10 확정("P/O=ERP 발행·MES 수신")의 근거 REQ이기도** 함. 기존 사용례 일치: WF01 S1(`워크플로우-01:60` "P/O 존재…✅ REQ-PR-0024"), 도식스펙-01(`:304` "P/O 목록에서 선택 (219:4519) | …REQ-PR-0024"). → #16의 "수신 P/O(REQ-PR-0024)" 근거 표기는 **기존 관행과 정합**.
- 후보 필터 필드 지원 확인: P/O는 **품목·공급사·발주잔량** 보유(`개념데이터모델-v2:131`·`ERP-MES-수신정보:51`) → "품목×공급사×미마감(발주잔량>0)" 필터 **데이터 공백 없음**.
- REQ-PR-0001 재해석·0007 개정은 재정의 #7(`:28`)에 기확정 — #16은 그 위 정제. **정합.**

---

## §2. 파급 지형 실측 (repo 전역 grep)

- **"사전부착/공급사 번호 수용" 문구 보유 문서(≈10)**: 도식스펙-01, WF01, WF02(`:153·154·262·270·322`), WF06(`:85·91·279`), WF04(`:39`), 통합분석(`:48·50·67·95·181`), 명세서(`:122`), 하드웨어스펙(`:138`), 개념모델(다수). **전부 §재정의 #4 인용** → #4 in-place 정제로 승계, 추가 수정 불요(§1-c).
- **`external_identifier` 보유 문서**: 데이터모델링 3종(`3-2 논리적 모델링`·`mes_postgresql_physical_model.sql`·`mes_erd.md`)뿐. 기획 문서에 "공급사 자체 번호" 개념 부재 → (C)가 개념모델에 **신규 도입**(가산·무충돌).
- **P/O 후보 필터 개념**: 신규. 현재 어느 문서에도 없음 → 파급은 **(A)(B)(C)(D) 4문서로 국한**. task 4문서 계획과 일치.
- **이번 결정 파급 대상 = (A)WF01 · (B)도식스펙-01 · (C)개념모델 · (D)재정의**. 그 외 파생 HTML·gen_coderef.py는 원본 갱신 후 재생성(후속). QA확정기록·설계결정서는 원문 보존(무수정).

---

## §3. 정본 앵커·버전 실측 (작성자 인계용)

| 문서 | 현재 버전(헤더) | 변경이력 표 최신행 | bump 목표 | 비고 |
| --- | --- | --- | --- | --- |
| (A) `research/2026-07-03-워크플로우-요구사항반영-01-자재창고관리.md` | v1.5 (2026-07-20) | v1.5 @ `:304`(신순-first) | **v1.6** | S2=`:71`, S3 Rule=`:90`, S4 Action=`:101` |
| (B) `research/2026-07-07-도식스펙-01-자재창고관리.md` | **v2.11** (2026-07-24) | 표는 v2.0 @ `:433`이 최신 — **v2.1~v2.11은 헤더 blockquote(`:3`) inline** | **v2.12(헤더 inline)** | 버전 이력은 헤더 `:3`에 기록하는 관행. §3.1 노드표 `:304`·§5 `:396` |
| (C) `research/2026-07-03-개념데이터모델-v2-요구사항통합.md` | v2.8 (2026-07-20) | v2.8 @ `:624`(신순-first) | **v2.9** | 자재LOT 행 단일 = `:206` |
| (D) `research/2026-07-20-자재LOT관리-재정의-확정기록.md` | v1.1 (2026-07-20) | v1.1 @ `:72`(신순-first) | **v1.2** | #4=`:25`, §5 잔여확인=`:67` |

> ※ (A)(C)(D) 변경이력 표는 **신순-first**(새 행을 헤더 바로 아래 삽입). (B)는 표가 v2.0에서 멈췄고 실제 이력은 헤더 blockquote에 누적 — (B) 진행 시 헤더 `:3` 방식 따를 것.

---

## §4. 추가 질문 (작성 전 해소 필요) — BLOCK 사유

### [질문 1 · BLOCK 핵심] (B) 도식스펙-01 — 신규 후보-필터를 as-is 전사에 넣을 수 없음
**문제**: task는 "219:4571(LOT 스캔)→219:4519(P/O 목록에서 선택) 흐름을 '필터링된 후보 P/O 선택'으로 정제 서술"을 지시하나, 현재 캔버스 전사 실측상:
1. **그 엣지가 존재하지 않음**. `219:4519`(P/O 목록에서 선택)는 **작업자1 블록(S1·S2)의 첫 노드**(`:64`), `219:4571`(LOT 스캔)은 **작업자2 블록(S3·S4)**(`:84`). 캔버스 순서는 **P/O 선택이 LOT 스캔보다 먼저**이고 다른 액터 블록이며, `4571`은 `→OR 545:4443(IQC)`로 진행(`:88`)할 뿐 `4519`로 가는 엣지 없음.
2. **문서 자체 규약과 충돌**: 헤더 `:5-6` — "§1·§2는 캔버스 있는 그대로의 문서화이며 **수정 제안은 §5에만 둔다**." §3.1은 "메인 **(as-is 요소)**"(`:299`). #16 후보 필터는 **아직 캔버스에 없는 신규 동작** → §1/§2/§3(as-is)에 서술하면 전사물이 캔버스와 모순.
3. **선례**: 재정의 §4 파급표 #10 — 도식스펙-01은 "**figjam 캔버스 선행 후 전사**"(`재정의:59`). 캔버스는 요청자 figjam 소관(task 명시 "캔버스 건드리지 말 것").

**해소 옵션(요청자/리더 택1)**:
- **B1**: §5(개선 후보·"그리지 않음—목록만", `:396`)에 #16 항목 1건 추가 — "사전부착 LOT No 파싱(품목·거래처)→수신 P/O 후보 필터→담당자 선택(#16). 캔버스 재작화=요청자 figjam 예정." **단 §5는 '개선 후보'라 확정 결정을 담기엔 성격 불일치**(#16은 candidate 아닌 confirmed).
- **B2(권장)**: (B)를 **캔버스 재작화 후 소급 전사로 유예**(선례 §4#10 준수). 이번 반영은 (A)(C)(D) 3문서만.
- **B3**: §3.1 노드표 `:304` 근거 셀에 최소 주석만("사전부착 시 LOT No 파싱→품목·거래처로 수신 P/O 후보 필터, #16 — 캔버스 대기") — as-is 규약 경미 위반 감수.
→ **작성자가 임의 선택 불가**. 리더가 요청자에게 확인해야 함.

### [질문 2 · 확인 권고] (D) — #16 결정의 근거 확정기록: in-place vs 신설
- repo 패턴상 유의미 결정은 **dated 확정기록**을 신설(2026-07-14/15×3/20×2/24 전례). #16(후보 필터 신설 + [확인필요]→고정폭 34자리 확정)은 동급 신규 결정.
- 현 계획은 **재정의 v1.2 in-place**만(§5에 고정폭 기록). 재정의가 "재정의 정본"(`:6`)이고 v1.0→v1.1 in-place 전례가 있어 **in-place도 방어 가능**하나, checklist §4("근거를 먼저 만든다")·§7 관점에선 별도 `2026-07-27-사전부착LOT-PO선택-확정기록.md` 신설이 정석.
- task의 "새 doc 신설 금지"는 **채번 설계 doc** 한정(자릿수는 확정 입력값)이며 **#16 결정 확정기록을 배제한 것 아님**. → 리더가 사용자에게 "재정의 in-place vs #16 확정기록 신설" 확인 권고.

### [질문 3 · 정밀화 권고] (C) — supplier_lot_no 누락 방지
- (C) 개념모델 주석이 "공급사 자체 번호 = external_identifier"로만 서술하면 **`supplier_lot_no`(입하 라인, optional) 층을 누락**. → 주석 문구를 "공급사 자체 번호(자유·optional)는 내부 구조화 lot_no와 별개 — 데이터모델링상 `lot_external_identifier`(외부 식별자 연결) 및 입하 라인 `supplier_lot_no`로 수용"으로 정밀화 권고(충돌 아님, §5-C 반영).

---

## §5. 작성 지시서 (초안 — PROVISIONAL) — (A)(C)(D) 정합 CLEAR·질문1 해소 후 발행

> 판정=BLOCK이므로 **정식 발행 아님**. (A)(C)(D)는 정합성 CLEAR·앵커 확정 상태로 pre-stage. 질문1(B 배치) 해소·질문2(확정기록) 확인 후 리더가 확정하면 즉시 작성 가능. 앵커는 원문 그대로 인용.

### (A) WF01 — S2 Action에 사전부착 후보-필터 서술 추가
- **대상**: `research/2026-07-03-워크플로우-요구사항반영-01-자재창고관리.md`
- **앵커(`:71` 내 서술 교체)** — old:
  `**입하 등록 시 담당자가 P/O를 확인하며**(§재정의 #6), **자재LOT 등록은 경로별 — 사전부착=공급사 번호 수용·등록 / 직접부착=MES 발번**(§재정의 #4).`
- **new**(예시 — 리더 확정 후 확정문):
  `**입하 등록 시 담당자가 P/O를 확인하며**(§재정의 #6), **자재LOT 등록은 경로별 — 사전부착=공급사가 MES 표준 양식대로 부여한 번호 수용·등록 / 직접부착=MES 발번**(§재정의 #4). **사전부착 경로에선 LOT 스캔 → 자재 LOT No 파싱({제품코드}→품목·{공급사 성분}→거래처) → 수신 P/O(REQ-PR-0024) 중 해당 품목×공급사×미마감 후보를 자동 필터해 담당자에게 제시, 담당자가 최종 P/O 선택·확인**(#16 — 담당자 최종 확인 유지, §재정의 #6 정합).`
- **성격**: 서술 교체+가산. **마커**: `✓확정 2026-07-27 #16`(또는 신설 확정기록 경로 — 질문2). **버전**: v1.5→**v1.6**. **변경이력 행**(`:304` 위 신규): `| v1.6 | 2026-07-27 | #16 사전부착 LOT→수신 P/O 후보 필터 반영(S2) — LOT No 파싱(품목·거래처)로 미마감 P/O 후보 자동 필터·담당자 최종 선택(#6 유지). 사전부착=공급사가 MES 양식대로 부여로 문구 정제. 근거 …#16/확정기록 |`
- **집계 재계수**: 없음(엔티티·마커 총계 미영향).

### (C) 개념모델 — 자재LOT 행(`:206`) 3점 정밀화
- **대상**: `research/2026-07-03-개념데이터모델-v2-요구사항통합.md` (자재LOT 행 = `:206` 단일)
- **편집 1(P/O 참조에 후보 필터 가산)** — old: `**P/O 참조 = 입하 등록 시 담당자 확정**(§재정의 #6)` → new 말미 추가: `— 사전부착 시 LOT No 파싱(품목·거래처)로 수신 P/O(REQ-PR-0024) 미마감 후보 자동 필터 후 담당자 선택(#16, 담당자 확정 유지)`
- **편집 2([확인필요]→고정폭)** — old: `공급사 성분 코드 체계·자릿수는 **[확인필요](상세 설계)**` → new: `공급사 성분 포함, **고정폭 34자리(제품코드9·수량스냅샷9·날짜6 YYMMDD·공급사성분6·번호4)·전부 숫자·구분문자 없음**(§재정의 §5 — 상세 채번 도출·검증·유일성은 별도 후속)`
- **편집 3(발번 주체 정제 + 데이터모델 정합)** — old: `**발번 주체 = 사전부착: 공급사 번호를 MES가 수용·등록 / 직접부착: MES 발번**` → new: `**발번 주체 = 사전부착: 공급사가 MES 표준 양식대로 부여한 번호 수용·등록(경로 무관 항상 MES 양식) / 직접부착: MES 발번**(§재정의 #4). **공급사 자체 번호(자유·optional)는 내부 구조화 LOT No와 별개** — 데이터모델링상 내부 번호=`lot.lot_no`, 공급사 자유 번호=`lot_external_identifier`(외부 식별자 연결)·입하 라인 `supplier_lot_no`로 수용(정상 설계·정합)`
- **성격**: 서술 교체+가산. **마커**: `✓확정 2026-07-27 #16`. **버전**: v2.8→**v2.9**. **변경이력 행**(`:624` 위): `| v2.9 | 2026-07-27 | #16 반영 — 자재LOT 행: P/O 참조에 후보 필터(파싱) 가산·[확인필요] 자릿수→고정폭 34자리(9/9/6/6/4)·사전부착 발번 문구 정제(공급사가 MES 양식대로 부여)·데이터모델링 정합(lot_no/external_identifier/supplier_lot_no 3층) 주석. 근거 …#16 |`
- **집계 재계수**: **불요** — 엔티티 신설 없음(64 엔티티 불변). 확인 완료.

### (D) 재정의 확정기록 — #4 정제 + §5 고정폭 확정
- **대상**: `research/2026-07-20-자재LOT관리-재정의-확정기록.md`
- **편집 1(#4 정제, `:25`)** — old: `| 4 | 번호 출처(경로별) | **사전부착 = 공급사 번호를 MES가 수용·등록** / **직접부착 = MES 발번 + 등록.** |` → new: `| 4 | 번호 출처(경로별) | **사전부착 = 공급사가 MES 표준 양식대로 부여한 번호를 MES가 수용·등록**(경로 무관 항상 MES 양식) / **직접부착 = MES 발번 + 등록.** 공급사 자체 번호(자유 발급·optional)는 **별개** — 데이터모델링 `lot_external_identifier`(별도). *(문구 정제 2026-07-27 #16 — 뒤집기 아님: #2·#3 MES 양식 구성과 정합 명확화)* |`
- **편집 2(§5 고정폭, `:67`)** — old: `- 잔여 확인: 번호 "공급사 성분"의 코드 체계·자릿수(고정폭)는 상세 설계 과제([확인필요]).` → new: `- **고정폭 확정(✓확정 2026-07-27 #16)**: 자재 LOT No = **34자리·전부 숫자·구분문자 없음** — 제품코드9[1-9]·수량 스냅샷9[10-18]·날짜6 YYMMDD[19-24]·공급사 성분6[25-30]·번호4[31-34](§3 5분절 순서). **상세 채번 설계(도출·검증·유일성 등)는 별도 후속 과제** — 자릿수는 확정 입력값(새 채번 doc 미신설).`
- **성격**: 문구 정제 + 미결 해소. **마커**: `✓확정 2026-07-27 #16`(뒤집기 아님 주석 병기). **버전**: v1.1→**v1.2**. **변경이력 행**(`:72` 위): `| v1.2 | 2026-07-27 | #16(사전부착 LOT→수신 P/O 후보 필터) 반영 — #4 문구 정제(사전부착=공급사가 MES 양식대로 부여·공급사 자체 번호=external_identifier optional 별개), §5 [확인필요](공급사 성분 자릿수)→고정폭 34자리(9/9/6/6/4) 확정·상세 채번 별도 후속. 정제이지 뒤집기 아님. |`
- **집계 재계수**: 불요.

### (B) 도식스펙-01 — **질문1 해소 전 보류**
- 앵커 후보: §3.1 노드표 `:304`(P/O 목록에서 선택 219:4519) / §5 개선 후보 `:396` / 헤더 이력 `:3`. **배치 방식(B1/B2/B3) 확정 후 작성**.

---

## §6. 최종 판정

- **판정: BLOCK** (리더 → 사용자 표면화)
- **정합성 충돌**: 0건 (하드 충돌 없음 — 5축 전부 정제/가산, 뒤집기 아님. §1)
- **BLOCK 사유**: 추가 질문 1건(질문1 — B 도식스펙-01 배치, 작성자 임의 선택 불가) + 확인 권고 2건(질문2 확정기록 신설 여부·질문3 supplier_lot_no 정밀화).
- **해소 경로**: 리더가 요청자/사용자에게 (1) B 배치(B1/B2/B3 중), (2) #16 확정기록 신설 vs 재정의 in-place 확인. → 회신 반영해 회차 `r2`로 재판정, (A)(C)(D)는 §5 초안대로 즉시 발행 가능(정합성 CLEAR).
- **선행 필요**: 질문2가 "확정기록 신설"로 결정되면 → **확정기록 신설이 (A)(C)(D) 인용보다 선행**(근거 문서 먼저).
