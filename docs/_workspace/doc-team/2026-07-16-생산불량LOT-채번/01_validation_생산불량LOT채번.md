# 검증 보고 — 생산불량LOT 채번·수량 방식 (B-7/8/9)

> 검증 담당자(doc-validator) · 2026-07-16 · run: `2026-07-16-생산불량LOT-채번`
> 모드: **게이트 모드** (결정 반영 전 정합성 검증 + 작성 지시서)
> 대상 결정: 사용자 확정 2026-07-16 — 생산불량LOT의 채번·수량 방식 (B-7·B-8·B-9)

## 0. 판정 요약

- **판정: CLEAR** — 작성 지시서 발행(§5).
- **정합성: 충돌 없음(신 확정끼리) · "단위 크기 분할" 잠정 모델 잔재 5개소가 교정 대상**(모두 사용자 본인의 2026-07-15 잠정 서술을 완성하는 것 — 확정 뒤집기 사고 아님).
- **추가 질문(차단성): 없음** — "1건" 해석은 B-7+B-8로 논리 확정, 원천별 입력 주체는 범위 밖 미세 논점(비차단, §4).
- 부수 발견: **WF02 헤더 버전 스큐(v1.6 vs 변경이력 v1.7) 선재 결함** — 이번 반영 시 해소(§5 File C). 2026-07-15 기록 §2 제목 "(9건)" vs 실제 11행 선재 불일치(비차단 메모, §6).

---

## 1. 반영할 결정 (원문)

- **B-7**: 생산불량LOT의 제품 수량은 별도 설정 없이 **실제 불량 수량을 작업자가 직접 입력**한다. (품목별 설정값·기본값+override 같은 "단위 크기" 개념 없음.)
- **B-8**: **불량 발생 즉시 1건씩 채번**한다. (누적 후 단위 충족 시 채번이 아님.)
- **B-9**: 마감 시 단위 미달 잔여분 처리 문제는 B-7·B-8로 자동 해소(단위 임계값이 없으므로 잔여분 개념 자체가 없음).

→ `research/2026-07-15-생산불량LOT-정책-확정기록.md` §5의 **잔여 열린 지점 3건**(단위 크기 기본값·채번 타이밍·마감 잔여분)을 종결한다.

---

## 2. 정합성 5단계 수행

### ① 정본 식별
- 엔티티·필드 성격(수량 필드 정의) → **개념데이터모델 v2**(의미 코어 정본).
- 프로세스·채번 타이밍 성격(B-8) → **WF02 생산실행**(워크플로우 정본, S9).
- 결정 근거(신규) → **2026-07-15 생산불량LOT 정책 확정기록**(자기 §5 미결의 완성이므로 신규 기록 신설이 아니라 동 기록 갱신 — §7 근거 참조).

### ② 상류 추적(근거)
- 2026-07-15 기록 §5는 3건을 **명시적으로 "미정"**으로 열어두었다(line 65-67). B-7/8/9는 그 미결의 **완성**이다 — 기존 `✓확정`을 뒤집는 것이 아님(리더 사전 판정과 일치).
- 단, 2026-07-15 기록 §2 #5(크기 분할)는 당시 **결정 사항 표에 확정처럼 기재**되어 있었다(line 26). 이 문장은 이제 폐기되므로 **`(기존 서술 폐기)` 마커 필수** — 되돌림 방지(checklist §3-2).

### ③ 하류 추적(파급) — repo 전역 grep
`생산불량LOT` 참조 6개 문서 + `단위 크기/단위 수량/누적/채번 타이밍/마감 잔여분` 키워드 전수:

| 문서 | 잔재/영향 | 조치 |
| --- | --- | --- |
| `research/2026-07-15-생산불량LOT-정책-확정기록.md` | §2 #5(line 26)·§3 속성(line 44)·§5(line 65-67)에 단위 크기 분할 모델 + 미결 | **교정**(File A) |
| `research/2026-07-03-개념데이터모델-v2-요구사항통합.md` | 속성(line 209)·ERD(line 236)·미결(line 246)에 단위 크기 분할 잔재 + 잔여 미정 | **교정**(File B) |
| `research/2026-07-03-워크플로우-요구사항반영-02-생산실행.md` | S9 body 잔재 **없음**(원천별 개별 채번만 서술) — B-8 채번 타이밍 미기재 | **보강**(File C, 프로세스 정본 완성 + 헤더 스큐 해소) |
| `research/2026-07-15-PQC-검사방식-확정기록.md` | line 24·29·59 — 단위 크기 잔재 **없음**, B-7/8/9와 정합 | **불요** |
| `research/2026-07-03-워크플로우-요구사항반영-03-품질관리.md` | line 298(변경이력)만 — 잔재 없음, 정합 표기 | **불요** |
| `research/2026-07-06-도식스펙-03-품질관리.md` | line 429·461 — 재투입 참조만, 잔재 없음(캔버스 별도) | **불요** |
| `research/2026-07-06-도식스펙-02-생산실행.md` | line 204·413는 **생산LOT**(양품목표÷LOT크기) — 생산불량LOT 무관 | **범위 밖** |

> `LOT크기`/`단위 크기` 다른 히트(개념데이터모델 line 208, 도식스펙-02 line 204, 현장점검 HTML 등)는 모두 **생산LOT의 양품 목표 LOT 크기** — 정당한 설계이며 이번 결정과 무관(생산LOT은 여전히 양품목표÷LOT크기로 분할).

### ④ 충돌 판정 (file:line) — 전부 "교정 대상"(신 확정끼리 모순 없음)

| # | 위치 | 현재 서술(잔재) | 결정과의 관계 |
| --- | --- | --- | --- |
| C1 | `개념데이터모델` **line 209** | `LOT크기(불량LOT 단위 수량 — 기본값+override, 원천별 개별 분할. 예: 총 불량 20건·단위 10건이면 2건 발행), 수량(누적)` | B-7(단위 크기 없음)·B-8(누적 아님)이 대체 → 교정 |
| C2 | `개념데이터모델` **line 236**(ERD) | `[생산불량LOT](… — 개별 채번·단위 크기 분할)` | "단위 크기 분할" 제거 |
| C3 | `개념데이터모델` **line 246**(미결) | `…개별 채번·단위 크기 분할…` + `잔여 미정: 단위 크기 기본값·채번 타이밍·마감 시 잔여분 처리` | "단위 크기 분할" 제거 + 잔여 미정 종결 |
| C4 | `2026-07-15 기록` **line 26**(§2 #5) | `생산불량LOT도 생산LOT처럼 단위 크기로 분할… 예: 총 불량 20개·단위 10개면 2건 생성` | 폐기 마커 + 대체 서술 |
| C5 | `2026-07-15 기록` **line 44**(§3 속성) | `LOT크기(단위 수량 — 기본값+override, 원천별 개별 분할) · 수량(누적)` | 폐기 마커 + `수량(직접 입력)`으로 교체 |
| C6 | `2026-07-15 기록` **line 65-67**(§5) | 3개 미결 bullet | 3건 종결(B-7/8/9 교차참조) |

- **개념데이터모델 line 209 끝 "발행 주체" 서술 `원천 이벤트(…) 발생 시 파생 생성"`은 B-8과 정합**(발생 즉시 생성) — 유지. B-8은 이 문구의 대안("누적 후 채번")을 배제할 뿐이므로 오히려 강화됨.

### ⑤ 추가 질문 표면화 → §4

---

## 3. 핵심 의심 지점 판정 (리더 지목)

- **의심**: 2026-07-15 잠정 기록의 "단위 크기 분할 모델"(기본값+override·원천별 개별 분할·예: 20건÷10=2건)을 B-7/B-8이 대체하는가?
- **판정: 그렇다. 단위 크기 개념 완전 삭제.** 최종 모델 = ①수량 = 실제 불량 수량 작업자 직접 입력(설정값·기본값+override 없음) ②채번 = 불량 발생 즉시 1건(원천 이벤트 1건당 생산불량LOT 1건) ③잔여분 개념 없음.
- **잔재 위치 명시**: C1(line 209)·C2(line 236)·C3(line 246)·C4(line 26)·C5(line 44). 전부 교정 지시서 발행(§5).
- **성격**: 사용자가 자기 잠정 서술(당시 "예시·미정" 표기)을 완성 → **확정 뒤집기 사고 아님.** 단 정본 본문 잔재는 `(기존 서술 폐기)` 마커로 반드시 교정.

---

## 4. 추가 질문 (하위 미결)

### 차단성 추가 질문: **없음**

### 참고 — 비차단 논점 (작성 진행에 지장 없음, 확인 시 반영)

1. **"1건"의 단위**: B-8 "발생 즉시 1건"은 **원천 이벤트 1건당 생산불량LOT 1건·수량 N**으로 읽힌다(개체 1개당 1LOT 아님). 근거: B-7이 "실제 불량 수량을 직접 입력"이라 했으므로 각 LOT이 개체 1개(수량 1)라면 입력이 무의미 → 이벤트 단위 해석이 B-7+B-8의 유일 정합 해. §3 "발행 주체"(원천 이벤트 발생 시 파생 생성)와도 일치. → 지시서에 이 해석을 명문화(사용자 재검토 시 veto 가능). **차단 아님**(결정 집합이 논리적으로 확정).
2. **원천별 수량 입력 주체**: "작업자 직접 입력"이 3원천 전부인지, 현장 발견분에 한정되고 PQC분은 검사실적 판정 수량을 승계하는지 — 미세 논점. **이번 교정에 불필요**(엔티티 필드를 "원천 이벤트 발생 시 직접 입력"으로 중립 서술해 회피). B-7/8/9의 핵심(단위 크기 제거·발생 즉시 채번)과 독립. 향후 별도 결정 가능. **차단 아님.**

---

## 5. 작성 지시서 (CLEAR — doc-writer 인계)

> 마커 규약: 2026-07-16 대화 결정은 `✓확정 2026-07-16(대화 결정 B-7·B-8·B-9)`(2026-07-15 선례와 동형). 폐기 흔적은 `(기존 서술 폐기: …)`.
> **앵커 불일치 시 doc-writer는 멈추고 보고**(창작 금지).

### 파급 대상 전체 목록
- **필수**: File A(2026-07-15 확정기록) · File B(개념데이터모델 v2)
- **권장(프로세스 정본 완성 + 선재 스큐 해소)**: File C(WF02 생산실행)
- **반영 불요(정합 확인됨)**: PQC 검사방식 확정기록 · WF03 품질관리 · 도식스펙-03(캔버스 별도) · 도식스펙-02(생산LOT 무관)

---

### File A — `research/2026-07-15-생산불량LOT-정책-확정기록.md` (버전 bump v1.0 → **v1.1**)

**A-0. 헤더 버전 표기 추가**
- 성격: 버전 갱신 · old_string:
```
> 작성일: 2026-07-15 · 작성 주체: CREFLE OMF 팀
```
- new_string:
```
> 작성일: 2026-07-15 (v1.1 — 2026-07-16 생산불량LOT 채번·수량 방식 확정 반영) · 작성 주체: CREFLE OMF 팀
```

**A-1. §2 #5 크기 분할 (폐기 표기)** [C4]
- 성격: 폐기 표기 · old_string:
```
| 5 | 크기 분할 | 생산불량LOT도 생산LOT처럼 **단위 크기로 분할**되어 여러 건 생성 가능. 예: 총 불량 20개·단위 10개면 2건 생성. |
```
- new_string:
```
| 5 | 크기 분할 | ~~생산불량LOT도 생산LOT처럼 단위 크기로 분할되어 여러 건 생성 가능. 예: 총 불량 20개·단위 10개면 2건 생성.~~ **(기존 서술 폐기: 2026-07-16 B-7·B-8로 대체) — 단위 크기(기본값+override) 개념 없음. 수량 = 실제 불량 수량을 발생 시 직접 입력, 채번 = 불량 발생 즉시 1건씩(원천 이벤트 1건당 생산불량LOT 1건·수량 N). ✓확정 2026-07-16** |
```

**A-2. §3 핵심 속성 (LOT크기·수량(누적) 교체)** [C5]
- 성격: 서술 교체 + 폐기 표기 · old_string:
```
| 핵심 속성 | W/O No(FK) · 원천(PQC/현장/의심자재 — 열거형, LOT당 단일값) · 불량코드/의심 사유 · LOT크기(단위 수량 — 기본값+override, 원천별 개별 분할) · 수량(누적) · 처분구분(재작업가능/폐기) · 상태(발생/재작업중/재투입완료/폐기완료/Release) · **재투입 생산LOT No(FK, nullable — 재작업 성공 또는 의심자재 Release 시 그 시점 진행 중인 생산LOT 참조)** · 생성일시 |
```
- new_string:
```
| 핵심 속성 | W/O No(FK) · 원천(PQC/현장/의심자재 — 열거형, LOT당 단일값) · 불량코드/의심 사유 · **수량(실제 불량 수량 — 원천 이벤트 발생 시 직접 입력. 기존 서술 폐기: ~~LOT크기(단위 수량 — 기본값+override, 원천별 개별 분할)·수량(누적)~~ — 단위 크기 분할 개념 없음·발생 즉시 1건 채번이라 누적 아님. ✓확정 2026-07-16 B-7·B-8)** · 처분구분(재작업가능/폐기) · 상태(발생/재작업중/재투입완료/폐기완료/Release) · **재투입 생산LOT No(FK, nullable — 재작업 성공 또는 의심자재 Release 시 그 시점 진행 중인 생산LOT 참조)** · 생성일시 |
```

**A-3. §5 잔여 열린 지점 3건 종결** [C6]
- 성격: 미결 종결 · old_string:
```
- 생산불량LOT의 "단위 크기(예: 10개)" 기본값 산정 기준(품목별 설정? 고정값?) — 구체 수치는 미정.
- 생산불량LOT 발생 즉시 채번인지, 누적 후 단위 충족 시 채번인지(타이밍) — 미정.
- W/O 마감 시점에 단위 미달로 남은 생산불량LOT 잔여분 처리(생산LOT의 미달/정상/초과 마감 3분류와 유사한 개념이 필요한지) — 미정.
```
- new_string:
```
- ~~생산불량LOT의 "단위 크기(예: 10개)" 기본값 산정 기준(품목별 설정? 고정값?) — 구체 수치는 미정.~~ → **종결(✓확정 2026-07-16 B-7): 단위 크기 개념 자체를 두지 않는다. 제품 수량 = 실제 불량 수량을 작업자가 직접 입력(품목별 설정값·기본값+override 없음).**
- ~~생산불량LOT 발생 즉시 채번인지, 누적 후 단위 충족 시 채번인지(타이밍) — 미정.~~ → **종결(✓확정 2026-07-16 B-8): 불량 발생 즉시 1건씩 채번(원천 이벤트 1건당 생산불량LOT 1건). 누적 후 단위 충족 채번 아님.**
- ~~W/O 마감 시점에 단위 미달로 남은 생산불량LOT 잔여분 처리(생산LOT의 미달/정상/초과 마감 3분류와 유사한 개념이 필요한지) — 미정.~~ → **종결(✓확정 2026-07-16 B-9): 단위 임계값이 없으므로 '잔여분' 개념 자체가 없다(B-7·B-8로 자동 해소).**
```

**A-4. 변경 이력 행 추가** (v1.0 행 위에 삽입)
- 성격: 변경 이력 · old_string:
```
| v1.0 | 2026-07-15 | 최초 작성 — 생산불량LOT 신설 확정(9개 결정 사항 + 게이트 문답 4건), 엔티티 정의, 파급 반영 대상 명시. |
```
- new_string:
```
| v1.1 | 2026-07-16 | **생산불량LOT 채번·수량 방식 확정(✓확정 2026-07-16 — 대화 결정 B-7·B-8·B-9)** — §5 잔여 열린 지점 3건 종결: ① 수량 = 실제 불량 수량 작업자 직접 입력(단위 크기·기본값+override 개념 삭제, B-7) ② 불량 발생 즉시 1건씩 채번(원천 이벤트 1건당·누적 아님, B-8) ③ 마감 잔여분 개념 없음(단위 임계값 부재로 자동 해소, B-9). §2 #5(크기 분할)·§3 속성(LOT크기·수량(누적)) 기존 서술 폐기 표기. 파급: 개념데이터모델 v2.7·WF02 v1.8 동기. |
| v1.0 | 2026-07-15 | 최초 작성 — 생산불량LOT 신설 확정(9개 결정 사항 + 게이트 문답 4건), 엔티티 정의, 파급 반영 대상 명시. |
```

---

### File B — `research/2026-07-03-개념데이터모델-v2-요구사항통합.md` (버전 bump v2.6 → **v2.7**)

**B-1. 헤더 버전 bump**
- 성격: 버전 갱신 · old_string:
```
> 정리일: 2026-07-07 (v2.6 — 2026-07-15 생산불량LOT 신설·PQC 검사 방식 확정 반영, 최초 작성 2026-07-03 v2.0) · 작성 주체: CREFLE OMF 팀
```
- new_string:
```
> 정리일: 2026-07-07 (v2.7 — 2026-07-16 생산불량LOT 채번·수량 방식 확정 반영, 최초 작성 2026-07-03 v2.0) · 작성 주체: CREFLE OMF 팀
```

**B-2. 생산불량LOT 엔티티 속성 교정** [C1] — old_string(부분 유일 매칭):
```
불량코드/의심 사유, **LOT크기(불량LOT 단위 수량 — 기본값+override, 원천별 개별 분할. 예: 총 불량 20건·단위 10건이면 2건 발행)**, 수량(누적), 처분구분(재작업가능/폐기)
```
- new_string:
```
불량코드/의심 사유, **수량(실제 불량 수량 — 원천 이벤트 발생 시 직접 입력, 단위 크기 분할 없음 · 기존 서술 폐기: 'LOT크기(단위 수량 기본값+override, 개별 분할)·수량(누적)' — ✓확정 2026-07-16 B-7·B-8)**, 처분구분(재작업가능/폐기)
```

**B-2b. 동 행 출처 셀 인용 보강** — ⚠ 출처 셀 `| ✓확정 2026-07-15(대화 결정) — …md |`만으로는 line 209(엔티티 행)·line 222(W/O 귀속 관계 행) **둘 다 매칭**되므로, "Lot Status…" 접두를 포함해 line 209로 유일 특정한다. old_string:
```
**Lot Status(Hold/Release 등) 비대상 — 처분구분·상태 필드로 자체 관리** | ✓확정 2026-07-15(대화 결정) — research/2026-07-15-생산불량LOT-정책-확정기록.md |
```
- new_string:
```
**Lot Status(Hold/Release 등) 비대상 — 처분구분·상태 필드로 자체 관리** | ✓확정 2026-07-15(대화 결정)·채번/수량 ✓확정 2026-07-16(B-7·B-8·B-9) — research/2026-07-15-생산불량LOT-정책-확정기록.md(v1.1) |
```
> line 222(W/O 귀속 관계 행)의 동일 출처 셀은 **변경하지 않는다**(엔티티 속성 교정만 대상). B-2와 B-2b는 line 209의 서로 다른 구간(중간 속성 vs 말미 출처 셀)이라 상호 비간섭.

**B-3. ERD 교정** [C2]
- 성격: 서술 교체 · old_string:
```
  [W/O] ──1:N──▶ [생산불량LOT](원천=PQC/현장/의심자재 — 개별 채번·단위 크기 분할)
```
- new_string:
```
  [W/O] ──1:N──▶ [생산불량LOT](원천=PQC/현장/의심자재 — 원천별 개별 채번·발생 즉시 1건·수량 직접 입력)
```

**B-4. 미결 종결** [C3]
- 성격: 서술 교체 + 미결 종결 · old_string:
```
- ✓[확정 2026-07-15] **생산LOT ↔ 불량 물량 분리** — 생산LOT은 양품만 귀속(불량수 필드 폐기), 불량·의심자재는 신설 생산불량LOT(원천별 개별 채번·단위 크기 분할·W/O 직접 귀속)으로 관리. 재작업 성공/의심자재 Release 시 재투입 시점 생산LOT으로 복귀(원 LOT 무관, 참조 필드로 추적). 근거: research/2026-07-15-생산불량LOT-정책-확정기록.md. 잔여 미정: 생산불량LOT 단위 크기 기본값·채번 타이밍·마감 시 잔여분 처리.
```
- new_string:
```
- ✓[확정 2026-07-15] **생산LOT ↔ 불량 물량 분리** — 생산LOT은 양품만 귀속(불량수 필드 폐기), 불량·의심자재는 신설 생산불량LOT(원천별 개별 채번·W/O 직접 귀속)으로 관리. 재작업 성공/의심자재 Release 시 재투입 시점 생산LOT으로 복귀(원 LOT 무관, 참조 필드로 추적). 근거: research/2026-07-15-생산불량LOT-정책-확정기록.md. **✓[확정 2026-07-16 B-7·B-8·B-9] 채번·수량 방식(잔여 미정 종결): 제품 수량 = 실제 불량 수량 작업자 직접 입력(단위 크기·기본값+override 개념 삭제)·불량 발생 즉시 1건 채번(원천 이벤트 1건당·누적 아님)·단위 임계값 부재로 마감 잔여분 개념 없음(기존 서술 폐기: '단위 크기 분할'). 근거: 동 기록 v1.1.**
```

**B-5. 정리 이력 행 추가** (v2.6 행 위에 삽입)
- 성격: 변경 이력 · old_string:
```
| v2.6 | 2026-07-15 | **PQC 검사 방식 확정 반영(✓확정 2026-07-15 — 대화 결정, research/2026-07-15-PQC-검사방식-확정기록.md)**
```
- new_string:
```
| v2.7 | 2026-07-16 | **생산불량LOT 채번·수량 방식 확정(✓확정 2026-07-16 — 대화 결정 B-7·B-8·B-9, research/2026-07-15-생산불량LOT-정책-확정기록.md v1.1)** — 2026-07-15 신설 시 잠정이던 '단위 크기 분할' 모델 폐기·완성: ① 속성 'LOT크기(기본값+override)·수량(누적)' → '수량(실제 불량 수량·발생 시 직접 입력)' 교체(B-7) ② 채번=불량 발생 즉시 1건(원천 이벤트 1건당·누적 아님, B-8) ③ 마감 잔여분 개념 없음(단위 임계값 부재, B-9). ERD·§미결(생산LOT↔불량 물량 분리)의 '단위 크기 분할' 문구 제거·잔여 미정 3건 종결. 엔티티 총계 불변(64). |
| v2.6 | 2026-07-15 | **PQC 검사 방식 확정 반영(✓확정 2026-07-15 — 대화 결정, research/2026-07-15-PQC-검사방식-확정기록.md)**
```
> 주의: B-5 old_string은 v2.6 행 **첫 문장까지만** 인용(행 전체는 매우 길다). doc-writer는 이 접두 문자열이 유일 매칭됨을 확인하고, v2.7 행을 그 앞 줄에 삽입만 한다(v2.6 행 원문은 변경 없음).
> 집계 재계수: 엔티티 총계 **64 불변**(생산불량LOT는 v2.5에서 이미 계수, 이번은 속성 교정만). 헤더 line 8 "v2.5 신설 1: 생산불량LOT" 문구도 변경 불요.

---

### File C — `research/2026-07-03-워크플로우-요구사항반영-02-생산실행.md` (버전 bump v1.6 → **v1.8**, 권장)

> **선재 결함 해소**: 헤더는 v1.6인데 변경이력 최신 행은 v1.7(생산불량LOT 신설, 2026-07-15) — 헤더가 v1.7 반영 시 미갱신된 스큐. 이번에 v1.8로 올리며 해소.

**C-1. 헤더 버전 정정**
- 성격: 버전 갱신(스큐 해소) · old_string:
```
> 정리일: 2026-07-03 · 최종 갱신: 2026-07-15 (v1.6 — 마감 후 실적 정정 운영 방식 확정 반영) · 작성 주체: CREFLE OMF 팀
```
- new_string:
```
> 정리일: 2026-07-03 · 최종 갱신: 2026-07-16 (v1.8 — 생산불량LOT 채번·수량 방식 확정 반영) · 작성 주체: CREFLE OMF 팀
```

**C-2. S9 Rule에 채번 타이밍·수량 입력 명시**
- 성격: 신규 추가(잔재 없음 — 프로세스 정본 완성) · old_string:
```
**불량 물량 자체는 생산불량LOT(원천=현장/PQC/의심자재 — 개별 채번)으로 추적**(생산LOT과 별개, W/O 직접 귀속 — research/2026-07-15-생산불량LOT-정책-확정기록.md).
```
- new_string:
```
**불량 물량 자체는 생산불량LOT(원천=현장/PQC/의심자재 — 원천별 개별 채번)으로 추적**(생산LOT과 별개, W/O 직접 귀속 — research/2026-07-15-생산불량LOT-정책-확정기록.md). **(2026-07-16 확정 B-7·B-8) 생산불량LOT 채번 = 불량 발생 즉시 1건(원천 이벤트 1건당·누적 아님)·제품 수량 = 실제 불량 수량 발생 시 직접 입력(단위 크기·기본값 개념 없음) — 동 기록 v1.1.**
```

**C-3. 변경 이력 행 추가** (v1.7 행 위에 삽입)
- 성격: 변경 이력 · old_string:
```
| v1.7 | 2026-07-15 | **생산불량LOT 신설(✓확정 2026-07-15 — 대화 결정, research/2026-07-15-생산불량LOT-정책-확정기록.md)**
```
- new_string:
```
| v1.8 | 2026-07-16 | **생산불량LOT 채번·수량 방식 확정 반영(✓확정 2026-07-16 — 대화 결정 B-7·B-8·B-9, research/2026-07-15-생산불량LOT-정책-확정기록.md v1.1)** — S9 Rule에 채번 타이밍·수량 입력 방식 명시: 불량 발생 즉시 1건 채번(원천 이벤트 1건당·누적 아님)·수량 = 실제 불량 수량 작업자 직접 입력(단위 크기·기본값+override 개념 없음). 개념데이터모델 v2.7 동기. (헤더 버전 v1.6→v1.8 정정 — v1.7(생산불량LOT 신설, 2026-07-15)이 변경이력에만 있고 헤더 미갱신이던 스큐 해소.) |
| v1.7 | 2026-07-15 | **생산불량LOT 신설(✓확정 2026-07-15 — 대화 결정, research/2026-07-15-생산불량LOT-정책-확정기록.md)**
```
> 주의: C-3 old_string은 v1.7 행 **첫 문장까지만** 인용(행 전체는 길다). doc-writer는 접두 문자열 유일 매칭 확인 후 v1.8 행을 앞 줄에 삽입만 한다.

---

## 6. 비차단 메모 (선재 결함 — 이번 반영과 독립, 선택 정리)

- **2026-07-15 기록 §2 제목 "(9건)" vs 실제 11행**(#1~#11): 선재 불일치. B-7/8/9와 무관하나, #5가 폐기 표기되므로 문맥상 혼동 가능. doc-writer가 File A 작업 중 여유가 되면 `## §2. 확정 결정 사항 (9건)` → `(11건 — #5 폐기)`로 정정 권장. **차단 아님**(이번 지시서 필수 항목 아님).

## 7. 구조 결정 근거 — 왜 2026-07-15 기록 edit-in-place(신규 확정기록 신설 아님)

- checklist §4 기본은 "신규 결정 → 확정기록 신설"이나, B-7/8/9는 **2026-07-15 기록 자신의 §5 미결의 완성**이다(별개 신규 주제 아님). 자기 기록의 열린 지점을 닫는 것은 그 기록을 갱신하는 것이 자연스럽다(대조: 2026-07-15 PQC 기록은 별개 결정 영역이라 독립 기록).
- "원문 보존"(checklist §4)은 폐기 문장을 **삭제하지 않고 취소선+폐기 마커로 보존**해 충족(§2 #5·§3 잔재를 지우지 않음).
- 리더 과제의 대상 후보 명시("생산불량LOT 확정기록 §2·§3·§5")와도 합치.
- **대안**(리더 판단 시): 별도 `research/2026-07-16-생산불량LOT-채번-확정기록.md` 신설도 가능. 그 경우 File A는 §5 종결+§2/§3 폐기 마커의 교차참조를 신 기록으로 돌리고, File B/C 인용을 신 기록 경로로 교체. 현 지시서는 edit-in-place 기준으로 작성됨.

---

## 8. 최종 판정

**CLEAR** — 충돌 없음(신 확정끼리)·잔재 교정만 필요·차단성 추가 질문 없음. 작성 지시서(§5) 발행. 필수 2개 파일(File A·B) + 권장 1개(File C). 비차단 참고 2건(§4)·선재 결함 2건(WF02 스큐=File C에서 해소, §2 카운트=선택).
