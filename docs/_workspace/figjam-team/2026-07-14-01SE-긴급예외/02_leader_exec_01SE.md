# 리더 실행 기록 — 01-S-E 긴급 예외·수동 진입 표기 (경량 전환 실행)

> run `2026-07-14-01SE-긴급예외` · 2026-07-14 · 실행: 오케스트레이터(리더)
> 정본 지시서: `01_designer_brief_01SE.md` (설계자 발행 r1 — 변경 4건·≤5으로 리더 경량 전환, 검토 독립성은 유지)

## 실행 결과 (지시서 §3 대조)

| 코드 | 노드 ID | 조치 결과 |
| --- | --- | --- |
| E1 | 219:5457 | 문구 교체 완료. **승인 편차 2건(아래)** |
| E2 | **442:6658** | 219:5457 clone · rel(40,186) · 240×62 · 「추가 자재 출고 요청 ← 02 도식」 · parent 219:5450 ✓ |
| E3 | **442:6661** | 263:5767 clone · rel(300,195) · **290×44(폭 보정)** · 「수동 발행 · 사유 '긴급 W/O 대응'」 · parent 219:5450 ✓ |
| E4 | **442:6664** | createConnector · start=E2(AUTO)·end=219:5458(AUTO) · ELBOWED · w4 · 회색 {.702} · 무라벨 ✓ |

## 승인 편차 (리더 판단 — 검토 시 이 기록이 지시서 문면보다 우선)

1. **E3 폭 170→290 보정**: clone이 원본 폭(170)을 유지해 긴 문구가 잘릴 위험 — 지시서 §3의 "폭 auto-fit(≈290)" 의도대로 resize(290,44). 격리 스크린샷 전문 렌더 확인.
2. **E1 높이 62→80(중심 유지, rel y 359→350)**: 1줄 렌더 시 「(긴급…」 클리핑 발생(격리 스크린샷 실측) → 지시서 §3 E1 규칙("2줄 클리핑 시 h≤80, 중심 y 유지") 그대로 적용.
3. **E1 줄바꿈 문자 삽입**: h 80 자동 wrap이 「제/외」로 단어 중간 분리 → 공백 1개를 `\n`으로 교체해 `W/O 생성 ← 02 도식\n(긴급 제외)` 2줄 정렬. **토씨·어절 동일, 공백→줄바꿈만 변경** — 경계 표기 가독 우선 리더 승인. 검토자는 문구 대조 시 이 형태를 정답으로 본다.

## 자가 검증
- 격리 스크린샷 3장(E1·E2·E3): 전문 렌더·클리핑 0. E1 = 「W/O 생성 ← 02 도식」/「(긴급 제외)」 2줄.
- 검토자 확인 요청: §6 주의 지점(E4↔5462가 5458 좌상단에서 근접 교차 여부 — AUTO 라우팅 실측)·유지 목록 §5 전수.
