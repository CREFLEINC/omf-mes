# 미니 지시서 — 04-S-C SC4 행위 노드 작화 (#44)

> 작성일: 2026-07-29 · 리더 직접(경량 경로 — 변경 요소 4건 ≤5 · 신규 섹션 없음 · 사용자 확정 「1번(작화)」)
> 보드 `Sgy5IKD4aWuzpKtJAbMyWz` · 페이지 `system workflow v2` (219:4509) · 섹션 `04.제품출하-사이드` (845:8641)
> 근거: 이슈 #44 · 사용자 확정 2026-07-29(2택 중 **행위 노드 작화**)

## 배경 — 무엇이 결함인가

`847:8878` SC4 「품질담당」에 배지 정본은 **웹**을 부여했는데(「재작업/폐기 판정 의뢰」) **부착된 행위 노드가 0개**다. 화면 매핑 규칙 §2의 「확인 필수 — 인접 행위 노드의 사용자 열로 흡수되었는지 대조」가 **작동할 수 없어**(흡수할 대상이 없어서) 04 전체에서 유일한 존치가 됐다. 현행 화면 `W-04-07`(신뢰도 추정)이 이 공백 위에 서 있다.

원인: H8이 「판정 → 03 도식」 경계 위임이라 **판정 자체는 04에서 그리지 않는데**, SC4가 04에서 하는 유일한 일인 「의뢰」도 작화되지 않았다. 액터만 세우고 행위는 03으로 넘어간 상태.

## 실측 — 04-S-C 현재 배치 (절대좌표)

```
y=24400  847:8775  Archive 반품·클레임 도착      x=-1580
y=24590  847:8788  User    완제품창고담당        x=-1580   [배지 웹 891:9016]
         847:8801  Desktop 반품입고 등록  (B4)   x=-1400   ← 행위
         847:8814  File    반품 기록      (B5)   x=-1220   ← 행위
         847:8913  Server  MES                   x=-970
y=24780  847:8827  Archive 불량창고 입고         x=-1580
y=24970  847:8878  User    품질담당  ← SC4       x=-1580   [배지 웹 891:9019]
         847:8888  hex     판정 → 03 도식  (H8)  x=-1400 (w192)
y=25160  847:8894  File    재고 재등록    (B6)   x=-1580
```

**핵심**: 사이드는 주 흐름이 **세로**(x=-1580 열), 행위 체인이 **가로**(오른쪽 전개·간격 180). SC4의 행위 자리(-1400)를 H8 hex가 점유하고 있다.

**배지 규약 확인**: `891:9019`가 SC4 기준 dx=+110·dy=−30 — 웹 지배 규약과 정확히 일치(#40에서 역산한 값 재확인).

## 변경 목록 (4건)

| # | 조치 | 대상 | 값 |
| --- | --- | --- | --- |
| 1 | **이동** | hex `847:8888` (H8) | x −1400 → **−1240** (Δ+160) — 행위 노드 자리 확보 |
| 2 | **신규** | Desktop 행위 노드 | `847:8801` clone → 라벨 **「판정 의뢰」** · x=**−1400**, y=**24970** (SC4와 같은 행) |
| 3 | **신규 커넥터** | SC4 → 신규 노드 | **초록** `{r:.243,g:.608,b:.294}` — 행위 체인 |
| 4 | **재연결** | 기존 커넥터 `848:8826` | `connectorStart`를 SC4 → **신규 노드**로 변경. **회색 유지**(위임) |

### 아이콘 유형 = Desktop (리더 판단)

배지가 **웹**(관리웹 화면 조작)이고 `W-04-07`이 화면이므로 Desktop이 맞다. **File은 두지 않는다** — 판정 기록은 H8로 03에 위임된 소관이라 04에 File을 만들면 소관 침범이다. 라벨은 「재작업/폐기 판정 의뢰」(12자)가 한도 10자를 넘으므로 **「판정 의뢰」**(5자).

### 간격 검산 (규칙: 노드 간 ≥40px)

- 신규 노드 −1400..−1280 · hex 신위치 −1240..−1048 → 간격 **40** ✓
- hex −1048 ↔ 스티키 `847:8923` −1000 → 간격 **48** ✓ (스티키 y 24760..25136, hex y 24985..25055 — 세로 겹침 있으므로 가로 간격이 관건)
- 배지 `891:9019` −1470..−1418 ↔ 신규 노드 −1400 → 간격 **18**(배지는 오버랩 표기 계층이라 노드 간격 규칙 비적용)
- 신규 노드 y 24970..25090 ↔ `847:8894` y 25160 → 간격 **70** ✓

## 검토자 전수 대조 기준

1. 신규 Desktop 1개(라벨 「판정 의뢰」)가 x=−1400·y=24970에 존재, 섹션 `845:8641` 자식
2. hex `847:8888`이 x=−1240으로 이동, 라벨·크기 불변
3. 체인이 **SC4 →(초록) 판정 의뢰 →(회색) H8** 순서로 연결 · SC4→hex 직결 커넥터 잔존 0
4. 겹침 0(위 검산 4쌍) · 배지 `891:9019` 위치 불변 · 04-S-C 그 외 노드 무변동

## 근거 1줄

이슈 #44의 2택 중 사용자가 **「행위 노드를 작화한다」**를 확정(2026-07-29). 이로써 `W-04-07`이 도식 앵커를 갖고, 화면 매핑 규칙 §2 「확인 필수」 대조가 작동 가능해진다.
