# 작업 지시서 — 01.자재창고관리-사이드 / 01-S-I (공급사 반품·폐기) **재배선 수정** (r1)

> 근거: research/2026-07-07-도식스펙-01-자재창고관리.md §2 사이드 E · figjam-workflow-diagram SKILL.md(범례 분기 규약·색 규약) · 사용자 승인 수정안(2026-07-13) · 캔버스 직접 조회(2026-07-13)
> 작성: workflow-designer · 2026-07-13
> 대상: FigJam `Sgy5IKD4aWuzpKtJAbMyWz` / 페이지 "system workflow v2"(219:4509) / 섹션 `01.자재창고관리-사이드`(219:5450)
> **성격: 신규 작화가 아니라 기존 01-S-I 밴드의 오배선 재배선.** 지시서에 없는 노드는 건드리지 않는다.

---

## 0. 배경 (왜 고치나)

01-S-I 밴드에서 User(E1. 물류담당, 219:5576)에서 **무라벨 선 4개**가 나가 "4택 선택"으로 오독됐다. 범례 분기 규약상 **조건 라벨 없는 선 = 공통 수행(모두 실행)**인데, 그중 2개(→반품완료·→폐기완료 직결)는 애초에 **잘못 연결된 선**이었다. 정상 흐름은 각 분기 체인의 끝 MES가 완료 축 상태로 닫혀야 한다.

수정 목표: ① 잘못된 직결 2선 제거 → 체인 끝 MES에서 완료로 재연결 ② 남는 2개 분기선에 **조건 헥사곤**을 붙여 "공통 수행" 오독 제거.

---

## 1. 섹션·좌표·클론 소스

- **섹션**: `01.자재창고관리-사이드` (id `219:5450`, x=2500, y=64, 3300×4300) — 기존, 신규 생성 없음.
- **좌표계**: 아래 모든 좌표는 **조회값과 동일한 공간**(섹션 219:5450 children 좌표계)이다. 신규 헥사곤은 **밴드 내 기존 헥사곤을 clone** 하므로 같은 부모·같은 공간에 생성된다 → 조회값 그대로 x,y를 set하면 정위치. (로컬/절대 논쟁 불필요 — 모든 대상 좌표를 이 공간의 노드 판독값에서 산정했다.)
- **신규 커넥터 부모**: 페이지 `219:4509`. `figma.createConnector()`는 currentPage에 append되므로 **currentPage를 219:4509로 맞춘 뒤** 생성한다(`figma.setCurrentPageAsync`).
- **클론 소스 (헥사곤)**: **`219:5592`** 「폐기 판정 시」(밴드 내 검정 조건 헥사곤, 150×62, fill 검정 {r:.117,g:.117,b:.117}·text 흰색·Inter Medium). 이걸 clone → 텍스트만 교체.
  - 참고: 사용자 원지시의 `219:4596`(Inter Medium)도 동일 스타일이나 **2줄 높이(116×86)**라 밴드(h=62)와 크기 어긋남. **밴드 정합을 위해 5592를 소스로 채택**(설계 판단 — §6 근거 표).
  - 폰트 미로드 방지: 텍스트 교체 전 `getStyledTextSegments(['fontName'])`로 Inter Medium 로드.

---

## 2. ① 제거 목록 (커넥터)

| 커넥터 ID | 현재 연결 | 색 | 조치 |
| --- | --- | --- | --- |
| `219:5598` | User E1(5576) → 반품 완료(5577) | 검정 | **remove()** — 잘못된 직결 |
| `219:5599` | User E1(5576) → 폐기 완료(5578) | 검정 | **remove()** — 잘못된 직결 |

- 제거 방법: `(await figma.getNodeByIdAsync('219:5598')).remove()` / 동일하게 5599.
- 그 외 어떤 커넥터도 제거하지 않는다.

---

## 3. ② 추가 목록

### 3.1 신규 커넥터 2개 (검정 주 흐름)

공통 스타일: `strokeWeight=4`, `connectorLineType='ELBOWED'`, stroke SOLID **검정 {r:.117, g:.117, b:.117}**, 양 끝 `magnet:'AUTO'`. (기존 5598/5599와 동일 스펙.)

| # | start(endpointNodeId) | end(endpointNodeId) | 색 | 의미 |
| --- | --- | --- | --- | --- |
| C1 | `219:5581` (MES · 반품 체인 끝) | `219:5577` (반품 완료) | 검정 | 반품 체인 종료 → 완료 축 상태 |
| C2 | `219:5589` (MES · 폐기 체인 끝) | `219:5578` (폐기 완료) | 검정 | 폐기 체인 종료 → 완료 축 상태 |

- 배선 확인용(참고, 그리지 않음): 반품 체인 = 반품등록(5579)→5580(반출기록)→**MES 5581**; 폐기 체인 = …→기타출고(5588)→**MES 5589**. 두 MES가 각 체인의 끝이 맞다(조회 확인).

### 3.2 신규 조건 헥사곤 2개 (검정)

- 소스 clone: `219:5592`. clone 후 **텍스트만** 아래로 교체하고 x,y를 set. fill/stroke/폰트/높이는 소스 그대로 둔다(교체 금지).
- 텍스트 교체 시 폭은 자동 맞춤된다(높이 62 유지). **배치는 CENTER 기준으로 지정** — `x = centerX − (width/2)`, `y = 1709` 로 놓는다.

| # | 문구(그대로) | 라벨 대상(어느 선) | CENTER(x, y) | top-left(폭≈150 기준) | 색 |
| --- | --- | --- | --- | --- | --- |
| H1 | `공급사 반품 시` | 커넥터 219:5600 (User E1 → 반품 등록, 초록) | **(2670, 1740)** | (2595, 1709) | 검정 |
| H2 | `판정 대기 시` | 커넥터 219:5605 (User E1 → 불량창고 입고, 검정) | **(2880, 1740)** | (2805, 1709) | 검정 |

- **문구 확정 근거는 §6.** H1 = 밴드 01-S-A·01-S-B의 「공급사 반품 시」와 통일(사용자 지시) + 스펙 반품 분기 정합. H2 = **폐기 '판정'은 03 위임 원칙** → "폐기 시" 금지, 불량창고 입고 후 판정 대기 상태를 라벨(스펙 §2 사이드 E: 불량창고 입고 → 판정 → 03 도식).
- 라벨 길이: H1 7자·H2 6자 (헥사곤 한도 ≤16자 충족).

**겹침 검증(설계자 산정 — 검토 기준):**
- H1 x[2595–2745]·y[1709–1771]: User(2720–2840,1550–1670) y 미겹침 / 반품등록(2500–2620,1810–1930) y 미겹침 / 반품·폐기대상(2480–2600,1550–1670) y 미겹침. **PASS**
- H2 x[2805–2955]·y[1709–1771]: User(2720–2840) y 미겹침 / 불량창고입고(2920–3040,1810–1930) y 미겹침 / 반품완료(2960–3080,1440–1560) y 미겹침 / 판정→03 헥사곤(2900–3090,1990–2052) y 미겹침. **PASS**
- H1 ↔ H2: x 갭 60px(2745↔2805). **PASS**
- 두 헥사곤은 User 아래·두 분기 첫 노드 위의 분기 포크 구간에 위치(= 분기 조건 라벨 정위치).

---

## 4. User(E1. 물류담당) 라벨 — 추가 판단 결과

- **조치 없음(현 상태 유지).** 원지시는 219:5576 라벨을 공백으로 봤으나 **캔버스 현재값 = `E1. 물류담당`** (조회 확인). 이는 스펙 §2 사이드 E 축의 「E1. 물류담당」과 **정확히 일치**하며, 형제 밴드(A1·B1·C1·D1 물류담당/작업자)와 알파벳 접두 규약도 일치. **근거 있음 → 그대로 유지, 신규 라벨 지정·창작 없음.**
- 검토자 확인 포인트: User 5576 라벨이 `E1. 물류담당`로 남아 있는지(변형·공백화 금지).

---

## 5. ③ 수정 후 기대 상태 (검토자 전수 대조 기준 · 정본)

### 5.1 User E1(219:5576)에서 **나가는 선 = 정확히 2개**

| 커넥터 | → 도착 | 색 | 조건 헥사곤 |
| --- | --- | --- | --- |
| 219:5600 | 반품 등록(5579) | 초록 | **H1 「공급사 반품 시」** |
| 219:5605 | 불량창고 입고(5586) | 검정 | **H2 「판정 대기 시」** |

- User E1로 **들어오는 선**: 219:5597(빨강, 반품·폐기 대상 5575 → User) — 유지.
- **무라벨로 나가는 선 0개**(오독 원인 해소). 5598·5599는 존재하지 않아야 한다.

### 5.2 완료 축 상태 incoming = 각 1개

| 완료 노드 | incoming 커넥터 | 출발 | 색 |
| --- | --- | --- | --- |
| 반품 완료(5577) | **C1(신규)** | MES 반품(5581) | 검정 |
| 폐기 완료(5578) | **C2(신규)** | MES 폐기(5589) | 검정 |

- 5577·5578로 들어오는 다른 선은 없어야 한다(기존 유일 incoming 5598·5599 제거됨).

### 5.3 밴드 전체 커넥터 인벤토리 (affected 노드 기준, 수정 후)

유지(변경 없음): 219:5597(반품·폐기대상→User, 빨강) · 219:5600(User→반품등록, 초록·**+H1**) · 219:5601(반품등록→5580, 초록) · 219:5602(5580→MES반품, 초록) · 219:5603(MES반품→5585 스티키, 초록) · 219:5604(MES반품→5582 ERP, 검정) · 219:5605(User→불량창고입고, 검정·**+H2**) · 219:5608(5588→MES폐기, 초록) · 219:5609(MES폐기→5593 스티키, 초록) · 219:5610(MES폐기→5590 Email, 초록) · 219:5611(불량창고입고→5591 판정→03, 회색).
신규: **C1** MES반품(5581)→반품완료(5577) 검정 · **C2** MES폐기(5589)→폐기완료(5578) 검정.
제거: **219:5598 · 219:5599**.
신규 헥사곤: **H1 「공급사 반품 시」 · H2 「판정 대기 시」**(둘 다 검정, clone from 5592).
라벨 유지: User 5576 = `E1. 물류담당`.

---

## 6. ④ 근거 표 (도식 비표기 — 검토·문서 정합용)

| 수정 요소 | 근거 |
| --- | --- |
| 무라벨 4선 오독 → 재배선 필요 | figjam-workflow-diagram SKILL.md 범례 분기 규약(조건 라벨 없는 선 = 공통 수행) + workflow-plan-brief §4(조건부 선 라벨 누락 = 설계 결함) |
| 5598·5599 제거(직결 오배선) | 스펙 §2 사이드 E 축: 「… → 분기 → 반품 완료 / 폐기 완료」는 **분기 체인**을 거쳐 완료에 도달. User 직결은 스펙에 없음 |
| C1 MES반품 → 반품 완료 | 스펙 §2 사이드 E 반품 분기(반품등록→반출기록→MES→…) 종료가 축 「반품 완료」로 닫힘 |
| C2 MES폐기 → 폐기 완료 | 스펙 §2 사이드 E 폐기 분기(…→기타출고→MES) 종료가 축 「폐기 완료」로 닫힘 |
| H1 「공급사 반품 시」 | 밴드 01-S-A·01-S-B 기사용 문구 통일(사용자 지시 2026-07-13) + 스펙 §2 사이드 E 반품 분기 = 공급사 반품(QA #15 — 재고차감+반출기록) |
| H2 「판정 대기 시」 | 스펙 §2 사이드 E 폐기 분기: 불량창고 입고(우선 입고) → **판정 → 03 도식** 위임. **판정 소관 = 03**(designer 판정 위임 원칙) → "폐기 시" 라벨 금지. 라벨은 폐기 결과를 예단하지 않고 판정 대기 상태를 표기 |
| User 5576 = 「E1. 물류담당」 유지 | 스펙 §2 사이드 E 축 「User E1. 물류담당」 + 형제 밴드 접두 규약(A1~E1). 근거 있음 → 창작 아님 |
| H2 대안 「불량·만료·손상」 미채택 사유 | 스펙 진입 3원 문구지만 IQC 불합격(반품 측)과 '불량' 중복 오독 여지 + 처분 축(반품=공급사 반품 시/폐기측=판정 대기 시)으로 H1과 병렬 명확화 우선 |

---

## 7. 작업 순서 (drawer 권장)

1. `figma.setCurrentPageAsync('219:4509')`.
2. 제거: 5598.remove() · 5599.remove().
3. 신규 커넥터 C1·C2 생성(검정·weight4·ELBOWED·magnet AUTO). — 여기까지 1콜.
4. 헥사곤 H1·H2: 5592 clone → 폰트 로드 → 텍스트 교체 → CENTER 기준 x,y set. — 다음 콜.
5. 각 단계 후 자가 스크린샷으로 겹침·연결 확인 후 다음 단계.
6. drawer 최종 보고에 **잘림 대비**: 생성한 C1·C2·H1·H2의 신규 노드 ID와 5598·5599 제거 여부를 명시.

---

## 개정 이력

| r | 날짜 | 변경 요지 |
| --- | --- | --- |
| r1 | 2026-07-13 | 최초 발행 — 01-S-I 재배선: 커넥터 2제거(5598·5599)·2신규(MES반품→반품완료·MES폐기→폐기완료)·조건 헥사곤 2신규(H1「공급사 반품 시」·H2「판정 대기 시」, clone 소스 5592). User 라벨은 현 「E1. 물류담당」 유지(스펙 근거·조치 없음). 캔버스 조회로 before/after 전수 인벤토리 확정. |
