# 개발팀 검토 요청 유형 분석 — API·UI/UX 전면 재검토 브리핑

> 작성 2026-08-28 · 대상 독자: 설계팀(API 계약·화면 스펙 재검토 수행자)
> 목적: **전면 재검토 전에 「어디서 무엇이 깨졌는지」를 유형으로 쥐고 들어간다.**
> 근거: `CREFLEINC/omf-mes` 인바운드 이슈 **전수 63건**(2026-08-05 ~ 08-28) 본문 정독.

---

## 0. 이 문서를 어떻게 쓰나

이 문서는 **과거 불만의 목록이 아니라 재검토의 점검 축**이다. 세 가지로 쓴다.

| 쓰임 | 어디를 보나 |
| --- | --- |
| 재검토 대상 화면·계약을 고를 때 | §2 집계 · §4 「같은 자리가 반복해서 돌아왔다」 |
| 한 화면·한 계약을 다시 볼 때 무엇을 물을지 | §5 **재검토 점검표** — 유형에서 역산한 질문 목록 |
| 「이 유형이 실제로 무슨 모습이었나」를 확인할 때 | §3 유형별 상세의 사례 · §6 전수 분류표 |

⚠ **이 문서는 스냅샷이다.** 미결 13건은 이 시점 기준이며, 재검토 착수 시 `gh issue list --repo CREFLEINC/omf-mes --state open` 으로 다시 센다.

---

## 1. 먼저 짚을 사실 하나 — 인바운드는 사실상 클라이언트 단일 채널이었다

| 발신 | 건수 | 비고 |
| --- | --: | --- |
| 클라이언트 개발팀 (T2·T3·T4) | **62** | `[client→uiux]` 41 · `[검토 요청]` 18 · 교차 프로젝트 폼 3 |
| 백엔드(데이터모델·서버) | **1** | #283 뿐 |

**백엔드가 조용한 것은 계약이 백엔드에게 맞아서가 아니다.** #283이 그 증거다 — 서버는 엄격한 파서(`redocly`)를 쓰는 바람에 계약 파일의 중복 키를 **우연히** 발견했고, 그 시점까지 logistics 89개 오퍼레이션의 타입 검사가 비어 있었다. 즉 **백엔드 쪽 계약 검증 경로가 아직 얇다.** 재검토 때 「클라이언트가 안 물어본 자리 = 문제없는 자리」로 읽으면 안 된다.

**시기 구분**: `[client→uiux]`(08-19~)가 먼저 돌다가 08-25 `multi-agent-team-workflow-v2` 도입과 함께 `[검토 요청]`(#232~)이 붙었다. 두 접두는 지금 공존하며 **내용상 차이는 없다** — 같은 종류의 문제가 같은 빈도로 온다.

---

## 2. 한눈에 — 유형별 집계

63건 중 **API 계약 관련 55건**, 계약 밖(디자인 시스템·단말·인프라) 8건.

| 유형 | 건수 | 한 줄 정의 |
| --- | --: | --- |
| **A. 계약에 자리가 없다 — 결손** | **20** | 스펙이 요구한 값을 계약이 실어 보내거나 내려 줄 자리가 아예 없다 |
| ├ A1 파생·집계값 | 7 | 「서버가 계산한다」고 정해 놓고 담을 필드가 없다 |
| ├ A2 조회 축 | 9 | 필터·정렬·기간·검색 규칙이 없다 |
| ├ A3 표시용 필드 | 2 | id만 오고 이름이 없어 N+1이거나 못 그린다 |
| └ A4 부분 수정 의미론 | 2 | `null`을 표현할 수 없어 「해제」를 보낼 수 없다 |
| **B. 값은 있는데 무슨 값인지 모른다** | **9** | `enum`을 안 박고 공통코드로 넘겼는데 그 그룹을 가리킬 방법이 없다 |
| **C. 쓰기 규약이 짝이 안 맞는다** | **10** | 잠금·트랜잭션·귀속의 세 축 |
| ├ C1 낙관적 잠금 | 4 | 스펙은 요구, 상태전이 오퍼레이션엔 `If-Match`/`ETag`/`409`가 없다 |
| ├ C2 트랜잭션 경계 | 3 | 스펙은 「한 트랜잭션」, 계약은 N 호출 |
| └ C3 귀속·게이팅 출처 | 3 | 화면이 알 수 없는 값을 필수로 받는다 |
| **D. 정본이 서로 다른 말을 한다** | **9** | 스펙·요구서·계약 셋이 같은 것을 다르게 적었다 |
| **E. 계약 파일·파이프라인 결함** | **4** | 필드가 아니라 계약 배포 자체가 막혔다 |
| **F. 화면 소관·진입 경로** | **3** | 계약엔 경로가 있는데 누가 쓰는 화면인지가 없다 |
| **G. 계약 밖** | **8** | DS·단말 실물·인프라·배포 |

> 건수는 **주 유형 기준**이다. 한 이슈가 여러 유형에 걸치는 경우가 많다 — #245는 A·B·C를 한 번에 갖는다.

---

## 3. 유형별 상세

각 항목은 **정의 → 왜 생기나(뿌리) → 사례 → 재검토 지침** 순이다.

---

### A. 계약에 자리가 없다 — 결손 (20건)

가장 큰 덩어리이자 **재작업 비용이 가장 큰** 유형이다. 화면 구조가 그 값을 전제로 그려져 있기 때문에, 값이 없으면 구획 하나가 통째로 비거나 만들어지지 않는다.

#### A1. 파생·집계값 — 「서버가 계산한다」고 정해 놓고 담을 자리가 없다 (7건)

**뿌리**: 화면 스펙이 세운 조항(**L-1** 「화면이 목록을 받아 세면 안 된다」, **L-2** 「달성률·지연 같은 파생은 서버가 낸다」)과 계약의 응답 형태(`{ items, page }`)가 **한 번도 대조되지 않은 채** 각각 확정됐다. 조항은 화면 스펙에서 태어나고 응답 형태는 계약에서 태어나는데, 둘을 잇는 검사가 없었다.

**사례**

- **#265 (W-02-08 W/O 진행현황)** — 스펙 §4-B가 요약 카드를 「필터 전체 기준」으로 정의하고 L-1이 「응답이 요약과 목록을 나눠 담는다」고 못박았는데, 계약 200 응답은 `{ items, page: {page, size, total} }` 뿐이다. **화면이 알 수 있는 건 전체 건수 하나**고 상태별 6종·합계·달성률은 받을 방법이 없다.
  - ⭐ 같은 문장이 낳은 둘 중 **달성률은 `WorkOrderProgress.achievementRate`로 제대로 들어갔고 지연만 빠졌다**(`지연`·`delay`·`overdue`·`isLate` 전수 0건). 조항 → 필드 전개가 **부분적으로만** 이뤄진 것이 이 유형의 전형이다.
- **#232 (W-04-02 출하 예정 목록)** — §4-B의 「요약」 구획(작업지시 건수·요청/배정/출하 합계·미배정·검사대기·피킹미완)이 §3 확정 레이아웃에 그려져 있는데, 계약 전체에 「요약」·`summary`·`aggregate` 키워드가 **0건**.
- **#253 (W-03-10 처분 판정)** — 스펙이 「⭐ 서버가 계산한다(L-2)」로 못박은 파생값 3건(남은 수량·목록 수량·판정 진행)이 전부 응답에 없다. 스펙 §3 말미가 「**②의 「남은 수량」이 이 화면의 핵심이다**」라고 적은 그 값이다.

**재검토 지침**
- 화면 스펙의 **L-조항 전수를 뽑아** 각 조항이 요구하는 필드가 계약에 실재하는지 1:1로 대조한다. 조항이 한 문장에 둘을 말하면(「달성률·지연」) **둘 다** 센다.
- 「필터 전체 기준」이라 적힌 표시값은 예외 없이 **응답에 별도 `summary` 블록**이 필요하다. 목록 응답에 얹을지 별도 경로로 뺄지는 그 다음 문제다.
- 페이지네이션이 있는 목록 위에 집계를 그리는 화면은 **설계 시점에 이미 이 위험을 안고 있다.** 화면 스펙 §3 레이아웃에 요약 구획을 그리는 순간 계약 요구를 같이 적는다.

#### A2. 조회 축 — 필터·정렬·기간·검색 규칙이 없다 (9건)

**뿌리**: 화면 스펙 §3 레이아웃에 필터 컨트롤을 그릴 때, 그 컨트롤이 **어느 질의 파라미터로 나가는지**를 적지 않았다. 요구서 §3의 액션표는 「메서드 + 경로」까지만 적고 질의 축을 적지 않는다.

**사례**

- **#202 (W-02-03 4M 자원배정)** — `plannedShiftId`가 필수인데 **교대를 조회할 경로도 `Shift` 스키마도 계약에 없다.** `shiftId`는 W/O·작업세션의 참조 숫자로만 나타난다. 착수 통지 §3의 「필요한 오퍼레이션이 모두 있다」 체크와 정면으로 어긋났다.
- **#170 (W-01-01 검사 대기 큐)** — 스펙 §3이 필터를 「품목·공급사」로 적었는데 계약 질의에 **공급사가 없다**. 같은 이슈에서 기간의 필수 여부가 설명·스키마·레이아웃 셋이 다 다르게 말하는 것도 함께 나왔다(D 유형 중복).
- **#254 (P-02-03 스캔 조회, 미결)** — `q`의 검색 규칙(정확/부분·대소문자)이 계약에 없다. 계약이 `q`에 대해 적은 것은 「LOT 번호·외부 식별자 검색」 한 줄이 전부고 `GET /mdm/molds`의 `q`엔 설명조차 없다.
  - ⚠ 클라이언트가 짚은 위험이 정확하다 — 「여러 건은 화면이 막을 수 있지만 **우연한 한 건은 정상처럼 담기고 그대로 계보에 들어간다.**」 투입 기록은 정정이 아니라 새 기록으로만 고칠 수 있다(B-3).
- **#269 (P-02-07 완료 LOT 목록, 미결)** — 「완료 / 미달 마감」을 고를 축이 계약의 **두 상태 축(`statusCode` 품질 판정 / `lifecycleStatusCode` 생명주기) 어디에도 없다.** 화면 왼쪽 구획 전체가 여기 걸려 화면이 서지 않는다.

**재검토 지침**
- 화면 스펙 §3에 그린 **모든 필터·정렬 컨트롤 옆에 질의 파라미터 이름을 병기**한다. 이름이 안 적히면 그 컨트롤은 아직 설계가 안 된 것이다.
- **페이지네이션이 있는 목록에서 클라이언트 측 필터링은 항상 오답이다** — 거른 뒤 개수가 페이지 크기와 달라진다. #196·#225·#245③·#271①이 전부 같은 형태로 걸렸다. 필터를 그렸으면 서버 축을 반드시 연다.
- 검색 칸(`q`)은 **정확/부분·대소문자·다건 처리 셋을 계약 설명에 적는다.** 스캔 입력 화면에서는 이것이 안전 요구다.

#### A3. 표시용 필드 — id만 오고 이름이 없다 (2건)

**사례**
- **#203 (W-05-07 수집채널 매핑)** — `CollectionChannel`이 `inspectionItemId`만 내려 주고 이름도 소속 버전도 없는데, `inspection_item_spec`을 읽는 길은 `GET /quality/inspection-plan-versions/{versionId}/items` 하나뿐이다. **`inspectionItemId`에서 `versionId`로 가는 길이 없어** 스펙 §4의 열을 그릴 수 없고, 그에 의존한 「옛 Rev 경고」(§6)도 함께 성립하지 않는다.
- **#282 (W-06-07 창고, 미결)** — 거래처가 **등록·수정 본문엔 있고(외부창고면 필수) 조회 응답엔 없다.** 수정 화면이 지금 값을 채울 수 없어 사용자가 매번 다시 골라야 한다.

**재검토 지침**
- **「저장에 필수인데 조회로 못 읽는 필드」를 계약 전수에서 뽑는다.** 이건 기계적으로 검사 가능하다 — `{X}Create`/`{X}Update`의 `required`에 있는데 `{X}` 응답 스키마에 없는 프로퍼티.
- 참조 id를 응답에 실을 때는 **표시명을 같이 실을지, 배치 조회 경로를 열지**를 그 자리에서 정한다. 안 정하면 화면은 N+1로 가거나 열을 포기한다.

#### A4. 부분 수정 의미론 — 「해제」를 보낼 수 없다 (2건)

**사례**: **#200**(계획 라인·비고 초기화) · **#201**(4M 자원 개별 해제). 둘 다 같은 형태다 — 상세 응답과 수정 요청의 필드가 모두 `optional number`이고 `null`을 허용하지 않아, **값을 바꿀 수는 있어도 「미지정」으로 되돌릴 수 없다.**

관련 파생: **#188⑤**·**#282** — PUT이 전체 교체라 화면이 보지도 고치지도 않는 필드를 **받은 값 그대로 되돌려 보내야** 지워지지 않는다.

**재검토 지침**
- 부분 수정(PUT/PATCH)을 가진 자원마다 **「이 필드를 비울 수 있는가」를 필드별로 판정**하고, 비울 수 있으면 `nullable`을 명시한다. 「생략 = 유지」와 「null = 해제」를 계약 문면에 적는다.
- PUT이 **전체 교체인지 부분 수정인지**를 오퍼레이션 설명에 못박는다. 전체 교체면 화면이 모르는 필드를 되돌려 보내야 하는데, 이건 화면이 스스로 알 수 없다.

---

### B. 값은 있는데 무슨 값인지 모른다 — 코드값 원천 (9건)

**뿌리**: `enum`을 일부러 안 박은 것은 **옳은 결정이었다**(G-2·G-6 — 값 목록은 공통코드가 갖고 늘 수 있다). 문제는 그 결정이 **「그러면 화면은 어디서 받나」를 같이 정하지 않은 채** 내려졌다는 것이다. 계약이 `codeGroupCode`를 적어 둔 자리는 전체에서 15건뿐이다.

**세 갈래**

| 갈래 | 사례 |
| --- | --- |
| **그룹코드 이름을 계약이 안 적었다** | #185(`EQUIPMENT_STATUS`) · #188(검교정 주기 단위) · **#272**(미결 — 클라이언트가 `WORK_ORDER_STATUS`로 **가정하고** 진행 중) |
| **값 목록 자체가 미확정** | #227(`targetLotStatusCode`에 「보류」에 해당하는 값이 없다) · #247③(W/O 상태 문자열) · #252①(`consumptionTypeCode`) · #259(긴급 = ?) |
| **조회 경로가 그것을 가리킬 수 없다** | **#179** — `GET /mdm/code-values`가 정수 `codeGroupId`만 필수로 받아 **그룹 코드 문자열로 부를 수 없고**, `GET /mdm/code-groups`엔 `q`(검색)만 있어 정확히 집을 수 없다 |

**가장 위험한 사례 — #259 (W-02-07 긴급 W/O 발행)**

긴급을 뜻하는 `work_order_type_code` 값이 정해져 있지 않은데, 화면이 할 수 있는 건 둘뿐이고 **둘 다 틀린다**:

| | 결과 |
| --- | --- |
| 값을 지어낸다 | 서버가 거부하거나, 더 나쁘게는 아무도 모르는 코드로 받아들인다 |
| 필드를 뺀다 | 서버 기본값 `NORMAL`이 붙는다 — **화면은 「유형: 긴급」이라 적어 놓고 양산 W/O를 만든다** |

두 번째가 특히 나쁘다. **오류가 안 나서 아무 데서도 안 보이고**, 그렇게 만들어진 W/O는 `P-02-12`(긴급 현장 투입) 목록에 뜨지 않는다. 화면 스펙 §8은 「차단 0건 — 지금 지어도 된다」로 닫혀 있었는데, 이 항목은 **읽는 화면(`P-02-12`)의 미결표에만 달려 있고 쓰는 화면(`W-02-07`)에는 없었다.**

**재검토 지침**
- **`Code`로 끝나는 모든 프로퍼티에 대해 「값 목록의 정본이 어디인가」를 세 갈래로 판정한다** — ⓐ 계약 `enum` ⓑ 공통코드 그룹(그러면 `codeGroupCode=…`를 설명에 적는다) ⓒ 아직 미정(그러면 **미결 대장에 등재**한다).
- **미결은 「읽는 화면」이 아니라 「쓰는 화면」에 단다.** 값을 만드는 자리가 막히면 읽는 자리는 영원히 빈다.
- 미결 등급에 **「운영 차단」**을 둔다. 「짓는 것」은 되는데 「쓰는 것」이 안 되는 항목이 「차단 0건」으로 닫히는 것을 막는다(스펙 §9-2가 이미 예고했으나 권한 하나만 보고 있었다).
- `code-values` 조회를 **문자열 그룹코드로 한 걸음에** 부를 수 있게 한다(#179 권고안 A). 코드값을 쓰는 화면은 계속 늘고, 두 걸음이면 그 조회 실패가 매 화면의 주 기능을 막는다.

---

### C. 쓰기 규약이 짝이 안 맞는다 (10건)

#### C1. 낙관적 잠금 — 필수로 받아 놓고 출처를 선언하지 않았다 (4건)

**뿌리**: 「저장 충돌 보호」가 화면 스펙(B-1)에서 요구로 확정되고 계약에서 헤더로 구현될 때, **`PUT`에는 붙었는데 `POST …:action` 형태의 상태 전이에는 안 붙었다.** 형제 자원끼리도 갈렸다.

**사례**
- **#205 / #208** — `PUT /production/work-orders/{id}`는 `Idempotency-Key`와 `If-Match`를 **둘 다** 요구하는데, 같은 자원의 상태 전이 `:release`·`:close`는 `Idempotency-Key`만 받는다. 두 관리자가 같은 W/O를 동시에 확정할 때 화면이 ETag를 실을 자리가 없다.
- **#210 (W-05-01 운영 정책)** — `ETag`·`If-Match`·`409`가 **셋 다** 없다. 형제 자원(`/mdm/molds`·`/maintenance/collection-channels`)은 셋을 다 갖고 있어 여기만 빠진 것이 눈에 띈다.
  - ⚠ 클라이언트가 짚은 실제 피해: 정책은 여러 사람이 같은 표를 보고, 이 화면이 정하는 것은 타발수 계산의 입력이다. **두 사람이 각자 고치면 나중 사람 값으로 조용히 덮이고 앞 사람은 자기 값이 들어간 줄 안다.**
- **#190 (W-03-02)** — 토큰 원천이 문서마다 다르다. 내부 설명은 `trace.lot.version_no`, `IfMatchVersion` 공개 설명은 「같은 리소스의 상세 GET ETag」, 보류 상세 GET의 ETag 설명은 「이 보류 행의 `version_no`」인데 **화면 정본상 `lot_hold`에는 `version_no`가 없다.**

**재검토 지침**
- **자원 단위로 잠금 정책을 통일한다.** 한 자원의 `PUT`이 `If-Match`를 요구하면 그 자원의 모든 상태 전이도 요구해야 한다. 예외를 두려면 「왜 이 전이는 서버가 다른 방식으로 동시성을 보장하는가」를 계약에 적는다.
- **토큰의 출처를 오퍼레이션마다 한 문장으로 못박는다** — 「이 요청의 `If-Match`는 `GET {경로}` 200 응답의 `ETag`다」. 값을 필수로 받으면서 출처를 안 적으면 구현이 반드시 막힌다(#178이 `X-Worker-No`에서 같은 형태로 걸렸다).
- 스펙 §5-5 상태 목록에 「충돌(B-1)」이 있으면 **계약에 `409`가 있는지 짝으로 확인**한다. 없으면 둘 중 하나가 잘못이다.

#### C2. 트랜잭션 경계 — 스펙은 「한 트랜잭션」, 계약은 N 호출 (3건)

**사례**
- **#258 (W-02-07 긴급 W/O 발행)** — 「발행·배포」 한 액션이 계약상 **세 호출**이다(`POST` → ETag 확보용 `GET` → `:release`). 스펙 §6은 「⛔ 전체 롤백 — 한 트랜잭션(B-8)」이고 착수 통지는 「부분 성공을 화면이 만들어 내지 마세요」인데, **B-8의 보장은 ①의 안쪽**이고 ①과 ③ 사이는 그 보장 밖이다.
  - 클라이언트 결론이 정확하다 — 「지킬 수 있는 것은 **「부분 성공을 성공이라고 말하지 않는다」**까지다.」 그리고 되돌릴 수단도 없다(스펙 §5-6 액션표에 취소가 없고, `:cancel`을 끌어다 쓰는 건 화면이 보상 트랜잭션을 발명하는 것).
- **#245④ (P-01-01)** — 스펙 §5-2는 「한 트랜잭션 — ① `trace.lot` 생성 ② `document_issue_log` 발행 기록」인데 계약은 두 호출이다. **「LOT은 생겼는데 발행 기록이 실패」한 상태의 처리가 스펙 §6에 없다.**
- **#199 (W-02-02)** — `:confirm`이 W/O를 자동 생성하는지, 클라이언트가 공정 수만큼 POST하는지가 안 적혀 있다. 후자면 부분 성공 복구·중복 방지·의존 생성 경로가 필요한데 bulk 전개 API가 없다.

**재검토 지침**
- 화면 스펙이 **「한 트랜잭션」·「전체 롤백」·「부분 성공 금지」**를 적은 액션을 전수로 뽑고, 그 액션이 계약상 **몇 호출인지** 센다. 2 이상이면 셋 중 하나를 택한다 — ⓐ 서버가 한 호출로 합친다 ⓑ 보상 경로를 계약에 연다 ⓒ **스펙 문면을 「성공이라고 말하지 않는다」로 낮춘다.**
- ETag 확보용 `GET` 왕복이 호출 수를 늘리는 자리를 눈여겨본다. #258③의 지적처럼 **생성 응답에 ETag를 실으면 왕복 자체가 없어진다.**

#### C3. 귀속·게이팅 — 화면이 알 수 없는 값을 필수로 받는다 (3건)

**뿌리**: 관리웹은 계정 토큰이라 서버가 인증 주체에서 「누가 했나」를 안다. **현장 단말·모바일은 단말 토큰이고 로그인이 없다.** 이 비대칭이 계약에 반영되지 않았다.

**사례**
- **#173 (W-01-01)** — `inspectorId`가 필수인데 **로그인 사용자와 작업자가 다른 번호 공간**이다(`mdm.worker.worker_id` ≠ `app.app_user.app_user_id`, 주석이 「작업자↔입력자 구분」). 세션은 `userId`만 준다. 그대로 보내면 **품질 감사 기록에 엉뚱한 사람이 검사자로 박히고 값이 그럴듯한 정수라 아무도 눈치채지 못한다.**
  - 이 회신(「서버가 인증 주체에서 푼다」)이 곧바로 **#178**(`X-Worker-No`가 계약 7벌에 0건 — 현장·모바일 쓰기 37경로가 「누가 했나」를 못 싣는다)을 드러냈다.
- **#246 (P-02-03)** — 스펙이 `can_input_material`을 진입 게이팅으로 쓰는데 계약 `TerminalProcess`의 8플래그에 **없다**(`canScanIn`·`canScanOut`·`canRegisterResult`·`canInspect`·`canPrintLabel`·`canMoveStock`·`canHold`·`canApprove`). 계약에서 게이팅은 **쓰기 시점 403으로만** 표현돼 있다.
- **#245② (P-01-01)** — `can_print_label`을 내려 주는 경로는 관리 화면용 `GET /mdm/terminals/{terminalId}/processes` 하나인데, **POP이 자기 `terminalId`를 아는 경로가 계약에 없다.** F-6(「판정할 수 없음」을 「통과」로 처리하지 않는다)에 걸려 통과시키면 위반이고 막으면 창고 단말은 항상 막힌다.
- **#271② (미결)** — `shiftId`가 필수인데 교대는 공장 단위 정의라 `plantId`가 필요하고, **`plantId`의 출처가 화면에 없다.** 어느 교대에도 안 드는 시각의 처리도 없다.

**재검토 지침**
- **「인증 주체에서 파생되는 값」을 화면이 보내게 되어 있는 자리를 전수로 뽑는다** — 검사자·작업자·단말·공장·교대. 원칙은 **서버가 푼다**로 통일하고, 서버가 풀 수 없는 셸(단말 토큰)에는 **그 근거를 실을 헤더를 계약에 선언**한다.
- 화면 스펙이 **선제 게이팅(비활성 + 사유)**을 요구하면, 그 플래그를 **런타임에 자기 기준으로 읽을 경로**가 계약에 있는지 확인한다. 없으면 스펙을 「403만 처리」로 낮추거나 경로를 연다 — 둘 중 하나를 골라야지 방치하면 F-6 위반이 된다.
- **셸이 소유하는 값(단말·공장·설비)의 목록과 전달 경로**를 한 곳에 정의한다. 화면마다 「이 값은 어디서 오나」가 반복해서 올라왔다.

---

### D. 정본이 서로 다른 말을 한다 (9건)

**뿌리**: 화면 스펙 · API 요구서 · OpenAPI 계약 · 공유계약 **넷이 서로를 인용하는데 갱신이 동시에 일어나지 않는다.** 특히 스펙이 개정되면 계약과 요구서가 따라가지 않는다.

**네 축과 사례**

| 축 | 사례 |
| --- | --- |
| **스펙 ↔ 계약** | **#251** — 스펙 §5-2가 2026-08-18에 「검사 기준이 없어도 검사할 수 있다」로 **명시적으로 철회·정정**했는데, 같은 스펙 §6 예외표는 §5-2를 가리키면서 **철회 이전 내용**(「진입 불가」)을 그대로 두었고 계약 `InspectionRequest.inspectionPlanVersionId`도 여전히 `required`다. 게다가 §5-2가 예고한 ⚠ 변경 통지가 클라이언트 저장소에 오지 않았다. |
| | **#235** — 출하검사 `shippingInspectionStatusCode`가 3값(`NOT_REQUIRED`·`PENDING`·`PASSED`)인데 롤업 원천인 `overallJudgmentCode`는 3값(`ACCEPTED`·`REJECTED`·`HELD`)이다. **불합격과 보류가 `PENDING`으로 뭉쳐** 운영자가 「아직 안 한 것」과 「떨어진 것」을 구분할 수 없다. |
| | **#206** — 화면은 「LOT 크기(슬롯당 계획 수량)」를 입력받는데 계약은 `lotSlotCount`(슬롯 개수)만 받는다. 요구서가 이 둘을 같은 값처럼 매핑해 **수량과 개수가 충돌**한다. |
| **요구서 ↔ 계약** | **#222** — 순서 재배치 필드가 요구서는 `displayOrder`, 계약은 `sequenceNo`(계약 전체에서 `displayOrder`를 갖는 건 `CodeValue` 계열 셋뿐). |
| | **#230** — #192 회신에서 확정한 서버 보장 3가지(뿌리 기준 페이지네이션·같은 페이지 동거·사슬 내부 순서)가 **계약 문면에서 사라졌다.** 회신으로 정한 것이 계약에 안 착지한 형태. |
| **계약 내부 자기모순** | **#257②** — `InspectionItemSpec.automaticJudgment`가 boolean **기본 true**인데, 같은 계약의 `InspectionMeasurementInput.judgmentCode` 설명은 「⛔ 규격 밖이어도 **자동 불합격이 아니다** — 사람이 판정한다」. |
| | **#170①** — `requestedFrom`이 **설명**엔 「기간 필수 — L-3」인데 **스키마에 `required`가 없고** 목 서버로 기간 없이 호출하면 200이 온다. 게다가 스펙 §3 레이아웃엔 기간 컨트롤이 없다 — **셋이 다 다르다.** |
| **화면 스펙 ↔ 공유계약** | **#164**(L-6 각주가 W-CO-03을 실시간 예외로 지목하나 스펙·계약은 자동 갱신 없음) · **#229**(필터 기본값 「전체」 ↔ L-7 「IQC/PQC/OQC를 합치지 않는다」) · **#209**(잔량 처분이 미달일 때만인데 계약은 항상 필수) |

**재검토 지침**
- **정정·개정이 일어난 자리를 우선 훑는다.** #251처럼 **같은 문서 안에서 §5-2는 고쳐지고 §6은 안 고쳐진** 형태가 실제로 나왔다. 개정 시 「이 절을 인용하는 다른 절」을 같이 검색한다.
- **계약 `description`과 스키마 제약(`required`·`enum`·`nullable`)의 불일치는 기계 검사가 가능하다.** 「필수」·「반드시」가 설명에 있는데 `required`에 없는 자리를 뽑는다.
- **enum의 값역이 롤업 원천을 다 담는지** 확인한다(#235). 상태를 다른 상태에서 파생시키는 필드는 원천의 값 개수 이상이어야 한다.
- 회신으로 확정한 내용은 **계약 문면에 착지시키고 그 사실을 회신에 적는다**(#230이 정확히 이 지점에서 재요청으로 돌아왔다).

---

### E. 계약 파일·파이프라인 결함 (4건)

**개별 필드가 아니라 계약 배포 자체를 막았다는 점에서 파급이 가장 컸다.**

- **#166 — 동명 스키마 3건의 형태 충돌.** `ConflictResponse`·`ErrorItem`·`LotHold`가 계약마다 다른 형태다. `ConflictResponse`는 아예 **두 계열**이다(판별 필드가 `conflictCause` vs `code`). 클라이언트 병합기가 여기서 멈춰 **`production-02`·`quality-03`·`shipment-04` 세 벌이 경로 66·스키마 97 통째로 편입 0건**이 됐고, **착수 통지를 이미 받은 25화면이 대기**했다.
- **#283 — `x-internal-note` 키가 한 객체 안에 두 번 (미결).** JSON 명세가 중복 키 동작을 정하지 않아 구현마다 갈린다. 관대한 파서(`JSON.parse`)는 앞엣것을 조용히 버리고, 엄격한 파서(`redocly`)는 거부한다. **결과: 서버는 타입 생성 실패, 클라이언트는 빌드가 안 깨지는 대신 그 메모를 영영 못 본다.** 하필 그 메모가 입하 등록 화면(M-01-01)을 만들 팀이 가장 알아야 할 결손(OCR 인식 결과를 담을 자리가 없다)이었고, **적힌 순간부터 3일간 어떤 도구에도 보이지 않았다.**
- **#167 — 정본 push 지연.** 08-17 이후가 원격에 없어 새 팀이 받은 정본이 클라이언트 반영본보다 낡았다. 그 clone으로 타입을 재생성하면 이미 이행한 ⛔ 변경 통지가 **되돌아간다**(`samplingRatio` → `samplingQty`, typecheck 13건 파손). 05설비툴 계약 부재로 W-05 계열 13화면이 함께 막혔다. **「착수 가능」 81건 중 실제 착수 가능은 6건**이었다.
- **#92 — `description`의 공개 저장소 유출.** `openapi-typescript`가 `description`을 JSDoc으로 그대로 옮겨, 비공개 문서 경로 57·설계 규칙 요약 193·미결 표기 48이 공개 저장소에 병합됐다. **사람이 옮긴 게 아니라 자동 생성이라 걸러지지 않았고, 계약이 갱신될 때마다 계속 유입된다.**

**재검토 지침**
- **계약 CI에 기계 검사를 넣는다.** 이 넷 중 셋은 짧은 스크립트로 발행 전에 잡힌다 — ⓐ JSON 중복 키(#283 본문에 파이썬 8줄로 제시돼 있다) ⓑ 계약 간 동명 스키마 형태 일치(`description`·`example`·`title` 제외 비교) ⓒ `description`의 비공개 경로·규칙 요약 패턴.
- **공통 스키마(`ConflictResponse`·`ErrorItem`·`PageMeta` 등)를 한 벌로 통일**하거나 이름을 갈라 둔다. 7벌로 나눈 계약이 공통 부분을 각자 정의하는 구조 자체가 이 사고의 원인이다.
- **`description`은 공개된다고 전제하고 쓴다.** 근거는 식별자만(`W-06-07 §5-3 · B-1`), 규칙 요약과 미결 표기는 넣지 않는다. `x-internal-note`로 분리하되 **그것도 생성물에 실리는지 확인**한다.
- 배포 규율 — **push 지연은 「팀마다 다른 정본」을 만들고 그건 되돌리기가 가장 어렵다.**

---

### F. 화면 소관·진입 경로 (3건)

계약에는 경로가 다 있는데 **누가 그것을 쓰는 화면인지**가 안 정해진 경우.

- **#186 / #220 (W-05-12)** — 점검 항목 마스터가 이 화면 범위인지, 범위라면 **만드는 자리**가 어디인지. 화면 스펙 §4-C·§5-1·§7과 계약은 「있다」인데 **착수 통지엔 한 번도 안 나온다.** API 요구서는 「마스터 CRUD — W-05-12가 **쓴다**」라고 적었는데 「쓴다」와 「관리한다」가 같은 뜻인지에서 갈렸다.
- **#194 (W-03-09)** — 결재함에서 이 화면으로 들어가는 정본 필드가 없다. 결재함은 `target.screenId`로 **결재 대상 원문 화면**을 여는데, W-03-09의 「부적합 상세로 나간다」가 같은 축을 써서 **같은 값으로 「결재함 → W-03-09」 진입을 표현할 수 없다.**
- **#257① (P-02-13)** — 스펙은 이 화면이 「대상이 이미 정해진 채로」 열린다고 정하고 §5-9 액션표에 조회·필터를 두지 않았는데, **그 진입 경로가 될 작업 화면이 아직 없다.**

**재검토 지침**
- **통합 IA에서 「이 경로를 쓰는 화면」과 「이 화면이 소유하는 마스터」를 양방향으로 채운다.** 계약 경로 309개 각각에 소비 화면이 하나 이상 붙는지 본다 — 안 붙은 경로는 소관 미정이다.
- **화면 전이(진입·이탈)를 스펙에 적을 때 그 전이를 실어 나를 계약 필드를 같이 적는다.** #194는 전이는 확정인데 그것을 표현할 필드가 없어서 막혔다.

---

### G. 계약 밖 (8건, 참고)

API 재검토 대상은 아니지만 **UI/UX 재검토에는 직접 걸린다.**

- **#275 (미결)** — 실기(POINTMOBILE PM95) 뷰포트가 **432 × 792 CSS px**인데 스펙 §1은 `360×720dp`로 적었다. **레이아웃 세로 검산의 기준값이 통째로 달라진다.** 함께: 보류 표시 자리(스펙은 맨 아래, 구현은 위 — 아래 두면 스크롤 밖으로 밀림) · 앱바에 화면 제목·뒤로 가기를 넘길 장치가 셸에 없음(모바일 19화면 공통) · **운영 서버 HTTPS 여부**(평문이면 모바일이 통신 자체를 못 함).
- **#277 (미결)** — POP에 **문자 입력 수단이 없다.** 숫자 키패드만 확정돼 있고 OS 키보드는 안 쓰기로 했는데, `P-02-13`의 자유 입력 갈래와 POP 목록 검색이 그것을 필요로 한다.
- **#243** — DS 고정 커밋에 72px(`2xl`)이 없었다. 핀 `91c233b`(00:17)이 72px을 넣은 `8c4a775`(00:26)를 **9분 차로 놓쳤다.** (#269③·#271③에서 이후 해소 확인)
- **#264 / #266 (미결)** — 스펙 §3 레이아웃과 §5-1 표기 규칙의 어긋남 · §6과 공유계약 D-2의 스캔 오류 처리 충돌. **#266은 스캔 화면 16건의 기준**이 되는 자리다.
- **#278 / #279 / #165** — POP 단말 실물 OS(Windows 10 LTSC 1809, 문서는 Windows 11 IoT) · 모바일 배포·서명 키 정책 · 어포던스 표기 재량 범위.

**재검토 지침**
- **화면 스펙 §1의 단말 전제를 실측값으로 갱신하고, 레이아웃 예산을 다시 계산한다.** 지금 모바일 19화면의 세로 검산이 낡은 기준 위에 서 있다.
- **셸이 소유하는 것(앱바·키보드·스캐너 어댑터)의 정본 장을 세운다.** `M-CO-01`·`P-CO-01`이 그 자리로 지목돼 있으나 앱바 구성이 정의돼 있지 않다.

---

## 4. 가로지르는 관찰

### ① 같은 자리가 반복해서 돌아왔다

| 화면 | 회차 | 이슈 |
| --- | --: | --- |
| W-05-12 설비·설비그룹 마스터 | 5 | #185 → #186 → #188 → #220 → #222 |
| W-03-05 검사실적 조회 | 4 | #192 → #228 → #229 → #230 |
| P-02-03 자재 투입 스캔 | 3 | #246 → #252 → #254 |
| W-02-05 W/O 마감 | 3 | #207 · #208 · #209 |
| W-04-02 출하 예정 목록 | 2 | #232 → #235 |
| P-02-01 작업 시작 | 2 | #247 → #271 |
| P-02-13 PQC 검사 | 2 | #251 → #257 |
| M-01-04 자재 위치 확인 | 2 | #264 · #266 |

**첫 회신이 한 자리만 풀고 인접 자리를 남기면 다음 회차가 곧바로 온다.** #188은 아예 「#185 추기의 두 물음이 답에 빠졌습니다 — 이슈가 닫혀 묻힐 것 같아서 다시 올립니다」로 왔고, #230은 「#192에서 확정한 것이 계약 문면에서 사라졌습니다」로 왔다.

→ **재검토는 화면 단위가 아니라 「한 화면이 만지는 계약 표면 전체」 단위로 한다.**

### ② 클라이언트의 대응 패턴은 일관되고, 그래서 비용이 안 보인다

전 건에서 같은 규율이 관찰된다:

- **값을 지어내지 않는다** — 「⛔ `INSPECTION_JUDGMENT` 같은 이름을 지어내지 않았습니다」(#179), 「⛔ 값을 지어내지 않았다」(#252)
- **비활성 + 사유 표시**(G-2) — 감추지 않고 왜 못 누르는지 화면이 밝힌다
- **잠정 처리를 한 파일에 모은다** — 회신 뒤 교체 비용을 낮춘다
- **페이지에서 세지 않는다** — L-1·L-2를 화면이 스스로 어기지 않는다
- **라우트를 안 연다** — 사용자에게 반쪽 화면을 보이지 않는다

⚠ **그래서 회신 지연의 비용이 「막힘」이 아니라 「미완성 누적」으로 나타난다.** 겉보기엔 진행 중인데 핵심이 잠겨 있다. #167 시점의 실측이 그 극단이다 — **「착수 가능」 81건 중 실제 착수 가능 6건.**

### ③ 검사기로 잡히는 유형과 아닌 유형이 갈린다

| | 유형 | 어떻게 잡나 |
| :-: | --- | --- |
| **기계 검사 가능** | E 전부 · D의 일부 · A3·A4 | 계약 CI 스크립트 — 중복 키 · 동명 스키마 형태 · 설명↔`required` 불일치 · Create에 있고 응답에 없는 필드 · `description` 유출 패턴 |
| **대조가 필요** | A1 · A2 · C1 · C2 | **스펙 조항 ↔ 계약 응답 형태**를 사람이 맞대야 나온다. 화면 스펙 확정 시점에 「이 화면이 세운 L-조항을 계약이 지킬 수 있는가」를 훑는 절차가 없으면 계속 나온다 |
| **결정이 필요** | B · C3 · F | 값·소관·출처를 **정하는** 일이라 검사로는 못 잡는다. 미결 대장의 등급과 소재(쓰는 화면)를 바로잡는 것이 유일한 방어 |

---

## 5. 전면 재검토 점검표

유형에서 역산한 질문이다. **화면 하나 / 계약 하나를 다시 볼 때 이 순서로 묻는다.**

### 5-1. 계약 파일 수준 (발행 전 기계 검사) — E·D

- [ ] JSON 중복 키가 있는가 (`object_pairs_hook`으로 전수)
- [ ] 계약 간 동명 스키마의 형태가 일치하는가 (`description`·`example`·`title` 제외 비교)
- [ ] `description`에 비공개 문서 경로·설계 규칙 요약·미결 표기가 남았는가
- [ ] `example`이 그 필드의 뜻과 맞는가 (자리채움 `"값"`·다른 축의 값이 섞여 있지 않은가)
- [ ] 설명에 「필수」라 적혔는데 `required`에 없는 자리가 있는가
- [ ] `{X}Create`/`{X}Update`의 `required`에 있는데 `{X}` 응답에 없는 프로퍼티가 있는가

### 5-2. 조회 계약 — A1·A2·A3

- [ ] 화면 §3의 **모든 필터·정렬 컨트롤**에 대응하는 질의 파라미터가 있는가
- [ ] 「필터 전체 기준」으로 그린 표시값이 있는가 → 응답에 `summary` 자리가 있는가
- [ ] 화면 스펙의 **L-조항 전수**가 계약 필드로 전개됐는가 (한 문장이 둘을 말하면 둘 다)
- [ ] 목록 응답의 참조 id 중 **화면이 이름을 그려야 하는 것**이 있는가 → 표시명 동봉 또는 배치 조회 경로
- [ ] `q` 검색의 **정확/부분·대소문자·다건 처리**가 설명에 있는가
- [ ] 기간 파라미터의 **끝 경계(이상/미만)**가 설명에 있는가
- [ ] 계층·페이지 의미(`includeChildren` 등)에서 `page/size/total`의 기준이 명시됐는가
- [ ] 오류 응답(400·403·409)이 선언됐는가

### 5-3. 쓰기 계약 — A4·C1·C2·C3

- [ ] 부분 수정 자원의 필드마다 **「비울 수 있는가」**가 판정됐는가 → `nullable` 명시
- [ ] PUT이 **전체 교체인지 부분 수정인지** 오퍼레이션 설명에 적혔는가
- [ ] 한 자원의 `PUT`이 `If-Match`를 요구하면 **그 자원의 모든 상태 전이**도 요구하는가
- [ ] `If-Match`/`Idempotency-Key`의 **출처가 오퍼레이션마다 한 문장으로** 적혔는가
- [ ] 스펙 §5-5에 「충돌(B-1)」이 있으면 계약에 `409`가 있는가
- [ ] 스펙이 「한 트랜잭션」이라 적은 액션이 계약상 **몇 호출**인가 → 2 이상이면 처리 확정
- [ ] 필수 필드 중 **인증 주체에서 파생되는 값**(검사자·작업자·단말·공장·교대)이 있는가 → 서버가 푸는가, 아니면 근거 헤더가 선언됐는가
- [ ] 스펙이 **선제 게이팅**을 요구하면 그 플래그를 **자기 기준으로 읽을 런타임 경로**가 있는가

### 5-4. 코드값 — B

- [ ] `Code`로 끝나는 프로퍼티마다 값 목록 정본이 ⓐ `enum` ⓑ 공통코드 그룹 ⓒ 미정 중 무엇인가
- [ ] ⓑ면 `codeGroupCode=…`가 설명에 적혔는가
- [ ] ⓒ면 **쓰는 화면**의 미결 대장에 등재됐는가 (읽는 화면이 아니라)
- [ ] 그 미결의 등급이 **「운영 차단」**인가 — 「짓는 것」은 되는데 「쓰는 것」이 안 되는가
- [ ] enum의 **값역이 롤업 원천의 값 개수를 다 담는가**

### 5-5. 문서 정합 — D·F

- [ ] 최근 개정된 절을 **인용하는 다른 절**이 같이 갱신됐는가 (같은 문서 안도 포함)
- [ ] 회신으로 확정한 내용이 **계약 문면에 착지**했는가
- [ ] 화면 스펙 ↔ API 요구서 ↔ 계약의 **필드명이 일치**하는가
- [ ] 계약 경로마다 **소비 화면**이 하나 이상 붙는가
- [ ] 화면 전이(진입·이탈)를 **실어 나를 계약 필드**가 있는가
- [ ] 착수 통지 §3의 「엔드포인트 존재」 체크가 **실제 계약 실측**에 근거하는가

### 5-6. UI/UX 고유 — G

- [ ] 화면 스펙 §1의 **단말 전제가 실측값**인가 → 레이아웃 예산 재계산
- [ ] 셸 소유물(앱바·키보드·스캐너 어댑터)의 **정본 장**이 있는가
- [ ] 스캔 화면 16건의 **오류 처리 규약**이 D-2로 통일됐는가
- [ ] POP의 **문자 입력 수단**이 정해졌는가
- [ ] DS 핀이 스펙이 요구한 치수를 **실제로 갖고 있는가** (이슈 상태가 아니라 설치본 실측)

---

## 6. 부록 — 인바운드 전수 63건 분류표

`S` 열: ● 미결 / ○ 종결. `유형` 열은 주 유형.

| # | S | 유형 | 제목 |
| --: | :-: | :-: | --- |
| 92 | ○ | E | 계약 `description`이 공개 저장소로 그대로 흘러간다 |
| 163 | ○ | A2 | W-CO-03 `occurredTo`의 끝 경계(이상/미만)가 계약에 없다 |
| 164 | ○ | D | W-CO-03 공유계약 L-6 각주와 화면 스펙 §8-4의 어긋남 |
| 165 | ○ | G | W-CO-03 「안 읽음」 어포던스·쪽 이동 표기 형태 |
| 166 | ○ | E | 계약 병합 중단 — 동명 스키마 3건의 형태 충돌 |
| 167 | ○ | E | 정본 push 지연 · 05설비툴 계약 미도착 |
| 170 | ○ | A2 | W-01-01 의뢰 기간 필수 여부 · 공급사 필터 자리 |
| 173 | ○ | C3 | W-01-01 `inspectorId` — 로그인 사용자와 작업자가 다른 번호 공간 |
| 175 | ○ | A1 | Lot Status 요약 API가 목록 필터 전체를 받지 않음 |
| 176 | ○ | A2 | W-03-01 상태·보류 이력 조회 입력 계약이 불완전 |
| 179 | ○ | B | W-01-01 판정 코드 그룹 — `code-values`가 정수 id만 받는다 |
| 185 | ○ | B | W-05-12 자산 수명주기 상태의 그룹 이름과 두 값 |
| 186 | ○ | F | W-05-12 점검 항목 마스터·부여가 이 화면의 범위인가 |
| 188 | ○ | B | #185 추기 두 물음 — 검교정 주기 단위 그룹 · 정밀도 소관 |
| 190 | ○ | C1 | W-03-02 전이 실행 경로 · 잠금 토큰 원천 |
| 192 | ○ | A1 | W-03-05 목록·집계·재검·측정 계약 보완 (7건) |
| 194 | ○ | F | W-03-09 결재함 진입·라우트·부적합 상세 이동 규약 |
| 195 | ○ | B | W-05-11 계측기 유형 값 목록이 없는 동안의 목록 |
| 196 | ○ | A2 | W-02-01 사업부 필터·계층 페이지·상세 구획 |
| 199 | ○ | C2 | W-02-02 전개 확정·W/O 생성 트랜잭션 |
| 200 | ○ | A4 | W-02-02 생산계획 선택 필드 초기화 |
| 201 | ○ | A4 | W-02-03 4M 자원배정 해제 |
| 202 | ○ | A2 | W-02-03 `plannedShiftId` 교대 조회 원천 |
| 203 | ○ | A3 | W-05-07 검사항목 이름·조건 축·옛 Rev 경고 |
| 205 | ○ | C1 | W-02-04 `:release` If-Match 계약 불일치 |
| 206 | ○ | D | W-02-04 LOT 크기 입력과 `lotSlotCount` 의미 충돌 |
| 207 | ○ | A1 | W-02-05 실적 집계·선발행 LOT 요약 조회 경계 |
| 208 | ○ | C1 | W-02-05 `:close` If-Match 계약 불일치 |
| 209 | ○ | D | W-02-05 비미달 마감의 `remainderDispositionCode` 의미 |
| 210 | ○ | C1 | W-05-01 스펙이 요구한 낙관적 잠금이 계약에 없다 |
| 219 | ○ | A2 | W-05-11 `equipmentTypeCode`가 값 하나만 받는다 |
| 220 | ○ | F | W-05-12 점검 항목 마스터를 «만드는» 화면이 어디인가 |
| 222 | ○ | D | 순서 재배치 — 요구서 `displayOrder`, 계약 `sequenceNo` |
| 225 | ○ | A2 | W-03-02 잔여 표시 계약과 API 데이터 원천 (4건) |
| 227 | ○ | B | W-03-03 `targetLotStatusCode` 코드값 원천 |
| 228 | ○ | A1 | W-03-05 측정 이상값 「외 N건」 산출 원천 |
| 229 | ○ | D | W-03-05 검사유형 「전체」 집계와 L-7 동작 |
| 230 | ○ | D | W-03-05 재검 사슬 페이지 보장이 계약 문면에서 사라짐 |
| 232 | ○ | A1 | W-04-02 요약 집계·검사 결과가 계약에 없다 |
| 235 | ○ | D | W-04-02 `shippingInspectionStatusCode` 3값이 불합격·보류를 못 가름 |
| 243 | ○ | G | P-CO-01 터치 규격 72px — DS 고정 커밋에 `2xl`이 없다 |
| 245 | ○ | C2 | P-01-01 채번 주체·`can_print_label` 경로·미부착 필터·부분 실패 |
| 246 | ○ | C3 | P-02-03 `can_input_material`이 계약 8플래그에 없다 |
| 247 | ○ | B | P-02-01 오프라인 정책·설비 축 필터·W/O 상태 코드 문자열 |
| 251 | ○ | D | P-02-13 검사 기준 미등록 갈래 — §5-2 정정과 §6·계약의 어긋남 |
| 252 | ○ | B | P-02-03 `consumptionTypeCode` 값과 사번·단말 출처 |
| 253 | ○ | A1 | W-03-10 「서버가 계산한다(L-2)」 파생값 3건이 응답에 없다 |
| **254** | ● | A2 | P-02-03 `q`의 검색 규칙과 한 건이 걸렸을 때의 처리 |
| 257 | ○ | D | P-02-13 진입 경로 · `automaticJudgment` 플래그 모순 |
| 258 | ○ | C2 | W-02-07 「발행·배포」가 세 호출이라 부분 성공 금지를 못 지킨다 |
| 259 | ○ | B | W-02-07 긴급을 뜻하는 `work_order_type_code` 값이 없다 |
| **264** | ● | G | M-01-04 소유처 축 — §5-1 표기 대상인데 §3에 칸이 없다 |
| **265** | ● | A1 | W-02-08 요약 집계와 지연 판정을 계약이 안 내려 준다 |
| **266** | ● | G | M-01-04 §6 「필드 유지」와 D-2 「비우되 포커스 유지」의 어긋남 |
| **269** | ● | A2 | P-02-07 「완료·미달 마감」을 고를 축과 값이 계약에 없다 |
| **271** | ● | C3 | P-02-01 중단 W/O 동시 조회 · 세션 `shiftId` 출처 |
| **272** | ● | B | W-02-08 W/O 상태 필터의 공통코드 그룹코드 |
| **275** | ● | G | 실기 시험 넷 — 화면 크기 전제·보류 자리·앱바·서버 HTTPS |
| **277** | ● | G | POP 문자 입력 수단 설계 요청 |
| **278** | ● | G | POP 단말 실물 OS 사양 공유 (Windows 10 LTSC 1809) |
| **279** | ● | G | 모바일 배포 정책 — 배포 경로·서명 키·난독화 |
| **282** | ● | A3 | W-06-07 거래처가 저장에는 필수인데 조회로는 못 읽는다 |
| **283** | ● | E | logistics 계약 `x-internal-note`가 한 객체에 두 번 |

### 미결 13건 — 재검토 착수 시 우선순위 제안

| 순위 | # | 왜 먼저인가 |
| :-: | --: | --- |
| 1 | **283** | 한 줄 수정으로 풀린다. 그동안 **서버의 logistics 89개 오퍼레이션 타입 검사가 비어 있다** |
| 2 | **269** | 화면 왼쪽 구획 전체가 걸려 **P-02-07이 서지 않는다**(유일한 완전 차단) |
| 3 | **272** · **265** | 같은 화면(W-02-08). 그룹코드는 지금 **가정으로 진행 중**이라 틀리면 조용히 빈 필터가 된다 |
| 4 | **266** | **스캔 화면 16건의 기준**이 여기서 정해진다 — 화면마다 다르게 굳으면 되돌리기가 비싸다 |
| 5 | **275** | 모바일 19화면의 **레이아웃 예산 기준값**이 낡았다. ④ HTTPS는 인프라 결정이라 리드타임이 길다 |
| 6 | **277** | POP 자유 입력 갈래가 **완성 불가** 상태로 대기 중 |
| 7 | 254 · 282 · 264 · 271 · 278 · 279 | 화면 단위로 격리돼 있거나 잠정 처리가 안정적 |

---

## 7. 근거·재현

```bash
# 인바운드 전수 재추출
gh issue list --repo CREFLEINC/omf-mes --state all --limit 300 --json number,title \
  --jq '.[] | select(.title | test("^\\[검토 요청\\]|^\\[client→uiux\\]|^\\[데이터모델→uiux\\]")) | .number'
# + 교차 프로젝트 폼 3건: #92 · #175 · #176

# 미결 현황 재확인
gh issue list --repo CREFLEINC/omf-mes --state open --limit 100
```

- 정독 대상: 위 63건의 이슈 본문 전문 (2026-08-28 추출)
- 분류는 **주 유형 기준**이며 다중 유형 건은 §3 본문에 교차 표기했다
- 이 문서는 이슈 본문의 실측 주장을 **그대로 인용**한다 — 계약 파일 재실측은 하지 않았다. 재검토 착수 시 각 항목은 현재 정본으로 다시 확인해야 한다
