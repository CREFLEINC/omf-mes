# 보류 89건 — 사람 확정 기록 (4단계)

> 작성일: 2026-07-29 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 입력: `node-screen-mapping/` 1~3단계 초안(753행 · 보류 89) + `보류결정-권고안.html`
> 결정 주체: **사람(리더)** — 규칙 §4가 정한 최종 확정 단계다. 이 문서가 **정본**이고, 매핑 문서의 보류 표기는 이 결정으로 닫힌다.

## 표기 안내

| 표기 | 뜻 |
| --- | --- |
| `219:4511` 꼴 | FigJam 캔버스 도형 번호 |
| `01-S-C` 꼴 | 사이드 섹션 (`S`=Side, 메인에서 빠져나온 예외·분기 구간) |
| `A11`·`B7`·`SC4` 꼴 | 도식 안 노드 이름표(도식스펙이 붙인 번호) |
| `W-01-01` · `P-` · `M-` | 화면 번호 — W=관리웹, P=POP, M=모바일 |
| `G-01-01` 꼴 | 화면 그룹 번호(매핑 규칙이 새로 묶은 단위) |
| Wallet · Mobile · Desktop · File | 도식 아이콘 — Wallet=목록에서 고름, Mobile=스캔, Desktop=화면 입력, File=등록·기록 |

**화면을 나누는 조건 네 가지**는 이름으로 부른다 — **사용자 바뀜** · **단말 바뀜** · **작업 끊김** · **따로 진입**.

## 1. 결과 — 89건 중 84건 확정, 5건 존치

| 도메인 | 보류였던 수 | 확정 | 존치 | 존치 사유 |
| --- | ---: | ---: | ---: | --- |
| 01 자재창고관리 | 15 | 14 | 1 | 고객사 협의 대기 |
| 02 생산실행 | 33 | 33 | 0 | — |
| 03 품질관리 | 10 | 7 | 3 | E3 판정유형 값 목록 회신 대기 |
| 04 제품출하 | 9 | **8** | **1** | 도식 결함(SC4) — 수정 후 재판정 |
| 05 설비·툴 관리 | 17 | 17 | 0 | — |
| 06 기준정보·연계 | 5 | 5 | 0 | — |
| **합계** | **89** | **84 (94.4%)** | **5 (5.6%)** | |

**존치 5건은 판단이 어려워서가 아니라 외부 입력을 기다리는 것이다.** 지금 정하면 회신·수정 후 전부 다시 해야 한다.

## 2. 전 도메인 공통 기준 — 여기서 47건이 닫혔다

이 다섯 기준은 개별 판단이 아니라 **규칙에 반영해야 할 판정 기준**이다. `screen-mapping-rule.md` v1.3에 반영한다.

### 2-1 · 목록(Wallet)은 진입 경로로 가른다 (6건)

> **독립 화면** — 메뉴나 홈 타일에서 **조회 목적으로 직접** 들어가는 목록
> **흡수** — 작업 흐름 한가운데서 **대상을 고르려고** 여는 목록

적대 검증 M8(「같은 Wallet 문법을 04는 독립으로 뽑고 05·06은 흡수」)의 진원이었다. 이 기준은 UI 구조가 아니라 **업무 진입 동선**을 본다.

| 노드 | 결정 | 화면 |
| --- | --- | --- |
| `825:8404` A3 출하 예정 목록 (04) | **독립** | `W-04-02` 유지 |
| `780:7866` PM 도래 목록 (05) | **독립** | `W-05-02` 유지 |
| `825:8417` A4 피킹 목록 (04) | **흡수** | `M-04-02` → **`M-04-01`에 통합** |
| `749:7637` 점검 항목 (05) | 흡수 | `M-05-01` |
| `784:8028` 예비품 목록 (05) | 흡수 | `W-06-08` |
| `717:7474` B14 송신 항목 목록 (06) | 흡수 | `W-06-12` |

### 2-2 · 축 노드는 화면이 아니다 — 사유 코드 `축상태` 신설 (14건)

축(Archive) 노드는 대장 분류가 `행위`지만 실체는 **흐름의 단계 표시**다. 조작은 축이 아니라 그 아래 붙은 행위 아이콘이 담당한다. 사유 코드 11종에 이 유형이 없었던 것이 문제였지, 화면인지가 애매했던 게 아니다.

- **02 축 Archive 8건** — `297:5372` `297:5374` `297:5376` `297:5378` `297:5380` `297:5382` `297:6305` `297:6357`
- **05 트리거 4건** — `746:7524` 점검주기도래 · `780:7758` 작업실적발생 · `784:7924` PM예정일 · `784:7937` 보전트리거
- **06 2건** — `675:7059` A6 기준정보완성 · `712:7333` SB1 변경발생

**예외 1건 — `675:6994` A3 MES 수신반영은 화면 진입으로 본다.** `676:7065` 확장속성편집(웹 배지)이 커넥터 `677:7070`으로 부착돼 있어 「수신본 목록에서 골라 확장속성을 편집한다」가 성립한다. → `G-06-01` 흡수.

근거 대비: 06-S-A의 SA1~SA4는 배지 정본이 「자동 파이프라인 = 배지 없음」으로 명시해 이미 무인 확정이다. 05의 4건은 그 명시가 없었을 뿐 성격이 같다.

### 2-3 · 관리웹 메뉴는 「저장되는 엔티티가 다르면 다른 화면」 (13건)

관리웹 메뉴 트리는 **데이터 관리 단위**를 따른다. 사용자가 "무엇을 다루러 왔는가"가 메뉴가 되지, 프로세스 단계가 메뉴가 되지 않는다. 도식이 아니라 **개념모델**을 근거로 삼으므로 판정이 재현 가능하다.

| 구간 | 결정 | 비고 |
| --- | --- | --- |
| **02 축1** (6건) | **4화면** — `W-02-01`~`04` 유지 | P/O 수신·W/O 전개·4M 배정·확정 배포가 각각 다른 엔티티. 마스터 점검 체인 `339:6639`·`330:6677`은 **W/O 전개 화면 내부 패널로 흡수**(조회일 뿐 저장하지 않음) |
| **03 판정 지점** (4건) | **2화면** — 현행 유지 | 의심자재 등록은 Lot Status를 *생성*, 판정·전이는 *변경*. 검사 판정 허브는 수행이 01·02·04에 co-locate라 03엔 수렴 기록만 남으므로 **조회 화면 흡수** |
| **05 `793:8131` 캘린더설정** | **독립** — `W-05-09` 유지 | WorkCalendar는 명백한 마스터 엔티티 |
| **06 `676:7123` B7 운영마스터** | **분해** — `W-06-07`·`W-06-08` 유지 | 창고/Location과 예비품은 별개 엔티티 |

### 2-4 · 「작업 끊김」의 조작적 정의 (11건)

> **실물 작업이 끼어 있고 전후가 다른 데이터를 다루면 분리. 같은 레코드를 이어서 완성하면 한 화면.**

「시간이 벌어지는가」는 잴 수 없지만 「같은 레코드를 마저 채우는가」는 도식과 엔티티로 판정할 수 있다. 규칙 §3-1의 「작업 끊김」 서술을 이 문장으로 교체한다.

| 구간 | 결정 | 근거 |
| --- | --- | --- |
| **05 보전·PM 6건** | **각 2화면** — 현행 유지 | 보전지시는 *계획* 데이터, 보전실적은 *실적* 데이터. 사이에 `784:7966` 보전수행 물리 작업 노드 실재 |
| **02 수리 왕복 2건** (`609:6894`·`609:6910`) | **1화면** — `M-02-02` 유지 | 투입 스캔과 반출 스캔이 **같은 수리이력 레코드**의 시작과 종료 |
| **01 도착 스캔** (`291:5775`, 종속 `291:5791`) | **1화면** — `M-01-10` 유지 | 반출과 도착이 같은 이동 레코드 |
| **01 입하 목록 선택** (`219:10999`) | **흡수** — `M-01-02` 유지 | §2-1 목록 기준 적용 — 작업 중 대상을 고르는 목록 |
| **04 재고 재등록** (`847:8894` B6) | **2화면** — 현행 유지 | 반품입고와 재등록 사이에 03 판정·재작업이 끼고, 다루는 재고 상태가 다름 |

네 건 모두 현행과 일치한다 — 현행 인벤토리가 감각적으로 옳게 잘랐다는 뜻이고, 이번에 그 감각에 이름을 붙였다.

### 2-5 · 라벨 출력은 「발행 시점」으로 가른다 (8건)

> **발행 시점이 앞 작업과 다르면 분리, 같은 작업의 마무리면 흡수.**
> **「부착」은 화면 밖이다** — 라벨 노드가 「출력 및 부착」인데 뒷부분은 물리 작업이므로 화면 범위는 출력까지.

| 구간 | 결정 |
| --- | --- |
| **04 납품라벨(A9)·포장라벨(A11)** | **분리** — `P-04-01`·`P-04-02` 유지. A9는 OQC 합격이 트리거, A11은 O1 일치 통과 후 사전발행이라 **발행 시점이 서로 다르다** |
| **02 인식표·LOT 라벨 6건** | 현행 유지 + **`P-02-05`·`P-02-07` 병합 검토**(둘 다 실적 등록 직후라 시점 동일). `P-02-09` 포장 재출력은 명시적 분기를 가진 별개 진입이라 유지 |
| **`297:6380`** (기준 불일치 재발 후보) | **긴급 경로도 정상 경로 화면을 재사용하고 긴급 플래그만 다르게 둔다.** 도식 노트 D가 「실적 3원·genealogy = 정상 동일」로 명시 |

## 3. 도메인별 개별 결정

### 3-1 · 01 자재창고관리 (15건 → 확정 14 · 존치 1)

#### 01-S-C 전체를 IQC 담당자·웹으로 확정 (배지 공백 1건 + 파급)

> **결정 근거(리더)**: 01-S-C 진입 **전에** 이미 현장 작업자가 IQC 대상 여부를 스캔해 분류했다. 따라서 01-S-C 작업은 **IQC 공간에서 진행**되는 것으로 본다.

이 결정은 배지 공백 1건을 채우는 데 그치지 않고 그룹 구조를 바꾼다.

| 이전 | 결정 후 |
| --- | --- |
| `G-01-08` IQC 대상 LOT 스캔 · **모바일** → `M-01-03` | `G-01-09`와 **병합** — **`M-01-03` 소멸** |
| `G-01-09` IQC 진행·결과 기록·판정 · 웹 → `W-01-01` | 유지(범위 확대 — `545:5843` 스캔부터 포함) |

- `545:6753` 점검 항목 확인 → **웹** · `G-01-09` 확정
- 파급 ①: 정본 배지 「01-S-C 품질담당 = **모바일**」을 **웹**으로 정정해야 한다 → docs 이슈
- 파급 ②: `545:5843` 자재 LOT 번호 스캔이 웹에서 수행되므로 **IQC 공간에 PC + 스캐너 입력 수단**이 필요한데, 하드웨어 스펙 §2.5 스테이션 3종에 IQC 스테이션이 없다 → docs 이슈

#### 나머지 01 결정

| 노드 | 라벨 | 결정 |
| --- | --- | --- |
| `545:6596` | 정상품 입하 처리 | **웹 · 화면 신설** — 배지 공백을 웹으로 채우고 대응 화면을 만든다 |
| `225:5730` | 신규 P/O 등록 | **독립 메뉴** — ERP 전표 연계 + 승인 마커(`225:5749`) |
| `219:5554` | 조정 등록 | **독립 메뉴** — ERP 조정 전표 연계 + 승인 마커 U-D1 |
| `219:5500` | 재생재 등록 | **독립 타일** — 「재생재 발생 시」는 출고분 입고와 **무관하게** 발생하는 사건이라 진입 동선이 다르다. `M-01-09`에서 분리 |
| `219:5499` | 잔량 입력 | **`M-01-09` 흡수** — 생산창고 입고 시점에 종속된 조건부 단계 |
| `219:5544` | 실사 개시 | **`G-01-15` 포함** — 정기/수시 태그(`263:5808`)가 선택 입력을 시사하므로 조작 |
| `219:5586`·`219:5587`·`219:5588` | 불량창고 입고 · 내부 품의 · 기타출고 | **분리 유지** — `W-01-05`·`W-01-06`. 액터·단말이 같아 규칙 기본값은 1그룹이지만 **공급사 반품 전표와 폐기 품의는 결재선과 ERP 전표 종류가 다르다**(§2-3 기준과 일관) |

#### 존치 1건

| 노드 | 라벨 | 존치 사유 |
| --- | --- | --- |
| `226:5738` | LOT 수량 정정 | **부분 입고 허용 여부가 「고객사 협의 사항」이므로 관련 결정 전부를 유보한다**(리더 결정). 액터 근거 부족은 부차 사유였고, 실제 선행 조건은 부분 입고 정책이다 |

### 3-2 · 02 생산실행 (33건 → 전건 확정)

공통 기준으로 닫힌 것 — 축 Archive 8(§2-2) · 메뉴 절단 6(§2-3) · 라벨 6(§2-5) · 수리 왕복 2(§2-4) = **22건**. 아래는 개별 11건.

#### 러닝체인지 2건 — 배지 공백 해소 + 도식 결함 발견

> **결정 근거(리더)**
> - **교체 시점**은 생산 현장 작업자가 **POP으로 새 부품 투입 시** 결정된다.
> - **LOT 분할**은 관리자가 **웹에서 생산 계획에 분할 시점을 지정할 때 자동으로** 분할된다.
> - 분할 기준은 「시간」이 아니라 **「생산 양품 실적」**이다.
> - 계획보다 기존 부품(기존 BOM)으로 **추가 생산**하면 기존 부품용 생산LOT 번호를 **추가 발행**한다(예: 구 1~10 · 신 11~20 사전 발번 → 추가 생산 시 21번 발행). 반대로 **덜 생산하면 나머지는 폐번**한다.

| 노드 | 결정 | 화면 |
| --- | --- | --- |
| `297:6320` 교체 시점 지정 | **POP** — `297:6324` 신규 부품 등록과 같은 세션(배지 정본 「02-S-B 신규 부품 등록 = POP」이 앵커) | `G-02-08` / `P-02-11` 흡수 |
| `297:6322` 생산LOT 분할 | **`화면없음:시스템자동`** — 분할 실행은 자동이고, 사람이 하는 「분할 시점(양품 N개) 지정」은 **기존 W/O 편성·계획 화면(관리웹)의 입력 항목**이다. 화면 신설 0건 | 대응 없음 |

**지정 타이밍**: W/O 편성 시점과 **진행 중 W/O 양쪽**에서 지정할 수 있어야 한다. 러닝체인지가 생산 도중 결정되는 경우가 있고, 리더 결정의 「계획보다 추가/미달 생산」 보정 규칙 자체가 계획–실제 괴리를 전제한다. → 계획 화면에 **「진행 중 W/O 수정」 경로**가 필요하고, 이미 실적이 쌓인 LOT 이후 구간만 재분할하는 제약이 붙는다.

**도식 결함 발견 — 02-S-B의 순서가 실제 시간과 반대다.**

```
도식:  297:6320 교체 시점 지정 → 297:6322 생산LOT 분할 → 297:6324 신규 부품 등록
실제:  [계획·웹·관리자] 6322 분할  ⟶⟶⟶  [생산 당일·POP·작업자] 6320 교체 시점 = 6324 투입
```

`297:6322`가 **먼저**(계획 시점) 일어나고 `297:6320`은 **나중**(현장)이다. 두 구간은 사용자(관리자↔작업자)·단말(웹↔POP)·시점(계획일↔생산일)이 전부 다르다. → docs 이슈.

#### 나머지 02 결정

| 노드 | 결정 |
| --- | --- |
| `297:5397`~`297:5400`·`492:7681` (POP 태스크 절단 5건) | **현행 유지.** 점검 이력 확인은 **독립**(작업 전 통제는 별도 게이트), 투입 스캔과는 **연속**(같은 W/O 세션) — §2-4 기준 |
| `297:5395` 사번 입력 | **`P-CO-01`(사번 경량 인증) 단일화.** `P-02-01`은 「작업 시작」에서 **W/O 선택으로 범위를 좁혀** 중복 제거 |
| `297:5503` 전달사항 등록 | **`W-02-04` 안의 섹션으로 복귀.** `W-CO-04`는 전사 공지 전용으로 역할을 좁힌다 |
| `497:5460` 제품 검사 | **POP · 화면 단계로 계상**(리더 결정) — `G-02-12` / `P-02-13`. 화면이 담을 내용(판정 유형·Lot Status 전이 범위)은 03 소관이라 **상세 스펙 단계에서 정한다** |
| `297:5457` 마감 미달 갈래 | **`W-02-05` 흡수.** 02-S-A 합류는 예외 경로일 뿐 독립 진입 동선이 아니다 |

### 3-3 · 03 품질관리 (10건 → 확정 7 · 존치 3)

| 묶음 | 결정 |
| --- | --- |
| A. 판정 지점 4건 | **2화면 — 현행 유지**(§2-3) |
| C. 경계 카드 3건 (`586:6764`·`586:6767`·`633:6934`) | **전부 `섹션인계`.** 화면은 수행하는 쪽인 01·02가 계상한다 — 03이 계상하면 `W-01-01`·`P-02-13`·`M-02-02`가 이중 계상된다 |

**존치 3건** — `587:6776` 한도승인 · `587:6779` 특채 · `587:6782` 폐기.
승인 매체는 관리웹으로 확정(#11 #9)됐지만 **E3 판정유형 값 목록이 상태 개수 자체를 바꾼다.** 3개가 아닐 수도 있는 상태를 놓고 화면 분해를 정하면 회신 후 전부 다시 해야 한다. `화면없음:상태값` + 「E3 대기」 표기로 두고 도식화와 함께 처리한다.

> ⭐ **2026-08-07 해소 — 그리고 이 보류 판단이 옳았다.** LOT 상태가 **`정상`·`불량`·`검사 대기`·`폐기` 4값**으로 확정되며 **한도승인·특채가 값에서 빠졌다**(상태는 둘 다 `정상`, 다른 것은 조건). **상태 개수만 바뀐 게 아니라 이 화면이 다루는 대상 자체가 재정의됐다.** 대기하지 않고 그렸다면 전부 다시 그렸을 것이다. → `uiux/2026-08-07-E3-판정유형-제안안/LOT상태-확정기록.md`

### 3-4 · 04 제품출하 (9건 → 확정 7 · 존치 2)

공통 기준으로 닫힌 것 — 목록 2(§2-1) · 라벨 2(§2-5) · 작업 끊김 1(§2-4) = 5건.

| 노드 | 결정 |
| --- | --- |
| `819:8239` M3 출하지시 단독 생성 | **「따로 진입」 인정.** 대안 진입 커넥터 `832:8620`이 캔버스에 실재한다. 다만 화면 신설보다 **`W-04-01`의 모드**로 두는 편이 현실적 |
| `849:8921` B8 재구성 이벤트 | **POP — `G-04-10` / `P-04-04` 현행 유지.** 배지 경계가 노드를 관통한 지점이므로 정본에 한 줄 추가해 영구 해소한다 → docs 이슈 |

**존치 1건 + 확정 1건 — 성격이 서로 달랐다.**

| 노드 | 상태 |
| --- | --- |
| `847:8878` SC4 품질담당 | **도식 결함.** 배지는 웹인데 **인접 행위 노드가 0개**다. 흡수할 대상이 없어 규칙이 작동할 수 없다. `W-04-07`은 수정까지 현행 유지 |
| `822:8290` H6 게이트 | **확정 — `G-04-01` 흡수**(출하작업지시 단계의 품목 속성 판정). 「미작화」는 **반증**됐다 — 「예」 경로는 **페이지 직속** 커넥터 `840:8642`(라벨 「예: Packing 스킵」 → O4 `822:8295`) + `840:8638`(라이저)로 실재한다. **근거** ① 「생산 포장품?」은 **품목 마스터 속성**(Routing에 포장 공정 명시 여부)이라 런타임 관찰이 아니다 ② 「예」의 도식상 귀결이 **완전 스킵**(O4 직행)이라 열리는 화면이 없어 `G-04-04`에 넣을 수 없다 ③ 「설정형」은 시스템 설정을 뜻한다 ④ 출구 3본(`824:8343` 아니오→Packing / `840:8642` 예→O4 / `831:8613` 재포장 필요 시→04-S-D) 중 2본만 화면으로 가는 **작업 경로 라우팅 지점**이고, 라우팅은 작업 생성(`W-04-01`)의 몫이다. ⚠ **재확인 조건** — 도식스펙-04 **[확인] 7**(기포장품 스킵 경로의 납품라벨 부착 절차·현재 「보수적으로 도식 무표기」)이 「납품라벨은 붙인다」로 풀리면 「예」도 `P-04-02`를 거쳐 **부분 스킵**이 되므로 재판단 대상. 함께 열린 **[확인] 6**(생산 포장품 실재 범위)이 판단의 무게를 정한다. **화면 수 무영향**(`화면없음:게이트`) |

> **⚠ 04 캔버스 조회 함정 — 페이지 직속 커넥터 3본.** 04 메인은 섹션(`819:8194`) 자식만 훑으면 `838:8635`(H7 브래킷) · `840:8638`(라이저) · `840:8642`(H6 「예」)를 **놓친다.** FigJam 커넥터를 섹션에 append하면 좌표가 재해석돼 불안정하므로 **페이지레벨+절대좌표 패턴**을 쓰기 때문이다(도식스펙-04 v1.5 · 이슈 #26). 이 함정 때문에 **노드 대장의 「미작화」 판정 2건이 모두 오판**이었다 — H7(`838:8635`)도 함께 정정했고, 04 캔버스 무대응 행은 이제 **0건**이다.

### 3-5 · 05 설비·툴 관리 (17건 → 전건 확정)

공통 기준으로 닫힌 것 — 트리거 축 4(§2-2) · Wallet 3(§2-1) · 메뉴 절단 1(§2-3) · 작업 끊김 6(§2-4) = 14건.

| 노드 | 결정 |
| --- | --- |
| `749:7621` B1 사번입력(모바일) | **`M-CO-01` 로그인으로.** `M-05-01` 흡수가 아니다 — 모바일은 계정 로그인이고 POP 사번 경량 인증과 성격이 다르다(결정 16) «(정정 2026-08-02: 「모바일=계정 로그인(결정 16)」은 **결정 16 원문에 근거가 없다** — 단말표 모바일 행에 인증 서술 없음. 말단 확정=**POP인증 §6-③ 「모바일=POP과 동일(단말 토큰+사번 귀속·로그인 생략)」**(✓확정 2026-07-28). **화면 귀속 판단 자체는 유효**하다 — B1 사번입력이 `M-CO-01`로 가는 것은 그대로이고, 바뀐 것은 그 화면의 인증 방식 서술뿐이다. 인벤토리 v1.4 §2.3 진입 주)» |
| `749:7679` B5 사번입력(POP) | **`P-CO-01`(사번 경량 인증) 공통 화면** |
| `746:7605` 생산진행 | **02 귀속 — `섹션인계`.** 05에 화면을 만들지 않는다 |

### 3-6 · 06 기준정보·연계 (5건 → 전건 확정)

| 노드 | 결정 |
| --- | --- |
| `675:6994` A3 MES수신반영 | **`G-06-01` 흡수** — §2-2의 유일한 예외(확장속성편집이 커넥터로 부착) |
| `675:7059` A6 기준정보완성 | **`화면없음:축상태`**(§2-2) |
| `712:7333` SB1 변경발생 | **`화면없음:축상태`**(§2-2) |
| `676:7123` B7 운영마스터 | **분해 — `W-06-07`·`W-06-08` 유지**(§2-3) |
| `717:7474` B14 송신항목목록 | **흡수 — `W-06-12`**(§2-1) |

## 4. 화면 집합 변동

| 구분 | 화면 | 프로그램 | 사유 |
| --- | --- | --- | --- |
| **삭제** | `M-01-03` IQC 대상 LOT 스캔 | 모바일 | 01-S-C 전체 웹 확정 → `W-01-01`에 병합 (§3-1) |
| **삭제** | `M-04-02` 피킹 대상 목록 | 모바일 | 목록 진입 경로 기준 → `M-04-01` 통합 (§2-1) |
| **신설** | 정상품 입하 처리 | 관리웹 | 배지 공백을 웹으로 확정하고 대응 화면 신설 (§3-1) |
| **신설** | 신규 P/O 등록 | 관리웹 | 독립 메뉴 — ERP 전표 연계 + 승인 마커. `W-01-03`에서 분리 (§3-1) |
| **신설** | 재고조정 | 관리웹 | 독립 메뉴 — ERP 조정 전표 + 승인 마커. `W-01-04`에서 분리 (§3-1) |
| **신설** | 재고 재등록 | 관리웹 | 반품입고와 분리 확정. `W-04-06`에서 분리 (§3-4) |
| **신설** | 재생재 등록 | 모바일 | 독립 진입 동선 인정 → `M-01-09`에서 분리 (§3-1) |
| **범위 축소** | `P-02-01` 작업 시작 | POP | 사번 인증을 `P-CO-01`로 넘기고 W/O 선택으로 축소 (§3-2) |
| **범위 축소** | `W-CO-04` 공지 | 관리웹 | 전사 공지 전용. 02 전달사항은 `W-02-04` 섹션으로 복귀 (§3-2) |
| **귀속 변경** | `M-05-01` → `M-CO-01` | 모바일 | 05 B1 사번입력은 모바일 로그인 소관 (§3-5) |
| **모드 추가** | `W-04-01` 출하지시 | 관리웹 | 「지시서 없이 단독 생성」 모드 (§3-4) |
| **병합 검토** | `P-02-05` · `P-02-07` | POP | 발행 시점이 같음 — 상세 스펙 단계에서 확정 (§2-5) |

**순증감: 112 − 2 + 5 = 115건.**

| 프로그램 | 이전 | 이후 | 증감 |
| --- | ---: | ---: | ---: |
| 관리웹 | 71 | **75** | +4 |
| POP | 22 | 22 | 0 |
| 모바일 | 19 | **18** | −1 |
| **합계** | **112** | **115** | **+3** |

도메인별로는 **01이 22 → 25**, **04는 17 유지**(−1 +1), 나머지 도메인은 변동 없다.

> **⚠ 권고안 정정** — 권고안 「결정 후 파급」은 증가를 「재생재 등록 1건」으로만 적었으나, 실제로는 **전표 메뉴 2건(신규 P/O 등록·재고조정)과 배지 공백 1건(정상품 입하 처리), 작업 끊김 1건(재고 재등록)이 각각 화면을 늘린다.** 특히 재고 재등록은 권고안이 「2화면(현행 유지)」로 부기했는데 현행 `W-04-06`이 두 단계를 한 화면에 담고 있어 실제로는 신설이다. 승인하신 개별 권고 내용 자체는 그대로 적용했고, **합계만 정정**했다.

## 5. 규칙 개정 사항 → `screen-mapping-rule.md` **v1.3 반영 완료 (2026-07-29)**

규칙에 되먹인 판정 기준. **다음 적용부터 자동으로 갈리므로 보류가 크게 준다** — 89건 중 **47건(53%)**이 이 기준들로 닫혔다.

| # | 개정 | 절 | 닫힌 수 |
| --- | --- | --- | ---: |
| 1 | **사유 코드 `축상태` 신설** (11종 → 12종) | §1 · §2 | 14 |
| 2 | **관리웹 절단 기준** — 「저장되는 엔티티가 다르면 다른 화면」 | §3-3 | 13 |
| 3 | **「작업 끊김」 조작적 정의 교체** — 「실물 작업이 끼어 있고 전후가 다른 데이터를 다루면 분리」 | §3-1 | 11 |
| 4 | **라벨 = 발행 시점 기준** — 「부착」은 화면 밖 | §3-3 | 8 |
| 5 | **목록(Wallet) 처리 원칙** — 진입 경로 기준 (M8 직접 처방) | §3-2 | 6 |

**파급: §3-4 주관 조건이 4개 → 1개.** 작업 끊김·따로 진입·연속 조작이 객관으로 이동하고 「결과가 입력으로」만 주관으로 남는다. 부수 정정 3건(`미작화` 예시 반증 · 성공 기준을 행위 노드 기준으로 · §5에 **입력 신뢰도 한계** 신설)도 함께 반영했다.

## 6. 후속 반영 — uiux 직접 작업 vs `[uiux→docs]` 이슈

**분류 정정(2026-07-29)** — 초안은 배지 정정 3건을 docs 이슈로 분류했으나, **배지 정본 `placement-plan.md`은 `uiux/` 소관**이다(uiux/CLAUDE.md 설계 문서 지도). 따라서 문서는 직접 고치고 **FigJam 캔버스 칩만 이슈**로 넘긴다.

### 6-1 · uiux 직접 작업 (완료)

| 대상 | 내용 |
| --- | --- |
| `screen-mapping-rule.md` | **v1.3** — 판정 기준 5종 명문화 · 사유 코드 `축상태` 신설 · 주관 조건 4→1 |
| `../2026-07-24-프로그램배지/placement-plan.md` | 배지 사각지대 4곳 확정 반영 — 01-S-C 품질담당 **모바일→웹** · 01 정상품 입하 처리 **웹**(신규) · 02-S-B 교체 시점 지정 **POP**(신규) · 04-S-D B8 **POP 귀속 명시**. 63배치/66칩 → **65배치/68칩** |
| `node-registry/` · `node-screen-mapping/` | 89건 결정 반영 + 04 「미작화」 2건 반증 정정 |

### 6-2 · `[uiux→docs]` 이슈 6건 — **발행 완료 (2026-07-29)**

uiux는 docs를 직접 고치지 않는다(`uiux/CLAUDE.md` 절대 규칙). FigJam 캔버스도 figjam 하네스 소관이라 이슈로 넘긴다.

| # | 대상 | 내용 |
| --- | --- | --- |
| **#40** | **FigJam v2 캔버스** (배지 레이어) | 배지 정본 확정분 3칩 반영 — 칩 `881:8943` 모바일→**웹** 교체 · 신규 칩 2(01 정상품 입하 처리=웹 · 02-S-B 교체 시점 지정=POP). 정본 = `placement-plan.md` 65배치/68칩 |
| **#41** | 하드웨어 스펙 §2.5 | **IQC 스테이션 부재** — 01-S-C 전체 웹 확정으로 IQC 공간에 PC+스캐너가 필요한데 스테이션 3종에 없다 |
| **#42** | 개념모델 · WF02 · 도식스펙-02 | **생산LOT 러닝체인지 정본 3건** — ① **추가 발행·폐번 규칙 신설**(P3 패턴이 자재LOT 라벨·포장라벨에만 명문, 생산LOT 「상태」는 값 목록 자체가 없음) ② **「전/후 분할」 표현 정정**(추가 발행분은 시간상 뒤인데 BOM은 '전' — 정확히는 「BOM 스냅샷별 LOT 집합·번호 순서 ≠ 시간 순서」) ③ **WF02 §최우선검증 「S4 생산LOT 선발행 vs 실시간」 종결**(개념모델이 2026-07-15에 이미 확정·WF02만 뒤처짐) |
| **#43** | 도식스펙-02 §2.2 · FigJam 02-S-B | **도식 순서가 실제 시간과 반대** — `297:6322`(계획·웹·자동)가 `297:6320`(현장·POP)보다 먼저다. 두 구간은 사용자·단말·시점이 전부 다르다 |
| **#44** | 도식스펙-04 · FigJam 04-S-C | **SC4 도식 결함** — `847:8878` 품질담당에 배지는 웹인데 **인접 행위 노드가 0개**라 흡수 대상이 없다. `W-04-07`이 이 공백 위에 서 있다 |
| **#45** | 도식스펙-04 §1-③ | **H6 「예」 분기 실물 ID 기재** — `840:8642`(페이지 직속). v1.5가 H7에 한 것과 같은 조치. 미기재로 두면 섹션 자식 조회 시 같은 오판이 재발한다 |

### 정합 검토 결과 — 리더 지시 「기존 설계 내용과 정합 검토 필수」에 대한 답

**일치 4건** — 러닝체인지·LOT 분할 결정은 기존 설계와 어긋나지 않는다.

| 리더 결정 | 정본 근거 |
| --- | --- |
| 분할 기준 = 생산 양품 실적 | 개념모델 C3 「분할 기준 = **양품 목표 수량**(정량 LOT) — 회차/교대/자재배치 무관」(✓확정 2026-06-24) |
| 사전 발번(1~10 / 11~20) | 개념모델 「생산LOT = W/O 배포 시 **선발행** — 번호/슬롯 예약일 뿐, 완료 전 실물 미귀속」(✓확정 2026-07-15) |
| 분할 지정 = 관리자·웹·생산 계획 | WF02 S2 「러닝체인지 대비 생산 계획의 부품별 분할 생성」(REQ-PR-0020) |
| 자동 분할 | `N = 양품목표 ÷ LOT크기` 정량 산식 |

**W/O 무분할**(QA #37)도 유지된다 — 리더 결정에 W/O 분할 언급이 없다.

**신규 2건**(이슈 4·5) · **표현·도식 결함 2건**(이슈 5·6) — 위 표 참조.

**충돌 아님으로 판정한 것**: QA #27 「미달 마감 잔량 = 이월/소멸 선택」과 리더의 「덜 생산하면 폐번」은 **층위가 다르다.** 러닝체인지 폐번은 *W/O 총량은 유지한 채* 구BOM↔신BOM 배분만 조정하는 것이고, QA #27은 *W/O 총량 자체*가 미달인 경우다. 러닝체인지 폐번 후에도 총량을 못 채우면 그때 QA #27이 적용된다.

## 7. 다음 단계

1. 매핑 문서 6종 + 총괄에 이 결정 반영 → **완료**
2. ~~`screen-mapping-rule.md` v1.3 개정~~ → **완료 (2026-07-29)** — 판정 기준 5종 명문화
3. `screen-inventory-ia.md` v1.2 개정 — §4 화면 집합 변동 반영
4. ~~`[uiux→docs]` 이슈 6건 발행~~ → **완료 (2026-07-29)** — **#40~#45**. 배지 정본 3건은 uiux 소관이라 직접 반영 완료
5. **도식 앵커 없는 현행 화면 28건 별도 트랙** — 이 규칙 밖 사안이라 REQ·WF 근거 확인 체계가 따로 필요하다(규칙 §5)
6. 존치 5건 재판정 — E3 회신(3) · 도식 수정(1 · SC4) · 고객사 협의(1)
7. **H6 재확인 감시** — 도식스펙-04 [확인] 7 회신 시 `G-04-01` 흡수 판정을 재검토한다(부분 스킵이면 뒤집힌다).

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| 1.4 | 2026-07-29 | **PR #39 · docs 이슈 #40~#45 발행 완료.** §6-2에 이슈 번호 연결. |
| 1.3 | 2026-07-29 | **규칙 v1.3 반영 완료 · 배지 정본 갱신 · 후속 분류 정정.** §5를 「반영 예정 4건」에서 **「반영 완료 5건」**으로 교체(라벨 발행 시점 기준 추가). §6 재작성 — 배지 정본 `placement-plan.md`이 **uiux 소관**임을 확인해 docs 이슈 9건 → **uiux 직접 작업 3 + docs 이슈 6**으로 재분류. 배지 63배치/66칩 → **65배치/68칩**(캔버스 반영은 이슈 A). |
| 1.2 | 2026-07-29 | **H6 확정 — `G-04-01` 흡수**(출하작업지시 단계의 품목 속성 판정). 미작화 반증으로 판단 가능해진 건을 리더가 확정. 보류 89 → **확정 84·존치 5**, 753행 = 확정 **748**·존치 5. 04 = 확정 104·존치 1, 「확인 필수」 게이트 8 전건 통과. **[확인] 7 회신 시 재검토 조건 명기.** 화면 집합 115건 불변. |
| 1.1 | 2026-07-29 | **04 「미작화」 판정 2건 전량 반증**(캔버스 실측) — 페이지 직속 커넥터 함정(섹션 자식 조회로는 3본 누락). H7 = `838:8635` 실재 → 대장 캔버스ID 100%·처분 `미작화`→`주석`·04 미작화 0건. H6 「예」 = `840:8642` 실재 → **존치 사유가 「도식 결함」에서 「리더 재판단 대상」으로 변경**(화면 수 무영향). docs 이슈 8번 축소. main c737ec5 위로 rebase(uiux 트리 무변경). |
| 1.0 | 2026-07-29 | 보류 89건 사람 확정 — 83건 확정·6건 존치. 전 도메인 공통 기준 5종 확정(목록 진입 경로·축상태 신설·관리웹 엔티티 절단·작업 끊김 조작적 정의·라벨 발행 시점). 01-S-C 전체 웹 확정으로 `M-01-03` 소멸. 러닝체인지 배지 공백 2건 해소 + 정합 검토(일치 4·신규 2·결함 2). 화면 집합 112건 유지·구성 변동 9건. docs 이슈 9건 도출. |
