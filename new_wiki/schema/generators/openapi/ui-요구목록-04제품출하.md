# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain 04` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-04제품출하.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.


## W-04-01

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 항상 — 고객·기간·편성 여부 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 지시서 선택 | 목록 행 클릭 |
| 지시서 가져오기 | ⚠ 파일 형식 미정(§5-6) — 비활성 + 사유 |
| 단독 생성 | 항상 — 고객·납품처를 사람이 고른다(§5-2) |
| 라인 추가·삭제 | 단독 생성 시 · 지시서 경유는 라인이 고정 |
| 배정 수량 입력 | ≤ 요청 수량(§5-1) |
| 출하검사 대상 토글 | 라인마다 |
| 편성 | 라인 ≥ 1 AND 배정 수량 ≥ 1 AND 출하 희망일 입력 |
| 취소 | 항상 — 화면 이탈 |
| 편성 취소 | ⛔ 두지 않는다 — 출하작업지시 취소는 범위 밖(FR-IM-079는 출하 취소이고 이것은 그 앞이다) |

## W-04-02

| 액션 | 활성 조건 |
| --- | --- |
| 조회 | 출하일 범위 입력됨(L-3) |
| 필터 | 고객·납품처·상태·검사 상태 |
| 정렬 | 출하일·고객·작업지시번호 셋만(L-4) |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 새로고침 | 항상 — 자동 갱신 없음(L-6) |
| 행 클릭 → 편성 화면 | W-04-01 로 이동 |
| 행 클릭 → 출하 확정 | W-04-04 로 이동 — 피킹·검사가 끝난 건만 |
| 내보내기 | ⛔ 두지 않는다 — 범위 밖 |
| 시간대 필터 | ⛔ 두지 않는다(§5-2) |

## W-04-03

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 항상 — 상태·기간·품목 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 검사 선택 | 목록 행 클릭 |
| 수량 입력 | 선택됨 — 셋의 합 = 검사 수량(§5-1) |
| 종합 판정 선택 | ⚠ 값 목록 미정 — 비활성 + 사유(G-2) |
| 판정 저장 | 합계 일치 AND 종합 판정 선택됨 — ⚠ 확인 필요(§5-2) |
| 재검사 | 판정 완료된 건 — 사유 필수(§5-3) |
| 회차 이력 보기 | 회차 ≥ 2 |
| 검사성적서 발행 | ⛔ 비활성 + 사유(§5-5) |
| 불합격 처분 의뢰 | W-04-07 로 이동 — 소관 이동 |
| 판정 수정 | ⛔ 두지 않는다 — 번복은 재검사다(§5-3) |

## W-04-04

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 출하일 · 피킹 완료 여부 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 대상 선택 | 목록 행 클릭 |
| 상차 정보 입력 | 선택됨 — 전부 선택 입력(§4-A) |
| 출하 수량 조정 | ⚠ ≤ 피킹 수량 — 부분 출하 허용 |
| 출하 처리 | 관문 3 통과(§5-4) — 미확정 출하를 만든다 |
| 출하 취소 | ⛔ 여기서 하지 않는다 — W-04-12 소관(미확정 구간에서 가능) |
| ERP 재전송 | ⛔ 두지 않는다 — W-06-10 소관 |
| 연계 현황 보기 | W-06-10 로 이동 |

## W-04-05

| 액션 | 활성 조건 |
| --- | --- |
| 생산 완료분 선택 | 항상 — 입고되지 않은 제품 LOT |
| 출하작업지시 선택 | LOT 선택됨 — 품목이 맞는 것만 |
| 수량 입력 | ≤ LOT 수량 AND ≤ 배정 잔여 |
| 상차 정보 입력 | 항상 — 선택 입력 |
| 긴급 사유 입력 | ✅ 필수(§5-5 · A-12) |
| 직행 출하 확정 | Release AND 수량 유효 AND 사유 입력 — ⚠ 확인 필요 |
| 취소 | 항상 — 화면 이탈 |
| 되돌리기 | ⛔ 두지 않는다 — 출하 취소 화면이 없다(W-04-04 §5-2와 같은 건) |

## W-04-08

| 액션 | 활성 조건 |
| --- | --- |
| 조회 | 창고 선택됨(L-3의 변형 — 파티션은 아니지만 범위가 필요하다) |
| 필터 | 품목·가용만·유효기간 임박·보류만 |
| 묶기 | 품목 / LOT / 위치 — L-7(묶어도 되는 축) |
| 정렬 | 유효기간·제조일·가용 수량 셋만(L-4) |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| LOT 선택 | 목록 행 클릭 — 하단 상세 |
| Lot Status 화면에서 보기 | 보류 중인 LOT — W-03-02 로 이동 |
| 새로고침 | 항상 — 자동 갱신 없음(L-6) |
| 상태 전이 | ⛔ 두지 않는다 — W-03-02 소관(§5-4) |
| 재고 재등록 | ⛔ 두지 않는다 — W-04-11 소관 |

## W-04-11

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 창고 · 판정 완료 여부 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 대상 선택 | 목록 행 클릭 |
| 판정 이력 보기 | 항상 — 시간순(§5-2) |
| 대상 창고·위치 선택 | 대상 선택됨 — 적치 규칙이 있으면 권장(W-06-14) |
| 수량 입력 | ≤ 현재 보유 — 부분 재등록 허용 |
| 재등록 확정 | 판정 완료 AND 수량 ≥ 1 AND 위치 지정 — ⚠ 확인 필요(§5-3) |
| 폐기로 보내기 | W-01-06 로 이동 — 소관 이동 |
| 되돌리기 | ⛔ 두지 않는다 — 다시 판정·보류를 건다 |

## W-04-12

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 출하일 범위 입력됨(L-3) — ⚠ 기본값이 「오늘」이 아니다(§5-7) |
| 정렬 | 경과일 긴 순(기본) · 출하일 · 고객 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 다중 선택 | 미확정 건 — ⚠ 경고 대상은 개별로만(§6) |
| 확정 | 미확정 AND 취소 결재 중 아님 — ⚠ 확인 필요(J-7) |
| 취소 요청 | 미확정 AND 취소 요청 중 아님 — 사유 필수(§5-8) |
| 취소 실행 | 승인 완료 — 실행 시점 재판정(J-8) |
| 자동 확정 설정 | ⚠ 이 화면에서 켜고 끄지 않는다 — 표시만. 설정은 W-06-06 계열 소관 §8-3 |
| 확정 취소 | ⛔ 두지 않는다 — 예외 경로 없음(§5-3) |
| ERP 재전송 | ⛔ 두지 않는다 — W-06-10 소관 |

## P-04-01

| 액션 | 활성 조건 |
| --- | --- |
| 납품라벨 스캔 | 항상 — D-2 |
| 생산LOT 스캔 | 납품라벨 스캔됨 |
| 직접 입력 | 항상 — D-3 대체 경로 의무 |
| 수량 입력 | LOT 매칭됨 — ≤ 배분 잔여 · 숫자 키패드(D-4) |
| 유형 선택 | 항상 — ⚠ 값 목록 미정 |
| 상위 포장 지정 | 상위 후보 존재 — 자기 하위 제외(§5-2) |
| 다시 스캔 | 항상 — 마지막 스캔 취소 |
| 포장 확정 | 내용물 ≥ 1 AND 유형 선택됨 |
| 포장 해체 | ⛔ 두지 않는다 — 02 도메인의 되돌리기 계열(#78 코멘트) |
| 라벨 출력 | P-04-02 로 이동 — 소관 이동 |

## M-04-01

| 액션 | 활성 조건 |
| --- | --- |
| 피킹 목록 조회 | 온라인 — 오늘 출하분 |
| 대상 선택 | 목록 행 |
| LOT 스캔 | 대상 선택됨 — D-2 스캔 규약 |
| 직접 입력 | 항상 — D-3 대체 경로 의무 |
| 수량 입력 | 스캔됨 — ≤ 가용 AND ≤ 배정 잔여 |
| 피킹 확정 | 수량 ≥ 1 AND Release AND 가용 충분 |
| 권장 외 선택 | ⚠ 경고 후 통과(§5-5) — 사유는 남지 않는다 |
| 피킹 취소 | 확정 전 — 화면 이탈 |
| 확정 후 되돌리기 | ⛔ 두지 않는다 — 예약이 소진된다. 되돌림은 W-01-13 계열 |

## M-04-04

| 액션 | 활성 조건 |
| --- | --- |
| 인식표 스캔 | 항상 — D-2 스캔 규약 |
| 직접 입력 | 항상 — D-3 대체 경로 의무 |
| 위치 스캔 | 인수 대상 확정 AND 창고가 위치를 관리할 때(§5-2) |
| 수량 수정 | 인수 대상 확정 시 — 실물대로(§5-4) |
| 입고·적치 완료 | LOT 확정 AND (위치 스캔됨 또는 위치 관리 안 함) |
| 취소 | 항상 — 화면 이탈(저장 전) |
| 임시 위치 적재 | ⚠ M-01-07이 자재 전용이다 — 제품용 경로가 없다 §8-4 |

## W-04-06

| 액션 | 활성 조건 |
| --- | --- |
| 원 출하 검색 | 항상 — 고객·기간·출하번호·LOT |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 원 출하 선택 | 목록 행 클릭 — 라인이 채워진다 |
| 원 출하 없이 등록 | 항상 — 품목·LOT·수량을 직접 입력(§5-3) |
| 원천 구분 선택 | 항상 — ⚠ reason_code 에 접어 넣는다(§5-2) |
| 반품 수량 입력 | ≤ 원 출하 수량(원 출하가 있을 때) |
| 입고 위치 선택 | ✅ 필수 — destination_location_id NOT NULL |
| 반품 입고 등록 | 라인 ≥ 1 AND 위치 지정 |
| 판정 의뢰 | W-04-07 로 이동 — 소관 이동 |
| 반품 취소 | ⛔ 두지 않는다 — 취소는 W-01-13 계열(입고 취소) |

## W-04-07

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 창고 · 상태(의뢰 전 / 판정 대기 / 판정 완료) |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 대상 선택 | 목록 행 클릭 |
| 부적합 등록 | 대상 선택됨 — 심각도·내용 필수 |
| 판정 의뢰 | 부적합 등록됨 AND 의뢰 수량 ≥ 1 |
| 판정 결과 보기 | 판정 완료 — W-03-02 로 이동 |
| 재작업 실적 등록 | 처분 = 재작업 — P-04-03 으로 이동 |
| 폐기 품의 | 처분 = 폐기 — W-04-10 으로 이동 |
| 재고 재등록 | 처분 = 정상 — W-04-11 로 이동 |
| 판정 | ⛔ 두지 않는다 — 03 소관(§5-1) |

## W-04-10

| 액션 | 활성 조건 |
| --- | --- |
| 대상 선택 | 처분 = 폐기 인 것만(§5-2) |
| 사유 입력 | ✅ 필수 — 처분 사유가 기본값(§5-5) |
| 폐기 계정 선택 | ⛔ 두지 않는다 — 회계는 MES 밖(DR-009 · E-4 종결) |
| 결재선 확인 | 자동 — GET /app/approval-routes |
| 품의 상신 | 대상 ≥ 1 AND 사유 입력 AND 결재선 존재 |
| ⭐ 자체 폐기 체크 | 승인 완료 후 — 체크하면 폐기 거래처를 비활성하고 값을 비운다(§5-6) |
| ⭐ 폐기 거래처 선택 | 승인 완료 후 AND 자체 폐기 미체크 — GET /mdm/partners?roleTypeCode=<폐기처리> |
| 기타출고 처리 | 승인 완료 후에만(§5-4) AND (자체 폐기 체크 OR 폐기 거래처 선택) |
| 승인 · 반려 | ⛔ 두지 않는다 — W-CO-09 결재함(J-10) |
| 품의 취소 | ⛔ 두지 않는다 — 상신 철회 경로가 승인 계약에 없다(01 요구서 §5-3) |

## P-04-03

| 액션 | 활성 조건 |
| --- | --- |
| W/O 선택 | 진입 시 — 내 재작업 W/O 목록 |
| 수량 입력 | 숫자 키패드(D-4) — 다섯 칸 |
| 불량 코드 선택 | 불량 > 0 일 때 필수(§5-3) |
| 실적 저장 | 합계 > 0 AND (불량 = 0 또는 불량 코드 선택됨) |
| 다시 입력 | 항상 — 입력 초기화 |
| 실적 수정 | ⛔ 두지 않는다 — 실적은 이력이다(B-3) |
| 재작업 완료 | ⚠ 이 화면에서 하지 않는다 — W/O 마감은 02 소관 |

## M-04-03

| 액션 | 활성 조건 |
| --- | --- |
| 원 포장 스캔 | 항상 — D-2 |
| 직접 입력 | 항상 — D-3 대체 경로 의무 |
| 포장 추가 스캔 | 합병 유형일 때 — 여러 원 포장 |
| 재구성 유형 선택 | 원 포장 스캔됨 |
| 구성 편집 | 유형 선택됨 — 숫자 키패드(D-4) |
| 재구성 확정 | 새 구성 유효 AND 수량 합 = 원 수량 합 |
| 라벨 발행 | ⛔ 여기서 하지 않는다 — P-04-04(§5-1) |
| 재구성 취소 | 확정 전 — 화면 이탈 |
| 확정 후 되돌리기 | ⛔ 두지 않는다 — 이벤트 이력이 없어 되돌릴 근거가 없다(§5-2) |

---

**합계: 화면 16 · 액션 149**
