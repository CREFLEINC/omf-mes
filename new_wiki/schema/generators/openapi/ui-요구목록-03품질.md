# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain 03` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-03품질.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.

⚠ **액션 표가 없는 화면 1장** — `W-01-01`.
이 화면들의 요구는 §5 본문에서 사람이 읽어 §3 에 옮긴다. **목록에 없다고 없는 것이 아니다.**


## W-03-01

| 액션 | 활성 조건 |
| --- | --- |
| 모드 전환(LOT / 이력) | 항상 — ⚠ 이력 탭 권한 제한은 E-9 후(§5-5) |
| LOT 조회 | 항상. ⭐ 목록과 요약을 같은 질의로 부른다(page·size·sort 만 뺀다) — 한쪽에만 필터가 있으면 카드와 목록이 서로 다른 것을 센다(#175) |
| 이력 조회 | 기간 입력 시에만(L-3) — ⭐ GET /quality/lot-hold-events(2026-08-21 신설 · #176). 등록·해제를 사건별 행으로 낸다 |
| 정렬·페이지 | 지정 열만(L-4) — ⭐ sort 로 서버에 맡긴다. ⛔ 현재 페이지만 다시 정렬하면 서버 전체를 정렬한 것처럼 보이면서 결과가 틀린다(#176) |
| LOT 선택 → 드로어 | 목록에서 |
| 판정·전이 처리 → | 드로어에서 — W-03-02로 이동 |
| 의심자재 등록 | ⛔ 없다 — W-03-03 경로 안내(W-01-07 §5-6과 같은 규칙) |
| 새로고침 | 항상 |

## W-03-02

| 액션 | 활성 조건 |
| --- | --- |
| 대상 조회 | 항상 — 상태·기간·LOT 필터 |
| LOT 선택 | 목록에서 |
| 전이 대상 선택 | 서버가 준 선택지에서만(§5-5) · 선택지 0개면 안내 문구 |
| 수량(전량/일부) | 보류 건이 있을 때 · 일부는 hold_qty 이하 |
| 사유 입력 | ⚠ 필수로 강제한다 — 컬럼이 remarks뿐이어도(§5-4) |
| 전이 확정 | 대상·사유가 모두 있을 때 · B-1 토큰 동봉 |
| 변경 이력 보기 | ⛔ W-03-01로 이동 — 이 화면은 처리만 한다 |

## W-03-03

| 액션 | 활성 조건 |
| --- | --- |
| 대상 조회 | 항상 — ⚠ 이미 전량 보류인 LOT은 선택 불가로 표시(§5-5) |
| LOT 선택(다중) | 목록에서 |
| 범위(전량/일부) | 1건일 때만 「일부」 가능(§5-3) |
| 사유 선택 | ⚠ 값 목록 미확정 — G-2 표시(§8-2) |
| 해제 조건 입력 | ⚠ 화면이 필수로 만든다(§5-4) |
| 보류 등록 | 대상 ≥1 · 사유 · 해제 조건이 있을 때 · B-1 토큰 동봉 |
| 판정·전이 처리 → | ⛔ 없다 — 등록 후 안내만(W-03-02 경로) |

## W-03-05

| 액션 | 활성 조건 |
| --- | --- |
| 필터·조회 | 기간 입력 시에만(L-3) |
| 보기 전환(목록 / 불량률 / 분포) | 항상 |
| 분리해 보기(교정 만료) | 해당 건이 있을 때(§5-6) |
| 정렬·페이지 | 지정 열만(L-4) |
| 검사 선택 → 드로어 | 목록에서 |
| 측정치 전체 보기 → | 드로어에서 — 별도 페이지(§5-1) |
| 검사 결과 수정 | ⛔ 없다 — 조회 화면(W-01-07 §5-6과 같은 규칙) |
| 내보내기 | ⚠ 미정 — §8-6 |

## W-03-09

| 액션 | 활성 조건 |
| --- | --- |
| 유형·상태 필터 | 항상 |
| 요청 선택 | 행 클릭 |
| 부적합 열기 | 특채이고 nonconformance_id 있음 |
| 승인 | 내 차례 AND 사유 입력됨 |
| 반려 | 동 — 사유 필수 |
| 승인 이력 보기 | 항상 |
| 조건 수정 | ⛔ 두지 않는다 |
| 요청 취소 | ⛔ 두지 않는다 — 요청자 소관 |

## W-03-10

| 액션 | 활성 조건 |
| --- | --- |
| 조회 | 기간 입력됨(L-3) |
| 부적합 선택 | 행 클릭 |
| 판정 저장 | 처분 선택 AND 수량 ≥ 1 AND 수량 ≤ 남은 수량 AND 사유 입력 |
| 취소 | 입력 있음 |
| ⛔ 승인·반려 | 두지 않는다 |

## P-02-13

| 액션 | 활성 조건 |
| --- | --- |
| 항목 판정(합격/불합격) | 육안 항목. ⭐ 목록은 GET /mdm/code-values?codeGroupCode=INSPECTION_MEASUREMENT_JUDGMENT — ACCEPTED·REJECTED 2값. ⚠ 종합 판정과 그룹이 다르다 — 항목에는 「보류」가 없다(#179 · G-32) |
| 측정값 입력 | 계측 항목 — 키패드(D-4 · G-6) |
| 수량 입력 | 합격·불합격·보류 — 키패드 |
| 종합 판정 선택 | 목록에서(§5-4 — 값 개수 무관) |
| 처분 선택 | 불합격 > 0일 때 — 잠정(§5-8) |
| 임시 저장 | 항상 |
| 검사 확정 | 합계 일치(A-3) AND 전 항목 판정 AND 종합 판정 선택 AND can_input_inspection |

---

**합계: 화면 7 · 액션 50**
