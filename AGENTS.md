# 연쇄 PR 작업 규칙

2026-09-07 사용자 승인. 이 문서는 Codex가 담당하는 부모·자식 연쇄 PR의 Git 작업 규칙을 추가한다.
기존 역할 경계·원자료 보호·검증 의무는 그대로 유지한다. 다른 담당자의 독립 PR 정책은 변경하지 않는다.

## 브랜치와 병합 방식

- 선행 작업이 미병합이면 직전 작업 브랜치에서 `codex/` 접두의 새 브랜치를 만든다. 새 PR의 base도 그 부모 브랜치로 지정한다.
- 선행 작업이 모두 main에 병합됐다면 최신 `origin/main`에서 시작한다.
- **연쇄 PR은 `Create a merge commit`으로 병합한다.** 팀 스킬의 일반적인 squash 기본값보다 이 사용자 승인 예외가 우선한다. `Squash and merge`와 `Rebase and merge`는 이 연쇄 PR에 사용하지 않는다.
- 부모 PR부터 순서대로 병합한다. 리뷰 통과만으로 에이전트가 자동 병합하지 않는다. 실제 병합은 사용자에게 맡긴다.
- PR 본문에 Merge commit 사용과 부모 관계를 명시한다. 이 문서는 GitHub 서버의 강제 설정이 아니므로 사용자가 병합 UI에서 방식을 선택해야 한다.

## 부모 병합 이후

- 부모 브랜치 삭제 후 자식 PR의 base가 main으로 바뀌는 것은 정상이다. 충돌 표시를 숨기려고 이미 병합된 부모 브랜치로 base를 되돌리지 않는다.
- fetch 후 부모 head가 최신 base의 조상인지 먼저 확인한다. 이력이 정상적으로 이어지고 충돌도 없다면 base 변경만을 이유로 main 재병합이나 전체 후속 브랜치 재작성을 하지 않는다.
- 실제 충돌이 있으면 공통 조상과 충돌 파일을 확인한다. 업무 내용 충돌과 과거 squash 이력에 따른 생성물 충돌을 구분한다.
- 생성 대장·요약은 양쪽 중 하나를 임의 선택하지 않고 해당 정본의 공식 생성기로 다시 만든다. 업무 소스와 다른 담당자의 변경은 보존한다.
- 과거 squash 때문에 조상이 이어지지 않는 경우에는 필요한 브랜치만 일회성 동기화하고 관련 후속 PR을 검증한다. rebase·강제 푸시처럼 공유 이력을 재작성하는 방식은 별도 승인 없이 사용하지 않는다.

## 연속 작업

- 사용자가 연속 처리를 요청한 경우 PR 제출만으로 작업을 끝내지 않는다.
- 한 작업이 끝날 때 담당 PR의 일반 코멘트·공식 리뷰·인라인 코멘트 및 충돌 상태를 확인한다.
- 타당한 지적과 담당 범위의 충돌은 수정·검증·푸시한다. 리뷰가 없거나 처리할 지적이 없으면 다음 안전한 작업을 이어간다.
- 새 업무 결정·추가 권한·외부 협의가 필요하면 임의로 정하지 않고 필요한 사용자 입력을 설명한다. 다른 담당자의 작업을 대신 수정하거나 병합하지 않는다.
