# OMF 저장소 Agent 협업 규칙

이 파일은 세션이 바뀌어도 유지해야 하는 사용자 지시다. 직접적인 최신 사용자 지시가 이 파일과 충돌하면 최신 사용자 지시를 우선하고, 충돌 사실을 사용자에게 알린다.

## 문서 작성

- 설계서, 계획서, 보고서, 의사결정 기록 등 사람이 읽는 문서를 새로 만들거나 수정할 때는 반드시 `crefle-agent-skills:crefle-doc` 스킬을 먼저 사용한다.
- 작업 시작 시 해당 스킬을 사용하는 이유를 사용자에게 알린다.
- 스킬이 설치되어 있지 않거나 현재 세션에서 사용할 수 없으면 임의의 다른 스킬로 조용히 대체하지 않는다. 가용성 문제를 사용자에게 알리고, 스킬 설치·활성화 또는 대체 방법을 사용자와 합의한 뒤 문서 작업을 진행한다.
- 생성된 HTML 등 파생 산출물이 있는 경우 정본과 생성물의 관계를 확인하고, 생성물을 직접 편집하지 않는다.

## 단계별 공동 진행

- 작업을 시작하기 전에 사용자가 이해할 수 있는 단계로 계획을 나눈다.
- 각 단계 시작 전에 그 단계의 목표, 산출물, 확인할 의사결정을 사용자에게 설명한다.
- 한 단계를 마치면 결과와 검증 내용을 공유하고 사용자의 확인을 받은 뒤 다음 단계로 진행한다.
- 사용자의 확인 없이 여러 단계를 연속으로 완료하거나 구현을 앞서 진행하지 않는다.
- 진행 중 새 가정, 범위 변경, 예상 밖의 제약이 생기면 즉시 공유하고 해당 단계 안에서 다시 합의한다.

## 사용자 승인 필수 의사결정

- 다음 항목은 Agent가 단독으로 확정하거나 구현하지 않는다.
  - 핵심 기능의 범위와 동작
  - 시스템 아키텍처와 서비스 경계
  - 데이터 모델과 정본·버전·권한 정책
  - Agent 도구 및 외부 API 계약
  - 검색·색인·랭킹·생성 전략
  - 데이터베이스, 검색엔진, 모델, 프레임워크 등 외부 의존성의 선택·추가·교체
  - 인증, 보안, 배포, 운영 방식
- 의사결정이 필요하면 Agent는 먼저 권장안과 근거를 제시한다.
- 가능한 대안과 주요 장단점, 비용·복잡도·향후 변경 가능성을 함께 설명한다.
- 사용자가 명시적으로 최종 확인한 뒤에만 해당 결정을 계획과 구현에 반영한다.
- 이전에 승인된 결정도 범위나 전제가 달라지면 다시 확인한다.

## 현재 정보 조회 플랫폼 작업의 진행 원칙

- 범용 정보 조회 플랫폼을 지향하되 첫 검증 대상은 OMF-MES 설계 문서다.
- 1차 목표는 3일 안에 두 명(사용자와 Agent)이 학습과 구현을 함께 진행하며 검증 가능한 MVP를 만드는 것이다.
- 사내 Agent가 지식 조회 필요성을 자율 판단하되, 내부 설계 사실·규정·정책·API·데이터 모델에 답할 때는 조회를 강제하는 구조를 사용한다.
- MVP의 대표 질의는 특정 기능의 요구사항 및 정책 결정 원문을 찾는 것이다. 응답에는 요약, 원문 인용, 파일 경로와 위치를 포함한다.
- 공유 서비스로 배포하며 Ubuntu와 Docker Compose를 사용한다. 초기 재색인은 관리자가 수동으로 실행해도 된다.
- 내부 원문과 인덱스는 사내 환경에 저장한다. 검색된 최소 근거는 답변 생성을 위해 Codex API로 전송할 수 있다.
- 현재 PostgreSQL 기반 하이브리드 검색은 사용자가 검토 후 승인한 MVP 출발안이다. 세부 스키마, 확장, 검색 방식 또는 대체 저장소 선택은 별도 승인 대상으로 유지한다.
