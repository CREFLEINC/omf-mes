# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain print` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-app공통출력물.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.

⚠ **액션 표가 없는 화면 3장** — `P-02-05` · `P-02-07` · `P-02-09`.
이 화면들의 요구는 §5 본문에서 사람이 읽어 §3 에 옮긴다. **목록에 없다고 없는 것이 아니다.**


## P-04-02

| 액션 | 활성 조건 |
| --- | --- |
| 라벨 종류 선택 | 항상 |
| 대상 선택 | 종류 선택됨 — 납품라벨은 합격 건만(§5-1) |
| 재발행 사유 선택 | 이미 발행된 대상 — ✅ 목록에서 고른다(그룹 REISSUE_REASON · §5-4) |
| 프린터 선택 | 항상 — ⚠ 자유 텍스트라 목록이 어디서 오는지 미정 |
| 미리보기 | 대상 선택됨 — 서버 렌더링 이미지(K-5) |
| 발행·인쇄 | 대상 선택됨 · (재발행이면) 사유 선택됨 — ✅ 열린다(§5-6 · POST /app/document-issues) |
| 발행 이력 보기 | 대상에 회차 ≥ 1 |
| 발행 취소 | ⛔ 두지 않는다 — 기록 전용(K-1 · B-3) |

## P-04-04

| 액션 | 활성 조건 |
| --- | --- |
| 대기 목록 조회 | 항상 — 상태 = 라벨 발행 대기 |
| 대상 선택 | 목록 행 클릭 |
| 인쇄 대상 선택 | 신규는 기본 체크 · 잔량은 선택(§5-3) |
| 재출력 사유 선택 | 잔량 체크됨 — ✅ 목록에서 고른다(그룹 REISSUE_REASON · §8-2) |
| 프린터 선택 | 항상 — ⚠ 목록 원천 미정 |
| 미리보기 | 대상 선택됨 — 서버 렌더링(K-5) |
| 발번·인쇄 | 대상 선택됨 · (잔량 재출력이면) 사유 선택됨 — ✅ 열린다(§5-5 · POST /app/document-issues) |
| 발행 이력 보기 | 대상에 회차 ≥ 1 |
| 발번 취소 | ⛔ 두지 않는다 — 번호는 회수하지 않는다 |

---

**합계: 화면 2 · 액션 17**
