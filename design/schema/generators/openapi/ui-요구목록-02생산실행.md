# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain 02` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-02생산실행.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.

⚠ **액션 표가 없는 화면 6장** — `P-02-03` · `P-02-06` · `P-02-08` · `P-02-10` · `P-02-11` · `P-02-12`.
이 화면들의 요구는 §5 본문에서 사람이 읽어 §3 에 옮긴다. **목록에 없다고 없는 것이 아니다.**


## P-02-04

| 액션 | 활성 조건 |
| --- | --- |
| 변경(LOT / 제품) | 항상 |
| 키패드 숫자·C·← | 수량 필드 포커스 시 |
| ＋10 · ＋100 | 동상 |
| 저장 | 양품수량 > 0 AND can_input_result 플래그 AND 잔여수량 검증 통과 |
| 취소 | 입력 있음 |

## W-02-04

| 액션 | 활성 조건 |
| --- | --- |
| LOT 크기 입력 | 항상 |
| 전달사항 등록 | 항상 |
| 확정·배포 | 배정 Validation OK |
| 취소 | 항상 |
| W/O 수정 | — |
| 출고요청 확인 | — |

## M-02-01

| 액션 | 활성 조건 |
| --- | --- |
| 화면 진입 | ⛔ 온라인일 때만(§5-1) |
| ① LOT 스캔 | 항상 — 포커스 고정 |
| ② 다음 공정 선택 | LOT 확정됨 — work_order_dependency로 후보 제시 |
| 수량 입력 | 공정 선택됨 — 키패드(G-6) |
| 직접 입력 | 항상(D-3) |
| 인계 확정 | LOT·공정·수량 확정 — C-3(건별 + 일괄 완료) |

## M-02-02

| 액션 | 활성 조건 |
| --- | --- |
| 화면 진입 | ⛔ 온라인일 때만(M-02-01 §5-1과 같은 처리) |
| 탭 전환(투입/반출) | 항상 |
| LOT 스캔 | 항상 — 포커스 고정 |
| 수량 입력 | LOT 확정 — 키패드(G-6) |
| 직접 입력 | 항상(D-3) |
| 투입 등록 | ⚠ 저장 자리 부재 — 비활성 + 사유(§5-3) |
| 반출 등록 | ⚠ 동 · 투입 건 선택 필요 |

## W-02-01

| 액션 | 활성 조건 |
| --- | --- |
| 조회 | 항상 |
| P/O 선택 | 목록에서 |
| 계층 펼침 | parent_production_order_id가 있는 행 |
| W/O 전개 → | ⚠ 전개되지 않은 P/O — W-02-02로 이동 |
| ERP 재동기 요청 | ⚠ 이 화면에 두지 않는다 — W-06-10(연계 현황) 소관 |
| 편집 | ⛔ 없다 — 전체 읽기 전용(§5-1) |

## W-02-02

| 액션 | 활성 조건 |
| --- | --- |
| 계획 추가·삭제 | 확정 전 |
| BOM·Routing Rev 선택 | 후보 2건 이상 시(§5-6) |
| 러닝체인지 분할 | 확정 전 · 계획 1건 이상 — 원본 계획을 골라 새 계획을 만든다(§5-5). ⚠ 편성 시점 분할만 — 진행 중 W/O 분할은 미해결(§8) |
| 저장 | 계획 1건 이상 |
| 전개 확정 → | 마스터 점검 ✅ AND 계획 1건 이상 AND 합계 검증 통과 → W-02-03 |

## W-02-03

| 액션 | 활성 조건 |
| --- | --- |
| W/O 선택 | 목록에서 |
| 자원 선택(설비·작업자·금형·라인·교대) | 각 1개(§5-1) |
| 계획 시각 입력 | 항상 — ⚠ 중복 판정에 필요(§5-4) |
| 우선순위 조정 | 항상 |
| Validation 재실행 | 배정 변경 시 자동 + 수동 버튼 |
| 경고 확인 체크 | ⓑ 경고가 있을 때 |
| 저장 | 배정 1건 이상 |
| 확정 대기로 → | ⓐ 차단 0건 AND (ⓑ 경고 0건 또는 확인 체크됨) → W-02-04 |

## W-02-05

| 액션 | 활성 조건 |
| --- | --- |
| 조회 · W/O 선택 | 항상 |
| 잔량 처분 선택(이월/소멸) | 미달 판정일 때만 |
| 사유 선택 | 미달·초과일 때 |
| 송신 항목 토글 | ⚠ 설정 테이블 부재 — 비활성 + 사유(G-2 · #66) |
| 마감 | 잔량 처분 선택됨(미달 시) AND 사유 선택됨 — ⚠ 되돌릴 수 없다 |

## W-02-06

| 액션 | 활성 조건 |
| --- | --- |
| 변경 알림 선택 | 목록에서 |
| 판정 선택(반영/강행) | 알림 선택됨 |
| 사유 입력 | 강행 선택 시 필수(화면 규칙 — DB는 강제하지 않는다) |
| 확인 처리 | 판정 선택됨 AND (반영 또는 사유 입력됨) |

## W-02-07

| 액션 | 활성 조건 |
| --- | --- |
| 품목 검색·선택 | 항상 |
| 수량·납기 입력 | 품목 선택됨 |
| 사유 입력 | 항상 — 화면 규칙으로 필수(§5-3) |
| 자동 전개(암묵) | 품목 선택 시 BOM·Routing 조회 |
| 발행·배포 | 품목·수량·사유 입력 AND BOM·Routing 존재 AND 권한 있음 |

## W-02-08

| 액션 | 활성 조건 |
| --- | --- |
| 기간 필터 | 비울 수 없다(§5-1 ③) |
| 조회 | 기간 입력됨 |
| 정렬 | 지정된 열만(§5-1 ④) |
| 페이지 이동 | 항상 |
| W/O 선택 → 드로어 | 목록에서 |
| 마감 → | 마감 대기 상태 → W-02-05 |
| 새로고침 | 항상 |
| 자동 갱신 | ⛔ 없다(§5-1 ⑥) |

## W-02-10

| 액션 | 활성 조건 |
| --- | --- |
| W/O 검색·선택 | 항상 |
| 도착 위치 선택 | W/O 선택됨 — 기본값 = work_order.default_wip_location_id |
| BOM 소요량 불러오기 | W/O 선택됨 |
| 품목 추가·삭제 | 항상 |
| 요청 수량 입력 | 라인당 |
| 사유 선택 | 값 목록 도착 전 비활성 + 사유(G-2) |
| 요청 발행 | 라인 1건 이상(수량 > 0) AND (사유 선택 또는 비고 입력) |

## P-02-01

| 액션 | 활성 조건 |
| --- | --- |
| 사번 입력 | 항상 — 숫자 키패드(D-4 · ds-gap G-6) |
| 지시 선택 | 사번 확인됨 · 목록은 GET /production/work-orders?plannedEquipmentId={헤더 설비}&open=true 로 받는다 — 설비 축 파라미터를 2026-08-27 에 계약에 신설했다(omf-mes#247). 「전체 보기」는 plannedEquipmentId 만 뺀다 |
| 작업 시작 | 사번 + 지시 + can_start_work + P-02-02 통과 |
| 재개 | 상태 = 중단 · 위와 같은 조건 · POST /production/work-sessions/{workSessionId}/events · eventTypeCode=RESUME(사유 비움) — ⛔ :resume 를 부르지 않는다 §5-4 |
| 전체 보기 | 이 설비 밖 지시도 본다 — ⚠ 경고(§6) |

## P-02-02

| 액션 | 활성 조건 |
| --- | --- |
| 돌아가기 | 항상 — P-02-01 로 |
| 다시 확인 | 항상 — 판정을 다시 돌린다(점검을 방금 올렸을 수 있다) |
| 우회하고 시작 | ⭐ 긴급 W/O + 판정이 「이력 없음」일 때만 · ⛔ NG 이면 비활성(§5-7) |
| 진행(경고 시) | 통제 수준 = WARN · 확인 다이얼로그 |

---

**합계: 화면 14 · 액션 81**
