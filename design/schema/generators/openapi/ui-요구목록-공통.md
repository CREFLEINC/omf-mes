# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain co` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-공통.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.


## W-CO-01

| 액션 | 활성 조건 |
| --- | --- |
| 로그인 | 아이디·비밀번호 둘 다 입력 |
| 언어 전환 | 항상 |

## W-CO-03

| 액션 | 활성 조건 |
| --- | --- |
| 읽음 처리 | 목록 항목 클릭 시 자동 |
| 모두 읽음 | 안 읽음 1건 이상 |
| 대상으로 이동 | openable 이 참일 때만(A-10 규칙 2 — 대응표는 서버가 갖는다 §5-3). 도착지는 screenId |
| 필터 | 안 읽음 / 전체 · 유형 · 기간(필수 — L-3) |

## W-CO-10

| 액션 | 활성 조건 |
| --- | --- |
| 변경 | 세 필드 입력 + 새 비밀번호 8자 이상 + 두 값 일치 |
| 취소 | 항상 — 직전 화면으로 |

## W-CO-11

| 액션 | 활성 조건 |
| --- | --- |
| 이벤트 선택 | 항상 |
| 조직×역할 추가 · 개인 추가 | 이벤트 선택됨 |
| 해제 | 행 선택 |
| Zalo 토글 | 이벤트 선택됨 |
| 저장 | 변경분 존재 |

## W-CO-04

| 액션 | 활성 조건 |
| --- | --- |
| 새 공지 | 작성 권한 |
| 게시 | 작성 중인 공지 · 작성 권한 — ⚠ 게시하면 본문이 잠긴다 |
| 수정 | ⚠ 게시 전에만 — 게시 후 본문 수정은 이미 확인한 사람이 다른 것을 본 것이 된다. ⭐ 확인 이력이 무엇에 대한 확인인지 알 수 없어진다 → G-19(고칠 수 없는 기록) |
| 종료일 당기기 | 게시 중 |
| 미확인자 보기 | 확인 요구 공지 · 관리자 |

## W-CO-05

| 액션 | 활성 조건 |
| --- | --- |
| 카드 클릭 |  |
| 알람 항목 클릭 |  |
| 자세히 |  |
| 갱신 |  |

## W-CO-08

| 액션 | 활성 조건 |
| --- | --- |
| 편집 | 전산담당·물류담당 |
| 도면 올리기 | 편집 모드 |
| 점 찍기·옮기기 | 편집 모드 · 위치 선택 |
| 재고 조회 | 위치 선택 |
| 저장 | 편집 모드 · 변경 있음 |

## W-CO-06

| 액션 | 활성 조건 |
| --- | --- |
| 단말 등록 | 항상 — terminal_code·plant_id·terminal_type_code·status_code 입력 시 |
| 단말 수정 | 선택 시. 단말 코드는 잠김(B-4 — 키는 바꾸지 않는다) |
| 토큰 발급·재발급 | 선택 시 — ⚠ 「이전 토큰 전부가 끊깁니다」 경고 필요 §6(§5-4) |
| 공정 추가 | 단말 선택 시. 이미 있는 공정은 목록에서 제외 |
| 공정 삭제 | 행 선택 시 — ⚠ 경고 필요 §6 |
| 플래그 토글 | 공정 행이 있을 때 |
| 저장 | 변경이 있을 때만. 단말 단위 한 트랜잭션(§5-3) |
| 사용 중지 | ⚠ 경고 필요 §6 |
| 변경 이력 | ⛔ 없다 — §I-5(audit_event 키 규약 부재) |

## M-CO-01

| 액션 | 활성 조건 |
| --- | --- |
| QR 스캔(등록) | 상태 A · 온라인일 때만 |
| 등록 코드 직접 입력 | 상태 A — ⚠ 채택 시(§5-2 C안) |
| 숫자 입력·확인 | 상태 B |
| 교대(사번 바꾸기) | 현재 작업자가 있을 때 |
| 작업 목록으로 → | 현재 작업자가 있을 때 |
| 언어 전환 | 항상 — 범위 문구 필수(§5-3) |
| 등록 해제 | 설정 메뉴 — ⚠ 미동기 건수 표시 필수(§5-5) |

## P-CO-01

| 액션 | 활성 조건 |
| --- | --- |
| 숫자 입력·지움·백스페이스 | 항상 |
| 확인 | 입력 길이 ≥ 1 — ⚠ 자릿수는 강제하지 않는다(§5-2) |
| 교대(사번 바꾸기) | 현재 작업자가 있을 때 |
| 작업 화면으로 → | 현재 작업자가 있을 때 |
| 단말 정보 표시 | 항상 — 헤더에 terminal_code·설치 위치 |

---

**합계: 화면 10 · 액션 48**
