# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --domain 01` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-01자재창고.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.

⚠ **액션 표가 없는 화면 4장** — `W-01-01` · `W-01-02` · `W-01-10` · `M-01-01`.
이 화면들의 요구는 §5 본문에서 사람이 읽어 §3 에 옮긴다. **목록에 없다고 없는 것이 아니다.**


## M-01-02

| 액션 | 활성 조건 |
| --- | --- |
| 입하 건·라인 선택 | 항상 — LOT 이 비어 있는 사전부착 라인만 목록에 든다(§5-1) |
| 스캔(암묵) | 스캔 필드 포커스 — 항상 유지 |
| 직접 입력 | 스캐너 실패 시 대체 경로. 반드시 제공(스캐너 배터리·손상) |
| 등록(1건) | 34자리 검증 통과 AND 입하 라인 선택됨 «(정합주: 2026-09-02 — 종전 「P/O 선택됨」 · omf-mes#354)» |
| 등록 완료 | 1건 이상 등록됨 |
| 목록 행 삭제 | 미동기 건에 한함 |

## W-01-03

| 액션 | 활성 조건 |
| --- | --- |
| 초과분 수량 조정 | 항상 |
| 예외 유형·사유 입력 | 초과분 존재 |
| 분리 등록 | 필수 충족 |
| 취소 | 항상 |
| 반품 진행 | — |
| 신규 P/O 등록 | 등록 완료 후 |

## W-01-09

| 액션 | 활성 조건 |
| --- | --- |
| 조회 | 항상 |
| 예정 건 선택 | 항상 |
| 정렬 | 항상 |
| 입하 등록으로 이동 | 선택 건 있음 |
| P/O 변경 확인 | 변경 이벤트 있음 |

## W-01-11

| 액션 | 활성 조건 |
| --- | --- |
| 라인 추가 | 항상 |
| 라인 삭제 | 라인 ≥ 2 |
| 등록 | 필수 충족 + 라인 ≥ 1 |
| 취소 | 항상 |
| 승인 요청 | 등록 완료 후 |

## M-01-06

| 액션 | 활성 조건 |
| --- | --- |
| 오류 등록 | 유형·수량 충족(사유는 선택 §4-A) |
| 취소 | 항상 |
| 반품 진행 | — |
| 폐기 전환 | — |

## P-01-01

| 액션 | 활성 조건 |
| --- | --- |
| 입하 라인 선택 | 항상 |
| 등록·인쇄 | 라인 선택됨 |
| 인쇄(등록 완료분) | 선택한 라인의 lotId가 있고 labelIssued = false |
| 재인쇄 | 발행 이력 있음 + 사유 입력 |
| 프린터 선택 | 프린터 2대 이상 |

## W-01-04

| 액션 | 활성 조건 |
| --- | --- |
| 실사 개시 | 창고·유형·계획일 입력 — 라인은 서버가 장부에서 생성 |
| 조회 | 항상 |
| 실사 선택 | 목록에서 |
| 블라인드 전환 | 개시 전에만(§5-2) |
| 차이 사유 입력 | 차이 있는 라인 |
| 실물 수량 수정 | ②안 채택 시 · 이력 남김(§5-3) |
| 실사 마감 | 미실사 = 0 AND 차이 = 0 또는 조정 완료(§5-5) |
| 조정 등록 | 차이 1건 이상 → W-01-12 |

## M-01-04

| 액션 | 활성 조건 |
| --- | --- |
| 스캔(암묵) | 스캔 필드 포커스 — 항상 유지(D-2) |
| 직접 입력 | 항상(D-3 의무) |
| 다음 스캔 | 조회 결과가 있을 때 — 화면 초기화 |

## M-01-05

| 액션 | 활성 조건 |
| --- | --- |
| 지시 선택 | 목록에서 |
| 위치 스캔 | 지시 선택됨 · 관리 창고 |
| 위치 선택 | 지시 선택됨 · 비관리 창고(§5-3) |
| LOT 스캔 | 위치 확정됨 |
| 직접 입력 | 항상(D-3) |
| 적치(1건) | 위치·LOT 모두 확정 AND allow_mixed_* 통과 |
| 적치 완료 | 1건 이상 — C-3(건별 저장 + 마지막 일괄 완료) |

## M-01-07

| 액션 | 활성 조건 |
| --- | --- |
| 임시 위치 스캔 | 대상 선택됨 |
| 목록에서 선택 | 항상(D-3 대체 경로) |
| 사유 선택 | ✅ 항상 활성 — 값 목록이 있다(NO_SPACE·INSPECTION_HOLD·LOCATION_UNASSIGNED·OTHER 4값 · GET /mdm/code-values?codeGroupCode=PUTAWAY_TASK_TEMPORARY_REASON). ⛔ G-2(비활성 + 사유)를 적용하지 않는다 — 목록이 «비어서» 오면 그때만 G-2 다 |
| 임시 적치 등록 | 위치 확정 AND (사유 선택 또는 비고 입력) §5-1 |

## M-01-08

| 액션 | 활성 조건 |
| --- | --- |
| 출고요청 선택 | 목록에서 |
| 라인 선택 | 요청 선택됨 · 보류 라인은 비활성 + 사유(F-1 원리) |
| LOT 스캔 | 라인 선택됨 |
| 수량 입력 | LOT 확정 — 키패드(D-4는 POP 규약이나 §7-1) |
| 직접 입력 | 항상(D-3) |
| 피킹(1건) | 수량 ≤ 계획 AND 보류 아님 |
| 출고 확정 | 1건 이상 — C-3 · 오프라인이면 「미확정」 표식(§5-3) |

## M-01-09

| 액션 | 활성 조건 |
| --- | --- |
| 출고 QR 스캔 | 항상 — 포커스 고정(D-2) |
| 직접 입력 | 항상(D-3) |
| 수량 입력 | 라인 매칭됨 — 키패드(G-6) |
| 차이 사유 | variance_qty ≠ 0일 때 노출 |
| 호퍼 잔량 입력 | ⛔ 비활성 + 사유(G-2) — §5-3 |
| 입고 확정 | 1라인 이상 수령 — C-3 |

## M-01-10

| 액션 | 활성 조건 |
| --- | --- |
| 유형 전환(일반/불량) | 항상 |
| 단위 전환(자재별/파렛트) | 정책이 허용할 때(§5-2) |
| ① 반출 스캔 | 항상 — 포커스 고정 |
| ② 도착 스캔 | ①이 1건 이상 |
| 직접 입력 | 항상(D-3) |
| 이동 완료 | ②까지 완료된 건 1개 이상 — C-3 |
| 미완 이동 이어하기 | 진입 시 목록에 있으면 |

## M-01-11

| 액션 | 활성 조건 |
| --- | --- |
| 실사 선택 | 진행 중 실사 목록 |
| 위치 스캔 | 실사 선택됨 — 포커스 고정 |
| 수량 입력 | 라인 선택됨 — 키패드(G-6) |
| 목록에 없는 재고 추가 | 온라인일 때만(§5-3) |
| 직접 입력 | 항상(D-3) |
| 이 위치 완료 | 그 위치의 라인 1개 이상 입력 — C-3 |

## P-01-02

| 액션 | 활성 조건 |
| --- | --- |
| 라인 선택 · 전체 선택 | 출고 전표 선택됨 |
| 유형 전환(라인/파렛트) | ✅ 항상 활성 — GOODS_ISSUE_LINE ↔ HANDLING_UNIT 둘 다 계약 enum 에 있다(§4-A). ⛔ G-2(비활성 + 사유)를 적용하지 않는다 «(정합주: 2026-09-03 — 옛 조건은 「값 목록 도착 전 비활성 + 사유」였다)» |
| 발행·인쇄 | 대상 선택됨 AND (회차 1 또는 사유 선택됨) |
| 재발행 사유 선택 | 회차 2 이상일 때 필수 노출(K-2) |

## W-01-05

| 액션 | 활성 조건 |
| --- | --- |
| 대상 조회·선택 | 항상 |
| 사유 선택 | 대상 선택됨. ✅ 값 목록이 있다 — GET /mdm/code-values?codeGroupCode=GOODS_ISSUE_REASON 5값(§4-A · §8-2 해소). ⛔ G-2(비활성 + 사유)를 적용하지 않는다 — 목록이 «비어서» 오면 그때만 G-2 다 |
| 대체입고 예정 선택 | 항상 |
| 반품 수량 입력 | 라인당 — 보유 수량 이하 |
| ERP 송신 토글 | 항상 |
| 반품 처리 | 라인 1건 이상 AND 사유 선택 |

## W-01-06

| 액션 | 활성 조건 |
| --- | --- |
| 대상 선택 | 불량창고 입고분만(REQ-PR-0025 — 판정은 입고 후) |
| 사유 입력 | 필수(J-4 — DB가 강제) |
| 결재선 확인 | 자동 — approval_type_code+사업부로 조회 |
| 승인 요청 | 대상 1건 이상 AND 사유 입력 AND 결재선 존재 |
| ⭐ 자체 폐기 체크 | 요청 작성 중 — 「승인 요청」 버튼이 이 둘 중 하나를 요구한다(§5-6). 체크하면 폐기 거래처를 비활성하고 값을 비운다 |
| ⭐ 폐기 거래처 선택 | 요청 작성 중 — 「승인 요청」 버튼이 이 둘 중 하나를 요구한다(§5-6) AND 자체 폐기 미체크 — GET /mdm/partners?roleTypeCode=<폐기처리> |
| 기타출고 처리 | 승인 완료 후에만(§5-5) |

## W-01-07

| 액션 | 활성 조건 |
| --- | --- |
| 필터·조회 | 항상 — ⚠ 창고는 필수(범위 제한) |
| 선택지(품질 상태·소유 구분·보류 사유) | 화면 진입 시 한 번 — 공통코드 조회로 채운다. ⛔ 값을 화면에 박지 않는다(G-31 마스터안전형) |
| 보기 전환(품목/LOT/위치) | 항상 |
| 정렬 | 지정 열만(§L ④) |
| 페이지 이동 | 항상 |
| LOT 선택 → 드로어 | 목록에서 |
| 수불 이력 → | 드로어에서 — 기간 입력 필수(§5-4) |
| 새로고침 | 항상 |
| 의심자재 등록 | ⛔ 없다 — W-03-03 경로 안내(§5-6) |

## W-01-12

| 액션 | 활성 조건 |
| --- | --- |
| 원천 선택(실사/직접) | 항상 |
| 실사 차이 불러오기 | 실사 선택됨 |
| 라인 추가·수정 | 직접 등록 시 |
| 사유 선택 | 라인당 ⚠ 저장 자리 없음(§5-1) · 헤더 사유는 필수 |
| ERP 송신 토글 | 항상 |
| 조정 상신 | 라인 1건 이상 AND 헤더 사유 입력 |
| 전기 | 승인 후(또는 승인 불요 시 상신 즉시) |

## W-01-13

| 액션 | 활성 조건 |
| --- | --- |
| 조회·필터 | 항상 — 문서 유형·상태·기간·품목·LOT·창고 |
| 페이지 이동 | 목록 2쪽 이상 — G-12 |
| 문서 선택 | 목록 행 클릭 |
| 원장 보기 | 전기된 문서 — inventory_transaction 조회 |
| 후속 목록 보기 | 후속 ≥ 1 |
| 취소 요청 | 후속 0건 AND 상태가 취소 가능 AND 취소요청 중이 아님 — 사유 필수(§5-4) |
| 취소 실행 | 승인 완료 AND 후속 재판정 통과(§5-4) |
| 취소 요청 철회 | 결재 진행 중 — ⚠ 상신자만 · 승인된 뒤에는 불가(J-6) |
| 대상 화면에서 보기 | 문서 유형에 대응 화면이 있을 때 |

## M-01-12

| 액션 | 활성 조건 |
| --- | --- |
| 스캔 | 항상 |
| 등록 | 자재·구분·수량(>0)·창고·위치 전부 |

## M-01-13

| 액션 | 활성 조건 |
| --- | --- |
| 스캔 | 항상 |
| 요청 | 대상 LOT · 사유 둘 다 |
| 내가 올린 요청 조회 | 진입 시 · 요청 성공 후 자동 — §4 아래 구획을 채운다. ⛔ 사번 헤더 없이 부르지 않는다(§5-4) — 셸이 사번을 쥐고 있다(M-CO-01) |

---

**합계: 화면 22 · 액션 126**
