# UI 요구 목록 — 화면이 API 에 요구하는 것

> 생성물이다. `python3 verify-ui-coverage.py --write --domain 05` 로 다시 만든다.
> 이 목록의 모든 행이 `06-API-요구서-05설비툴.md` §3 매핑표에서 다뤄져야 한다.
> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.


## W-05-11

| 액션 | 활성 조건 |
| --- | --- |
| 계측기 추가 | 편집 권한 |
| 저장 / 취소 | 변경분 존재 · 필수 충족 |
| 사용 중지 | is_active=true · 물리 삭제 버튼 없음(B-4) |
| 폐기 처리 | status_code=운용 · 확인 다이얼로그 |
| 검교정 이력 등록으로 이동 | 계측기 선택 — ⛔ 등록은 W-05-10 이 소유(중복 구현 금지 · B-4-1 ④ 와 같은 취지) |
| 변경 이력 | 항상 — audit.audit_event(B-5) · 기간 필수 |

## W-05-12

| 액션 | 활성 조건 |
| --- | --- |
| 그룹 추가 · 설비 추가 | 편집 권한 |
| 저장 / 취소 | 변경분 존재 · 필수 충족 |
| 사용 중지 | is_active=true · 물리 삭제 버튼을 두지 않는다(B-4) |
| 점검 항목 추가 | 편집 권한 — ⭐ 이 화면이 마스터를 만든다 §5-1-1 |
| 점검 항목 저장 / 취소 | 변경분 존재 · 필수 충족 · ⛔ 짝 제약 — 판정 방식이 「측정값」이면 단위·상하한이 함께 필요(§4-C-1) |
| 점검 항목 사용 중지 | is_active=true · 물리 삭제 없음(B-4) |
| 점검 항목 순서 재배치 | 목록 2건 이상 — sequence_no(§4-C-1 표시 순서 · G-7) · ⭐ 행마다 개별 갱신(§4-C-1 주 · A-5 대상 아님) |
| 점검 항목 부여 편집 | 그룹 또는 설비 선택 상태 |
| 변경 이력 | 항상 — audit.audit_event(target_type_code+target_id, B-5) · 기간 필수(파티션) |
| 계층 텍스트 확인 | 설비 선택 시 §5-3 |

## W-05-13

| 액션 | 활성 조건 |
| --- | --- |
| 툴 추가 | 편집 권한 |
| 저장 / 취소 | 변경분 존재 · 필수 충족 |
| 사용 중지 | is_active=true · 물리 삭제 버튼 없음(B-4) |
| 폐기 처리 | status_code=운용 · 확인 다이얼로그 — WF05 S9 「MES 직접 상태 전환」 |
| 엑셀 업로드(DX 마이그레이션) | 편집 권한 — §5-5 |
| QR 라벨 이미지 생성 | 저장된 툴 선택 · §5-4 |
| 발행 이력 보기 | 발행 기록 1건 이상 |
| 변경 이력 | 항상 — audit.audit_event(B-5) · 기간 필수 |

## M-05-01

| 액션 | 활성 조건 |
| --- | --- |
| 설비 스캔 / 목록 선택 | 항상 (D-3 — 스캔 실패 시 직접 선택) |
| 점검 완료 | 필수 항목 전건 판정 + 설비·유형 선택 |
| 다시 입력 | 미저장 상태 |

## M-05-02

| 액션 | 활성 조건 |
| --- | --- |
| 설비 QR 스캔 / 목록에서 선택 | 항상(D-3) — 스캔 실패 시 목록(§6-1) · 폐기 설비는 목록에서 제외 |
| 증상 입력 | 설비 선택됨 · ✅ 필수(A-12) — 미입력이면 보고 버튼 비활성 + 인라인 |
| 최근 증상 재사용 | 같은 설비의 과거 증상이 있을 때(§5-2) |
| 사진 촬영 | 설비 선택됨 · 0~3장 — 3장이면 비활성 + 사유 · ⚠ 미전송 사진이 큐 한도를 넘으면 비활성 + 사유(§6-2) |
| 발생 상태 선택 *(멈췄다 / 돌지만 이상하다)* | 설비 선택됨 · ✅ 필수(§5-3) |
| 정지 시각 입력 | 「멈췄다」일 때만 의미 있음 · 선택 — 모르면 비운다(§3-3) |
| 설비담당에게 알린다 | 기본 켬 · ⛔ 오프라인이어도 자동으로 끄지 않는다(§6-2 · §9-1) |
| 고장 보고 | 설비 선택 AND 증상 입력 AND 발생 상태 선택 |

## P-05-01

| 액션 | 활성 조건 |
| --- | --- |
| QR 스캔 / 코드 직접 입력 | 항상(D-3) |
| 환산 켜기·끄기 | 툴 선택됨 · 캐비티 수 또는 비율이 있을 때만(§5-4) |
| 실적 저장 | 툴 선택 + 타발수 > 0 |
| 다시 입력 | 입력값 있음 |

## P-05-02

| 액션 | 활성 조건 |
| --- | --- |
| 지금 종료 | 진행 중 비가동 존재 — ①에서만 |
| 시작 [지금] · 종료 [지금] | 항상 — 단말 시각을 넣는다 |
| 「아직 진행 중」 체크 | 종료 시각 입력 시 상호 배타 |
| 실적 저장 | 시작 시각 + 사유 |
| 고장 연결 / 해제 | 이 설비에 열린 고장이 있을 때 |

## W-05-02

| 액션 | 활성 조건 |
| --- | --- |
| 툴 선택(다중) | PM 도래 상태 · 미등록 툴은 선택 불가 |
| 오더 발행 | 선택 1건 이상 + 예정일·담당자 |
| 오더 취소 | 상태 = 발행 · PM 실적 0건일 때만 |

## W-05-03

| 액션 | 활성 조건 |
| --- | --- |
| 오더 선택 | 미완료 오더 |
| 부위 추가·삭제 | 저장 전 |
| 리셋 체크 | 오더 선택됨 |
| 실적 저장 | 오더 + 시행일 + 결과 내용 + 부위 1개 이상 |
| 마감 체크 | 실적 저장과 함께 |

## W-05-04

| 액션 | 활성 조건 |
| --- | --- |
| 필터·조회 *(미처리 전건 · 설비 · 기간)* | 항상 — 기본은 미처리 전건이고 경과일 긴 순(L-12 적체) |
| 목록에서 고장 선택 | 항상 |
| 사진 보기 | 첨부가 있을 때 · ⛔ 읽기 전용 — 현장이 적은 것을 사무가 고치지 않는다(§5-2) |
| 원인 코드 선택 | 선택된 건이 완료가 아님 · ⛔ 값 목록이 비면 비활성 + 사유(G-2 · §6) |
| 처리 내역 입력 | 선택된 건이 완료가 아님 · A-12 형식 유도 |
| 처리 중으로 | 상태 = 접수 — ⛔ 되돌리지 않는다(§5-1) |
| 보전 지시 발행 | 상태 = 접수 또는 처리중 · ⭐ 선택이다 — 강제하지 않는다(§5-4). 발행하면 지시가 자동 연결된다(§4-B) |
| 완료 | 상태 = 접수 또는 처리중 AND 원인 코드 입력 AND 처리 내역 입력(§5-1 · §6) |

## W-05-05

| 액션 | 활성 조건 |
| --- | --- |
| 트리거 선택(다중) | 같은 설비끼리만(§6-1) |
| 지시 발행 | 대상·유형·예정일·담당자 + 항목 1개 이상 |
| 취소 | 상태 = 발행 · 실적이 0건일 때만 |

## W-05-06

| 액션 | 활성 조건 |
| --- | --- |
| 지시에서 선택 / 고장에서 선택 | 항상 — 둘 중 하나(§5-2) |
| 출고 건 연결 / 해제 | 대상 선택됨 · 저장 전에는 해제 가능 |
| 실적 저장 | 대상 + 수행 시작 + 결과 내용 |
| 마감 체크 | 지시가 있을 때만 · 항목이 전부 완료/해당없음 |

## W-05-01

| 액션 | 활성 조건 |
| --- | --- |
| 환산 사용 켜기·끄기 | 편집 권한 |
| 정책 추가·수정 | 동상 |
| 정책 종료(유효 종료일 지정) | ⛔ 삭제하지 않는다 — 과거 실적이 그 비율로 계산됐다 |
| 미리보기 | 툴·품목·공정 선택 |

## W-05-07

| 액션 | 활성 조건 |
| --- | --- |
| 채널 추가 | 설비 선택됨 |
| 수신 로그에서 가져오기 | 설비 선택됨 + 최근 수신 있음 |
| 검사항목 연결·해제 | 채널 행 선택 |
| 사용 중지 | B-4(물리 삭제 없음) |
| 변경 이력 | audit.audit_event(B-5) |

## W-05-08

| 액션 | 활성 조건 |
| --- | --- |
| 조건 입력·조회 *(기간 · 공장 · 그룹 · 설비 · 묶음)* | ✅ 기간 필수(L-3) — 비면 조회 불가. 기본 최근 1개월이 들어 있다 |
| 탭 전환 *(사유별 · 설비별 · 추이)* | 조회 결과 있음 |
| 열린 구간 보기 | 열린 구간 ≥ 1건 — ⭐ 집계에서 빠진 건을 목록으로 보인다(§5-4) |
| 겹친 구간 보기 | 겹친 구간 ≥ 1건 — ⭐ 합계는 한 번만 세되 목록은 그대로 보인다(§5-3) |
| 목록 내려받기 | 조회 결과 있음 · 상한 초과 시 안내 + 기간 좁히기 권고(§6) |

## W-05-09

| 액션 | 활성 조건 |
| --- | --- |
| 캘린더 만들기 | 편집 권한 |
| 이 날 적용 | 날짜 선택 + 구분 |
| 요일 일괄 | 「매주 일요일 휴무」 — 연 단위로 한 번에 |
| 기간 일괄 | 「08-13 ~ 08-17 휴무」 — 연휴 |
| override 지정·해제 | 그룹 선택 |
| 공장 기본 지정 | A-6 한 트랜잭션 |

## W-05-10

| 액션 | 활성 조건 |
| --- | --- |
| 계측기 선택 | — |
| 이력 저장 | 유형 + 수행일 + 결과 (+ 검교정이면 전용 4필드) |
| ⛔ 수정·삭제 | 없다(B-3 · updated_at 부재) |

---

**합계: 화면 17 · 액션 90**
