# OMF-MES 01 자재창고 도메인 API 요구서

> 작성일: 2026-08-06 (v0.1) · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 대상 정본: `openapi/logistics-01자재창고.json` · `openapi/mdm-기준정보.json`(적치 규칙분)
> 선행: `06-API-요구서.md`(기준정보 도메인) — **구조·검증 축·표기 규약을 그대로 계승하고 다른 점만 이 문서가 정한다.**

---

## §1. 개요

### 1-1. 이 문서가 무엇인가

**계약 정본은 OpenAPI다. 이 문서는 그것이 왜 그렇게 생겼는지를 적는다.**

| | 담는 것 |
| --- | --- |
| `openapi/logistics-01자재창고.json` | **무엇을** — 경로 · 스키마 · 요청/응답 · 제약 · 상태코드 · `example` |
| **이 문서** | **왜·어디서** — 화면→API 매핑 · 적합성 판정 · 미착지가 무엇을 막는가 · 범위 밖 |

**필드표를 여기 쓰지 않는다.** 같은 것을 두 곳에 적은 것이 공유계약 §I 불일치 34건의 원인이었다.

### 1-2. 어디에 쓰이는가

프론트가 **화면 하나를 지금 시작해도 되는지** 판단하는 근거다. §3의 매핑에 「없음」이 없고 §5의 미착지가 그 화면을 건드리지 않으면 착수 통지를 낸다.

백엔드에는 **구현 대상 목록**이다. 물리 모델은 이미 있으므로(01~04 테이블 78개) 대부분은 옮기는 일이고, 모델이 없는 한 곳만 §5-2가 따로 표시한다.

### 1-3. 범위

| | |
| --- | --- |
| **대상 화면** | **24장** — 관리웹 11 · 모바일 **11** · POP 2 *(2026-08-13 — `M-01-12` 동결 해제로 +1)* |
| **리소스** | **22** — 21건은 01 정본(**재생재 등록 +1**), `putaway_rule` 1건은 기준정보 정본 |
| **경로** | **66** (`/logistics` 44 · `/inventory` 17 · `/trace` 5) + 기준정보 **4** |
| **오퍼레이션** | **89** + 기준정보 5 |

> ⚠ **경로·오퍼레이션은 2026-08-13 실측값이다** — 트랙 종료(2026-08-06) 이후 세 번 늘었다: 오퍼레이션 3건 보완(PR #142) · 폐기 도착지 완화(PR #149) · **재생재 등록 신설**(§3-24). 기준 = `deliverables/openapi/logistics-01자재창고.json` 의 `paths` 전수.

✅ **`M-01-12`(재생재 등록) 동결 해제**(2026-08-13) — 트랙 종료 시점에는 #64 로 동결돼 있었으나 **DR-006 이 확정**(2026-08-10)돼 확대 17차에서 스펙이 작성됐다. **계약만 따라오지 않아** 그 화면이 「API 계약이 없어 착수 통지를 내지 않는다」로 남아 있었다 → **§3-24 신설.** `W-01-08`은 결번.

---

## §2. 읽는 법

### 2-1. 세 층으로 읽는다

```
화면 상세 스펙 §5 액션        무엇을 하고 싶은가
        ↓  §3 매핑
OpenAPI 경로·오퍼레이션        어떻게 부르는가
        ↓  x-source-table / x-source-column
물리 모델 컬럼                 어디에 저장되는가
```

**세 층이 다 이어져야 계약이다.** 어느 한 층이 끊기면 §3의 「없음」 다섯 가지 중 하나로 분류한다(§3-0).

### 2-2. 근거 추적 — `x-source-table` · `x-source-column`

전 스키마에 붙어 있다. 필드 하나를 잡고 어느 테이블 어느 컬럼인지 바로 볼 수 있다. **비어 있는 것은 한 곳뿐**이고 그것이 §5-2의 조정 대상이다.

### 2-3. 55개 경로를 규칙으로 읽는 법 — **리소스 패턴이 여섯이다**

기준정보는 셋(마스터·부여회수·버전마스터)이었다. 01은 **업무 문서**라 셋이 더 필요했다.

| 패턴 | 판별 | 01 개수 | 생김새 |
| :-: | --- | :-: | --- |
| 가 | `is_active` + `version_no` | 1 | `GET`·`POST`·`PUT`·`:deactivate` |
| 나 | `created_at`·`created_by` 만 | 3 | 하위 컬렉션 `GET`·`PUT`(치환) |
| 다 | 헤더–버전–라인 | **0** | 01에는 없다 |
| **라** | **`status_code` + `version_no`** | **15** | `GET`·`POST`·`PUT`·`PUT …/lines`·**상태 전이 동사** |
| **마** | 파티션 + 멱등키 + append-only | 1 | **`GET` 만** |
| **바** | `GENERATED` 파생 | 1 | **`GET` 만** |

**업무 문서 형에 끄기(`:deactivate`)가 없다.** 문서는 끄는 것이 아니라 상태를 옮긴다.

**상태 전이 동사는 셋이다** — `:confirm`(더 못 고친다) · `:post`(재고가 움직인다) · `:request-approval`(승인 요청). 네 번째로 정했던 `:cancel`은 **하나도 쓰이지 않았다**(§5-1).

**문서 고유 동사는 넷** — `:split` · `:complete` · `:complete-temporary` · `:arrive` · `:close` · `:pick`.

### 2-4. 오류가 두 계열로 갈린다 — **409와 400을 섞지 않는다**

| | 뜻 | 화면이 하는 일 |
| :-: | --- | --- |
| **409** | 저장 충돌 — **다시 읽어 오면 풀린다** | 재로드 후 재시도 안내 |
| **400** | 검증·상태 위반 — **고쳐야 풀린다** | 인라인·배너 오류(`ErrorItem.scope`) |

**`STATE_LOCKED`가 400인 것이 핵심이다.** 확정·전기된 전표를 수정하려 한 것은 재로드해도 풀리지 않는다.

⚠ **`If-Match`를 받는 오퍼레이션은 전부 409를 선언한다.** 토큰을 받아 놓고 불일치 응답을 안 적으면 클라이언트가 두 계열을 섞는다 — 검사기가 이 조건을 지킨다(§2-5).

### 2-5. 검산

```bash
cd deliverables

# ① UI 요구 목록 재생성 — 화면 스펙 §5 액션 표에서 전건 추출
python3 verify-ui-coverage.py --domain 01     # → 화면 19 · 액션 109 (+ 표 없는 화면 4)
python3 test-verify-ui-coverage.py            # 회귀 테스트 14건

# ② 커버리지 축 — 그 액션이 이 문서 §3 매핑표에 다 있는가
python3 verify-mapping-coverage.py --domain 01
python3 test-verify-mapping-coverage.py       # 회귀 테스트 11건

# ③ 계약이 계약으로 성립하는가 — 구조 검사 9종
python3 openapi/check-structure.py

# ④ description 이 공개돼도 되는가 (#92)
python3 openapi/check-public-safe.py
```

**대조 규칙**: `openapi/ui-요구목록-01자재창고.md`의 액션 **109건**이 §3 매핑표에 하나도 빠짐없이 있어야 한다. 엔드포인트가 없는 액션도 **「없음 + 이유」로 한 행을 차지**한다. **빈칸은 누락이다.**

⚠ **「전건 소화」를 사람이 세지 않는다.** 기준정보 트랙에서는 눈으로 대조했는데, 109건에서는 몇 건이 빠져도 보이지 않는다 — ②가 그 대조를 기계로 한다. **실제로 초안에서 2건을 잡았다**(`W-01-05`의 「대체입고 예정 선택」·「반품 수량 입력」을 줄여 적어 대조에 걸리지 않았다).

⚠ **②는 §3 매핑표의 첫 열만 본다.** 소절 본문 전체를 대조하면 「조회」·「취소」처럼 짧은 액션이 **산문에 우연히 등장하기만 해도 통과**한다 — 커버리지를 보증하려는 도구가 커버리지를 부풀린다. 회귀 테스트가 이 경로를 잠근다.

⚠ **액션 표가 없는 화면 4장**(`W-01-01`·`W-01-02`·`W-01-10`·`M-01-01`)은 목록에 안 잡힌다. **§5 본문에서 사람이 읽어** §3-20~23에 따로 옮겼다 — 목록에 없다고 요구가 없는 것이 아니다.

---

## §3. 화면 → API 매핑

### 3-0. 「없음」의 다섯 가지 — 이유가 다르면 대응이 다르다

| 표기 | 뜻 | 나중에 무엇이 채우나 |
| --- | --- | --- |
| **범위 밖** | 이 스펙이 다루지 않는 도메인·화면의 소관 | 다른 스펙 |
| **미착지** | 계약·모델에 근거가 없어 지금 만들면 **지어내는 것**이 된다 | **횡단 계약 신설** 또는 이슈 해소 |
| **선행 미결** | 화면 스펙 자신이 정책·자료를 미결로 남겼다 | 해당 화면 §8 미결 결정 |
| **소관 이동** | 다른 화면이 실행을 소유한다(중복 구현 금지) | — 정상 |
| **API 불필요** | 화면 안에서 끝난다 — 스캔 입력 · 로컬 취소 · **사용자가 손으로 치는** 요청 필드 채우기. ⛔ **선택칸의 «값 목록»은 여기 들지 않는다**(2026-09-01 정정) | — 정상 |

집계는 §3-27에 둔다.

⛔ **「요청 필드 채우기」가 선택칸의 «값 목록»을 삼켰다 — 2026-09-01 정정.**
공통코드에서 오는 선택지는 **API 호출이다**(`GET /mdm/code-values?codeGroupCode=…`). 그것을 「API 불필요」로 적으면 프론트는 그 호출을 만들지 않고, 화면은 오류 없이 **빈 선택칸**으로 선다 — 실측으로 **8화면**이 그 상태였다(2026-09-01). §1-3 이 이미 「그 화면들이 **선택 목록으로 필요로 하는 조회 전용**」을 범위에 넣어 두었는데 §3 표가 그것을 어긴 자리다.

**판별은 하나다 — 그 칸의 값이 «어디서 오는가».**

| 값의 출처 | §3 표기 |
| --- | --- |
| 사용자가 손으로 친다 · 앞 화면이 넘겨준다 · 스캔이 채운다 | **API 불필요** |
| **공통코드 마스터** · 다른 자원의 목록 | ⭐ **그 조회 경로를 «행으로» 적는다** |


---

### 3-1. `M-01-02` 자재LOT 스캔·등록 *(모바일 · **보완 경로**)*

⭐ **사전부착 LOT 의 «본 경로»는 이 화면이 아니라 입하 등록(`M-01-01` §3-23)이다**(✓확정 2026-09-02 · `omf-mes#354`). 이 화면은 **입하 등록 때 못 스캔한 사전부착 라인**을 나중에 채운다.

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| **입하 건·라인 선택** | ⭐ `GET /logistics/inbound-receipts?supplierLotMissing=false` → `GET /logistics/inbound-receipts/{inboundReceiptId}/lines?supplierLotMissing=false` — 응답에서 **`lotId` 가 빈 라인**이 대상이다(값이 응답에 들어 있어 화면이 정확히 거른다) | §5-1 |
| 스캔(암묵) / 직접 입력 | **API 불필요** — 34자리 검증은 화면이 한다 | §5-2 · D-2·D-3 |
| 등록(1건) | `POST /trace/lots`(`numberSourceCode = SUPPLIER` · 스캔값이 `lotNo` · ⭐ **원천 짝은 `sourceTypeCode = INBOUND_RECEIPT_LINE` + `sourceId = inboundReceiptLineId`** — 발번 단위가 라인이다 · `omf-mes#326`) «(정합주: 2026-09-02 — 종전 「`POST /trace/lots` — P/O 후보는 `GET /logistics/purchase-orders?openOnly=true`」였다. ⛔ **P/O 를 고르는 것은 입하 등록이 이미 하는 일**이고, 여기서 다시 하면 같은 LOT 이 두 번 만들어진다. 원천 짝은 §3-6 에만 적혀 있어 **이 화면 몫이 비어 있었다** · `omf-mes#354`)» | §5-1·§5-2 · C-1 |
| 등록 완료 | **API 불필요** — 건별로 이미 저장됐다. 화면을 닫는 행위다 | §5-2 · C-3 |
| 목록 행 삭제 | **API 불필요** — 「미동기 건에 한함」이라 오프라인 큐에서 빼는 일이다 | §5-2 · C-1 |

- 같은 공장에 같은 LOT 번호가 있으면 **409가 아니라 400**이다 — 스캔 값이 그대로 번호라 재로드해도 풀리지 않는다.
- ⛔ **`sourceTypeCode` 는 계약이 값을 하나로 닫았다** — `LotCreate.sourceTypeCode` `enum: [INBOUND_RECEIPT_LINE]`(`omf-mes#354`). 재생재 등록의 `RECYCLE_ENTRY` 는 이 경로로 오지 않는다(§3-24 는 `POST /logistics/recycle-entries` 를 쓴다). ⚠ **응답 `Lot.sourceTypeCode` 는 열어 두었다** — 서버가 만드는 원천이 더 있다.

### 3-2. `W-01-03` 초과 입하 분리 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 초과분 수량 조정 / 예외 유형·사유 입력 | 수량과 «사유»는 **API 불필요** — 사용자가 손으로 친다. ⭐ **유형의 «선택지»는 아래 행이다** | §5-1 |
| ⭐ **예외 «유형» 선택지** | **`GET /mdm/code-values?codeGroupCode=INBOUND_RECEIPT_EXCEPTION_TYPE`** — 공통코드에서 온다. ⛔ **2026-09-01 신설** — 앞 행이 이것까지 「API 불필요」로 삼켜 화면이 호출을 만들지 않았다 | §5-1 · G-32 |
| **분리 등록** | `POST /logistics/inbound-receipts:split` — `mode` 세 갈래가 화면의 세 버튼이다 | §5-1·§5-4 · B-8 |
| 취소 | **API 불필요** — 「아직 아무것도 저장되지 않았다」 | §5-1 |
| 반품 진행 | **소관 이동** — `W-01-05` | §5-1 |
| 신규 P/O 등록 | **소관 이동** — `W-01-11` | §5-1·§5-3 |

- **부분 실패를 허용하지 않는다.** 1단계 도출은 `BatchResult`(C-2)를 예상했으나 화면 §5-4가 「입하 2건을 한 트랜잭션으로」라 못박았다.

### 3-3. `W-01-09` 입하 예정 조회 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 조회 | `GET /logistics/asns` — 기간·공급사·상태·품목 | §5-1 · B-7 |
| 예정 건 선택 | `GET /logistics/asns/{asnId}` + `…/lines` | §5-1 |
| 정렬 | **API 불필요** — 스펙이 「화면 내 처리」라 명시 | §5-1 |
| 입하 등록으로 이동 | **소관 이동** — `M-01-01`(모바일) | §5-1 |
| P/O 변경 확인 | **소관 이동** — `W-02-06` | §5-1 |

- **등록·수정·삭제가 없다.** ERP 수신본이고 `asn_line`에 `version_no`가 없는 것이 이 규칙과 맞는다.
- ⭐ **라인 응답이 「진행」 열의 두 값을 함께 내린다** — `AsnLine.orderedQty`·`receivedQty`(2026-08-29 신설). 화면이 라인마다 P/O 를 따로 조회하지 않는다(공유계약 L-2). 무발주 행은 널이고 화면은 「—」로 그린다.

### 3-4. `W-01-11` 신규 P/O 등록 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 라인 추가 / 라인 삭제 | **API 불필요** — 등록 «전»이라 `{purchaseOrderId}` 가 없다. `POST` 본문의 `lines[]` 를 채우는 일이다 | §5-1 |
| 등록 | `POST /logistics/purchase-orders` — 헤더·라인 한 트랜잭션 | §5-1 · B-8 |
| 취소 | **API 불필요** — `W-01-03`으로 복귀 | §5-1 |
| **승인 요청** | `POST /logistics/purchase-orders/{id}:request-approval` — ⚠ **요청 생성까지만**. 진행 상태 조회·결재는 §5-3 | §5-1·§5-6 · J-4 |

- **라인 치환이 신규 행을 만든다.** 기준정보의 `RoutingOperationUpsert`는 만들지 않았다 — 여기는 화면에 「라인 추가」가 있다. **패턴이 아니라 화면이 정한다.**
- ⚠ `PUT …/{purchaseOrderId}/lines` 와 `PUT …/{purchaseOrderId}` 는 이 화면이 부르지 않는다 — 「경로는 있는데 부르는 화면이 없다」(`09-API-계약서` §5-2)의 이 도메인 사례다. 등록 뒤 헤더·라인을 고치는 화면이 인벤토리에 «없다는 것»이 결손인지 정상인지는 별도 판단이 필요하다. ⛔ 경로를 지우지 않는다.
- ⭐ **ERP P/O 번호는 이 화면이 쓰지 않는다** — 사후 매칭은 02 P/O 수신 인터페이스 소관이고, 등록 폼은 이 값을 받지 않는다(`W-01-11` §5-2 확정 2026-08-04 · §5-3). ⛔ 표에 행을 더하지 않는다.

### 3-5. `M-01-06` 입하 오류 등록 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 오류 등록 | `POST /logistics/inbound-receipt-lines/{id}/variances` | §5-1 |
| 취소 | **API 불필요** — `M-01-01`로 복귀 | §5-1 |
| 반품 진행 | **소관 이동** — `W-01-05` | §5-1 |
| 폐기 전환 | **선행 미결** — 화면 스펙 §8-3이 회신 **E-4** 대기로 남겼다 | §5-1·§8-3 |

- **수정·삭제 경로를 두지 않았다.** 화면이 「한 번 등록하면 수정 불가」라 정했으므로 계약도 `PUT`·`DELETE`를 만들지 않는다.

### 3-6. `P-01-01` 자재LOT 등록·라벨 발행 *(POP)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 입하 **라인** 선택 | `GET /logistics/inbound-receipts?supplierLotMissing=true&labelIssued=false` → `GET .../{id}/lines?supplierLotMissing=true&labelIssued=false` | §3-6·§6 |
| **등록·인쇄** | LOT 생성은 `POST /trace/lots`(`numberSourceCode = MES` · 서버 채번 · ⭐ **원천 짝은 `sourceTypeCode = INBOUND_RECEIPT_LINE` + `sourceId = inboundReceiptLineId`** — 발번 단위가 라인이다 §3-6 · `omf-mes#326`) + 발행 기록은 **`POST /app/document-issues`**(공통 계약 v0.2). 인쇄는 `GET …/rendition` → 셸이 보내고 **`POST /app/document-issues/{documentIssueLogId}:report-print`** 로 결과 보고. ⛔ **두 호출은 원자적이지 않다** — 부분 실패 규약은 자재LOT 등록·라벨 발행 화면(`P-01-01`) §5-2 | §5-1·§5-2 · K-1·K-4 |
| **인쇄**(등록 완료분) | 등록은 끝났고 인쇄만 실패한 라인을 다시 인쇄한다 — `GET /app/document-issues/{documentIssueLogId}/rendition` → `POST /app/document-issues/{documentIssueLogId}:report-print`. ⛔ 등록(`POST /trace/lots`)을 다시 부르지 않는다 — 부르면 LOT 이 둘 생긴다 | §5-2 · K-4 |
| **재인쇄** | ✅ **해소** — 같은 `POST /app/document-issues` 에 `reissueReasonCode` 를 실어 보낸다. **회차는 서버가 매긴다** | §5-3 · K-2 |
| 프린터 선택 | ✅ **`GET /app/printers`**(공통 계약 v0.2) — ⚠ **서버가 무엇을 보고 목록을 만드는지는 여전히 미결**(`mdm.terminal` 에 프린터 축 0건) | §5-1·§8-4 · 출력물 요구서 §5-3 |

### 3-7. `W-01-04` 재고 실사 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| **실사 개시** | `POST /inventory/counts` — **라인은 서버가 장부에서 만든다** | §5-7 |
| 조회 | `GET /inventory/counts` | §5-7 · B-7 |
| 실사 선택 | `GET /inventory/counts/{id}` — **요약 4칸을 함께 내린다** | §5-6·§5-7 |
| 블라인드 전환 | **API 불필요** — 「개시 전에만」이라 `POST` 본문의 `blindCount`다 | §5-2·§5-7 |
| ⭐ **실사 유형·차이 사유 선택지** | **`GET /mdm/code-values?codeGroupCode=INVENTORY_COUNT_TYPE`** · **`GET /mdm/code-values?codeGroupCode=VARIANCE_REASON`** — 공통코드에서 온다(**2026-09-01 신설**) | §5-7 · G-32 |
| 차이 사유 입력 | `PUT /inventory/counts/{id}/lines` | §5-7 |
| 실물 수량 수정 | 같은 `PUT` — ⚠ **「이력 남김」은 선행 미결**(②안 채택 여부 §5-3) | §5-3·§5-7 |
| **실사 마감** | `POST /inventory/counts/{id}:close` — 미실사 0 · 차이 0/조정 완료 | §5-5·§5-7 |
| 조정 등록 | **소관 이동** — `W-01-12` | §5-7 |

- **요약은 서버가 계산한다.** 화면이 전 라인을 받아 세면 페이지네이션과 어긋난다. 창고 하나의 라인이 수백~수천 건이다.

### 3-8. `M-01-04` 자재 위치 확인 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 스캔(암묵) | `GET /trace/lots?q=` → `GET /inventory/balances?lotId=&groupBy=LOCATION` + `GET /trace/lots/{id}/holds` | §5-7 |
| 직접 입력 / 다음 스캔 | **API 불필요** | §5-7 · D-3 |

- ⭐ **`GET /inventory/balances`의 창고를 필수로 두지 않은 이유가 이 화면이다.** 현장은 LOT을 스캔하고 「어디 있나」를 묻는다 — 창고를 모른 채 들어온다. `warehouseId`·`itemId`·`lotId` 중 **적어도 하나**를 요구하는 것으로 바꿨다.
- **저장 액션이 없다.** 이 화면은 「저장중」 상태를 갖지 않는다.
- ⭐ **창고명·위치명·품목코드는 잔액 응답이 함께 내린다** — `InventoryBalance` 의 `itemCode`·`itemName`·`lotNo`·`locationCode`(2026-08-29 신설). ⛔ 화면이 `GET /mdm/locations/{id}` → `GET /mdm/warehouses/{id}` → `GET /mdm/items/{id}` 를 이어 부르지 않는다 — 모바일 진입 호출 수(적합성 5축 ①)에 걸리고 오프라인에서는 애초에 부를 수 없다. 소유처 이름만 `GET /mdm/partners/{partnerId}` 로 푼다(§5-1).

### 3-9. `M-01-05` 적치·입고 완료 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 지시 선택 | `GET /logistics/putaway-tasks` — 담당자 본인 것 | §5-7 |
| 위치 스캔 / 위치 선택 | `GET /mdm/locations?warehouseId={지시의 warehouseId}` — 어느 쪽인지는 지시의 `warehouseManagementLevelCode`가 가른다. ⚠ 창고는 «지시»가 준다 | §5-3·§5-7 |
| LOT 스캔 / 직접 입력 | **API 불필요** | §5-7 · D-3 |
| **적치(1건)** | `POST /logistics/putaway-tasks/{id}:complete` | §5-2-1·§5-7 · C-1 |
| 적치 완료 | **API 불필요** — 건별로 이미 저장됐다 | §5-7 · C-3 |

- **권장 위치가 있는데 다른 곳에 적치하면 400이다.** 권장이 없는 품목만 `confirmedNoRule`로 통과하며 **실제 위치는 반드시 남는다**(`ck_putaway_completed`).
- **혼적 판정**(`allow_mixed_item`·`allow_mixed_lot`)은 화면이 `GET /mdm/locations/{id}` + `GET /inventory/balances?warehouseId={지시의 warehouseId}&locationId=`로 미리 보고, **서버가 `:complete`에서 다시 막는다.**

### 3-10. `M-01-07` 임시 위치 적재 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 임시 위치 스캔 / 목록에서 선택 | `GET /mdm/locations?warehouseId={지시의 warehouseId}` — ⚠ 창고는 «지시»가 준다. 화면이 따로 조회하지 않는다 | §5-5 · D-3 |
| 사유 선택 | **선행 미결** — 값 목록 도착 전 화면이 비활성으로 둔다 | §5-5 · G-2 |
| **임시 적치 등록** | `POST /logistics/putaway-tasks/{id}:complete-temporary` — 사유 코드와 비고 중 하나는 필수 | §5-1·§5-5 |

- **정상 적치와 상태가 구분된다.** 구분되지 않으면 `M-01-10`이 정위치 이동 대상을 찾을 수 없다 — `GET /logistics/putaway-tasks?temporaryOnly=true`가 그 목록이다.

### 3-11. `M-01-08` 자재 출고 피킹 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 출고요청 선택 | `GET /logistics/picking-orders` | §5-8 |
| 라인 선택 | `GET /logistics/picking-orders/{id}` — **보류 라인은 `held`로 표시돼 온다** | §5-8 · B-4 원리 |
| LOT 스캔 / 수량 입력 / 직접 입력 | **API 불필요** | §5-8 · D-3·G-6 |
| 피킹(1건) | `POST /logistics/picking-orders/{id}/lines/{lineId}:pick` | §5-8 · C-3 |
| **출고 확정** | `POST /logistics/goods-issues` + `postImmediately: true` | §5-3·§5-8 · C-1 |

- **예약에 쓰기 경로를 두지 않았다.** §5-5가 「화면은 직접 쓰지 않고 서버 전기에 맡긴다」고 못박았다 — `GET /inventory/reservations`만 있다.

### 3-12. `M-01-09` 생산창고 입고·호퍼 잔량 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 출고 QR 스캔 | `GET /logistics/goods-issues?goodsIssueLineId=` → `GET /logistics/goods-issues/{goodsIssueId}/lines` — ⛔ `?q=` 는 출고번호 전용이라 출고 QR 을 못 푼다(도식 노트 E — 출고 No ≠ 출고 QR No) | §5-6 |
| 직접 입력 / 수량 입력 / 차이 사유 | **API 불필요** — 요청 본문 필드 | §5-6 · D-3·G-6 |
| **호퍼 잔량 입력** | ⛔ **미착지** — 담을 컬럼이 없다. 화면도 비활성으로 둔다 | §5-3·§5-6 · G-2 |
| **입고 확정** | `POST /logistics/shopfloor-receipts` — 한 버튼이라 등록이 곧 확정 | §5-6 |

- `variance_qty`는 **서버가 계산한다**(`GENERATED STORED`). 화면이 빼지 않는다.

### 3-13. `M-01-10` 재고 이동·불량 반출 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 유형 전환(일반/불량) | **API 불필요** — `transferTypeCode` 필드 | §5-6 |
| 단위 전환(자재별/파렛트) | `GET /inventory/handling-units` — ⚠ 「정책이 허용할 때」의 판정 근거는 §5-2 | §5-2·§5-6 |
| **① 반출 스캔** | `POST /logistics/stock-transfers` — **생성과 반출이 한 오퍼레이션** | §5-6 |
| **② 도착 스캔** / 이동 완료 | `POST /logistics/stock-transfers/{id}:arrive` | §5-6 |
| 직접 입력 | **API 불필요** | §5-6 · D-3 |
| 미완 이동 이어하기 | `GET /logistics/stock-transfers?inTransitOnly=true` | §5-6 |

- **`:depart`를 두지 않았다.** 화면의 버튼이 「반출 스캔」 하나다. 나누면 오프라인 큐에 「생성됐으나 반출 전」 상태가 남고 **그것을 볼 화면이 없다.**
- ⚠ **같은 창고 «안» 위치 이동을 저장할 오퍼레이션이 없다.** `POST /logistics/stock-transfers` 는 창고 간만 받고(`ck_stock_transfer_warehouses`), 그래서 `M-01-07` 임시 적치의 복귀(임시 위치 → 정위치)가 계약에서 끊긴다. ⛔ 원장에 직접 쓰는 경로를 신설하지 않는다(`02-URL동작규약` §3-2) — 헤더 없는 이동은 `W-01-13` 진행현황·취소에서 통째로 사라진다. **유력 대안은 데이터 모델 쪽 제약 완화이고 계약 변경이 0 이다** — 사람 결정 대기.

### 3-14. `M-01-11` 실물 카운트 *(모바일)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 실사 선택 | `GET /inventory/counts?inProgressOnly=true` | §5-6 |
| 위치 스캔 | `GET /inventory/counts/{id}/lines?locationId=` | §5-6 |
| 수량 입력 / 직접 입력 | **API 불필요** | §5-6 · D-3·G-6 |
| 목록에 없는 재고 추가 | `PUT /inventory/counts/{id}/lines` — 신규 행. 「온라인일 때만」은 화면 규칙 | §5-3·§5-6 |
| **이 위치 완료** | 같은 `PUT` — **위치 단위로 끊는다** | §5-6 · C-3 |

- **실사 전체가 아니라 위치 단위 치환이다.** 창고 하나의 라인이 수천 건이라 전체를 보낼 수 없다.

### 3-15. `P-01-02` 출고 QR 발행 *(POP)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 라인 선택 · 전체 선택 | `GET /logistics/goods-issues/{id}/lines` | §5-6 |
| 유형 전환(라인/파렛트) | **선행 미결** — 값 목록 도착 전 비활성 | §5-6 · G-2 |
| **발행·인쇄** | ✅ **해소** — **`POST /app/document-issues`**(공통 계약 v0.2) · 인쇄는 `rendition` → `POST /app/document-issues/{documentIssueLogId}:report-print` 로 분리 | §5-6 · K-1·K-4 |
| **재발행 사유 선택** | ⛔ **미착지** — 회차가 발행 기록에 붙는다 | §5-6 · K-2 |

### 3-16. `W-01-05` 공급사 반품 처리 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 대상 조회·선택 | `GET /inventory/balances` + `GET /logistics/goods-receipts` | §5-7 |
| 사유 선택 / 대체입고 예정 선택 / 반품 수량 입력 / ERP 송신 토글 | 값을 «싣는» 것은 **API 불필요** — `reasonCode`·`replacementExpected`·라인·`sendToErp` 필드. ⭐ **사유의 «선택지»는 아래 행이다** | §5-7 · G-2 |
| ⭐ **출고 유형·출고 사유·입고 유형 선택지** | **`GET /mdm/code-values?codeGroupCode=ISSUE_TYPE`** · **`GET /mdm/code-values?codeGroupCode=GOODS_ISSUE_REASON`** · **`GET /mdm/code-values?codeGroupCode=RECEIPT_TYPE`** — 공통코드에서 온다. ⛔ **2026-09-01 신설** — 앞 행이 사유 «선택»까지 「API 불필요」로 적어 화면이 호출을 만들지 않았다 | §5-7 · G-2·G-32 |
| **반품 처리** | `POST /logistics/goods-issues` + `postImmediately: true` | §5-7 |

- **경로를 나누지 않았다.** 일반 출고·반품·기타 출고가 `issueTypeCode`로 갈린다 — 나누면 같은 전표가 세 벌로 갈라지고 조회가 세 곳을 봐야 한다.

### 3-17. `W-01-06` 폐기 요청·기타 출고 *(관리웹)* ⛔ **개칭 2026-08-18 — 「품의」가 아니다**(DR-009)

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 대상 선택 | 1단 전표 `GET /logistics/goods-receipts?warehouseId=&receiptDateFrom=&receiptDateTo=` → 2단 라인 `GET /logistics/goods-receipts/{goodsReceiptId}/lines`. ⛔ 재고 축으로 바꾸지 않는다 — 입고일·원천이 필요하다(DR-010) | §5-7 · DR-010 |
| 사유 입력 | **API 불필요** — `reasonCode` 필드. ⭐ **설계가 필수로 둔다** — 폐기는 **사유 없이 승인할 수 없다**. *(2026-08-18 근거 정정 — 앞서 「**DB가 강제**」라 적었는데 모델은 필수 여부를 정하지 못한다)* | §5-7 · J-4 |
| ⭐ **출고 유형·폐기 사유·입고 유형·창고 유형 선택지** | **`GET /mdm/code-values?codeGroupCode=ISSUE_TYPE`** · **`GET /mdm/code-values?codeGroupCode=GOODS_ISSUE_REASON`** · **`GET /mdm/code-values?codeGroupCode=RECEIPT_TYPE`** · **`GET /mdm/code-values?codeGroupCode=WAREHOUSE_TYPE`** — 공통코드에서 온다(**2026-09-01 신설** — 이 표가 그동안 이 넷을 세지 않았다) | §5-7 · G-32 |
| **결재선 확인** | `GET /app/approval-routes?approvalTypeCode=&businessUnitId=` — ✅ **2026-08-07 해소**(app-공통 계약) | §5-7 · J-2 |
| **승인 요청** | `POST /logistics/goods-issues/{goodsIssueId}:request-approval` — ⚠ **요청 생성까지만**. 진행 중인 요청이 있으면 400 | §5-7 · J-4 |
| ~~**승인 · 반려**~~ | ⛔ **2026-08-07 이 화면에서 제거** — `W-CO-09` 결재함이 소유한다(J-10). 액션 표에서 뺐다 | §5-7 · J-10 |
| ⭐ 자체 폐기 체크 | ⛔ **API 없음 — 화면 상태다.** 체크하면 폐기 거래처를 **비활성하고 값을 비운다.** 저장은 **「승인 요청」의 전표 생성 본문**(`POST /logistics/goods-issues` 의 `destinationTypeCode`·`destinationId`)에 실린다 — `:post` 의 본문은 `businessDate`·`occurredAt` 뿐이고 전표 헤더를 고칠 `PUT` 도 없다(2026-08-16 확정 · client#128) | DR-013 · §5-6 |
| ⭐ 폐기 거래처 선택 | ⭐ **기준정보 계약** `GET /mdm/partners` — `roleTypeCode=<폐기 처리>` *(역할 다섯 중 폐기 업체 · 2026-08-16 확정)* | DR-013 · §5-6 · 요청 작성 시점 결정(2026-08-16) |
| **기타출고 처리** | `POST /logistics/goods-issues/{goodsIssueId}:post` — 승인 전이면 400. 활성 조건은 **승인 완료 후**. 도착지 짝은 요청 작성 때 이미 정해졌다 — 여기서 다시 묻지 않는다 | §5-5·§5-7 · DR-013 |

### 3-18. `W-01-07` 재고 현황·상태 조회 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 필터·조회 / 새로고침 | `GET /inventory/balances` — 유효기한(`expiryDateTo`)·보류만(`heldOnly`)·신재/재생재 구분(`mesCategoryCode`) 세 축을 함께 받는다. ⭐ 요약 5칸은 서버가 낸다(응답 `summary` · 공유계약 L-1) | §5-7 |
| **선택지(품질 상태·소유 구분·보류 사유)** | ⭐ **기준정보 계약** `GET /mdm/code-values?codeGroupCode=LOT_STATUS` · `GET /mdm/code-values?codeGroupCode=OWNERSHIP_TYPE` · `GET /mdm/code-values?codeGroupCode=LOT_HOLD_REASON` — 필터 선택칸을 채운다. ⛔ **목록은 서버가 갖는다 — 값을 화면에 박지 않는다**(셋 다 G-31 마스터안전형이라 고객이 `W-06-06` 에서 바꾼다). ⚠ **재고 상태는 계약이 `enum` 으로 닫았다** — 이 조회가 필요 없다 | §5-7 · G-32 |
| 보기 전환(품목/LOT/위치) | 같은 경로의 `groupBy` | §5-1·§5-7 · L-7 |
| 정렬 | `sort` — **지정 열만** 받는다(`earliestExpiryDate`·`manufacturedAt` 포함) | §5-7 · **L-4** |
| 페이지 이동 | `page`·`size` | §5-7 · B-7 |
| LOT 선택 → 드로어 | `GET /trace/lots/{id}` — 외부 식별자·보류를 함께 | §5-7 |
| **수불 이력 →** | `GET /inventory/transactions` — **영업일 범위 필수** | §5-4·§5-7 · L-3 |
| 의심자재 등록 | **소관 이동** — `W-03-03` | §5-6·§5-7 |

- **소유 구분은 어떤 축에서도 합치지 않는다.** 자사 재고와 고객 지급품을 더하면 오독이다(L-7).
- **기간을 빼먹으면 400이다.** 원장이 영업일로 나뉘어 저장되므로 성능이 아니라 **가능/불가능**의 문제다.

### 3-19. `W-01-12` 재고 조정 *(관리웹)*

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 원천 선택(실사/직접) / ERP 송신 토글 | **API 불필요** — `inventoryCountId`·`sendToErp` 필드 | §5-6 |
| 실사 차이 불러오기 | `GET /inventory/counts/{id}/lines?varianceOnly=true` | §5-6 |
| 조정 저장(라인 확정) | `POST /inventory/adjustments` — 201 이 `ETag` 를 내려 준다 | §5-6 · B-1 |
| 라인 추가·수정 | `PUT /inventory/adjustments/{id}/lines` | §5-6 · A-5 |
| **사유 선택** | 헤더는 `reasonCode`. ⭐ **선택지는 `GET /mdm/code-values?codeGroupCode=INVENTORY_ADJUSTMENT_REASON`** — ⛔ 프론트가 `ADJUST_REASON` 으로 부르고 있어 **빈 목록이 온다**(2026-09-01 실측 · 변경 통지 대상). ⚠ **라인 사유는 미착지** — 저장 자리가 없다 | §5-1·§5-6 · G-32 |
| **조정 상신** | `POST /inventory/adjustments/{inventoryAdjustmentId}:request-approval` — **한 호출**. ⚠ 진행 중인 요청이 있으면 400 — 한 전표에 살아 있는 요청은 하나다 | §5-6 · J-4 |
| ~~**승인 · 반려**~~ | ⛔ **2026-08-07 이 화면에서 제거** — `W-CO-09` 결재함이 소유한다(J-10). 액션 표에서 뺐다 | §5-3·§5-6 · J-10 |
| **전기** | `POST /inventory/adjustments/{id}:post` | §5-6 · B-8 |

⚠ **`W-01-12` 는 결재선이 없으면 상신을 «부르지 않고» 즉시 전기로 간다**(스펙 §5-3 「결재선이 있으면 상신, 없으면 즉시 전기」) — `:request-approval` 의 400(code=`ROUTE_NOT_FOUND`)과 모순이 아니라 «호출하지 않는 분기»다.

---

### 액션 표가 없는 화면 4장 — §5 본문에서 읽었다

### 3-20. `W-01-01` IQC 수입검사·판정 *(관리웹)*

| 화면이 하는 일(§5) | 엔드포인트 | 근거 |
| --- | --- | --- |
| 측정치·수량 임시 저장 | **소관 이동** — `06-API-요구서-03품질.md` §3-1 (`POST /quality/inspection-results`) | §5-2 |
| 판정 확정(`confirmed_at` + Lot Status 전이) | **소관 이동** — `06-API-요구서-03품질.md` §3-1 (`POST /quality/inspection-results/{inspectionResultId}:confirm`) | §5-2 |
| 재검사 회차 추가 | **소관 이동** — `06-API-요구서-03품질.md` §3-1 (`PUT /quality/inspection-results/{inspectionResultId}`) | §5-3 |
| 판정 결과로 이어지는 입고 | **소관 이동** — `W-01-10` | §5-1 |

✅ **2026-08-12 해소** — 03 품질 계약이 덮었고 착수 통지가 나갔다(client #85). 이 소절은 「01 이 왜 못 맡았나」의 기록으로만 남긴다. 상세는 §6-1.

### 3-21. `W-01-02` 긴급 IQC 생략 한도승인 *(관리웹)*

| 화면이 하는 일(§5) | 엔드포인트 | 근거 |
| --- | --- | --- |
| 한도 구간 표시 | **없음 — 범위 밖.** ✅ **2026-08-07 확정 — 1차에 값 구간을 쓰지 않는다**(금액 컬럼 0건 · 값 성격은 수량) | §5-1 · J-2 |
| 승인 대상 표시 | `GET /app/approval-requests/{id}` 의 `target` — ✅ **해소.** 계약이 **표시명·화면 ID·열 수 있는지**를 함께 내려 준다(A-10 보강) | §5-2 |
| 승인·반려 | `POST /app/approval-requests/{id}:approve` · `:reject` — ✅ **해소** | §5-4 · J-8 |
| 승인 결과의 Lot Status 전이·G/R 제외 | **소관 이동** — `W-01-10` | §5-4·§8-5 |
| 변경 이력 | `GET /audit/events` — 다만 화면이 **자리만 두고 비활성**이다(#68) | §5-5·§8-6 |

### 3-22. `W-01-10` 정상품 입하 처리 *(관리웹)*

| 화면이 하는 일(§5) | 엔드포인트 | 근거 |
| --- | --- | --- |
| 대상 조회(IQC 합격·샘플링 미대상·한도승인) | `GET /trace/lots?awaitingReceiptOnly=true&receiptDispositionCode=…` — ⭐ 세 갈래를 서버가 판정한다(`W-01-10` §5-1). `GET /logistics/inbound-receipts` 는 원천 문서 표시용 보조 조회다 | §5-1 |
| 창고·위치 지정 | `GET /mdm/warehouses` · `GET /mdm/locations` *(기준정보 계약)* | §5-2 |
| ⭐ **입고 유형·입고 사유·식별자 유형·대체 LOT 사유 선택지** | **`GET /mdm/code-values?codeGroupCode=RECEIPT_TYPE`** · **`GET /mdm/code-values?codeGroupCode=GOODS_RECEIPT_REASON`** · **`GET /mdm/code-values?codeGroupCode=LOT_EXTERNAL_IDENTIFIER_TYPE`** · **`GET /mdm/code-values?codeGroupCode=SUBSTITUTE_LOT_REASON`** — 공통코드에서 온다(**2026-09-01 신설**) | §5-2 · G-32 |
| **입고 처리 — 한 버튼** | `POST /logistics/goods-receipts` — 전표·LOT 전이·원장·잔액·ERP 적재가 **한 트랜잭션** | §5-4 · B-8 |
| 라인별 위치 지정(1:N 혼적) | 같은 `POST`의 `lines[].destinationLocationId` | §5-6 |
| ERP 송신 상태 표시 | 응답의 `erpMessageQueued` — **적재이지 전송이 아니다** | §5-4 |

- **한도승인 건은 `erpMessageQueued`가 거짓이다** — G/R은 합격품 한정이다.
- ⚠ **원천 문서 표시가 막힌다** — `sourceDocumentTypeCode`↔테이블 규약이 없다(§5-3). 세 번째 사용처다.

### 3-23. `M-01-01` 입하 등록 *(모바일)*

| 화면이 하는 일(§5) | 엔드포인트 | 근거 |
| --- | --- | --- |
| LOT 번호 스캔 → P/O 후보 필터 | `GET /logistics/purchase-orders?openOnly=true&q=` | §5-1 |
| 「LOT 번호 없음」 → 전체 P/O | 같은 경로에서 `q` 없이 | §5-1 |
| **거래명세서 촬영 → OCR** | ⛔ **미착지** — `app.attachment` 계약이 없다 | §5-3 |
| 명세서 번호 직접 입력 | **API 불필요** — `deliveryNoteNo` 필드 | §5-3 |
| 입하 검증 3갈래(정상/초과/부족) | ⭐ `GET /logistics/purchase-orders/{purchaseOrderId}` — 응답 `lines[]` 의 `orderedQty`·`receivedQty`·`toleranceOverQty`·`toleranceUnderQty` 로 판정한다 «(정합주: 2026-09-02 — 종전 「**API 불필요** — 허용치는 P/O 라인이 갖고 화면이 판정한다」였다. ⛔ 판정은 화면이 하지만 그 «입력값 넷»이 목록 응답 `PurchaseOrder` 에 하나도 없다 — 라인은 이 상세 경로로만 온다. 경로는 계약에 이미 있었고 여기서 지목하지 않았을 뿐이다 · `omf-mes#347`)» | §5-2 |
| **등록** | `POST /logistics/inbound-receipts` — 입하·라인·LOT 한 트랜잭션 | §5-4 · B-8 |
| **무발주 — 공급사 고르기** | ⭐ **기준정보 계약** `GET /mdm/partners?roleTypeCode=SUPPLIER` — 역할이 「공급사」인 거래처만 «(정합주: 2026-09-02 — 발주가 없으면 승계할 곳이 없다. 경로는 계약에 원래 있었고 여기서 지목하지 않았을 뿐이다 · `omf-mes#348`)» | §5-5 |
| **무발주 — 품목·단위 고르기** | ⭐ **기준정보 계약** `GET /mdm/items?q=` ＋ `GET /mdm/uoms?q=` | §5-5 |
| 무발주 예외 입하 | 같은 `POST`의 `exceptionTypeCode`·`exceptionReason` — ⭐ **별도 승인을 걸지 않는다** «(정합주: 2026-09-02 — 종전 「⚠ 승인 절차는 미착지」였다. 승인 유형 8값(사용자 확정 2026-09-01)에 무발주 입하가 없고 상신 경로도 없다. 통제는 예외 유형·사유 필수 ＋ Lot Status=Hold 둘이다 · `omf-mes#348`)» | §5-5 |

---

### 3-24. ⭐ `M-01-12` 재생재 등록 *(모바일)* — **2026-08-13 추가**

**트랙 종료 뒤에 확정이 왔다.** 2026-08-06 이 문서를 쓸 때는 `M-01-12` 가 #64 로 동결돼 있었는데, **DR-006 확정**(2026-08-10)으로 풀려 확대 17차(2026-08-11)에 스펙이 작성됐다. **계약만 따라오지 않았다.**

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 자재 스캔 / 검색 | **기준정보 계약** `GET /mdm/items?q=` — ⭐ 품목코드 하나에 **행이 둘**(신재·재생재)이라 둘 다 받는다 | §5-A · §3-3 |
| 구분 선택(신재/재생재) | **API 불필요** — 받은 두 행 중 하나를 고른다. ⭐ **기준정보 계약에 `mesCategoryCode` 를 노출**해 화면이 가른다 | §5-A·§5-B |
| 수량 · 창고 · 위치 입력 | **API 불필요** — 요청 본문 필드 | §5-A · D-3 |
| **등록** | ⭐ **`POST /logistics/recycle-entries`** — 새 자재 LOT 과 재고를 **한 트랜잭션**으로 만든다 | §5-1·§5-3 · B-8 |

- ⭐ **번호는 서버가 매긴다.** 클라이언트가 정하면 오프라인 두 단말이 같은 번호를 만든다(C-2). 그래서 화면이 「번호는 저장 후 정해집니다」를 **먼저** 말한다(G-19).
- ⛔ **기존 `POST /trace/lots` 를 쓰지 않는다.** 그쪽은 `lotNo` 를 `required` 로 받는다 — 스캔한 값이 그대로 번호가 되는 `M-01-02` 전용이다. 여기에 태우려면 그 `required` 를 빼야 하는데 **그러면 `M-01-02` 가 번호를 안 보내도 계약이 통과시킨다.**
- ⭐ **원천 유형 값이 하나 는다** — `RECYCLE_ENTRY`(A-16 대로 계약이 먼저 값을 갖는다). `source_id` 가 가리키는 것이 **등록 건 자체**다.
- ⛔ **계보를 싣지 않는다.** 부모 LOT 자리와 계보 표가 **둘 다 실재하는데 비운다** — DR-006 이 「어디서 어떻게 만들어졌는지 추적은 불필요」로 확정했다. ⚠ **일부만 채워진 계보가 빈 계보보다 위험하다**(§3-1).
- ⛔ **목록·상세 조회를 두지 않았다.** 부르는 화면이 0건이다 — 이 화면은 저장 후 입력만 비우고 머물고, 횡단 조회(`/logistics/document-progress`)가 덮는 9종에도 없다. `material_loss` 를 뺀 것과 같은 기준이다(§4).

### 3-25. `W-01-13` 물류 문서 진행현황·취소 *(관리웹 · 2026-08-07 추가)*

⭐ **초판이 남긴 「되돌리는 전이가 없다」를 이 소절이 닫는다.** 23장의 「취소」가 전부 화면 이탈이라 `:cancel` 을 0건으로 만들었는데(§5-1), 취소 화면이 생기면서 근거가 섰다.

| 화면 액션(§5-7) | 엔드포인트 | 근거 |
| --- | --- | --- |
| 조회·필터 | `GET /logistics/document-progress?documentTypeCode=…` — **유형을 필수로 받는다** | §5-2·§5-3 |
| 페이지 이동 | 같은 GET 의 `page`·`size` | §5-7 · G-12 |
| 문서 선택 | `GET /logistics/document-progress/{documentTypeCode}/{documentId}` | §3 |
| 원장 보기 | `GET /inventory/transactions/{businessDate}/{inventoryTransactionId}` — 상세가 번호와 **영업일**을 함께 내린다 | §4-B |
| 후속 목록 보기 | 상세 응답의 `successors` — **서버가 판정한다** | §5-3 · A-10 보강 |
| 취소 요청 | `POST /logistics/inbound-receipts/{inboundReceiptId}:request-cancel` · `POST /logistics/goods-receipts/{goodsReceiptId}:request-cancel` · `POST /logistics/goods-issues/{goodsIssueId}:request-cancel` — 입하·입고·출고 3리소스. 본문은 `ApprovalRequestCreate` 를 그대로 쓴다. If-Match 토큰은 **대상 문서**의 상세 GET 이 내려주는 ETag 다(B-1·A-4) | §5-4·§5-5 |
| 취소 실행 | `POST /logistics/inbound-receipts/{inboundReceiptId}:cancel` · `POST /logistics/goods-receipts/{goodsReceiptId}:cancel` · `POST /logistics/goods-issues/{goodsIssueId}:cancel` — ⭐ **실행 시점에 후속을 다시 판정한다.** If-Match 토큰의 원천은 위와 같다 | §5-4 · J-8 |
| 취소 요청 철회 | ⛔ **미착지** — 상신 철회 경로가 승인 계약에 없다 | §5-7 |
| 대상 화면에서 보기 | 목록·상세의 `screenId` | §5-3 · A-10 보강 |

**계약이 정본으로 갖는 것 셋**

1. ⭐ **후속 판정을 서버가 한다** — 원천 참조가 다형(`source_document_type_code` + `source_document_id`)이라 규약이 없는데, 프런트가 「입고 → 출고·이동·투입」 관계표를 하드코딩하면 **유형이 늘 때마다 취소 판정이 조용히 틀린다**
2. ⭐ **실행 시점 재판정** — 승인을 기다리는 사이에 후속이 생기면 자동 실행은 그것을 못 본다. 재판정에 걸려도 **승인은 유효하고** 새 요청을 다시 올리지 않는다
3. **취소는 반드시 승인을 탄다** — 물류 문서 **12테이블에 `cancel*` 컬럼이 0건**이라 승인 기록이 이력을 대신한다. 승인 없는 취소 경로를 두면 **흔적 없는 취소**가 생긴다

⚠ **덮는 것은 9종이다** — 발주·입하·입고·출고요청·피킹·이동요청·외주출고·외주입고·**출고**. 계약이 `enum` 으로 닫았다(2026-09-02).

```
PURCHASE_ORDER · INBOUND_RECEIPT · GOODS_RECEIPT · MATERIAL_ISSUE_REQUEST ·
PICKING_ORDER · STOCK_TRANSFER · SUBCONTRACT_ISSUE · SUBCONTRACT_RECEIPT · GOODS_ISSUE
```

⭐ **셈** — 물류 12테이블 − 출하 2 = 10(FR-IM-086) · 10 − 생산품 입고 1 = 9. 출하·생산품 입고는 다른 도메인이다.

⭐ **외주 2문서는 2026-08-30 에 되살아났다** — 다른 10문서와 동형으로 상태를 둔다. **비활성으로 그리지 않는다**(구 표기 「상태 컬럼 자체가 없고 … 비활성으로 그린다」는 그 재판정으로 폐기됐다). 물리 컬럼 결손은 통지 사항이고 기다리지 않는다(A-11 v4.6).

⭐ **출고는 2026-09-02 에 편입됐다** — FR-IM-078 출고취소가 이 화면 소관인데 선택지에 없어 **그 문서에 닿을 길이 없었다.** 계약 `POST /logistics/goods-issues/{goodsIssueId}:request-cancel` 이 후속 목록의 원천으로 `GET /logistics/document-progress/{documentTypeCode}/{documentId}` 를 지목한다.

⚠ **취소 «결과» 스키마(`CancelResult`)는 3값이다** — 자리마다 값 집합이 다르다(공유계약 A-16 · `omf-mes#336` 선례).

⚠ **취소 실행 경로는 3리소스에만 있다** — 입하·입고·출고. FR-IM-076~078 이 그 셋만 요구했고, 출하 취소는 04 도메인이다.

---

### 3-26. ⭐ `M-01-13` 긴급 IQC 생략 요청 *(모바일)* — **2026-08-29 추가**

| 화면 액션 | 엔드포인트 | 근거 |
| --- | --- | --- |
| 스캔 | `GET /trace/lots?lotNo=` + `GET /quality/inspection-requests?lotId=&pendingOnly=true` — 검사 대기 여부를 서버가 판정한다(§6 「검사 대기가 아닌 LOT」 차단의 근거) | §5-2 |
| 요청 | `POST /trace/lots/{lotId}:request-iqc-skip` — 요청 생성까지만. 한도는 승인자가 정한다 | §5-2·§5-A |
| ⭐ 내가 올린 요청 조회 | **공통 계약** `GET /app/approval-requests?requestedByMe=true` ＋ **`X-Worker-No` 헤더**(선택판) — 모바일에는 계정 로그인이 없어 서버가 「나」를 이 헤더로 푼다(2026-09-02 신설) | §5-4 · **D-5**·**F-2** |

- ⛔ **「나」를 묻는 조회에는 사번 헤더가 함께 간다** — `requestedByMe=true` 만 보내면 서버가 「나」를 **단말**로 풀 수밖에 없고, 한 단말을 여러 작업자가 교대로 쓰므로 **남이 올린 요청이 섞인다.** 빈 목록이 아니라 «틀린 목록»이라 화면으로는 안 보인다(구현팀 검토 요청 `omf-mes#335`). 선례는 현장 진입 공지 조회(요구서 공통 §3-11).
- **한도를 받지 않는다** — 요청은 「생략해 달라」까지이고 한도는 `W-01-02` 가 정한다(`M-01-13` §9-3).
- **취소·회수를 두지 않는다** — 잘못 올렸으면 다시 올린다(`M-01-13` §5-1 · 공유계약 G-24).
- **결재선이 없으면 요청 자체를 막는다** — `POST …:request-iqc-skip` 이 400(code=`ROUTE_NOT_FOUND`)을 낸다(공유계약 J-4).
- ⚠ **`approvalTypeCode` 값 `IQC_SKIP` 은 아직 미결이다** — 공유계약 A-16 등재 대상.

---

### 3-27. 커버리지 집계

> ⚠ **2026-08-29 재실측 — 아래 집계는 낡았다.** `python3 design/schema/generators/verify-ui-coverage.py --domain 01` 이 「**화면 22 · 액션 126**」을 낸다(액션 표 없는 화면 4장 별도). ⭐ **2026-09-02 화면 +2** — 생성기 명단이 스펙보다 낡아 `M-01-12`(재생재 등록 · DR-006 으로 동결 해제)와 `M-01-13`(긴급 IQC 생략 요청 · 2026-08-12 신설)이 **커버리지 밖에 있었다**(`omf-mes#335`). 명단 밖 화면은 「액션 표 없는 화면」 경고에도 뜨지 않아 **스펙에 액션을 더해도 조용히 초록**이었다. 아래 116 은 2026-08-07 기준이고, **분류별 재계수는 별건으로 세운다** — 총계만 고치면 분류 합이 안 맞는 표가 남는다.

**액션 표 116건**(화면 20장) 전건을 소화했다.

| 분류 | 건수 | 뜻 |
| --- | :-: | --- |
| **엔드포인트 있음** | **65** | 지금 부를 수 있다 |
| **API 불필요** | **33** | 화면 안에서 끝난다 — 정상 |
| **소관 이동** | **7** | 다른 화면이 실행을 소유한다 — 정상 |
| **미착지** | **7** | 횡단 계약 신설이 필요하다 |
| **선행 미결** | **4** | 화면 스펙 §8의 결정 대기 |
| **합계** | **116** | |

**정상인 없음이 40건**(API 불필요 + 소관 이동)이다. 결손은 **11건**(미착지 7 + 선행 미결 4)이고 §5에서 무엇을 막는지 적는다.

> ⭐ **2026-08-07 변동** — 액션 109 → **116**(`W-01-13` 9건 추가 · 「승인·반려」 2건은 `W-CO-09` 로 이관하며 표에서 뺐다). **미착지 9 → 7** — 승인 계약이 결재선 확인·승인 대상 표시·승인/반려를 풀었고, 대신 **상신 철회**가 새 미착지로 하나 늘었다.

액션 표가 없는 화면 4장은 위 집계에 넣지 않았다 — §3-20~23에서 개별로 다뤘다.

---

## §4. UI 적합성 판정 — 「이 API로 이 화면을 그릴 수 있는가」

`openapi/ui-적합성-점검표.md`의 5축을 그대로 쓴다.

### 4-0. 판정 결과

| 판정 | 화면 | 수 |
| :-: | --- | :-: |
| ✅ **그릴 수 있다** | `W-01-03` · `W-01-04` · `W-01-05` · `W-01-07` · `W-01-09` · `W-01-10` · `M-01-02` · `M-01-04` · `M-01-05` · `M-01-08` · `M-01-10` · `M-01-11` | **12** |
| ⚠ **일부가 빈다** | `W-01-11` · `W-01-12` · `M-01-01` · `M-01-06` · `M-01-07` · `M-01-09` · `P-01-01` · `P-01-02` | **8** |
| ⛔ **주 기능이 빈다** | `W-01-02` · `W-01-06` | **2** |
| ❌ **범위 밖** | `W-01-01` | **1** |

### 4-1. ⚠ 8건 — 무엇이 비는가

| 화면 | 비는 것 | 화면 골격이 바뀌나 |
| --- | --- | :-: |
| `W-01-11` | 승인 요청 뒤의 진행 상태 표시 | ❌ — 등록이 주 기능이다 |
| `W-01-12` | 승인·반려 탭 · **라인 사유 저장 자리** | ❌ — 조정 등록·전기가 주 기능이다 |
| `M-01-01` | 거래명세서 촬영·OCR | ❌ — 수기 입력이 대안으로 설계돼 있다 |
| `M-01-06` | 폐기 전환 경로 | ❌ — 오류 등록이 주 기능이다 |
| `M-01-07` | 임시 적재 **사유 코드 값 목록** | ❌ — 비고 입력이 대안이다 |
| `M-01-09` | 호퍼 잔량 | ❌ — 화면이 이미 비활성으로 설계했다 |
| `P-01-01` · `P-01-02` | ~~발행 기록·인쇄~~ ✅ **해소 2026-08-07** | **`app-공통.json` v0.2 출력물 편**이 덮는다(`design/wiki/api-contracts/06-API-요구서-app공통출력물.md`). ⚠ 남은 미착지는 **프린터 목록 원천** 하나다 |

**⚠ 8건은 전부 「비활성 + 사유」로 처리 가능**하다 — 자리가 확정이고 값·계약만 안 왔다.

### 4-2. ⛔ 2건 — 껍데기만 남는다

`W-01-02`(긴급 IQC 생략 한도승인)와 `W-01-06`(폐기 품의·기타 출고)은 **승인이 주 기능**이다. 승인을 비활성하면 화면에 남는 것이 목록뿐이다.

> **「비활성 + 사유 표시」로 먼저 보내지 않는다.** 하네스의 처리 3방법 중 하나이긴 하나, 이 둘은 그 장치를 쓰면 프론트가 만들 것이 없다.

### 4-3. ❌ 1건

`W-01-01` 은 01 소관이 아니다 — 03 품질 계약이 덮었다(2026-08-12 · `06-API-요구서-03품질.md` §3-1 · 착수 통지 client #85). §6-1.

### 4-4. 5축 점검 — 이 계약이 특별히 신경 쓴 곳

| 축 | 이 도메인에서 걸린 곳 |
| --- | --- |
| **① 진입 호출 수** | 모바일은 오프라인을 타므로 진입 호출을 **한 번으로** 묶었다 — 상세 응답이 헤더+라인을 함께 내린다 |
| **② 필요한 정보가 응답에 있는가** | 서버가 세는 것 5개(§4-5) |
| **③ 안 쓰는 것을 담고 있지 않은가** | 원장 응답에서 `idempotency_key`를 뺐다 — 클라이언트가 보낸 값이고 화면이 쓸 데가 없다 |
| **④ 예외를 표현할 수 있는가** | 400/403/404/409 네 계열. `If-Match`를 받는 오퍼레이션은 **전부 409를 선언**한다 |
| **⑤ 상태 전이를 지원하는가** | `:confirm`·`:post`·`:request-approval` + 문서 고유 동사. ⚠ **되돌리는 전이가 없다**(§5-1) |

### 4-5. 서버가 세는 것 — 화면이 따로 조회하지 않게

| 필드 | 없으면 |
| --- | --- |
| `InventoryCountSummary` 4칸 | 화면이 전 라인을 받아 센다 — 페이지네이션과 어긋난다 |
| `PickingLine.held` · `holdReasonCode` | 라인마다 보류를 따로 조회한다 |
| `InventoryBalance.heldLotCount` | 같은 문제 |
| `PutawayTask.warehouseManagementLevelCode` | 위치 스캔 유무를 화면이 창고를 따로 조회해 판정한다 |
| `GoodsReceipt.erpMessageQueued` | 「전송됐다」와 「적재됐다」를 구분 못 한다 |

---

## §4-1. 공통 규약 대조표 — 공유계약이 어느 오퍼레이션으로 구현됐는가

| 조항 | 구현 | 비고 |
| :-: | --- | --- |
| **A-1** 유일 위반은 범위를 담아 | `ErrorItem.uniqueScope` | |
| **A-4** `version_no`는 노출하지 않되 전달 | `ETag` 응답 헤더 ↔ `If-Match` | 본문 필드로 내리지 않는다 |
| **A-5** 하위 컬렉션은 전체 치환 | `PUT …/lines` 7곳 · `PUT …/contents` · `PUT …/external-identifiers` | ⚠ **신규 행을 만든다** — 기준정보와 갈린다 |
| **B-1** 낙관적 잠금 | `IfMatchVersion`(required) — 수정·치환·상태 전이 | |
| **B-4** 참조 건수를 서버가 | `Editability`(적치 규칙) · `heldLotCount` · `held` | |
| **B-7** 페이지네이션 | 목록 전건 `page`·`size`·`PageMeta` | |
| **B-8** 트랜잭션 경계 | 입고·입하·출고 생성이 다중 테이블 한 트랜잭션. **외부 호출은 밖** | `erpMessageQueued` |
| **C-1** 멱등키 | `Idempotency-Key` — **전 쓰기 필수** | |
| **C-2** 부분 실패 | ⛔ **쓰지 않았다** — `W-01-03`이 「한 트랜잭션」이라 못박았다 | §3-2 |
| **C-3** 건별 저장 + 마지막 완료 | `:pick` · `:complete` · `PUT …/lines`(위치 단위) | |
| **C-8** `business_date`는 클라이언트가 | 전기·오프라인 오퍼레이션의 **필수 본문 필드** | |
| **C-9** 큐는 잠금 토큰을 안 싣는다 | `IfMatchVersionOptional` — **오프라인 대상 15건** | 2026-08-29 재실측 |
| **D-5** 귀속은 세션이 아니다 | `WorkerNo`(required) **13건** · `WorkerNoOptional` **4건** — 현장 단말·모바일 쓰기 **17 오퍼레이션** | ⭐ **2026-08-21 신설**(#178). 선택 4건은 관리웹도 같은 경로를 부른다 — 실사 행 치환·출고·입고·재고 이동 |
| **G-1** 오류 범위·`STATE_LOCKED` | `ErrorItem.scope`·`code` · 400↔409 분리 | |
| **G-2** 미확정 코드 목록 | **전 코드 필드에 `enum`을 두지 않았다** | #64 계열 |
| **J-4** 승인 사유 필수 | `ApprovalRequestCreate.reason` | ⚠ 요청 생성까지만 |
| **L-2** `GENERATED`는 다시 계산하지 않는다 | `availableQty` · `varianceQty` — `readOnly` | |
| **L-3** 파티션 키를 필터에 강제 | `businessDateFrom`·`To` **required** | |
| **L-7** 묶으면 안 되는 축 | `ownershipTypeCode`는 `groupBy`와 무관하게 항상 행을 나눈다 | |

### 규약 대비 편차 2건 — 기록으로 남긴다

1. **C-2(부분 실패)를 쓰지 않았다.** 1단계 도출은 `W-01-03`에 `BatchResult`를 예상했으나 화면이 「한 트랜잭션」이라 못박았다. 계약에서 `BatchResult` 스키마를 **지웠다** — 죽은 정의는 쓰이지 않는 타입을 만든다.
2. **A-5의 결론이 기준정보와 다르다.** 같은 조항인데 01은 **신규 행을 만드는 치환**이다. 화면에 「라인 추가」가 있기 때문이다.

---

## §5. 미착지가 무엇을 막는가

### 5-1. ✅ **되돌리는 전이가 생겼다** — 초판의 `:cancel` 0건이 닫혔다 *(2026-08-07)*

> **초판 진단이 옳았고, 그 진단이 화면 신설로 이어졌다.** 아래는 초판 기록이고 **§5-1-1이 결과**다.

23장의 「취소」 버튼을 전수 확인했다. **전부 화면 이탈**(저장 전 복귀)이다.

```
W-01-03  「아직 아무것도 저장되지 않았다」
M-01-06  M-01-01 로 복귀
W-01-11  W-01-03 으로 복귀
```

**문서를 취소 상태로 옮기는 액션이 화면에 0건**이므로 계약에도 만들지 않았다. 그 결과:

| 무엇이 막히나 | |
| --- | --- |
| 전기된 전표를 되돌리는 경로 | **없다.** 원장의 역처리 행을 만들 오퍼레이션이 없다 |
| 현재 유일한 보정 경로 | **실사 → 조정**(`W-01-04` → `W-01-12`) |

> **지어내지 않았다.** 화면 없는 오퍼레이션을 만들면 프론트가 부를 데가 없고 진짜 화면이 생기면 버린다. **취소·역처리 화면이 인벤토리에 필요한지**를 먼저 정해야 한다 → §8-1.

#### 5-1-1. ✅ 화면이 생겼고 계약이 따라왔다 *(2026-08-07)*

`W-01-13` 물류 문서 진행현황·취소가 인벤토리 v1.7에 신설됐다(FR-IM-076~080·086이 **P0**인데 대응 화면이 없었다). 그 스펙을 근거로 **경로 8개**를 더했다.

```
POST /logistics/inbound-receipts/{id}:request-cancel   ·  :cancel
POST /logistics/goods-receipts/{id}:request-cancel     ·  :cancel
POST /logistics/goods-issues/{id}:request-cancel       ·  :cancel
GET  /logistics/document-progress
GET  /logistics/document-progress/{documentTypeCode}/{documentId}
```

| 초판이 막힌다고 적은 것 | 지금 |
| --- | --- |
| 전기된 전표를 되돌리는 경로 | ✅ `:cancel` 이 원장에 역트랜잭션을 만든다 |
| 현재 유일한 보정 경로가 실사 → 조정 | ✅ **조정은 증감이고 취소는 원 거래를 가리키는 역처리**다 — 둘은 다른 것이고 이제 둘 다 있다 |

⭐ **순서가 옳았다** — 「화면이 없으니 만들지 않는다」로 두었기 때문에, 화면이 생겼을 때 **버릴 것이 없었다.** 미리 지어냈다면 3단 관문·실행 시점 재판정 같은 규칙이 없는 형태로 만들어져 통째로 갈렸을 것이다.

### 5-2. 모델이 없는 곳 — **1건**

| 리소스 | 무엇이 없나 | 무엇이 막히나 |
| --- | --- | --- |
| **`inventory_adjustment_line`** | **테이블 자체가 없다** | `W-01-12`의 조정 라인. #88 회신으로 신설이 확정됐고 계약이 먼저 정의했다 |

⚠ **라인 사유 코드의 저장 자리**도 같이 정해져야 한다 — `W-01-12` §5-1의 「라인당 사유 ⚠ 저장 자리 없음」이다. #84·#87이 다룬 「기록하라는데 자리가 없다」 패턴의 연장이다.

### 5-3. 횡단 계약 3종 — **미착지 9건의 원인**

| 리소스 | 막는 화면 | 조항은 있다 |
| --- | --- | :-: |
| ~~`app.approval_request` · `approval_step` · `approval_route`~~ | ~~`W-01-02` · `W-01-06` · `W-01-11` · `W-01-12`~~ | ✅ **2026-08-07 해소** — `app-공통.json` |
| ~~`app.document_issue_log`~~ | ~~`P-01-01` · `P-01-02`~~ | ✅ **2026-08-07 해소** — `app-공통.json` v0.2 출력물 편 · **§K 7조항** |
| `app.attachment` | `M-01-01` | — |

~~**규약은 있는데 계약이 없다.**~~ ✅ **첫째가 풀렸다** — `app-공통.json`(경로 9 · 오퍼레이션 12)이 2026-08-07에 신설됐고 `06-API-요구서-app공통승인.md`가 근거다. **`W-01-02`·`W-01-06`·`W-01-11`·`W-01-12` 넷이 착수 통지로 나갔다.**

**남은 하나(`app.attachment`)는 POP·모바일 셸이 생길 때** 같은 파일에 더한다.

⚠ **새로 생긴 미착지 하나 — 상신 철회.** `W-01-13`의 「취소 요청 철회」가 갈 곳이 없다. **승인 워크플로우 일반의 문제**(상신자가 자기 요청을 거두는 것)이고 `W-01-13`만의 것이 아니라, 여기서 발명하지 않고 승인 계약 보강으로 넘긴다.

### 5-4. 다형 참조의 유형↔테이블 규약 — **세 번째 사용처**

```
goods_receipt.source_document_type_code + source_document_id     W-01-10
approval_request.target_type_code + target_id                    W-01-02
audit_event.target_type_code + target_id                         #68
```

**셋 다 FK가 없다.** 규약이 없으면 **화면이 원천 문서를 표시할 수 없다.** `W-01-02` §9-2가 제안한 계약 조항이 여기서 세 번째 사용처를 얻어 「세 곳 규칙」을 충족한다.

### 5-5. 선행 미결 5건 — 화면 스펙이 스스로 남긴 것

| 화면 | 미결 | 대기 |
| --- | --- | --- |
| `M-01-06` | 폐기 전환 경로 | 회신 **E-4** |
| `P-01-01` | 프린터 설치 구성 | §8-4 |
| `M-01-07` | 임시 적재 사유 코드 값 목록 | #64 계열 |
| `P-01-02` | 라인/파렛트 유형 값 목록 | #64 계열 |
| `P-01-02` | 출고 파렛트(HU) 생성 소유 — 이 화면인가 `M-01-08` 인가 | §8-6 |

---

## §6. API를 정의할 수 없는 화면 1건

### 6-1. `W-01-01` IQC 수입검사·판정

**계약을 만들 수 없어서가 아니라 우리 도메인이 아니다.**

```
쓰는 것   quality.inspection_request · inspection_result · inspection_measurement
테이블    ✅ 실재한다 (quality 스키마 15테이블)
계약      ⛔ 없다 — mdm-기준정보.json 의 /quality 16경로는 전부 마스터
          (inspection_plan · plan_version · item_spec · defect_code · cause_code)
소관      03 품질 도메인 — trace.lot_hold 쓰기를 03으로 보낸 것과 같은 규칙
```

> ✅ **해소 2026-08-12** — 03 품질 계약(`quality-03품질.json`)이 검사 3계층을 덮었다.
> `W-01-01` 착수 통지 발행 완료(client #85). 매핑은 `06-API-요구서-03품질.md` §3-1.

⚠ **1단계 도출이 이 화면을 「지금 착수 가능 7장」에 넣었다.** 「승인 불요」만 보고 세었고 **계약이 있는가는 따로 확인해야 했다.** → **착수 가능 6장**.

---

## §7. 범위 밖 — 명시

| 제외 | 이유 |
| --- | --- |
| `quality.inspection_*` **실행** | **03 품질** 도메인 — §6-1 |
| `trace.lot_hold` **쓰기** | **03 품질** — `W-03-02`·`W-03-03`. 01은 읽기만 |
| `sales_order*` · `shipment*` · `shipment_lot_allocation` | **04 제품출하** |
| `subcontract_*` 4테이블 | **화면이 0건**이다. 「추후 구체화」 확정이 이미 있다 |
| `trace.impact_analysis` · `serial_number*` | 화면 없음 — 리콜·시리얼은 후속 |
| `production.*` 생산 실적 | **02 생산실행** |
| `app.approval_*` · `document_issue_log` · `attachment` | **횡단** — `app-공통.json` 소관(§5-3) |

---

## §8. 후속

### 8-1. 이 문서가 넘기는 것

| # | 무엇을 | 어디로 |
| :-: | --- | --- |
| 1 | `inventory_adjustment_line` 신설 + **라인 사유 코드 자리** | `[docs→데이터모델]` 이슈 · assignee `wooju-shin` |
| 2 | 다형 참조 유형↔테이블 규약 | 같은 이슈에 묶는다 — 세 곳 규칙 충족 |
| 3 | **취소·역처리 화면이 필요한가** | 화면 인벤토리 검토 — `[uiux→docs]` 후보 |
| 4 | `app-공통.json`(승인) | 다음 트랙 — `W-01-02`·`06`·`11`·`12` 4장이 풀린다 |
| 5 | `app-공통.json`(출력물·첨부) | POP·모바일 셸이 생길 때 |
| 6 | 03 품질 실행 계약 | 별도 트랙 — `W-01-01`이 여기서 풀린다 |
| 7 | **착수 가능 통지 6장** | `uiux-client-handoff` 하네스 |

### 8-2. 착수 가능 6장

```
W-01-03  초과 입하 분리
W-01-04  재고 실사
W-01-05  공급사 반품 처리
W-01-07  재고 현황·상태 조회
W-01-09  입하 예정 조회
W-01-10  정상품 입하 처리
```

**모바일 10장·POP 2장은 계약이 다 있으나 클라이언트에 셸이 없다**(`apps/web` 하나뿐). 셸이 생기면 `M-01-02`·`04`·`05`·`08`·`10`·`11` 6장이 바로 나간다.

---

## §9. 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| **v0.3** | **2026-08-21** | **귀속 사번(`X-Worker-No`)을 「받을 자리」로 계약에 선언했다 — 이슈 #178.** 공유계약 **D-5**(귀속 정보는 세션이 아니다)가 「현장 단말·모바일의 쓰기 요청에 사번 **필수** — 없으면 서버가 거부」로 정해 두었는데 **그 사번을 받을 자리가 계약 일곱 벌 어디에도 없었다**(전수 검색 **0건**). ⛔ 저장 충돌 토큰(`If-Match`)에서 겪은 것과 **같은 형태**다 — 필수로 받아 놓고 받을 곳을 선언하지 않아 구현이 **19건** 막혔다(2026-08-17). **이 도메인 대상 17 오퍼레이션**(전체 36 · 필수 30 · 선택 6 · 계약 6벌 · 부르는 화면 36). ⭐ **필수판·선택판 두 벌을 둔 이유** — 관리웹도 **같은 경로·같은 메서드**를 부르는 자리가 여섯 있어(이 도메인은 실사 행 치환·출고·입고·재고 이동 넷) 일괄 필수로 걸면 관리웹이 **없는 사번을 지어내게 된다** — 관리웹 화면에는 사번 입력 자리가 없다. 그래서 **서버가 인증 토큰의 종류로 가른다**: 계정 토큰이면 인증 주체에서 행위자를 풀고, 단말 토큰인데 헤더가 없으면 **거부**한다. 선례 `IfMatchVersion`/`IfMatchVersionOptional` 을 그대로 따랐다. ⛔ **「헤더가 선택」이 「사번이 선택」은 아니다** — 완화는 관리웹에서만이다. **경로 불변 · 질의 불변 · 쓰기 본문 불변 — 헤더만 는다.** 계수 정본 `design/schema/generators/openapi/check-worker-no.py`(⚠ 그 파일 첫머리의 「안 보는 것」과 함께 읽는다 — 이 수치는 **하한**이다) · 패치 `design/raw/process/openapi-patches/patch-178-worker-no.py`. |
| **v0.2** | **2026-08-07** | **취소·역처리 경로 신설 — 경로 55 → 63 · 스키마 100 → 105 · 액션 109 → 116 · 화면 19 → 20.** ✅ **초판의 「되돌리는 전이가 없다」가 닫혔다**(§5-1-1) — `W-01-13` 신설이 근거를 만들었고 계약이 따라왔다. ⭐ **순서가 옳았다** — 화면이 없을 때 지어내지 않았기 때문에 화면이 생겼을 때 **버릴 것이 없었다.** **계약이 정본으로 갖는 것 셋** — ① **후속 판정을 서버가 한다**(다형 원천 참조라 프런트가 관계표를 갖게 하면 취소 판정이 조용히 틀린다) ② ⭐ **실행 시점 재판정**(승인 대기 중 후속이 생기면 자동 실행은 못 본다 · 걸려도 승인은 유효) ③ **취소는 반드시 승인을 탄다**(문서 12테이블에 `cancel*` 컬럼 0건이라 승인 기록이 이력을 대신한다). **미착지 9 → 7** — 승인 계약이 결재선 확인·승인 대상 표시·승인/반려를 풀었고 **상신 철회**가 새로 하나 늘었다. 「승인·반려」 2건은 `W-CO-09` 결재함으로 이관하며 액션 표에서 뺐다. |
| v0.1 | 2026-08-06 | 초안. 화면 **23장**의 액션 **109건 + 표 없는 4장**을 전건 소화. 커버리지 = 엔드포인트 56 · API 불필요 33 · 소관 이동 7 · 미착지 9 · 선행 미결 4. 적합성 = ✅12 · ⚠8 · ⛔2 · ❌1. ⭐ **되돌리는 전이가 없다** — 23장의 「취소」가 전부 화면 이탈이라 `:cancel` 을 만들지 않았고, 그 결과 **전기된 전표를 되돌릴 경로가 계약에 없다**(§5-1). ⛔ **`W-01-01` 은 검사 실행 계약이 없어 착수 가능이 7→6장**(§6-1). 규약 편차 2건 — **C-2 를 쓰지 않았다**(화면이 「한 트랜잭션」) · **A-5 의 결론이 기준정보와 다르다**(화면에 「라인 추가」가 있다). 미착지 9건의 원인은 **횡단 계약 3종**이고 §J·§K 조항은 이미 있다. |
