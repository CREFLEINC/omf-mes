# W-06-01 · Routing(공정) 등록·관리

> 작성일: 2026-08-03 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **MES 정본 마스터의 대표.** `W-06-05`가 ERP 수신본이었다면 이 화면은 정반대 축이다. 키 구조·편집 규칙이 **결정 07로 통째로 확정**돼 있어 설계 여지가 가장 적고, 그만큼 **하류 대조에서 결함이 잘 드러난다.**

## §0. 요약

- **개요**: 관리웹(기준정보연계 도메인)의 MES 정본 마스터 CRUD 화면. **축이 둘이다** — ① 품목별 Routing(공정) 헤더를 Rev 단위로 관리하고(헤더–라인 2계층(결정 07)에 품목 선택 축이 더해진 3단), ② 그 라인이 고르는 **공정 마스터(`mdm.process`) 자체를 등록·관리**한다(평면 목록). 형태가 달라 한 화면에 섞지 않고 **최상위 탭 2개**(《Routing》·《공정 마스터》)로 나눈다(사용자 확정 2026-09-03 · §3 · §8-5).
- **주요 데이터**: 헤더 `planning.routing`(품목+Routing코드+Rev, 상태: 작성중/확정/폐기) · 라인 `planning.routing_operation`(Sequence·공정·관리 플래그 7개·표준 C/T·표준 수율) · 선후행 `planning.routing_operation_dependency`(부여·회수형) · **공정 마스터 `mdm.process`**(공정코드·공정명·공정 유형·사용여부 — **MES 정본**, `REQ-PR-0026` · §4-D).
- **핵심 액션**: 신규 Rev 발행 · 저장/취소 · 공정 추가/삭제 · 순서 이동(`[↑][↓]`) · 선후행 설정 · 확정/폐기(상태 전이, 확정 후 in-place 편집 금지) · **공정 마스터 등록·수정·사용 중지/재개**(물리 삭제 없음 — `B-4`).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-06-01` · Routing(공정) 등록·관리 |
| 프로그램 · 도메인 | **관리웹** · 06 기준정보연계 |
| 유형 | 설정·마스터 |
| 행위자 | 생산관리자 · 전산담당 (+생산기술) |
| 신뢰도 | **확정** |
| IA 위치 | 기준정보 > 마스터 > Routing(공정) |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | WF06 **S4** 「Routing/공정 등록(MES 정본·CRUD)」 — Action=「품목별 **공정 순서(Sequence)·공정코드·외주공정 구분·Routing Rev·표준 C/T·L/T·UPH·사용여부**를 직접 CRUD」 · 라인에 **PQC 검사 공정 명시 플래그** · Rule=「**수정=신규 Rev 발행, 확정 Rev in-place 수정 금지(작성중에서만 편집)**」 · 「W/O 전개 시 **Rev 스냅샷 고정** — 소급 없음」 · 도식스펙06 §1.2 **B4**·MES-3 | 실측 |
| **화면·IA** | `04` §3 `W-06-01` 행 · 인벤토리 v1.4 — 「공정 순서/외주구분 CRUD·Rev 발행·표준 C/T·PQC 검사 공정 플래그」 | 실측 |
| **요구** | ✅**REQ-PR-0026** MES 공정 정보 관리 기능 — **ERP에 공정 정보가 없는 경우가 전제**(충돌① — 「ERP 정본」 가설 폐기) · Routing 미비 품목은 **BOM 기반 4M 투입 계획으로 fallback**<br>✅**REQ-PR-0002** 세부 공정 추가/삭제/수정 — 현행=작업자 수기 기록 · 「단일 제품에 다수의 세분화 공정」(사출→조립→검사→부착→검사)<br>✓설계확정 **결정 07**: **헤더–라인 2계층** — 헤더 키=**품목코드+Routing Rev**(속성: 상태(작성중/확정/폐기)·**기본(default) Rev 플래그**·유효 시작일) / 라인 키=**헤더 FK+Sequence**(속성: 공정코드·**외주 구분**·PQC 플래그) · **BOM Rev와 느슨한 참조**(강제 동기화 없음) | 실측 |
| **데이터** | `planning.routing`(SQL 617~) · `planning.routing_operation`(635~) · `planning.routing_operation_dependency`(660~) · `mdm.process`(443~) | 실측 |

## §3. 레이아웃 (관리웹 — 구조 기술)

**최상위 탭 2개**다 — 《Routing》과 《공정 마스터》(사용자 확정 2026-09-03 · §8-5 해소).

```
┌ PageHeader  기준정보 > 마스터 > Routing(공정)                           ┐
├─────────────────────────────────────────────────────────────────────────┤
│ [ Routing ] [ 공정 마스터 ]        ← 최상위 탭 2개                      │
└─────────────────────────────────────────────────────────────────────────┘
```

**《Routing》 탭 — 3단.** 품목 → 그 품목의 Rev 목록 → 선택 Rev의 공정 라인. 결정 07의 「헤더–라인 2계층」에 **품목 선택 축이 하나 더 붙는다**(헤더 키가 품목코드+Rev라서).

```
┌ PageHeader  기준정보 > 마스터 > Routing(공정)        [＋ 신규 Rev 발행] ┐
├──────────────┬─────────────────┬────────────────────────────────────────┤
│ 좌 (1/4)     │ 중 (1/4)        │ 우 (2/4)                                │
│              │                 │                                         │
│ [품목 검색]  │ 《Rev 목록》    │ 《헤더》 §4-A                           │
│ ┌ 품목 ────┐ │  Rev 상태 기본  │   Routing코드 · Rev · 상태 · 유효기간   │
│ │ 코드 명  │ │  ─────────────  │   ⚠ 상태=확정 → 전 항목 잠김 §5-4       │
│ │ ...(선택)│ │  3  확정   ●    │                        [취소] [저장]   │
│ └──────────┘ │  2  폐기        │  ────────────────────────────────────  │
│              │  1  폐기        │ 《공정 라인》 §4-B  — 순서 있는 표      │
│              │                 │  ┌─────────────────────────────────┐   │
│              │  [Rev 비교]     │  │Seq│공정│공정명│관리플래그7│C/T│  │   │
│              │                 │  │ 1 │사출│ …   │☑☐☑☐☑☐☐   │45 │  │   │
│              │                 │  │ 2 │조립│ …   │…          │   │  │   │
│              │                 │  └─────────────────────────────────┘   │
│              │                 │   [＋ 공정 추가] [↑][↓] [선후행 설정]  │
└──────────────┴─────────────────┴────────────────────────────────────────┘
```

- **Rev 목록을 접지 않는다.** 「지금 확정본이 무엇이고 과거가 무엇인지」가 이 화면의 핵심 정보다.
- 공정 라인은 **순서가 의미를 갖는 표**다 — 정렬은 `operation_seq` 오름차순, `[↑][↓]`로 재배치. **표시 번호는 목록 내 위치이며 `operation_seq`를 그대로 보이지 않는다**(1부터 연속 — 공유계약 A-5) — 채번 간격(10·20…)이 바뀌어도 화면은 흔들리지 않는다.
- **관리 플래그가 7개**라 열이 넓다(§4-B). 표에는 아이콘·체크만 두고 **행 선택 시 우측 인스펙터나 Dialog에서 전체 편집**한다.

**《공정 마스터》 탭 — 평면 목록 + 편집.** 품목 축이 **없다.** 공정 목록에서 행을 고르면 우측에서 편집한다(§4-D).

```
┌ 《공정 마스터》 탭                                       [＋ 공정 추가] ┐
├─────────────────────────────────┬───────────────────────────────────────┤
│ 좌 (1/2) 《공정 목록》          │ 우 (1/2) 《등록·편집》 §4-D           │
│ ┌─────────────────────────────┐ │                                       │
│ │공정코드│공정명│유형  │사용  │ │  공정코드  [ INJ-01     ] ⚠ B-4       │
│ │───────────────────────────  │ │   ⓘ 참조 12건 — 코드를 바꿀 수 없음   │
│ │INJ-01  │사출  │가공  │  ●   │ │  공정명    [ 사출       ]             │
│ │ASM-01  │조립  │조립  │  ●   │ │  공정 유형 [ 가공 ▼     ]             │
│ │INS-01  │검사  │검사  │  ─   │ │  사용여부  ● 사용                     │
│ └─────────────────────────────┘ │                                       │
│  [검색] [사용여부 ▼]            │                     [취소] [저장]     │
│                                 │  [사용 중지] / [사용 재개]            │
└─────────────────────────────────┴───────────────────────────────────────┘
```

- **왜 탭인가 — 축이 다르다.** Routing 은 품목 → Rev → 라인 **3단**이고, 공정 마스터는 품목과 무관한 **평면 목록**이다. 한 화면에 섞으면 품목을 고르지 않아도 살아 있어야 하는 **「품목 축이 없는 덩어리」**가 3단 안에 생긴다 — 좌측 품목 선택이 그 덩어리를 지배하지 못해 빈 상태·잠금 규칙이 두 벌로 갈린다.
- **왜 여기인가 — 소관이 이미 이 화면에 있다.** ① WF06 **S4** 가 이 화면에 준 Action 이 「공정 순서(Sequence)·**공정코드**·외주공정 구분·Routing Rev…를 직접 CRUD」다(§2) — 「공정코드 CRUD」가 처음부터 들어 있었다. ② **`REQ-PR-0026`** 은 「**ERP 에 공정 정보가 없는 경우가 전제**」이므로 `mdm.process` 는 **MES 정본**이지 ERP 수신본이 아니다 — 만드는 화면이 없으면 데이터가 생길 길이 없다. ③ 화면명이 이미 「Routing(**공정**) 등록·관리」다.
- ⛔ **모달로 붙이지 않는다.** 「공정 선택 드롭다운에 ＋ 새 공정」 식이면 **만들기만 되고 수정·사용 중지할 자리가 없다** — `B-4`(참조가 있으면 코드 수정 불가 → `is_active` 를 내리고 새로 만든다)를 쓸 곳이 사라진다. 그래서 **최상위 탭**이다.
- **`B-4` 코드 수정 잠금은 「필드 하나만」 잠근다.** 참조가 있으면 **공정코드 입력란만 비활성**되고 공정명·공정 유형·사용여부는 그대로 편집된다. 비활성 옆에 **사유와 참조 건수**를 함께 보인다(`ProcessDetailResponse.editability` — `F-1`·`G-2` · 해결 경로까지 담는다 §9-1).

## §4. 필드

감사 컬럼과 `version_no`는 표에서 제외(공유계약 A-4).

### §4-A. Routing 헤더 `planning.routing`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 품목 | `item_id` | FK → `mdm.item` | ✅ | 유일키 구성 | 좌측 선택 품목으로 고정 | 신규만 |
| Routing 코드 / *(미정)* | `routing_code` | `code_t` varchar(50) | ✅ | 유일키 구성 | | ⚠ B-4 |
| Rev | `routing_version` | integer | ✅ | 유일키 구성 | `CHECK > 0` · `uq_routing(item_id, routing_code, routing_version)` — **결정 07의 헤더 키 그대로** | ⛔ 시스템 채번 §5-4 |
| 상태 / *(미정)* | `status_code` | `code_t` | ✅ | | `DRAFT`(작성중) · `CONFIRMED`(확정) · `OBSOLETE`(폐기) **3값** — 결정 07 이 정한 값이 그대로 등재됐다. 값 목록은 `GET /mdm/code-values?codeGroupCode=MASTER_VERSION_STATUS` 로 받는다(공유계약 `G-32` · 코드 사전 `CD-MASTER-VERSION-STATUS` — Routing·BOM·검사기준 버전 공용). ⛔ **고객이 편집할 수 없다**(`registry-system`) · §8-4 | ⚠ 전이만 §5-4 |
| 유효시작 / *(미정)* | `effective_from` | date | **—** | | ⭐ **선택이다 — 결정 07 그대로**(2026-08-18 정정). 앞서 「하류가 `NOT NULL` 이니 필수」로 뒤집었던 것을 되돌린다. **물리 모델은 설계 결정을 앞설 수 없다**(사용자 확정 2026-08-18). 📨 모델 결손은 작업 통지 · §8-3 | ✅(작성중) |
| 유효종료 / *(미정)* | `effective_to` | date | — | | `ck_routing_dates` 짝 제약(A-2) | ✅(작성중) |

**✅ 결정 07이 정한 헤더 속성 「기본(default) Rev 플래그」를 되살렸다**(2026-08-30) — `Routing.isDefault` + `POST :set-default`. `Bom.isDefault`와 같은 형태(부분 유일 인덱스). 저장 컬럼은 데이터 모델 담당에게 통지, 기다리지 않는다.

### §4-B. 공정 라인 `planning.routing_operation`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| Sequence | `operation_seq` | integer | ✅ | ✅ **(헤더 내 · DB만)** | `CHECK > 0` · `uq_routing_operation(routing_id, operation_seq)` — **결정 07 라인 키.** 사용자는 값을 직접 입력하지 않는다 — **표시는 목록 내 위치**이고, 재배치 시 서버가 일괄 채번한다(공유계약 A-5 조항 2) | ⛔ **직접 입력 불가 — 행 이동`[↑][↓]`으로만**(§5-1) |
| 공정 | `process_id` | FK → `mdm.process` | ✅ | | 선택 목록 = `W-06-06`이 아니라 **공정 마스터** — ✅ 소관 해소(2026-09-03): **이 화면 《공정 마스터》 탭이 관리한다**(§4-D · §8-5). 목록은 `GET /mdm/processes` · `is_active=false` 인 공정은 **새로 고를 수 없다**(기존 라인은 유지 §6) | ✅(작성중) |
| 공정명 | `operation_name` | `name_t` varchar(200) | ✅ | | 공정 마스터명과 별개로 라인마다 이름을 다시 준다(같은 공정이 두 번 나오는 「사출→조립→검사→부착→**검사**」를 구분하기 위함 `[추정]`) | ✅(작성중) |
| **공정명(한/베)** | `nameKo`·`nameVi` | `name_t` varchar(200) | — | | ✅ **되살림**(2026-08-30) — `W-06-06` §8-1과 같은 해법 | ✅(작성중) |
| MES 관리 | `mes_managed` | boolean 기본 `true` | ✅ | | 끄면 이 공정은 MES가 통제하지 않는다 | ✅(작성중) |
| 자재투입 관리 | `material_input_managed` | boolean 기본 `false` | ✅ | | `P-02-04`·`M-01-02` 투입 화면 노출 조건 | ✅(작성중) |
| 실적 관리 | `production_result_managed` | boolean 기본 `true` | ✅ | | | ✅(작성중) |
| **검사 관리(PQC)** | `inspection_managed` | boolean 기본 `false` | ✅ | | ✓확정 **QA #4 PQC opt-in의 근거 필드**(결정 07) — 끄면 **PQC 생략** | ✅(작성중) |
| 산출 LOT 필수 | `output_lot_required` | boolean 기본 `false` | ✅ | | 이 공정이 생산 LOT을 만드는가 | ✅(작성중) |
| 설비 필수 | `equipment_required` | boolean 기본 `false` | ✅ | | 4M 배정 통제(`W-02-03`) | ✅(작성중) |
| 금형 필수 | `mold_required` | boolean 기본 `false` | ✅ | | 동 | ✅(작성중) |
| 표준 C/T(초) | `standard_cycle_time_sec` | numeric(18,6) | — | | `CHECK > 0` — **0 불가** · 단위가 **초**임을 라벨에 명시 | ✅(작성중) |
| 표준 수율 | `standard_yield_rate` | numeric(9,6) | — | | `CHECK BETWEEN 0 AND 1` — **비율이지 퍼센트가 아니다**(`W-06-05` §9-2) | ✅(작성중) |

**⚠ 결정 07·WF06 S4가 요구한 라인 속성 중 셋이 물리 모델에 없다 — 실측.**

| 요구 속성 | 근거 | 물리 모델 | 판정 |
| --- | --- | --- | --- |
| PQC 검사 공정 명시 플래그 | 결정 07 · QA #4 | `inspection_managed` | ✅ 있다 |
| 표준 C/T | WF06 S4 · 결정 07 | `standard_cycle_time_sec` | ✅ 있다 |
| **외주공정 구분** | **결정 07 라인 속성 명시** · WF06 S4 Action·Exception ③ | `RoutingOperation.isOutsourced` | ✅ **되살림**(2026-08-30) — §4-B-1 |
| **L/T · UPH** | WF06 S4 Action | **없음** | ⛔ **§8-2**(범위 밖 가능성) |
| **사용여부** | WF06 S4 Action | 없음(헤더 `status_code`가 대체) | 판정 보류 §8-2 |

### §4-B-1. ✅ 외주 공정 구분 — **되살림**(2026-08-03 사용자 확정 · 2026-08-30 계약 반영)

**사용자 확정 내용**

> 외주품은 「외주 제품 또는 반제품」과 **동일**하다. **외주 가공품도 결국 외주품에 포함**된다.
> **Routing에 외주 공정이 필요**하다. 외주 공정은 **최대한 단순화**하여 정의한다.
> 외주품 관리 설계가 아직 완성되지 않았으니 **지금은 최소한의 구조만 설계하고 추후 구체화**한다.

**신설 요청 — 컬럼 1개**

| 라벨(한 / 베) | 제안 컬럼 | 타입 | 필수 | 비고 |
| --- | --- | --- | :-: | --- |
| 외주 공정 / *(미정)* | `isOutsourced` | boolean 기본 `false` | ✅ | 「이 공정은 외주로 나간다」 — 계약에 신설. 물리 컬럼은 이미 `is_subcontract`로 존재(`omf-mes-server#42` 2026-08-28 확인) — 이름 정합화만 데이터 모델 통지 |

> ⚠ **정정(2026-08-30)** — 아래 「신설 요청」은 2026-08-03 초안 시점 기록이다. 2026-08-28 재검토에서 물리 컬럼이 **이미 있음**이 확인됐다(다만 이름이 `is_outsourced`가 아니라 `is_subcontract`). 남은 것은 컬럼 신설이 아니라 **이름을 어느 쪽으로 맞출지의 결정**이다.

**왜 boolean 하나면 되는가 — 거래 시점 정보는 이미 하류에 있다.**

`logistics.subcontract_order`가 **거래처·공정·품목·수량·회수예정일·상태**를 전부 갖고 있다(실측 SQL 2216~). 마스터에 기본 거래처·리드타임·단가를 두면 **「추후 구체화」할 때 두 곳을 고쳐야** 하고, 같은 공정을 여러 업체에 맡기는 경우 마스터 값이 곧 무의미해진다. **실제 업체는 발주 시점에 정해진다.**

**이 한 칸으로 성립하는 것 둘** — ① 외주 발주 화면의 **공정 선택 목록 필터**(지금은 전 공정을 다 보여줘야 한다) ② W/O 전개 시 **외주 흐름 분기 판정**(WF06 S4 Exception ③ 「외주공정 누락 → 외주 흐름 통제 불가」 해소).

**화면 반영**: §4-B 관리 플래그가 **7개 → 8개**가 된다. 컬럼이 생기기 전까지는 **G-2(비활성 + 사유)**.

> **⚠ 「추후 구체화」의 방향을 미리 좁히지 않는다.** 지금 넣지 않기로 한 것(기본 거래처·리드타임·단가·외주 유형 코드)은 **필요해지면 추가가 쉽고, 잘못 넣으면 제거가 어렵다.** 외주품 관리 설계가 서면 그때 이 칸을 확장할지 별도 테이블로 뺄지 정한다.

### §4-C. 공정 선후행 `planning.routing_operation_dependency` — 부여·회수 형태

| 라벨 | 출처 컬럼 | 타입 | 필수 | 검증·비고 |
| --- | --- | --- | :-: | --- |
| 선행 공정 | `predecessor_operation_id` | FK → `routing_operation` | ✅ | `ck_routing_dependency_self` — **자기 자신 금지** |
| 후행 공정 | `successor_operation_id` | FK → `routing_operation` | ✅ | 유일: `uq_routing_dependency(선행, 후행)` |
| 의존 유형 | `dependency_type_code` | `code_t` 기본 `'FINISH_TO_START'` | ✅ | `FINISH_TO_START`(종료-시작 · 기본) · `START_TO_START`(시작-시작) · `FINISH_TO_FINISH`(종료-종료) · `START_TO_FINISH`(시작-종료) **4값** — 값 목록은 `GET /mdm/code-values?codeGroupCode=ROUTING_OPERATION_DEPENDENCY_TYPE` 로 받는다(공유계약 `G-32` · 사용자 확정 2026-09-03). ⛔ **고객이 편집할 수 없다**(`registry-system`) |

> **⚠ 상류가 요구하지 않은 것이 하류에 있다.** 결정 07은 라인을 **Sequence 일렬**로 정했고 선후행 그래프를 말하지 않았다. 그런데 하류에 의존 테이블이 있다 — `uiux/CLAUDE.md` §데이터모델의 「**하류가 앞서간다**」 사례다. 순차 진행만 필요하면 이 테이블은 비어 있어도 되므로, 화면은 **선택 기능([선후행 설정])으로 두고 기본은 Sequence 순**으로 본다 → §8-6.
> **순환 방지는 DB가 하지 않는다** — 자기참조만 막고 A→B→A는 통과한다. 화면·서버 책임(`W-06-06` §8-6과 같은 형태).

### §4-D. 공정 마스터 `mdm.process` — ✅ 신설(2026-09-03 사용자 확정 · §8-5 해소)

**《공정 마스터》 탭의 필드다.** 계약 `Process` 스키마가 정본이고, 항목은 **정확히 넷**이다.

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 공정코드 / *(미정)* | `process_code` | `code_t` varchar(50) | ✅ | ✅ | ⚠ **`B-4`** — **참조 건수 0 일 때만 수정 가능.** 참조가 있으면 이 입력란**만** 비활성 + 사유·참조 건수 표시(`ProcessDetailResponse.editability`) · 바꾸려면 사용을 중지하고 새 공정을 등록한다 | ⚠ B-4 |
| 공정명 / *(미정)* | `process_name` | `name_t` varchar(200) | ✅ | | | ✅ |
| 공정 유형 / *(미정)* | `process_type_code` | `code_t` varchar(50) | ✅ | | 공통코드 — `MACHINING`(가공)·`ASSEMBLY`(조립)·`INSPECTION`(검사)·`PACKAGING`(포장) **4값**. 값 목록은 `GET /mdm/code-values?codeGroupCode=PROCESS_TYPE` 로 받는다(공유계약 `G-32`). ⭐ **고객이 늘린다 — 위 넷은 초기 시드다**(`registry` · `G-31`) — 화면은 값을 **하드코딩하지 않는다** | ✅ |
| 사용여부 | `is_active` | boolean 기본 `true` | ✅ | | **물리 삭제 없음** — `POST :deactivate` / `POST :activate` 로만 바꾼다(`B-4`) | ⛔ 액션으로만 |

**계약 대응** — `GET /mdm/processes`(목록 · **기존**) · `POST /mdm/processes`(등록) · `GET /mdm/processes/{processId}`(상세) · `PUT /mdm/processes/{processId}`(수정) · `POST /mdm/processes/{processId}:deactivate` · `POST /mdm/processes/{processId}:activate`. 상세 응답 `ProcessDetailResponse` = `{ process, editability }` 이고 **`editability` 가 `B-4` 의 「수정 가능 여부와 사유·참조 건수」를 담는다** — 화면은 그 값으로 공정코드 입력란을 잠그고 사유를 표시한다(판정을 화면이 계산하지 않는다).

**⭐ `B-4` 참조 건수는 «셀 수 있다» — 무조건 잠그는 갈래가 아니다.** 공정의 참조처는 **전부 `process_id` FK** 다 — `W-06-01` §4-B 공정 라인 · `W-06-02` §4-A 검사 대상 공정 · `W-06-03` §4-A·§4-C · `W-06-05` §4-F · `W-06-06` §4-E 자격 대상 공정. 그러므로 서버가 참조 건수를 실제로 세어 `editability` 에 실을 수 있고, `code_value` 처럼 **`NOT_COUNTABLE` 로 무조건 잠그는 처리를 쓰지 않는다.** 화면도 「참조 12건」처럼 **숫자를 그대로 보인다.**

**⛔ 범위 밖 — 다국어 명칭.** `nameKo`·`nameVi` 는 형제 마스터(`CauseCode` · §4-B 공정 라인)에는 있지만 **2026-09-03 승인 범위가 아니다.** 이 표에 넣지 않고 §8 에 물음으로 남긴다(§8-10).

## §5. 액션·상태

### §5-1. 액션

| 액션 | 위치 | 활성 조건 | 비고 |
| --- | --- | --- | --- |
| **신규 Rev 발행** | 헤더 | 품목 선택됨 + **복사할 확정 Rev 있음** | **`W-06-11`과 같은 행위** — 소관 중복 §8-7. 최신 Rev를 복사해 `작성중`으로 만든다 ⛔ 마지막 확정 Rev 를 폐기하면 복사할 원본이 없어 이 버튼이 부를 경로가 사라진다 — §8 |
| 저장 / 취소 | 헤더·라인 | 상태=**작성중** + 변경 있음 | |
| 공정 추가 / 삭제 | 라인 | 상태=작성중 | |
| 순서 이동 `[↑][↓]` | 라인 | 상태=작성중 · 2행 이상 | `operation_seq` 재배치 — **유일 제약 때문에 중간 상태가 충돌한다** §8-8 |
| 선후행 설정 | 라인 | 상태=작성중 | 부여·회수 — 즉시 반영 |
| **확정** | 헤더 | 상태=작성중 + 라인 1건 이상 | **상태 전이**(작성중 → 확정). 이후 in-place 편집 불가(결정 07) — B-2 「입력과 확정의 분리」 |
| **폐기** | 헤더 | 상태=확정 | 확정 → 폐기 |
| Rev 비교 | Rev 목록 | 2건 이상 | `[추정]` — 근거 문구 없음. 검증: 사용자 확인 |
| 변경 이력 | 헤더 | 저장 이력 있음 | `audit.audit_event` · **다만 Rev 자체가 이력이다**(결정 07 「변경 이력 요구는 Rev 이력으로 충족」) |
| **공정 추가** | 《공정 마스터》 탭 — 목록 머리 | 권한 있음 | `POST /mdm/processes` — 우측 편집란을 빈 채로 열고, 저장되면 목록을 갱신해 그 행을 선택한다(§4-D) |
| **공정 수정** | 《공정 마스터》 탭 — 편집란 | **행 선택됨** + 변경 있음 | `PUT /mdm/processes/{processId}`(`If-Match` §5-3). ⚠ **공정코드는 참조 건수 0 일 때만 고칠 수 있다**(`B-4`) — 참조가 있으면 그 입력란만 비활성이고 나머지는 저장된다. 판정은 `editability` 가 내려준다 |
| **사용 중지 / 사용 재개** | 《공정 마스터》 탭 — 편집란 | 행 선택됨 · 현재 `is_active` 에 따라 **한쪽만** 활성 | `POST /mdm/processes/{processId}:deactivate` / `:activate` — **물리 삭제 없음**(`B-4`). 중지해도 **기존 확정 Rev 는 영향 없다**(§6) |
| 공정 유형 선택 | 《공정 마스터》 탭 — 편집란 | 편집 중 | 값 목록 = `GET /mdm/code-values?codeGroupCode=PROCESS_TYPE`(공유계약 `G-32`) — ⭐ **고객이 늘리는 그룹**(`registry`)이라 화면에 값을 박지 않는다 |

### §5-2. 화면 상태

**《Routing》 탭**: `초기(품목 미선택 — EmptyState)` → `Rev 미선택` → `조회(확정·폐기 — 전 항목 읽기 전용)` / `편집(작성중)` → `저장 중` → `성공(Toast)` / `검증 실패(인라인)` / `충돌(B-1)`

**《공정 마스터》 탭**: `초기(목록 조회 — 행 미선택)` → `조회(행 선택)` / `신규(＋ 공정 추가 — 빈 편집란)` → `편집` → `저장 중` → `성공(Toast)` / `검증 실패(인라인)` / `충돌(B-1)` · ⚠ 참조가 있는 공정을 고르면 **조회·편집 상태 안에서 공정코드 입력란만 비활성**이다(§4-D — 별도 상태가 아니다).

### §5-3. 낙관적 잠금

`routing`·`routing_operation`에 `version_no`가 있으므로 **B-1 적용**. `routing_operation_dependency`는 부여·회수라 해당 없음.

**`mdm.process` 도 같은 규칙이다.** 계약이 `GET /mdm/processes/{processId}` 응답에 **`ETag`**, `PUT /mdm/processes/{processId}` · `:deactivate` · `:activate` 요청에 **`If-Match`** 를 두었다 — 화면은 상세를 받을 때 토큰을 쥐고 쓰기마다 되돌려 보낸다. ⛔ **토큰은 헤더로 전송하되 화면에 표시하지 않는다**(공유계약 `A-4`).

### §5-4. ⚠ 상태 기반 편집 잠금 — 이 화면의 핵심 규칙

**결정 07**: 확정 Rev는 **in-place 수정 금지**, **작성중 상태에서만 편집**.

| 상태 | 헤더 | 라인 | 가능한 전이 |
| --- | --- | --- | --- |
| **작성중** | ✅ 편집 | ✅ 편집 | → 확정 |
| **확정** | ⛔ 잠김 | ⛔ 잠김 | → 폐기 · **신규 Rev 발행** |
| **폐기** | ⛔ 잠김 | ⛔ 잠김 | (없음) |

**표시 방법**: `W-06-06` §5-4의 수신본 게이트와 **같은 원리, 다른 이유**다 — 감추지 않고 **비활성 + 사유 표시**.

```
《헤더》  ⓘ 확정된 Rev는 수정할 수 없습니다. 변경하려면 신규 Rev를 발행하세요.  [＋ 신규 Rev 발행]
```

> **⚠ 잠금의 근거가 둘로 갈린다.** ① **수신본이라 잠김**(`W-06-05`·`W-06-06`) ② **상태가 확정이라 잠김**(이 화면). 사용자에게는 둘 다 「비활성」으로 보이지만 **해결 방법이 다르다**(ERP에서 고쳐라 / 신규 Rev를 발행하라). 사유 문구가 해결 경로를 담아야 한다 → §9-1.

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 권한 없음 | 편집 컨트롤 비활성 + 사유(회신 E-9 대기) |
| **순서 일괄 치환 실패 (Sequence)** | 서버 오류 토스트 · 「순서 변경에 실패했습니다. 다시 시도해 주세요.」 — **사용자가 값을 직접 입력하지 않으므로 인라인 유일 위반 오류는 성립하지 않는다**(공유계약 A-5 조항 2) |
| 짝 제약 위반 (유효기간) | 두 필드 함께 인라인(A-2) |
| 범위 위반 (C/T ≤0 · 수율 0~1 밖) | 인라인 · **허용 범위를 문구에** |
| 선후행 자기참조 | 저장 전 차단 |
| **선후행 순환** | DB가 막지 않는다 — 화면·서버가 검사 §8-6 |
| **라인 0건인 채 확정 시도** | 확정 버튼 비활성 + 사유(「공정을 1건 이상 등록해야 확정할 수 있습니다」) |
| **Routing 없는 품목의 W/O 전개** | 이 화면의 예외가 아니다 — **차단하지 않고 BOM 기반 fallback**(REQ-PR-0026 확정). 여기서는 **Routing 미보유 품목 필터**를 제공해 보강 등록을 돕는다 |
| **참조가 있는 공정의 코드를 고치려 함** (`B-4`) | **공정코드 입력란만 비활성** + 사유·참조 건수(「이 공정을 참조하는 곳이 12건 있어 코드를 바꿀 수 없습니다. **사용을 중지하고 새 공정을 등록**하세요」). 나머지 항목은 그대로 저장된다. 판정·건수는 `editability` 가 내려준다 — 화면이 세지 않는다(§4-D · `F-1`·§9-1) |
| **사용 중지하려는 공정이 확정 Routing 에 물려 있음** | **중지는 된다** — 확정 Rev 는 스냅샷이라 **기존 Routing 은 영향 없다.** 달라지는 것은 앞으로다: **새 공정 라인의 선택 목록에서 빠진다.** 확인 Dialog 에 「이 공정을 쓰는 Routing N건이 있습니다 — 기존 Rev 는 그대로 유지되고, 새 라인에서만 고를 수 없게 됩니다」를 함께 보인다 |
| 오프라인 | **해당 없음** — 관리웹은 온라인 전제(결정 17) |

## §7. DS 매핑 (`@crefle/web-ui` v0.1.0)

### §7-1. 그대로 쓰는 것 (a) · 조합 (c)

| 용도 | 컴포넌트 | 유형 |
| --- | --- | :-: |
| 페이지 틀 | `AppShell` · `PageHeader` · `Breadcrumb` | a |
| 3단 목록 | `Table` ×3 (`density="compact"` · `selectable`) | a |
| 공정 라인 편집 그리드 | `Table` + `TableColumn.render`(TextField·Checkbox) | **c** (기등재) |
| 상태 표시 | `Chip`(작성중/확정/폐기) | a — **색은 G-1 상태 토큰** |
| 잠금 사유 | `AlertBanner`(info) + 비활성 컨트롤 | **c** (기등재) |
| 검색·필터 | `SearchInput` + `Select` | **c** (G-2) |
| 확정·폐기 확인 | `Dialog` | a |
| 알림·오류 | `Toast` · `AlertBanner` | a |
| 빈 상태 · 로딩 | `EmptyState` · `Skeleton` | a |

### §7-2. ~~⚠ 갭 후보~~ → **DS에 있다** — 순서 재배치(행 이동)
> ⛔ **2026-08-06 정정 — `Table` 에 `reorderable`·`onRowReorder` 가 실재한다.** 고정 커밋 `a554d11`(`omf-mes-client` 의 `pnpm-lock.yaml` 기준) 설치본에서 확인했고 **DS 이슈 #39 도 CLOSED** 다. 이 절의 「없다」·`b` 판정은 **문서가 쓰인 뒤 DS 가 앞서간 것**이다 — `ds-gap.md` §「DS가 우리 문서보다 앞서간 두 번째 사례」.
>
> ⚠ **제약 둘을 함께 안다** — **`groupBy` 와 병용 불가**(그룹 순서가 데이터에서 파생돼 이동의 의미가 성립하지 않는다) · **정렬이 켜져 있는 동안 재배치 버튼 비활성**. 그리고 `onRowReorder` 주석이 **공유계약 A-5 와 그대로 맞는다** — 「순서 컬럼에 유일 제약이 있으면 최종 순서 전체를 한 번에 보낸다」.


공정 라인은 **순서가 데이터**다. `[↑][↓]` 버튼은 `IconButton` 조합으로 되지만, 실무에서 기대되는 **드래그 재정렬**은 `Table`에 없다.

~~**판정: `b`(변형·prop 부족)** — `Table`은 있으나 `reorderable`·`onRowReorder` 류 prop이 없다.~~ → **판정 `a`.** 둘 다 실재한다(2026-08-06 실측). **`[↑][↓]` 대체 구현을 만들지 않는다.**

| 사용처 후보 | 근거 |
| --- | --- |
| `W-06-01` 공정 라인 | `operation_seq` (이 화면) |
| `W-06-06` 코드값 정렬 | `display_order` (실측 — SQL 97) |
| `W-06-02` 검사항목 순서 | `W-06-02`에서 확인 예정 |

**≥3곳이면 DS 이슈**(컴포넌트의 언어로: 「행 순서를 사용자가 바꿀 수 있는 표」). `ds-gap.md`에 **G-7로 신규 등재** · 이슈는 전 화면 매핑 후 몰아서(§6-2).

## §8. 미결

| # | 항목 | 성격 | 처리 |
| --- | --- | --- | --- |
| **1** | ✅ **되살렸다**(2026-08-30) — `Routing.isDefault` + `:set-default` 신설로 `Bom`과 대칭을 이뤘다 | — | **해소** — 📨 데이터 모델 통지(저장 컬럼) |
| **2** | **외주공정 구분 · L/T · UPH** | **상류↔하류 불일치 — 부분 해소** | **외주 구분: ✅ 완전 해소(2026-08-30)** — `isOutsourced` 계약 반영. 물리 컬럼은 이미 `is_subcontract`로 존재(`omf-mes-server#42`) — 이름 정합화만 데이터 모델 통지(§4-B-1).<br>**L/T·UPH: 미해소 유지** — 「목표관리(3.2.3) 연계」용이고 **목표관리 화면이 인벤토리에 없다.** 범위 밖일 가능성이 높아 확인 대상으로 남긴다 ⭐ **2026-09-04 재판정 — 표지를 `A-20` 으로 간다.** ⛔ **옛 표지 둘 다 우리 이슈가 아니었다** — `#42` 는 위 산문의 「`omf-mes-server#42`」(개발팀 저장소 번호)를 생성기가 우리 `#42`(생산LOT 러닝체인지 · 2026-07-29 **코멘트 0건** 종결)로 잘못 센 것이고, 미결 대장이 함께 실은 `B-1` 은 공유계약 조항이 아니라 위 산문의 **「§4-B-1」 절 번호**다. ⇒ 이 행에는 살아 있는 표지가 **하나도 없었다.** ⭐ **남은 것은 L/T·UPH 하나**이고(외주 구분은 `RoutingOperation.isOutsourced` 실측 확인), 공유계약 §I 대장 **8-b** 가 「~~L/T · UPH~~ … **요청 제외**(범위 밖 가능성 · **확인 대상**)」로, 계약 `x-internal-note` 가 「L/T·UPH 부재 — 목표관리 화면이 인벤토리에 없어 범위 밖 가능성」으로 같은 상태를 적는다. ⇒ 회신 대기가 아니라 **`A-20` 이 정한 판정 절차**(「어느 화면이 이것을 필요로 하는가」를 이름으로 못 적으면 이월이 아니라 보류 · 「지금 필요한가」를 판정하고 아니면 이월을 종료한다)를 돌릴 자리다 — 실측상 이 축을 필요로 하는 화면은 **0건**이다 |
| 4 | ~~`status_code` 값 목록 — 결정 07이 **작성중/확정/폐기**로 정했으나 공통코드 등록은 미완~~ | 공통코드 | ✅ **해소(2026-09-03)** — 코드 사전에 등재됐다. `MASTER_VERSION_STATUS` = `DRAFT`(작성중)·`CONFIRMED`(확정)·`OBSOLETE`(폐기) 3값(`registry-system` — 고객 편집 불가)이고 **BOM·검사기준 버전이 이 그룹을 준용**한다(사용자 결정 2026-09-02). `GET /mdm/code-values?codeGroupCode=MASTER_VERSION_STATUS` 로 받는다 |
| 5 | ~~**공정 마스터(`mdm.process`) 관리 화면이 어디인가** — `process_code`·`process_name`·`process_type_code`를 등록하는 화면이 인벤토리 108건에 **명시적으로 없다**~~ | 화면 도출 | ✅ **해소(2026-09-03 사용자 확정)** — **이 화면이 흡수한다. 최상위 탭 2개**(《Routing》·《공정 마스터》)로 나눴다(§3 · §4-D · §5-1 액션 4건 · §5-2·§5-3 · §6). 근거 넷 — ① **소관이 이미 배정돼 있었다**: WF06 **S4** 가 이 화면에 준 Action 이 「공정 순서(Sequence)·**공정코드**·외주공정 구분·Routing Rev…를 직접 CRUD」다(§2) — 「공정코드 CRUD」가 처음부터 들어 있었고 §4 가 그것을 안 썼을 뿐이다 ② **`REQ-PR-0026`** 「**ERP 에 공정 정보가 없는 경우가 전제**」 — `mdm.process` 는 **MES 정본**이지 ERP 수신본이 아니므로 **만드는 화면이 없으면 데이터가 생길 길이 없다** ③ 화면명이 이미 「Routing(**공정**) 등록·관리」다 ④ **선례** — `quality.cause_code`(하류가 참조하는데 관리 화면 부재)를 `W-06-03` 이 흡수한 것과 같은 모양이다(2026-08-03 사용자 확정). ⛔ **모달이 아니라 탭이다** — 드롭다운에 「＋ 새 공정」을 붙이면 만들기만 되고 **수정·사용 중지할 자리가 없어** `B-4` 를 쓸 곳이 사라진다 |
| 6 | **선후행 그래프를 쓸 것인가** — 상류(결정 07)는 Sequence 일렬만 정했는데 하류에 의존 테이블이 있다 | 설계 결정 | 쓰지 않으면 테이블이 비고, 쓰면 Sequence와 두 체계가 공존한다. **하류가 앞서간 사례**라 상류 확정이 필요 |
| 8 | **Sequence 재배치 시 유일 제약 충돌** | 설계 | 10↔20을 맞바꾸려면 중간에 반드시 중복이 생긴다. **서버가 한 트랜잭션으로 일괄 갱신**해야 한다(`W-06-05` §9-3과 같은 형태) ✅ **해소 2026-09-02** — 공유계약 A-5 해당 목록에 `routing_operation.operation_seq` 가 있고 「— `W-06-01` §8-8」로 이 미결을 명시 인용한다(화면은 최종 순서만·서버 일괄) |
| 9 | **마지막 Rev 를 폐기한 뒤 다시 시작할 길이 없다** | 상류↔하류 불일치 | `:obsolete` 는 「되돌릴 수 없다」가 확정이고 「신규 Rev 발행」은 복사할 원본을 요구한다. 마지막 확정 Rev 를 폐기하면 이 품목의 Routing 을 다시 만들 경로가 계약에 없다 — 화면은 항상 켜 두는 버튼인데 부를 엔드포인트가 없다. **결정 필요** |
| 10 | **공정 마스터에 다국어 명칭을 두는가** | 설계 결정 | 형제 마스터는 다국어를 갖는다 — `quality.cause_code`(`CauseCode`)에 있고, 이 화면 §4-B 공정 라인·`W-06-06` 코드값도 `nameKo`·`nameVi` 를 되살렸다. 그런데 2026-09-03 승인 범위(§4-D)는 **필드 넷**(`process_code`·`process_name`·`process_type_code`·`is_active`)뿐이라 공정 마스터만 한국어 단일 명칭이다. 형제와 맞출지, 공정은 예외로 둘지 — **결정 필요** |

## §9. 이 장에서 나온 공유 계약 후보

1. **비활성 사유는 「왜」가 아니라 「어떻게 풀 것인가」를 담는다.**
   잠금의 근거가 최소 4갈래로 갈렸다 — ① ERP 수신본(→ ERP에서 수정 후 재동기화) ② 상태=확정(→ 신규 Rev 발행) ③ 참조 존재로 코드 수정 불가(B-4 → `is_active` 내리고 새로 만들기) ④ 권한 없음(→ 관리자 문의). **사용자에게는 넷 다 「회색 컨트롤」로 보인다.** F-1·G-2가 「사유를 표시한다」까지 정했으므로, 여기에 **해결 경로를 함께 표시**한다는 조항을 더한다.
2. ~~**상류 문서와 물리 모델의 제약이 어긋나면 물리 모델을 따른다.**~~ ⛔ **철회 — 2026-08-18 사용자 확정**
   ~~`00-화면스펙서식.md` §8은 「원본과 어긋나면 **원본이 이긴다**」인데, **필수·유일·범위 같은 제약**은 물리 모델이 정본이다. 화면은 하류를 따르고 **불일치를 §8에 기록**한다.~~
   ⛔ **이 후보는 조항이 되지 않았고, 되어서도 안 된다.** **물리 데이터 모델은 설계 결정을 앞설 수 없으며, 설계 단계에서 물리 모델 자료를 참조하지 않는다.** 확정과 모델이 어긋나면 **확정이 이긴다** — `00-화면스펙서식.md` §8 의 「원본이 이긴다」에 **예외가 없다.**
   ⭐ **이 후보가 실제로 해를 끼쳤다** — 이 조항 논리로 결정 07 의 「유효 시작일(선택)」이 필수로 뒤집혔고(§8-3), 같은 논리가 다른 화면에도 퍼졌다. **모델이 못 담으면 계약은 화면대로 쓰고 모델 쪽에 📨 작업 통지를 낸다.**
3. **순서가 데이터인 표의 재배치 규약.**
   `operation_seq`·`sequence_no`처럼 **유일 제약이 걸린 순서 컬럼**은 화면에서 맞바꿀 때 중간 상태가 반드시 제약을 위반한다. **일괄 재배치는 서버가 한 트랜잭션으로** 수행하고, 화면은 최종 순서만 보낸다. 최소 3곳(`routing_operation.operation_seq` · `quality.inspection_item_spec.sequence_no` · `planning.bom_component.sequence_no`). ⚠ **2026-08-03 정정** — `code_value.display_order`는 유일 제약이 없어(중복 허용) 대상이 아니다(공유계약 A-5 정정).
4. **B-2(입력/확정 2단계)는 판정 화면만의 것이 아니다.**
   B-2는 `confirmed_at`을 가진 **검사·판정 13건**에서 도출됐는데, 이 화면의 `작성중 → 확정`이 **같은 구조**다(값 저장과 상태 전이의 분리 · 확정 후 편집 잠금). **마스터 Rev 관리도 B-2의 적용 대상**임을 계약에 명시한다.

## 근거·이력

### §8 해소된 미결

| # | 항목 | 성격 | 처리 |
| --- | --- | --- | --- |
| 3 | ~~`effective_from` 필수/선택 불일치~~ ✅ **해소 2026-08-18 — 선택이다** | 📨 **작업 통지** | ⛔ **앞의 판단을 뒤집는다.** 「화면은 하류를 따른다 · **제약은 하류가 정본**」이라 적었던 것이 **틀렸다.** **물리 모델은 설계 결정을 앞설 수 없고, 설계 단계에서 물리 모델을 참조하지 않는다**(사용자 확정 2026-08-18). → **결정 07 그대로 「선택」**이며 계약에서도 필수를 풀었다. 모델의 `NOT NULL` 은 **남의 작업이고 우리를 막지 않는다**. ⛔ **§9-2 후보 2번도 철회한다** |
| ~~7~~ | **신규 Rev 발행의 소관** | ✅ **확정 2026-08-03 (사용자 확정)** | **실행은 이 화면**(헤더+라인을 함께 복사·편집하는 일이라 3단 레이아웃 없이는 성립하지 않는다). `W-06-11`은 **조회·창구**로 확정 — 그 화면의 「신규 Rev 발행」 버튼은 이 화면으로 **이동만** 시킨다 |

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| **v0.5** | **2026-09-03** | **공정 마스터(`mdm.process`)를 이 화면이 흡수 — 최상위 탭 2개 신설.** §3 에 《Routing》·《공정 마스터》 탭 2개(기존 3단은 《Routing》 탭 안에 그대로) · **§4-D 신설**(`process_code`·`process_name`·`process_type_code`·`is_active` 4필드 · `PROCESS_TYPE` 4값은 초기 시드이고 고객이 늘린다 `G-31`·`G-32`) · §5-1 액션 4건 추가(등록·수정·중지/재개·유형 선택) · §5-2 탭별 상태 · §5-3 `ETag`/`If-Match` (`A-4`) · §6 예외 2건 · **§8-5 해소** · §8-10(다국어 명칭) 신규 · §4-B 공정 행의 소관 참조 갱신. **근거 넷** — ① WF06 S4 Action 에 「**공정코드** 직접 CRUD」가 처음부터 있었다 ② `REQ-PR-0026` 「ERP 에 공정 정보가 없는 경우가 전제」 → `mdm.process` 는 **MES 정본**이라 만드는 화면이 없으면 데이터가 생기지 않는다 ③ 화면명이 이미 「Routing(**공정**) 등록·관리」다 ④ `W-06-03` 의 `cause_code` 흡수 선례. ⛔ 모달이 아니라 탭인 이유 — 모달은 만들기만 되고 **수정·사용 중지 자리가 없어** `B-4` 를 쓸 곳이 사라진다. 사용자 확정 2026-09-03. |
| **v0.4** | **2026-08-06** | **DS 판정 정정 1건 — 순서 재배치는 갭이 아니다.** 고정 커밋 `a554d11` 설치본에서 `Table` 의 **`reorderable`·`onRowReorder` 실재**를 확인했고 DS 이슈 **#39 도 CLOSED** 다. `b`(또는 `c` 대체 구현) → **`a`**. 제약 둘을 함께 기록 — **`groupBy` 병용 불가** · 정렬 중 재배치 비활성. `onRowReorder` 주석이 **공유계약 A-5 와 일치**한다. 근거: `ds-gap.md` PR #97. |
| v0.3 | 2026-08-03 | **수정 라운드 1 — §3만 고치고 §4·§6이 남았던 파급을 마저 닫음.** §4-B Sequence 행을 「사용자 입력 필드」에서 「서버 채번 값」으로 정정(편집·유일 표기 조정) · §6 「유일 제약 위반 (Sequence)」 인라인 오류 행을 삭제하고 **「순서 일괄 치환 실패」**(서버 오류)로 대체 — §3의 목록 내 위치 표기와 이제 모순 없음. §9 후보 3의 A-5 대상 목록도 `code_value`→`inspection_item_spec`으로 정정(대상은 항상 3곳이었는데 구성이 틀려 있었다). |
| v0.2 | 2026-08-03 | **API 설계 과정에서 드러난 정정**(기준정보 API 스펙 트랙 Task 10). §3 레이아웃의 `Seq` 열 예시(10·20)를 **목록 내 위치**(1·2)로 정정하고, 표시 번호는 `operation_seq` 저장값이 아니라는 설명을 추가 — 공유계약 A-5 정정(대상 4곳→3곳)에 연동. |
| v0.1 | 2026-08-03 | 초안. 4면 근거 대조 완료(물리 모델 4테이블 + 제약 9건). **상류↔하류 불일치 3건 신규**(기본 Rev 플래그 부재 — BOM과 비대칭 · 외주 구분/L/T/UPH 부재 · `effective_from` 필수 여부) · **하류 선행 1건**(선후행 의존 테이블) · **미도출 화면 1건**(공정 마스터 귀속) · **DS 신규 갭 G-7**(행 순서 재배치 — `b`, 사용처 2곳 확인) · 공유 계약 후보 4건. |

출처: design/wiki/screens/06/W-06-01-Routing공정등록관리.md · 최종 대조일: 2026-08-25
