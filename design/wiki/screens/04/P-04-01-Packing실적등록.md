# P-04-01 · Packing(P&P) 실적 등록

> 작성일: 2026-08-07 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> ⭐ **매칭 스캔 화면이다.** 두 개를 스캔해 **같은 것인지 확인**하는 형태는 04에서 처음이다(`P-02-03` 오투입 검증의 대칭).
> **POP 1024×768** — E-1 세로 예산 검산 · E-2 긴 식별자 분절 적용.

## §0. 요약

- **개요**: POP(1024×768 터치+스캔)에서 물류 출하담당이 납품라벨과 생산LOT을 순서대로 스캔하면 서버가 「스캔한 둘이 서로 맞는가」를 판정하고, 포장 단위(카톤·팔레트 등)를 만들어 내용물을 배분하는 포장 실적 등록 화면. 라벨 출력은 `P-04-02`가 맡는다.
- **데이터**: 포장 단위(`inventory.handling_unit` — `parent_handling_unit_id` 자기참조로 계층 구성)와 포장 내용물(`inventory.handling_unit_content` — 포장·품목·LOT당 1행 유일)을 다루며, 출하 배분(`logistics.shipment_lot_allocation.handling_unit_id`, nullable)과 연결된다.
- **핵심 액션**: 납품라벨 스캔 → 생산LOT 스캔(서버가 배분과 대조해 매칭 판정) → 품목·LOT·수량 담기(같은 LOT 재스캔 시 수량을 합산) → 상위 포장 선택(선택 사항) → 포장 확정.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `P-04-01` · Packing(P&P) 실적 등록 |
| 프로그램 · 도메인 | **POP**(Electron 키오스크 · 1024×768) · 제품출하(04) |
| 유형 | **입력** |
| 행위자 | 물류 출하담당 |
| 신뢰도 | **확정** — 메인 M8 · 배지 M8 = POP |
| IA 위치 | POP > 출하 모드 > Packing |
| 범위 | **포장 실적 등록**(포장 단위 생성 + 내용물 배분). 라벨 출력은 `P-04-02` |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` — **WF04 S6** · **M8 매칭 스캔(납품라벨 ↔ 생산LOT)** · 포장 실적 · 고객 바코드 · **배지 M8 = POP** |
| **화면·IA** | 인벤토리 v1.7 `P-04-01` — 유형 입력 · 데이터=포장단위·납품라벨·생산LOT·포장구성 · 신뢰도 **확정** |
| **요구** | 공유계약 **E-1**(POP 화면 예산 · 슬랙 0) · **E-2**(긴 식별자 분절) · **E-3**(터치 규격) · **D-2**(스캔 규약) · **F-1**(단말 기능 구성) |
| **데이터** | `inventory.handling_unit`(**`parent_handling_unit_id` 자기참조** · `handling_unit_type_code` · `status_code`) · `handling_unit_content`(**`uq_handling_unit_content UNIQUE(HU, 품목, LOT)`** · `qty CHECK > 0`) · `logistics.shipment_lot_allocation`(**`handling_unit_id` nullable**) · `app.document_issue_log` — 전건 실측 |

> ⭐ **포장 단위가 자기참조 계층이다** — `parent_handling_unit_id`. 박스 안에 박스(팔레트 → 카톤)를 담을 수 있다.

## §3. 레이아웃 (POP 1024×768 — 세로 예산 검산)

```
┌ 헤더 64 ──────────────────────────────────────────────────────────────┐
│ Packing 실적 등록          SR-2026-0456 · B사        박출하  ●온       │
├ 본문 616 (패딩 16×2 제외 584) ────────────────────────────────────────┤
│ ┌ ① 납품라벨 스캔 · 88 ─────────────────────────────────────────────┐ │
│ │  [ 납품라벨 QR ]                    DL-2026-0455-001              │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│ ┌ ② 생산LOT 스캔 · 88 ──────────────────────────────────────────────┐ │
│ │  [ 생산LOT QR  ]     000123450 · 000001200 · 260731 · 000123 · 0007│ │
│ │                      ✅ 매칭 — 이 납품라벨의 LOT 이 맞습니다       │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│ ┌ ③ 포장 구성 · 320 ────────────────────────────────────────────────┐ │
│ │  포장 단위  CTN-2026-0091   유형 [ 카톤 ▾ ]                       │ │
│ │  ────────────────────────────────────────────────────────────      │ │
│ │  품목      LOT(분절 표시)                     수량                │ │
│ │  FG-1001   000123450 · 000001200 · …           120                │ │
│ │  FG-1001   000123450 · 000001180 · …            60                │ │
│ │                                          합계  180 / 180 ✅        │ │
│ │  상위 포장  [ (없음)          ▾ ]  ⓘ 팔레트에 담으면 지정         │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│ ┌ ④ 진행 · 88 ──────────────────────────────────────────────────────┐ │
│ │  이 출하  포장 2 / 예상 3        미포장 120 EA                     │ │
│ └───────────────────────────────────────────────────────────────────┘ │
├ 액션바 88 ────────────────────────────────────────────────────────────┤
│                            [ 다시 스캔 ]        [ 포장 확정 ]          │
└───────────────────────────────────────────────────────────────────────┘
```

### §3-1. 세로 예산 검산 — **E-1 슬랙 0**

```
헤더        64
본문       616   ← ① 88 + ② 88 + ③ 320 + ④ 88 = 584 + 패딩 16×2 = 616  ✅
액션바      88   ← 핵심 버튼 72 + 여백 16
──────────────
합계       768   슬랙 0
```

⚠ **③ 포장 구성이 320px 로 가장 크고 유일한 조정 여지다.** 행이 6줄을 넘으면 **③ 안에서 스크롤**한다 — 화면 전체를 스크롤하면 액션바가 밀려 E-1이 깨진다.

### §3-2. 가로 제약 — **E-2 를 적용한다**

파일럿 실측에서 **34자리 LOT 행이 597/599px(여유 2px)** 였다. 이 화면은 **LOT 열이 표 안에 들어가므로 더 좁다.**

**E-2 그대로 분절 표시한다** — 저장은 원문, 표시는 그룹핑. **붙여 쓰면 실물 라벨과 눈으로 대조가 불가능하다.**

## §4. 필드

### §4-A. 포장 단위 `inventory.handling_unit`

| 라벨(한/베) | 출처 컬럼 | 타입 | 필수 | 편집 | 검증·비고 |
| --- | --- | --- | :-: | :-: | --- |
| 포장 번호 | `handling_unit_no` | `business_no_t` | ✅ | 자동 | UNIQUE |
| **유형** | `handling_unit_type_code` | `code_t` | ✅ | ✅ | ⚠ 값 목록 미정 — 카톤·팔레트 등 |
| **상위 포장** | `parent_handling_unit_id` | bigint FK **자기참조** | — | ✅ | `ck_handling_unit_parent`: 자기 자신 불가 |
| 창고 · 위치 | `warehouse_id` · `location_id` | bigint FK | — | 자동 | nullable |
| 상태 | `status_code` | `code_t` | ✅ | — | ⚠ 값 목록 미정 |

### §4-B. 포장 내용물 `inventory.handling_unit_content`

| 라벨 | 출처 컬럼 | 검증 |
| --- | --- | --- |
| 품목 · LOT | `item_id` · `lot_id` | 둘 다 **NOT NULL** |
| 수량 | `qty` | `CHECK > 0` |
| — | — | ⭐ **`uq_handling_unit_content UNIQUE(HU, 품목, LOT)`** — 같은 포장에 같은 LOT 을 두 행으로 못 넣는다(§5-3) |

### §4-C. 출하 배분 연결 *(읽기·갱신)*

| 라벨 | 출처 컬럼 | 비고 |
| --- | --- | --- |
| 배분 | `shipment_lot_allocation.handling_unit_id` | ⚠ **nullable** — 포장하지 않는 출하도 있다 |

**감사 컬럼·`version_no` 제외**(A-4). `handling_unit` 에 `version_no` 가 있으므로 **B-1 적용** · `handling_unit_content` 는 **부여 형**(B-6 — `version_no` 없음).

## §5. 액션·상태

### §5-1. ⭐ 매칭 스캔 — **두 개를 대조한다**

`P-02-03`(자재 투입 오투입 검증)이 「스캔한 것이 지시와 맞는가」였다면, 이것은 **「스캔한 둘이 서로 맞는가」**다.

```
① 납품라벨 스캔  →  이 라벨이 어느 출하·어느 품목인지가 정해진다
② 생산LOT 스캔   →  그 LOT 이 ① 의 출하에 배분된 것인가
```

| 판정 | 화면 |
| --- | --- |
| 맞다 | ✅ 「매칭 — 이 납품라벨의 LOT 이 맞습니다」 |
| **다르다** | ⛔ **차단** — 「이 납품라벨은 FG-1002 용입니다」 + 다시 스캔 |
| 배분에 없는 LOT | ⛔ 「이 출하에 배분되지 않은 LOT 입니다」 |

⚠ **매칭 판정을 화면이 하지 않는다** — 배분(`shipment_lot_allocation`)을 서버가 보고 판정한다. 프런트가 목록을 받아 비교하면 **오프라인·캐시 상태에서 틀린다.**

### §5-2. 포장 단위는 **계층이다** — 박스 안에 박스

```
handling_unit.parent_handling_unit_id   자기참조
ck_handling_unit_parent                 자기 자신은 불가
```

| 층 | 예 |
| --- | --- |
| 최하위 | 카톤 — 내용물(품목·LOT·수량)을 직접 갖는다 |
| 상위 | 팔레트 — **카톤들을 담는다** |

**화면 규칙** — 이 화면은 **한 번에 한 포장**을 만들고, **상위 포장은 선택으로 지정**한다. 팔레트를 먼저 만들고 카톤을 담는 순서도, 카톤을 만들며 팔레트를 고르는 순서도 둘 다 된다.

⚠ **순환 참조는 `ck` 가 자기 자신만 막는다** — A → B → A 는 DB 가 막지 않는다. **화면이 상위 후보에서 자기 하위를 제외**한다.

⚠ **G-1(계층 목록·편집)이 걸린다** — 상위 포장 선택이 **2단 이내면 갭 불성립**이다(`W-06-07` §8-5 판정과 같은 기준). 팔레트–카톤 2단이면 `Select` 들여쓰기로 성립한다 → §8-4.

### §5-3. ⭐ 같은 LOT 을 두 번 담을 수 없다 — **합쳐서 넣는다**

```
uq_handling_unit_content   UNIQUE (handling_unit_id, item_id, lot_id)
```

같은 포장에 같은 LOT 을 **두 행으로 못 넣는다.** 현실에서는 「120 넣고 나중에 60 더 넣기」가 자연스러운데 **데이터는 180 한 행**을 요구한다.

**화면이 합친다** — 이미 있는 LOT 을 다시 스캔하면 **수량을 더한다.**

```
FG-…-0311  120  →  같은 LOT 재스캔 60  →  180 (한 행)
```

⚠ **더한 것인지 새로 넣은 것인지 화면이 말한다** — 「기존 120 에 60 을 더해 180 이 됩니다」. 조용히 합치면 **중복 스캔을 못 알아챈다.**

### §5-4. ⛔ 「고객 바코드」의 마스터가 보류 상태다

인벤토리 근거가 「고객 바코드」를 명시한다. **고객 바코드 체계 마스터는 v1.3에서 [가설] 단계로 보류**됐다(인벤토리 v1.3 잔여).

| 있는 것 | 없는 것 |
| --- | --- |
| `mdm.item_external_code` — 품목의 외부 코드 | **고객별 바코드 체계 규칙**(자릿수·구성) |

**A-11로 물러난다** — 납품라벨의 바코드는 **`P-04-02` 가 발행한 값을 그대로 읽고**, 형식 검증은 하지 않는다. 「이 고객은 13자리 EAN」 같은 규칙이 없기 때문이다.

⚠ **형식 검증을 안 하면 오스캔이 통과한다** — 그래서 §5-1의 **매칭 판정이 유일한 방어선**이다. 라벨 값 자체는 안 믿고 **그것이 가리키는 배분**을 본다.

### §5-5. 단말 기능 구성 — **F-1 이 이 화면을 여는가**

```
mdm.terminal_process   8플래그   can_input_material · can_input_result · … · can_return_material
```

⛔ **포장에 맞는 플래그가 없다**(§I 등재 · 이슈 **#77**). `can_input_result`(실적 입력)로 물러날 수 있으나 **의미가 다르다.**

**A-11로 물러난다** — 1차는 **`can_input_result` 로 게이팅**하고 그 사실을 적는다. 플래그 신설은 #77 회신에 달렸다.

⚠ **#77 회신이 「플래그를 추가할 일이 아니다」로 올 수도 있다** — 2026-07-28 설계검토가 모바일에 대해 그렇게 판정한 선례가 있다(`W-CO-06` §5-1). 그러면 **게이팅 자체를 두지 않는다.**

### §5-6. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 납품라벨 스캔 | 항상 — **D-2** |
| 생산LOT 스캔 | 납품라벨 스캔됨 |
| 직접 입력 | 항상 — **D-3 대체 경로 의무** |
| 수량 입력 | LOT 매칭됨 — **≤ 배분 잔여** · **숫자 키패드**(D-4) |
| 유형 선택 | 항상 — ⚠ 값 목록 미정 |
| 상위 포장 지정 | 상위 후보 존재 — **자기 하위 제외**(§5-2) |
| 다시 스캔 | 항상 — 마지막 스캔 취소 |
| **포장 확정** | 내용물 ≥ 1 AND 유형 선택됨 |
| 포장 해체 | ⛔ **두지 않는다** — 02 도메인의 되돌리기 계열(#78 코멘트) |
| 라벨 출력 | `P-04-02` 로 이동 — **소관 이동** |

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 납품라벨 ↔ LOT 불일치 | ⛔ **차단** — 「이 납품라벨은 FG-1002 용입니다」(§5-1) |
| 배분에 없는 LOT | ⛔ 「이 출하에 배분되지 않은 LOT 입니다」 |
| **같은 LOT 재스캔** | ⚠ **합친다** + 「기존 120 에 60 을 더해 180 이 됩니다」(§5-3) |
| 배분 잔여 초과 | ⛔ 「배분 180 을 넘을 수 없습니다」 |
| 상위 포장에 자기 하위 지정 | ⛔ 선택지에서 제외(§5-2) |
| 내용물 0개로 확정 | ⛔ 「포장에 담긴 것이 없습니다」 |
| 스캔 실패 | **직접 입력**으로 물러난다(D-3) |
| **③ 행이 6줄 초과** | ③ 구획 안에서 스크롤 — **화면 전체를 스크롤하지 않는다**(§3-1) |
| **오프라인** | ⛔ **온라인 전용** — 매칭 판정이 서버에 있다(C-6) |
| 낙관적 잠금 충돌 | **B-1** — `handling_unit.version_no` |

## §7. DS 매핑

> ✅ **판정 절차 ① 수행** — 고정 커밋 `a554d11` `src/components/` 전수 조회(29개).

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 스캔 입력 | `TextField` + 포커스 관리 | **c** |
| 매칭 결과 | `AlertBanner` | a — G-1 |
| 포장 구성 표 | `Table` + `TableColumn.render` | **c** |
| 수량 입력 | `TextField` + **숫자 키패드** | **d** — **G-6** |
| 유형·상위 포장 선택 | `Select` — 들여쓰기로 계층 | **c** — **G-1**(2단이면 불성립) |
| 진행 표시 | **`Progress`** — 포장 2/3 | a |
| 확정 버튼 | `Button` — ⚠ **72px 필요** | **b** — **G-5** |
| 상태 배지 | `Chip` | a |

**신규 DS 갭 0건.** ⚠ **G-5 가 이 화면의 실질 제약이다** — 액션바 88px 예산이 핵심 버튼 72px 를 전제하는데 DS 최대가 48px 다.

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ⛔ **포장에 맞는 단말 플래그가 없다** — `can_input_result` 로 물러난다(§5-5) | 상류↔하류 불일치 | **조정** | **#77** — ⚠ 「추가할 일이 아니다」로 올 수도 있다 |
| 2 | ⛔ **고객 바코드 체계 마스터 보류** — 형식 검증 불가(§5-4) | 회신 대기 | **조정** | 매칭 판정이 유일한 방어선 |
| 3 | **포장 유형·상태 값 목록** | 공통코드 | **조정** | `W-06-06` · #145 |
| 4 | **포장 계층 깊이** — 2단인가 그 이상인가 | 설계 결정 | **조정** | **G-1 성립 여부가 여기 달렸다**(§5-2) |
| 5 | ⛔ **순환 참조를 DB 가 막지 않는다** — 자기 자신만 막는다 | 상류↔하류 불일치 | **표시** | 화면이 상위 후보를 거른다 · `mdm.location` 과 같은 형태 |
| 6 | **G-5 터치 타겟** — 액션바 예산이 72px 를 전제한다 | DS | **조정** | **POP 확산 착수 전 필수**(E-3) |

**차단 0건 — 이 화면은 지금 지어도 된다.**

## §9. 이 장에서 나온 공유 계약 후보

1. ⭐ **매칭 판정은 서버가 한다 — 라벨 값 자체를 믿지 않는다** — 고객 바코드 체계 마스터가 없어 **형식 검증을 할 수 없으므로**, 방어선은 「라벨이 가리키는 배분이 실제로 있는가」 하나뿐이다. 프런트가 배분 목록을 받아 비교하면 캐시 상태에서 틀린다. **A-10 보강(계약이 판정 결과를 내려 준다)의 스캔 판** (§5-1·§5-4)
2. **유일 제약이 「합치라」고 말하는 곳에서는 화면이 합치되 합쳤다고 말한다** — `uq_handling_unit_content` 가 같은 LOT 두 행을 막으므로 재스캔은 수량을 더해야 한다. **조용히 합치면 중복 스캔을 못 알아챈다** — 「기존 120 에 60 을 더해 180」을 보인다. A-1(유일키 범위를 문구에 담는다)의 **입력 판** (§5-3)
3. **POP 화면은 어느 구획이 스크롤하는지를 스펙이 지정한다** — E-1 슬랙이 0이라 **화면 전체를 스크롤하면 액션바가 밀려 예산이 깨진다.** 「행이 넘치면 어디가 스크롤하는가」를 §3에 못박지 않으면 구현이 화면 스크롤을 택한다 (§3-1)

## 근거·이력

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-07 | 초안 — ⭐ **04의 첫 매칭 스캔 화면** — 「스캔한 둘이 서로 맞는가」이고, **고객 바코드 마스터가 보류라 형식 검증이 불가**해 **매칭 판정이 유일한 방어선**이다. ⭐ **포장 단위가 자기참조 계층**(팔레트 → 카톤)이고 **순환 참조를 DB 가 막지 않는다**(자기 자신만). ⭐ **`uq_handling_unit_content` 가 「합치라」고 말한다** — 재스캔은 수량을 더하되 **합쳤다고 말한다.** **E-1 세로 예산 검산 통과**(64+616+88=768 · 슬랙 0)이고 **③ 구획 안에서만 스크롤**한다. ⛔ 포장에 맞는 단말 플래그가 없어 `can_input_result` 로 물러난다(#77). 미결 6건(**차단 0**) · 계약 후보 3건 · 신규 DS 갭 0건. |

출처: uiux/2026-08-07-화면상세스펙-확대8차/P-04-01-Packing실적등록.md · 최종 대조일: 2026-08-25
