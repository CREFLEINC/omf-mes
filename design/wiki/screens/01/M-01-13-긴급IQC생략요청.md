# M-01-13 · 긴급 IQC 생략 요청

> 작성일: 2026-08-12 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **화면 완주(115) 뒤 처음 신설된 화면**이다 — **2026-08-12 팀 리더 결정**(`omf-mes#73`)으로 **116**이 됐다.
> ⭐ **데이터가 화면을 요구한 사례**다 — 「있으면 좋겠다」가 아니라 **`NOT NULL` 이 없으면 못 채운다**.

## §0. 요약

- **개요**: 모바일(PDA) 화면. 생산(현장)담당·물류담당이 입하돼 수입검사(IQC) 대기 중인 LOT에 대해 긴급 생략을 요청한다. 요청을 **만들기만** 하며, 승인·한도승인은 관리웹 화면(`W-01-02`)에서 권한자(MES 권한 보유 관리자)가 따로 처리한다.
- **주요 데이터**: 승인 요청(`app.approval_request`) — 공통 승인 워크플로우 테이블을 그대로 쓴다(신설 0). `target_type_code`=`INBOUND_LOT`·`approval_type_code`=`IQC_SKIP` 고정, `reason`(사유)이 **NOT NULL**이라 요청자가 반드시 적어야 한다.
- **핵심 액션**: LOT 스캔 → 사유 입력 → 요청 전송(대상 LOT·사유 둘 다 있어야 활성). 수량·한도 입력은 없고(한도는 승인자가 정한다), 취소·회수·수정 액션도 없다 — 잘못 올렸으면 다시 올린다.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `M-01-13` · 긴급 IQC 생략 요청 |
| 프로그램 · 도메인 | **모바일**(Capacitor Android PDA) · 01 자재창고관리 |
| 유형 | 입력 |
| 행위자 | 생산(현장)담당 · 물류담당 |
| 신뢰도 | 확정 (`#73` · 2026-08-12) |
| IA 위치 | 모바일 스캔 타일 > **🚨 긴급 요청** |

⚠ **결번 `M-01-03` 을 되살리지 않았다** — v1.2 에서 `W-01-01` 로 병합된 번호다. 되살리면 **과거 문서의 `M-01-03` 참조가 다른 것을 가리킨다.**

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | ✓확정 **§재정의 #8** — 「긴급 IQC 생략 = **작업자 요청** → **권한자 승인** → 한도승인」<br>✅확정 2026-07-27 — **승인/기록 매체 = 관리웹 · 권한자 = MES 권한 보유 관리자**<br>⛔ **요청 측 화면이 없었다** — `omf-mes#73`(2026-08-04 발행 · **2026-08-12 「만든다」로 확정**) | 실측 |
| **화면·IA** | `design/wiki/project-spec/04-통합-IA.md` **v2.2** — 화면 **116** · 모바일 **20** · 🚨 긴급 요청 타일 신설 | 실측 |
| **요구** | 위 확정 둘. ⚠ **REQ 직접 대응은 없다** — 확정 절차의 결손을 메우는 화면이다 | 실측 |
| **데이터** | ✅ **`app.approval_request`(SQL 2477) 그대로 쓴다 — 신설 0**<br>`approval_request_no`(2479 UNIQUE) · `approval_type_code`(2480) · **`target_type_code`·`target_id`**(2481·2482 · **다형 참조**) · **`requested_by` NOT NULL**(2483) · `requested_at`(2484) · `status_code`(2485) · ⭐ **`reason` text NOT NULL**(2486) | 실측 |

## §3. ⭐⭐ 이 화면이 **왜 반드시 있어야 하는가**

**「있으면 편하다」가 아니다. 데이터가 요구한다.**

```
app.approval_request.reason        text NOT NULL      (2486)   ← 사유 없이 행이 안 선다
app.approval_request.requested_by  NOT NULL FK        (2483)   ← 요청자를 적어야 한다
app.approval_step.approver_id      NOT NULL                    ← 승인자는 따로다
```

⛔ **승인 화면(`W-01-02`)에서 대신 적을 수 없다.** 대신 적으면 **`requested_by` 가 승인자가 되어** ✓확정 §재정의 #8 의 3단계(요청 → 승인 → 한도승인)가 **2단계로 무너진다.**

⭐ **`W-01-02` 스펙이 이미 그렇게 적어 뒀다** — §4 필드 표의 `reason` 행 입력 열이 **「입력(요청 화면)」**이다. **그 화면을 전제하고 쓰였는데 없었다.**

### §3-1. ⭐ 신설이 **0** 이다

`app.approval_request` 는 **공통 승인 워크플로우**(§J)가 이미 쓰는 테이블이고, `W-CO-09`(결재함)·`W-06-15`(결재선 정의)가 서 있다. **이 화면은 그 위에 요청 하나를 올릴 뿐**이다.

| 무엇 | |
| --- | :-: |
| 테이블 신설 | ⛔ **0** |
| 컬럼 신설 | ⛔ **0** |
| 계약 신설 | ⚠ **1** — `POST /trace/lots/{lotId}:request-iqc-skip`(01 계약 · 2026-08-29) §7-2 |

## §4. 레이아웃 (모바일 PDA · 1080×2160 · 스캔 퍼스트)

```
┌─────────────────────────────┐
│ ← 긴급 IQC 생략 요청          │
├─────────────────────────────┤
│                             │
│  ① 어느 자재인가             │
│  ┌───────────────────────┐  │
│  │ 📷 입하 LOT 스캔       │  │
│  └───────────────────────┘  │
│  LOT-2026-0812-014          │
│  PP 수지 ABC-100 · 500 kg   │
│  ⏳ 수입검사 대기 중          │
│                             │
│  ② 왜 급한가  *필수*         │
│  ┌───────────────────────┐  │
│  │                       │  │
│  │                       │  │
│  └───────────────────────┘  │
│                             │
│  ─────────────────────────  │
│  ⚠ 권한자 승인 후에 쓸 수    │
│     있습니다. 지금 바로       │
│     투입되지 않습니다.        │
│                             │
│      [    요청    ]         │
│                             │
├─────────────────────────────┤
│  내가 올린 요청               │
│  ● 대기  LOT-…-011  08-12   │
│  ✅ 승인  LOT-…-009  08-11   │
└─────────────────────────────┘
```

⭐ **아래 「내가 올린 요청」 구획이 있다** — 요청하고 나면 **결과를 어디서 보는지**가 필요하다. ⛔ 관리웹에 들어가 보라고 할 수 없다(현장은 관리웹을 안 쓴다).

## §5. 필드·규칙

### §5-A. 입력

| 라벨(한 / 베) | 출처 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| 대상 LOT | `target_id`(2482) | ✅ | **스캔 우선** · ⚠ **입하돼 검사 대기인 LOT 만** §6 |
| 대상 유형 | `target_type_code`(2481) | ✅ | **자동** — 값 `INBOUND_LOT` 고정 §9-1 |
| 요청 유형 | `approval_type_code`(2480) | ✅ | **자동** — 값 `IQC_SKIP` 고정 |
| ⭐ **사유** | **`reason`**(2486) | ✅ **NOT NULL** | ⛔ **비우면 저장이 안 된다** — 화면이 먼저 막는다 |
| 요청자 | `requested_by`(2483) | ✅ | **자동** — 사번 인증에서 |
| 요청 시각 | `requested_at`(2484) | ✅ | **자동** · ⚠ 오프라인이면 **단말 시계**(C-12) |

⛔ **수량·한도를 입력받지 않는다** — **한도는 승인자가 정한다**(`W-01-02` 가 「**한도승인**」이다). 요청자가 한도를 적으면 **승인의 뜻이 사라진다.**

### §5-1. 상태는 **요청자가 못 바꾼다**

`status_code`(2485)는 **승인 흐름이 정한다.** 이 화면은 **만들기만** 한다.

| 액션 | 있나 |
| --- | :-: |
| 요청 | ✅ |
| **취소·회수** | ⛔ **두지 않는다** — §8-2 |
| 수정 | ⛔ **없다** — 잘못 올렸으면 **다시 올린다** |

### §5-2. 액션·상태

| 액션 | 활성 조건 |
| --- | --- |
| 스캔 | 항상 |
| 요청 | **대상 LOT · 사유** 둘 다 |
| **내가 올린 요청 조회** | **진입 시 · 요청 성공 후 자동** — §4 아래 구획을 채운다. ⛔ **사번 헤더 없이 부르지 않는다**(§5-4) — 셸이 사번을 쥐고 있다(`M-CO-01`) |

**상태**: `초기` → `LOT 선택됨` → `사유 입력 중` → `전송 중` → **`요청됨`** / `대기(오프라인 큐)` / `실패`

### §5-3. ⭐ 화면이 **기대를 낮춘다**

```
⚠ 권한자 승인 후에 쓸 수 있습니다. 지금 바로 투입되지 않습니다.
```

⛔ **「긴급」이라는 이름이 「누르면 된다」로 읽힌다.** **G-3**(어떻게 풀 것인가를 담는다)의 역방향 — **무엇이 아직 안 되는지를 먼저 말한다.**

### §5-4. ⭐ 「내가 올린 요청」의 **「나」는 사번이다**

⛔ **모바일 셸에는 계정 로그인이 없다** — 단말 토큰 + 사번 귀속이다(`M-CO-01` §2 · 공유계약 **D-5**·**F-2**).
그래서 `requestedByMe=true` 만 보내면 서버가 **「나」를 풀 근거가 단말밖에 없고**, 한 단말을 여러
작업자가 **교대로** 쓰므로 **남이 올린 요청이 이 구획에 섞인다.**

| 셸 | 「나」를 무엇으로 푸나 |
| --- | --- |
| 관리웹(`W-CO-09` 결재함) | 계정 토큰 — 서버가 로그인 사용자에서 푼다 |
| **모바일**(이 화면) | **`X-Worker-No` 헤더의 사번** — 셸이 싣는다 |

⭐ **같은 규칙의 선례가 이미 계약에 있다** — 현장 진입 공지 조회
(`GET /app/notices?unacknowledgedByMe=true`)가 「현장 셸은 계정 세션이 없다 — 이 질의를 쓸 때는
`X-Worker-No` 로 주체를 싣는다」로 적어 두었다(요구서 공통 §3-11).

⛔ **사번이 아직 안 들어왔으면 이 조회를 부르지 않는다** — 헤더 없이 부르면 목록이 **조용히
남의 것으로 채워진다**(빈 목록이 아니라 «틀린 목록»이라 화면으로는 안 보인다).

## §6. 예외 · 오프라인

| 상황 | 처리 |
| --- | --- |
| 사유 미입력 | ⛔ 저장 거부 — `reason NOT NULL`(2486)을 **화면이 먼저 막는다** |
| 검사 대기가 아닌 LOT | ⚠ 「이미 검사가 끝난 자재입니다」(**G-3**) · ⛔ 요청 불가 |
| 같은 LOT 에 대기 중 요청이 있다 | ⚠ **막지 않고 알린다** — 「이미 요청이 올라가 있습니다(08-12 09:14)」 |
| 결재선이 없다 | ⛔⛔ **요청이 설 수 없다** — `approval_step.approver_id` 가 **`NOT NULL`** 이라 승인자가 정해져야 한다. 「결재선이 없어 요청할 수 없습니다. 전산담당에게 문의하세요」(**F-6**) |
| 통신 실패(요청) | **큐에 담는다** — 아래 |
| 통신 실패(**내가 올린 요청** 조회) | ⚠ **마지막으로 받은 목록을 그대로 두고** 「연결되면 갱신됩니다」를 붙인다. ⛔ **큐에 담지 않는다** — 조회는 담을 것이 없다. ⛔ **빈 목록으로 그리지 않는다** — 「요청이 없다」로 읽혀 **다시 올리게 만든다**(취소·회수가 없으므로 §8-2 · **C-7** 두 등급) |

### §6-1. 오프라인 — ⚠ **담되 「급하지 않게」 담긴다**

**C-14**(판정값을 쓰는가)로 판정한다.

| 물음 | 답 |
| --- | :-: |
| 판정값을 쓰는가 | ⛔ 아니다 — **요청을 만들 뿐** |
| 내가 만든 것이 남의 판정 근거인가(**C-15**) | ⚠ **그렇다** — 승인자가 이걸 보고 판정한다 |

⭐ **모순이 하나 있다 — 「긴급」인데 큐에 들어간다.** 화면이 그렇게 말한다.

```
⏳ 요청 대기 1건 — 연결되면 전송됩니다
   ⚠ 급하면 유선으로 함께 알리세요
```

⛔ **큐에 담긴 것을 「요청 완료」로 보이지 않는다**(**C-7** 두 등급). → §9-2

## §7. DS 매핑 (`2891ae8` v0.2.0 · 판정 절차 ①·② 수행 · 실재 30종)

| 요소 | 유형 | 컴포넌트 |
| --- | :-: | --- |
| LOT 스캔 | **a** | `SearchInput` |
| 대상 정보 | **a** | `Card` + `Chip`(검사 대기) |
| 사유 | **a** | `TextArea` |
| 요청 버튼 | **a** | `Button`(`lg` — 터치) |
| 기대 낮추기 안내 | **a** | `AlertBanner`(info) |
| 내가 올린 요청 | **a** | `Table` 또는 `Card` 목록 + `Chip`(대기/승인/반려) |
| 대기 큐 | **a** | `AlertBanner`(warning) |

**신규 갭 0건.**

⚠ **PDA 터치 타겟은 `lg`** — DS 에 PDA 프리셋이 없다(기존 갭).

### §7-2. 계약 — ⚠ **신설 1**

**`app-공통.json`(승인) 실물을 다시 열어 확인했다 — 조회는 있고 생성은 없었다(34경로 실측 · 2026-08-29).**

```
POST /trace/lots/{lotId}:request-iqc-skip     ← 요청 생성 (01 계약 · 2026-08-29 신설)
GET  /app/approval-requests?requestedByMe=…    ← 내가 올린 요청
     + X-Worker-No: <사번>                     ← 「나」를 푸는 근거 (2026-09-02 신설)
```

⛔ **조회만 있다 — 요청 «생성» 경로는 `app-공통.json` 에 없었다**(`/app/approval-requests` 는 `get` 하나뿐 · 34경로 실측). ⚠ 그래서 **01 계약에 `POST /trace/lots/{lotId}:request-iqc-skip` 을 신설했다**(2026-08-29) — 요구서 01 line 20 규약(「요청 생성은 각 도메인 계약의 상신 오퍼레이션이 만든다」)을 따른다.

⭐ **「내가 올린 요청」 필터는 이미 있다** — `GET /app/approval-requests` 의 **`requestedByMe`**(실물 확인). ⚠ **요청 생성 경로는 01 계약에 신설했다**(2026-08-29 · 아래).

⚠ **«(정합주: 필터는 있었으나 「나」를 실을 자리가 없었다.** `GET /app/approval-requests` 에 귀속 헤더
선언이 0건이었다 — **`X-Worker-No`(`WorkerNoOptional` 선택판)를 2026-09-02 신설**했다. 관리웹
결재함(`W-CO-09`)도 같은 오퍼레이션을 부르므로 필수판이 아니다. 근거: 구현팀 검토 요청
`omf-mes#335` · §5-4)»

## §8. 미결

| # | 항목 | 성격 | 처리 |
| :-: | --- | --- | --- |
| **1** | **`approval_type_code` 값 `IQC_SKIP`** · **`target_type_code` 값 `INBOUND_LOT`** | **공통코드** | **A-16 다형 참조 대응표**에 등재 · `W-06-06` ✅ **해소 2026-09-02** — `IQC_SKIP` 은 `approval_type_code` 8값에, `INBOUND_LOT` 은 `ApprovalTarget.targetTypeCode` 7값에 있다(`omf-mes#336` · 2026-09-01) |
| **2** | **요청 취소·회수를 두지 않는다** | **설계 결정** | ⭐ **G-24**(만들지 않기로 한 것). 잘못 올렸으면 **다시 올리고 승인자가 반려**한다. ⚠ 대기 요청이 쌓이면 그때 다시 본다 |
| **4** | **결재선이 없을 때** | **설계 결정** | ⛔ **요청 자체를 막는다**(§6). ⚠ **F-6** — 판정할 수 없으면 통과시키지 않는다 |
| **5** | **`status_code` 값 목록** | **공통코드** | `W-01-02` §8 과 같은 건 |

⛔ **차단 미결 0건.**

## §9. 이 장에서 나온 공유 계약 후보

### 9-1. ⭐⭐ **`NOT NULL` 이 화면의 존재를 요구할 수 있다**

지금까지 「데이터가 없어 화면을 못 만든다」는 여러 번 있었다. **이번은 반대다 — 데이터가 있어서 화면이 있어야 한다.**

| 신호 | 뜻 |
| --- | --- |
| `reason` **NOT NULL** | **사람이 적는 자리**가 있다 |
| `requested_by` ≠ `approver_id` | **두 사람**이 관여한다 |
| 둘을 곱하면 | ⭐ **화면이 둘이어야 한다** |

⚠ **승인 화면만 만들고 요청 화면을 빠뜨리면** `requested_by` 가 승인자로 채워지고 **절차가 조용히 2단계로 줄어든다.** 데이터는 통과한다 — **아무도 못 잡는다.**

⭐ **점검법 — 「이 `NOT NULL` 을 누가 채우나」를 사람으로 답해 본다.** 답이 **승인자**면 요청 화면이 빠진 것이다.

— `M-01-13` §3 · `W-01-02` §4·§5-3 · `omf-mes#73`

### 9-2. ⭐ **「긴급」이 붙은 화면은 오프라인에서 그 사실을 말한다**

**C-7**(저장 피드백 두 등급)의 **긴급 판**이다.

⛔ **「긴급 요청」이 큐에 담긴 것을 「요청됨」으로 보이면 안 된다** — 사람이 **기다리면 된다**고 오해하고, 실제로는 **아무에게도 안 갔다.**

```
⏳ 요청 대기 1건 — 연결되면 전송됩니다
   ⚠ 급하면 유선으로 함께 알리세요
```

⭐ **계약이 못 하는 것을 화면이 말한다** — 큐는 언제 나갈지 모른다.

— `M-01-13` §6-1 · C-7 · C-15

### 9-3. **요청 화면은 「한도」를 받지 않는다**

⭐ **승인이 「한도승인」이면 한도는 승인자가 정한다.** 요청자가 적으면 **승인이 「예/아니오」로 줄어든다.**

⚠ **요청 화면을 만들 때 「무엇까지 요청자가 정하나」를 먼저 가른다.**

— `M-01-13` §5-A · `W-01-02`

## 근거·이력

### §8 해소된 미결

> 원 §8 항목 번호를 그대로 유지한다. 아래 항목은 해소로 명시되어 이 절로 옮겨졌다.

| # | 항목 | 성격 | 처리 |
| :-: | --- | --- | --- |
| **3** | ~~「내가 올린 요청」 조회 필터~~ | ✅ **확인 완료** | **`requestedByMe`** 가 이미 있다(공통 계약 실물). ⛔ **계약 신설 0** ⚠ **«(정합주: 이 해소는 «반만» 맞았다 — 필터의 «존재»만 보고 그 필터가 «무엇으로 풀리는지»를 보지 않았다.** 모바일에는 계정 로그인이 없어 「나」를 실을 자리가 필요했고, 그 자리(`X-Worker-No`)가 계약에 **없었다**. **2026-09-02 신설 1건**(`omf-mes#335` · §5-4·§7-2). 「계약 신설 0」은 그때 표기 그대로 둔다)»** |

### 변경 이력

| 판 | 날짜 | 요지 |
| :-: | --- | --- |
| **v0.2** | **2026-09-02** | **「내가 올린 요청」의 «나»를 사번으로 못박았다 — 구현팀 검토 요청 `omf-mes#335`.** ⛔ **필터는 있었는데 「나」를 실을 자리가 없었다** — `GET /app/approval-requests` 의 세 질의(`assignedToMe`·`requestedByMe`·`myTurnOnly`)가 전부 「**로그인 사용자**」로 적혀 있었는데 **모바일 셸에는 계정 로그인이 없다**(`M-CO-01` §2 — 인벤토리 v1.4 정정으로 계정 로그인이 폐기되고 단말 토큰 + 사번 귀속이 됐다). 헤더 없이 부르면 서버가 **단말로 풀 수밖에 없고**, 한 단말을 여러 작업자가 교대로 쓰므로 **남이 올린 요청이 섞인다** — 빈 목록이 아니라 «틀린 목록»이라 화면으로는 안 보인다. **계약에 `X-Worker-No`(`WorkerNoOptional` 선택판) 1건 신설** — 관리웹 결재함(`W-CO-09`)도 같은 오퍼레이션을 부르므로 필수판이 아니다. ⭐ **규칙 자체는 이미 계약 안에 있었다** — 현장 진입 공지 조회(`GET /app/notices?unacknowledgedByMe=true`)가 같은 자리를 이미 그렇게 풀어 두었는데(요구서 공통 §3-11) **이 오퍼레이션에만 안 왔다.** 전 계약 스윕 — 「나」를 묻는 질의 **4개** 중 헤더가 있던 것은 그 하나뿐. ＋ §5-4 신설 · §5-2 액션 표에 「내가 올린 요청 조회」 행 추가(§4 가 그린 구획인데 액션 표에 없어 커버리지 검사기 둘 다 못 봤다) · 요구서 §3-26 에 같은 행 추가. **경로 불변 · 질의 불변 · 본문 불변 — 헤더만 는다.** |
| v0.1 | 2026-08-12 | 초안 — **화면 완주 뒤 처음 신설된 화면**(115 → 116 · `#73` 확정). ⭐⭐ **데이터가 화면을 요구했다** — `reason` **NOT NULL**(2486) + `requested_by` ≠ `approver_id`(2483)라 **승인 화면에서 대신 적을 수 없다**. `W-01-02` 가 이미 「입력(요청 화면)」이라 적어 뒀는데 그 화면이 없었다. ⭐⭐ **신설 0 — 테이블·컬럼·계약 경로 전부**. `app.approval_request` 와 공통 승인 계약(`POST /app/approval-requests` · `GET …?requestedByMe`)이 **그대로 받는다**. ⛔ **수량·한도를 받지 않는다**(한도는 승인자가 정한다 · §9-3). ⛔ **취소·회수 없음**(G-24). ⭐ **화면이 기대를 낮춘다** — 「지금 바로 투입되지 않습니다」. ⭐ **오프라인에서 「긴급인데 큐에 들어간다」를 말한다**(§9-2). ⚠ **결번 `M-01-03` 을 되살리지 않았다**. DS 신규 갭 0건. 계약 후보 3건. |

출처: design/wiki/screens/01/M-01-13-긴급IQC생략요청.md · 최종 대조일: 2026-08-25
