# P-CO-01 · 사번 경량 인증

> **POP 공통 개정(2026-09-08)**: 글꼴·버튼·입력·선택·키패드·목록 행과 터치 영역은 관리웹 기본 밀도(E-3)를 사용한다. 본문의 구 56~72px 수치와 확대 터치 판정은 변경 이력이며 구현 기준이 아니다. 모든 선택형 목록은 선택값 + [선택] 버튼과 로컬 검색·목록 스크롤·[페이지 위]·[페이지 아래] 팝업(G-34)을 사용한다.

## §0. 요약

- **개요**: POP 진입점 화면. 셸 선행 상태 A에서 단말 토큰을 붙여넣고 서버 검증·업무 준비를 마친다. 등록 후 상태 B에서 생산 작업자·현장 관리자가 사번을 입력해 **현재 작업자를 지정**한다. 사번은 인증이 아니라 귀속이며 계정 권한 판정을 하지 않는다(§5-1).
- **주요 데이터**: 검증된 단말 정보(§4-B), `mdm.worker`(사번·이름·소속·재직 상태 — **읽기 전용**), `mdm.worker_qualification`(현재 0건, 미사용 §5-5), `production.work_session_worker`. **상태 B는 사번을 서버에 저장하지 않는다** — 단말 메모리에만 있고 이후 요청 헤더(`X-Worker-No`)로 실린다. 상태 A의 적용 토큰은 OS 자격증명 저장소에 보관한다.
- **핵심 액션**: 숫자 키패드로 사번 입력 → 확인(존재·재직 확인) → 현재 작업자 지정 또는 교대 → 작업 화면으로 이동.

> 작성일: 2026-08-06 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> POP 22화면의 진입점. ⭐ **화면명이 설계와 어긋난다** — 이 화면은 **인증이 아니라 귀속**이다(§5-1).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `P-CO-01` · 사번 경량 인증 ⚠ **개칭 후보 — 「작업자 지정」**(§5-1) |
| 프로그램 · 도메인 | **POP**(1024×768 터치) · 공통(CO) |
| 유형 | **입력** |
| 행위자 | 생산 작업자 · 현장 관리자 |
| 신뢰도 | **확정** |
| IA 위치 | POP > 진입(셸 공통) |
| 범위 | **상태 A: 셸의 단말 등록·재등록·준비**, **상태 B: 사번 입력·확인·현재 작업자 지정·교대**. 사번 권한 판정 없음 |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §4 **WF02 S5** — 「사번 입력(`297:5395`)은 **`P-CO-01`로 단일화**」(v1.2 · 결정 16) · `P-02-01`은 **W/O 선택부터** · WF05 S2 점검자 귀속도 같은 모델 |
| **화면·IA** | 인벤토리 v1.5 `P-CO-01` — 유형 입력 · 데이터=`app_user`↔작업자·사번 · 신뢰도 **확정** · 「[06 현장 사번 경량 인증 병합]」 · **적대 검증 M3** 「3곳 중복 정의 → 셸 공통으로 단일화」 해소분 |
| **요구** | ✅**REQ-PR-0023** POP 로그인 생략 — 「작업 시작 전 **사번만** 입력」 · **D1 병기 확정**: 권한 통제와 **이원화**(관리 화면=계정 권한 / POP=사번 귀속) · **도용 리스크 수용** · 잔여 = 「POP 사번 인증과 권한의 정합(회신 **E-9** 연동)」 · ✓확정 2026-07-28 「POP 단말인증 설계검토」 **§3.1 3층 분리**·**§3.3 `X-Worker-No` 헤더**·**§6-④ 자격 검증 보류** |
| **데이터** | `mdm.worker`(`worker_no` **전역 UNIQUE** · `worker_name` · `status_code` · `is_active` · `app_user_id` **nullable**) · `mdm.worker_qualification`(**현재 0건** — §5-5) · `production.work_session_worker` — 실측 |

## §3. 레이아웃 (POP — 1024×768)

### §3-A. 미등록·재등록 패널 — 셸 선행 상태

기존 화면 ID 안에서 헤더와 등록 패널을 표시한다. 헤더에는 연결 상태·언어·로컬 기기 정보만 둔다. 본문은 「관리웹에서 복사한 단말 토큰을 붙여넣으세요」 안내, 토큰 입력란, [붙여넣기]·[지움]·[등록 확인]으로 구성한다. 카메라나 긴 토큰 수기 타이핑을 전제하지 않는다. 서버 검증 후 단말 코드·유형을 보여 [등록 적용]으로 설치 담당자가 확인한다. 준비 중에는 작업자 목록·공정 목록 수신 상태와 [준비 재시도]를 보인다.

등록 검증·준비 전에는 사번 입력, 업무 화면 이동, 서버 업무 조회·저장·발행을 열지 않는다. 화면을 떠나면 후보 토큰 입력란을 지운다.

### §3-B. 등록·준비 완료 — 작업자 지정

**E-1 세로 예산**: 헤더 64 + 본문 **704**(액션바 없음 — 확인은 키패드 옆에 둔다) = 768.

```
┌ 1024 ────────────────────────────────────────────────────────────────────┐
│ 오마이팩토리 MES        POP-L1 · A동 사출라인          ● 연결됨      64  │
├──────────────────────────────────┬───────────────────────────────────────┤
│ 《사번 입력》              512    │ 《현재 작업자》              512      │
│                                  │                                       │
│    사번                          │    ┌─────────────────────────────┐    │
│   ┌────────────────────────┐     │    │                             │    │
│   │      9 0 0 0 2 8       │     │    │        김 · 900028          │    │
│   └────────────────────────┘     │    │        사출 1반             │    │
│                                  │    │                             │    │
│   ┌─────┬─────┬─────┐            │    │   지정 08-06 07:02          │    │
│   │  1  │  2  │  3  │  72px      │    └─────────────────────────────┘    │
│   ├─────┼─────┼─────┤            │                                       │
│   │  4  │  5  │  6  │            │    ⓘ 이 단말에서 하는 기록은          │
│   ├─────┼─────┼─────┤            │      이 사번으로 남습니다             │
│   │  7  │  8  │  9  │            │                                       │
│   ├─────┼─────┼─────┤            │    ┌─────────────────────────────┐    │
│   │ ←   │  0  │ 지움│            │    │      교 대 (사번 바꾸기)    │ 72 │
│   └─────┴─────┴─────┘            │    └─────────────────────────────┘    │
│                                  │                                       │
│   ┌────────────────────────┐     │    ┌─────────────────────────────┐    │
│   │        확  인          │ 72  │    │      작업 화면으로  →       │ 72 │
│   └────────────────────────┘     │    └─────────────────────────────┘    │
└──────────────────────────────────┴───────────────────────────────────────┘
```

**터치 규격**(E-3): 키패드 72px · 확인·교대·이동 `Button`(`size="2xl"` 72px) · 간격 `--space-3`(12px). **핵심 조작만 있는 화면이라 여유가 크다.**

⚠ **비밀번호 칸이 없다.** 그것이 이 화면의 요점이다(§5-1).

## §4. 필드

### §4-A. 작업자 `mdm.worker` (읽기 — 조회용)

| 라벨(한/베) | 출처 컬럼 | 타입 | 검증·비고 |
| --- | --- | --- | --- |
| **사번** | `worker_no` | `code_t` = varchar(50) | ⚠ **DB 제약은 「빈 문자열 아님」뿐** — 형식은 전부 화면 책임 §5-2 |
| 이름 | `worker_name` | `name_t` | 확인 후 표시 |
| 소속 | `department_id` | bigint FK | 확인 후 표시 |
| 재직 상태 | `status_code` · `is_active` | `code_t` · boolean | ⛔ **비재직이면 거부** §6 |
| 계정 연결 | `app_user_id` | bigint FK **nullable** | ⚠ **이 화면은 쓰지 않는다** §5-3 |

**상태 B는 사번을 서버에 저장하지 않는다.** 사번은 **단말 메모리에만** 있고 이후 요청 헤더로 실린다(§5-4).

### §4-B. 단말 등록·셸 정보

| 라벨 | 출처 | 검증·보관 |
| --- | --- | --- |
| 후보 토큰 | 관리웹 발급 결과 복사·로컬 붙여넣기 | 서버 검증 전에는 적용하지 않음. 입력 변경 시 이전 검증 결과 폐기, 화면 이탈 시 입력란 지움 |
| 단말 ID·코드 | 자기 단말 조회의 `terminalId`·`terminalCode` | ID는 호출 식별자, 코드는 표시용. 코드에서 ID를 추측하지 않음 |
| 공장·단말 유형 | 자기 단말 조회의 `plantId`·`terminalTypeCode` | `POP`(고정 단말)만 적용 |
| 설비·설치 위치 | 자기 단말 조회의 `equipmentId`·`equipmentCode`·`equipmentName`·`locationId` | 선택 정보. 이름이 없으면 코드/ID 또는 미지정, 임의 명칭 생성 금지 |
| 적용 토큰·검증 단말 정보 | 검증 200 및 설치 담당자 코드 확인 | 토큰은 OS 자격증명 저장소, 단말 정보는 검증 결과 캐시 |
| 업무 준비 상태 | 기존 공장 작업자 캐시·자기 공정 목록 수신 결과 | 등록 성공과 준비 실패를 분리 표시 |

## §5. 액션·상태

### §5-1. ⭐ 이 화면은 **인증이 아니다** — 화면명을 고쳐야 한다

「POP 단말인증 설계검토」 **§3.1**이 3층으로 갈랐다:

| 층 | 질문 | 수단 | 성격 |
| --- | --- | --- | --- |
| **인증** | 이 요청이 우리가 설치한 단말에서 왔는가 | **단말 토큰** | **유일한 보안 경계** |
| **기능 구성** | 이 단말에 이 기능을 열어 둘 것인가 | `terminal_process` 8플래그 | 오조작 방지 |
| **귀속** | 누가 한 일로 기록할 것인가 | **사번** | **도용 리스크 수용** |

> 핵심은 **사번을 인증에 쓰지 않는 것**이다. … 사번을 인증으로 승격시키면 REQ-PR-0023이 요구한 「로그인 생략」을 우회로 되살리는 셈이 된다. (§3.1)

**상태 B의 사번 지정은 셋째 층만 한다.** 상태 A는 그 앞에서 셸의 단말 인증을 준비하는 등록 패널이며 사번 인증을 추가하는 것이 아니다.

**등록·재등록 흐름**(공유계약 F-4): 후보 토큰의 `sub`는 `terminalId` 의미다. 셸은 최초 자기 조회 주소를 구성할 때만 이를 읽으며, 아직 검증된 신원으로 보지 않는다. int64를 반올림하거나 코드에서 번호를 추측하지 않는다. 후보 토큰으로 `GET /mdm/terminals/{terminalId}`를 온라인 호출하고 서버가 서명·종류·세대·활성·만료를 검증한다. 200에서 단말 코드를 확인하고 `POP`(고정 단말) 유형일 때 적용한다. 이후 공장 작업자 목록과 `GET /mdm/terminals/{terminalId}/processes` 준비가 끝나야 상태 B로 간다. 자기 조회 둘은 사번 불요이며 다른 단말은 403, 무효 토큰은 401이다. 업무 화면은 토큰을 해독하지 않고 셸의 검증 정보를 받는다.

재발급은 이전 토큰을 즉시 무효화하므로 같은 검증·확인을 다시 거친다. 같은 단말·공장의 새 토큰은 기존 큐를 그대로 재전송하며 현재 작업자는 다시 지정한다. 다른 단말·공장으로 바꿀 때 미전송 기록이 있으면 적용을 차단하고 건수와 기존 단말을 안내한다. 큐를 비우는 우회 버튼은 없다. 큐가 없을 때만 확인 후 교체하고 기존 캐시·현재 작업자·작업 문맥을 초기화한다. 큐의 단말·공장·사번·본문·발생 시각·업무일자·멱등 키는 변경하거나 삭제하지 않는다.

⚠ **「인증」이라는 이름이 실제 위험을 만든다.** 개발자가 화면명을 보고 만들 것들 —

```
⛔ 비밀번호 칸        ⛔ 로그인 실패 횟수 잠금        ⛔ 세션 만료 후 재로그인
```

**셋 다 REQ-PR-0023을 되돌리는 것**이고, 셋 다 **`app_user`에 담을 컬럼이 없다**(§I-3 — `password_hash`·`failed_login_count` 전무). 이름 하나가 없는 기능을 요구하게 만든다.

→ **개칭 후보: 「작업자 지정」.** 화면명은 인벤토리 소관이므로 **우리가 고칠 수 있다** — `[uiux→docs]` 이슈가 아니라 인벤토리 개정 건이다(§8-1).

### §5-2. 사번 형식은 **전부 화면 책임**이다

```
worker_no  app.code_t = varchar(50) CHECK (VALUE <> '')      ← 이것이 전부다
```

**실물 형식은 6자리 숫자**다 — ERP 사원 마스터 `EMP_CODE` 대표값 `900028`, 출문증 실물 신청자 사번 `903677`. 둘 다 `90****` 형태다(설계검토 §3.3 실측).

**A-9 등급을 나눈다**:

| 검증 | 등급 | 화면 동작 |
| --- | :-: | --- |
| 빈 값 아님 | ⓐ **DB가 막는다** | 확인 버튼 비활성 |
| **6자리 숫자** | ⓑ **화면이 막는다** | ⚠ **경고만** — 자릿수를 강제하지 않는다 §아래 |
| 실재·재직 | ⓑ 서버가 막는다 | 조회 실패 시 오류 |

⚠ **6자리를 강제하지 않는 이유**: 실물 786건의 **대표값 2개만** 확인했다. 전수가 아니다. 강제했다가 5자리 사번이 하나라도 있으면 **그 사람이 POP을 못 쓴다.** 자릿수가 다르면 경고를 띄우되 **확인은 눌리게** 한다.

`M-01-02`의 34자리 LOT과 같은 형태다 — **DB에 제약이 없다고 규칙이 없는 게 아니다.** 다만 이쪽은 **확신의 정도가 낮아 등급을 한 단계 낮춘다.**

⚠ **«(정합주: 이 등급이 «자릿수»에만 걸려 있었다 — 「숫자 전용」은 판정된 적이 없다.**
그런데 §3 의 **숫자 키패드**는 문자에 대해 **강제**다(문자키가 없으므로). 근거의 강도는
자릿수와 **같은데**(표본 2/786) 등급이 다르다 — 자릿수는 경고, 숫자는 불가능이다.
⛔ **계약에는 `pattern` 을 두지 않는다** — 계약이 막으면 화면보다 **더 강하게** 막게 되고,
다른 형식의 사번이 하나라도 있으면 그 사람은 **경고조차 못 보고 400 을 받는다.**
⚠ 문자 사번의 **반례는 0건**이라 지금 키패드를 바꾸지 않는다. 반례가 나오면 §8-6 과 함께
**문자 입력 수단**(`omf-mes#277` 트랙)이 필요하다. 근거: 구현팀 검토 요청 `omf-mes#367`)»

### §5-3. `app_user_id`는 쓰지 않는다 — **작업자 ≠ 입력자**

`mdm.worker.app_user_id`는 **nullable**이고 「작업자↔입력자 구분」 주석이 달려 있다.

**POP은 작업자만 다룬다.** 사번으로 찾은 `worker_id`가 실적의 귀속 주체이고, 계정 연결 여부와 무관하다. **현장 작업자 대부분은 계정이 없다.**

⚠ **계정이 연결돼 있다고 권한을 주지 않는다.** 그렇게 하면 F-2의 두 축(사용자×기능 / 단말×공정)이 섞인다 — `W-CO-02` §9-5가 경고한 것과 같은 실수다.

### §5-4. 세션이 아니다 — **매 요청에 사번을 싣는다**

설계검토 §3.3:

```http
POST /api/pop/production-results
Authorization: Bearer <단말 토큰>       ← 인증
X-Worker-No: 900028                     ← 귀속. 인증 아님
Idempotency-Key: <UUID>
```

**서버가 사번 세션을 갖지 않는다.** 단말이 「현재 작업자」를 보유하고 매 쓰기 요청에 붙인다.

| | |
| --- | --- |
| 읽기 요청 | **사번 불요** — 단말 토큰만으로 기준정보를 읽는다(설계검토 §3.6) |
| 쓰기 요청 | **사번 필수** — 없으면 서버가 거부 |

**⛔ 그래서 「언제 지우나」가 화면 문제가 된다.** 사번이 남아 있으면 **다음 사람이 그 사번으로 기록된다.**

| 시점 | 확정 유무 | 이 화면의 처리 |
| --- | :-: | --- |
| 교대 시 | — | **「교대」 버튼**으로 사람이 바꾼다 |
| 유휴 시간 경과 | ⛔ **규약 없음** | §8-2 — **지우지 않는다**(작업 중 화면을 떠나는 일이 흔하다) |
| 단말 재시작 | ⛔ 규약 없음 | **지운다** — 메모리 보유이므로 자연히 사라진다 |
| 근무 종료 | ⛔ 규약 없음 | 사람이 「교대」를 눌러야 한다 |

**화면은 현재 사번을 항상 크게 보인다**(§3 우측). 이것이 「내가 아닌 사람으로 기록되는 것」을 막는 **유일한 장치**다 — 도용 리스크를 수용한 설계에서 화면이 할 수 있는 최선이다.

→ **§D 신규 조항 후보** §9-1.

### §5-5. 자격 검증은 **보류다** — 화면이 그것을 지어내지 않는다

설계검토 **§6-④**(2026-07-28 사용자 판단: **기준정보 API 완료 후 재개**):

- 요구는 있다 — **FR-WO-022**(작업 시작 시 「작업자가 해당 공정을 수행할 권한이 있는지」) · **FR-QM-014**(검사자 자격 없으면 입력 제한)
- 데이터도 있다 — `mdm.worker_qualification`(유형·공정·유효기간)
- **⛔ 지금 켜면 현장이 선다** — 고객이 정리해 넣어야 채워지는 데이터이고 **현재 0건**이다. 강제하면 **전원이 무자격**이 된다
- 재개 시 형태 = `app.operation_policy`에 `WORKER_QUALIFICATION_ENFORCEMENT` = `BLOCK`|`WARN`|`OFF`(기본) — QA #9의 3단계 설정형과 같은 꼴

**이 화면은 존재·재직만 확인한다.** 자격 표시도 하지 않는다 — 0건인 데이터를 보이면 「자격 없음」이 전원에게 뜬다.

⚠ **다만 자리는 비워 둔다.** 재개되면 확인 직후에 경고가 붙을 자리다(§3 우측 카드 아래).

### §5-6. 오프라인 — 작업자 목록을 **미리 받아 둔다**

**C-5 판정: 오프라인 허용.** POP은 오프라인 내성이 요구된다(결정 17 · QA #32).

**C-11(선행 데이터 명시)**:

| 미리 받을 것 | 왜 |
| --- | --- |
| **해당 공장 재직 작업자 목록**(`worker_no`·`worker_name`·`department`·`is_active`) | 오프라인에서 사번을 확인하려면 목록이 있어야 한다 |

**C-6(판정값은 캐시 금지)에 걸리지 않는다** — 재직 여부는 **판정이 아니라 표시**다. 오프라인 중 퇴사한 사람의 사번이 통과해도, 그 기록은 복구 시 서버가 받고 **어차피 귀속은 사후 확인 대상**이다(도용 리스크 수용).

⚠ **자격 검증이 재개되면 판정값이 된다** — 그때는 C-6이 걸린다. `worker_qualification.valid_to`는 **캐시하면 안 되는 값**이다. §8-3에 남긴다.

**단말 토큰은 장기라 오프라인 구간을 넘어 산다**(설계검토 §3.5) — 사람 토큰(8시간)이었다면 복구 시점에 outbox가 통째로 401이 났다. **이 화면이 그 이점의 근거다.**

### §5-7. F-1이 걸리지 않는다

8플래그 어디에도 「사번 입력」이 없다. **이 화면은 기능 구성의 대상이 아니다** — 단말에 붙어 있으면 누구나 자기 사번을 넣을 수 있어야 한다.

`W-CO-06` §5-1의 정정과 같은 맥락이다: **매트릭스는 보안 경계가 아니라 기능 구성**이므로, 진입 화면을 막는 데 쓰지 않는다.

### §5-8. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 토큰 입력·붙여넣기·지움 | 상태 A — 로컬 입력 |
| 등록 확인·검증 재시도 | 상태 A · 후보 토큰 있음 · 온라인 |
| 등록 적용 | 서버 자기 조회 200으로 검증한 바로 그 후보 토큰 · 단말 코드 확인 · POP(고정 단말) 유형 · 다른 신원으로 교체 시 미전송 0건 |
| 준비 재시도 | 등록 검증 완료 · 업무 준비 실패 · 온라인 |
| 재등록 | 등록 정보 교체 필요 시 — 큐 보존·신원 확인은 §5-1 |
| 연결 상태·언어·로컬 기기 정보 | 항상 — 등록 전에는 로컬 정보만 |
| 숫자 입력·지움·백스페이스 | 상태 B · 업무 준비 완료 |
| **확인** | 상태 B · 입력 길이 ≥ 1 — ⚠ **자릿수는 강제하지 않는다**(§5-2) |
| **교대(사번 바꾸기)** | 상태 B · 업무 준비 완료 · 현재 작업자가 있을 때 |
| **작업 화면으로 →** | 상태 B · 업무 준비 완료 · 현재 작업자가 있을 때 |
| 단말 정보 표시 | 서버 검증 후 — 헤더에 단말 코드·설치 위치, 빈 선택 정보는 미지정 |

## §6. 예외·오프라인

| 상황 | 처리 |
| --- | --- |
| 사번 없음(미등록) | ⛔ 「등록되지 않은 사번입니다 (900028)」 — **입력값을 문구에 담는다**(오타 확인용) |
| **비재직**(`is_active=false` 또는 `status_code` 퇴사) | ⛔ 「현재 재직 중이 아닌 사번입니다. 관리자에게 문의하세요」 |
| 자릿수가 6이 아님 | ⚠ **경고만** — 확인은 눌린다(§5-2) |
| 다른 공장 사번 | ⚠ **막지 않는다** — `worker_no`가 전역 유일이라 조회는 된다. **⛔ 다른 공장 소속임을 표시**한다 §8-4 |
| 단말 토큰 만료·폐기·위조·비활성 또는 없는 단말(401) | ⛔ 상태 A로 안내하고 업무를 막는다. 단순 네트워크 실패와 달라 기존 캐시로 업무를 재개하지 않는다 |
| 다른 단말 조회(403)·잘못된 단말 유형 | ⛔ 등록 적용 안 함. 관리자에게 올바른 등록 정보를 요청하도록 안내 |
| 등록 중 네트워크 실패 | 등록 성공으로 보지 않고 온라인 검증 재시도 제공 |
| 등록 검증 성공·준비 실패 | 등록 완료와 업무 준비 실패를 구분하여 준비 재시도 제공. 업무 화면 진입 안 함 |
| 다른 단말·공장 재등록 + 미전송 기록 | ⛔ 적용 차단·건수와 기존 단말 표시. 기록 삭제·재귀속 금지 |
| **오프라인** | ✅ 이미 검증·준비된 같은 단말의 기존 사번 확인·큐 동작만 유지(§5-6). 신규 등록은 온라인 전용. 헤더에 `● 오프라인` 표시 |
| 오프라인 + 목록 미수신 | ⛔ 「작업자 목록을 아직 받지 못했습니다. 연결된 상태에서 한 번 실행해야 합니다」 — **C-11** |
| 교대 중 미전송 큐 존재 | ⚠ **경고**: 「보내지 못한 기록 3건이 이전 사번으로 남아 있습니다」 — **큐의 사번을 바꾸지 않는다**(B-3 이력 불변) |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 사번 표시 필드 | `TextField`(읽기 전용 · 큰 글자) | a — **읽기 전용 표시라 터치 등급 대상이 아니다.** 높이는 `size="xl"`(60픽셀) — 터치 규격 조항(E-3)의 「56~60픽셀 급은 `xl`」에 따른다. ⚠ 「큰 글자」는 **글자 크기 요구이지 높이 요구가 아니다** — 둘을 섞지 않는다 |
| **숫자 키패드** | *(없음)* | **d** — **G-6** §7-1 |
| 현재 작업자 카드 | `Card` | a |
| 확인·교대·이동 버튼 | `Button`(`size="2xl"` — 72픽셀) | a — 디자인 시스템 기본 **안**이다(고정 커밋 `8c4a775`) |
| 연결 상태 표시 | `Chip` | a |
| 오류·경고 | `AlertBanner` | a — G-1 |

**신규 DS 갭 0건.**

### §7-1. G-6 판정 — 사번용·수량용은 **한 부품**이다

`ds-gap.md` **G-6 「화면 내장 숫자 입력 패드」**는 **d**(새 원시 요소)로 등재돼 있고 「POP 확산 시 재판정」이 붙어 있다.

| 사용처 | 화면 |
| --- | --- |
| 1 | `P-02-04` 작업실적 등록 |
| 2 | `P-02-13` PQC 검사 수량 |
| **3** | **`P-CO-01` 사번 입력** ← 이 장 |

**세 곳이 아니라 18곳이다** — POP 7 · 모바일 11(2026-08-28 전 화면 실측). 「실적·수량 입력 = 필드 옆·팝업」이라던 위치 축은 근거가 없다 — `P-02-04`의 키패드는 우측 단에 고정된 블록이고, 전 화면에서 팝업으로 그린 곳이 0건이다. **갈리는 축은 값 형식 · 빠른 입력 · 키 크기 셋**이고, 상세는 숫자 키패드 조항(D-4)이 정본으로 담는다(내용을 복사하지 않는다).

**판정 — 한 부품이다. 두 형태가 아니라 속성 셋이다.** 답은 숫자 키패드 조항(D-4)이 정본으로 담는다.

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ⭐ **화면명이 설계와 어긋난다** — 「인증」이 아니라 「귀속」이다. 이름 때문에 없는 기능(비밀번호·잠금)을 만들 위험(§5-1) | 설계 결정 | **조정** | **인벤토리 개정 — 「작업자 지정」** |
| 2 | **사번을 언제 지우나** — 유휴·근무 종료 규약이 없다(§5-4) | 설계 결정 | **조정** | 지우지 않기로 시작 · 현장 확인 |
| 3 | **자격 검증 재개 시 C-6이 걸린다** — `worker_qualification.valid_to`는 캐시 금지 대상(§5-5·§5-6) | 설계 결정 | **표시** | 설계검토 §6-④ 재개 시 |
| 4 | **다른 공장 사번의 처리** — `worker_no`가 전역 유일이라 조회가 된다(§6) | 설계 결정 | **표시** | 표시만 하고 막지 않음 ✅ **해소 2026-09-02** — §6 이 「⚠ 막지 않는다 · ⛔ 다른 공장 소속임을 표시한다」로 확정했다(공유계약 F-2 귀속 축과 정합) |
| 5 | **G-6 세 번째 사용처** — 사번용과 수량용이 한 컴포넌트인지(§7-1) ✅ **해소 2026-08-28** | DS | **확정** | **한 부품 확정** — 숫자 키패드 조항(D-4)이 정본 · `omf-mes#243` |
| 6 | **사번 실물 형식을 전수 확인하지 않았다** — 786건 중 2건만 봤다(§5-2) | 회신 대기 | **표시** | 자릿수 강제하지 않음 ⚠ **«(정합주: 축이 «둘»이다 — 자릿수와 «숫자 전용».** 자릿수는 경고로 두었으나 숫자 전용은 **숫자 키패드가 이미 강제**하고 있다(§5-2 정합주). 계약에는 `pattern` 을 두지 않았다(`omf-mes#367` · 2026-09-02). 전수 확인이 오면 **두 축을 함께** 닫는다 · `M-CO-01` §8-**8** 과 같은 건)»** ⚠ **2026-09-04 표지 재판정**(`omf-mes#378` 3회차) — **이 표지 `omf-mes#367` 은 닫혔고(2026-09-02) 그 결말이 이 미결을 닫지 않는다.** 그 닫는 코멘트는 「이 이슈 번호로 찾을 수 있다」를 근거로 닫았는데, **닫힌 이슈는 찾아와도 아무도 없다.** ⛔ **살아 있는 표지가 없다.** ⚠ 전수 확인의 원천은 ERP 사원 마스터 `T_ERP_EMP_TIG_DOWN.EMP_CODE`(786행)이고 이 저장소에는 **대표 1건**만 있다(2026-09-04 실측) — 우리가 여기서 전수를 셀 수 없다. ⚠ `M-CO-01` §8 미결 8 과 **같은 건**이다. |
| 7 | **회신 E-9**(권한 값 목록) — D1 잔여 「POP 사번 인증과 권한의 정합」 | 회신 대기 | **표시** | POP은 권한 판정을 안 하므로 이 화면은 무관 |
| 8 | **업무 대상별 공정 공급** — 셸이 자기 공정 목록 전체를 공급하는 것과 개별 업무의 실제 공정 원천은 다르다 | 설계 결정 | **조정** | 요청 #536의 ②-b는 열어 둔다. 0행은 창고 단말에서 정상이며 조회 실패와 구분한다. 전역 첫 행 선택·플래그 OR 합산·임의 공정 선택으로 대신하지 않는다. 등록 전 인증 차단과 등록 후 액션 판정은 별개이며 화면 이동 후보 기준 G-34는 바꾸지 않는다. |

**차단 0건 — 이 화면은 지금 지어도 된다.**

## §9. 이 장에서 나온 공유 계약 후보

1. **귀속 정보는 세션이 아니라 「단말이 들고 매 요청에 싣는 값」이다 — 화면이 그것을 항상 보인다** — 서버가 사번 세션을 갖지 않고 `X-Worker-No` 헤더로 매 쓰기에 실린다(설계검토 §3.3). 그래서 **「언제 지우나」가 화면 문제**가 된다. 도용 리스크를 수용한 설계에서 **현재 귀속 대상을 크게 상시 표시하는 것이 유일한 방어**다. POP·모바일 전 화면 공통 (§5-4)
2. **DB 제약이 없고 실물 표본도 적으면 검증 등급을 한 단계 낮춘다** — `worker_no`는 `varchar(50)`에 「빈 문자열 아님」뿐이고 실물은 786건 중 **2건만** 확인됐다. `M-01-02`의 34자리 LOT은 강제했지만 **이쪽은 경고에 그친다.** A-9에 「**표본의 크기가 등급을 정한다**」를 더한다 (§5-2)
3. **오프라인 캐시 판정에서 「판정값」과 「표시값」을 가르는 기준은 「틀렸을 때 무엇이 일어나는가」다** — 재직 여부는 캐시해도 된다(틀려도 사후 확인 대상). 자격 유효기간은 안 된다(틀리면 무자격자가 검사한다). **C-6에 이 판별 기준을 적는다** (§5-6)

## 근거·이력

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-06 | 초안. ⭐ **화면명이 설계와 어긋남을 발견** — 2026-07-28 설계검토 §3.1의 3층 분리(인증=단말 토큰 / 기능 구성=매트릭스 / **귀속=사번**)에서 이 화면은 **셋째 층만** 한다. 「인증」이라는 이름이 **비밀번호·실패 잠금·세션 만료**를 요구하게 만들고 셋 다 REQ-PR-0023을 되돌리며 담을 컬럼도 없다(§I-3) → **「작업자 지정」 개칭 후보**. 사번 형식은 실물 표본이 2건뿐이라 **강제하지 않고 경고**로 물러남(A-9 등급 하향). **세션이 아니라 매 요청 헤더**이므로 「언제 지우나」가 화면 문제가 됨을 정리. 자격 검증은 **보류**(§6-④ · `worker_qualification` 0건)라 지어내지 않되 자리는 비워 둠. C-11로 **작업자 목록 선행 수신** 명시. **G-6 세 번째 사용처** 도달(사번용 ↔ 수량용 형태 차이 기록). 미결 7건(**차단 0**) · 계약 후보 3건 · 신규 DS 갭 0건. |
| **v0.3** | **2026-09-02** | **사번 형식 등급의 «축이 둘»임을 밝혔다 — 구현팀 검토 요청 `omf-mes#367`.** §5-2 의 A-9 등급은 **자릿수**에만 걸려 있었고 「**숫자 전용**」은 한 번도 판정된 적이 없는데, §3 의 **숫자 키패드가 이미 강제**하고 있었다 — 문자키가 없으므로. 근거의 강도는 자릿수와 **같다**(표본 2/786)는데 등급이 다르다(자릿수 = 경고 · 숫자 = 불가능). ⛔ **계약에 `pattern` 을 두지 않기로 못박았다** — 계약이 막으면 화면보다 **강해져** 다른 형식의 사번이 **경고조차 못 보고 400** 을 받는다. ⚠ 문자 사번의 **반례가 0건**이라 키패드는 바꾸지 않는다 — 반례가 나오면 **문자 입력 수단**(`omf-mes#277` 트랙)이 함께 필요하다. §8-**6** 의 처리 칸에 두 축을 함께 적었다(`M-CO-01` §8-8 과 같은 건). **차단 0건 유지 · 계약 신설 0.** |
| v0.2 | 2026-08-28 | 터치 규격이 버튼 쪽에서 해소됐다(고정 커밋 상향 2026-08-27). 부품 매핑의 존재하지 않는 크기 값을 바로잡았다. 숫자 키패드는 **한 부품**으로 판정하고 답을 숫자 키패드 조항(D-4)에 두었다. 미결 5 해소, 잔여 0. |

출처: design/wiki/screens/공통/P-CO-01-사번경량인증.md · 최종 대조일: 2026-08-25
