# W-CO-02 · 사용자·역할·권한 관리

> 작성일: 2026-08-03 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 나머지 12개 마스터가 전부 「누가 편집할 수 있나」를 정의해야 하는데 그 원천이 이 화면이다. 빠지면 같은 가정을 12번 적게 된다(`공유계약.md` §H 확산 순서).

## §0. 요약

- **개요**: 관리웹에서 시스템 관리자·운영자·전산담당이 사용자·역할·데이터 접근범위·기능 권한을 관리하는 화면. 탭 2개(사용자/역할)로 나뉘고, 사용자에 역할을 붙이는 일과 역할에 기능 권한을 붙이는 일을 분리해 편집한다.
- **주요 데이터**: 마스터 `app.app_user`·`app.role`(폼 저장 + 낙관적 잠금, 물리 삭제 없음) / 부여·회수 3종 `app.user_role`·`app.user_data_scope`·`app.role_permission`(행 INSERT/DELETE, `is_active` 없음 — 즉시 반영).
- **핵심 액션**: 사용자·역할 추가 · 기본 정보 저장/취소 · 역할 체크·해제(즉시 반영) · 데이터 접근범위 추가/삭제(즉시 반영) · 기능 권한 체크·해제(⛔ 현재 비활성 — 회신 E-9 대기) · 변경 이력 조회.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-CO-02` · 사용자·역할·권한 관리 |
| 프로그램 · 도메인 | **관리웹** · 공통(CO) — 근거는 06 기준정보연계 S9 |
| 유형 | 설정·마스터 |
| 행위자 | 시스템 관리자 · 운영자 · 전산담당 |
| 신뢰도 | **확정** |
| IA 위치 | 시스템/공통 > 계정·권한 > 사용자·역할·권한 관리 |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | WF06 **S9** 「사용자 권한 관리(자료·화면·법인 접근 통제 + 현장 사번 경량 인증)」 — Action=「사용자별 **메뉴·데이터·법인** 단위 접근 권한 부여/회수」 · Rule=「권한 통제가 전 단계(S1~S8) 마스터 접근의 **횡단 선행조건**」 · 단말=Desktop(관리웹) | 실측 |
| **화면·IA** | `04` §3 `W-CO-02` 행 · 인벤토리 v1.4 — 주요 데이터=사용자/역할/권한·`role_permission`·`user_data_scope`·부서 · **[06 사용자·권한 관리 병합]** · 도식스펙06 §2.3 사이드 C **B11 권한부여회수(Desktop)** + MES-9 | 실측 |
| **요구** | ✅**REQ-PR-0015** 사용자별 접근 권한 분리 — ✓확정 **D1**(2026-07-01) 권한 마스터 MES 편입 · **이원화**: 관리 화면=계정 권한 / POP=사번 귀속 · 파생=권한 목록에 「긴급 W/O 발행 권한」 등재 필요(EWO #1)<br>**잔여**: 회신 **E-9** 사용자 유형/권한/접근 범위 정의(양측 워크숍·고객 액션) · 권한 입도(메뉴·데이터·법인) · **마스터별 편집 권한 매트릭스**(QA #34 연계) | 실측 |
| **데이터** | `app.app_user`(SQL 229~) · `app.role`(244~) · `app.role_permission`(258~) · `app.user_role`(267~) · `app.user_data_scope`(277~ + 부분 유일 인덱스 288~) · `mdm.department`(213~) | 실측 |

**⚠ 이 화면은 POP·모바일의 사번 인증을 다루지 않는다.** WF06 S9는 두 채널을 함께 서술하지만(B11 권한부여회수 / B12 사번입력), 도식스펙06 §2.3이 「**이원화**」로 갈라 두었고 화면도 갈라져 있다 — 사번 인증은 `P-CO-01`·`M-CO-01`. 이 화면은 **B11 채널만** 담당한다.

## §3. 레이아웃 (관리웹 — 구조 기술)

탭 2개. **사용자 축과 역할 축은 편집 단위가 다르므로 한 화면에 섞지 않는다** — 사용자에 역할을 붙이는 일과, 역할에 기능 권한을 붙이는 일은 다른 사람이 다른 빈도로 한다.

```
┌ PageHeader  시스템 > 계정·권한 > 사용자·역할·권한 관리                     ┐
│  Tabs:  ● 사용자   ○ 역할                                                  │
├──────────────────┬─────────────────────────────────────────────────────────┤
│ 좌 (약 1/3)      │ 우 (약 2/3)                                              │
│                  │                                                          │
│ [검색] [부서 ▾]  │  《기본 정보》 — §4-A                                    │
│ [상태 ▾]         │     로그인ID · 이름 · 부서 · 이메일 · 상태 · 사용        │
│ [ ] 미사용 포함  │                              [변경 이력]  [취소] [저장]  │
│                  │  ─────────────────────────────────────────────────────  │
│ ┌ 사용자 목록 ─┐ │  《역할 배정》 — §4-C   (부여·회수 — 즉시 반영)          │
│ │ ID 이름 부서 │ │     ☑ 품질담당   ☑ 전산담당   ☐ 생산관리자 …            │
│ │ ...(선택)    │ │                                                          │
│ └──────────────┘ │  《데이터 접근범위》 — §4-D  (부여·회수 — 즉시 반영)      │
│                  │   ┌ 목록 ────────────────────────────────────────────┐  │
│  [＋ 사용자 추가]│   │ 사업부      공장        [삭제]                   │  │
│                  │   │ VINA        (전체)      ⊖                        │  │
│                  │   │ (전체)      P1 1공장    ⊖                        │  │
│                  │   └──────────────────────────────────────────────────┘  │
│                  │   [＋ 접근범위 추가]                                     │
└──────────────────┴─────────────────────────────────────────────────────────┘

《역할》 탭 — 같은 좌우 2단
  좌: 역할 목록(코드·명·사용)          [＋ 역할 추가]
  우: 기본 정보(§4-B) + 《기능 권한》(§4-E · 부여·회수 — 즉시 반영)
      ⚠ 기능 권한 목록은 회신 E-9 대기 → 비활성 + 사유 표시(공유계약 G-2)
```

**⚠ 「기본 정보」와 「부여·회수」는 저장 방식이 다르다.** 기본 정보는 `version_no`가 있어 폼 저장 + 낙관적 잠금(B-1)이고, 역할 배정·접근범위·기능 권한은 **행 추가/삭제가 곧 반영**이다(아래 §4 머리). 한 화면 안에서 저장 규칙이 갈리므로 **시각적으로 구획을 나누고 각 구획에 저장 UI를 따로 둔다.**

## §4. 필드

감사 컬럼과 `version_no`는 표에서 제외(공유계약 A-4).

> **⚠ 이 화면의 5개 테이블은 두 종류다 — 실측.**
>
> | 종류 | 테이블 | 컬럼 구성 | 저장 방식 |
> | --- | --- | --- | --- |
> | **마스터** | `app_user` · `role` | `is_active` · `updated_at` · `version_no` **있음** | 폼 저장 + 낙관적 잠금(B-1) · 물리 삭제 금지(B-4) |
> | **부여·회수** | `role_permission` · `user_role` · `user_data_scope` | `created_at`·`created_by`**만** — `is_active`·`updated_at`·`version_no` **없음** | **행 INSERT/DELETE** |
>
> 부여·회수 테이블에 `is_active`가 없다는 것은 **비활성 개념 자체가 없다**는 뜻이다. 「권한을 회수한다」는 행을 지우는 것이고, 그 기록은 `audit.audit_event`가 남긴다(§8-1). **공유계약 B-4의 「물리 삭제 금지」를 여기 적용하면 안 된다** → §9-1.

### §4-A. 사용자 `app.app_user`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 로그인 ID / *(미정)* | `login_id` | varchar(100) | ✅ | ✅ **(전역)** | 중복 시 「이미 사용 중인 로그인 ID입니다」 · **유일 범위가 전역**이므로 문구에 범위를 넣지 않는다(A-1) | ⚠ §8-5 |
| 이름 / *(미정)* | `user_name` | `name_t` varchar(200) | ✅ | | 공백만 불가 | ✅ |
| 부서 / *(미정)* | `department_id` | FK → `mdm.department` | — | | 선택 목록 · **계층**(`parent_department_id` 자기참조) — §7-2 | ✅ |
| 이메일 / *(미정)* | `email` | varchar(200) | — | | 형식 검증은 **화면 책임** — DB에 제약 없음 ⚠ | ✅ |
| 상태 / *(미정)* | `status_code` | `code_t` 기본 `'ACTIVE'` | ✅ | | 공통코드 — **값 목록 미정 §8-4** · `is_active`와의 역할 분담 미정 **§8-4** | ✅ |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | 끄면 로그인 불가 · 물리 삭제 없음(B-4) | ✅ |

**✅ 비밀번호 관리자 초기화를 되살렸다**(2026-08-30) — DR-002 2-B ③ 「관리자 초기화」. `POST /app/users/{appUserId}:reset-password`가 임시 비밀번호를 생성해 한 번만 보인다(§8-2). `password_hash`·`last_login_at`·`failed_login_count`는 `W-CO-01`이 이미 계약에 정의해 뒀다.

### §4-B. 역할 `app.role`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 역할코드 / *(미정)* | `role_code` | `code_t` varchar(50) | ✅ | ✅ **(전역)** | 「이미 사용 중인 역할코드입니다」 | ⚠ §8-5 |
| 역할명 / *(미정)* | `role_name` | `name_t` varchar(200) | ✅ | | 공백만 불가 | ✅ |
| 설명 / *(미정)* | `description` | text | — | | | ✅ |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | | ✅ |

### §4-C. 역할 배정 `app.user_role` — 부여·회수

| 라벨 | 출처 컬럼 | 타입 | 비고 |
| --- | --- | --- | --- |
| 사용자 | `app_user_id` | FK → `app.app_user` | 좌측 선택 사용자로 고정 |
| 역할 | `role_id` | FK → `app.role` | `uq_user_role(app_user_id, role_id)` — **한 사용자에게 같은 역할 두 번 부여 불가** |

체크박스 목록으로 표현한다. 체크=INSERT, 해제=DELETE. **역할은 다중 부여가 정상**이다(테이블이 1:N).

### §4-D. 데이터 접근범위 `app.user_data_scope` — 부여·회수

| 라벨 | 출처 컬럼 | 타입 | 필수 | 검증·비고 |
| --- | --- | --- | :-: | --- |
| 사용자 | `app_user_id` | FK → `app.app_user` | ✅ | 고정 |
| 사업부 | `business_unit_id` | FK → `mdm.business_unit` | **조건부** | 아래 |
| 공장 | `plant_id` | FK → `mdm.plant` | **조건부** | 아래 |

- `ck_user_data_scope_target` — **둘 중 하나 이상은 반드시 있어야 한다.** 둘 다 비우면 저장 불가 → 「사업부와 공장 중 최소 하나는 지정해야 합니다」(A-2 조건부 필수)
- `uq_user_data_scope` = **부분 유일 인덱스** `(app_user_id, COALESCE(business_unit_id,0), COALESCE(plant_id,0))` — 비어 있는 축을 `0`으로 접어 유일 판정한다. 즉 **「사업부 VINA · 공장 전체」를 두 번 넣을 수 없다**
- 비어 있는 축의 사용자 표기는 **`(전체)`**. 「없음」이 아니라 「제한 없음」이라는 뜻이므로 빈칸으로 두면 오해한다 → §9-2

### §4-E. 기능 권한 `app.role_permission` — 부여·회수

| 라벨 | 출처 컬럼 | 타입 | 비고 |
| --- | --- | --- | --- |
| 역할 | `role_id` | FK → `app.role` | 고정 |
| 권한코드 | `permission_code` | `code_t` varchar(50) | `uq_role_permission(role_id, permission_code)` |

**⚠ `permission_code`는 코드 목록이 어디에도 없다.** 공통코드 그룹으로 관리되는지, 애플리케이션 상수인지 하류에 근거가 없고, 목록 자체가 **회신 E-9 대기**다. → **공유계약 G-2 적용**: 필드를 감추지 않고 **비활성 + 사유 표시**한다.

```
기능 권한  [ 선택지 준비 중 ]  ⓘ 권한 목록은 고객 회신(E-9) 대기 중입니다
```

## §5. 액션·상태

### §5-1. 액션

| 액션 | 위치 | 활성 조건 | 비고 |
| --- | --- | --- | --- |
| 사용자 추가 / 역할 추가 | 좌측 하단 | 항상 | |
| 저장 / 취소 | 기본 정보 폼 | 변경 있음 (+검증 통과) | **기본 정보 구획에만 있다** |
| 사용 중지 | 기본 정보 폼 | 사용 중일 때 | 물리 삭제 없음(B-4) |
| **비밀번호 초기화** | 기본 정보 폼 | 항상 | **되살림**(2026-08-30) — DR-002 2-B ③ · 임시 비밀번호 1회 표시 §8-2 |
| 역할 체크·해제 | 역할 배정 | 항상 | **즉시 반영** — 저장 버튼 없음 |
| 접근범위 추가 / ⊖ | 데이터 접근범위 | 항상 / 행 존재 | **즉시 반영** |
| 기능 권한 체크·해제 | 역할 탭 | ⛔ **비활성**(E-9 대기) | G-2 사유 표시 |
| 변경 이력 | 기본 정보 폼 | 저장 이력 있음 | `audit.audit_event` 조회 — §8-1 |

**⚠ 「즉시 반영」은 되돌리기가 없다는 뜻이 아니다.** 해제도 한 번의 조작이므로 되돌릴 수 있다. 다만 **자기 자신의 역할을 해제하는 경우**는 예외 — §6.

### §5-2. 화면 상태

`초기(미선택 — EmptyState)` → `조회` → `편집(변경 있음)` → `저장 중(버튼 비활성)` → `저장 성공(Toast)` / `검증 실패(필드 인라인)` / `충돌(B-1)` / `권한 없음(AlertBanner)`

### §5-3. 낙관적 잠금

`app_user`·`role`에 `version_no`가 있으므로 **B-1을 그대로 적용**한다. 부여·회수 3종에는 `version_no`가 없어 해당 없음 — 대신 유일 제약 위반이 경합 신호가 된다(§6).

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 권한 없음 | 이 화면 자체가 권한 관리 화면이므로 **진입 자체를 막고 배너로 사유 표시**(G-1 화면 수준 → `AlertBanner`) |
| 유일 제약 위반 (`login_id`·`role_code`) | 필드 인라인. **blur 선검사 + 저장 시 서버 재검증**(A-1) |
| 조건부 필수 위반 (접근범위 둘 다 빔) | 두 필드에 함께 인라인(A-2) |
| 접근범위 중복 | 「이미 같은 범위가 부여돼 있습니다」 — `uq_user_data_scope`의 **`(전체)` 접힘 규칙을 문구에 담는다**(A-1) |
| 역할 중복 부여 경합 | 두 관리자가 동시에 같은 역할을 체크 → 서버가 유일 위반으로 거부 → **이미 반영된 상태이므로 오류가 아니라 최신화**로 처리(체크 상태 유지 + 조용히 갱신) |
| **자기 자신의 권한 회수** | 자신에게서 관리자 역할을 해제하면 **다음 조작부터 이 화면에 못 들어온다.** 확인 다이얼로그로 결과를 명시 — §8-6 |
| **마지막 관리자 제거** | 시스템에 관리 권한 보유자가 0이 되는 조작 — **차단 여부 미결 §8-6** |
| 참조 중인 역할 사용 중지 | 배정된 사용자가 있는 역할 → 확인 다이얼로그 + 배정 건수 표시 |
| 오프라인 | **해당 없음** — 관리웹은 온라인 전제(결정 17) |

## §7. DS 매핑 (`@crefle/web-ui` v0.1.0)

### §7-1. 그대로 쓰는 것 (a) · 조합 (c)

| 용도 | 컴포넌트 | 유형 |
| --- | --- | :-: |
| 페이지 틀 | `AppShell` · `PageHeader` · `Breadcrumb` | a |
| 탭 | `Tabs` | a |
| 사용자·역할 목록 | `Table` (`density="compact"` · `sort` · `selectable`) | a |
| 검색·필터 바 | `SearchInput` + `Select` + `Chip` | **c** (G-2 기등재) |
| 입력 폼 | `TextField` · `Select` · `Switch` | a |
| 역할 배정·기능 권한 | `Checkbox` 목록 | a |
| 접근범위 행 편집 | `Table` + `TableColumn.render`(Select) + `IconButton` | **c** (편집 그리드 — 기등재) |
| 변경 이력 | `Dialog` + `Table` | **c** (G-3 기등재) |
| 알림·오류 | `Toast` · `AlertBanner` | a |
| 빈 상태 · 로딩 | `EmptyState` · `Skeleton` | a |

### §7-2. 갭 — 부서 계층 선택 (기등재 **G-1** 세 번째 사용처)

`department_id` 선택에 **`mdm.department`의 `parent_department_id` 자기참조 계층**이 걸린다 — 실측(SQL 217). `ds-gap.md` **G-1「계층 구조 목록·선택」**의 예고된 사용처가 **확인**됐다.

| 사용처 | 상태 |
| --- | --- |
| `mdm.location` (`W-06-07`) | ✅ 확인 |
| `mdm.department` (**이 화면** + `W-06-06`) | ✅ **확인 — 이 장에서 확정** |
| `mdm.production_line` | 미확인(귀속 화면 미정) |

**단, 이 화면의 요구는 「선택」뿐이고 「편집」이 아니다** — 부서 트리를 만드는 곳은 `W-06-06`이다. 선택만이면 `Select`에 들여쓰기 라벨(`— 하위부서`)로도 성립하므로 **이 화면 단독으로는 갭이 아니다**(c). 갭 판정은 **편집이 필요한 `W-06-06` 쪽에서** 내린다. → `ds-gap.md` 갱신.

### §7-3. `MatrixGrid` — 역할×권한 매트릭스의 자리 *(2026-08-03 추가 — `W-06-02` §7-4 실측)*

DS에 **`MatrixGrid`**(`columns`·`rows`·`groupHeaders`·`summaryRow`·`renderCell`·`onCellClick` · `MatrixStatus` 6종)가 실재한다 — DS 저장소 직접 대조.

**용도**: 역할이 여럿일 때 「어느 역할이 어느 권한을 갖는가」를 **역할 × 권한코드 교차표**로 한 번에 보고 셀 클릭으로 토글한다. `groupHeaders`가 있어 권한을 **메뉴 그룹으로 묶는** 표현도 된다(WF06 S9의 「메뉴·데이터·법인 단위」 중 메뉴 축).

**판정: `a`(그대로 있다).** 다만 **지금은 쓰지 않는다** — `permission_code` 목록이 회신 E-9 대기라 열을 만들 수 없다(§4-E · G-2). **목록이 오면 §3의 역할 탭에 매트릭스 뷰를 추가**한다.

### §7-4. 신규 갭 없음

이 화면에서 새로 발견한 DS 갭은 **0건**이다.

## §8. 미결

| # | 항목 | 성격 | 처리 |
| --- | --- | --- | --- |
| **2** | ✅ **되살렸다**(2026-08-30) — `POST :reset-password` 신설(DR-002 2-B ③). 인증 컬럼 3종은 `W-CO-01`이 이미 계약에 정의했다 | — | **해소** — 비밀번호 초기화 액션 활성 |
| **3** | **「법인 단위」 접근 권한을 표현할 컬럼이 없다** — WF06 S9 Action은 「메뉴·데이터·**법인** 단위」이고 Exception ③이 「다법인(비나) 데이터 격리」인데, `user_data_scope`에는 `business_unit_id`·`plant_id`뿐이고 `legal_entity_id`가 없다(실측) | 상류↔하류 불일치 | 법인 단위 부여는 **그 법인 소속 사업부를 전부 나열**하는 방식으로만 가능하고, **법인에 사업부가 추가되면 자동 반영되지 않는다.** 운영 중 격리 구멍이 된다 → **`[docs→데이터모델]` 이슈 후보**(assignee 필수) |
| 4 | `status_code`와 `is_active`의 역할 분담 · `status_code` 값 목록 | 공통코드 | 두 컬럼이 겹친다(둘 다 「쓸 수 있나」를 표현). 값 목록은 `W-06-06` 소관 · 분담은 설계 결정 |
| 5 | **코드 필드(`login_id`·`role_code`) 수정 허용** | 규칙 | 공유계약 **B-4 확정 규칙 적용** — 참조 건수 0일 때만. **단 `login_id`의 「참조」가 무엇인지가 불명**하다(FK가 아니라 `created_by`류 bigint로 흩어져 있고 **FK를 의도적으로 걸지 않았다** — SQL 210~211 주석). 참조 건수를 셀 수 없으면 규칙이 성립하지 않는다 → §9-4 |
| **6** | **자기 자신·마지막 관리자의 권한 회수 차단 여부** | 설계 결정 | 차단하면 안전하나 「관리자 1인 체제에서 역할을 갈아끼우는」 정상 조작도 막힌다. 결정권자 확인 |
| 7 | **`permission_code` 목록** (기능 권한의 입도 — 메뉴 단위? 조작 단위?) | 회신 대기 | **회신 E-9**. G-2 적용(비활성+사유). 파생 요구 「긴급 W/O 발행 권한」(EWO #1)이 이 목록에 들어가야 한다 |
| 8 | **마스터별 편집 권한 매트릭스** | 회신 대기 | 회신 E-9 · QA #34 연계. **나머지 12화면이 전부 이것을 참조**한다 — 각 화면 §6 「권한 없음」은 이 매트릭스가 정해질 때까지 「비활성+사유」로만 쓴다 |

## §9. 이 장에서 나온 공유 계약 후보

1. **부여·회수 테이블은 물리 삭제가 정상이다 — B-4의 예외 축을 세워야 한다.**
   `role_permission`·`user_role`·`user_data_scope`에는 `is_active`·`updated_at`·`version_no`가 **없다**(실측). B-4 「물리 삭제 금지·`is_active`로만」은 **마스터 테이블 규칙**이며, 이 형태에 적용하면 존재하지 않는 컬럼을 요구하게 된다. **구분 기준을 계약에 명시**: `version_no`·`is_active`를 가진 것 = 마스터(B-4 적용) / `created_at`·`created_by`만 가진 것 = 부여·회수(INSERT/DELETE + `audit.audit_event`).
2. **부분 유일 인덱스(`COALESCE`)의 사용자 표기·오류 문구 규칙.**
   `uq_user_data_scope`·`uq_item_external_code`·`uq_worker_qualification`·`uq_lot_external_identifier`·`uq_inventory_balance_dim`·`uq_putaway_rule`·`uq_shipment_lot_allocation`·`uq_operation_policy`·`uq_numbering_rule`이 모두 `COALESCE(...,0)`으로 NULL을 접어 유일 판정한다(실측 9곳). 화면은 ① 빈 축을 **`(전체)`**로 표기하고 ② 중복 오류 문구에 **접힘 규칙을 담아야** 한다. A-1의 「유일키 범위를 문구에 담는다」를 이 형태로 확장.
3. **변경 이력의 정본은 `audit.audit_event`다** — B-4 ⬜를 닫는 근거(§8-1). 「변경 이력」 버튼은 전 마스터가 **같은 조회 규약**(`target_type_code` + `target_id`)을 쓴다. 화면마다 다른 이력 테이블을 찾지 않는다.
4. **B-4 「참조 건수 0일 때만 코드 수정」의 적용 한계.**
   `created_by`/`updated_by`는 **의도적으로 FK를 걸지 않았다**(SQL 210~211 주석 — 대량 적재 성능). 따라서 사용자 코드의 참조 건수는 DB가 세어 줄 수 없다. **참조 건수를 셀 수 있는 대상과 없는 대상을 계약이 구분**해야 한다.
5. **권한 게이팅은 축이 둘이고, 관리웹은 한 축만 쓴다.**
   F-1은 `terminal_process` 8플래그(**단말 × 공정**)로 POP·모바일을 게이팅한다. 관리웹은 `role_permission.permission_code`(**사용자 × 기능**)다. **13화면은 전부 관리웹이라 F-1이 걸리지 않는다** — F-1을 관리웹 화면에 적으려다 축을 섞지 않도록 계약에 명시.

## 근거·이력

### §8 해소된 미결

| # | 항목 | 성격 | 처리 |
| --- | --- | --- | --- |
| **1** | **`audit.audit_event`가 범용 이력 테이블이다 — 실측 확인** (SQL 2661: `target_type_code`·`target_id`·`event_type_code`·`before_value jsonb`·`after_value jsonb`·`reason`·`performed_by`·`occurred_at` 파티션) | ~~데이터~~ | ✅ **해소.** 공유계약 **B-4 「변경 이력」 ⬜**과 `W-06-07` §8-4가 「범용 이력 테이블이 확인되지 않음」으로 남겨둔 항목인데, **실재한다.** 확인 범위 누락이었고 `[docs→데이터모델]` 이슈는 **불요** → §9-3 |

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-03 | 초안. 4면 근거 대조 완료(물리 모델 5테이블 컬럼 전건 + 제약 6건). **B-4 변경 이력 미결 해소**(`audit.audit_event` 실재 확인) · 상류↔하류 불일치 2건 신규(인증 수단 부재 · 법인 축 부재) · DS 신규 갭 0건(G-1 세 번째 사용처 확인) · 공유 계약 후보 5건. |

출처: design/wiki/screens/공통/W-CO-02-사용자역할권한관리.md · 최종 대조일: 2026-08-25
