# M-CO-01 · 기기 등록·사번 인증

## §0. 요약

- **개요**: 모바일 셸 진입 화면. 미등록 상태(상태 A)에서 관리웹이 발급한 등록 QR로 기기를 등록(토큰 주입)하고, 등록 후(상태 B)에는 사번을 입력해 작업자를 단말에 귀속시킨다. 화면이 열거하는 네 가지 기능(기기 등록·사번 입력·스캐너 준비 상태 표시·언어 선택) 중 서버에 저장할 것은 기기 등록 하나뿐인데, 그 저장 컬럼(`token_version`)이 없다(§5-2).
- **주요 데이터**: `mdm.terminal`(읽기 전용 — 토큰 클레임에서 옴) · `mdm.worker`(읽기 전용 — 조회만, 저장 없음). 언어·스캐너 준비 상태·현재 사번·단말 토큰은 서버 저장 없이 단말 로컬/단말 내장(어댑터)/앱 메모리/Keystore에 둔다(§4-C).
- **핵심 액션**: QR 스캔(기기 등록, 온라인 전용) → 사번 숫자 입력·확인(오프라인 허용) → 작업 목록으로 이동. 부가: 교대(사번 재입력)·언어 전환·등록 해제(오버플로 메뉴).

> 작성일: 2026-08-06 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 모바일 19화면의 진입점. ⭐ **네 가지를 하는데 서버에 저장할 것은 하나뿐이고, 그 하나가 없다**(§5-2).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `M-CO-01` · 기기 등록·사번 인증 ⚠ **개칭 후보 — 「기기 등록·작업자 지정」**(`P-CO-01` §5-1과 같은 이유) |
| 프로그램 · 도메인 | **모바일**(360×720dp · Android **13+** PDA) · 공통(CO) |
| 유형 | **입력** |
| 행위자 | 입하·자재·창고·출하 담당자 |
| 신뢰도 | **추정** — 도식 앵커 밖. 인증 방식은 확정(v1.4 정정) |
| IA 위치 | 모바일 > 진입(셸 공통) |
| 범위 | ① **기기 등록**(단말 토큰 주입) ② **사번 입력** ③ 스캐너 준비 상태 표시 ④ 언어 선택 |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` — **도식 앵커 없음**. 05 B1 사번입력(`749:7621`)이 이 화면으로 귀속(보류결정-확정기록) · 배지단말 **#8 오프라인 단일 단말 축퇴**(통신 두절 시 입·출측 스캔을 단일 단말로) |
| **화면·IA** | 인벤토리 **v1.4 정정** — 「계정 로그인」 **폐기** → **단말 토큰 + 사번 귀속**. 근거였던 결정 16 원문 단말표 **모바일 행에 인증 서술이 없다**(117~119행 실측). 말단 확정 = **POP인증 §6-③**(✓확정 2026-07-28) · 신뢰도는 **추정 유지**(도식 앵커 유무로 매기므로) |
| **요구** | ✓확정 2026-07-28 「POP 단말인증 설계검토」 **§6-③ 「모바일 = POP과 동일 — 단말 토큰 + 사번 귀속, 만료 1년」** · 근거 4가지: `TERMINAL_TYPE`에 **`MOBILE` 기존재** · 하드웨어 「**배치 지점당 1식**」 8식(**공용 장비**) · **오프라인 시나리오 2** 대상 · WF05 S2 사번 귀속 · ✅**REQ-PR-0012** 한/베 지원 · ✓설계확정 **결정 16**(Capacitor Android)·**결정 17**(오프라인 내성) |
| **데이터** | `mdm.terminal`(⛔ **`token_version` 없음** · ⛔ **기기 식별자 컬럼 없음**) · `mdm.worker` · ⛔ **스캐너 준비 상태 0건**(일체형이라 페어링 대상 자체가 없다 §5-4) · ⛔ **사용자 환경설정 0건** — 전 파일 검색(실측) |

## §3. 레이아웃 — 360×720dp

### §3-1. 상태 A — **미등록**(토큰 없음 · 최초 1회)

```
360 × 720 dp
┌ 앱바 56 ─────────────────────────────┐
│    오마이팩토리 MES        [한국어 ▾] │
├──────────────────────────────────────┤
│ 본문 664                              │
│                                       │
│   이 기기는 아직 등록되지 않았습니다  │
│                                  48   │
│  ┌──────────────────────────────┐     │
│  │                              │     │
│  │      [ 카메라 미리보기 ]     │ 280 │
│  │                              │     │
│  │   관리자 화면의 등록 QR을    │     │
│  │   비추세요                   │     │
│  └──────────────────────────────┘     │
│                                  16   │
│  ┌──────────────────────────────┐     │
│  │   등록 코드 직접 입력         │ 56  │
│  └──────────────────────────────┘     │
│                                       │
│   ⓘ 등록은 관리자가 관리웹 >          │
│     시스템 > 설정에서 만듭니다   48   │
│                                       │
│   기기 정보                      32   │
│   모델 Zebra TC21 · Android 13   40   │
└──────────────────────────────────────┘
```

**세로 검산**: 48+280+16+56+48+32+40 = 520 / 664 — 통과(여유 144).

### §3-2. 상태 B — **등록됨**(상시 진입 화면)

```
360 × 720 dp
┌ 앱바 56 ─────────────────────────────┐
│  PDA-W01   ● 온라인  📶 스캐너  미동기0│
├──────────────────────────────────────┤
│ 본문 592                              │
│   사번                           32   │
│  ┌──────────────────────────────┐     │
│  │        9 0 0 0 2 8           │ 64  │
│  └──────────────────────────────┘     │
│  ┌─────┬─────┬─────┐                  │
│  │  1  │  2  │  3  │  56dp        56  │
│  ├─────┼─────┼─────┤                  │
│  │  4  │  5  │  6  │              56  │
│  ├─────┼─────┼─────┤                  │
│  │  7  │  8  │  9  │              56  │
│  ├─────┼─────┼─────┤                  │
│  │ ←   │  0  │ 지움│              56  │
│  └─────┴─────┴─────┘                  │
│  ┌──────────────────────────────┐     │
│  │           확 인               │ 56  │
│  └──────────────────────────────┘     │
│  ─────────────────────────────   16   │
│   현재 작업자  김 · 900028       40   │
│   ⓘ 이 기기의 기록은 이 사번으로 40   │
├──────────────────────────────────────┤
│ 하단 72   [ 작업 목록으로  → ]        │
└──────────────────────────────────────┘
```

**세로 검산**: 32+64+56×4+56+16+40+40 = 472 / 592 — 통과.

**터치 규격**: 키패드 56dp(모바일 최소 48dp 이상 · POP의 72px보다 작다 — 폼팩터가 다르다).

> ⚠ **설정(언어·스캐너·기기 정보)은 상태 B의 앱바 오버플로 메뉴로 뺀다.** 매일 쓰는 화면에 상시 노출할 것이 아니다.

> ⭐ **앱바의 `📶 스캐너` 는 「블루투스 연결됨/끊김」이 아니라 「스캐너 준비됨 / 오류」다** — 단말이 일체형이라 연결이라는 상태가 없다(§5-4).

## §4. 필드

### §4-A. 단말 `mdm.terminal` (읽기 — 토큰 클레임에서 온다)

| 라벨(한/베) | 출처 | 비고 |
| --- | --- | --- |
| 단말 코드 | `terminal_code` | 앱바에 상시 표시 |
| 공장 | `plant_id` | 토큰 클레임 `plantId` |
| **토큰 세대** | ⛔ **`token_version` 없음** | ✓확정 2026-07-28 §6-② · **미착지** §5-2 |
| **기기 식별자** | ⛔ **컬럼 없음** | ⚠ **없어야 정상이다** §5-2 |

### §4-B. 작업자 `mdm.worker` (읽기)

`P-CO-01` §4-A와 **동일**하다. 사번·이름·소속·재직 상태를 확인하고 **아무것도 저장하지 않는다.**

### §4-C. 단말 로컬 설정 (⛔ 서버 저장 없음)

| 항목 | 저장 위치 | 왜 서버가 아닌가 |
| --- | --- | --- |
| 언어(한/베) | **단말 로컬** | 공용 장비 — 사람별이 아니라 **기기별** 설정 §5-3 |
| 스캐너 준비 상태 | **단말 내장**(어댑터) | 일체형이라 연결·페어링 대상이 없다 §5-4 |
| 현재 사번 | **앱 메모리** | 세션이 아니다(`P-CO-01` §5-4) |
| 단말 토큰 | **Android Keystore** | 앱 삭제 시 함께 사라진다 §5-5 |

## §5. 액션·상태

### §5-1. 넷 중 서버에 저장할 것은 **하나뿐**이다

화면명이 열거하는 네 가지를 물리 모델과 대조했다.

| 하는 일 | 저장 위치가 있나 | 판정 |
| --- | :-: | --- |
| **① 기기 등록**(토큰) | ⛔ **`token_version` 없음** | **없어서 문제다** §5-2 |
| ② 사번 입력 | ✅ `mdm.worker` 조회만 | 정상 — 저장하지 않는 것이 설계다 |
| ③ 스캐너 준비 상태 | ⛔ 0건 | **없어야 정상이다** — 일체형이라 연결 개념 자체가 없다 §5-4 |
| ④ 언어 선택 | ⛔ 0건 | **없어야 정상이다** §5-3 |

> **⚠ 「전부 없다」가 아니다.** 세운 판별(「**없어야 정상인 것**」과 「**없어서 문제인 것**」을 가른다 — `W-01-05` §5-1)을 그대로 적용한 결과다. 셋은 **애초에 서버가 알 필요가 없고**, 하나만 진짜 결손이다.

**이 구분을 하지 않으면 이슈 4건을 내게 된다.** 셋은 회신을 받아도 「두지 않는다」가 답이라 왕복만 늘어난다.

### §5-2. ⛔ ① 기기 등록 — **토큰 세대가 없다** · **주입 경로도 정해져 있지 않다**

**결손 1 — `mdm.terminal.token_version` 미착지.** `W-CO-06` §5-4와 **같은 항목**이다. 모바일은 파급이 더 크다:

> 분실 위험이 POP보다 크지만 `token_version`으로 즉시 폐기가 가능해져 만료를 짧게 둘 실익이 없다. **분실 시 재발급으로 끊는다.** (설계검토 §6-③)

**그 전제가 성립하지 않는다.** PDA는 들고 다니는 물건이고, 잃어버렸을 때 `is_active` 해제로 물러나면 **다시 켜는 순간 유출 토큰이 되살아난다**(실측 200→401→**200**).

**결손 2 — 토큰을 기기에 어떻게 넣는가가 정해져 있지 않다.**

설계검토 **§5**가 이것을 예고했다:

> `uiux` POP 화면 | **경계 위임** | 단말 최초 등록(**토큰 주입**) 화면 필요 여부 → `[docs→uiux]` 이슈 발행 | POP UI 착수 시

**그 이슈가 오기 전에 화면이 먼저 왔다.** 이 장이 답한다.

| 안 | 어떻게 | 왜 아닌가 / 왜 이것인가 |
| :-: | --- | --- |
| A | 관리자가 **토큰 문자열을 직접 입력** | ⛔ JWT를 PDA 키보드로 친다. 비현실적 |
| B | 관리웹이 **등록 QR**을 보이고 기기가 스캔 | ✅ **권고** — 스캐너·카메라가 이미 있다(D-2) · **무인증 엔드포인트가 안 생긴다** |
| C | 짧은 **1회용 등록 코드** 입력 → 서버가 토큰 교환 | ⚠ **토큰 없이 서버를 호출해야 한다** — 무인증 엔드포인트가 하나 생긴다. 코드 만료·재사용 통제가 따라온다 |

**B를 권고하는 이유**: 인증 경계를 늘리지 않는다. 관리웹이 이미 인증된 상태에서 토큰을 만들고, 기기는 그것을 **읽기만** 한다.

⚠ **B의 대가**: **QR이 유출되면 토큰이 그대로 유출된다.** 화면·인쇄물로 남지 않게 해야 한다 — `W-CO-06` §5-4의 「한 번만 보인다」가 그 통제다. 그리고 **유출을 끊는 수단이 `token_version`이므로 결손 1과 묶인다.**

→ **§I 신규 후보 · 이슈** §9-1. **`W-CO-06`과 한 건으로 낸다.**

### §5-3. ④ 언어 선택 — 저장할 곳이 없는 것이 **맞다** · 그러나 **바뀌는 범위를 밝혀야 한다**

인벤토리 v1.3이 `W-CO-07`(다국어 환경설정)을 삭제하며 실측했다 — **「사용자 환경설정」 엔티티 0건**. 그 판정은 지금도 맞다.

**모바일은 공용 장비다**(하드웨어 「배치 지점당 1식」 8식). **사람별 설정을 서버에 둘 이유가 없다** — 다음 사람이 쓰면 어차피 바뀐다. **단말 로컬 저장이 정확하다.**

**⛔ 그런데 무엇이 바뀌는지가 문제다.** 다국어는 성격이 셋인데(인벤토리 v1.3 §3-1) 이 토글은 **셋째만** 한다:

| 성격 | 담당 | 이 토글이 바꾸나 |
| --- | --- | :-: |
| ① 마스터 한/베 명칭 | `W-06-05`·`W-06-06` | ⛔ **아니다** — 그리고 **컬럼 자체가 없다**(§I-1) |
| ② 라벨 출력물 다국어 | 서버 렌더링(결정 18) | ⛔ 아니다 |
| ③ **내 화면 언어** | **이 토글** | ✅ |

**§I-1이 여기서 눈에 보이는 형태로 드러난다** — `item_name`·`process_name`·`defect_name`이 전부 `name_t` **단일 컬럼**이다. **베트남어로 바꿔도 품목명은 한국어로 남는다.**

**규칙**: 언어 전환 메뉴에 **범위를 적는다.**

```
언어  [ Tiếng Việt ▾ ]
      ⓘ 화면 문구만 바뀝니다. 품목명·공정명은 등록된 그대로 표시됩니다
```

**G-5(사용자가 바꿀 수 없는 것에는 확정 번호를 문구에 넣는다)의 변형**이다 — 여기서는 번호 대신 **범위**를 넣는다. 이 문구가 없으면 「번역이 안 됐다」는 오해가 현장에서 반복된다.

→ **§G 신규 조항 후보** §9-2.

### §5-4. ③ 스캐너 — **앱이 연결을 관리하지 않는다** ✅ *(전제 확정 2026-08-26)*

**`[문서]`** — 단말이 **바코드 스캐너 일체형 산업용 단말(PDA)**로 확정됐다(omf-mes-client#442 · 2026-08-25). 붙였다 떼는 외부 장치가 아니라 **스캐너가 단말의 일부**다. 따라서 **페어링 대상이 존재하지 않고**, 앱이 저장하거나 관리할 연결 상태도 없다.

**v0.1의 `[추정]`이 무엇이었나.** 무선 스캐너를 **블루투스 HID(키보드)**로 보고 「페어링은 Android 설정 소관」이라 적었다. **결론(앱이 관여하지 않는다)은 맞았으나 근거가 틀렸다** — 운영체제가 대신 해 주기 때문이 아니라 **애초에 페어링할 것이 없기 때문**이다.

⭐ **v0.1이 경고한 「SDK 연동이면 판정이 뒤집힌다」는 더 이상 성립하지 않는다.**

| v0.1이 든 위험 | 지금 |
| --- | --- |
| 앱이 스캐너를 직접 연다 → **연결·해제 상태**를 화면이 갖는다 | ⛔ 성립 안 함 — **연결할 외부 장치가 없다** |
| **페어링 화면**을 그려야 한다 | ⛔ 성립 안 함 — #442가 「페어링 UI 를 만들지 않는다」로 확정 |
| `D-2` 「포커스 고정」 전제가 무너져 **전 스캔 화면에 파급** | ⛔ 성립 안 함 — 스캔값이 **어댑터를 거쳐 문자열로** 도착한다. `TextField` + 포커스 고정 그대로 |

**#442가 강제하는 것**: 기종이 아직 미확정이라 **기종에 의존하는 코드를 쓰지 않고**, 스캔 결과를 **어댑터 인터페이스**(스캔값 문자열 · 오류 · **준비 상태**)로 받는다. 키보드 입력 구현체가 기본이며, 실기가 키보드 방식이면 그것이 그대로 운영 구현이 된다.

⚠ **그래서 「연결됨/끊김」이 사라지는 것이 아니라 뜻이 바뀐다.** 일체형이라도 스캐너 모듈 고장·설정 미적용은 있고, 어댑터가 「준비 상태·오류」를 갖는다. 앱바 아이콘은 **「스캐너 준비됨 / 오류」**로 남긴다. ⛔ **이 표시를 없애면 `D-3`(직접 입력 대체 경로 의무)을 화면에서 띄울 근거가 사라진다.**

**페어링 절차는 그리지 않는다** — 안내할 페어링 자체가 없다. v0.1의 「OS 설정으로 안내한다」도 함께 뺀다.

→ **§8-3 재정의.** 「고객사 H/W 정리 회신」 대기 항목이 아니다. 남는 것은 **기종 조달 시 스캔값 도착 형태를 확인**하는 것뿐이고, 어댑터가 흡수하므로 **화면 재작업이 없다**(등급 「조정」 → 「표시」).

### §5-5. 앱을 지우면 등록이 풀린다 — 화면이 그것을 말한다

토큰은 **Android Keystore**에 둔다(설계검토 §3.2의 Windows DPAPI에 대응하는 자리). **앱 삭제·데이터 초기화 시 함께 사라진다.**

```
⚠ 앱을 지우거나 저장소를 비우면 기기 등록이 풀립니다 — 관리자에게 재등록을 요청하세요
```

**미동기 큐도 함께 사라진다**(C-1 — 로컬 outbox). 이것이 더 위험하다:

```
⛔ 보내지 못한 기록 12건이 있습니다. 앱을 지우면 사라집니다
```

**설정 메뉴에서 「등록 해제」를 누를 때 미동기 건수를 반드시 보인다.**

### §5-6. F-1이 걸리지 않는다 — **모바일은 매트릭스 밖이다**

`W-CO-06` §5-1에서 정리한 그대로다. 설계검토 §2.3:

> **모바일에는 이 매트릭스가 거의 적용되지 않는다 — 그리고 그게 정상이다.** … 배지단말 #8이 「통신 두절 시 입·출측 스캔을 **단일 단말기 하나로**」 수행하는 축퇴 규칙을 명문화했다 — 단말의 행위 범위는 상황에 따라 **넓어져야** 한다.

**이 화면은 기능 구성 표를 보이지 않는다.** 모바일 단말은 `W-CO-06`에서 공정 행 0건으로 남는 것이 정상이다.

### §5-7. 오프라인 — **등록은 온라인, 사번은 오프라인 가능**

**C-5 판정**:

| 동작 | 오프라인 |
| --- | :-: |
| **기기 등록**(상태 A) | ⛔ **온라인 전용** — QR을 읽어도 서버가 받아 주는지 확인해야 한다 |
| **사번 확인**(상태 B) | ✅ **허용** — 작업자 목록 캐시(C-11 · `P-CO-01` §5-6과 동일) |
| 언어 전환 | ✅ 로컬 |

**C-11 선행 데이터**: 해당 공장 재직 작업자 목록. **등록 직후 온라인 상태에서 한 번 받아 둔다.**

⚠ **등록 직후 곧바로 오프라인 현장에 들어가면 사번 확인이 안 된다.** 등록 완료 화면에서 **「기준정보를 받는 중」을 끝까지 보이고**, 완료 전에는 작업 화면으로 넘기지 않는다.

### §5-8. 액션

| 액션 | 활성 조건 |
| --- | --- |
| **QR 스캔(등록)** | 상태 A · **온라인일 때만** |
| 등록 코드 직접 입력 | 상태 A — ⚠ **채택 시**(§5-2 C안) |
| 숫자 입력·확인 | 상태 B |
| **교대(사번 바꾸기)** | 현재 작업자가 있을 때 |
| 작업 목록으로 → | 현재 작업자가 있을 때 |
| 언어 전환 | 항상 — **범위 문구 필수**(§5-3) |
| **등록 해제** | 설정 메뉴 — ⚠ **미동기 건수 표시 필수**(§5-5) |

## §6. 예외·오프라인

| 상황 | 처리 |
| --- | --- |
| 미등록 상태로 실행 | 상태 A로 진입 — **다른 화면으로 못 간다** |
| QR이 다른 공장 단말 | ⛔ 「이 QR은 2공장 단말용입니다」 |
| **QR 만료·재발급됨** | ⛔ 「등록 정보가 만료됐습니다. 관리자에게 새 QR을 요청하세요」 — ⚠ **`token_version`이 없어 실제로는 만료 판정이 안 된다**(§5-2 · A-11) |
| 등록 중 오프라인 | ⛔ 「연결된 상태에서 등록해야 합니다」 |
| 사번 없음·비재직 | ⛔ `P-CO-01` §6과 동일 |
| **오프라인 + 작업자 목록 미수신** | ⛔ 「기준정보를 아직 받지 못했습니다」 — C-11 |
| **스캐너 오류·미준비** | ⚠ 앱바 아이콘 회색 + 「직접 입력으로 진행할 수 있습니다」 — **D-3(직접 입력 대체 경로 의무)**. 일체형이라 「끊김」이 아니라 **모듈 오류·설정 미적용**이다 §5-4 |
| 등록 해제 시 미동기 존재 | ⛔ **「보내지 못한 기록 12건이 사라집니다」** — 2단계 확인 |
| 앱 데이터 초기화 | ⛔ 등록·큐 동시 소실(§5-5) — 사전 경고만 가능 |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 앱바(단말·온라인·스캐너 준비·미동기) | `AppBar` + `Chip` | a |
| **QR 스캔 뷰** | *(없음)* — 카메라 프리뷰 | **c** §7-1 |
| 사번 표시 필드 | `TextField`(읽기 전용) | a |
| **숫자 키패드** | *(없음)* | **d** — **G-6** · **네 번째 사용처** |
| 언어 선택 | `Select` | a |
| 현재 작업자 | `Card` | a |
| 경고·오류 | `AlertBanner` | a — G-1 |
| 등록 해제 확인 | `Dialog` | a |

**신규 DS 갭 0건.**

### §7-1. QR 스캔 뷰는 DS 갭이 아니다

카메라 프리뷰는 **Capacitor 플러그인이 제공하는 네이티브 뷰**다. DS 컴포넌트로 감쌀 것이 아니라 **플랫폼 기능**이고, 그 위에 얹는 안내 문구·프레임만 DS(`Card`·`Text`)로 만든다.

**`c`(조합) 판정** — 서식 §6-1이 요구하는 근거다. `d`로 올리면 **DS가 카메라를 갖게 된다.**

⚠ **바코드 스캔(D-2)과 다르다.** 그쪽은 스캔값이 어댑터를 거쳐 포커스된 입력 필드에 문자열로 도착하므로 `TextField` + 포커스 관리로 끝난다(#442). **이 화면의 QR만 카메라를 쓴다.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ⛔ **`token_version` 미착지 + 토큰 주입 경로 미정**(§5-2) — 분실 PDA를 끊을 수 없다 | 상류↔하류 불일치 | **조정** | **§I 신규 · 이슈** — `W-CO-06` §8-1과 **한 건** |
| 2 | **등록 QR 유출 위험** — B안의 대가. 끊는 수단이 §8-1에 묶여 있다 | 설계 결정 | **조정** | 「한 번만 보인다」 + 재발급 |
| 3 | ⚠ **스캐너 준비 상태를 앱바에 무엇으로 보이나** — 일체형 확정(#442)으로 「연결됨/끊김」이 성립하지 않는다. 어댑터의 「준비 상태·오류」를 어떤 문구·아이콘으로 표시할지 정한다(§5-4). 기종은 여전히 미확정이나 **어댑터가 흡수해 화면 재작업이 없다** | 설계 결정 | **표시** | 다음 개정에서 확정 — ⛔ `D-3` 표시 근거이므로 **없애지 않는다** |
| 4 | **언어 전환이 마스터 명칭을 바꾸지 못한다** — §I-1 (§5-3) | 상류↔하류 불일치 | **표시** | #64 · **화면이 범위를 밝힌다** |
| 5 | `terminal_type_code`에 **`MOBILE` 값이 있어야 한다** | 공통코드 | **표시** | `W-06-06` · #64 |
| 6 | **화면명 「인증」** — `P-CO-01` §5-1과 같은 문제 | 설계 결정 | **표시** | 인벤토리 개정 시 함께 |
| 7 | **이 화면의 도식 앵커가 없다**(신뢰도 추정) | 프로세스 | **표시** | 정상 |

**차단 0건** — 단 §8-1 해소 전까지 **「기기 등록 해제·재발급으로 이전 기기를 끊는다」를 그리지 않는다**(A-11).

## §9. 이 장에서 나온 공유 계약 후보

1. ⛔ **단말 토큰의 세대(`token_version`)와 주입 경로를 계약이 정한다** — ✓확정 2026-07-28 §6-②의 컬럼이 물리 모델에 없어 **분실 단말을 영구히 끊을 수 없다**. 주입 경로도 미정인데 설계검토 §5가 「`[docs→uiux]` 이슈로 넘긴다」고 예고한 항목이다. **QR(관리웹이 발급 → 기기가 읽음)을 권고**한다 — 무인증 엔드포인트가 생기지 않는다. 대가는 QR 유출이고 그 통제가 세대 번호이므로 **두 결손은 한 건이다** (§5-2)
2. **언어 전환의 「범위」를 화면이 밝힌다** — 다국어는 성격이 셋인데(마스터 명칭 / 출력물 / 화면 문구) 토글은 셋째만 한다. 게다가 첫째는 **컬럼 자체가 없다**(§I-1 — `name_t` 단일). 「화면 문구만 바뀝니다」를 적지 않으면 **「번역이 안 됐다」는 오해가 현장에서 반복된다.** G-5의 변형 — 확정 번호 대신 **범위**를 넣는다 (§5-3)
3. **「저장할 곳이 없다」를 셋으로 가른다 — 없어야 정상 / 없어서 문제 / 아직 안 정함** — 이 화면이 하는 네 가지 중 **셋은 서버가 알 필요가 없다**(공용 장비의 언어·**연결 대상이 없는 일체형 스캐너**·조회만 하는 사번). 넷째만 진짜 결손이다. 판별(「없어야 정상인 것」)을 **이슈 발행 전 필수 관문**으로 §I 머리말에 못박는다 — 그러지 않으면 답이 「두지 않는다」인 질문을 3건 보내게 된다 (§5-1)
4. **오프라인 화면의 「선행 데이터」에는 받는 시점도 함께 적는다** — C-11이 「무엇을 미리 받나」를 요구하는데, 등록 직후 곧장 현장에 들어가면 **받을 기회가 없다.** 등록 완료 화면이 수신을 끝까지 보이고 **완료 전에는 작업 화면으로 넘기지 않는다** (§5-7)

## 근거·이력

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-06 | 초안. ⭐ **「넷을 하는데 서버에 저장할 것은 하나뿐이고 그 하나가 없다」** — 화면이 열거하는 4기능(기기 등록·사번·스캐너·언어)을 물리 모델과 대조해 **셋은 「없어야 정상」**(공용 장비의 언어 · OS 소관 페어링 · 조회만 하는 사번), **하나만 진짜 결손**(`token_version`)임을 갈랐다. 판별을 이슈 발행 전 관문으로 계약화 제안. ⛔ **토큰 주입 경로 미정**을 확인하고 3안 비교 후 **QR 권고**(무인증 엔드포인트가 안 생긴다) — 설계검토 §5가 예고한 `[docs→uiux]` 위임에 **화면이 먼저 답한다**. **언어 전환 범위**(§I-1로 마스터 명칭이 안 바뀐다)를 화면이 밝히도록 규칙화. 스캐너는 **`[추정]` HID**로 두고 검증 방법을 회신 항목으로 명시 — **SDK면 D-2가 전 화면에서 흔들린다**. QR 스캔 뷰는 **플랫폼 기능이라 `c`**(DS가 카메라를 갖지 않는다). 미결 7건(**차단 0**) · 계약 후보 4건 · 신규 DS 갭 0건. |
| v0.2 | 2026-08-26 | ⭐ **스캐너 전제 확정 — 「없어야 정상」 결론은 그대로, 근거가 바뀌었다.** 단말이 **바코드 스캐너 일체형 산업용 PDA**로 확정(omf-mes-client#442)돼 §5-4를 전면 재작성했다. v0.1이 경고한 「SDK 연동이면 페어링 화면이 필요하고 `D-2`가 전 화면에서 흔들린다」는 **세 갈래 모두 성립하지 않는다** — 연결할 외부 장치가 없고, #442가 페어링 UI 를 만들지 않기로 확정했으며, 스캔값이 **어댑터**를 거쳐 문자열로 도착해 「포커스 고정」이 유지된다. ⚠ **「연결됨/끊김」을 지우지 않고 「준비됨/오류」로 뜻만 바꿨다** — 지우면 `D-3`(직접 입력 대체 경로)의 화면 근거가 사라진다. 미결 3을 「연동 방식 회신 대기(조정)」에서 「준비 상태 표시 문구(표시)」로 재정의. 미결 7건 유지 · **차단 0** · 신규 DS 갭 0건 · 필드·계약 변동 0. |

출처: uiux/2026-08-06-화면상세스펙-확대6차/M-CO-01-기기등록사번인증.md · 최종 대조일: 2026-08-25
