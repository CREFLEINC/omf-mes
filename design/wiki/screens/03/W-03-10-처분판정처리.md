# `W-03-10` 처분 판정 처리(재작업/폐기/정상)

> ⭐ **DR-008 확정 3-A 로 신설된 화면**(2026-08-13). 인벤토리 **116 → 117**.
>
> **왜 없었나** — `F04` 정합 확정(2026-07-07)이 「재작업/폐기 **판정**은 전 도메인에서 **03 소관** 통일」이라 했는데 **03 의 다섯 화면에 그 화면이 만들어지지 않았다.** 03 요구서는 `disposition_decision` 을 「04 것」이라 넘겼고, `W-04-07` 은 「03 이 만든다 · 04 는 의뢰만」이라 적었다 — **서로에게 미뤘다.**
>
> 04 계약 트랙 1단계에서 **116화면 액션을 전수로 뽑아** 처분을 저장하는 화면이 **0건**임을 확인했다.

## §0. 요약

- **개요**: 관리웹 화면. 품질담당·품질책임자가 부적합(재작업/폐기/정상)을 판정해 저장하는 판정·승인 유형 화면이다. `F04` 정합 확정(2026-07-07)의 「재작업/폐기 판정은 03 소관 통일」이 어느 화면에도 착지하지 않았던 공백을 DR-008 확정 3-A(2026-08-13)로 메운다.
- **주요 데이터**: `quality.nonconformance`·`quality.nonconformance_lot`(부적합·대상 LOT, 읽기) · `quality.disposition_decision`(처분 판정 — `decision_qty`·`reason`·`decided_by` 모두 NOT NULL, 신설 0). 판정 저장과 `trace.lot.status_code` 전이는 한 트랜잭션이다(공유계약 B-8).
- **핵심 액션**: 판정 대기 목록 조회(기간 ✅필수 · 품목 · 심각도 · **원천**(제품/반품) — §5-4 · §8-6) → 부적합 선택 → 처분(재작업/폐기/정상) 선택 + 수량(남은 수량 이하) + 사유 입력 → 판정 저장. 승인·반려는 이 화면에 두지 않는다(`W-CO-09` 결재함 소관).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID | `W-03-10` |
| 화면명 | 처분 판정 처리(재작업/폐기/정상) |
| 프로그램 | **관리웹** |
| 도메인 | **03 품질관리** |
| 유형 | 판정·승인 |
| 행위자 | **품질담당 · 품질책임자** |
| 신뢰도 | **확정** — DR-008 팀 리더 확정(2026-08-13) |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **요구** | ✅**REQ-PR-0025** 「재작업 가능 불량 vs 폐기 불량 **구분** — 판정은 **불량창고 입고 후**」 | **확정** |
| **결정** | ✅**`F04` 정합 확정**(2026-07-07) 「재작업/폐기 판정은 전 도메인 **03 소관** 통일」 · ✅**DR-008**(2026-08-13) 3-A | **확정** |
| **프로세스** | `05` §3 **S11**(01-S-I 폐기 분기) 「1. [판정 대기 시] 불량창고 입고 2. **판정 → 03 전이도** 3. [폐기 판정 시] …」 · WF03 S5 · 도식 **H8** 「판정 → 03 도식」 경계 위임 | **확정** |
| **화면·IA** | 인벤토리 v2.3 `W-03-10` — 관리웹 트리 **품질 › 처분 판정** · 흐름축 **검사포장** 단 | **확정** |
| **물리 모델** | `quality.nonconformance` · `quality.nonconformance_lot` · `quality.disposition_decision` — ⭐ **신설 0** | **확정** |

### §2-1. ⭐ 데이터가 「사람이 화면에서 정한다」를 말하고 있었다

```sql
quality.disposition_decision
    nonconformance_id      NOT NULL   ← 부적합이 선행한다
    disposition_type_code  NOT NULL   ← 재작업 · 폐기 · 정상
    decision_qty           NOT NULL CHECK (> 0)   ⭐ 부분 처분이 된다
    uom_id                 NOT NULL
    reason                 text NOT NULL          ⭐ 사람이 쓴다
    decided_by             NOT NULL → app.app_user  ⭐ 사람이 정한다
    decided_at             NOT NULL
    approval_request_id                            ← 승인이 붙을 수 있다
```

⭐ **처분 판정은 사람이 사유를 적고 판정자를 남기는 업무다** — `DR-008` 확정 3-A 가 이 화면을 신설한 이유가 그것이다. 자동 판정이 아니다. ⚠ **물리 제약(`decided_by`·`reason` 의 `NOT NULL`)이 그 업무와 일치하는 것은 방증이지 근거가 아니다**(`schema/data-model-boundary.md` — 모델의 제약으로 화면의 필수를 정하지 않는다). ⭐ **다만 발견 경로로는 유용했다** — `M-01-13` 때와 같은 형태로, 「이 칸을 누가 채우나」를 사람으로 답해 보니 **앞단계 화면이 빠져 있었다**(공유계약 **A-24**).

## §3. 레이아웃 — 관리웹 3영역

```
┌───────────────────────────────────────────────────────────────────────┐
│ 품질 › 처분 판정                                                       │
├───────────────────────────────────────────────────────────────────────┤
│ 탭  [ 판정 대기 ] [ 처리 이력 ]                                        │
├───────────────────────────────────────────────────────────────────────┤
│ 《① 판정 대기 목록》                                                    │
│  기간 [        ~        ] ✅필수(L-3)  품목 [      ]  심각도 [ ▾ ]     │
│                                                          [ 조회 ]      │
│  원천 [ 전체 ▾ ]  ⓘ 04 제품·04 반품만 옵니다 — 수리·자재는 다른 경로 │
│  ▸ NC-…0042  ABC-123   320  중대  08-12   미판정                      │
│  ▸ NC-…0039  DEF-456   140  경미  08-11   ⚠ 일부 판정                 │
├───────────────────────────────────────────────────────────────────────┤
│ 《② 선택한 부적합》                     ⓘ 의뢰는 W-04-07 에서 옵니다   │
│  부적합 NC-…0042 · ABC-123 · 심각도 중대 · 08-12 접수                  │
│  설명  「도장 표면 박리 — 3차 공정 후 발견」                            │
│  대상 LOT     LOT-…0088  320 EA   불량 → (판정 후)                     │
│  ─────────────────────────────────────────────────────────           │
│  판정 이력   ▸ 재작업 200 EA  08-12 14:20  김OO                        │
│  ⭐ 남은 수량  120 EA        ← 서버가 계산한다(L-2)                    │
├───────────────────────────────────────────────────────────────────────┤
│ 《③ 판정》                                                             │
│  처분   ( ) 재작업   ( ) 폐기   ( ) 정상    ⚠ 선택지 준비 중(G-2)      │
│  수량   [      ] EA  ✅필수 · 0 초과 · ⓘ 남은 수량은 저장 시 서버 확정 │
│  사유   [                                        ] ✅필수              │
│                                          [ 판정 저장 ]  [ 취소 ]       │
└───────────────────────────────────────────────────────────────────────┘
```

⭐ **②의 「남은 수량」이 이 화면의 핵심이다** — 부적합 하나를 **수량으로 갈라** 일부는 재작업, 일부는 폐기로 정할 수 있다(`decision_qty`). 남은 수량이 0 이 되면 판정이 끝난다.

## §4. 필드

### §4-A. ① 판정 대기 목록 (읽기)

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 비고 |
| --- | --- | --- | --- |
| 부적합번호 / *(미정)* | `nonconformance.nonconformance_no` | `business_no_t` | 유일 |
| 품목 / *(미정)* | `nonconformance.item_id` → `mdm.item` | FK | 코드·명 함께 |
| 수량 / *(미정)* | `nonconformance.affectedQtyTotal`(파생 — `nonconformance_lot.affected_qty` 합) | `qty_t` | ⭐ 서버 집계(L-2) — 화면이 `lots` 를 세지 않는다 |
| 심각도 / *(미정)* | `nonconformance.severity_code` | `code_t` | ⚠ 값 목록 미정 §8-2 |
| 접수일 / *(미정)* | `nonconformance.opened_at` | `timestamptz` | |
| 상태 / *(미정)* | `nonconformance.status_code` | `code_t` | ⚠ 값 목록 미정 §8-2 |
| 판정 진행 / *(미정)* | `nonconformance.dispositionProgressCode`(파생) | `code_t` | ⭐ **미판정 / 일부 판정 / 완료** — 남은 수량으로 서버가 낸다(L-2). ⛔ `status_code` 로는 못 가른다 — 「판정 대기」 한 값이 앞의 둘을 함께 담는다(§5-5) |

### §4-B. ③ 판정 (입력)

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 검증 |
| --- | --- | --- | :-: | --- |
| 처분 / *(미정)* | `disposition_decision.disposition_type_code` | `code_t` | ✅ | ⚠ **값 목록 미확정**(G-2) — 재작업·폐기·정상 셋. ⛔ **선별은 받지 않는다**(#118) |
| 수량 / *(미정)* | `disposition_decision.decision_qty` | `qty_t` | ✅ | ⭐ **0 초과**(`CHECK (> 0)`) — 정수 하한이 아니다. 소수 자릿수는 `mdm.uom.decimal_scale` 을 따른다 · **남은 수량 이하는 서버가 판정(409)** — 화면은 막지 않고 알린다(§5-5) |
| 단위 / *(미정)* | `disposition_decision.uom_id` | FK → `mdm.uom` | ✅ | 대상 LOT 의 단위로 고정 |
| 사유 / *(미정)* | `disposition_decision.reason` | `text` | ✅ | **화면이 필수로 만든다** — 남이 읽고 판단할 자유 텍스트다(공유계약 **A-12**). 공백만 불가 |

⛔ **`decided_by`·`decided_at` 은 화면에 없다** — 서버가 인증 주체와 수신 시각으로 채운다(공유계약 **B-6**).

## §5. 액션·상태

### §5-1. 액션

| 액션 | 위치 | 활성 조건 | 비고 |
| --- | --- | --- | --- |
| 조회 | ① | 기간 입력됨(L-3) | `GET /quality/nonconformances` |
| 부적합 선택 | ① | 행 클릭 | `GET /quality/nonconformances/{id}` + `…/disposition-decisions` |
| **판정 저장** | ③ | 처분 선택 **AND** 수량 > 0 **AND** 사유 입력 | ⭐ `POST /quality/nonconformances/{id}/disposition-decisions` · ⛔ **잔량 초과는 활성 조건이 아니다** — 경고만 하고 판정은 서버가 한다(§5-5 · L-2 · A-9 ⓑ) |
| 취소 | ③ | 입력 있음 | 폼 초기화 |
| ⛔ **승인·반려** | — | **두지 않는다** | 승인이 붙는 처분이면 **`W-CO-09` 결재함**이 처리한다 |

### §5-2. ⭐ 판정 저장과 Lot Status 전이는 **한 트랜잭션**이다

```
disposition_decision INSERT
        +
trace.lot.status_code UPDATE      ← 재작업 → 검사 대기 / 폐기 → 폐기 / 정상 → 정상
```

**공유계약 B-8**(§5-2)**.** 처분만 남고 LOT 이 안 바뀌면 **다음 화면이 잘못된 대상을 집는다** — `W-04-11`(재등록)이 아직 불량인 LOT 을 정상으로 본다.

⚠ **Lot Status 변경이력 테이블이 없다**(`#64`·`#68`) — 전이는 되지만 **누가 왜 바꿨는지가 남지 않는다.** 공유계약 **A-11** 대로 화면이 그 사실을 결과 표 머리에 적는다.

### §5-3. ⭐ 이 화면이 푸는 것 — 세 화면의 진입

| 화면 | 진입 조건 | 지금까지 |
| --- | --- | :-: |
| `W-04-10` 제품 폐기 | 처분 = **폐기** | ⏳ 목록이 비었다 |
| `W-04-11` 재고 재등록 | 처분 = **정상** | ⏳ |
| `P-04-03` 재작업 실적 | 처분 = **재작업** | ⏳ |

### §5-4. ⛔ 이 화면은 **부적합을 만들지 않는다**

부적합 등록·판정 의뢰는 **`W-04-07`**(04)이 한다. **소유가 갈린다** — 공유계약 **B-13**.

| 테이블 | 쓰는 화면 | 계약 |
| --- | --- | --- |
| `nonconformance` · `_lot` | `W-04-07` (04) | **제품출하** |
| ⭐ `disposition_decision` | **`W-03-10`** (03) | **품질** |

⛔ **2026-08-28 정정 — 「02 수리·01 자재 불량도 여기로 온다」가 오늘은 사실이 아니다.** 「판정 기준이 하나여야 한다」가 `F04` 확정의 취지이나, 부적합을 만드는 경로는 **`W-04-07` 부적합 등록과 `W-04-06` 반품 입고 둘뿐**이다(계약 실측). 02 수리는 `production.repair_execution` 이 `defect_record` 를 물고, 01 자재는 부적합을 만드는 경로가 계약에 **0건**이다.

**정정(2026-08-30)** — 04 제품과 04 반품이 **같은 경로·같은 스키마**로 들어와 서로 가를 근거가 없던 것은 `nonconformance`에 원천 열이 없었기 때문이다. `sourceCode`(`PRODUCT`/`RETURN`)를 명시 축으로 신설해 되살린다. 02 수리·01 자재는 애초에 이 경로로 안 들어오므로(§5-4 정정) 이 축의 대상이 아니다 — 별개 사실이다.

⇒ ⭐ **① 에 원천 필터(제품/반품)를 둔다.** 저장 컬럼은 데이터 모델 담당에게 통지한다(A-11) — 기다리지 않는다.

### §5-5. ⭐ 잔량은 **보이되 막지 않는다** — 화면과 서버의 몫이 다르다

| 무엇 | 누가 | 근거 |
| --- | :-: | --- |
| 남은 수량을 **낸다** | **서버** | `GET …/disposition-decisions` 응답의 `summary.remainingQty`. 화면이 대상 합에서 결정 합을 빼지 않는다(**L-2**) |
| 남은 수량을 **보인다** | 화면 | ② 구획 Chip |
| 초과 입력을 **알린다** | 화면 | ⚠ 경고 — 「남은 수량은 저장할 때 서버가 확정합니다」 |
| 초과를 **거절한다** | **서버** | ⛔ **409** `DISPOSITION_QTY_EXCEEDED` + `remainingQty` (§6) |

⛔ **저장 단추를 잔량으로 잠그지 않는다.** 잔량은 **읽은 시점의 값**이라 두 사람이 같은 부적합을
동시에 판정하면 둘 다 화면을 통과하고 서버에서 하나가 떨어진다. **`W-03-03` §5-5 가 「부분 보류
합계 초과」로 이미 겪은 것과 같은 모양**이고, 공유계약 **A-9** 가 이런 합계 검증을 **ⓑ 경고**
(저장은 되나 확인 요구)로 등급 지었다 — ⓐ 차단이 아니다.

⭐ **잔량 필드가 내려온 뒤에도 이 규칙은 그대로다.** 서버가 값을 준다는 것과 화면이 판정한다는 것은
다르다.

⭐ **동시 판정을 실제로 잡는 것은 저장 충돌 토큰이다.** 판정 저장은 `If-Match` 를 **필수로** 받고,
그 값은 **부적합 상세**(`GET /quality/nonconformances/{nonconformanceId}`)의 `ETag` 를 그대로 담는다
— 잠그는 대상이 처분 결정 한 건이 아니라 **부적합**이기 때문이다(잔량이 부적합 단위로 정해진다).
⚠ 그러려면 판정 저장이 `nonconformance.version_no` 를 올려야 한다 — **§8 미결 8**.

⚠ **`status_code` 로는 판정 진행을 못 가른다.**

| 화면이 보이려는 것 | 잔량으로 본 조건 | `status_code` |
| --- | --- | --- |
| **미판정** | 결정 0건 | **판정 대기** |
| **⚠ 일부 판정** | 결정 1건 이상 · 잔량 > 0 | **판정 대기** ← 같다 |
| 완료 | 잔량 0 | 판정 완료 |

그래서 §4-A 의 「판정 진행」은 `status_code` 가 아니라 **`disposition_progress_code`** 파생을 쓴다.

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 남은 수량 초과 | ⛔ **409** `DISPOSITION_QTY_EXCEEDED` — 응답의 `remainingQty` 로 「남은 수량은 120 EA 입니다」를 만든다. ⛔ `message` 자유 텍스트를 파싱하지 않는다. **서버가 판정한다**(L-2 — 클라이언트가 잔량을 계산하지 않는다 · §5-5) |
| 이미 닫힌 부적합 | ⛔ **409** `INVALID_STATE` — 「이미 종결된 부적합입니다」 |
| 낙관적 잠금 충돌 | **409** — `If-Match` (공유계약 C-2 계열) |
| 권한 없음 | **403** — 판정 컨트롤 비활성 + 사유 표시 |
| 처분 선택지 비어 있음 | ⚠ **G-2** — 「선택지 준비 중」 비활성. **저장 버튼도 비활성** |
| **판정을 잘못 저장했다** | ⛔ **되돌릴 수 없다** — 계약에 `DELETE`·`PUT`·`:cancel` **0건**(계약 7벌 실측) · `decisionQty` 가 `exclusiveMinimum: 0` 이라 상쇄도 불가 · 이미 닫힌 부적합은 **409 `INVALID_STATE`**. **B-3**(이력 불변)·**J-6**(번복은 새 요청) 계열이며 **정정 경로가 미정**이다 — §8-9 |
| 오프라인 | **해당 없음** — 관리웹은 온라인 전제(결정 17) |

## §7. DS 매핑 (`@crefle/web-ui` 고정 커밋 `2891ae8`)

| 용도 | 컴포넌트 | 유형 |
| --- | --- | :-: |
| 페이지 틀 | `AppShell` · `PageHeader` · `Breadcrumb` | a |
| 탭 | `Tabs` | a |
| 대기 목록 | `Table` (`density="compact"` · `selectable`) | a |
| 검색·필터 바 | `SearchInput` + `Select` + `DateRangePicker` | **c** (조합 — 제품 소유) |
| 처분 선택 | `RadioGroup` | a |
| 수량·사유 입력 | `TextField` · **`TextArea`** | **a** — ~~G-14~~ 해소 «(정합주: 2026-09-01 — **해소됐다.** `TextArea` 가 디자인 시스템에 착지했다(DS #74 CLOSED 2026-08-26 · 고정 커밋 `d63fdcf` 실물 확인). 이 화면의 여러 줄 입력은 `TextArea` 로 만든다 — 토큰 입힌 `<textarea>` 로 먼저 만들라던 «임시 방편은 폐기**다)» *(구 기록: *(⛔ 2026-08-13 정정 — 「DS #74 로 추가됨」이 틀렸다. **#74 는 아직 OPEN 이고** 고정 커밋 `2891ae8` 전체 트리에 `TextArea` 도 `multiline` 도 0건이다. **이슈 상태와 고정 커밋 실물은 다른 사실**이라는 규칙에 그대로 걸렸다)* |
| 남은 수량 강조 | `Chip` | a |
| 판정 이력 | `Table` (`density="compact"`) | a |
| 알림·오류 | `Toast` · `AlertBanner` | a |
| 빈 상태·로딩 | `EmptyState` · `Skeleton` | a |

⛔ **2026-08-13 정정 — 「신규 갭 0건」이 아니었다.** `TextArea` 는 **DS #74 로 요청만 했고 반영되지 않았다**(이슈 OPEN · 고정 커밋 실물 0건). 이 화면은 **기존 갭 G-14 의 17번째 사용처**다. 조합 1건(`c`)은 제품이 소유한다.

⚠ **날짜 범위 선택의 이름이 다르다** — 스펙이 `DateRangePicker` 라 적었으나 실물은 `DatePicker` 의 `mode="range"` 다. 컴포넌트는 있고 이름만 다르다.

## §8. 미결

| # | 항목 | 성격 | 처리 |
| :-: | --- | --- | --- |
| 1 | **`disposition_type_code` 값 목록** — 재작업·폐기·정상의 **코드 문자열**이 미정 | 공통코드 | **2차 값 목록 제안안** 대상(업무 코드 44종 중 하나) · 확정 전까지 G-2 |
| 2 | `nonconformance.severity_code` · `status_code` 값 목록 | 공통코드 | 동상 · `#145` |
| 3 | ⛔ **Lot Status 변경이력 테이블 부재** — 판정으로 인한 전이가 이력에 안 남는다 | 데이터 | **`#64`·`#68`** — 전용 테이블 권고(`W-03-01` §5-2) |
| 4 | ⚠ **승인이 붙는 처분이 무엇인가** — `approval_request_id` 가 nullable 이라 「붙을 수도 있다」인데 **어떤 처분에 붙는지**가 안 정해졌다 | 규칙 | 결정 필요 — 폐기는 `W-01-06`·`W-04-10` 이 별도 승인을 받으므로 **여기서 또 받으면 이중**일 수 있다 |
| 5 | ⛔ **선별(sorting) 처분** | 범위 | **`#118`** — 1차 범위 밖. 값이 와도 실행 화면이 없다 |
| 6 | ✅ **되살렸다**(2026-08-30) — 04 제품·04 반품 원천 필터를 `sourceCode` 신설로 정의했다(§5-4). 02 수리·01 자재는 이 경로 대상이 아니다(별개 사실 — 정정 유지) | — | **해소** — 📨 데이터 모델 통지(`nonconformance.sourceCode`) |
| 7 | ⚠ **대상 LOT 의 단위가 섞이면 수량 합이 성립하지 않는다** — `affected_qty` 합을 서버가 내리는데 `nonconformance_lot.uom_id` 가 LOT 마다 있다 | 설계 결정 | **표시** — 지금은 「한 부적합 = 한 품목 = 한 단위」를 전제로 합을 낸다. 섞인 사례가 나오면 A-11 보강대로 합을 내리지 않고 두 값을 그대로 보인다 |
| 8 | ⚠ **판정 저장이 부적합의 버전을 올리는가** — 저장 충돌 토큰을 부적합 단위로 받기로 했는데(§5-5), §5-2 의 트랜잭션은 `disposition_decision` INSERT 와 `trace.lot.status_code` 전이뿐이라 `nonconformance.version_no` 가 오른다는 근거가 없다 | 데이터 | **조정** — 📨 데이터 모델 통지. 오르지 않으면 두 사람이 같은 부적합을 동시에 판정해도 토큰이 경합을 못 잡는다 |
| 9 | ⛔ **오판정을 되돌릴 때의 정정 경로 안내가 없다**(공유계약 **G-19** 미적용) — `disposition_decision` 은 기록 전용이라 계약에 `DELETE`·`PUT`·`:cancel` 이 **0건**이고 `decisionQty` 가 `exclusiveMinimum: 0` 이라 음수 상쇄도 못 한다(계약 실측). 잔량 0(판정 완료) 뒤 오판정이 드러나면 되돌릴 길이 없고, 이미 닫힌 부적합에는 **409 `INVALID_STATE`** 가 돌아온다 | 설계 결정 | **조정** — 「되돌리지 않는다」 자체는 **B-3**(이력 불변)·**J-6**(번복은 새 요청)이 이미 정한 기본값이다. 남은 것은 G-19 규칙 ①②③(저장 전 요약 · 파급 명시 · 정정 경로 안내)의 적용이고, 그 정정 경로를 04(부적합 소유)와 함께 정한다 |

## §9. 공유 계약 후보

1. ⭐ **부분 처분의 잔량은 서버가 낸다** — 「남은 수량 = 대상 수량 합 − 결정 수량 합」을 **클라이언트가 계산하지 않는다**. L-2 의 사례 추가이지 신설이 아니다. ✅ **2026-08-28 계약 착지** — `GET …/disposition-decisions` 응답의 `summary` 가 그 값이다(`#253`). 같은 모양이 `ShipmentRequestSummary.unallocatedQtyTotal`(요청 − 배정)에 이미 있다
2. ⚠ **판정 화면과 의뢰 화면이 갈릴 때 의뢰 화면은 「여기서 판정하지 않는다」를 상시 표시한다** — `W-04-07` 이 이미 그렇게 하고 있다. **B-13 의 화면 판** 후보

## 근거·이력

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-13 | 초안 — **DR-008 확정 3-A 로 신설**(인벤토리 116 → 117). `F04` 확정의 「판정은 03 소관」이 03 에 착지하지 않아 **116화면 전체에 처분을 저장하는 화면이 0건**이었다(액션 전수). ⭐ **데이터 신설 0** — `disposition_decision` 의 `decided_by`·`reason` 이 NOT NULL 이라 「사람이 화면에서 정한다」가 이미 박혀 있었다(A-24 와 같은 형태). ⭐ **부분 처분이 설계 전제**(`decision_qty`) — 남은 수량은 서버가 낸다(L-2). ⭐ **판정 저장 ↔ Lot Status 전이는 한 트랜잭션**(B-8) — 어긋나면 `W-04-11` 이 잘못된 대상을 집는다. **세 화면(`W-04-10`·`W-04-11`·`P-04-03`)의 진입이 풀린다.** 미결 5 · DS **신규 갭 0** · 공유 계약 후보 2. |
| v0.2 | 2026-08-28 | 구현팀 검토 요청(`#253`) 5건 반영. ⭐ **잔량이 계약에 착지했다** — `GET …/disposition-decisions` 응답에 `summary`(대상 합·결정 합·남은 수량·단위) 신설. 목록의 「수량」·「판정 진행」은 `Nonconformance` 의 `affectedQtyTotal`·`dispositionProgressCode` 로 세우고 `lots` 를 필수로 올렸다 — `status_code` 세 값으로는 미판정과 일부 판정을 가를 수 없음을 실측으로 확인. ⛔ **① 의 원천 필터를 걷었다** — 부적합에 원천 축이 없고 4값 중 둘(02 수리·01 자재)은 부적합을 만드는 경로 자체가 없다. 삭제가 아니라 **A-11** 로 물러난 수준을 목록 머리에 상시 표시하고 §8 미결 6 으로 세웠다. ⭐ **§5-5 신설** — 잔량은 보이되 막지 않는다(**A-9 ⓑ** · `W-03-03` §5-5 와 같은 형태). §5-1 활성 조건에서 잔량 항을 빼고 §6 의 409 에 코드와 `remainingQty` 를 붙였다. §4-B 의 「1 이상(CHECK)」을 **「0 초과」로 정정** — 물리는 처음부터 `> 0` 이었고 단위마다 소수 자릿수가 있다(`mdm.uom.decimal_scale`). ⛔ **판정 저장의 저장 충돌 토큰을 선택에서 필수로 조였다** — 이 화면은 관리웹 전용이라 그 완화의 조건에 해당하지 않았다. 토큰의 원천이 부적합 상세임을 §5-5 에 못박고, 그러려면 판정 저장이 `nonconformance.version_no` 를 올려야 한다는 사실을 미결 8 로 세웠다. 미결 5 → **8**. |

출처: design/wiki/screens/03/W-03-10-처분판정처리.md · 최종 대조일: 2026-08-25
