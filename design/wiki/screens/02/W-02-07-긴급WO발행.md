# W-02-07 · 긴급 W/O 발행

## §0. 요약

- **개요**: 관리웹 생산실행 화면. 허가된 권한자(생산관리자)가 P/O 없이 긴급 W/O를 직접 발행한다 — 발행 즉시 내부 P/O·생산계획이 자동 생성되고 BOM·Routing이 자동 전개되며, 별도 승인 없이 무배정으로 배포된다. 현장 실행은 `P-02-12`(POP)가 맡는다.
- **주요 데이터**: 작업지시(`production.work_order` — `production_plan_id` NOT NULL·`work_order_type_code`=긴급 고정), 발행 시 자동 생성되는 생산오더(`planning.production_order` — `erp_order_no` NULL)·생산계획(`planning.production_plan`).
- **핵심 액션**: 품목 검색·선택 → 수량·납기·사유 입력 → BOM·Routing 자동 전개 확인 → **발행·배포**(품목·수량·사유 입력 AND BOM·Routing 존재 AND 권한 있음일 때 활성).

> 작성일: 2026-08-05 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **§I-15(#76)의 진짜 당사자.** `P-02-12`는 긴급 W/O를 *쓰는* 화면이었고, 이 화면이 *만드는* 화면이다. **`W-02-01`에서 찾은 `erp_order_no` nullable이 해법 후보다.**

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-02-07` · 긴급 W/O 발행 |
| 프로그램 · 도메인 | **관리웹** · 02 생산실행 |
| 유형 | 입력 |
| 행위자 | **허가된 권한자**(생산관리자) |
| 신뢰도 | **확정** |
| IA 위치 | 관리웹 > 생산실행 > 계획·지시 |
| 범위 | 발행까지. **현장 실행은 `P-02-12`**(POP) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §4.2 **02-S-D** — 진입=긴급 생산 요청(**리셉션-only** · 메인 발신은 **의도 삭제 유지**) · 태스크 1 「**긴급 W/O 발행(P/O 비귀속·발행자 직접 전개·별도 승인 없음)** [점검 통제 우회 허용·**배정 없이 투입**]」 · 복귀=「긴급 직행 출하 → 04 도식」(창고 적치 생략 — ✓확정 QA #26) |
| **화면·IA** | 인벤토리 v1.5 `W-02-07` — 유형 입력 · 데이터=W/O · **「P/O 비귀속·공정/BOM 자동 전개·무배정 배포」** · REQ-PR-0014 |
| **요구** | ✅**REQ-PR-0014** 긴급 생산도 긴급 W/O로 처리(무지시 출고 아님) · **R15** W/O 유형=양산/**긴급**/개발품 · **R18~R20·R22**(02-SW설계사양서 §6) · ✓확정 2026-07-14 #4·#5·#7·#8 「긴급 W/O = 자동 출고요청 **제외**」 |
| **데이터** | ⛔ `production.work_order.production_plan_id` **NOT NULL** → `planning.production_plan.production_order_id` **NOT NULL** · `planning.production_order.erp_order_no` **nullable** — 실측 |

## §3. 레이아웃 (관리웹 — 구조 기술)

```
┌ 헤더 ─────────────────────────────────────────────────────────────────┐
│ 긴급 W/O 발행                                    ⚠ 긴급 · 승인 없음   │
├───────────────────────────────────────────────────────────────────────┤
│ ⛔ P/O 없이 W/O를 만들 수 없습니다 (모델 제약 — 이슈 #76)             │
│    발행 시 내부 P/O·계획이 자동 생성됩니다. 아래 §5-2 참조.           │
├───────────────────────────────────────────────────────────────────────┤
│ 《발행 정보》                                                          │
│   품목      [ ABC-123               🔍 ]                              │
│   수량      [    200    ] EA                                          │
│   납기      [ 2026-08-06 ]                                            │
│   사유      [ 고객 긴급 요청                              ]           │
│   유형      긴급 (고정)                                                │
├───────────────────────────────────────────────────────────────────────┤
│ 《자동 전개》                                                          │
│  ┌ BOM ─────────────────────┬ Routing ────────────────────────────┐   │
│  │ ✅ BOM-ABC-123 Rev 3      │ ✅ RT-ABC-123 Rev 2                 │   │
│  └───────────────────────────┴─────────────────────────────────────┘   │
│   순서  공정      수량    LOT수                                        │
│    10   사출       200      1                                          │
│    20   조립       200      1                                          │
│    30   검사       200      1                                          │
│  ⚠ 자원 배정 없이 배포합니다 (02-S-D 확정)                            │
│  ⚠ 자재 출고요청을 자동 발행하지 않습니다 — 부족 시 W-02-10           │
├───────────────────────────────────────────────────────────────────────┤
│ 액션   [ 발행·배포 ]                                                   │
└───────────────────────────────────────────────────────────────────────┘
```

## §4. 필드

### §4-A. 작업지시 `production.work_order`

| 라벨(한/베) | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| W/O 번호 | `work_order_no` | ✅ UNIQUE | MES 채번 |
| **생산계획** | `production_plan_id` | ✅ **NOT NULL** | ⛔ **§5-1** |
| 공정 | `routing_operation_id` | ✅ NOT NULL | 자동 전개 |
| 품목 · 수량 | `item_id` · `order_qty` | ✅ | `CHECK > 0` |
| **유형** | `work_order_type_code` | ✅ | **긴급 고정** — G-4(구조로 표현) §5-4 |
| 계획 자원 5종 | `planned_*` · `production_line_id` · `responsible_worker_id` | — | ⛔ **전부 비운다**(무배정 배포) |
| 기본 위치 3종 | `default_*_location_id` | — | 자동 전개 시 Routing에서 |
| 비고 | `remarks` | — | ⚠ **사유의 임시 거처** §5-3 |
| 상태 | `status_code` | ✅ | 발행 즉시 배포 상태 |

### §4-B. ⛔ 자동 생성되는 상위 2개

| 테이블 | 왜 만들어야 하나 | 무엇을 채우나 |
| --- | --- | --- |
| `planning.production_order` | `production_plan.production_order_id` **NOT NULL** | `erp_order_no` = **NULL**(§5-2) · `order_qty` · `due_date` · `bom_level` = 0 |
| `planning.production_plan` | `work_order.production_plan_id` **NOT NULL** | `plan_date` = 오늘 · `bom_id`·`routing_id` = 자동 전개분 |

## §5. 액션·상태

### §5-1. ⛔ §I-15 — P/O 없이 W/O를 만들 수 없다

**확정**(02-S-D): 「긴급 W/O 발행 — **P/O 비귀속**·발행자 직접 전개·별도 승인 없음」

**실측 체인**:

```
production.work_order.production_plan_id      bigint NOT NULL
    └ planning.production_plan.production_order_id   bigint NOT NULL
          └ planning.production_order
```

**두 단계가 다 NOT NULL이라 P/O 없이 내려갈 수 없다.**

> 이것이 **#76**이고, `P-02-12`가 「화면 존재 근거가 걸린다」로 **차단** 등급을 매겼다가 **사용자 지적으로 조정으로 하향**된 건이다. 해석 3안이 셋 다 화면을 바꾸지 않기 때문이었다.
> **이 화면은 만드는 쪽이라 파급이 더 직접적**이다 — 그럼에도 **화면 골격(품목·수량·납기·사유·자동 전개·발행)은 어느 해석에서도 같다.** **조정** 등급을 유지한다.

### §5-2. ★ `W-02-01`에서 찾은 것이 해법 후보다

`W-02-01` §5-2에서 확인했다:

```
planning.production_order.erp_order_no   varchar(100)     ← nullable
```

**R07이 「P/O = ERP 발행」이라고만 했는데 ERP 번호가 nullable이다.** 즉 **ERP에서 오지 않은 P/O를 데이터가 이미 허용한다.**

**해법 3안**

| 안 | 방법 | 판단 |
| :-: | --- | --- |
| **① 내부 P/O 자동 생성** *(권고)* | 발행 시 `erp_order_no = NULL`인 P/O + 계획을 서버가 한 트랜잭션으로 만든다 | ✅ **모델 변경이 필요 없다.** 「P/O 비귀속」을 **「ERP P/O에 귀속되지 않는다」**로 읽으면 확정과도 맞는다 |
| ② `production_plan_id` nullable화 | 모델 변경 | ⚠ 파급이 크다 — 계획 없는 W/O가 모든 조회에서 예외가 된다 |
| ③ 긴급 전용 테이블 | 모델 변경 | ⛔ 같은 업무에 테이블이 둘 |

**①을 권고한다.** 근거:

- **`bom_level`·`parent_production_order_id`가 이미 「ERP가 안 보낸 P/O」를 상정한다** — 반제품 오더는 MES가 전개해 만들 수 있는 것이고, 그것도 `erp_order_no`가 없다
- **B-8(트랜잭션 경계)**대로 P/O·계획·W/O 생성이 **한 트랜잭션 안**이다 — 중간에 끊기면 고아 P/O가 남는다

⚠ **①이면 화면이 사용자에게 이 사실을 보여야 한다.** 「P/O 비귀속」이라 배웠는데 P/O 번호가 생기면 혼란스럽다.

```
ⓘ 내부 관리번호 PO-…0099 가 생성됩니다. ERP P/O 에는 귀속되지 않습니다.
```

→ **#76에 이 해법을 코멘트로 제안**한다 — §8-1.

### §5-3. 「사유」를 담을 곳이 없다 — 5번째

**02-S-D는 사유를 명시적으로 요구하지 않지만**, 예외 흐름이고 **승인이 없으므로**(「별도 승인 없음」) 사유 기록이 유일한 통제 수단이다.

`work_order`에 사유 컬럼이 있는가:

```
completion_variance_reason_code   ← 미달·초과 완료 사유 (마감용)
remarks text                       ← 자유 텍스트
```

**발행 사유용 컬럼은 없다.** `completion_variance_reason_code`는 **마감 시 쓰는 것**이라 용도가 다르다.

**A-11 물러난 수준**: `remarks`에 담고, **사유별 집계 불가**를 명시한다.

⚠ **「기록하라는데 자리가 없다」 5번째**다(`W-02-06` §9-1의 4건 + 이것). 다만 **이 건은 확정 문구가 사유를 명시하지 않아** 앞의 넷보다 근거가 약하다 — **§I에 올리지 않고 미결로만** 둔다.

### §5-4. 「긴급 고정」·「승인 없음」·「출고요청 제외」를 **구조로 표현한다**

**G-4** — 확정이 「끌 수 없다」·「하지 않는다」일 때 기본값 on/off로 구현하면 확정이 무너진다.

| 확정 | 잘못된 표현 | 옳은 표현 |
| --- | --- | --- |
| 유형 = 긴급 | `[유형 ▾]` 드롭다운(기본 긴급) | **선택 불가 · 「긴급」 고정 표시** |
| 별도 승인 없음 | `[✓] 승인 요청` 체크박스 | **컨트롤 없음** + 안내 |
| 자동 출고요청 **제외** | `[ ] 출고요청 자동 발행`(기본 끔) | **컨트롤 없음** + 「부족 시 `W-02-10`」 안내 |
| 자원 배정 없음 | 배정 구획(비활성) | **구획 자체를 두지 않는다** + 안내 |

**G-5 적용** — 확정 번호를 문구에:

```
자재 출고요청을 자동 발행하지 않습니다 (2026-07-14 확정 #4·#5·#7·#8)
부족분은 「추가 자재 출고 요청」으로 정식 경유하세요 → W-02-10
```

### §5-5. 권한 — F-2의 **관리웹 축**이다

02-S-D: 「**허가된 권한자**(생산관리자)」

**F-2**: 권한 게이팅은 축이 둘이고 폼팩터마다 다르다 — **관리웹은 `app.role_permission.permission_code`**.

⚠ **권한 값 목록이 회신 E-9 대기**다(F-3 표). 화면은 **권한 판정을 서버에 맡기고** 없으면 **버튼 비활성 + 사유**(F-1 원리).

⚠ **이 화면은 승인이 없는 대신 권한이 유일한 통제다.** 권한 값이 정해지기 전에는 **누구나 발행할 수 있는 상태**가 된다 → §8-3.

### §5-6. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 품목 검색·선택 | 항상 |
| 수량·납기 입력 | 품목 선택됨 |
| 사유 입력 | 항상 — **화면 규칙으로 필수**(§5-3) |
| 자동 전개(암묵) | 품목 선택 시 BOM·Routing 조회 |
| **발행·배포** | 품목·수량·사유 입력 AND BOM·Routing 존재 AND **권한 있음** |

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| **권한 없음** | ⛔ 버튼 비활성 + 사유(F-1) — 숨기지 않는다 |
| BOM 없음 | ⛔ 차단 → 02-S-F(⚠ 미정 골격) |
| Routing 없음 | ⛔ 차단(`routing_id NOT NULL` — `W-02-02` §5-3) |
| Routing Rev 중복 | ⚠ 수동 선택(§I-7 · `W-02-02` §5-6) |
| 사유 미입력 | ⛔ 화면이 막는다 — DB는 안 막는다 |
| **P/O 자동 생성 실패** | ⛔ 전체 롤백 — **한 트랜잭션**(B-8) |
| 같은 품목의 긴급 W/O 중복 | ⚠ **막지 않는다** — 경고만 |
| **오프라인** | ⛔ 해당 없음 — 관리웹(C-5) |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 품목 검색 | `SearchInput` + `Select` | **c** — G-2 |
| 수량 입력 | `TextField`(number) | a |
| 납기 | **날짜 입력** | **c** — G-8 |
| 사유 | `TextField` | a |
| 자동 전개 표 | `Table` | a |
| 고정 값·제외 안내 | `AlertBanner`(info / warning) | a |
| 모델 제약 안내 | `AlertBanner`(warning) | a |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ~~⛔ **P/O 없이 W/O 불가**(§I-15 · #76)~~ | 상류↔하류 불일치 | ✅ **해소** | **2026-08-26 확정 — §5-2 ①안(내부 P/O 자동 생성)을 채택했다.** 계약에서 501 을 걷고, 화면이 계획 참조를 비우고 부르면 서버가 내부 P/O(`erp_order_no` 없음)와 계획을 한 트랜잭션으로 만든다. ⚠ ②안(계획 참조 nullable 화)을 검토했다가 물렀다 — 「계획이 없을 수 있다」가 **모든 W/O 조회의 예외**가 되어 비용이 하류 전체로 퍼진다. ①은 발행 트랜잭션 한 곳에 가둔다 |
| 2 | **발행 사유를 담을 컬럼이 없다** — 확정 문구가 사유를 명시하지 않아 §I에는 올리지 않는다 | 데이터 | **조정** | `remarks`로 시작 |
| 3 | **권한 값 목록 미정**(회신 E-9) — 승인이 없어 **권한이 유일한 통제**인데 비어 있다 | 회신 대기 | **조정** | ⚠ 운영 전 필수 |
| 4 | **내부 P/O 번호를 사용자에게 보일 것인가** | 설계 | **표시** | ✅ **확정 2026-08-26 — 보이되 설명 병기.** ①안을 채택했으므로 이 항목이 살아 있다. 「ⓘ 내부 관리번호가 생성됩니다. ERP P/O 에는 귀속되지 않습니다」 |
| 5 | **긴급 W/O 중복 발행 통제** — 같은 품목을 여러 번 발행하는 것을 막지 않는다 | 규칙 | **표시** | 경고만 |

**차단 0건 — 이 화면은 지금 지어도 된다.** ⚠ 2026-08-26 에 「(①안 전제로)」 단서를 걷었다 — **①안이 확정돼 전제가 아니라 사실이 됐다.**

## §9. 이 장에서 나온 공유 계약 후보

1. **§I 항목의 해법이 다른 화면에서 나올 수 있다** — #76(P/O 없이 W/O 불가)의 해법 후보가 `W-02-01`의 `erp_order_no` nullable이었다. **§I 대장에 「해법 후보」 열을 두어** 차수를 넘어 발견된 것을 잇는다 (§5-2)
2. **승인이 없는 화면에서는 권한이 유일한 통제다** — 02-S-D가 「별도 승인 없음」을 확정했으므로 §J를 적용하지 않는다. **그러면 F-2(관리웹 권한 축)의 값 목록이 운영 전 필수 조건**이 된다 — 미결 등급을 「조정」이 아니라 **운영 차단**으로 따로 표시할 필요가 있다 (§5-5)
3. **「하지 않는다」가 여럿 겹치는 화면은 안내가 화면의 절반이 된다** — 이 화면은 승인 없음·배정 없음·출고요청 없음·유형 고정 넷이 겹친다. **G-4(구조로 표현)를 넷 다 적용하면 컨트롤이 사라지고 안내만 남는다.** 안내를 배너로 흩지 말고 **한 구획에 모으는** 표준형이 필요하다 (§5-4)

## 근거·이력

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-05 | 초안 — **§I-15(#76)의 진짜 당사자**로서 발행 측을 다룸. **`W-02-01`에서 찾은 `erp_order_no` nullable이 해법 후보**임을 확인 — 「P/O 비귀속」을 「**ERP P/O에 귀속되지 않는다**」로 읽으면 **내부 P/O 자동 생성으로 모델 변경 없이 성립**한다(#76에 코멘트 제안). 확정 넷(유형 고정·승인 없음·출고요청 제외·무배정)을 **G-4대로 컨트롤 없이 구조로** 표현. **승인이 없어 권한이 유일한 통제**인데 값 목록이 회신 E-9 대기임을 확인 — 운영 전 필수. 미결 5건(**차단 0**) · 공유 계약 후보 3건 · 신규 DS 갭 0건. |

출처: uiux/2026-08-05-화면상세스펙-확대5차/W-02-07-긴급WO발행.md · 최종 대조일: 2026-08-25
