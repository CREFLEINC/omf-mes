# W-02-05 · W/O 마감·ERP 실적 송신

## §0. 요약

- **개요**: 관리웹에서 생산관리자가 W/O를 마감 판정(미달/정상/초과 3분류)하고, 미달 잔량의 이월/소멸을 선택한 뒤 ERP로 생산 실적을 송신하는 화면. 도메인 02(생산실행)의 출구이며 **마감 재오픈은 없다**(R83 — 정정은 상쇄식 보정 트랜잭션).
- **주요 데이터**: 작업지시(`production.work_order` — `completion_variance_reason_code`·`closed_at`), 생산실적(`production.production_result` — `corrects_production_result_id`), 생산LOT(`trace.lot.status_code`), ERP 연계 메시지(`integration.integration_message`).
- **핵심 액션**: W/O 선택 → 판정 확인(자동 3분류) → 미달 시 잔량 처분(이월/소멸)·사유 선택 → **마감**(되돌릴 수 없음) 한 번으로 ① 마감 시각 기록 ② 선발행 미등록 슬롯 자동 폐번 ③ ERP 실적 송신 메시지 적재(생산 실적 필수, 기타 항목 on/off, 검사 결과 비연계)가 함께 일어난다.
- ⭐ **마감 후 실적 정정도 이 화면이 갖는다**(2026-09-04 사용자 결정 — 전용 화면을 신설하지 않는다 · 화면 수 **117 유지**). 상태 필터를 「마감됨」으로 두면 《실적 목록》에서 원본을 골라 **정정**한다 — 원본을 고치지 않고 정정 실적을 **덧붙이고**(R83·`B-10`), 수불·기간업무시스템에 영향을 주는 **A급 정정은 승인을 거친다**(2026-08-31 사용자 확정 · `#61`).

> 작성일: 2026-08-05 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 도메인 02의 출구. **B-8(트랜잭션 경계)·C-4(outbox 동형)가 처음 「송신하는 쪽」에서 검증**된다. `W-06-10`은 받는 쪽이었다.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-02-05` · W/O 마감·ERP 실적 송신 |
| 프로그램 · 도메인 | **관리웹** · 02 생산실행 |
| 유형 | **판정·승인** |
| 행위자 | 생산관리자(+현장반장·전산담당) |
| 신뢰도 | **확정** |
| IA 위치 | 관리웹 > 생산실행 > 마감·실적 |
| 범위 | 마감 판정 + 이월/소멸 선택(**흡수**) + ERP 송신 + ⭐ **마감 후 실적 정정**(상신·기록 — 2026-09-04 신설). **재오픈 없음**(R83) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §4 **S12** — 「1. W/O 마감 처리 2. 마감 결과 선택 3. 분기: **「미달」→이월/소멸 선택**(`W-02-05` 흡수) / 「초과, 정상」→MES 4. **선발행 LOT 중 실적 미등록 슬롯 자동 폐번** 5. 계획 대비 실적 집계 6. 제조이력 확정 7. ERP 회신 — 생산 실적 **「개발품 제외, 필수 송신」**·「기타 항목 on/off 선택」(**강조 인라인 297:5490·5491 — 위반 금지**)」 |
| **화면·IA** | 인벤토리 v1.5 `W-02-05` — 유형 판정·승인 · 데이터=W/O마감·P/O · REQ-PR-0019 · **OR 3분류(미달/정상/초과)** |
| **요구** | **R80** 마감 결과 **3분류**(✅REQ-PR-0019) · **R81** 미달 잔량=관리자 **이월/소멸**·초과분=실적 인정(✓확정 QA #27) · **R82** 선발행 미등록 슬롯 **자동 폐번**(재사용 금지·**마감 시점 전용**)(✓확정 2026-07-29) · **R83** **마감 재오픈 금지**·정정=**상쇄식 보정 트랜잭션**·LOT 정정 3종(✓확정 2026-07-14) · **R84** MES→ERP 생산 실적 **필수**(개발품 제외)·기타 항목별 on/off·**검사 결과 비연계**(✓확정 QA #4·#6·#35) |
| **데이터** | `production.work_order`(**`completion_variance_reason_code`·`closed_at`**) · `production.production_result`(**`corrects_production_result_id`**) · `trace.lot`(`status_code`) · `integration.integration_message` — 실측 |

## §3. 레이아웃 (관리웹 — 구조 기술)

```
┌ 헤더 ─────────────────────────────────────────────────────────────────┐
│ W/O 마감·ERP 실적 송신                         마감 대기 7건          │
├───────────────────────────────────────────────────────────────────────┤
│ 필터  P/O[▾]  기간 [2026-08-01]~[2026-08-31]  상태[마감대기▾] [조회]  │
├───────────────────────────────────────────────────────────────────────┤
│ 《마감 대기 목록》                                                     │
│  ☐ W/O번호     공정   지시    양품    불량   손실   달성   판정        │
│  ☑ WO-…013    사출   3,000   2,850    120     30    95%   미달 ⚠     │
│  ☐ WO-…014    조립   3,000   3,000      0      0   100%   정상       │
│  ☐ WO-…015    검사   3,000   3,120      0      0   104%   초과       │
├───────────────────────────────────────────────────────────────────────┤
│ 《선택한 W/O — WO-…013》                                               │
│  ┌ 실적 집계 ────────────┬ 생산LOT ───────────────────────────────┐   │
│  │ 지시     3,000 EA      │ 선발행 6 슬롯                          │   │
│  │ 양품     2,850         │  실적 있음 5 → 유지                    │   │
│  │ 불량       120         │  실적 없음 1 → ⚠ 자동 폐번            │   │
│  │ 손실        30         │  ⚠ 「폐번」 값 미정 (#46)              │   │
│  │ ─────────────          │                                        │   │
│  │ 미달       150 EA      │                                        │   │
│  └────────────────────────┴────────────────────────────────────────┘   │
│  판정   미달 (3분류 자동)                                              │
│  잔량   ( ) 이월 — 새 W/O 로 넘긴다   ( ) 소멸 — 종결한다             │
│  사유   [ 자재 결품                                    ▾ ]            │
│  ⚠ 이 W/O 는 P/O 와 불일치 상태입니다 (poMismatch)                    │
├───────────────────────────────────────────────────────────────────────┤
│ 《ERP 송신》                                                           │
│   ✅ 생산 실적          필수 — 끌 수 없습니다 (✓확정 QA #35)          │
│   ⬜ 자재 투입 실적     [on/off]  ⚠ 설정 테이블 없음 (#66)            │
│   ⬜ 불량 실적          [on/off]  ⚠ 동                                │
│   ⛔ 검사 결과          비연계 — 목록에 없습니다 (✓확정 QA #4)        │
├───────────────────────────────────────────────────────────────────────┤
│ 액션   [ 마감 ]  ⚠ 재오픈 불가                                         │
└───────────────────────────────────────────────────────────────────────┘
```

**위 도면은 상태 필터가 「마감대기」일 때다** — 이 회차에 **마감 흐름은 한 줄도 바뀌지 않았다.**

⭐ **상태 필터에 값 하나를 더한다 — 「마감됨」**(`GET /production/work-orders` 의 `statusCode=CLOSED`(마감)). **정정은 이 축에서만 나타난다.** 「마감대기」를 고르면 화면은 위 도면 그대로다.

**상태 = 「마감됨」 — 마감 후 실적 정정** *(2026-09-04 신설 · 사용자 결정 — §5-4)*

```
┌ 헤더 ─────────────────────────────────────────────────────────────────┐
│ W/O 마감·ERP 실적 송신                         마감됨 24건            │
├───────────────────────────────────────────────────────────────────────┤
│ 필터  P/O[▾]  기간 [2026-08-01]~[2026-08-31]  상태[마감됨▾] [조회]    │
├───────────────────────────────────────────────────────────────────────┤
│ 《마감된 W/O》                                                        │
│  ☐ W/O번호     공정   지시    양품    불량   마감일    판정           │
│  ☑ WO-…013    사출   3,000   2,850    120   08-31    미달             │
│  ☐ WO-…011    조립   3,000   3,000      0   08-30    정상             │
├───────────────────────────────────────────────────────────────────────┤
│ 《실적 목록 — WO-…013》   정정할 「원본」을 고른다                    │
│  ☐ 순번  발생 시각        양품   불량   등록자   정정 관계            │
│  ☑  3    08-31 14:20      900     40   김○○   —                       │
│  ☐  2    08-31 10:05      950     50   김○○   ← 순번 3 이 정정함      │
│  ☐  1    08-31 06:10    1,000     30   박○○   —                       │
│  ⓘ 정정 실적도 같은 목록에 쌓인다 — 원본은 지워지지 않는다(B-3)       │
├───────────────────────────────────────────────────────────────────────┤
│ 액션   [ 정정 ]   ⛔ 재오픈 없음 — 정정은 「덧붙인다」                │
└───────────────────────────────────────────────────────────────────────┘
```

⭐ **《실적 목록》이 이 축의 새 구획이다** — 정정은 W/O 가 아니라 **실적 한 건**을 지목한다(`POST /production/production-results/{productionResultId}:correct` 가 원본 식별자를 **경로**로 받는다 · 실측). 그래서 「어느 실적을 고치는가」를 고르는 자리가 화면에 있어야 한다.

**[ 정정 ]을 누르면 — 정정 입력**

```
┌ 정정 입력 — 원본 WO-…013 순번 3 ──────────────────────────────────────┐
│ 양품  [    900 ] → [    860 ]    불량  [     40 ] → [     80 ]        │
│ 보류  [      0 ]   스크랩 [      0 ]   재작업 [      0 ]              │
│ 사유  [ 검사 재판정                                       ▾ ]         │
│ 비고  [                                                     ]         │
├───────────────────────────────────────────────────────────────────────┤
│ ⓘ 등급(A/B)은 «서버»가 정정 내용으로 판정한다 — 화면은 고르지 않는다  │
│                                     [ 취소 ]   [ 정정 저장 ]          │
└───────────────────────────────────────────────────────────────────────┘
```

**A급 판정이 돌아오면 — 승인 상신** *(정정 저장이 400 으로 막힌 뒤에 열린다 · §5-8)*

```
┌ 승인 상신 — 정정 저장이 400 으로 막혔다 ──────────────────────────────┐
│ ⚠ 수불·기간업무시스템에 영향을 주는 정정(A급)이라 승인이 필요합니다   │
│ 사유  [ 교대 종료 후 집계에서 양품 40 EA 과다가 확인됨        ]       │
│       ⓘ 결재함이 보는 «유일한» 업무 값입니다 — 무엇을 얼마나          │
│          고치는지 이 문장에 적습니다                                  │
├───────────────────────────────────────────────────────────────────────┤
│                                     [ 취소 ]   [ 상신 ]               │
└───────────────────────────────────────────────────────────────────────┘
```

> 도면 폭은 **기존 프레임 행(`┌`·`├`·`└`)의 실측 표시폭 73칸**에 맞췄다(동아시아 폭 W·F 를 2칸, 그 밖을 1칸으로 셈). ⚠ 기존 본문 행은 72~74칸으로 흔들린다 — 새로 넣은 36행은 **전부 73칸**이다.

## §4. 필드

### §4-A. 마감 `production.work_order`

| 라벨(한/베) | 출처 컬럼 | 타입 | 필수 | 검증·비고 |
| --- | --- | --- | :-: | --- |
| 지시 수량 | `order_qty` | `qty_t` | ✅ | 읽기 |
| **완료 시각** | `completed_at` | timestamptz | — | 생산 완료 |
| **마감 시각** | `closed_at` | timestamptz | — | **이 화면이 채운다** |
| **미달·초과 사유** | `completion_variance_reason_code` | `code_t` | — | ✅ **R80이 그대로 착지** §5-1 |
| 상태 | `status_code` | `code_t` | ✅ | 마감 상태 |

✅ **「이월/소멸」은 이미 계약에 있다** — `WorkOrderClose.remainderDispositionCode`(`CARRY_OVER`·`WRITE_OFF`)가 처분을, `completion_variance_reason_code`가 사유를 각각 담는다. §5-2.

### §4-B. 실적 집계 `production.production_result` (읽기)

| 항목 | 컬럼 | 비고 |
| --- | --- | --- |
| 양품·불량·보류·스크랩·재작업 | `good_qty`·`defect_qty`·`hold_qty`·`scrap_qty`·`rework_qty` | **5컬럼** — ⚠ **#60**(3원 ↔ 5컬럼) §5-6 |
| 정정 원본 | `corrects_production_result_id` | ✅ **R83 상쇄식 보정이 착지**(B-10) |
| 순번 | `result_sequence` | `UNIQUE(work_order, sequence)` |
| **발생 시각** | `occurred_at` | ⭐ 《실적 목록》의 정렬·식별 축 — 화면이 보낸다 |
| **기록 시각** | `recorded_at` | 서버가 남긴다 — 발생과의 격차가 「사후 입력」이다. ⛔ **임계값을 두지 않는다**(2026-08-31 사용자 확정 · `#61` — 시간 격차로 사유를 받지 않는다) |
| 등록자 | `worker_id` | 《실적 목록》 표시용(읽기) |

⭐ **《실적 목록》은 이 표를 그대로 그린다** — `GET /production/production-results?workOrderId=…`(실측 — 질의 축에 `workOrderId` 가 있다). 정정 실적도 같은 목록에 쌓이고 `corrects_production_result_id` 로 원본을 가리킨다.

### §4-C. ERP 송신 `integration.integration_message`

| 라벨 | 출처 컬럼 | 비고 |
| --- | --- | --- |
| 메시지 키 | `message_key` | UNIQUE — **클라이언트 생성**(C-4) |
| 인터페이스 | `interface_code` | ⚠ **FK 없는 맨 `code_t`**(§I-13 · #66) |
| 방향 · 대상 | `direction_code` · `target_type_code`+`target_id` | **A-10 다형** |
| 원문 | `payload` | jsonb |
| 상태 · 재시도 · 백오프 | `status_code` · `retry_count` · `available_at` | C-4 |

### §4-D. 정정·상신 본문 *(2026-09-04 신설)*

| 라벨 | 본문 칸 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| **정정 사유** | `ProductionResultCorrect.reasonCode` | ✅ | 값 목록은 `GET /mdm/code-values?codeGroupCode=PRODUCTION_RESULT_CORRECT_REASON`(공유계약 `G-32`) — ⛔ 화면이 값을 지어내지 않는다 |
| 비고 | `ProductionResultCorrect.note` | — | 자유 문장 |
| **정정 수량 5칸** | `goodQty`·`defectQty`·`holdQty`·`scrapQty`·`reworkQty` | — | ⭐ **이 다섯 칸이 등급 판정의 입력이다** — 서버가 본다(§5-8). 화면은 **등급을 보내지 않는다** |
| **상신 사유** | `ApprovalRequestCreate.reason` | ✅ | ⭐ **결재함(`W-CO-09`)이 보는 «유일한» 업무 값**이다(계약 실측 — `approval_request` 에 업무 값이 `reason` 하나뿐) ⇒ 화면이 **자리표시자로 형식을 유도한다**(`A-12` 보강 · `06-API-요구서-app공통승인.md` §5-1) |

⛔ **이 화면은 승인 «유형»도 «대상 유형»도 보내지 않는다** — 서버가 `approval_type_code=PRODUCTION_RESULT_CORRECT`·`target_type_code=PRODUCTION_RESULT` 를 채운다(계약 내부 주석 실측). 폐기 품의(`W-01-06`)가 유형을 안 보내는 것과 같은 형태다.

## §5. 액션·상태

### §5-1. R80 3분류가 **컬럼에 그대로 있다** — 다만 판정은 화면이 한다

```
work_order.completion_variance_reason_code   app.code_t
-- [M] 미달·초과 완료 사유 (2-1 FR-WO-032)
```

**주석이 R80을 그대로 적었다.** 「모델이 앞섬」 사례다.

**단 3분류 판정 자체는 DB가 하지 않는다** — 화면이 계산한다.

```
양품 합계 vs order_qty  →  미달 / 정상 / 초과
```

**A-9 등급**: 판정은 **표시**(ⓒ)이고 차단하지 않는다. **초과도 실적으로 인정**한다(R81).

⚠ **「정상」의 폭이 정해지지 않았다** — 2,999개는 미달인가 정상인가. **허용 오차가 미정**이다. `app.operation_policy`가 그런 파라미터를 담는 자리다(실측 — 「초과생산 정책」 주석) → §8-1.

### §5-2. ✅ 「이월/소멸」은 명시 축으로 되살렸다 — 정정(2026-08-30)

**R81**: 「미달 잔량 = 관리자 **이월/소멸**·초과분 = 실적 인정」(✓확정 QA #27)

`WorkOrderClose.remainderDispositionCode`가 `CARRY_OVER`(이월)·`WRITE_OFF`(소멸) 두 값을 명시 축으로 담는다 — 미달 판정일 때만 조건부 필수이고, 정상·초과는 이 칸을 비운다.

| 처분 | 실재 |
| --- | --- |
| **이월** | `remainderDispositionCode=CARRY_OVER` + 새 `work_order`(`parentWorkOrderId`로 원본을 가리킨다) |
| **소멸** | `remainderDispositionCode=WRITE_OFF` + `remarks`(선택 이유) — **집계 가능**: 코드값으로 셀 수 있다 |

⚠ **이전 버전은 `completion_variance_reason_code`(사유)와 처분을 혼동해 「담을 컬럼이 없다」로 잘못 적었다** — 실제로는 사유·처분이 각각 다른 컬럼(`reasonCode`·`remainderDispositionCode`)으로 이미 분리돼 있었다.

### §5-3. R82 자동 폐번 — 값이 없어 실행할 수 없다

**R82**: 「선발행 미등록 슬롯 **자동 폐번**(재사용 금지·**마감 시점 전용**)」

```
trace.lot.lifecycleStatusCode   app.code_t NULL(생산LOT 한정) DEFAULT 'WAITING'(안)
```

~~**「폐번」 값이 정의되지 않았다**~~ ⚠ **정정(2026-08-25 · 공유계약 §I-49)** — 2026-08-07에는 「폐번」을 `trace.lot.status_code`에 둔다고 확정했으나(#46), 그 컬럼은 이후 품질 판정 축(정상·불량·검사 대기·폐기)으로 확정돼(2026-08-21 이후 quality-03 계약) 두 확정이 부딪쳤다. **결정 10의 「한 필드에 섞지 않는다」를 지키는 방법은 컬럼을 나누는 것이다** — 「폐번」은 별도 신설 컬럼 `trace.lot.lifecycleStatusCode`(생명주기 축 — 대기/활성/폐번, 생산LOT 한정)에 둔다. §I-32가 이미 품질 판정(`lot.status_code`)과 보류 문서 진행(`lot_hold.status_code`)을 나눈 것과 같은 형태로, 세 번째 축이 갈린 것이다. **물리 컬럼 신설은 데이터모델 담당 소관.**

**화면 설계**: 폐번 대상을 **집계해 보이고**(§3의 「실적 없음 1 → 자동 폐번」) 값이 정해지면 서버가 전이한다. **값이 없으면 마감은 되고 폐번만 안 된다.**

⚠ **부작용** — 폐번이 안 되면 그 슬롯이 **`ACTIVE`인 채로 남아** 「실물 없는 활성 LOT」이 쌓인다. `W-01-07`(재고 조회)에 잡힐 수 있다 → §8-3.

✅ **취소 경로도 닫혔다** — `POST /production/work-orders/{workOrderId}:cancel` 이 「취소 상태로 옮기고 선발행된 생산LOT 슬롯을 자동 폐번한다」로 확정했다(**DR-007** 2026-08-12 · R27 과 같게 즉시 폐번). R82 는 여전히 「마감 시점 전용」이고, 취소는 별도 오퍼레이션이 받는다.

✅ **이 화면이 일으키는 두 전이(대기→활성 L1은 실적 등록 화면 소관 · 대기→폐번 L2는 이 화면의 마감)와 취소가 일으키는 전이(활성→폐번 L3)는 이제 `GET /trace/lot-lifecycle-events`에 기록된다**(2026-08-31 신설 — `LotStatusHistoryEvent`와 같은 형태의 별도 이력, 품질 판정 축과 섞지 않는다). 저장 테이블은 데이터 모델 담당에게 통지 — 기다리지 않는다. 화면 노출(조회 탭 추가)은 이번 범위 밖이다 — 기록 자리만 먼저 확보한다. 근거: `omf-mes-server#50` 정정 회신.

### §5-4. ★ R83 상쇄식 보정이 **착지해 있다** — B-10의 실증

**R83**: 「마감 **재오픈 금지** · 정정 = **상쇄식 보정 트랜잭션** · LOT 정정 3종」

```
production_result.corrects_production_result_id
    bigint REFERENCES production.production_result
-- [M] 정정·취소 시 원본 실적 참조 (2-3 FR-PR-033/034/045)
```

**B-10(정정·교체·대체 구분 · 3차 신설)의 `corrects_*` 형태가 그대로다.**

**화면 규칙**

1. **마감 버튼에 「재오픈 불가」를 명시**한다(G-5 — 확정 번호 병기)
2. 마감 후 정정은 **`POST /production/production-results/{productionResultId}:correct`(실적 정정)로만 기록한다** — 원본을 지목해야 `correctsProductionResultId` 가 남는다. ⛔ **`P-02-04` 의 일반 저장(`POST /production/production-results`)으로는 안 된다** — 그 본문 `ProductionResultCreate` 에 원본 참조 칸이 없어(실측) 정정 연결이 **빈 채로 저장되고 오류도 나지 않는다**(B-3·B-10 이 요구한 연결이 사라진다). ⭐ **이 액션은 «이 화면»이 갖는다**(✓확정 2026-09-04 사용자 · `#61`) — 상태 필터 「마감됨」 축의 《실적 목록》이 그 자리다(§3 · §5-8). ~~이 액션을 어느 화면이 갖는지는 아직 정해지지 않았다 → §8-5~~ «(정합주: 2026-09-04 확정 — 전용 화면을 신설하지 않고 이 화면에 얹는다. 화면 수 **117 유지**)». 이 화면은 재오픈을 제공하지 않는다
3. ✅ **마감된 W/O에 상쇄 실적을 넣을 수 있다** — R83 의 「재오픈 금지」와 부딪히지 않는다. **재오픈은 마감을 «되돌리는» 것**이고(`work_order.closed_at`·`status_code` 를 되돌린다) **정정은 실적을 «덧붙이는» 것**이라(`production_result` 에 행이 하나 는다) 손대는 개체가 다르다. 마감된 W/O 는 마감된 채로 남고, ERP 재송신은 상쇄 전표로 나간다(`design/raw/decisions/2026-07-15-마감후정정-운영방식-확정기록.md` §2-3 — **인식표·재고 수불·기간업무시스템 재송신을 자동 연동하고 사용자 수동 책임으로 남기지 않는다**). ~~⚠ 마감된 W/O에 상쇄 실적을 넣을 수 있는가가 미정이다 → §8-5~~ «(정합주: 2026-09-04 — 부르는 화면이 정해지면서 함께 닫혔다 · §8-5)»

⚠ **예외 E-12 ②가 「실행·승인 권한 주체·UNIERP 역분개성 재송신 수용 방식 잔여(U6)」**를 남겼다. ⭐ **앞의 하나는 닫혔다** — 실행은 이 화면(생산관리자)이고 승인 주체는 결재선 정의(`W-06-15`)가 정한다(`#72` 확정 2026-08-04 — 「관리자의 권한 설정으로 처리한다」). **뒤의 하나는 그대로 남는다**(§8-8).

### §5-5. ERP 송신 — B-8·C-4가 **보내는 쪽**에서 처음 검증된다

**B-8 트랜잭션 경계**: 업무 엔티티는 전부 안, **외부 시스템 호출은 밖**(outbox 적재까지가 안).

```
┌ 트랜잭션 ─────────────────────────────────┐
│ ① work_order.closed_at · status_code 갱신 │
│ ② 선발행 LOT 폐번 전이 (§5-3)             │
│ ③ integration_message INSERT (적재까지)   │
└───────────────────────────────────────────┘
   ④ 실제 ERP 호출                    ← 트랜잭션 밖 · 워커가 집어간다
```

**마감은 ERP 응답을 기다리지 않는다.** 프린터가 죽어도 발행 기록이 남는 것(K-4)과 같은 구조다.

**C-4(outbox 동형)** — `message_key`를 **화면이 만들어** 넣고, 재전송은 같은 키로. `W-06-10`(연계 현황)이 재처리 화면이다.

**화면 표시**: 마감 직후 상태는 **「송신 대기」**이지 「송신 완료」가 아니다.

```
마감되었습니다. ERP 송신은 별도로 진행됩니다.  [연계 현황 → W-06-10]
```

⚠ **C-7(저장 피드백 2등급 · 4차 신설)의 관리웹 판**이다 — 오프라인이 아니어도 **「즉시 성공」이 아닌 것**이 있다.

### §5-6. R84 송신 항목 — **필수·on/off·비연계 셋을 G-4로 표현**

| 항목 | 확정 | 화면 |
| --- | --- | --- |
| **생산 실적** | **필수**(개발품 제외 — QA #35) | **비활성 컨트롤 + 확정 번호**(G-4·G-5) |
| 기타 항목 | **항목별 on/off**(QA #6) | 토글 — ⚠ **설정 테이블이 없다**(§I-13 · #66) |
| **검사 결과** | **비연계**(QA #4) | ⛔ **목록에서 제외** — 꺼진 상태로도 안 보인다(G-4) |

~~⚠ **「개발품 제외」의 기준이 문서 간 갈린다**(§I-14 · **#70**)~~ → ✅ **닫혔다.**

> ⭐ **확정 2026-08-24 (사용자 · `#70`) — 「개발품 구분은 «품목»이다.」** 인벤토리의 **품목 플래그** 쪽이 맞고, 결정 14·개념모델의 「W/O 유형(양산/긴급/개발품)」은 **송신 제외 판정의 근거가 아니다**. 계약에 `Item.developmentItem`(참/거짓)을 앉혔다.

**남은 것은 컬럼 하나다** — `mdm.item` 에 담을 자리가 아직 없다(§I-6 — 품목 확장속성 4종 부재에 「개발품 플래그」 포함). 📨 **작업 통지**이고 우리를 막지 않는다.

**✅ 되살렸다(2026-08-31 · A-11 v4.6).** 화면은 **확정대로 개발품을 제외하고 송신한다** — 계약에 `Item.developmentItem` 이 이미 앉아 있으므로(사용자 확정 2026-08-24 · `#70`) 판정 축이 서 있다. ⛔ **「컬럼이 서기 전까지 전건 송신」으로 물러나 있던 것을 걷는다** — 담을 컬럼이 없다는 것은 **통지 사항이지 화면을 축소할 근거가 아니다**(`schema/data-model-boundary.md`). 서버가 값을 못 내리는 동안 무엇이 보이는지는 §8-6 에 적는다.

⚠ **강조 인라인 297:5490·5491이 「위반 금지」로 표시된 항목**이다. 물러난 수준을 반드시 명시한다.

### §5-7. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 조회 · W/O 선택 | 항상 |
| 잔량 처분 선택(이월/소멸) | **미달 판정일 때만** |
| 사유 선택 | 미달·초과일 때 |
| 송신 항목 토글 | ⚠ **설정 테이블 부재 — 비활성 + 사유**(G-2 · #66) |
| **마감** | 잔량 처분 선택됨(미달 시) AND 사유 선택됨 — ⚠ **되돌릴 수 없다** |
| **정정** | **상태 필터 = 「마감됨」 AND 《실적 목록》에서 실적 1건 선택됨** — 그 밖에는 비활성 + 사유(G-2·G-3). ⛔ 「마감대기」 축에는 이 버튼이 **보이지 않는다** |
| **정정 저장** | 정정 입력 대화상자에서 **정정 사유 선택됨**(`reasonCode` 필수 · 실측) — 수량 5칸은 고치는 칸만 채운다 |
| **승인 상신** | ⭐ **`:correct` 가 400(A급인데 승인된 요청이 없음)을 돌려준 뒤에만** 열린다 — 화면이 등급을 «먼저» 판정해 버튼을 가르지 않는다(§5-8) |

### §5-8. ⭐ 마감 후 실적 정정 — A/B 등급 흐름 *(2026-09-04 신설)*

**결정** — 실적 정정 오퍼레이션을 **이 화면이 부른다**(✓확정 2026-09-04 사용자 · `#61`). 전용 화면을 신설하지 않는다.

| 왜 이 화면인가 | 근거 |
| --- | --- |
| A급 정정은 **수불·기간업무시스템 영향**이라 생산관리자 판단이다 | 그 행위자가 **이미 이 화면의 행위자**다(§1 행위자 = 생산관리자) |
| §6 예외표가 **정정하려는 사람을 이미 맞이하고 있었다** | 「마감 후 정정 시도 → ⛔ 재오픈 없음 — 「상쇄 실적을 등록하세요」」인데 **갈 곳을 못 주고 있었다**(§6) |
| 현장 단말 둘은 **스스로 거부했다** | 작업실적 등록(`P-02-04`) **§6 예외표** 「이 화면 범위 밖 — 부르는 화면 미지정」(실측 175행) · 재작업 실적 등록(`P-04-03`) §5-6 「⛔ 두지 않는다 — 실적은 이력이다(`B-3`)」 |
| 신설은 **이 저장소에서 할 수 없다** | 화면 목록 정본이 불변층(`design/raw/…/screen-inventory-ia.md`)이다 — 화면 수 **117 유지** |

**등급 2분류**(`design/raw/decisions/2026-07-15-마감후정정-운영방식-확정기록.md` §2-4)

| 등급 | 무엇인가 | 승인 |
| :-: | --- | :-: |
| **A급** | 수불·기간업무시스템에 **영향을 주는** 정정 — 수량 조정 · 양품↔불량 재분류 · 자재LOT 오스캔 정정 · 지연 실적 편입 · 잔량 이월/소멸 선택 정정 | ✅ **붙인다**(2026-08-31 사용자 확정 · `#61`) |
| **B급** | 수불 **무관** — 불량코드 · 처분구분 · 비가동 사유 · 작업자/설비 귀속 정정 | ⛔ 없다 — 정정 이력만 남기는 경량 경로 |

⭐⭐ **등급은 «서버»가 판정한다 — 화면이 고르지도 보내지도 않는다.** 정정 본문(`ProductionResultCorrect`)이 담는 것은 **사유·비고·수량 5칸**뿐이고(실측), 그 **수량이 바뀌는가**가 서버가 보는 입력이다. 계약이 그렇게 못박았다.

```
POST /production/production-results/{productionResultId}:request-approval
  description  A급 보정(수불·기간업무시스템에 영향을 주는 정정)을 승인에 올린다.
               **등급은 서버가 정정 내용으로 판정한다** — 화면이 등급을 보내지 않는다.
```

⛔ **화면이 등급을 판정하면 서버와 갈린다** — `W-02-08` §5-2 가 마감 3분류에서 이미 정한 원칙이다(「조회에서 95%인데 마감에서 다르게 나오면 신뢰를 잃는다」). **같은 규칙을 두 곳에 두지 않는다.**

**흐름 — 두 갈래가 «같은 버튼»에서 갈린다**

```
《실적 목록》에서 원본 1건 선택 → [ 정정 ] → 정정 입력(수량·사유)
        │
        └─ [ 정정 저장 ]  POST …/{productionResultId}:correct
                 │
                 ├─ 201  B급이다 → 정정 실적이 덧붙는다. 끝
                 │
                 └─ 400  A급인데 승인된 요청이 없다
                          → 「승인 상신」 대화상자를 연다
                              [ 상신 ]  POST …/{productionResultId}:request-approval
                                   202  ApprovalRequestRef
                                        → 결재함(W-CO-09)이 받는다
                                        → 승인 뒤 이 화면에서 다시 [ 정정 저장 ] → 201
```

| 오퍼레이션 | 헤더 | 왜 |
| --- | --- | --- |
| `:request-approval` | `Idempotency-Key` **만** | ⛔ `If-Match` 없다 — **원본을 고치지 않으니 잠글 것이 없다**(`:correct` 와 같은 이유). ⛔ `X-Worker-No` 없다 — **관리웹**이 부른다(`D-5` 는 현장 단말·모바일 조항이다) |
| `:correct` | `Idempotency-Key` **만** | `X-Worker-No` 없음 — 관리웹 계정의 인증 주체로 귀속한다. `If-Match` 없음 — 원본 수정 없이 상쇄 레코드를 추가한다 |

**현재 계약은 `:correct` 에 `X-Worker-No` 를 받지 않는다.** 관리웹 계정의 인증 주체로 행위자를 귀속하며, 계약 정정을 기다리는 항목은 없다. 현장 단말·모바일의 일반 실적 쓰기에 적용되는 `D-5` 사번 규칙은 그대로다.

⚠ **상신 본문은 「정정 내용」을 실어 나르지 않는다** — `ApprovalRequestCreate` 는 `reason` 한 칸뿐이다(실측). 그래서 **승인자가 무엇을 승인하는지는 그 문장이 전부**이고(§4-D), 승인이 끝난 뒤 정정 값은 **이 화면에서 다시 입력해 저장한다.** ⛔ 상신이 정정을 «예약»하지 않는다.

⚠ **B급 정정 항목의 전수 목록은 아직 확정되지 않았다**(확정기록 §4 잔여 — 「§2-4의 B급 예시를 전수 목록으로 확정(내부 설계)」). 📄 **기록만** — 등급을 서버가 파생하므로 **화면은 막히지 않는다.**

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| **진행 중 세션이 있음** | ⛔ 차단 — 「작업 세션이 열려 있습니다」 |
| 실적 0건 | ⚠ 경고 + 마감 가능(전량 미달) |
| **P/O 불일치 상태** | ⚠ **경고 + 마감 가능** — `WorkOrder.poMismatch` 가 참이면 「이 W/O 는 P/O 와 불일치 상태입니다」를 보이고 마감은 막지 않는다(§3) ~~⛔ 확인할 근거가 없다(`W-02-06` §5-2) — 화면이 경고를 못 낸다 §8-2~~ «(정합주: 2026-08-30 정정이 §3·§8 만 고치고 이 줄로 돌아오지 않았다 — omf-mes#305 처리 중 발견)» |
| 미달인데 처분 미선택 | ⛔ 마감 비활성 |
| **폐번 값 미정** | ⚠ 마감은 되고 폐번만 안 된다(§5-3) |
| ERP 송신 실패 | ⚠ **마감은 유지** — 재전송은 `W-06-10`(§5-5) |
| **마감 후 정정 시도** | ⛔ 재오픈 없음 — ⭐ **갈 곳이 생겼다**: 상태 필터를 「마감됨」으로 바꾸고 《실적 목록》에서 원본을 골라 **[ 정정 ]** 한다(§3 · §5-8). ~~「상쇄 실적을 등록하세요」(G-3 경로 안내)~~ «(정합주: 2026-09-04 — 그 안내는 «어디서» 등록하는지를 말하지 못했다. 부르는 화면이 이 화면으로 정해져 실제 동선이 됐다 · `#61`)» |
| **결재선이 없다** | ⛔ **상신 거부** — `400` `code=ROUTE_NOT_FOUND`. 「이 정정을 올릴 결재선이 없습니다 — 결재선 정의(`W-06-15`)에서 「실적 정정」 결재선을 먼저 등록하십시오」. ⛔ **상신할 곳이 없는 요청을 만들지 않는다**(공유계약 `J-4`) |
| **결재선이 둘 이상 걸린다** | ⛔ `400` `code=ROUTE_AMBIGUOUS` — **서버가 임의로 하나를 고르지 않는다**(`B-12`). 화면은 결재선을 고르는 입력을 두지 않는다 |
| **이미 진행 중인 승인 요청이 있다** | ⛔ `400` — **한 실적에 살아 있는 요청은 하나다.** 「이 실적은 이미 승인 진행 중입니다 — 결재함(`W-CO-09`)에서 확인하십시오」. ⭐ **반려된 요청은 진행 중이 아니라 다시 상신할 수 있고, 그때는 새 요청이 만들어진다**(`J-6` — 번복은 새 요청이다) |
| **A급인데 승인 전 정정 시도** | ⛔ `400` — 정정이 저장되지 않는다. 화면은 **승인 상신 대화상자를 연다**(§5-8). ⚠ **화면이 미리 막지 않는다** — 등급은 서버가 판정한다 |
| **B급인데 상신 시도** | ⛔ `400` — 「이 정정은 승인이 필요하지 않습니다」. ⭐ 정상 동선에서는 일어나지 않는다(상신 대화상자는 `400` 을 받은 뒤에만 열린다) |
| **저장 충돌**(B-1) | `409` + 다시 불러오기 |
| **오프라인** | ⛔ 해당 없음 — 관리웹(C-5) |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 필터 바 | `SearchInput` + `Select` + 기간 | **c** — G-2 · G-8 |
| 마감 대기 목록 | `Table` + **행 선택(단건)** — 고른 한 건이 §3 상세 패널에 뜬다 | a |
| 실적 집계 | `StatCard` | a |
| 판정·처분 선택 | `RadioGroup` | a |
| 송신 항목 | `Switch` 또는 `Checkbox` + 비활성 | a · **G-2·G-4** |
| 필수·비연계 안내 | `AlertBanner`(info) | a |
| **마감 확인** | `Dialog`(되돌릴 수 없음 경고) | a |
| **실적 목록**(마감됨 축) | `Table` + **행 선택(단건)** — 마감 대기 목록과 같은 컴포넌트다 | a |
| **정정 입력** | `Dialog` + `NumberInput` 5 + `Select`(사유) + `TextArea`(비고) | a · **G-2**(사유 미선택 시 저장 비활성) |
| **승인 상신** | `Dialog` + `TextArea`(사유 · 자리표시자로 형식 유도) | a · **A-12 보강** |
| 승인 필요 안내 | `AlertBanner`(warning) — `400` 을 받은 자리에 뜬다 | a · **G-1·G-3** |

**신규 DS 갭 0건** — 정정·상신도 기존 컴포넌트 조합으로 선다(2026-09-04 재확인).

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | **「정상」의 허용 오차** — 2,999개는 미달인가. `operation_policy`가 담을 자리 | 규칙 | **조정** | 정확히 일치=정상으로 시작 |
| 2 | ✅ **완전 해소(2026-08-30)** — `remainderDispositionCode`(이월/소멸 명시 축) 확인, `WorkOrder.poMismatch`로 불일치 확인 가능 | — | **해소** | 없음 — 계약 재확인으로 종결 |
| 4 | **취소 시 선발행 LOT 회수** — R82는 「마감 시점 전용」이라 취소를 커버하지 않는다 | 프로세스 | **조정** | ✅ **종결 — DR-007**(2026-08-12) · `:cancel` 이 전건 즉시 폐번한다 |
| 5 | ✅ **완전 해소(2026-09-04)** — ~~마감된 W/O에 상쇄 실적을 넣을 수 있는가 · `:correct` 를 부르는 화면 미지정~~ | 규칙 | **해소** | 종결 — ① 넣을 수 있다(재오픈은 마감을 되돌리는 것, 정정은 실적을 덧붙이는 것 · §5-4 규칙 3) ② **`:correct` 는 이 화면이 부른다**(✓확정 2026-09-04 사용자 · `#61` — 전용 화면 신설 없음 · 화면 수 117 유지 · §5-8) |
| 6 | ✅ **해소(2026-08-31)** — 판정 축이 확정됐고(「개발품 구분은 품목이다」 · 사용자 2026-08-24 · `#70`) 계약에 `Item.developmentItem` 이 앉았다. 화면은 확정대로 제외 송신한다. 📨 `mdm.item` 컬럼 신설은 **작업 통지**이지 우리를 막지 않는다 | **상류↔하류 불일치** | **해소** | 종결 — 컬럼은 통지 |
| 7 | **송신 항목 on/off 설정 테이블 부재**(§I-13 · #66) | 데이터 | **조정** | #66 종료(2026-08-25 · PR #236 · 계약 완전 흡수) — `GET/PUT /integration/outbound-item-settings` 와 `/integration/interface-definitions` 가 그 답이다 ✅ **해소 2026-09-02** — `GET/PUT /integration/outbound-item-settings` 가 계약에 있다 |
| 8 | ⚠ **고객사 기간업무시스템(UNIERP)이 역분개성 상쇄 전표를 «어떤 형태로» 받는가** — A급 정정이 나가는 재송신의 상대편 규격 *(2026-09-04 신설)* | 프로세스 | **조정** | ⛔ **설계 소관 밖 — 상대 시스템 협의다**(확정기록 §4 잔여 · `#61` 이 이 한 가지로 남았다). 우리 쪽은 서 있다 — 정정은 `:correct` 로 기록되고 재송신은 `integration_message` 로 적재된다(§5-5 · C-4). **형태가 정해지면 `payload` 규약만 는다** |

**차단 0건 — 이 화면은 지금 지어도 된다**(물러난 수준 셋을 명시한 채로). ⭐ **미결 8도 차단이 아니다** — 상쇄 전표의 «형태»는 outbox 원문(`payload`)의 문제이고, 화면·계약·상신 동선은 그와 무관하게 선다.

## §9. 이 장에서 나온 공유 계약 후보

1. **「즉시 성공이 아닌 것」은 오프라인만의 문제가 아니다** — 마감은 되고 ERP 송신은 outbox로 간다. C-7(저장 피드백 2등급)이 **관리웹에도 적용**된다. **외부 시스템 호출이 딸린 저장은 「완료」와 「접수」를 구분해 보인다** (§5-5)
2. **「사유」와 「처분」은 다른 컬럼이다** — `completion_variance_reason_code`는 왜(자재 결품)이고 이월/소멸은 무엇을 할지다. **한 컬럼에 둘을 담으면 집계가 무너진다** — 확정이 둘을 함께 요구하면 스펙이 나눠서 확인한다 (§5-2)
3. **「위반 금지」로 강조된 확정이 물러날 때는 특별히 표시한다** — R84의 「개발품 제외, 필수 송신」은 도식에 **강조 인라인(297:5490·5491)**으로 표시된 항목인데 판정 근거가 없어 전건 송신으로 물러난다. **A-11의 물러난 수준에 「원문의 강조 여부」를 함께 적는다** (§5-6)
4. ⭐ **「승인이 필요한가」를 화면이 판정하지 않는다 — 서버가 거절로 알려 준다** *(2026-09-04 신설)*. 실적 정정의 A/B 등급은 **정정 내용에서 파생**되므로 화면이 미리 가르면 규칙이 두 곳에 생기고 반드시 갈린다. 화면은 **저장을 시도하고 `400` 을 받은 뒤 상신 동선을 연다.** `W-02-08` §5-2 가 마감 3분류에서 정한 「판정은 서버가 한 곳에서 낸다」의 **승인 축 판**이다 (§5-8)

## 근거·이력

### §8 해소된 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 3 | ~~**폐번 값 미정**(#46)~~ ✅ **축 확정(공유계약 §I-49, 2026-08-25)** | **확정** | — | 생명주기 축(신설 `trace.lot.lifecycleStatusCode`)에 들어간다 — `trace.lot.status_code`(품질 판정 축)와는 다른 컬럼이다. 물리 컬럼 신설·영문 값 표기 확정은 데이터모델 담당 대기(#46) |

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| **v0.2** | **2026-09-04** | ⭐⭐ **마감 후 실적 정정을 이 화면에 얹었다**(사용자 결정 · `#61`) — 전용 화면을 **신설하지 않는다**(화면 수 **117 유지**). ① §3 에 **상태 필터 「마감됨」 축**을 신설하고 도면 셋을 더했다(마감된 W/O 목록 · **《실적 목록》** · 정정 입력 · 승인 상신). ⛔ **「마감대기」 축의 도면은 한 줄도 고치지 않았다** — 마감 흐름은 그대로다 ② §4-D 신설(정정·상신 본문) ③ §5-4 규칙 2·3 정정 — 「어느 화면이 갖는지 미정」과 「마감된 W/O 에 상쇄 실적을 넣을 수 있는가 미정」이 **둘 다 닫혔다** ④ **§5-8 신설 — A/B 등급 흐름.** ⭐ **등급은 서버가 정정 내용으로 판정하고 화면은 고르지도 보내지도 않는다**(계약 설명문이 못박았다). A급은 `:request-approval` → 결재함(`W-CO-09`) → `:correct` ⑤ §6 에 예외 5행 추가 — 결재선 부재(`ROUTE_NOT_FOUND`)·중복(`ROUTE_AMBIGUOUS`)·진행 중 요청·승인 전 정정·B급 상신. ⭐ **「마감 후 정정 시도 → 상쇄 실적을 등록하세요」가 처음으로 «갈 곳»을 갖는다** ⑥ §7 에 DS 4행(신규 갭 여전히 0건) ⑦ §8 미결 5 **해소** · **미결 8 신설**(UNIERP 상쇄 전표 수용 형태 — 상대 시스템 협의라 설계 소관 밖). **차단 0건 유지.** |
| v0.1 | 2026-08-05 | 초안. **B-8·C-4가 「보내는 쪽」에서 처음 검증**됨 — 마감은 ERP 응답을 기다리지 않고 outbox 적재까지가 트랜잭션. **C-7(저장 피드백 2등급)이 관리웹에도 적용**됨을 확인. **R80 3분류·R83 상쇄식 보정이 컬럼에 그대로 착지**(`completion_variance_reason_code`·`corrects_production_result_id` — 모델이 앞섬 2건). **「이월/소멸」 처분을 담을 컬럼 부재** 발견 — 이월은 새 W/O로 표현되나 소멸이 안 남는다(**§I 후보 · `W-02-06` 건과 묶음**). **「개발품 제외」 판정 근거가 없어 전건 송신으로 물러남**(§I-14·§I-6 · #70) — **도식에 「위반 금지」로 강조된 항목**이라 물러난 수준을 특별 표시. 미결 7건(**차단 0**) · 공유 계약 후보 3건 · 신규 DS 갭 0건. |

출처: design/wiki/screens/02/W-02-05-WO마감ERP실적송신.md · 최종 대조일: 2026-08-25
