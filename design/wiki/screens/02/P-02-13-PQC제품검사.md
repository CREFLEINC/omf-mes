# P-02-13 · PQC 제품 검사·검사 결과 입력

## §0. 요약

- **개요**: POP(1024×768 터치) 화면. 작업자(자주검사)·품질담당(초중종 입회·판정)이 검사 항목별 결과를 입력하고 종합 판정을 내린다. 단말×공정 플래그 `can_input_inspection`이 이 화면에서 **처음 실증**된다. 02(생산실행)와 03(품질관리)의 경계에 서며, Lot Status 전이·판정유형 목록 자체는 03 거버넌스다.
- **주요 데이터**: `quality.inspection_request`(검사 의뢰, 대개 읽기 전용), `quality.inspection_result`(검사 결과 — 합격·불합격·보류 수량에 `ck_inspection_result_qty` CHECK 제약), `inspection_measurement`, `defect_record`, `mdm.terminal_process`(`can_input_inspection`).
- **핵심 액션**: 항목별 판정·측정값 입력 → 수량(검사·합격·불합격·보류) 입력 → 종합 판정 선택(합격/불합격/보류, 목록 조회) → 불합격 시 처분(재작업 가능/폐기, **잠정** — 확정은 불량창고 입고 후) → 임시 저장 또는 검사 확정(합계 일치·전 항목 판정·종합 판정·권한 플래그 충족 시).

> 작성일: 2026-08-05 · 서식: `00-화면스펙서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 유일한 **POP** 화면이고 **02와 03의 경계**에 선다. `can_input_inspection`이 **처음 실증**되는 화면이다.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `P-02-13` · PQC 제품 검사·검사 결과 입력 |
| 프로그램 · 도메인 | **POP**(1024×768 터치) · 02 생산실행 *(담을 내용은 **03 소관** — §8-8)* |
| 유형 | **판정·승인** |
| 행위자 | 작업자(자주검사) · 품질담당(초중종 입회·판정) |
| 신뢰도 | **확정** |
| IA 위치 | POP > 생산 > 검사 |
| 범위 | 검사 수행·결과 입력·판정. **Lot Status 전이·판정유형 목록은 03 거버넌스** |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §4.2 **02-S-E** — 태스크 「1. 제품 검사 2. 검사 결과 입력 **[재작업/폐기 2분류 · 불량창고 우선 입고]**」 · 복귀 「**「양품」→메인 실적 등록** / **「재작업가능」→02-S-I 발신** / 폐기 갈래=03 소관 미표기」 · **분업(Q1)**: 「검사 수행·결과 입력 **지점**=02-S-E · **검사실적 소유·판정 유형·Lot Status 전이·검사방식·합격판정개수·재검사 Hold gating = 03 거버넌스**」 |
| **화면·IA** | 인벤토리 v1.5 `P-02-13` — 유형 판정·승인 · 데이터=검사실적·생산불량LOT·불량·비가동기록 · 배지=**POP(결정 12·정책 2 예외)** · REQ-OA-0003 · **「담을 내용은 03 소관 — §8-8」** |
| **요구** | ✓확정 **QA #4** PQC = **opt-in**(품목·공정 단위 생략 설정 + Routing 미명시 시 항상 생략) · ✓확정 **QA #13** 양품/불량/**손실** 3원 · ✅**REQ-PR-0025** 재작업 가능 vs 폐기 구분 — **판정은 불량창고 입고 후** · ✅ **회신 E-3 종결**(2026-08-07 — 판정 3값 · LOT 상태 4값) |
| **데이터** | `quality.inspection_request`(**`inspection_plan_version_id` NOT NULL** *(물리 실측 — 화면·계약은 이것을 따르지 않는다 §5-2)* · **`coverage_from_at`/`_to_at`**) · `inspection_result`(**`ck_inspection_result_qty`** · `previous_result_id`·`reinspection_reason_code`) · `inspection_measurement` · `defect_record` · `mdm.terminal_process`(**`can_input_inspection`**) — 실측 |

## §3. 레이아웃 (POP — 1024×768)

**E-1 세로 예산**: 헤더 64 + 본문 616 + 액션바 88 = 768 (슬랙 0).

```
┌ 1024 ────────────────────────────────────────────────────────────────────┐
│ 제품 검사   WO-…013 · ABC-123 · 사출        LOT …0031   POP-L1 ●     64 │
├───────────────────────────────┬────────────────────────┬─────────────────┤
│ 《검사 항목》            464  │ 《결과 입력》  312     │ 《숫자 키패드》 │
│  기준 IP-ABC-123 v2           │                        │  216 · D-4      │
│  샘플 30  ⚠ 비율? 수량?       │  검사 수량 [  30 ] EA  │                 │
│                               │  합격     [  28 ]      │  [7] [8] [9]    │
│  ① 외관 (육안)                │  불합격   [   2 ]      │  [4] [5] [6]    │
│     기준 스크래치 없음        │  보류     [   0 ]      │  [1] [2] [3]    │
│     [ 합격 ] [ 불합격 ]       │  ────────────────────  │  [0] [C] [←]    │
│                               │  합계  30 / 30  ✅     │                 │
│  ② 치수 A (캘리퍼)            │                        │  ⓘ 포커스한 칸  │
│     기준 12.00 ± 0.05         │  종합 판정             │    들어간다     │
│     측정 [  12.02  ] mm ✅    │   [ 합격 ▾ ] ⓘ 코드    │                 │
│                               │   합격 / 불합격 / 보류 │  좌단 측정값도  │
│  ③ 치수 B (캘리퍼)            │                        │    이 패드로    │
│     기준 8.00 ± 0.03          │  불합격 처분           │                 │
│     측정 [   8.05  ] ⛔       │   ( ) 재작업 가능      │  D-4 · 배치는   │
│                               │   ( ) 폐기             │   화면이 정한다 │
│  진행 2 / 3                   │   ⚠ 입고 후 판정(§5-8) │                 │
├───────────────────────────────┴────────────────────────┴─────────────────┤
│ 액션바        [ 임시 저장 ]            [    검사 확정    ]  72       88  │
└──────────────────────────────────────────────────────────────────────────┘
```

⚠ **본문 616 · 실질 584**(패딩 16×2). 좌단 항목 목록은 스크롤.

### §3-1. 숫자 키패드는 **우단 《결과 입력》 안에 놓는다** ⭐ **[2026-09-03 지정 · `omf-mes#286`]**

§5-9 액션 표와 §7 디자인 시스템 매핑 표가 「측정값·수량은 **화면 내장 숫자 키패드**로 넣는다」를
적어 두었는데 **도면에 그 블록이 없었다.** 숫자 키패드 조항(D-4)이 「**배치 — 부품이 정하지 않는다.**
**화면이 놓는다**」로 못박았으므로, **화면이 놓지 않으면 아무도 놓지 않는다.**

```
우단 544  =  《결과 입력》 312  +  간격 16  +  《숫자 키패드》 216
```

**왜 여기인가 — 셋을 재고 골랐다.**

| 근거 | 실측 |
| --- | --- |
| **가로가 선례보다 넓다** | `P-02-04`(작업실적 등록)가 **우단 392** 안에 키패드 블록과 입력 칸을 함께 넣었다(그 화면 §3-2 · 51·60행). 이 화면 우단은 **544** 라 키패드를 빼고도 **312** 가 남는다 — 선례의 잔여 **176** 보다 136 넓다 |
| **세로 예산을 건드리지 않는다** | 키패드가 **기존 구획 «안»**에 들어가므로 헤더 64 + 본문 616 + 액션바 88 이 그대로다. ⛔ 「슬랙 0 이라 자리가 없다」는 근거가 되지 못한다 — **키패드를 그린 현장 단말 4화면 중 3장**(`P-02-01`·`P-02-04`·`P-05-01`)이 **스스로 「슬랙 0」을 적은 채로 그렸다**(2026-09-03 전수 실측 — 키패드를 쓴다고 적은 현장 단말 7화면 중 그린 것 4 · 안 그린 것 3=`P-02-13`·`P-04-01`·`P-04-03`) |
| **좌단 측정값도 이 패드가 받는다** | D-4 골격이 「**포커스된 필드에 연동되는 입력 버퍼**」다. 패드를 두 벌 두지 않는다 — 좌단 《검사 항목》의 측정값 칸도 이 패드로 넣는다 |

⚠ **216 은 「표준 치수」가 아니다.** 저장소 전체에서 `216 × 292` 가 나오는 곳은 **두 곳뿐이고 둘 다**
**`P-02-04` 것**이다(그 화면 60행 · 그 화면의 레이아웃 검증 HTML 79행). 키 64 · 간격 12 로 **그 화면이**
**자기 화면에서 계산한 값**이다 — D-4 의 제약 표는 오히려 「**키 크기 … 크기를 부품이 정하지 않는다**」로
적혀 있다. 도면의 216은 자리 배분이며 현재 부품 치수는 아래 고정 원천 산식으로 확인한다.

**현재 부품 확인(2026-09-05 · #444)** — `NumberPad`는 실재한다. 아래는 고정 커밋 `b28c91878d4e9276abb9b20f8c4d921f476274e7`의 [CSS](https://github.com/CREFLEINC/design-system-v2-webui/blob/b28c91878d4e9276abb9b20f8c4d921f476274e7/src/components/NumberPad/NumberPad.module.css)·[토큰](https://github.com/CREFLEINC/design-system-v2-webui/blob/b28c91878d4e9276abb9b20f8c4d921f476274e7/styles/web-tokens.css)으로 계산한 치수이며 화면 렌더 실측은 아니다. [TSX](https://github.com/CREFLEINC/design-system-v2-webui/blob/b28c91878d4e9276abb9b20f8c4d921f476274e7/src/components/NumberPad/NumberPad.tsx)의 `allowDecimal` 또는 `onConfirm`이 있으면 5행이고 둘 다 없을 때만 4행이다.

| 지원 size | 키 높이 | 4행 너비×높이 | 5행 너비×높이 |
| --- | --- | --- | --- |
| `lg` | 48픽셀 | 208×216 | 208×272 |
| `xl` | 60픽셀 | 256×264 | 256×332 |

산식은 너비=키 높이×4+8×2, 높이=키 높이×행수+8×(행수−1)이다. **D-4 수량·측정값형은 소수를 허용하므로 이 화면은 5행 기준으로 검산한다.** 확인행을 숨겨도 소수 행이 남으며, 소수를 금지해 공간 문제를 풀지 않는다. 208×216·256×264는 4행 조건의 값일 뿐 최종 화면 치수가 아니다.

**부품 실재와 요구 크기 충족은 별개다.** `lg`·`xl` 키는 모두 D-4의 **64픽셀** 요구에 미달한다. E-3 폼 핵심 입력의 **72픽셀** 등급은 별도 조건이다. 72픽셀 토큰은 있지만 NumberPad는 md/lg/xl만, [TextField](https://github.com/CREFLEINC/design-system-v2-webui/blob/b28c91878d4e9276abb9b20f8c4d921f476274e7/src/components/TextField/TextField.module.css)는 sm/md/lg/xl만 지원해 2xl 변형은 없다. 이 화면의 최종 DS size는 미확정이며, 지원하지 않는 size를 지정하지 않는다. 향후 부품 변경 후 다시 검산하되 TextField 변경이 NumberPad 치수를 반드시 바꾼다고 단정하지 않는다.

**확인행은 현재 편집 필드의 입력만 확정한다.** 업무 저장·검사 확정 액션을 실행하지 않는다. 패드가 편집하는 필드를 분명하게 표시하며 읽기 전용·판정 선택 필드에는 연결하지 않는다. `max`·`maxLength`는 임의로 지정하지 않고 기존 업무 검증을 보존한다.

**우단 544 안의 기존 배분과 좌단 스크롤은 유지한다.** 현행 `xl` 너비256은 키패드 자리216보다40 넓다. 기존 조정 정책대로 결과 입력312에서40을 덜면272가 되지만, 이것만으로 적합을 선언하지 않는다. `lg`208도 가로에는 들어가지만 키 높이48이라 터치 요구를 충족하지 못한다.

**§8 미결 10 유지** — 우단 실질584 안의 행별 높이, 소수·확인행을 포함한 세로 배분, 우단 추가 스크롤 허용 여부를 확정하지 않았다. 기존 좌단 항목 목록 스크롤 외에 독립 스크롤 구획을 새로 지정하지 않는다. 최종 size와 행별 정책이 정해진 뒤 재검산한다.

## §4. 필드

### §4-A. 검사 의뢰 `quality.inspection_request` (읽기 — 대개 자동 생성)

| 라벨(한/베) | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| 의뢰번호 | `inspection_request_no` | ✅ UNIQUE | |
| 유형 | `inspection_type_code` | ✅ | **PQC** |
| **검사 기준 버전** | `inspection_plan_version_id` | — | ⭐ **비어 있을 수 있다** — 기준 없이 만들어진 의뢰다. 그때 화면은 두 번째 갈래로 간다 §5-2. ⚠ 물리 모델은 아직 `NOT NULL` 이나 **그것으로 화면을 정하지 않는다**(`design/schema/data-model-boundary.md`) |
| 대상 | `target_type_code` + `target_id` | ✅ | **A-10 다형**(16쌍 중 하나) |
| 품목 · LOT · W/O · 실적 | `item_id` · `lot_id` · `work_order_id` · `production_result_id` | 일부 | LOT·W/O·실적은 nullable |
| 대상 수량 | `target_qty` | ✅ | `CHECK > 0` |
| **적용 생산구간** | `coverage_from_at` · `coverage_to_at` | — | ✅ **모델이 앞섬** §5-5 |

### §4-B. 검사 결과 `quality.inspection_result`

| 라벨 | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| 결과번호 | `inspection_result_no` | ✅ UNIQUE | |
| **회차** | `inspection_round` | ✅ CHECK > 0 | `UNIQUE(의뢰, 회차)` — **K-1과 같은 형태** |
| **검사 수량** | `inspected_qty` | ✅ CHECK > 0 | |
| **합격·불합격·보류** | `accepted_qty` · `rejected_qty` · `held_qty` | ✅ | ⛔ **`CHECK (합 = inspected_qty)`** — **A-3** §5-3 |
| **종합 판정** | `overall_judgment_code` | ✅ **NOT NULL** | ✅ **`합격`·`불합격`·`보류` 3값** §5-4. ⭐ **목록을 받는 법** — `GET /mdm/code-values?codeGroupCode=INSPECTION_RESULT_OVERALL_JUDGMENT`. 코드값 **`ACCEPTED`·`REJECTED`·`HELD`** 로 ⬆ 수량 세 칸과 **같은 이름**이다. ⚠ 채번 식별자(`codeGroupId`)를 하드코딩하지 않는다(구현팀 질의 #179) |
| 검사자 · 시각 | `inspector_id` · `inspected_at` | ✅ | ⛔ **검사자는 화면이 보내지 않는다 — 서버가 채운다.** 현장 단말은 사번 귀속 헤더(`X-Worker-No`)의 사번에서 작업자를 푼다(귀속 조항 D-5) — 구현팀 질의 #173 |
| 확정 시각 | `confirmed_at` | — | ⚠ **검사자 ≠ 확정자 분리(#62)** §5-6 |
| 단말 | `terminal_id` | — | K-3과 같은 성격 · ⛔ **화면이 보내지 않는다** — 요청을 인증한 것이 단말 토큰이므로 서버가 이미 안다(#173) |
| **재검사 원본** | `previous_result_id` | — | ✅ **B-10** — 정정이 아니라 **재검** |
| 재검사 사유 | `reinspection_reason_code` | — | ⚠ 값 목록 |
| 멱등키 | `idempotency_key` | ✅ **UNIQUE** | **C-1 대상 4곳 중 하나** §5-7 |

### §4-C. 검사 측정치 `quality.inspection_measurement`

| 라벨 | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| **검사 항목** | `inspection_item_spec_id` | ✅ | 기준 버전의 항목을 가리킨다. **화면이 만드는 값이 아니다** — 항목 목록을 받아 그대로 되돌려 준다 |
| **표본 번호** | `sample_no` | ✅ | 1부터. 상한은 항목의 `measurement_count` |
| **측정값** | `numeric_value` · `text_value` · `boolean_value` | — | ⛔ **셋 중 하나만**(`num_nonnulls ≤ 1`). 어느 칸인지는 항목의 `data_type_code`가 정한다. ⚠ **셋 다 비어도 된다** — 육안 항목은 판정만으로 성립한다 |
| **항목 판정** | `judgment_code` | ✅ **언제나** | `ACCEPTED`·`REJECTED` **2값**(「보류」 없음). ⭐ **자동 판정이 서는 항목은 화면이 채운 채로 시작한다** — §5-11 |
| 측정 시각 | `measured_at` | ✅ | |
| 사용 장비 | `inspection_equipment_id` | — | 교정 만료 여부는 **서버가 판정해 내린다** — 화면이 계산하지 않는다 |

⚠ **측정값과 판정은 다른 축이다.** 측정값은 「무엇이 나왔나」이고 판정은 「그것을 합·불 중 어디로 보나」다. 규격 밖이 나왔다고 판정이 **확정되지는 않는다**(§6).

**감사 컬럼·`version_no` 제외**(A-4). `version_no`가 있으므로 **B-1 적용** — 단 오프라인 큐는 **C-9**(토큰 미동봉).

## §5. 액션·상태

### §5-1. F-1 — `can_input_inspection`이 **처음 실증**된다

```
mdm.terminal_process.can_input_inspection   boolean
```

F-1 매핑표에서 **미실증**으로 남아 있던 3종(`can_input_inspection`·`can_cancel_input`·`can_return_material`) 중 **첫 번째가 여기서 확인**된다.

| 플래그 | 실증 화면 | 상태 |
| --- | --- | :-: |
| `can_input_inspection` | **`P-02-13`** | ✅ **신규** |

**규칙**: 플래그가 없으면 **버튼 비활성 + 사유**(숨기지 않는다).

```
이 단말은 이 공정의 검사 입력 권한이 없습니다
```

⚠ **「창고 작업에 대응하는 공정이 있는가」**는 여기서 문제되지 않는다 — **생산 공정이라 `mdm.process`에 있다.**

### §5-2. 검사 기준이 없어도 **검사할 수 있다** ⛔ **[2026-08-18 정정 — 앞의 판단을 뒤집는다]**

**QA #4 가 확정한 것**: 「검사기준 미비 시 **정상/불량 단순 선택 + 자유 입력 허용**」. **예외 E-2**(도메인 01)도 「검사기준 미등록·특채·보류」를 남겼다.

⛔ **앞서 이 화면은 그 확정을 «철회»했다.** 물러난 문장은 이랬다 —

> ~~**A-11 물러난 수준**: 검사 기준을 먼저 등록해야 한다. 화면은 의뢰가 없으면 진입할 수 없고, 「기준 미등록」 안내와 `W-06-02` 경로를 보인다.~~

**철회의 유일한 근거가 물리 모델이었다** — `inspection_request.inspection_plan_version_id` 가 `NOT NULL` 이라는 것. ⛔ **물리 모델은 설계 결정을 앞설 수 없다**(사용자 확정 2026-08-18 · `00-화면스펙서식.md` v1.1 §2 = 현행 `design/schema/00-authoring-rules.md` 규칙 2). **확정이 이긴다.**

#### 설계 — 두 갈래를 화면이 갖는다

| 기준 | 화면 |
| --- | --- |
| **있다**(정상) | 검사 항목별 규격·판정 — §4·§5-3 그대로 |
| **없다**(확정이 허용한 갈래) | ⭐ **정상/불량 단순 선택 + 자유 입력.** 항목표 대신 판정 두 값과 자유 입력란만 보인다 |

⭐ **왜 물러서면 안 되나** — 기준 미등록은 **현장에서 실제로 일어나는 상태**이고, 그때 검사를 못 하면 **제품이 멈춘다.** 확정이 「단순 선택이라도 하라」고 한 이유가 그것이다. 물러선 판단은 **「기준을 먼저 등록하라」는 안내로 현장을 막는 것**이었다.

📨 **모델 결손은 작업 통지다** — 기준 없는 의뢰를 담으려면 `inspection_plan_version_id` 가 **널을 받아야** 한다(물리 모델은 아직 `NOT NULL`). **우리를 막지 않는다** — 계약은 화면대로 쓰고 기다리지 않는다(§8).

✅ **계약이 착지했다(2026-08-28)** — `InspectionRequest.inspectionPlanVersionId` 를 `required` 에서 빼고 널을 받게 했다. 갈래에 필요한 나머지 셋도 함께 확인했다: 측정치 배열은 **원래 필수가 아니라** 빈 채로 보내면 되고, 자유 입력은 `remarks` 가 받으며, 종합 판정은 **같은 3값 목록을 그대로** 쓴다(값을 새로 만들지 않는다).

⚠ **등급이 올라간다** — 「조정」이 아니라 **화면이 갈래 하나를 더 갖는다.** 구현팀에 ⚠ 변경으로 알렸다(2026-08-18 · `omf-mes-client#86` 코멘트). 계약 착지는 **⛔ 등급**으로 따로 알린다 — 같은 스키마를 `W-01-01`·`W-04-03` 도 읽는다.

### §5-3. A-3 합계 제약이 **DB로 강제**된다

```
ck_inspection_result_qty  CHECK (accepted_qty + rejected_qty + held_qty = inspected_qty)
```

**A-3**(합계 제약의 UI 표현) 그대로: **실시간 합계·잔여를 표시하고 불일치 상태에서 확정 버튼을 비활성.**

```
합계  30 / 30  ✅        ← 일치
합계  28 / 30  ⚠ 2 남음   ← 확정 비활성
```

⚠ **A-3의 단서도 적용된다** — 「**자동 계산은 규칙이 확정된 뒤에만** 얹는다. 어느 칸을 자동으로 할지가 정해져야 한다」(`W-01-01` §8-4 미결). **여기서도 자동 계산을 넣지 않는다** — 합격만 넣으면 불합격이 자동으로 채워지는 것이 자연스러워 보이나, **보류가 있어 2칸이 남는다.**

### §5-4. 종합 판정 — ✅ **회신 E-3 종결(2026-08-07)**

**판정 3값 = `합격`·`불합격`·`보류`.** 검사 판정 **3값**(`합격`·`불합격`·`보류`) — `ck_inspection_result_qty`(`accepted+rejected+held = inspected`)가 **수량 3칸으로 이미 강제**하고 있었다. 각각 LOT 상태 `정상`·`불량`·`검사 대기` 로 전이한다. → `design/raw/process/uiux/2026-08-07-E3-판정유형-제안안/LOT상태-확정기록.md`

> **⛔ 아래는 대기 시절 서술이다 — 보존만 한다.**

#### (구) 회신 E-3 대기 시 G-6 표준 형태

```
inspection_result.overall_judgment_code   app.code_t NOT NULL
```

**회신 E-3**(판정 유형 **전체 목록**)이 대기 중이다. 확정된 것은 **한도승인·특채·폐기** 등 일부뿐이다.

**G-6**(회신 대기 화면의 표준 형태 · v0.4 신설)을 적용한다:

1. **확정분과 대기분을 한 목록에 함께 보이고 표식으로 구분** — 대기분을 감추면 「없는 것」으로 오해한다
2. 회신 전 **임시 등록을 막지 않되 「재검토 대상」으로 표시**
3. **화면 머리에 대기 사유와 회신 번호 명시**(E-3)

⚠ **G-6의 경고가 그대로 걸린다** — 「**대기 범위를 좁게 적지 않는다.** 회신 E-3의 문구는 「판정 유형 **전체 목록**」이라 확정 4종 외에 더 나올 수 있다. **화면은 목록 길이를 가정하지 않는다.**」

**→ 그래서 이 화면은 차단이 아니다.** 버튼을 고정 개수로 그리면 차단이지만, **목록에서 고르는 형태**면 값이 몇 개든 골격이 같다.

### §5-5. `coverage_from_at`/`coverage_to_at` — 모델이 앞섬

```
inspection_request.coverage_from_at  timestamptz   -- [L] 검사결과 적용 생산구간: 시간 (2-6 §7.5)
inspection_request.coverage_to_at    timestamptz
```

**「이 검사 결과가 어느 시간대의 생산분에 적용되는가」**를 담는다. 프로세스·확정 어디에도 없는 개념인데 **데이터가 갖고 있다.**

**왜 필요한가** — 초중종 검사는 **표본**이다. 「09:00~11:00 생산분을 대표한다」가 성립해야 불합격 시 **회수 범위**가 정해진다.

**화면이 쓸 것인가** — **쓴다.** 검사 시작·종료 시각으로 자동 채우되 **작업자가 조정 가능**하게 둔다.

⚠ **불합격 시 이 구간의 생산분을 어떻게 하나**가 미정이다 — 03 소관(Lot Status 전이) → §8-3.

### §5-6. #62(검사자 ≠ 확정자 분리)가 여기서도 걸린다

```
inspection_result.inspector_id   NOT NULL   ← 검사한 사람
inspection_result.confirmed_at   nullable    ← 확정 시각 (누가?)
```

**`confirmed_by`가 없다.** 확정한 사람을 담을 컬럼이 없다.

**#62**(검사자와 확정자를 분리할 것인가)는 `W-01-01`(IQC)에서 **차단** 등급으로 열린 미결이다 — 「분리하면 화면이 둘로 갈리고 권한 축이 생긴다」.

**이 화면에서도 같다.** 다만 **02-S-E의 분업(Q1)이 「검사 수행·결과 입력 지점 = 02-S-E」**라고 하여 **이 화면은 입력까지**로 읽힌다.

| 해석 | 이 화면 |
| --- | --- |
| **분리 안 함** | 작업자가 판정까지 |
| **분리함** | 작업자 입력 → **품질담당 확정은 다른 화면**(`W-03-01` 계열) |

⚠ **02-S-E는 「작업자(자주검사)·품질담당(초중종 **입회·판정**)」**이라 **행위자가 이미 둘**이다. **같은 화면에서 역할만 다를 수 있다.**

**권고**: 이 화면에 **입력과 확정을 함께 두되 `can_input_inspection` 권한으로 가른다.** #62 회신이 오면 재검토 → §8-2.

⚠ **`W-01-01`(IQC)의 #62는 차단이지만 여기서는 조정**이다 — IQC는 화면이 둘로 갈릴 수 있으나, PQC는 **POP 한 화면에서 권한으로 가르는 것이 자연스럽다.**

### §5-7. 오프라인 — `idempotency_key`가 있다

```
inspection_result.idempotency_key  varchar(150) NOT NULL UNIQUE
```

**C-1 대상 4곳 중 하나**(`M-01-05` §5-4에서 전수 확인). **전역 UNIQUE라 `business_date` 문제(C-8)가 없다.**

**POP 오프라인 규약**은 `P-01-02` §8-5가 미결로 남겼다 — 결정 17 시나리오 2는 **모바일** 축퇴다.

**이 화면은 오프라인을 지원한다**고 보는 것이 자연스럽다(현장 검사가 통신에 묶이면 안 된다). C-1·C-7(미확정 표식)·C-9(토큰 미동봉) 적용.

⚠ **다만 판정 결과가 Lot Status에 영향을 준다** — 결정 10의 「캐시 없음」과 `M-01-08` §5-2의 충돌이 여기서도 잠재한다. **판정은 기록이지 차단이 아니므로** 가볍다 → §8-4.

### §5-8. 재작업/폐기 2분류 — 처분구분이 없다

**REQ-PR-0025**: 「재작업 가능 불량 vs 폐기 불량 구분 — **판정은 불량창고 입고 후**」

**§I-10**이 이미 기록했다 — **결정 12가 정한 상세 코드 속성 「처분구분(재작업가능/폐기)」이 `defect_code`에 없다**(#64 묶음 A).

**화면 설계**: 불합격분에 대해 **처분을 사람이 고른다**(자동 판정 불가). 고른 값은 `quality.disposition_decision`에 담긴다(실재).

⚠ **REQ-PR-0025는 「판정은 불량창고 입고 후」**라고 순서를 못박았다. **이 화면에서 처분을 고르면 순서가 뒤집힌다.**

**해석**: 이 화면의 처분 선택은 **잠정**이고, **확정 판정은 불량창고 입고 후 `W-03-10`(처분 판정 처리)**이 한다 — `W-04-07`(04)은 부적합 등록·판정 «의뢰»까지이고 스스로 판정하지 않는다(`W-04-07` §5-1 · 04 요구서 §3-7 「판정 ⛔ 없음 — 두지 않는다」 · `F04` 정합 확정). *(v0.1의 「`W-03-*`」는 당시 03 에 처분 저장 화면이 0건이라 가리킬 곳이 없었고, `W-03-10` 이 2026-08-13 **DR-008 3-A** 로 신설되며 채워졌다 — 03 요구서 §3-8.)* 화면 문구로 밝힌다:

```
⚠ 처분은 불량창고 입고 후 확정됩니다 (✅REQ-PR-0025)
```

### §5-9. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 항목 판정(합격/불합격) | **전 항목**. ⭐ 자동 판정이 서는 항목은 화면이 **채운 채로** 시작하고 **사람이 바꿀 수 있다**(§5-11) — 그 밖의 항목은 비어 있다. ⭐ **목록은 `GET /mdm/code-values?codeGroupCode=INSPECTION_MEASUREMENT_JUDGMENT`** — `ACCEPTED`·`REJECTED` **2값**. ⚠ **종합 판정과 그룹이 다르다** — 항목에는 「보류」가 없다(#179 · `G-32`) |
| 측정값 입력 | 계측 항목 — `NumberPad`(D-4 · 실재·키 높이 요구 미충족) — **우단 《결과 입력》 안**(§3-1), 현재 편집 가능한 측정 필드에만 연동 |
| 수량 입력 | 합격·불합격·보류 — `NumberPad`(D-4 · 실재·키 높이 요구 미충족) — 같은 패드(현재 편집 필드 연동), 확인행은 입력 확정만 |
| 종합 판정 선택 | **목록에서**(§5-4 — 값 개수 무관) |
| 처분 선택 | 불합격 > 0일 때 — **잠정**(§5-8) |
| 임시 저장 | 항상 |
| **검사 확정** | **합계 일치**(A-3) AND 전 항목 판정 AND 종합 판정 선택 AND `can_input_inspection` |

### §5-10. 진입 — **대상을 받아서 연다. 목록을 두지 않는다** ⭐ **[2026-08-28 명문화 · `omf-mes#257` ①]**

```
진입 인자   inspectionRequestId   (검사 의뢰 식별자 하나)
```

**이 화면은 대상을 고르지 않는다.** 작업 화면에서 검사 의뢰가 정해진 채로 열린다.
계약이 이미 그렇게 적어 두었다 — 검사 의뢰 **한 건** 조회 경로의 설명이 「이 화면 진입 시
기준 버전·대상 수량을 읽는다」이고, 검사 의뢰 **목록** 경로의 설명은 소비자를 `W-01-01`로 못박았다.

| 상황 | 화면 |
| --- | --- |
| 진입 인자가 있다 | 그 의뢰를 읽어 §3 머리줄을 채우고 검사를 시작한다 |
| 진입 인자가 **없다** | ⛔ **목록을 그리지 않는다.** 「작업 화면에서 진입하세요」 안내 한 장만 그린다 |
| 진입 인자가 있으나 의뢰가 없다(404) | 같은 안내 + 「그 검사 의뢰를 찾을 수 없습니다」 |

⛔ **검사 대기 목록을 이 화면에 얹지 않는다.** 「아직 안 끝난 의뢰만」을 받는 질의(`pendingOnly`)가
계약에 있으나 그것은 **`W-01-01`(수입검사 판정, 관리웹)의 좌단 큐**다. 이 화면이 부르면 같은 큐가
두 곳에 생기고, 그중 하나는 §5-9 액션표에 없는 행위가 된다.

⚠ **진입 경로 화면(`P-02-01`·`P-CO-01`)이 아직 서지 않은 것은 이 규약을 바꾸지 않는다.**
그 화면들이 서기 전에는 진입 인자가 안 오므로 위 두 번째 행(안내 한 장)이 평소 모습이다.

⭐ **이 규약을 다른 POP 화면에 그대로 복사하지 않는다.** §3 도면의 **머리줄이 받는 것**과
**좌단 구획이 고르는 것**은 화면마다 다르다 — `P-02-05`는 좌단이 《대상 LOT》 목록이고
`P-02-09`는 좌단이 《포장 단위》 내용물이다. 이 화면만 좌단이 《검사 항목》이라 고를 것이 없다. §9-4

### §5-11. 자동 판정 — **채우되 잠그지 않는다** ⭐ **[2026-08-28 확정 · `omf-mes#257` ②]**

```
mdm.inspection_item_spec.automatic_judgment   boolean   기본 true
```

`W-06-02`(검사기준 등록) §4-C가 이 플래그의 뜻을 「**상·하한으로 합·불을 자동 판정할지**」로
정해 두었다. 이 화면이 그것을 **읽는 유일한 화면**이다.

**자동 판정이 서는 조건 — 셋이 모두 참일 때만**

| 조건 | 값 |
| --- | --- |
| 자동 판정 플래그 | `automatic_judgment = true` |
| 데이터 유형 | `data_type_code = NUMERIC` — 상·하한이 의미를 갖는 유일한 유형이다 |
| 기준 | 하한·상한 중 **하나 이상**이 채워져 있다 |

**서면**: 화면이 측정값을 상·하한과 대조해 항목 판정을 **채운 채로** 보인다(§3 도면의 ✅·⛔ 그대로).
⭐ **사람이 바꿀 수 있다.** 채운 값은 시작점이지 확정이 아니다.

**서지 않으면**: 판정을 비워 두고 사람이 고른다. 저장을 막지 않는다 — 조항 G-15(비어 있으면 판정이
서지 않는 필드는 저장을 막지 말고 보인다) 그대로다. 화면에 사유를 함께 보인다.

```
기준 없음 — 직접 판정하세요
```

⛔ **종합 판정은 어떤 경우에도 자동으로 내리지 않는다.** 항목이 전부 불합격이어도 종합 판정은
사람이 고른다 — §6의 「자동 불합격 아님」이 가장 강하게 걸리는 자리가 여기다.

⚠ **`M-05-01`(설비 점검 입력)은 같은 형태를 반대로 정했다** — 그쪽은 「자동 판정을 사람이 덮어쓸 수
없게 한다」이다. 설비 점검에는 자동 판정 여부를 가르는 마스터 스위치가 없고(판정 방식 코드 한 축),
제품 검사에는 있다. **스위치가 있는데 결과를 잠그면 스위치를 두 번 쓰는 것**이 된다. §9-5

## §6. 예외·오프라인

| 상황 | 처리 |
| --- | --- |
| **권한 없음** | ⛔ 버튼 비활성 + 사유(F-1) |
| **검사 기준 미등록** | ⭐ **진입한다** — 두 번째 갈래로 간다(§5-2). 항목표 대신 판정 선택과 자유 입력만 보인다. ⛔ **막지 않는다** — 기준 등록을 요구하며 되돌리면 현장이 멈춘다 |
| 합계 불일치 | ⛔ 확정 비활성 + 잔여 표시(A-3) |
| 측정값이 규격 밖 | ⚠ **판정이 «확정»되지 않는다** — 자동 판정이 서는 항목이면 화면이 불합격을 **채워** 보이되 **사람이 바꿀 수 있고**(§5-11), 종합 판정은 언제나 사람이 고른다 |
| **자동 판정 기준이 비어 있다** | ⚠ 자동 판정 플래그가 켜져 있어도 상·하한이 둘 다 비면 판정이 서지 않는다 — 저장을 막지 말고 「기준 없음 — 직접 판정하세요」로 그린다(`G-15`) |
| **진입 인자 없음** | ⛔ 「작업 화면에서 진입하세요」 안내만 — **목록을 그리지 않는다**(§5-10) |
| ~~**판정유형 목록 미확정**~~ ✅ **해소** | 값 3개는 2026-08-07 확정, 그것을 **받을 방법**은 2026-08-21 확정(#179) — `codeGroupCode=INSPECTION_RESULT_OVERALL_JUDGMENT`. ⚠ 조회가 **비면** 그때는 G-2 대로 비활성 + 사유다 |
| **재검사** | 새 `inspection_result`(회차 +1 · `previous_result_id`) — **정정이 아니다**(B-10) |
| 불합격 처분 미선택 | ⚠ **막지 않는다** — 확정 판정은 나중(§5-8) |
| **오프라인** | 큐잉(C-1) · **미확정 표식**(C-7) · **`If-Match` 미동봉**(C-9) |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 검사 항목 목록 | `List` 또는 `Table`(compact) | a · ⚠ **G-5** |
| 항목 판정 버튼 | `Button`(큰 타겟) | a · ⚠ **G-5** |
| 측정값·수량 입력 | `NumberPad` — 우단 자리216, 고정 원천 치수는 §3-1 | **실재·요구 크기 미충족** — 최종 size·세로 적합성 미결10. 과거 `ds-gap` G-6의 d는 현재 판정이 아니다 |
| 합계·잔여 | 인라인 텍스트 | a · **A-3** |
| 종합 판정 선택 | `Select` 또는 `RadioGroup` | a · **공유계약 G-2·G-6** *(⚠ 위 행의 `ds-gap` G-6 과 «다른 체계»다 — 이쪽은 회신 대기 화면 표준)* |
| 처분 선택 | `RadioGroup` | a |
| 규격 밖 표시 | `Chip`(error) | a |
| 회신 대기 안내 | `AlertBanner`(info) | a |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 3 | **불합격 시 `coverage` 구간 생산분 처리** — 03 소관(Lot Status 전이) | 프로세스 | **조정** | 03 거버넌스 |
| 4 | **POP 오프라인 규약** — `P-01-02` §8-5와 같은 미결. 이 화면은 지원이 자연스럽다 ⚠ 늦게 도착한 확정이 그 사이 `W-03-02` 가 전이시킨 LOT 을 만나는 경우의 서버 동작(덮기/거부)이 아직 없다 | 설계 | **조정** | C-1 적용으로 시작 |
| 5 | ⚠ **§I-9 검사정책 3속성·샘플 비율↔수량**(#64) — 「샘플 30」이 30개인가 30%인가 | **상류↔하류 불일치** | **조정** | **A-8 — 화면에 단위 명시 필수** ✅ **해소 2026-09-02** — `InspectionPlanVersion.samplingRatio` 실재(미착지 9 부분 되살림) |
| 6 | ⚠ **§I-10 처분구분 부재**(#64) — 재작업/폐기를 사람이 고른다 | **상류↔하류 불일치** | **조정** | 잠정 선택으로 시작 ✅ **해소 2026-09-02** — `dispositionTypeCode` 값 확정 2026-09-01(`omf-mes#336`) — `REWORK`·`SCRAP`·`NORMAL` |
| 7 | ~~`overall_judgment_code`~~ ✅ / ~~`disposition_type_code`~~ ✅ / `reinspection_reason_code` 값 목록 | 공통코드 | **조정** | ⛔ **「남은 둘 다 운영 중 추가 가능」은 틀렸다**(2026-09-03 정정) — `disposition_type_code` 는 계약이 `enum` 으로 닫았다(`CD-DISPOSITION-DECISION-DISPOSITION-TYPE` = `NORMAL`·`REWORK`·`SCRAP` · `#336` 2026-09-01) **→ 고객이 늘릴 수 없다.** 남은 것은 `reinspection_reason_code` 하나이고 그것은 그룹 `REINSPECTION_REASON` 이 `G-32` 에 섰으나 **시드가 없다 — 고객이 `W-06-06` 에서 채운다**(`G-31`) |
| 8 | **#60 실적 수량 3원↔5컬럼** — 검사의 `held_qty`가 실적의 `hold_qty`와 어떻게 잇는가 | **#60 닫힘**(2026-08-25) | **조정** | 화면은 정본 R50 대로 양품만 다룬다 · 조회는 `W-02-08` §5-5 ⭐ **2026-09-02 좁힘** — 화면 판정(R50 양품만)은 확정됐다. 남은 것은 조회처인 `W-02-08` §5-5 가 스스로 「잠정(`#60` 회신 전)」이라 적는 것 ⚠ **2026-09-04 표지 재판정**(`omf-mes#378` 3회차) — **이 표지 `#60` 은 닫혔고(2026-08-25 · 코멘트 0건) 그 결말이 이 미결을 닫지 않는다.** 검사의 `held_qty` 와 실적의 `hold_qty` 를 잇는 규약은 2026-09-04 재실측에서도 **어디에도 없다** — 계약이 둘을 **다른 축**으로 둔다(`InspectionResult` 는 `acceptedQty + rejectedQty + heldQty = inspectedQty` 를 강제하고 `ProductionResult` 는 다섯 칸을 그대로 내린다). ⛔ **살아 있는 표지가 없다.** |
| 9 | ⚠ **기준 없는 검사 의뢰를 «물리»가 아직 못 담는다** — 계약은 착지했고(2026-08-28) `inspection_plan_version_id` 의 `NOT NULL` 해제만 남았다 | 📨 작업 통지 | **조정** | 화면·계약 차단 아님 — 백엔드 통지 `omf-mes#280` ✅ **해소 2026-09-02** — `omf-mes#280` 종결(2026-08-31) — 닫은 회신이 「물리 컬럼 해제를 기다린 것이 원칙 위반이었다」로 적었다 |
| 10 | **우단 《결과 입력》 세로 적합성·최종 size 미확정** — 현재 부품의 키 높이 미달 및 5행 치수 검산 필요(§3-1) | DS | **조정** | `NumberPad` 실재 확인. `lg`48·`xl`60은 D-4 키64 요구 미달이며 소수 허용 시 5행이다. `xl` 너비256은 자리216보다40 넓고, 결과 입력312→272 조정만으로는 적합하지 않다. 행별 높이·소수/확인행 포함 세로 검산·우단 추가 스크롤 허용은 미결로 유지한다. 좌단 스크롤과 우단544 경계는 보존한다. `#286`·`#284` 종료는 이 미결의 해소가 아니다. 과거 부품 부재 판정은 현재 원천 확인으로 대체하며, 추적 기준은 D-4·E-3·E-4다 |

**차단 0건 — 이 화면은 지금 지어도 된다.**

> ⚠ **배치표가 이 화면에 「#60 · E3 회신」을 경고로 달았는데 둘 다 차단이 아니다.** 판정유형은 **목록에서 고르는 형태**라 값 개수와 무관하고(G-6), #60은 **입력이 아니라 조회 쪽 매핑** 문제다. **화면 골격이 바뀌지 않는다.**

## §9. 이 장에서 나온 공유 계약 후보

1. **G-6(회신 대기 화면 표준)이 차단 등급을 낮춘다** — 「목록에서 고른다」로 설계하면 값이 3개든 10개든 골격이 같다. **값 목록 미정을 차단으로 볼지 조정으로 볼지는 UI 형태가 가른다** — 고정 버튼이면 차단, 목록이면 조정. **미결 등급 판정 기준에 이 구분을 넣는다** (§5-4) «(정합주: 2026-09-03 — 이 항목이 말하는 것은 «등급 판정 기준»이라는 일반 원칙이고, 그 원칙은 지금도 그대로다. ⛔ 여기 적힌 「값 목록 미정」을 **이 화면의 지금 상태로 읽지 마라** — 이 화면이 쓰는 코드는 값이 섰다(판정 3값 2026-08-07 · `dispositionTypeCode` 2026-09-02 — §8 해소표 1 · §8 미결 6))»
2. **같은 미결이 화면에 따라 등급이 다르다** — #62(검사자≠확정자)는 `W-01-01`(IQC · 관리웹)에서 **차단**인데 여기서는 **조정**이다. IQC는 화면이 둘로 갈릴 수 있고 PQC는 POP 한 화면에서 권한으로 가른다. **미결 등급은 미결 자체가 아니라 그 화면에서의 파급으로 정한다** (§5-6)
3. **A-3의 「자동 계산 보류」 단서가 두 번째로 확인된다** — `W-01-01`에서 「어느 칸을 자동으로 할지 미정」이었는데, 여기도 **보류가 있어 2칸이 남아** 자동 계산이 성립하지 않는다. **합계 제약이 3항 이상이면 자동 계산은 규칙 확정 전까지 넣지 않는다** (§5-3)
4. **POP 화면의 「받는 것」과 「고르는 것」은 §3 도면이 이미 가른다** — 머리줄이 받는 것이 진입 인자이고, 좌단 구획이 화면 안에서 고를 것이다. `P-02-05`는 좌단이 대상 LOT 목록, `P-02-09`는 포장 내용물, 이 화면은 검사 항목이라 고를 것이 없다. **「POP은 목록을 두지 않는다」로 일반화하면 앞 둘의 선택 구획을 잘못 걷어낸다.** 3화면 실증이 모이면 조항으로 올린다 (§5-10)
5. **마스터가 「자동으로 할지」를 스위치로 갖고 있으면 그 결과를 화면이 잠그지 않는다** — 제품 검사는 항목마다 자동 판정 스위치가 있어 채우되 바꿀 수 있고, 설비 점검(`M-05-01`)은 스위치가 없어 잠근다. **스위치의 유무가 잠금 여부를 가른다** — 같은 「자동 판정」이라는 말로 두 화면을 같게 만들면 안 된다 (§5-11)

## 근거·이력

### §8 해소된 미결

> 원 §8 항목 번호를 그대로 유지한다. 아래 항목은 해소로 명시되어 이 절로 옮겨졌다.

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ~~**회신 E-3 판정 유형 전체 목록**~~ ✅ **해소 2026-08-07** — 19장이 걸려 있었다 | **확정** | — | 판정 3값 · LOT 상태 4값 → `design/raw/process/uiux/2026-08-07-E3-판정유형-제안안/LOT상태-확정기록.md` |
| 2 | ~~**#62 검사자 ≠ 확정자**~~ ✅ **해소 2026-08-07** | **확정** | — | **분리하지 않는다.** §5-6 의 권고(입력+확정 한 화면 · `can_input_inspection` 로 가르기)가 **그대로 확정안이 됐다.** `confirmed_at` 은 저장 시각으로 채운다 |

### 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.3 | 2026-09-03 | ⭐ **§3 도면에 숫자 키패드 자리를 넣었다**(`omf-mes#286`). §5-9·§7 이 「키패드로 넣는다」를 적어 두고 **도면이 자리를 말하지 않아** 아무도 놓지 않는 상태였다 — D-4 가 「배치는 화면이 놓는다」이므로 도면이 정본이다. **우단 544 를 《결과 입력》 312 + 간격 16 + 《숫자 키패드》 216 으로 갈랐다**(§3-1 신설). ⛔ **회신(`omf-mes#286`)이 든 근거 둘을 그대로 쓰지 않았다** — 「216×292 는 D-4 가 정한 표준」은 거짓이고(저장소 전수 2곳 · 둘 다 `P-02-04` 것), 「슬랙 0 이라 자리가 없다」도 거짓이다(키패드를 그린 현장 단말 4화면 중 3장이 스스로 「슬랙 0」을 적었다 — 2026-09-03 전수 실측). 근거를 실물로 다시 세웠다. §5-9·§7 의 `G-6` 에 **`ds-gap` 접두**를 붙이고 같은 표의 «공유계약» `G-6` 과 갈랐다. 미결 9 → 10(**차단 0 유지**) — 「우단 세로 검산 미실시」 신설. |
| v0.2 | 2026-08-28 | **검토 요청 2건 반영**(`omf-mes#251`·`#257`). ⭐ **§5-2 정정이 유효함을 확정하고 낡은 세 자리를 고쳤다** — §6 예외 표·§4-A 필드표가 §5-2 를 근거로 달고 §5-2 와 반대말을 하고 있었다. **계약이 착지했다** — 검사 의뢰의 기준 버전을 `required` 에서 빼 기준 없는 의뢰를 표현할 수 있게 했고, 갈래에 필요한 나머지 셋(측정치 배열 생략·자유 입력·종합 판정 값)은 **이미 계약이 받고 있었음**을 확인했다. **§5-10 신설** — 이 화면은 대상을 받아서 열고 목록을 두지 않는다(계약의 진입 경로 설명이 이미 그렇게 적혀 있었다). **§5-11 신설** — 자동 판정 플래그를 읽되 **채우고 잠그지 않는다.** 세 조건(플래그·수치 유형·상하한 존재)이 모두 참일 때만 선다. **§4-C 신설** — 품질 요구서 §3-7 이 인용하는데 없던 절이다. **§5-8 정정** — 「확정 판정은 `W-03-*`」을 `W-04-07` 로(요구서 §7 이 지적한 자리). 미결 8 → 9(차단 0 유지) · 공유 계약 후보 3 → 5 · 신규 DS 갭 0건. |
| v0.1 | 2026-08-05 | 초안. **`can_input_inspection`이 처음 실증**됨(F-1 미실증 3종 중 첫 번째). **배치표가 단 「#60 · E3」 경고가 둘 다 차단이 아님**을 확인 — 판정유형은 **G-6의 목록 형태**로 설계하면 값 개수와 골격이 무관하고, #60은 조회 쪽 매핑 문제다. **미결 등급이 화면마다 다를 수 있음**을 #62로 실증(`W-01-01` 차단 ↔ 이 화면 조정). `ck_inspection_result_qty`가 **A-3을 DB로 강제**하고, **보류 때문에 자동 계산이 성립하지 않음**을 확인(A-3 단서 2번째). **`coverage_from_at`/`_to_at`이 확정에 없는 개념인데 실재**(모델이 앞섬) — 표본 검사의 적용 구간. §I-9(샘플 비율↔수량)가 **A-8 단위 명시**로 화면에 걸림. 미결 8건(**차단 0**) · 공유 계약 후보 3건 · 신규 DS 갭 0건. |

출처: design/wiki/screens/02/P-02-13-PQC제품검사.md · 최종 대조일: 2026-08-25
