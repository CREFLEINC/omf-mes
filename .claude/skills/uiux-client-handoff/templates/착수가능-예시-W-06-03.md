### 1. 화면 ID · 이름

W-06-03 · 불량·원인코드 2계층 마스터

### 1-2. 리소스 패턴

마스터 형 — 목록 + 등록/수정 + 사용 중지

### 2. 근거 — 무엇을 보고 판단했는가

- 화면 스펙: uiux/2026-08-03-화면상세스펙-확대1차/W-06-03-불량코드2계층마스터.md (개정 2026-08-03)
- 계약 정본: deliverables/openapi/mdm-기준정보.json (2026-08-06판)
- 공유계약: 공유계약.md v0.9
- 디자인 시스템: design-system-v2-webui — client 저장소 pnpm-lock.yaml 이 가리키는 커밋의 설치본에서 Table·groupBy·Chip 실물 확인. 계층 표시는 2계층 고정이라 Table 로 성립
  <!-- ⛔ 커밋 해시를 이 예시에 «박지» 않는다 — 복사될 때마다 옛 핀이 퍼진다.
       2026-08-18 실측: 통지 #36·#37·#38 이 옛 핀(29종)을 근거로 「Progress 도 있다」·
       「Drawer 는 없다」를 단언했는데, 그 사이 컴포넌트가 30종으로 늘고 DatePicker 가 생겼다.
       발행할 때마다 lock 파일이 가리키는 커밋을 «그 자리에서» 확인해 적는다. -->

### 2-2. 확인 시점

2026-08-06

### 3. 확정된 것 — 이 화면에서 더 바뀌지 않는 것

- [x] **필드 목록** — 화면에 놓일 항목과 표시 순서
- [x] **검증 규칙** — 필수 · 형식 · 길이 · 조건부 필수 · 짝 제약
- [x] **예외 표시** — 오류 · 경고 · 빈 상태 · 권한 없음의 문구와 위치
- [x] **화면 전이** — 어디서 들어오고 어디로 나가는가
- [x] **엔드포인트 존재** — 계약 정본에 필요한 오퍼레이션이 모두 있다

### 4. 미결 항목 — 확정되지 않은 것과 그 처리 방법

| 항목 | 왜 미결 | 이번에 어떻게 처리 | 추적 번호 |
| --- | --- | --- | --- |
| 공정 매핑 탭 | 불량코드와 공정을 다대다로 잇는 테이블이 계약에 없다 | 만들지 않는다 — 이번 범위에서 뺀다. 붙어도 목록·등록 구조는 안 바뀐다 | omf-mes#64 |
| 처분 구분 열 | 코드 마스터 쪽 속성이 아직 없다 | 만들지 않는다 — 열을 두지 않는다 | omf-mes#64 |
| 한국어·베트남어 명칭 분리 입력 | 명칭이 단일 항목이라 분리해 담을 자리가 없다 | 만들지 않는다 — 단일 입력으로 둔다 | omf-mes#64 |
| 대분류 선택지 | 초기값 5종이 초안이라 고객 확인 전 | 자리표시 상수 + 안내 — 프론트 상수 한 곳에 모으고 「임시 목록」 표시 | omf-mes#64 |

### 5. 선행·순서 — 먼저 끝나야 하는 것과 재작업 위험

- 선행: 없음. 마스터 형 골격은 이미 병합된 두 화면과 같다
- 함께 열린 이슈 중 순서: 이 화면 → W-06-10 (마스터 형을 먼저 굳히고 조회 형으로 넘어간다)
- 재작업 위험: 낮음. 위 미결이 풀리면 탭과 열이 늘어나지만 목록·등록 구조는 그대로다

### 6. 특별히 조심할 것

- 원인 코드는 별도 화면이 아니라 이 화면의 두 번째 탭이다. 불량 코드와 구조가 같아 같은 컴포넌트를 재사용한다
- 자기 자신을 상위로 지정하는 것과 3계층 이상을 데이터베이스가 막지 않는다. 둘 다 화면과 서버가 막아야 한다
- 계층은 2계층 고정이다. 트리 컴포넌트가 아니라 그룹 헤더가 있는 목록으로 충분하다
- 손실 사유 코드는 이 화면이 다루지 않는다. 공통코드 화면 소관이다
