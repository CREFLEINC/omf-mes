---
name: design-request-intake
description: >-
  사용자가 전달한 개발팀 요청 자료(정보 요청·설계 개선·개선 제안서) 1건을 접수해 적합성 판정
  → 독립 재실측 → 파급 산정 → 반영(PR) → 답변서 작성 → 자기 이슈 닫기까지 처리하는
  오케스트레이터. "이 자료 처리해줘", "개발팀 요청 확인해줘", "개선 제안서 검토해줘",
  "답변서 만들어줘", "이어서 처리해줘", "저번 요청 다시 봐줘" 같은 요청에 반드시 이 스킬을
  사용한다. V3 규칙 2 — 요청은 자료로 만들어 사용자를 통해 전달되고, 답변서도 사용자를 통해
  돌아간다. 이 스킬은 개발팀 저장소·개발팀 이슈에 아무것도 쓰지 않는다.
  ⛔ 「232번 검토 요청 처리해줘」처럼 `[검토 요청]` 이슈 번호로 부르는 것은 V3 이전 유산이다 —
  이 스킬은 받지 않는다(열린 것은 사용자가 정리한다). 설계 변동 공지는 `design-change-notice`
  소관이다(이 스킬은 「다음 공지에 실린다」 한 줄만 남긴다).
---

# design-request-intake — 개발팀 요청 자료 접수·처리

`multi-agent-team-workflow-v3.md` 규칙 1(설계팀 우선주의 — 개선 제안서) · 규칙 2(직접 소통
금지 — 자료 → 사용자 → 답변서) · 규칙 4(투명한 업무 공개 — 자기 이슈)를 절차로 세운 것이다.
사람 개입은 게이트 두 곳(접수 확인 · 분석 승인)으로 최소화한다.

**입력**은 GitHub 이슈가 아니라 **사용자가 준 자료**(파일 경로 또는 붙여넣기)다. **출력**은
파일(`tmp/requests/<…>/답변서.md`)이고, 개발팀에 전달하는 것은 **사용자**다. 이 스킬이 GitHub 에
쓰는 자리는 **설계팀 자기 저장소(`CREFLEINC/omf-mes`)의 자기 이슈와 반영 PR** 두 가지뿐이다.

⛔ **Phase 6b(자기 이슈 닫기)가 Phase 5b(PR 병합 확인)보다 먼저 오면 안 된다.** V2 시절 이슈가
PR 병합 34~43분 **전에** 닫힌 사고(#206·#196)가 이 순서 위반에서 났다. 닫는 코멘트에 실리는
답변서는 «병합 해시»를 인용하는데, 병합 전에는 그 해시가 없다. 아래 Phase 순서 자체가 재발
방지 장치다 — 재배열하지 않는다.

이 스킬은 `team-issue-protocol`(자기 이슈 라벨·접두·8유형·답변서 서식)을 전제로 한다. 먼저 그
스킬을 `Skill` 도구로 읽는다.

## 무엇을 받고 무엇을 받지 않나

| 받는다 | 받지 않는다 |
| --- | --- |
| 사용자가 「이 자료 처리해줘」와 함께 준 **파일 경로** 또는 **붙여넣기** | `[검토 요청]`·`[client→uiux]` 이슈 번호 — V3 이전 유산. 사용자가 정리한다 |
| 요청 종류 3종 — **정보 요청**(설계 자료의 오류·누락) · **설계 개선**(요구사항 달성에 설계 변경 필요) — 규칙 2(1) · **개선 제안서** — 규칙 1 | `gh issue view` 로 개발팀 저장소·개발팀 이슈를 읽어 오는 것 — 규칙 2 가 금지한 직접 소통이다 |

⭐ **개선 제안서(규칙 1)도 같은 절차로 처리한다.** 다른 것은 답변서의 판정 어휘뿐이다 —
「반영 / 부분 반영 / 반영하지 않음 / 정보 회신」 대신 **「채택 / 부분 채택 / 채택하지 않음」**.
개발팀은 제안서를 냈다고 업무를 멈추지 않는다(규칙 1) — 그러니 답변서가 「채택」이어도 개발팀이
언제 어떻게 따라올지는 우리가 알거나 정하는 정보가 아니다.

## Phase 0 — 컨텍스트 확인

```
ROOT="$(git rev-parse --show-toplevel)"
```

1. **식별자**를 정한다 — 자료 제목을 kebab 으로(한글 가능 · 공백→`-` · 기호 제거). 제목이
   없으면 `req-<n>`(`$ROOT/tmp/requests/` 에 이미 있는 `req-*` 최댓값 +1, 없으면 `req-1`).
   접수 날짜는 오늘(`YYYY-MM-DD`)이다.
2. `$ROOT/.design-runs/<식별자>-*/` 존재 여부 확인:
   - **없음** → 초기 실행. `$ROOT/.design-runs/<식별자>-<YYYYMMDD>/` 생성.
   - **있고 사용자가 부분 수정 요청** → 부분 재실행. 기존 파일을 읽고 해당 Phase 만 재실행.
   - **있고 사용자가 새 자료(개정본)를 줌** → 기존 디렉토리를 `<...>_prev/` 로 이동 후 재생성.
3. ⛔ **런 디렉토리는 절대경로만 쓴다.** 상대경로 `_workspace/` 를 쓰지 않는다 — 구 하네스가
   그래서 산출물이 두 갈래로 쪼개졌다(`docs/_workspace/` 에 117파일이 실수로 커밋됨). 이름도
   `_workspace` 로 하지 않는다 — grep·훅이 오폭한다.
4. `.design-runs/`·`tmp/` 는 둘 다 `.gitignore` 에 등재돼 있다 — 커밋하지 않는다. 감사 추적의
   정본은 **자기 이슈 코멘트(답변서 전문) + 병합 커밋 SHA + 설계 변동 공지 이슈**다(런
   디렉토리와 `tmp/` 는 사본·스크래치일 뿐).

## Phase 1 — 접수 (`gh` 호출 0)

```
REQ="$ROOT/tmp/requests/<YYYY-MM-DD>-<식별자>"; mkdir -p "$REQ"
```

1. 자료를 `$REQ/요청.md` 에 저장한다 — **경로면 복사**(`cp`), **붙여넣기면 그대로 저장**.
   원본이 `.md` 가 아니면(docx·pdf·xlsx·txt 등) **원본도 `$REQ/` 에 옆에 두고** `요청.md` 에는
   텍스트로 옮긴다(옮길 수 없는 표·그림은 「원본 <파일명> p.N 참조」로 자리를 남긴다).
2. 런 디렉토리에 사본을 둔다 — `cp "$REQ/요청.md" "$RUN/00_request.md"`.
3. 자료를 훑어 **「무엇을 묻는가」 한 줄 요약**을 만든다. 화면 ID(`W-04-03`·`P-02-03`)·문서
   경로·계약 오퍼레이션(`GET /quality/inspections`)·조항(`A-21`)이 **인용돼 있는지**를 함께
   적는다 — 인용이 하나도 없으면 Phase 2 에서 유형 F(서식 불비)로 갈 후보다.
4. **요청 종류**를 자료 문면에서 식별한다 — 정보 요청 / 설계 개선 / 개선 제안서. 자료가
   스스로 밝히지 않았으면 문면으로 추정하되 「추정」이라고 적는다.

⛔ 이 Phase 에서 `gh` 를 부르지 않는다. 자료가 개발팀 이슈 URL 을 인용하고 있어도 **그 이슈를
읽으러 가지 않는다** — 자료에 적힌 것만이 요청이다(부족하면 답변서로 보완을 요청한다).

출력: `$REQ/요청.md`(+원본) · `$RUN/00_request.md` · 한 줄 요약과 요청 종류(Phase 0'·2 가 쓴다).

## Phase 0' — 자기 이슈 (규칙 4) — ★ 접수 확인 게이트

설계팀이 「지금 하고 있는 일」을 자기 저장소에 드러낸다. **개발팀 저장소가 아니라 `omf-mes`
자신**이므로 규칙 2 위반이 아니다.

먼저 사용자에게 **접수 요약**(식별자 · 요청 종류 · 한 줄 요약 · 인용된 화면/경로/오퍼레이션)을
제시하고 「이대로 접수해 자기 이슈를 세웁니다」 확인을 받는다. **확인 뒤에** 아래를 실행한다:

```
gh issue create --repo CREFLEINC/omf-mes \
  --title "[요청 처리] <자료 제목>" \
  --label "Agent : Architect" --label "status:in-progress" \
  --body-file $RUN/00_issue_body.md
```

`00_issue_body.md` 는 세 줄이면 된다 — **접수 날짜 · 자료 식별자 · 요청 종류**(+ 한 줄 요약).
자료 본문을 옮겨 싣지 않는다(정본은 `요청.md`, 결과는 닫을 때 답변서 전문으로 남는다).

⚠ **라벨 `Agent : Architect` 는 아직 저장소에 없다**(2026-09-03 실측 — `gh label list` 에
`Agent : Client`·`T1`~`T4` 만 있다). 처음 쓰는 회차에 **같은 접수 확인 게이트 뒤에서** 만든다:
```
gh label create "Agent : Architect" --repo CREFLEINC/omf-mes --description "설계팀 자기 이슈"
```
⛔ `in progress` 를 새로 만들지 않는다 — `status:in-progress` 가 정본이다(`team-issue-protocol` §2).

부분 재실행(Phase 0 의 둘째·셋째 경우)이면 이미 있는 자기 이슈를 그대로 쓴다 — 새로 세우지
않는다. 재개정 자료가 오면 그 이슈에 「개정본 접수 <날짜>」 코멘트 한 줄만 남긴다.

출력: 자기 이슈 번호(`$RUN/00_issue_no.txt` 에 적어 둔다 — 6b 가 닫을 대상).

## Phase 2 — 적합성 판정

자료를 **물음(문장) 단위**로 분해한다. 각 물음에 `team-issue-protocol` §5 의 8유형표를
적용하고, 판정 근거로 삼은 문장의 **주어를 원문 그대로** 옮겨 적는다(주어를 못 옮기면 판정
불가로 남긴다 — 확정문의 주어를 오독해 소관을 잘못 넘긴 선례가 있다).

**자료 기준 항목** — 옛 「인바운드 형식(이슈 폼·라벨)」 검사는 없다. 대신 **요청 종류(정보
요청 / 설계 개선 / 개선 제안서 — V3 규칙 1 · 2(1))가 식별되는가**를 본다. 식별되지 않고 화면·
경로도 지목하지 않았으면 유형 F(서식 불비)다 — 반려가 아니라 **답변서로 보완을 요청**한다.

자료가 화면 ID·조항·`DR-nnn` 을 인용했으면
```
python3 design/schema/generators/collect-open-items.py --issue <표지>
```
로 그 표지가 이미 미결로 걸린 화면이 있는지 확인한다(있으면 그 화면들을 분석 범위에 넣는다).
⚠ **표지가 0건이어도 그것이 「분석할 것이 없다」는 뜻은 아니다** — 표지는 권장일 뿐 의무가
아니라(Phase 8), 실측상 살아 있는 미결의 3분의 2에 표지가 없다. 분석 범위는 표지가 아니라
물음이 가리키는 화면으로 정한다.

**전 물음이 유형 E(업무배정)·F(서식불비)·H(채널오인)면 여기서 멈춘다** — Phase 3 으로 가지
않고 Phase 6a 로 건너뛰어 답변서(판정 「반영하지 않음」 또는 「정보 회신」)만 만든다. 그
경우에도 Phase 6b 의 자기 이슈 닫기는 거친다(PR 이 없으므로 잠금 ①·② 는 「해당 없음」으로 적는다).

출력: `$RUN/01_scope.md`(머리에 한 줄 요약·요청 종류, 본문은 물음별 판정표).

## Phase 3 — 독립 재실측·분석

`design-review-analyst`(opus, 읽기전용)를 스폰한다:

```
Agent(
  subagent_type: "design-review-analyst",
  model: "opus",
  prompt: "$RUN/00_request.md 와 01_scope.md 를 읽고, 각 적합 물음을 독립 재실측하라.
           요청자의 「없다/N건이다」 주장을 그대로 인용하지 말고 직접 명령을 돌려 재현하라.
           판정 단위는 요청 범위가 아니라 화면 전체다 — §4·§5 전건을 계약과 대조하고
           '요청 밖에서 드러난 것'을 반드시 확인하라. V2 §1-3-2 변경 적절성(부작용·영향 범위·
           이익 비교 — V3 는 라벨 형식만 V2 를 계승하지만 이 판단 기준은 그대로 쓴다)까지
           판단해 02_measure.md · 03_brief.md · 03_reply.md 를 $RUN/ 에
           산출하라. design-review-analyst.md 정의를 따르라."
)
```

analyst 산출물이 다음을 만족하는지 확인한다(미달이면 재작업 지시):
- `03_reply.md` 가 Phase 6a 의 답변서 서식(머리 표 4행 · `## 답변` · `## 다음 공지`)을 갖췄다.
  「반영 PR」·병합 해시 칸은 이 시점에 **자리표시자**여도 된다(6a 에서 채운다).
- `03_reply.md` 의 「답변」에 "⭐ 요청 밖에서 함께 드러난 것" 절이 채워져 있다(없으면 "없음 +
  확인 범위" 명시).
- `03_brief.md` 가 계약 JSON 을 건드리면 변경 등급표(Phase 5)의 행이 **빈칸 없이** 채워져 있다.

⛔ analyst 는 `03_labels.md`·`03_notice.md` 를 만들지 않는다 — 라벨은 자기 이슈에만 붙고
(Phase 0'·6b) 통지는 배포 단위 공지(`design-change-notice`)의 몫이다.

## Phase 4 — ★ 사람 게이트 — 분석 승인

`01_scope.md`·`02_measure.md`·`03_brief.md`·`03_reply.md` 전문을 **파일로 첨부**하고, 아래
서식으로 **본문 브리핑**을 함께 낸다. **이 승인 하나가 Phase 6b(자기 이슈 닫기)까지 커버한다**
— Phase 5 이후 새 사실이 드러나면(예: 반영 중 추가 미정 항목 발견) 이 게이트를 다시 돈다.

승인 없이는 Phase 5 로 진행하지 않는다. 공지 발행 게이트는 이 스킬에 없다 — 그것은
`design-change-notice` 가 배포 선언 때 따로 연다.

### ⛔ 브리핑 서식 — 사용자 확정 (2026-08-28)

⚠ **파일 첨부만 하고 요약을 화면 코드로 늘어놓지 마라.** 사용자가 두 차례 「더 이해하기 쉽게
설명해 달라」고 되물었다 — 승인을 구하는 자리에서 되묻게 만들면 게이트가 제 역할을 못 한다.
아래 네 절을 **이 순서로, 이 제목 취지대로** 쓴다.

```markdown
## 1. 어떤 화면이고, 개발팀이 왜 물어봤나
<화면 전체 이름 + 코드>. <이 화면이 현장에서 무엇을 하는 화면인지 한두 줄 — 업무 용어로>
<무엇이 막혔는지. 필요하면 「못 채우는 항목 / 무슨 값인가 / 왜 못 채우나」 3열 표>

## 2. 조사해보니 — <한 줄 결론>
<요청자 전제가 맞았는지 틀렸는지. 틀렸으면 무엇이 실제였는지>

## 3. 제 권고
<무엇을 하자는 것인지 한 문단. 그렇게 하면 개발팀에 무엇이 풀리는지>

## 4. 당신이 결정할 것 — <몇 가지인지>
<선택지가 있으면 「무엇을 하나 / 장점 / 단점」 3열 표 + 권장안 명시>
<선택지가 없으면 「이대로 진행할까요」 한 줄>
```

**지켜야 할 것**

| ⛔ | 무엇 |
| --- | --- |
| 화면 코드 단독 표기 금지 | `P-02-03` ❌ → **자재 투입 스캔·오투입 검증**(`P-02-03`) ✅. 조항·이슈 번호도 같다 — `A-21` ❌ → 부분 확정 위험 조항(`A-21`) ✅ |
| 내부 용어를 설명 없이 쓰지 않는다 | 「계약」·「미결」·「착지」·「파생」은 우리 말이다. 처음 쓰는 자리에서 풀거나 일상어로 바꾼다 |
| 산출물 파일명을 브리핑 본문의 뼈대로 삼지 않는다 | 사용자는 `03_brief.md` 가 무엇인지 알 필요가 없다. 파일은 첨부로 두고 본문은 **업무 이야기**로 쓴다 |
| 결정을 뭉뚱그리지 않는다 | 「승인할까요」 ❌ → **무엇을 고르는 것이고 각각 무슨 대가가 있는지** 표로 ✅ |
| 권고를 숨기지 않는다 | 선택지를 나열만 하지 말고 **어느 쪽을 권하는지와 그 이유**를 반드시 적는다 |

⭐ **범위가 요청보다 넓어졌으면 그 사실을 4절에서 별도 항목으로 세운다** — 「요청 범위 / 실제
반영 범위」를 표로 대조하고, 넓힌 근거와 좁힐 때의 퇴로를 함께 준다. `#252` 가 그 형태였다
(요청은 화면 하나인데 계약 세 곳을 고치게 된 건).

⭐ **영향받는 화면을 나열할 때는 「무슨 화면인지 · 설계가 지금 어떤 상태인지 · 왜 걸리는지」를
함께 적는다.** 이슈 번호만 나열하면 사용자가 다시 물어야 한다(`#252` 에서 실제로 되물었다).
⚠ 여기서 말하는 「상태」는 **우리 설계 문서의 상태**다. 개발팀이 그 화면을 어디까지 만들었는지는
**우리가 보유하는 정보가 아니므로 브리핑에 적지 않는다**(2026-09-03 개정 · V3 규칙 3).

⭐ **사용자 확인이 더 필요해 여기서 멈추면** — 자기 이슈의 `status:in-progress` 를 `help wanted`
로 바꾸고, 물을 것을 `[확인 요청] <제목>` 이슈(같은 `omf-mes` 저장소 · `Agent : Architect`)로
세운다. 답이 오면 `help wanted` 를 되돌리고 이 게이트를 다시 연다.

## Phase 5 — 반영

`design-doc-writer`(sonnet)를 스폰해 승인된 `03_brief.md` 를 실행시킨다. writer 가 검사기
빨간불로 되돌리면 Phase 3(analyst)으로 되돌아가 재판정한다 — writer 가 스스로 고치지 않는다.
커밋 메시지 꼬리는 `(요청 <식별자>)` 다(옛 `(#N)` 은 이슈 번호가 없어졌으므로 쓰지 않는다).

검사기 호출 — **표 전건이 정본이다.** 「지시서가 건드린 파일 종류」로 고르되, ⭐ 표가 실물보다
적으면 안 돌린 것이 사고가 된다(2026-09-01 실측 — 표에 8건뿐이라 `verify-mapping-coverage` 를
**6개 PR 에서 0회** 돌렸고 `#334` 가 초록으로 병합된 뒤 빨강이 됐다).

**① 언제나 돌린다 — 무엇을 고쳤든**

| 대상 | 명령 |
|---|---|
| 인용 경로 | `verify-doc-citations.py` |
| 조항 인용 | `verify-contract-citation.py` |
| 박힌 수치 | `verify-counts.py` — 문서에 적은 「N건」이 실물과 같은가 |
| 미결 정합 | `collect-open-items.py --check` |
| 죽은 경로 | `check-dead-path-citations.py` — 2026-08-25 구조 삭제 전 경로 인용 |
| 생성 스냅숏 | `verify-generated-fresh.py [--kind md\|html] [--domain NN]` — 인자 없이 돌리면 두 축(요구목록 마크다운 9건 + HTML 배포본 9건)을 다 본다. ⛔ `--domain` 은 마크다운 축 개념이라 `--kind html` 과 함께 주지 않는다 |

**② 화면 스펙·요구서를 고쳤으면**

| 대상 | 명령 |
|---|---|
| 화면 액션 커버리지 | `verify-ui-coverage.py --domain <NN>` |
| ⭐ 매핑 커버리지 | `verify-mapping-coverage.py` — 요구목록의 액션이 요구서 §3 에 다 있는가. **스펙과 요구서 중 한쪽만 고치면 여기서 터진다** |
| 화면 정본 | `verify-screen-inventory.py` — 인벤토리와 통합 IA 가 같은 화면을 말하는가 |
| 옛 표기 | `verify-stale-terms.py <기준ref>` — 표기를 바꿨을 때만. ⛔ 막지 않는다(항상 종료 0). ⛔ 인자 없이 돌리면 기본 `HEAD` 비교라 「대조한 파일 0」로 공허하게 초록이다 |

**③ 계약 JSON 을 고쳤으면** — `git diff --name-only -- design/wiki/api-contracts/openapi/` 가 비지 않으면 전건

| 대상 | 명령 | 무엇을 막나 |
|---|---|---|
| 계약 구조 | `openapi/check-structure.py` | 계약으로 성립하는가 |
| 공개 안전 | `openapi/check-public-safe.py` | 단가·내부 주소 유출 |
| enum 협착 | `openapi/check-enum-narrowing.py $(git merge-base origin/main HEAD)` | 자유문자열→enum · 값 삭제. ⛔ 인자 없이 돌리면 기본 `HEAD` 비교라 커밋 후 항상 초록 |
| ⭐ 필수 변경 | `openapi/check-required-change.py $(git merge-base origin/main HEAD)` | optional→required. **`check-enum-narrowing` 이 못 보는 파괴 변경**. ⛔ 인자 없이 돌리면 기본 `HEAD` 비교라 커밋 후 항상 초록 |
| ⭐ 조회 표준형 | `openapi/check-query-envelope.py` | 목록·요약 응답이 §L 게이트를 지키는가. **목록에 질의를 더하고 `/summary` 짝을 안 고치면 여기서 터진다**(`L-1-1 ⑶`) |
| ⭐ 코드그룹 이름 | `openapi/check-code-group-pointer.py` | 등록부에 없는 그룹 이름을 계약이 가리키면 화면이 **빈 목록**을 받는다 |
| ⭐ 코드그룹 도달 | `openapi/check-code-group-reachable.py` | 계약엔 있는데 요구서 §3 에 없어 **화면이 부를 줄 모르는** 그룹(래칫 — 늘면 ⛔) |
| 예시값 | `openapi/check-example-placeholder.py` | `example` 이 확정값 밖 — 구현팀이 그 값으로 만든다(`#191`). ⛔ 아직 **막지 않는다**(종료 0) — `#191` 반영이 끝나 0건이 되면 게이트로 올린다 |
| 저장 충돌 토큰 | `openapi/check-lock-token-source.py` | `If-Match` 를 쓰라면서 `ETag` 받을 곳이 없는 자리 |
| 귀속 사번 | `openapi/check-worker-no.py` | 사번을 받을 곳이 계약에 있는가 |
| 오프라인 표기 | `openapi/check-offline-consistency.py` | 계약의 오프라인 표기 ↔ 그 오퍼레이션을 부르는 화면의 판정 |

⛔ **초록을 「내가 안 깼다」로, 빨강을 「내가 깼다」로 읽지 마라.** ③ 중 일부는 **손대기 전부터
비초록**이다. ⭐ **고치기 전에 먼저 돌려 기준선을 잡고**, 반영 뒤 값과 대조해 «내가 낸 것»만 가른다.

| 검사기 | 2026-09-02 `055557d` 기준선 | 성격 |
| --- | --- | --- |
| `check-lock-token-source` | ⚠ 2건 · 종료 1 | 의도된 보류 — 「⛔ 자동으로 붙이지 마세요, 원천이 부모 자원인가 자식 집합인가는 판단이다」 |

⭐ **`check-worker-no` 는 초록이 됐다**(2026-09-02 · `omf-mes#350`) — 이 표에서 지운다.
⚠ 종료 0 이지만 **⚠ 3건은 남아 있다**(현장·모바일이 안 부르는 오퍼레이션에 귀속 헤더).
그건 검사기 자신이 「막지 않는다 — 요구서에 아직 안 적힌 화면일 수 있다」로 둔 것이다.

⛔ **초록인 검사기가 «안 보는 것»도 함께 읽는다.** `check-required-change` 는 스키마의
필드만 보고 **헤더·질의 파라미터의 `required` 뒤집힘은 초록으로 통과시킨다**(`omf-mes#350`
에서 실물 확인 · 확장은 `omf-mes#359`). 그 자리의 등급은 아직 사람이 매긴다.

⚠ 이 표는 **떠 있는 값이다.** 기준선이 바뀌면 여기 날짜와 커밋을 함께 갱신한다 — 갱신을 잊은
표는 「내가 깼나」를 다시 못 가리게 만든다.

각 검사기가 "안 보는 것"은 `03_brief.md` 에 명시한다 — `check-enum-narrowing` 은 필드 삭제·
경로 삭제·필수 헤더 신설·**의미 변경**을 못 잡는다(required 승격은 `check-required-change` 가
따로 본다). ⭐ **의미 변경은 어느 검사기도 못 잡는다** — 자리 수도 글자 수도 그대로인 채 뜻만
바뀌므로 등급표로 수동 보강한다.

계약 JSON 을 고쳤으면(`git diff --name-only -- design/wiki/api-contracts/openapi/`) 아래
등급표 **전 행을 채운다**(빈칸 금지). 등급 어휘의 정본은
`design-change-notice/references/change-grades.md` 다 — 아래 표는 그 등급을 검사기에 대응시킨
것이고, 어긋나면 그쪽이 이긴다.

| 계약 변경 | 등급 | 자동 검출 |
|---|:-:|:-:|
| `required`·널 허용 변경 — **요청/응답으로 갈라** 판정(요청 `required` 추가 ⛔ · 응답 `required` 제거 ⛔ · 그 반대는 ⚠ — `change-grades.md` 「필수 여부」 4행 표) | ⛔/⚠ | `check-required-change.py`(스키마 필드만 — 헤더·질의 파라미터는 수동) |
| 경로/오퍼레이션 삭제, 필드 삭제, 필수 헤더 신설, **의미 변경** | ⛔ | 없음 — 수동 |
| 자유문자열→enum, enum 값 삭제 | ⛔ | `check-enum-narrowing.py` |
| 필드·경로 신설, enum 값 추가 | ⚠ | 없음 — 수동 |
| description만 | ℹ | — |

⭐ **등급은 우리 안에서 쓰는 값이다.** V3 공지는 등급을 싣지 않고(「달라진 지점」만), 답변서의
「근거」 칸도 등급이 아니라 **경로·절**을 싣는다(「답변」 본문에 「응답 스키마에서 `required` 가
빠져 값이 `null` 로 올 수 있다」 같은 **사실**을 적는 것은 된다 — 「그러니 널 가드를 넣어라」는
지시가 안 된다). 등급표를 채우는 이유는 **파괴 변경을 의식하고 고쳤는가**를 6b 잠금 ② 가 되짚기
위해서다.

반영을 PR 로 올린다(`gh pr create --repo CREFLEINC/omf-mes`). 계약 JSON·화면 스펙 수정
자체에는 별도 게이트를 두지 않는다 — PR 이 게이트이고 병합 전까지 완전 가역이다.

## Phase 5b — PR 병합 확인

```
gh pr view <PR> --repo CREFLEINC/omf-mes --json mergedAt,mergeCommit
git fetch origin main
git merge-base --is-ancestor <mergeCommit.oid> origin/main && echo "조상 ✅"
```
`mergedAt` 이 null 이거나 `mergeCommit.oid` 가 `origin/main` 의 조상이 아니면 **정지하고
병합을 기다린다.** 그 상태로 Phase 6a 로 넘어가지 않는다 — #206·#196 이 이 확인 없이 진행해
병합 34~43분 전에 닫혔다. 답변서가 인용할 병합 해시는 여기서 확인된 `mergeCommit.oid` 다.

## Phase 6a — 답변서 작성 (닫지 않는다)

`03_reply.md` 를 확정해 **`$REQ/답변서.md`** 로 쓴다. 서식은 아래 그대로다(`team-issue-protocol`
§7 이 같은 서식을 정본으로 둔다) — 첫 줄은 `# 개발팀에 전달사항 — <요청 식별자>` 로 **시작**한다
(V2 line 62 규약 계승).

```markdown
# 개발팀에 전달사항 — <요청 식별자>

| 항목 | 값 |
| --- | --- |
| 요청 | <자료 제목> (접수 <YYYY-MM-DD>) |
| 판정 | 반영 / 부분 반영 / 반영하지 않음 / 정보 회신 |
| 반영 PR | `CREFLEINC/omf-mes#<n>` · 병합 `<해시7>` (반영 없으면 「—」) |
| 근거 | <경로 · 절> 목록 |

## 답변

<사실과 판정. 개발팀이 무엇을 어떻게 고쳐야 하는지는 적지 않는다(V3 규칙 3). DB 를 근거로 인용하지 않는다>

## 다음 공지

이 변경은 다음 「설계 변동 공지」에 실린다 / 설계 변경 없음 — 공지에 실리지 않는다
```

개선 제안서면 「판정」 칸의 어휘만 「채택 / 부분 채택 / 채택하지 않음」 이다.

확정 전 검사:
- `python3 .claude/skills/uiux-design/scripts/check-report-language.py $REQ/답변서.md` —
  화면 번호·조항 번호·업무 약어가 표 밖에서 한 번 전개돼 있는가.
- **근거 블록의 모든 경로를 `test -f` 로 확인**, 실재하지 않으면 그 줄을 제거하거나 대체 경로로
  교체한다(2026-08-25 구조 삭제로 죽은 경로를 근거로 남긴 회신 `#222` 선례).
- 「반영 PR」 칸에 PR 번호 + **병합 해시**(Phase 5b 의 `mergeCommit.oid` 앞 7자)를 명시 —
  자리표시자(`<n>`·`<해시7>`·`TODO`)를 남기지 않는다. 반영이 없으면 「—」.
- 「답변」에 **DB 테이블·컬럼·제약**을 근거로 인용하지 않았는가 — DB 는 설계의 결과물이지
  근거가 아니다. 근거는 언제나 `design/wiki/` 의 경로·절이다.
- 「답변」에 개발팀이 **무엇을 어떻게 고쳐야 하는지**가 적혀 있지 않은가(규칙 3) — 「`null` 로
  올 수 있다」는 **사실**은 적되 「널 가드를 넣어 달라」는 **지시**는 적지 않는다.
- 답변서는 사용자가 어디로 전달할지 이 스킬이 모른다 — 단가·내부 주소 등 **공개 금지 항목**
  (`design-change-notice/references/public-boundary.md`)은 처음부터 싣지 않는다.

**전달은 사용자가 한다(규칙 2).** 이 스킬은 「답변서 경로 `$REQ/답변서.md`」를 사용자에게
보고하는 것으로 이 Phase 를 끝낸다.

⛔ **`gh issue comment` 로 개발팀 저장소·개발팀 이슈에 회신하지 않는다.** V2 에서는 「요청
이슈의 코멘트」가 게시 수단이었지만, V3 규칙 2 가 설계팀→개발팀 직접 소통을 공지 하나로
좁혔다 — 대상이 「개발팀 이슈」에서 「사용자에게 건네는 파일」로 바뀐 것이다. `gh issue create`
도 개발팀 저장소에는 쓰지 않는다 — 우리가 개발팀 저장소에 세우는 유일한 이슈는 설계 변동
공지(`design-change-notice`)다.

⛔ **이 Phase 에서 자기 이슈를 닫지 않는다.** 닫기는 6b 의 3중 잠금 뒤다.

## Phase 7 — 공지 (한 줄)

**이 변경은 다음 「설계 변동 공지」에 실린다 — 즉시 통지하지 않는다**(`design-change-notice`,
배포 선언 단위 — 직전 `notice/*` 태그 → HEAD 를 묶어 사용자가 「공지해」라고 할 때 1건).
답변서 「다음 공지」 절에 그 문장을 적는다. 설계 변경이 없었으면(정보 회신·반영하지 않음)
「설계 변경 없음 — 공지에 실리지 않는다」로 적는다.

## Phase 6b — 자기 이슈 닫기 (3중 잠금)

셋 다 통과해야 닫는다. 하나라도 실패하면 닫지 않고 사유를 `$RUN/05_close.md` 에 기록한다.

| 잠금 | 확인 | 통과 조건 |
|:-:|---|---|
| ① PR 병합 | `gh pr view <PR> --repo CREFLEINC/omf-mes --json mergedAt,mergeCommit` → `git merge-base --is-ancestor <mergeCommit.oid> origin/main` | `mergedAt` non-null **이고** oid 가 `origin/main` 조상(Phase 5b 재확인). PR 없는 회차(E·F·H 정지 · 정보 회신)는 「해당 없음」 |
| ② 계약 등급 | `git diff --name-only $(git merge-base origin/main <oid>^)..<oid> -- design/wiki/api-contracts/openapi/` | 비지 않으면 `design-change-notice/references/change-grades.md` 등급표로 ⛔/⚠ 판정이 **전 행 채워져** 있고, 답변서 「근거」에는 등급이 아니라 **경로·절**이 실려 있다. 비면 「해당 없음」 |
| ③ 답변서 실재 | 답변서의 병합 해시 `git cat-file -e <해시>^{commit}` · 근거 경로 전건 `test -f` · PR 번호 `gh pr view` 존재 | 셋 다 실재. 자리표시자 0 |

통과하면 **답변서 전문을 닫는 코멘트로** 남기고 닫는다 — 자기 저장소라 공개 비용이 0 이고,
`tmp/` 는 gitignore 라 이 코멘트가 답변서의 **유일한 영구 사본**(감사 추적)이다:
```
gh issue edit  <자기 이슈> --repo CREFLEINC/omf-mes --remove-label "status:in-progress"
gh issue close <자기 이슈> --repo CREFLEINC/omf-mes --comment "$(cat $REQ/답변서.md)"
```
(`team-issue-protocol` §7-1 과 같은 형태 — 코멘트와 닫기를 한 명령으로 묶어 «코멘트만 남고
안 닫힘»·«닫혔는데 코멘트 없음»을 막는다.)

닫을 수 없는 경우(사용자 확인 대기 · 반영 PR 미병합)는 `status:in-progress` → `help wanted` 로
교체하고 사유 코멘트를 남긴 채 열어 둔다. 사용자에게 물을 것이 있으면 `[확인 요청] <제목>`
이슈(같은 저장소)로 세운다 — 그것도 자기 저장소라 규칙 2 위반이 아니다.

⛔ **옛 잠금 ④(「표지로 찾은 화면 0건」)는 2026-09-03 에 걷어냈다 — 되살리지 않는다.** 안 지켜진
데이터(표지 없는 미결 244/369행)에 건 게이트가 `#349` 에서 살아 있는 미결을 「해소」로 세었다.

## Phase 8 — 되먹임 (권장)

⭐ **추적 표지는 «권장»이다.** 반영한 화면의 스펙 §8 에 **요청 식별자**(예: `요청
2026-09-03-lot-hold-필드-누락`)를 심어 두면 나중에 그 결정을 되짚을 수 있다. 붙이면 편하지만
**없다고 게이트가 막지 않는다**(6b 에 그 잠금이 없다).

⚠ **왜 의무가 아닌가** — 표지는 「답이 오면 어디를 고칠지」를 **미리 적는 예측**이다. V3 는
설계팀이 개발팀의 진행을 예측·보유하지 않고 **사후에 무엇이 언제 바뀌었는지만** 공지하는
쪽이다. 예측을 의무로 걸면 안 지켜지고, 안 지켜진 표지에 게이트를 걸면 틀린 답이 나온다.

⚠ `collect-open-items.py` 의 표지 인식(`TRACKS`·`tag_issues`)은 아직 `#N`·`DR-nnn`·조항
형식만 읽는다 — kebab 식별자는 `--issue` 역조회에 잡히지 않는다. 생성기를 넓히기 전까지는
`grep -rn "요청 <식별자>" design/wiki/screens/` 로 되짚는다.

표지를 심었든 아니든 그 후:
```
python3 design/schema/generators/collect-open-items.py
python3 design/schema/generators/build-screen-progress.py
```
을 재생성한다.

## V3 이전 유산 — `[검토 요청]` 이슈

사용자가 「232번 검토 요청 처리해줘」처럼 **이슈 번호**로 부르면 이 스킬은 받지 않는다 — 열린
`[검토 요청]`·`[client→uiux]` 이슈는 V3 이전 유산이고 사용자가 정리한다. 「그 이슈의 내용을
자료로 만들어 주시면 처리합니다」로 답하고 멈춘다. 그 자료가 오면 보통 절차(Phase 0 부터)다 —
이슈 번호는 자료 제목의 일부로만 남고, 그 이슈에 코멘트를 달거나 닫지 않는다.

## 에러 핸들링

- `gh` 실패(네트워크·권한) — 재시도 1회, 재실패 시 "확인 불가"로 표시하고 그 Phase 를 완료로
  간주하지 않는다. `gh` 가 필요한 자리는 Phase 0'·5·5b·6b 뿐이다.
- analyst/writer 가 도구 실패로 중단 — 산출된 부분 파일은 보존하고, 사용자에게 상태를 보고한 뒤
  재개 여부를 확인한다(같은 식별자로 재호출하면 Phase 0 이 부분재실행으로 잡는다).
- Phase 0'·4 게이트에서 사용자가 반려 — 반려 사유를 `$RUN/` 에 기록하고 해당 Phase 의 산출물만
  다시 만든다(전체를 재시작하지 않는다). 접수 자체를 반려하면 자기 이슈를 세우지 않고 끝낸다.
- 사용자 확인이 필요해 멈춤 — `help wanted` + `[확인 요청]` 이슈(Phase 4·6b 참조). 대기 중에도
  개발팀은 업무를 멈추지 않는다(규칙 1) — 「기다려 달라」는 문장을 답변서에 쓰지 않는다.

## 테스트 시나리오

**정상 흐름** — 자료 「W-04-03 Lot 보류 이력 화면의 계약 결손」(단일 화면 · 설계 개선):
P0 식별자 `w-04-03-lot-보류-이력-계약-결손` · 런 디렉토리 생성 → P1 `요청.md`·`00_request.md`
저장, 한 줄 요약 「보류 이력 목록 응답에 해제 사유 필드가 없다」 → P0' 접수 확인 → 자기 이슈
`[요청 처리] …` 생성(`Agent : Architect` 첫 회차면 라벨 생성) → P2 물음 3개로 분해, 2건 적합·
1건 유형 B → P3 analyst 재실측 후 3종 산출 → **P4 게이트에서 정지**(승인 후) → P5 writer 가
계약에 필드 추가, 커밋 꼬리 `(요청 w-04-03-…)` → P5b 병합·조상 확인 → P6a `답변서.md`(판정
「부분 반영」 · 병합 해시 · 근거 경로) → 사용자에게 경로 보고 → P7 「다음 공지에 실린다」 →
P6b 3중 잠금 통과 → 답변서 전문 코멘트 + 닫힘 → P8 재생성(표지는 권장).

**정지 흐름** — 자료가 화면도 경로도 지목하지 않고 「일정을 정해 달라」만 담김: P2 에서 전
물음이 E·F → P3 없이 P6a 로 — 답변서 판정 「반영하지 않음」(E: 우리는 정하지 않는다 · F:
어느 화면·어느 절인지 보완 요청) → P6b 잠금 ①·② 「해당 없음」, ③ 통과 → 닫힘.

**개선 제안서 흐름** — 규칙 1 의 제안서: 절차는 정상 흐름과 같고, 답변서 「판정」 만
「채택 / 부분 채택 / 채택하지 않음」. 채택이면 반영 PR 이 있고 다음 공지에 실린다.

**유산 흐름** — 「232번 검토 요청 처리해줘」: 받지 않는다. 「이슈 내용을 자료로 주시면
처리합니다」로 멈춘다 — `gh issue view 232` 를 돌리지 않는다.

**에러 흐름** — analyst 가 재실측 중 인용 경로 다수가 죽어 있음을 발견(2026-08-25 구조 삭제
영향): `02_measure.md` 에 "근거 경로 붕괴 — <N>곳"으로 기록, `03_reply.md` 에는 살아있는 경로만
남기고 Phase 4 게이트에서 "이 요청 범위 밖에서 구경로 인용이 광범위하게 남아 있다"를 별도로
사용자에게 보고(하네스 밖 후속 과제 — `[설계] <제목>` 자기 이슈로 분리, 이 요청 처리는 계속 진행).
