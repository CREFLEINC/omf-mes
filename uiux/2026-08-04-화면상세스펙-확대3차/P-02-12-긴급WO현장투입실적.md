# P-02-12 · 긴급 W/O 현장 투입·실적

> 작성일: 2026-08-04 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 3차 10/10** — ⚠ **이 장에서 확정과 물리 모델이 정면으로 부딪힌다.** 「P/O 비귀속 긴급 W/O」가 **데이터로 만들어질 수 없다.**

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `P-02-12` · 긴급 W/O 현장 투입·실적 |
| 프로그램 · 도메인 | **POP**(1024×768 + 스캔) · 02 생산실행 |
| 유형 | 스캔 |
| 행위자 | 작업자 · 현장반장 |
| 신뢰도 | **확정** |
| 범위 | 긴급 W/O의 현장 실행 전 구간 — **정상 경로 화면 재사용 + 긴급 플래그**(4단계 확정) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §4.2 **02-S-D 긴급 생산** — 진입=긴급 생산 요청(**리셉션-only** · 메인 발신은 **의도 삭제 유지** — 2026-07-24 ③ 확정)<br>태스크: ① **긴급 W/O 발행**(**P/O 비귀속** · 발행자 직접 전개 · **별도 승인 없음**) **[점검 통제 우회 허용 · 배정 없이 투입]** ② 자재 즉시 투입(기출고 라인 재고 · **BOM 정합 교차 투입** — 부족 시 「추가 자재 출고 요청」 수동+사유 → `W-02-10`) ③ 금형·설비 투입 ④ 실적 입력 ⑤ LOT 완료·인식표 발행<br>「**현장 실행 전 구간 = 정상 경로 화면 재사용 + 긴급 플래그**(`P-02-12`, 4단계 확정)」 |
| **화면·IA** | 인벤토리 v1.5 `P-02-12` — 유형 스캔 · 데이터=작업지시·4M투입·작업실적 · 배지=POP |
| **요구** | ✓확정 **QA #9** 작업 통제 3단계 + **긴급 W/O 우회 허용** · **R24** 투입→배정 **nullable 참조**(긴급=null) |
| **데이터** | `production.work_order`(35열 — **`production_plan_id` NOT NULL**·`work_order_type_code`) · `planning.production_plan`(**`production_order_id` NOT NULL**) · `material_consumption` · `production_result` — 실측 |

## §3. 레이아웃 (POP — 1024×768)

**정상 경로 화면 재사용**이 확정이므로 이 화면은 **긴급 표식과 진입점**을 제공하고 나머지는 기존 화면으로 보낸다.

```
┌ 1024 ────────────────────────────────────────────────────────────────────┐
│ 🚨 긴급 W/O                                          단말 POP-L1 ●       │
├──────────────────────────────┬───────────────────────────────────────────┤
│ 《긴급 W/O 목록》            │ 《WO-…E-002》  🚨 긴급                    │
│ ┌──────────────────────────┐ │   품목    XYZ-999                         │
│ │ 🚨 WO-…E-002  XYZ-999   │ │   수량    50 EA                           │
│ │    50 EA   발행 14:20    │ │   발행    14:20 · 김OO                    │
│ └──────────────────────────┘ │   ──────────────────────                  │
│                              │   ⚠ 배정 없음 · 점검 통제 우회            │
│  ⓘ 긴급 W/O 는 관리웹        │                                           │
│    W-02-07 에서 발행합니다    │   ┌──────────────┐  ┌──────────────┐     │
│                              │   │  자재 투입   │  │   실적 입력  │     │
│                              │   └──────────────┘  └──────────────┘     │
│                              │      → P-02-03          → P-02-04         │
│                              │      (긴급 플래그 유지)                    │
└──────────────────────────────┴───────────────────────────────────────────┘
```

## §4. 필드

### §4-A. 긴급 W/O `production.work_order`

| 라벨 | 출처 컬럼 | 필수 | 비고 |
| --- | --- | :-: | --- |
| **유형** | `work_order_type_code` | ✅ 기본 `NORMAL` | **긴급 값이 필요하다** ⚠ 값 목록 §8-1 |
| **생산계획** | `production_plan_id` | ✅ **NOT NULL** | ⛔ **여기가 부딪힌다** §5-2 |
| 4M 계획 | `planned_equipment_id` · `planned_mold_id` · `planned_shift_id` | — | **전부 nullable** — 배정 없이 발행 가능 ✅ |
| (그 밖) | `W-02-04` §4-A 참조 | | |

### §4-B. 투입·실적

`P-02-03`(투입)·`P-02-04`(실적, 앵커)와 **같은 테이블**을 쓴다. 긴급이라고 별도 테이블이 없다.

**R24** — 「투입 → 배정 **nullable 참조**(긴급 = null)」. `material_consumption`에 배정 참조가 없고 `work_session_id`가 nullable이므로 **배정 없이 투입이 기록된다** ✅.

## §5. 액션·상태

### §5-1. 이 화면은 진입점이다

**4단계 확정** — 「현장 실행 전 구간 = **정상 경로 화면 재사용 + 긴급 플래그**」

긴급이라고 투입·실적 화면을 따로 만들지 않는다. 이 화면은

1. **긴급 W/O 목록**을 보이고
2. 선택 시 **정상 화면으로 보내되 긴급 플래그를 유지**한다

**긴급 W/O 발행 자체는 관리웹**(`W-02-07`)이다 — 프로세스가 「발행자 직접 전개」라 하고, 인벤토리에 `W-02-07`(긴급 W/O 발행)이 실재한다.

### §5-2. ⛔ 확정과 데이터가 정면으로 부딪힌다

**확정** — 02-S-D 태스크 ① 「긴급 W/O 발행(**P/O 비귀속** · 발행자 직접 전개 · 별도 승인 없음)」

**데이터** — 실측

```
production.work_order
    production_plan_id   bigint NOT NULL  REFERENCES planning.production_plan

planning.production_plan
    production_order_id  bigint NOT NULL  ← P/O
    bom_id               bigint NOT NULL
    routing_id           bigint NOT NULL
```

**W/O → 생산계획 → P/O 체인이 전부 `NOT NULL`이다.** 즉 **P/O 없이는 W/O를 만들 수 없다.**

| 확정이 요구하는 것 | 데이터 |
| --- | --- |
| 긴급 W/O는 **P/O 비귀속** | ⛔ `production_plan_id` NOT NULL → `production_order_id` NOT NULL |
| 발행자가 **직접 전개** | ⛔ 생산계획(`bom_id`·`routing_id` NOT NULL)이 선행돼야 한다 |

**세 가지로 읽을 수 있다.**

| 해석 | 뜻 | 대가 |
| --- | --- | --- |
| **가. 더미 P/O·계획을 만든다** | 긴급용 P/O를 자동 생성해 체인을 채운다 | ERP 정본 원칙(R07 「P/O = ERP 발행·MES 수정 불가」)과 충돌 |
| **나. 컬럼을 nullable로** | `production_plan_id`를 nullable로 바꾼다 | 데이터 모델 변경 필요 |
| **다. 긴급 전용 계획** | MES가 만드는 「긴급 생산계획」을 허용 | `production_order_id`가 여전히 NOT NULL이라 P/O가 필요 |

**셋 다 데이터 모델 담당의 판단이 필요하다.** 화면이 정할 수 있는 것이 아니다.

> ⚠ **이것이 이 차수 세 번째 확정↔모델 불일치이고, 성격이 가장 무겁다** — 앞의 둘(`W-02-04` 선발행 · `P-02-11` LOT BOM 스냅샷)은 **표현이 어색한 것**이었으나 이것은 **아예 만들 수 없는 것**이다.

### §5-3. 통제 우회 — 데이터에 근거가 없다

**✓확정 QA #9** — 「작업 통제 = 차단/경고/미적용 **3단계 정책 설정형** + **긴급 W/O 우회 허용**」

**우회를 표현할 컬럼이 없다.** `work_order`에 「통제 우회」 플래그가 없고, 통제 정책 자체(3단계)를 담을 테이블도 확인되지 않는다.

`work_order_type_code = 긴급`으로 **서버가 판단**하는 것으로 설계한다 — 유형이 긴급이면 점검 게이트를 건너뛴다.

> ⚠ 다만 **점검 게이트 자체가 `P-02-01`·`P-02-02`이고 #67로 보류**됐다. **우회할 대상이 지금 없다** — 이 화면의 「통제 우회」는 실질적으로 무의미하다. §8-3.

### §5-4. 자재 부족 시 — 수동 출고 요청

02-S-D 태스크 ② — 「부족 시 **「추가 자재 출고 요청」 수동 + 사유**(대응 화면 = `W-02-10` **[추정]** · 도식 미작화)」

**정상 경로는 자동**(R29 「W/O 생성 완료 → 출고요청 자동 발행」)인데 **긴급은 수동**이다. `W-02-04` §5-5의 ④가 「긴급 W/O 제외」인 이유다.

화면은 **부족을 감지하면 `W-02-10`을 안내**한다. 다만 `W-02-10`은 신뢰도 **추정**이고 5차 대상이다.

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| **긴급 W/O가 만들어질 수 없음** | ⛔ **§5-2** — 데이터 모델 결정 전까지 이 화면에 표시할 W/O가 없다 |
| 배정 없음 | ✅ 정상 — `planned_*`가 전부 nullable |
| 점검 미실시 | ✅ 우회(QA #9) — 다만 §5-3 |
| 자재 부족 | `W-02-10` 안내 (§5-4) |
| BOM 정합 교차 투입 | ✅ 허용 — `P-02-03` §5-3과 같은 판정 |
| 오프라인 | 큐잉 — C-1~C-4 |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 긴급 W/O 목록 | `Table`(comfortable) + 🚨 강조 | a · ⚠ G-5 |
| 긴급 배지 | `Chip`(error) | a |
| W/O 정보 | `Card` + 읽기 전용 구획 | **c** — G-10 |
| 우회 경고 | `AlertBanner`(warning) | a |
| 화면 이동 | `Button` ×2 (큰 타겟) | a · ⚠ G-5 |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | `work_order_type_code`에 **긴급 값**이 정의됐는가 | 회신 대기 | **조정** | 기본값이 `NORMAL`이므로 다른 값이 전제돼 있다 |
| 2 | ⛔ **P/O 비귀속 긴급 W/O를 데이터가 허용하지 않는다** — `production_plan_id` NOT NULL → `production_order_id` NOT NULL | 데이터 | **차단** | ⚠ **이 화면의 존재 근거가 걸린다.** `[docs→데이터모델]` 이슈 대상 §5-2 |
| 3 | **통제 우회를 표현할 컬럼이 없고, 우회할 대상(`P-02-01`·`P-02-02`)도 #67로 보류** | 데이터 | **조정** | 유형으로 서버가 판단. 게이트 복구 시 재검토 |
| 4 | **`W-02-10`(추가 자재 출고 요청)이 신뢰도 추정·도식 미작화** | 프로세스 | **조정** | 5차 대상. 이 화면은 안내만 |
| 5 | **긴급 W/O 발행 화면(`W-02-07`)과의 경계** — 발행은 관리웹, 실행은 이 화면 | 설계 | **표시** | |

**⚠ 차단 1건(#2) — 이 화면은 지금 지을 수 없다.**

데이터 모델이 P/O 없는 W/O를 허용하지 않으면 **표시할 긴급 W/O가 존재할 수 없다.** 화면 골격(목록 → 정상 화면 이동)은 단순하나 **대상이 만들어지지 않는다.**

## §9. 이 장에서 나온 공유 계약 후보

1. **「예외 경로」가 정규 체인의 `NOT NULL`에 걸리면 화면이 아니라 데이터가 답해야 한다** — 긴급·무발주 같은 우회 경로는 정규 참조를 채울 수 없다. 화면이 더미 데이터로 우회하면 정본이 오염된다 (§5-2)
2. **우회 규칙은 우회 대상이 있을 때만 의미가 있다** — 「긴급은 점검을 우회한다」가 점검 화면 부재 시 공허하다. 규칙의 전제를 스펙이 확인한다 (§5-3)

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-04 | 초안 — 확대 3차 10/10. ⛔ **확정(02-S-D 「긴급 W/O = P/O 비귀속」)과 물리 모델이 정면 충돌**함을 발견 — `work_order.production_plan_id` NOT NULL → `production_plan.production_order_id` NOT NULL 체인이라 **P/O 없이 W/O를 만들 수 없다**. 해석 3안을 정리하고 **차단 등급**으로 판정 — 이 차수 유일한 차단이자 세 번째 확정↔모델 불일치이고 **성격이 가장 무겁다**(앞의 둘은 표현이 어색한 것, 이것은 만들 수 없는 것). **통제 우회를 표현할 컬럼이 없고 우회 대상(`P-02-01`·`P-02-02`)도 #67로 보류**돼 규칙이 공허함을 기록. 미결 5건(**차단 1**) · 공유 계약 후보 2건 · 신규 DS 갭 0건. |
