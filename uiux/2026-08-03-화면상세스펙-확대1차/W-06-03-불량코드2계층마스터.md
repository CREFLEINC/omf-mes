# W-06-03 · 불량·원인코드 2계층 마스터

> 작성일: 2026-08-03 · **갱신: 2026-08-03 (범위 정정 확정 — 화면명 변경·원인코드 흡수·손실사유코드 이관)** · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 1차 8/13** — **확정된 설계 결정이 하류에 반영되지 않은 사례.** 결정 12가 「N:M 매핑 채택 — 데이터모델 관계 표기 조정 **승인**」까지 명시했는데, 그보다 **17일 뒤에 만들어진 물리 모델이 조정 전 형태**다(§8-1).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-06-03` · **불량·원인코드 2계층 마스터** *(v1.5 화면명 변경 — 원인코드 흡수)* |
| 프로그램 · 도메인 | **관리웹** · 06 기준정보연계 |
| 유형 | 설정·마스터 |
| 행위자 | 품질담당 · 운영관리자 |
| 신뢰도 | 추정 (근거 등급 3 — 설계 확정) |
| IA 위치 | 기준정보 > 품질기준 > 불량코드 |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | WF06 **S5** Action 「**불량코드에 처분구분(재작업가능/폐기) 속성 추가**」 · 도식스펙06 §1.4 **MES-11** · 도식 배지 없음(MES 노트 근거) | 실측 |
| **화면·IA** | `04` §3 `W-06-03` 행 · 인벤토리 v1.4 — 핵심 데이터 「불량코드(대분류/상세)·**공정-불량코드매핑(N:M)**·손실사유코드」 | 실측 |
| **요구** | ✅**REQ-PR-0025** 재작업/폐기 구분 — 불량 창고 우선 입고 후 판단<br>✅**REQ-PR-0035** 손실 관리(퍼징·시작불량)<br>✓설계확정 **결정 12**: **2계층 — 전사 대분류(고정 축: 외관/치수/기능/이물/기타) + 공정별 상세 코드(설정형)** · 상세 = 「대분류 귀속 + P1 패턴(관리자 추가) + **처분구분** + **다국어 명칭(ko/vi)**」 · **공정—불량코드 연결 = N:M 매핑 테이블** · **손실 사유는 별도 코드 그룹**(불량코드 체계에 섞지 않음) | 실측 |
| **데이터** | `quality.defect_code`(SQL 1892~) · `quality.cause_code`(1907~) · 참조 `quality.defect_record`(1921~ — `defect_code_id`·`suspected_cause_code_id`·`confirmed_cause_code_id`) · `production.material_loss.reason_code`(1616) | 실측 |

## §3. 레이아웃 (관리웹 — 구조 기술)

탭 3개. **불량 「현상」과 「원인」은 다른 체계**이고(하류가 분리했다 — §4-C), **공정 매핑은 교차표**다.

```
┌ PageHeader  기준정보 > 품질기준 > 불량코드                                  ┐
│  Tabs:  ● 불량코드(현상)   ○ 원인코드   ○ 공정 매핑                        │
├──────────────────┬──────────────────────────────────────────────────────────┤
│ 《불량코드》 탭                                                              │
│ 좌: 2계층 목록   │ 우: 상세 §4-A                                            │
│  ▾ 외관 (대분류) │   대분류/상세 구분 · 코드 · 명칭(한/베) · 상위 대분류    │
│    · 스크래치    │   처분구분 ⚠미착지 · 사용                                │
│    · 찍힘        │                              [변경 이력] [취소] [저장]   │
│  ▾ 치수          │                                                          │
│    · 외경 초과   │   《이 코드가 매핑된 공정》 (읽기 — 3번째 탭에서 편집)    │
│  ▾ 기능 / 이물 / 기타                                                        │
│  [＋ 대분류] [＋ 상세]                                                       │
├──────────────────┴──────────────────────────────────────────────────────────┤
│ 《원인코드》 탭 — 같은 구조 (§4-C)                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ 《공정 매핑》 탭 — 공정 × 상세 불량코드 교차표 (MatrixGrid)                  │
│        │ 사출 │ 조립 │ 검사 │ 부착 │ …                                      │
│  스크래치 │  ☑  │  ☑  │  ☐  │  ☑  │                                        │
│  외경초과 │  ☑  │  ☐  │  ☑  │  ☐  │      셀 클릭 = 매핑 토글               │
│  ⚠ 이 탭은 N:M 매핑 테이블이 있어야 성립한다 — §8-1                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

- **대분류는 고정 축**이다(결정 12 「개수 적고 안정적」). 화면은 추가를 막지 않되 **경고**한다 — 대분류가 늘면 전사 집계·사업부 간 비교가 흔들린다.
- **손실사유코드 탭은 두지 않는다.** 결정 12가 「손실 사유는 **별도 코드 그룹** — 불량코드 체계에 **섞지 않음**」으로 확정했으므로 `W-06-06`(공통코드) 소관이다 → **인벤토리 핵심 데이터 정정 §8-4**.

## §4. 필드

감사 컬럼과 `version_no`는 표에서 제외(공유계약 A-4).

### §4-A. 불량코드 `quality.defect_code`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 불량코드 / *(미정)* | `defect_code` | `code_t` varchar(50) | ✅ | ✅ **(전역)** | 「이미 사용 중인 불량코드입니다」 | ⚠ B-4 |
| 불량명 / *(미정)* | `defect_name` | `name_t` varchar(200) | ✅ | | **⚠ 결정 12가 「다국어 명칭(ko/vi)」로 컬럼 형태까지 지정했는데 단일 컬럼이다** §8-2 | ✅ |
| 상위 대분류 / *(미정)* | `parent_defect_code_id` | FK → `quality.defect_code` (자기참조) | — | | **비우면 대분류, 채우면 상세**로 읽는다 `[추정]` — **2계층 강제 제약이 없다** §8-3 · **자기참조 CHECK도 없다**(계층 컬럼을 가진 테이블 중 `location`·`defect_code`에만 없다 — `department`·`production_line` 등에는 있음 — 실측) | ✅ |
| 공정 / *(미정)* | `process_id` | FK → `mdm.process` | — | | ⚠ **N:1이다 — 결정 12가 N:M으로 조정 승인한 바로 그 컬럼** §8-1 | ⚠ §8-1 |
| **처분구분** / *(미정)* | **⛔ 없음** | — | — | | ✅REQ-PR-0025·결정 12 확정인데 **컬럼이 없다** §8-2 | — |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | 끄면 신규 입력 선택지에서 제외 · **과거 실적은 그대로**(`W-06-06` §9-2) | ✅ |

### §4-B. 대분류/상세 구분 — 화면이 만드는 축

물리 모델에는 **「대분류인가 상세인가」를 직접 말하는 컬럼이 없다.** `parent_defect_code_id`의 NULL 여부로 읽는다.

| 구분 | 판별 | 결정 12의 성격 |
| --- | --- | --- |
| **대분류** | `parent_defect_code_id IS NULL` | 전사 공통·고정 축 — 외관/치수/기능/이물/기타 |
| **상세** | `parent_defect_code_id IS NOT NULL` | 공정별·설정형(관리자 추가 가능) |

⚠ **3계층 이상을 DB가 막지 않는다.** 상세의 하위에 또 코드를 달 수 있다. 결정 12는 **2계층**이므로 **화면이 깊이를 강제**한다 → §8-3.

### §4-C. 원인코드 `quality.cause_code` — **인벤토리에 없던 것**

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 비고 |
| --- | --- | --- | :-: | :-: | --- |
| 원인코드 / *(미정)* | `cause_code` | `code_t` | ✅ | ✅ (전역) | |
| 원인명 / *(미정)* | `cause_name` | `name_t` | ✅ | | 다국어 결함 동일 |
| 상위 원인 / *(미정)* | `parent_cause_code_id` | FK (자기참조) | — | | `defect_code`와 **완전히 같은 구조** |
| 공정 / *(미정)* | `process_id` | FK → `mdm.process` | — | | 동 |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | |

> **⚠ 하류가 앞서간 사례.** SQL 1906 주석: 「**불량 원인 기준정보: 불량현상(`defect_code`)과 분리 관리** (2-6 §7.3)」. 인벤토리 v1.4의 `W-06-03` 핵심 데이터에는 **원인코드가 없고**, 다른 화면에도 없다 — **관리할 곳이 없는 마스터**다. 그런데 `quality.defect_record`가 `suspected_cause_code_id`(추정 원인)·`confirmed_cause_code_id`(확정 원인) **두 컬럼으로 참조**한다(실측) — **쓰는 곳은 있는데 관리할 곳이 없는 상태**로, 인벤토리 v1.3이 `W-06-06`에 작업자를 귀속시킬 때와 **같은 패턴**이다(적대 검증 N4). **이 화면이 흡수하는 것을 제안** → §8-5.

### §4-D. 공정 매핑 — **테이블이 없다**

결정 12가 확정한 **N:M 매핑 테이블**이 물리 모델에 없다(실측 — `defect_code_process`·`process_defect_code_map` 류 0건). §3의 「공정 매핑」 탭은 **테이블 신설 후에 성립**한다 → §8-1.

**신설 요청 명세** *(이 화면이 필요로 하는 최소 형태)*:

| 컬럼 | 타입 | 비고 |
| --- | --- | --- |
| `defect_code_id` | FK → `quality.defect_code` | **상세 코드만** 매핑(대분류는 전사 축) |
| `process_id` | FK → `mdm.process` | |
| UNIQUE | (`defect_code_id`, `process_id`) | 부여·회수 형태(`created_at`·`created_by`만) |

## §5. 액션·상태

### §5-1. 액션

| 액션 | 위치 | 활성 조건 | 비고 |
| --- | --- | --- | --- |
| 대분류 추가 | 불량코드 탭 | 권한 | **경고 동반** — 「대분류는 전사 집계 축입니다. 추가하면 기존 보고서 비교가 끊깁니다」 |
| 상세 추가 | 불량코드 탭 | 대분류 선택됨 | `parent_defect_code_id` 자동 설정 |
| 저장 / 취소 | 상세 폼 | 변경 있음 + 검증 통과 | |
| 사용 중지 | 상세 폼 | 사용 중일 때 | 물리 삭제 없음(B-4) · **참조 건수 표시** |
| 원인코드 추가·편집 | 원인코드 탭 | 권한 | 불량코드 탭과 동일 |
| **매핑 토글** | 공정 매핑 탭 | 매핑 테이블 존재 시 | 셀 클릭 = INSERT/DELETE · **즉시 반영** |
| 변경 이력 | 각 폼 | 저장 이력 있음 | `audit.audit_event` |

### §5-2. 화면 상태

`초기(미선택 — EmptyState)` → `조회` → `편집(변경 있음)` → `저장 중` → `성공(Toast)` / `검증 실패(인라인)` / `충돌(B-1)`

### §5-3. 낙관적 잠금

`defect_code`·`cause_code`에 `version_no`가 있으므로 **B-1 적용**.

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 권한 없음 | 편집 컨트롤 비활성 + 사유 |
| 유일 제약 위반 | 인라인(A-1) — 유일 범위가 **전역**이므로 범위 문구 불요 |
| **자기 자신을 상위로 지정** | ⚠ **DB가 막지 않는다**(`ck_defect_code_parent`가 없다 — 계층 컬럼을 가진 테이블 중 `location`·`defect_code`에만 없다). **화면이 반드시 검사** §8-3 |
| **3계층 이상 시도** | 결정 12 위반 — **차단**하고 사유 표시 §8-3 |
| **대분류 사용 중지** | 하위 상세 코드가 딸려 있다 → 확인 다이얼로그 + 하위 건수 표시 |
| 참조 중 코드 수정 | B-4 — 참조 건수 0일 때만 코드 수정. **참조처는 `defect_record`**(FK 실재 — 셀 수 있다) |
| 오프라인 | **해당 없음** — 관리웹은 온라인 전제(결정 17) |

## §7. DS 매핑 (`@crefle/web-ui` v0.1.0)

### §7-1. 그대로 쓰는 것 (a) · 조합 (c)

| 용도 | 컴포넌트 | 유형 |
| --- | --- | :-: |
| 페이지 틀 · 탭 | `AppShell` · `PageHeader` · `Breadcrumb` · `Tabs` | a |
| **공정 × 불량코드 교차표** | **`MatrixGrid`** (`columns`=공정 · `rows`=상세코드 · `renderCell`+`onCellClick`) | **a** — §7-2 |
| 입력 폼 | `TextField` · `Select` · `Switch` | a |
| 검색·필터 | `SearchInput` + `Select` | **c** (G-2) |
| 경고 다이얼로그 | `Dialog` + `AlertBanner`(warning) | **c** |
| 변경 이력 | `Dialog` + `Table` | **c** (G-3) |
| 알림·오류 | `Toast` · `AlertBanner` | a |
| 빈 상태 · 로딩 | `EmptyState` · `Skeleton` | a |

### §7-2. `MatrixGrid` 첫 실사용 — **갭이 아니다**

`W-06-02` §7-4에서 실재를 확인한 **`MatrixGrid`**가 이 화면의 공정 매핑에 **그대로 맞는다** — `columns`(공정) × `rows`(불량코드) · `renderCell`로 체크 표시 · `onCellClick`으로 토글 · `groupHeaders`로 **대분류 묶음** 표현.

**판정: `a`.** N:M 매핑 UI를 새로 만들 필요가 없다.

> **파일럿의 교훈이 세 번째로 재현됐다** — 착수 전 「N:M 교차 편집기」를 갭으로 예상했는데 **DS에 전용 컴포넌트가 이미 있었다.** 앞의 둘은 「조합이더라」였고 이번은 「**있더라**」다. **DS 인벤토리를 먼저 열어 보는 것이 조합 시도보다도 앞선다** → §9-1.

### §7-3. 계층(트리) — G-1 사용처 추가

`defect_code.parent_defect_code_id`·`cause_code.parent_cause_code_id` 두 곳이 자기참조 계층이다. **단 2계층 고정**(결정 12)이므로 **`Table`+`groupBy`(1단 그룹 헤더)로 충분**하다 — `W-06-07` §8-5가 세운 선행 조건(「2단 이내면 갭 불성립」)을 **이 화면은 명시적으로 통과**한다.

**판정: `c`(조합) — G-1 갭 아님.** `ds-gap.md` G-1 「세 곳 규칙」의 **사용처로 세지 않는다.**

### §7-4. 신규 갭 0건

## §8. 미결

| # | 항목 | 성격 | 처리 |
| --- | --- | --- | --- |
| **1** | **결정 12의 「N:M 매핑」이 하류에 반영되지 않았다** — 물리 모델은 `defect_code.process_id` **N:1**(2026-07-23)이고, 결정 12(2026-07-06)는 **「N:M 매핑 채택 — 데이터모델 관계 표기 조정 **승인**」**을 명시했다. **결정이 17일 앞서는데 반영이 안 됐다** | **상류↔하류 불일치** | ⚠ **결정 12가 피해까지 적어 두었다**: 「1:N을 코드 체계로 굳히면 **동일 불량(외관 스크래치)이 공정 수만큼 복제 등록**되어 코드 관리와 전사 집계가 함께 나빠진다」. 화면은 지금 형태로는 **공정 매핑 탭을 만들 수 없다**(§4-D). **`[docs→데이터모델]` 이슈**(assignee 필수) — **결정 번호와 승인 문구를 인용**해 조정 누락임을 명확히 |
| **2** | **결정 12가 정한 상세 코드 속성 2개가 없다** — ① **처분구분**(재작업가능/폐기 — ✅REQ-PR-0025) ② **다국어 명칭(ko/vi)** | **상류↔하류 불일치** | ①은 「불량 창고 우선 입고 후 재작업/폐기 판단」(REQ-PR-0025)의 **판단 기준**이라 없으면 `W-04-07`·`W-01-06`이 무엇을 보고 나누는지 알 수 없다. `quality.disposition_decision.disposition_type_code`는 **건별 판정 트랜잭션**이지 코드 마스터 속성이 아니다(실측). ②는 `W-06-06` §8-1·`W-06-05` §4-A와 **같은 결함의 세 번째 확인**이며, 결정 12는 **`(ko/vi)`로 컬럼 형태까지 지정**했다 — 다국어 이슈에 **가장 강한 근거** |
| 3 | **DB가 막지 않는 계층 규칙 2건** ① 자기참조(`ck_*_parent` CHECK **부재** — `department`·`production_line` 등에는 있는데 `location`·`defect_code`·`cause_code`에는 없다) ② **2계층 초과** | 설계 | 전부 화면·서버 책임(`W-06-02` §9-1의 등급 분류 적용 — 둘 다 **ⓐ 차단**). CHECK 부재는 **비대칭**이라 데이터모델 이슈에 **함께 담는 것을 권고** |
| ~~**4**~~ | **손실사유코드는 이 화면 소관이 아니다** | ✅ **확정 2026-08-03 (사용자 확정) — 인벤토리 v1.5·`deliverables/04` v1.1 반영 완료** | 결정 12가 「손실 사유는 **별도 코드 그룹**(퍼징/시작불량/기타) — **불량코드 체계에 섞지 않음**(3원 집계와 정합)」으로 확정했다. 하류도 이를 따른다 — `production.material_loss.reason_code`는 **`app.code_t` 맨몸**(`defect_code` FK 아님, 실측)이라 **공통코드 = `W-06-06` 소관**. **인벤토리 v1.4 `W-06-03` 행의 핵심 데이터에서 「손실사유코드」를 빼고 `W-06-06`으로 옮긴다**(화면 수 변동 없음) |
| ~~**5**~~ | **원인코드(`cause_code`) 관리 화면이 없다** | ✅ **확정 2026-08-03 (사용자 확정) — 이 화면이 흡수. 화면명 변경·인벤토리 v1.5 반영 완료(화면 수 변동 0)** | 하류에 실재하고 `defect_record`가 **두 컬럼으로 참조**하는데 인벤토리 109건에 관리 화면이 없다. **이 화면이 흡수하는 것을 제안**(§3 원인코드 탭) — 구조가 `defect_code`와 **완전히 동일**하고 품질담당이 같이 쓰므로 별도 화면은 과하다. **화면 수 변동 없음 · 인벤토리 `W-06-03` 행의 핵심 데이터·화면명에 반영 필요** |
| 6 | 대분류 5종(외관/치수/기능/이물/기타)이 **초안**이다(결정 12 「초안」) | 회신/확정 | 값 자체는 데이터라 화면 변경 없음. 초기 적재 시 고객 확인 |

## §9. 이 장에서 나온 공유 계약 후보

1. **DS 갭 판정의 순서를 바꾼다 — 인벤토리 조회가 조합 시도보다 앞선다.**
   `00-서식.md` §6-1은 「없다고 적기 전에 **조합 시도**를 기록한다」인데, 이 장에서 **조합을 시도할 필요조차 없이 전용 컴포넌트가 있었다**(`MatrixGrid`). 파일럿 정정 3건(스캔·편집 그리드·검색 바)은 전부 「조합이더라」였지만 이번은 「**있더라**」다. **절차를 ① DS 인벤토리 전수 조회 → ② 조합 시도 → ③ 갭 판정**으로 명시한다. *(이 장 이전 7장은 인벤토리를 열지 않고 기억으로 적었고, 그래서 `DatePicker`·파일 업로드를 **실재하는 것처럼 쓴 오류**가 있었다 — `W-06-02` §7-2에서 정정.)*
2. **확정된 설계 결정의 하류 반영을 화면 스펙이 검증한다.**
   결정 12는 「데이터모델 관계 표기 조정 **승인**」까지 적었는데 17일 뒤 물리 모델이 조정 전 형태다. **결정서와 물리 모델을 대조하는 공정이 어디에도 없었고**, 화면 스펙 작성이 그 자리가 됐다. 확대 1차에서만 **같은 성격의 미반영이 5건**(다국어 · 검사정책 3속성 · 기본 Rev 플래그 · 외주 구분 · 이 장 2건). **각 스펙 §2 「근거 4면」 대조 시 「확정 문구 ↔ 컬럼」을 표로 남기는 것을 서식에 넣는다.**
3. **코드 마스터의 「전사 고정 축」과 「설정형 축」을 화면이 구분해 보인다.**
   결정 12의 대분류=고정·상세=설정형처럼, **추가하면 과거 집계가 끊기는 축**이 있다. 그런 축의 추가에는 **경고를 붙인다**(차단은 아니다 — `W-06-02` §9-1의 ⓑ 경고 등급). 같은 성격: 창고유형·판정유형·손실사유.

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-03 | 초안 — 확대 1차 8/13. 4면 근거 대조 완료(물리 모델 2테이블 + 참조 2곳). **상류↔하류 불일치 2건 신규**(결정 12의 N:M 조정 승인 미반영 — 결정이 17일 앞섬 · 처분구분·다국어(ko/vi) 컬럼 부재). **인벤토리 정정 2건 제안**(손실사유코드 → `W-06-06` 이관 · 원인코드 `cause_code` 흡수 — 화면 수 변동 없음). **DS: `MatrixGrid` 첫 실사용 — `a` 판정**(N:M 교차 편집기를 갭으로 예상했으나 전용 컴포넌트 실재) · G-1 계층은 2계층 고정이라 갭 불성립. 공유 계약 후보 3건(갭 판정 절차 변경 포함). |
