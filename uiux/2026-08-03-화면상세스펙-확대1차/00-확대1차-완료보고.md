# 화면 상세 스펙 확대 1차 — 완료 보고

> 작성일: 2026-08-03 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 대상: **기준정보연계 도메인 12 + `W-CO-02` = 13화면** (공유계약 §H 확산 순서 — 백엔드팀 요청으로 조정된 1차)
> 서식: `00-서식.md` v1.0 · 규약: `공유계약.md` → **v0.4로 갱신** · DS 대장: `ds-gap.md` → **갱신**

## 1. 한 문단 요약

**13화면 전건을 서식 v1.0의 9블록으로 작성했다.** 그런데 이 차수의 가장 큰 결과는 화면 스펙이 아니라 **확정 사항이 물리 모델에 착지하지 않은 지점 14건**이다(공유계약 **§I 신설**). 확정기록·설계결정서와 물리 모델을 **대조하는 공정이 그동안 없었고**, 화면 상세 스펙 작성이 사실상 첫 대조가 됐다. 그중 **2건은 파급이 화면 하나를 넘는다** — 수신본 편집 게이트가 판정 근거 컬럼이 없어 **구현 불가**이고, **설비보전이 물리 모델 선언 범위 밖**이라 도메인 05 화면 17건은 지금 착수하면 멈춘다.

## 2. 산출물

| 구분 | 산출물 | 비고 |
| --- | --- | --- |
| 화면 스펙 | **13장** — `W-CO-02` · `W-06-01`~`06` · `08`~`13` | 9블록 전건 · 4면 근거 대조 완료 |
| 공통 자산 | `공유계약.md` **v0.3 → v0.4** | 신규 조항 **17건** + **§I 불일치 대장 14건** |
| 공통 자산 | `ds-gap.md` 갱신 | 신규 갭 **4건**(G-7~G-10) · **판정 절차 변경** · 대조 **17/109** |
| 보고 | 이 문서 | 결정 요청 4건 · 이슈 계획 7묶음 |

**작성 순서**(의존 관계대로): `W-CO-02`(권한 원천) → `W-06-06`(코드 원천) → `W-06-05`(ERP 축) → `W-06-01`(MES 축) → `W-06-11`(변경 창구) → `W-06-02`·`13`·`03`·`04`(품질 4장) → `W-06-08`(예비품) → `W-06-09`·`12`·`10`(I/F 3장)

## 3. ⚠ 사용자 결정이 필요한 것 — 4건

### 3-1. `W-06-13` 검사정책 설정 — **`W-06-02`로 통합·폐지 제안** (화면 109 → 108)

개념모델 v2.6의 「검사정책」 속성 **11개를 물리 모델과 전수 대조**한 결과:

| 착지 | 개수 | 위치 |
| --- | :-: | --- |
| **완전 착지** | **7** | `inspection_plan`(품목·공정·검사유형·승인자) · `inspection_plan_version`(검사 방식·샘플 기준·합격판정개수) |
| 형태 상이 | 1 | 샘플 **비율(%)** ↔ `sampling_qty`(**수량**) |
| **미착지** | 3 | PQC 생략 허용·생략 근거·검사기준 미비 시 단순 선택 허용 |

인벤토리 v1.3이 이 화면을 신설한 근거는 「**`W-06-02`의 엔티티에 검사정책이 없다**」였는데, **물리 모델에서는 같은 테이블에 있다.** 개념모델이 별도 엔티티로 둔 것을 하류가 버전 테이블에 합친 것이며, 검사 방식·샘플·합격판정개수는 **검사기준 버전마다 달라지는 값**이라 정상적인 정규화로 보인다.

**통합 근거 다섯**: ① 신설 전제 붕괴 ② 인벤토리 자신의 절단 기준(「저장 엔티티가 같으면 같은 화면」) 위배 ③ 같은 버전을 두 화면에서 저장하면 **낙관적 잠금이 자기 자신과 충돌** ④ 컬럼 3개 때문에 화면을 만드는 것은 인벤토리 §5 미결 4(「설정 항목 1개짜리 화면은 과하다」)와 같은 판단 ⑤ 통합하면 **Rev 스냅샷이 따라와** 「과거 W/O가 어느 정책으로 검사됐나」가 해결된다

**부결 시**: `W-06-13` §4~§7이 그대로 독립 스펙이다(부결 대비로 함께 작성했다).
**가결 시 후속**: 인벤토리 §3.1 표·IA 트리·신뢰도 집계(추정 25 → 24)·`deliverables/04` 갱신.

### 3-2. `W-06-11` 마스터 변경관리 — **소관 판정**

**제안**: **실행은 각 마스터 화면, 이 화면은 조회·창구.**

- Routing Rev 발행은 **헤더+라인을 함께 복사·편집**하는 일이라 `W-06-01`의 3단 레이아웃 없이는 성립하지 않는다
- 이 화면은 **전용 테이블이 없다**
- 도식 앵커 **B9(변경이력기록)**가 고유분이고 **B10(작성중편집)**은 `W-06-01` §5-4가 이미 구현한다

**고유 기능**: `audit.audit_event` 횡단 조회 · Rev 현황 통합 뷰 · **ERP 정본 행의 정규 경로 안내**(WF06 S8이 우려한 「엑셀 우회」를 줄이는 자리).
**부결 시**: `W-06-01` §5-1의 「신규 Rev 발행」을 이 화면으로 옮긴다.

### 3-3. 인벤토리 정정 2건 — **화면 수 변동 없음**

| 정정 | 내용 | 근거 |
| --- | --- | --- |
| **손실사유코드 이관** | `W-06-03` 핵심 데이터에서 빼고 **`W-06-06`(공통코드)** 으로 | **결정 12** 「손실 사유는 **별도 코드 그룹** — 불량코드 체계에 **섞지 않음**」 · 하류도 `material_loss.reason_code`를 **맨 `code_t`**로 둠 |
| **원인코드 흡수** | `quality.cause_code`(불량 **원인**)의 관리 화면이 없다 → **`W-06-03`이 흡수** | 하류 실재 · `defect_record`가 **추정·확정 두 컬럼으로 참조** · 구조가 `defect_code`와 **완전 동일** · 「쓰는 곳은 있는데 관리할 곳이 없는」 상태(v1.3 작업자 귀속과 같은 패턴) |

### 3-4. 이슈 7묶음 발행 승인

공유계약 **§I 이슈 묶음 계획** 참조. **전부 assignee 필수**이며, **#8(외주 구분)은 열린 이슈 #53 회신 후** 별도 발행.

| 묶음 | 제목(안) | 유형 | 파급 |
| :-: | --- | --- | --- |
| **A** | 확정 사항의 물리 모델 미착지 — 기준정보 마스터 7건 | `[docs→데이터모델]` | 다국어·확장속성·Rev 플래그·검사정책·불량코드·판정유형 |
| **B** | 연계 수신본 식별 플래그 부재 — **편집 게이트 구현 불가** | `[docs→데이터모델]` | ⚠ QA #34 확정이 구현 불가 |
| **C** | I/F 연계정의·송신 항목 테이블 부재 | `[docs→데이터모델]` | `W-06-09`·`W-06-12` 전체 |
| **D** | 설비보전이 물리 모델 선언 범위 밖 | `[docs→데이터모델]` | ⚠ **도메인 05 화면 17건** |
| **E** | `audit.audit_event` 사용 규약 부재 | `[docs→데이터모델]` | `W-06-11` 주 기능 · 전 마스터 이력 |
| **F** | 사용자·권한 — 인증 수단·법인 축 부재 | `[docs→데이터모델]` | `W-CO-01`·`W-CO-02` · 다법인 격리 |
| **G** | 「개발품」의 기준 — 품목 속성인가 W/O 유형인가 | `[uiux→docs]` | 문서 간 상충 |

## 4. 파급이 화면 하나를 넘는 것 — 2건

### 4-1. 수신본 편집 게이트가 **구현 불가**다 (§I-2)

WF06 S7 Result가 「연계분에 **'Legacy 출처' 플래그** + MES 원천 등록분과 연계분의 **구분 플래그**」를 **신규 설계로 요구**했는데, 대상 마스터 **5개 전수 확인 결과 어디에도 없다**(`item`·`code_value`·`department`·`worker`·`bom`).

→ `W-06-06` §5-4와 `W-06-05`가 세운 게이트(「수신본은 읽기 전용, MES 확장만 편집」)를 **화면이 판정할 수 없다.** 어느 행이 ERP에서 왔는지 알 방법이 없기 때문이다. **✓확정 QA #34가 구현 불가 상태**다.

여기서 공유계약 조항 하나가 나왔다 — **F-3: 게이트를 계약에 적을 때 「무엇을 보고 판정하는가」를 함께 적고 그 컬럼의 실재를 확인한다.** 현재 게이트 넷 중 **하나가 근거를 갖지 못한다.**

### 4-2. 설비보전이 물리 모델 **선언 범위 밖**이다 (§I-12)

물리 모델 v3 머리말이 스스로 범위를 선언한다 — `Scope: MDM, Production, Inventory, Quality, Traceability, Logistics, Integration, Audit`. **보전이 없고 스키마 10개에도 없다.** 설비·툴 테이블은 **셋뿐**(`mdm.equipment`·`mdm.mold`·`quality.equipment_calibration`)이고 점검·고장·보전지시·보전실적·**예비품은 0건**이다.

**✓확정 QA #10**이 「설비 보전 = MES 경량 보유 · 범위 = 점검+고장 수집+보전 계획·지시·실적」이고 **「공백 ≠ 불요」**까지 명시했는데 하류가 범위에서 제외했다.

→ **파급은 `W-06-08` 1건이 아니라 도메인 05 화면 17건 전체**다. **05 상세 스펙 착수 전에 해소돼야 한다** — 확대 1차에서 드러난 것이 다행이다.

## 5. 공유계약 v0.4 — 신규 조항 17건

| 절 | 조항 | 요지 |
| --- | --- | --- |
| A-5 | 순서 재배치는 서버 일괄 | 유일 제약 때문에 중간 상태가 반드시 충돌(4곳) |
| A-6 | 「하나만 참」 전환도 서버 | 2회 호출하면 중간에 0개 상태 |
| A-7 | `COALESCE` 유일 인덱스 표기 | 빈 축은 **`(전체)`** · 오류 문구에 접힘 규칙 |
| **A-8** | **수치 컬럼 저장 단위 표** | 비율 0~1을 `%`로 보이면 **100배 오입력이 조용히 통과** |
| **A-9** | **DB 미강제 검증 3등급** | 차단 / 경고 / 표시만 — 화면마다 다르면 같은 오류가 다르게 굴러간다 |
| B-1 확장 | 충돌 원인 3종 | 사용자 / **ERP 재동기화 배치** / **워커 리스** — 현행 문구는 뒤 둘에 **거짓말** |
| B-4 확장 | 참조 건수를 셀 수 없는 대상 | 의도적 비-FK · 코드 문자열 참조 → **무조건 잠금** |
| **B-4-1** | **ERP 수신본 편집 게이트** | v0.3 예고분 확정 · 입도 둘(구획/열) · ⚠ **현재 구현 불가** |
| B-5 | 변경 이력 = `audit.audit_event` | **B-4 ⬜ 해소** · 단 「쓸 수 있는가는 별개」 |
| B-6 | 마스터 ↔ 부여·회수 구분 | 부여·회수에 B-4 적용하면 **없는 컬럼을 요구** |
| B-7 | 버전 마스터 공통 골격 | Routing·검사기준·BOM이 같은 3계층 · **B-2 적용 대상 확장** |
| C-4 | outbox는 서버에도 동형 | C-1 규약을 한 벌로 — 같은 개념을 두 이름으로 부르지 않는다 |
| F-2 | 권한 게이팅 2축 | 단말×공정(POP·모바일) ↔ 사용자×기능(관리웹) |
| **F-3** | **게이트는 판정 근거 실재 확인** | 넷 중 하나가 **근거 없음** |
| G-3 | 사유는 **해결 경로**를 담는다 | 잠긴 이유 넷이 다 「회색」으로 보인다 |
| G-4 | 「필수」·「비연계」는 구조로 | 기본값으로 구현하면 확정이 무너진다 |
| G-5·G-6·G-7·G-8 | 확정 번호 병기 · 회신 대기 표준 · 고정 축 경고 · 선택 목록은 서버 판정 | |

## 6. DS 대조 — 신규 갭 4건, 이슈 대상은 1건

| 갭 | 판정 | 사용처 | 비고 |
| --- | :-: | --- | --- |
| **G-7 행 순서 재배치** | **b** | 4곳 | **세 곳 규칙 충족** → 이슈 대상 |
| G-8 날짜 입력 | **c** *(b 승격 후보)* | 유효기간 8곳 + 기간 조회 | `DatePicker` **없음** — `TextField type="date"`로 성립하나 **세 셸 렌더 불일치** |
| G-9 파일 업로드 | **c** | 엑셀 DX 2곳(+3곳 예상) | `FileInput` **없음** — 단일 선택은 조합으로 성립 |
| G-10 읽기 전용 구획 | **c** | 6곳 | 잠금 근거 4갈래 · 컴포넌트는 같음 |

**⚠ 판정 절차를 바꿨다.** 확대 1차에서 **인벤토리를 열지 않고 기억으로 적어 오류 2건**을 냈고(`DatePicker`·`FileInput`을 실재하는 것처럼 사용), 반대로 **갭으로 예상한 것이 전용 컴포넌트로 이미 있었다**(`MatrixGrid` — `W-06-03` 공정×불량코드 N:M에 그대로 맞음). 절차를 **① DS 인벤토리 전수 조회 → ② 조합 시도 → ③ 갭 판정**으로 못박았다(`00-서식.md` §6-1은 ②부터 시작한다).

> DS 문서의 경고에 하나를 더한다 — 「없다」의 절반 이상은 **조합**이고, 「없다」의 일부는 **이미 있다.**

## 7. 다음 단계

1. **§3의 결정 4건** — 사용자 확정
2. **이슈 7묶음 발행**(승인 후) · assignee 지정
3. **인벤토리 v1.5 갱신** — 결정 결과 반영(화면 수·핵심 데이터·IA 트리·`deliverables/04`)
4. **확대 2차 계획** — ⚠ **데이터 면 충실도로 차수를 재정렬**한다(§H). 특히 **도메인 05는 §I-12 해소 전 착수 금지**
5. *(선택)* 검토 요청 보고서 HTML — 파일럿과 같은 형태(`crefle-doc` 단일 파일)로 만들지 결정

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-03 | 확대 1차 완료 보고 — 스펙 13장 · 공유계약 v0.4(조항 17 + §I 14건) · ds-gap 갱신(신규 갭 4). 결정 요청 4건 · 이슈 7묶음 계획. |
