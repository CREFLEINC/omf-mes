# 기준정보 도메인 API 스펙 — 실행 계획

> **작업자에게:** 이 계획은 `superpowers:subagent-driven-development`(권장) 또는 `superpowers:executing-plans`로 태스크 단위 실행한다. 단계는 체크박스(`- [ ]`)로 추적한다.
> **설계 정본**: `uiux/2026-08-03-API스펙/00-설계.md` — 이 계획은 그것을 실행 단위로 쪼갠 것이다.
> **⚠ 계획 문서 위치**: 스킬 기본은 `docs/superpowers/plans/`이나 이 저장소는 **`docs/` 불가침**(`uiux/CLAUDE.md` §절대 규칙)이라 `uiux/` 작업 폴더에 둔다.

**목표:** 확대 1차 화면 스펙에서 도출한 기준정보 도메인 API를 **요구서 + OpenAPI + 자동 검산** 세 벌로 만들어 `deliverables/06`에 싣는다.

**구조:** OpenAPI는 물리 모델을 옮긴 것이고(정본=물리 모델), 요구서는 화면→API 매핑과 근거만 담으며(필드표 없음), 검산 스크립트가 둘의 어긋남을 기계로 잡는다. 리소스는 **테이블 경계 = 리소스 경계**로 자른다.

**기술:** Python 3.9 **표준 라이브러리만**(기존 스크립트 관행) · OpenAPI 3.1 **JSON**

## ⚠ 설계에서 벗어난 것 1건 — 계획 시점 발견

| 설계 §3-1 | 계획 | 이유 |
| --- | --- | --- |
| `deliverables/openapi/mdm-기준정보.yaml` | `deliverables/openapi/mdm-기준정보.json` | **PyYAML 미설치**(Python 3.9.6)이고 기존 스크립트는 표준 라이브러리만 쓴다. 문서 저장소에 의존성을 더하는 것보다 JSON이 낫다. OpenAPI 3.1은 JSON도 정식 형식이며 도구 호환성이 동등하다. YAML의 가독성 이점은 **요구서 md·HTML이 대신**한다 |

## 전역 제약

이 절은 **모든 태스크의 요구사항에 암묵적으로 포함**된다.

- **표준 라이브러리만 사용** — `pip install` 금지. 기존 스크립트 관행(`base64, html, io, os, re, sys`)을 따른다
- **물리 모델이 정본** — `docs/research/2026-07-23-데이터모델링/mes_postgresql_physical_model.sql`. 타입·필수·유일·CHECK를 자의로 바꾸지 않는다. **다르면 결함이고 이슈로 낸다**
- **`docs/` 불가침** — 읽기만 한다. 수정이 필요하면 `[uiux→docs]` 이슈
- **없는 필드를 지어내지 않는다** — 물리 모델에 없으면 스키마에 넣지 않고 `description`에 「미착지 — #이슈번호」로 남긴다
- **감사 컬럼 제외** — `created_at`·`created_by`·`updated_at`·`updated_by`·`version_no`는 스키마에 넣지 않는다(공유계약 A-4). 단 `version_no`는 **낙관적 잠금 헤더**로 별도 전달
- **매핑 선언** — 물리 모델에 대응물이 있는 스키마·프로퍼티에 `x-source-table`·`x-source-column` 을 단다. **근거 추적용이며 검산 입력이 아니다**(설계 §5-4). 공통 응답 스키마처럼 대응물이 없는 것은 달지 않는다
- **근거 한 줄** — 모든 경로의 `description` 끝에 `근거: <화면ID> §<절> · 공유계약 <항목>`
- **커밋 메시지** — `docs:` 접두. 본문은 한국어. 저장소 관례를 따른다

## 파일 구조

| 파일 | 책임 |
| --- | --- |
| `deliverables/verify-ui-coverage.py` | 화면 스펙에서 UI 요구(액션) 전건 추출 · 커버리지 검산의 기준 목록 생성 |
| `deliverables/openapi/ui-요구목록.md` | 위의 생성물 — 요구서 매핑표가 전건 소화해야 한다 |
| `deliverables/openapi/ui-적합성-점검표.md` | 「이 API 로 이 화면을 그릴 수 있는가」 5항목 기준 |
| `deliverables/openapi/mdm-기준정보.json` | 계약 — 경로·스키마·제약·상태코드 |
| `deliverables/06-API-요구서.md` | 화면→API 매핑 · 근거 · 미착지가 무엇을 막는가 · 규약 대조표 |
| `deliverables/06-API-요구서.html` | 위의 HTML 판 |
| `deliverables/build-doc-html.py` | **수정** — `CONFIG`에 06 추가 |

---

## Task 1: UI 요구 추출 — 화면이 무엇을 요구하는가

> **⚠ 이 태스크는 2026-08-03 교체됐다.** 구 Task 1(SQL 파서)·구 Task 2(OpenAPI 대조기)는 **폐기**하고 커밋을 되돌렸다. 사용자 지시 — 「SQL과 OpenAPI를 직접 대조하는 검증은 필요없다. 중요한 것은 ① UI에 필요한 API가 모두 있는지 ② UI에서 쓰기에 적절한지」. 물리 모델 정합은 데이터모델 담당 소관이고 발견분은 이미 이슈 #64~#70으로 넘겼다.

**파일**
- 생성: `deliverables/verify-ui-coverage.py`
- 생성: `deliverables/test-verify-ui-coverage.py`
- 생성: `deliverables/openapi/ui-요구목록.md`

**인터페이스**
- 소비: 화면 스펙 10장
- 생산: `extract_actions(md_path) -> list[dict]` — 각 항목은 `{"screen": "W-06-07", "action": "창고 추가", "condition": "항상", "note": "..."}`. `extract_all(base_dir) -> list[dict]`. Task 8(요구서)이 이 목록을 전건 소화해야 하고 Task 9가 차집합 0을 확인한다

**대상 화면 10개** — 확대 1차 13장에서 `W-06-13`(통합 폐지)·`W-06-08`·`W-06-09`·`W-06-12`(API 정의 불가)를 뺀 9개 + 파일럿 `W-06-07`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`deliverables/test-verify-ui-coverage.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# verify-ui-coverage.py 의 단위 테스트. 표준 라이브러리만 쓴다.
import os, sys, unittest, importlib

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)
cov = importlib.import_module("verify-ui-coverage")

W0607 = os.path.join(HERE, "..", "uiux", "2026-07-31-화면상세스펙-파일럿",
                     "W-06-07-창고Location마스터.md")


class ExtractActionsTest(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.rows = cov.extract_actions(W0607)

    def test_화면ID를_파일명에서_읽는다(self):
        self.assertTrue(all(r["screen"] == "W-06-07" for r in self.rows))

    def test_액션을_일곱_뽑는다(self):
        self.assertEqual(len(self.rows), 7)

    def test_첫_액션이_창고_추가다(self):
        self.assertEqual(self.rows[0]["action"], "창고 추가")

    def test_굵은표기를_벗긴다(self):
        acts = [r["action"] for r in self.rows]
        self.assertIn("사용 중지", acts)
        self.assertIn("라벨 이미지 생성", acts)

    def test_구분선과_헤더행은_제외한다(self):
        acts = [r["action"] for r in self.rows]
        self.assertNotIn("액션", acts)
        self.assertFalse(any(a.startswith("---") for a in acts))

    def test_전체_화면을_모으면_10개다(self):
        rows = cov.extract_all(HERE)
        screens = sorted({r["screen"] for r in rows})
        self.assertEqual(len(screens), 10, screens)
        self.assertIn("W-CO-02", screens)
        self.assertIn("W-06-07", screens)
        self.assertNotIn("W-06-13", screens)
        self.assertNotIn("W-06-08", screens)


if __name__ == "__main__":
    unittest.main(verbosity=2)
```

⚠ **`test_액션을_일곱_뽑는다` 의 기대값 7은 실측이다.** `W-06-07` §5-1 표의 데이터 행은 창고 추가 · 저장 · 취소 · 사용 중지 · Location 추가/하위 추가 · 라벨 이미지 생성 · 변경 이력 = **7행**. 파서가 다른 수를 내면 파서가 틀린 것이다 — **기대값을 바꾸지 마라.**

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/rangkim/projects/crefle/ohmyfactory/worktrees/uiux-screen-spec-expansion/deliverables
python3 test-verify-ui-coverage.py
```
기대: `ModuleNotFoundError: No module named 'verify-ui-coverage'`

- [ ] **Step 3: 추출기를 구현한다**

`deliverables/verify-ui-coverage.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# UI 요구 추출 — 화면 상세 스펙에서 액션을 전건 뽑는다.
#
# 목적은 커버리지 검산이다. 화면이 요구하는 액션 하나하나가 요구서의
# 화면→API 매핑표에서 다뤄졌는지 대조하기 위한 기준 목록을 만든다.
#
# 물리 모델과 OpenAPI 를 대조하지 않는다 — 그것은 데이터모델 담당 소관이다.
# 표준 라이브러리만 쓴다(저장소 관행).
import io, os, re, sys

EXPANSION = "uiux/2026-08-03-화면상세스펙-확대1차"
PILOT = "uiux/2026-07-31-화면상세스펙-파일럿"

# 대상 화면 — 확대 1차 13장 − W-06-13(통합 폐지) − W-06-08·09·12(API 정의 불가) + 파일럿 W-06-07
SCREENS = [
    (EXPANSION, "W-CO-02"),
    (EXPANSION, "W-06-01"),
    (EXPANSION, "W-06-02"),
    (EXPANSION, "W-06-03"),
    (EXPANSION, "W-06-04"),
    (EXPANSION, "W-06-05"),
    (EXPANSION, "W-06-06"),
    (EXPANSION, "W-06-10"),
    (EXPANSION, "W-06-11"),
    (PILOT, "W-06-07"),
]

_SCREEN_ID = re.compile(r"(W-(?:CO|[0-9]{2})-[0-9]{2})")
_BOLD = re.compile(r"\*\*(.+?)\*\*")
_STRIKE = re.compile(r"~~(.+?)~~")


def _strip_md(text):
    # 굵게·취소선·코드 표기를 벗기고 앞뒤 공백을 없앤다.
    text = _BOLD.sub(r"\1", text)
    text = _STRIKE.sub(r"\1", text)
    return text.replace("`", "").strip()


def extract_actions(md_path):
    # 한 화면 스펙의 §5-1 액션 표를 [{screen, action, condition, note}] 로.
    m = _SCREEN_ID.search(os.path.basename(md_path))
    screen = m.group(1) if m else os.path.basename(md_path)
    text = io.open(md_path, encoding="utf-8").read()

    start = text.find("### §5-1")
    if start == -1:
        return []
    end = text.find("###", start + 4)
    block = text[start:end] if end != -1 else text[start:]

    rows = []
    for line in block.split("\n"):
        line = line.strip()
        if not line.startswith("|"):
            continue
        cells = [c.strip() for c in line.strip("|").split("|")]
        if len(cells) < 2:
            continue
        first = _strip_md(cells[0])
        if not first or first.startswith("---") or first == "액션":
            continue
        rows.append({
            "screen": screen,
            "action": first,
            "condition": _strip_md(cells[2]) if len(cells) > 2 else "",
            "note": _strip_md(cells[3]) if len(cells) > 3 else "",
        })
    return rows


def extract_all(base_dir):
    # 대상 화면 10개의 액션을 모은다. base_dir 은 deliverables/ 경로.
    root = os.path.normpath(os.path.join(base_dir, ".."))
    out = []
    for folder, sid in SCREENS:
        d = os.path.join(root, folder)
        if not os.path.isdir(d):
            continue
        for fn in sorted(os.listdir(d)):
            if fn.startswith(sid) and fn.endswith(".md"):
                out.extend(extract_actions(os.path.join(d, fn)))
                break
    return out


def render(rows):
    # 요구 목록을 markdown 으로.
    lines = [
        "# UI 요구 목록 — 화면이 API 에 요구하는 것",
        "",
        "> 생성물이다. `python3 verify-ui-coverage.py` 로 다시 만든다.",
        "> 이 목록의 모든 행이 `06-API-요구서.md` §3 매핑표에서 다뤄져야 한다.",
        "> 엔드포인트가 없는 액션은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.",
        "",
    ]
    cur = None
    for r in rows:
        if r["screen"] != cur:
            cur = r["screen"]
            lines += ["", "## " + cur, "", "| 액션 | 활성 조건 |", "| --- | --- |"]
        lines.append("| %s | %s |" % (r["action"], r["condition"]))
    lines += ["", "---", "",
              "**합계: 화면 %d · 액션 %d**"
              % (len({r["screen"] for r in rows}), len(rows)), ""]
    return "\n".join(lines)


def main():
    here = os.path.dirname(os.path.abspath(__file__))
    rows = extract_all(here)
    dst = os.path.join(here, "openapi", "ui-요구목록.md")
    d = os.path.dirname(dst)
    if not os.path.isdir(d):
        os.makedirs(d)
    with io.open(dst, "w", encoding="utf-8") as f:
        f.write(render(rows))
    print("생성: %s" % dst)
    print("화면 %d · 액션 %d" % (len({r["screen"] for r in rows}), len(rows)))
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과를 확인하고 목록을 만든다**

```bash
cd deliverables
python3 test-verify-ui-coverage.py
python3 verify-ui-coverage.py
head -40 openapi/ui-요구목록.md
```
기대: 테스트 `OK` (6 tests) · `화면 10 · 액션 <N>`

**생성된 목록을 눈으로 훑어라.** 액션이 아닌 것이 섞였거나(주석 행 등) 빠진 화면이 있으면 파서를 고친다. 이 목록이 이후 전건의 기준이 되므로 여기서 틀리면 뒤가 다 틀린다.

- [ ] **Step 5: 커밋**

```bash
git add deliverables/verify-ui-coverage.py deliverables/test-verify-ui-coverage.py deliverables/openapi/ui-요구목록.md
git commit -m "docs: UI 요구 추출 — 화면 스펙에서 액션 전건 목록

화면 10개의 §5-1 액션을 뽑아 커버리지 검산의 기준 목록을 만든다.
이 목록의 모든 행이 요구서 매핑표에서 다뤄져야 하고, 엔드포인트가
없는 것은 「없음 + 이유」로 명시한다 — 빈칸은 누락이다.

물리 모델과 OpenAPI 를 대조하는 검산은 만들지 않는다. 그것은
데이터모델 담당 소관이고 발견분은 이슈 #64~#70 으로 넘겼다."
```

---

## Task 2: UI 적합성 점검 기준

> **⚠ 이 태스크도 2026-08-03 교체됐다**(구 Task 2 = OpenAPI 대조기, 폐기).
> **API 를 쓰기 전에 기준을 먼저 세운다** — 그래야 API 가 그 기준을 만족하도록 설계된다. 나중에 검사만 하면 이미 굳은 것을 되돌려야 한다.

**파일**
- 생성: `deliverables/openapi/ui-적합성-점검표.md`

**인터페이스**
- 소비: 설계 §5-2 의 다섯 항목
- 생산: 점검표. Task 3~7이 API 를 쓸 때 이것을 만족시키고, Task 8이 화면별로 채운다

- [ ] **Step 1: 점검표를 쓴다**

`deliverables/openapi/ui-적합성-점검표.md` 를 아래 내용 그대로 만든다. 다섯 항목을 **예/아니오로 답할 수 있는 문장**으로 적었다 — 「적절한가」 같은 모호한 말을 쓰지 않는다.

```markdown
# UI 적합성 점검표

> 「이 API 로 이 화면을 그릴 수 있는가」를 화면별로 판정한다.
> 근거: `uiux/2026-08-03-API스펙/00-설계.md` §5-2

## 1. 진입 호출 수

- 화면을 처음 그리는 데 필요한 호출을 **전부 나열**했는가
- 선택 목록 조회가 **3회를 넘으면** 그 이유를 적었는가

관리웹은 온라인 전제(결정 17)라 호출 수 자체가 결함은 아니다 — **세지 않은 것이 결함이다.**

## 2. 필요한 정보가 응답에 있는가

화면이 **스스로 계산하거나 추가 호출로 알아내야 하는 것이 없어야** 한다.

| 확인 | 근거 |
| --- | --- |
| 코드 수정 가능 여부와 사유가 응답에 있는가 | 공유계약 B-4 |
| 선택 목록의 유효성 판정을 서버가 했는가 | G-8 |
| 저장 충돌의 원인 구분이 응답에 있는가 | B-1 |
| 오류의 범위(필드/화면) 구분이 응답에 있는가 | G-1 |
| 유일 위반의 범위가 응답에 있는가 | A-1 |
| 일괄 처리의 건별 결과가 응답에 있는가 | C-2 |

## 3. 안 쓰는 것을 담고 있지 않은가

- 응답 필드 중 **화면 스펙 §4 필드표에 없는 것**이 있는가
- 있다면 왜 필요한지 적었는가(정렬 키·다음 호출의 입력 등)
- ⚠ **대외비가 섞이지 않았는가** — `W-06-10` 의 payload 가 대표 사례다

## 4. 예외를 표현할 수 있는가

화면 스펙 **§6 의 각 행**이 어느 오류 코드·상태코드로 오는지 대응이 있어야 한다.

조건부 필수 위반 · 짝 제약 위반 · 범위 위반 · 유일 위반 · 권한 없음 · 상태 불일치

## 5. 상태 전이를 지원하는가

- 화면 스펙 **§5-2 의 각 상태**를 만들 수 있는가
- **§5-4 의 편집 잠금**(수신본 / 상태=확정 / 참조 존재 / 권한)을 **판정할 근거가 응답에 있는가**
- ⚠ 「수신본이라 잠김」은 **현재 판정 근거 컬럼이 없다(#65)** — 그 사실을 적는다

## 판정

화면별로 다섯 항목에 **✅ / ⚠(조건부) / ❌** 를 매긴다. **❌ 는 API 설계 결함**으로 보고 고친다.
```

- [ ] **Step 2: 커밋**

```bash
git add deliverables/openapi/ui-적합성-점검표.md
git commit -m "docs: UI 적합성 점검표 — API 를 쓰기 전에 기준을 세운다

「이 API 로 이 화면을 그릴 수 있는가」를 화면별로 판정하는 다섯 항목.
각 항목을 예/아니오로 답할 수 있는 문장으로 적었다.

기준을 먼저 세우는 이유는 Task 3~7 이 API 를 쓸 때 이것을 만족시키게
하기 위해서다. 나중에 검사만 하면 이미 굳은 것을 되돌려야 한다."
```

---

## Task 3: OpenAPI 골격 + 공통 응답 스키마

**파일**
- 생성: `deliverables/openapi/mdm-기준정보.json`

**인터페이스**
- 소비: `verify`(Task 2)
- 생산: `components.schemas` 의 공통 7종 — `ErrorResponse`·`ErrorItem`·`ConflictResponse`·`BatchResult`·`BatchFailure`·`Editability`·`PageMeta`. 이후 모든 태스크가 `$ref` 로 참조한다

- [ ] **Step 1: 검산을 실패시킨다**

```bash
mkdir -p deliverables/openapi
echo '{}' > deliverables/openapi/mdm-기준정보.json
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 키', list(d.keys()))"
```
기대: `JSON 유효 — 키 []`. **이 단계의 목적은 파일 자리를 만들고 JSON 으로 읽히는지 확인하는 것**이다.

- [ ] **Step 2: 골격과 공통 스키마를 쓴다**

`deliverables/openapi/mdm-기준정보.json`

```json
{
  "openapi": "3.1.0",
  "info": {
    "title": "omf-mes 기준정보 도메인 API (초안)",
    "version": "0.1.0",
    "description": "화면 상세 스펙 확대 1차에서 도출한 계약 초안. 정본은 물리 모델이며 이 문서는 그것을 옮긴 것이다. 근거·맥락은 deliverables/06-API-요구서.md 를 본다. WBS P0.4 API 계약 v0 의 초안 입력이며 계약 확정·소유는 통합 Agent 소관이다."
  },
  "servers": [{ "url": "/api", "description": "온프레미스 설치형" }],
  "tags": [
    { "name": "mdm", "description": "기준정보 마스터" },
    { "name": "app", "description": "사용자·권한" },
    { "name": "planning", "description": "공정 순서·BOM" },
    { "name": "quality", "description": "품질 기준" },
    { "name": "integration", "description": "ERP 연계 실행" },
    { "name": "audit", "description": "변경 이력" },
    { "name": "lookup", "description": "선택 목록 — 관리 화면이 없는 조회 전용" }
  ],
  "paths": {},
  "components": {
    "parameters": {
      "IdempotencyKey": {
        "name": "Idempotency-Key",
        "in": "header",
        "required": true,
        "schema": { "type": "string", "format": "uuid" },
        "description": "전 쓰기 API 필수. 근거: deliverables/02-SW설계사양서 §3.3"
      },
      "IfMatchVersion": {
        "name": "If-Match",
        "in": "header",
        "required": true,
        "schema": { "type": "string" },
        "description": "낙관적 잠금용 version_no. 근거: 공유계약 B-1"
      }
    },
    "schemas": {
      "ErrorItem": {
        "type": "object",
        "required": ["scope", "code", "message"],
        "properties": {
          "scope": {
            "type": "string",
            "enum": ["field", "screen"],
            "description": "오류의 범위. 근거: 공유계약 G-1 — 한 필드의 문제는 인라인, 화면 수준 문제는 배너"
          },
          "field": { "type": "string", "description": "scope=field 일 때 대상 프로퍼티명" },
          "code": { "type": "string", "description": "UNIQUE_VIOLATION · REQUIRED · RANGE · PAIR · PERMISSION_DENIED 등" },
          "uniqueScope": {
            "type": "array",
            "items": { "type": "string" },
            "description": "code=UNIQUE_VIOLATION 일 때 어느 유일키 범위에서 중복인지. 근거: 공유계약 A-1 — 범위 없이 「중복」만 오면 화면이 문구를 만들 수 없다"
          },
          "message": { "type": "string" }
        }
      },
      "ErrorResponse": {
        "type": "object",
        "required": ["errors"],
        "properties": {
          "errors": { "type": "array", "items": { "$ref": "#/components/schemas/ErrorItem" } }
        }
      },
      "ConflictResponse": {
        "type": "object",
        "required": ["conflictCause", "message"],
        "properties": {
          "conflictCause": {
            "type": "string",
            "enum": ["user", "erpSync", "workerLease"],
            "description": "충돌 원인. 근거: 공유계약 B-1 확장 — user=다른 사용자, erpSync=ERP 재동기화 배치, workerLease=워커가 처리 중. 구분 없이 내려주면 화면이 「다른 사용자가 먼저 수정했습니다」라는 사실과 다른 안내를 하게 된다"
          },
          "message": { "type": "string" }
        }
      },
      "BatchFailure": {
        "type": "object",
        "required": ["index", "errors"],
        "properties": {
          "index": { "type": "integer", "description": "요청 배열에서의 위치" },
          "key": { "type": "string", "description": "식별 가능한 값(코드 등)" },
          "errors": { "type": "array", "items": { "$ref": "#/components/schemas/ErrorItem" } }
        }
      },
      "BatchResult": {
        "type": "object",
        "required": ["succeeded", "failed"],
        "description": "일괄 처리 결과. 근거: 공유계약 C-2 — 전체 롤백하지 않고 거부 건만 되돌린다",
        "properties": {
          "succeeded": { "type": "integer" },
          "failed": { "type": "array", "items": { "$ref": "#/components/schemas/BatchFailure" } }
        }
      },
      "Editability": {
        "type": "object",
        "required": ["codeEditable", "reason"],
        "description": "코드 필드 수정 가능 여부. 근거: 공유계약 B-4 — 참조 건수가 0일 때만 수정 허용. 화면이 별도 호출로 세면 화면마다 다르게 구현된다",
        "properties": {
          "codeEditable": { "type": "boolean" },
          "reason": {
            "type": "string",
            "enum": ["EDITABLE", "REFERENCED", "NOT_COUNTABLE", "RECEIVED_FROM_ERP"],
            "description": "NOT_COUNTABLE = 참조가 FK 가 아니라 세지 못함(의도적 비-FK 또는 코드 문자열 참조) → 화면은 무조건 잠근다. RECEIVED_FROM_ERP = 수신본이라 항상 읽기 전용"
          },
          "referenceCount": {
            "type": ["integer", "null"],
            "description": "reason=NOT_COUNTABLE 이면 null"
          }
        }
      },
      "PageMeta": {
        "type": "object",
        "required": ["page", "size", "total"],
        "properties": {
          "page": { "type": "integer" },
          "size": { "type": "integer" },
          "total": { "type": "integer" }
        }
      }
    }
  }
}
```

- [ ] **Step 3: 검산이 통과하는지 확인한다**

```bash
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
```
기대: `JSON 유효 — 스키마 7 · 경로 0`

공통 7종은 **물리 모델에 대응물이 없으므로 `x-source-table` 을 달지 않는다**(설계 §5-4). 화면이 필요로 하는 정보를 담는 자리이지 테이블을 옮긴 것이 아니다.

- [ ] **Step 4: 커밋**

```bash
git add deliverables/openapi/mdm-기준정보.json
git commit -m "docs: OpenAPI 골격 + 공통 응답 스키마 7종

공유계약 규약을 응답 구조로 못박았다 — G-1 오류 범위(field/screen) ·
A-1 유일 위반 범위(uniqueScope) · B-1 충돌 원인 3종(user/erpSync/
workerLease) · C-2 일괄 결과(전체 롤백 없음) · B-4 수정 가능 여부
(참조를 셀 수 없으면 NOT_COUNTABLE → 무조건 잠금).

02-SW설계사양서 §3.3 의 Idempotency-Key 를 공통 파라미터로 두었다."
```

---

## Task 4: 창고·위치 — 마스터 형 패턴 확립

**파일**
- 수정: `deliverables/openapi/mdm-기준정보.json`

**인터페이스**
- 소비: 공통 7종(Task 3)
- 생산: **마스터 형 패턴** — `Warehouse`·`WarehouseDetail`·`WarehouseCreate`·`WarehouseUpdate` 스키마 4종과 경로 6개. Task 6이 이 패턴을 다른 마스터에 그대로 적용한다

**대상 테이블**: `mdm.warehouse`(SQL 295~) · `mdm.location`(SQL 333~)
**근거 화면**: `W-06-07` §4-A·§4-B·§5-1

- [ ] **Step 1: 경로와 스키마를 쓴다**

`paths` 에 추가

```json
"/mdm/warehouses": {
  "get": {
    "tags": ["mdm"],
    "summary": "창고 목록",
    "description": "근거: W-06-07 §3 좌측 목록 · 공유계약 G-8(유효성 판정은 서버가 한다)",
    "parameters": [
      { "name": "q", "in": "query", "schema": { "type": "string" }, "description": "코드·명칭 검색" },
      { "name": "warehouseTypeCode", "in": "query", "schema": { "type": "string" } },
      { "name": "includeInactive", "in": "query", "schema": { "type": "boolean", "default": false },
        "description": "기본은 사용 중인 것만. 과거 데이터 표시용으로 켜면 isActive 표식을 함께 내린다" },
      { "name": "page", "in": "query", "schema": { "type": "integer", "default": 1 } },
      { "name": "size", "in": "query", "schema": { "type": "integer", "default": 50 } }
    ],
    "responses": {
      "200": {
        "description": "목록",
        "content": { "application/json": { "schema": {
          "type": "object",
          "required": ["items", "page"],
          "properties": {
            "items": { "type": "array", "items": { "$ref": "#/components/schemas/Warehouse" } },
            "page": { "$ref": "#/components/schemas/PageMeta" }
          }
        } } }
      }
    }
  },
  "post": {
    "tags": ["mdm"],
    "summary": "창고 등록",
    "description": "근거: W-06-07 §5-1 「창고 추가」 · 공유계약 A-1(유일 위반은 범위를 담아 응답)",
    "parameters": [{ "$ref": "#/components/parameters/IdempotencyKey" }],
    "requestBody": { "required": true, "content": { "application/json": {
      "schema": { "$ref": "#/components/schemas/WarehouseCreate" } } } },
    "responses": {
      "201": { "description": "등록됨", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/WarehouseDetail" } } } },
      "400": { "description": "검증 실패", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ErrorResponse" } } } }
    }
  }
},
"/mdm/warehouses/{warehouseId}": {
  "parameters": [{ "name": "warehouseId", "in": "path", "required": true,
    "schema": { "type": "integer", "format": "int64" } }],
  "get": {
    "tags": ["mdm"],
    "summary": "창고 상세",
    "description": "근거: W-06-07 §4-A · 공유계약 B-4(참조 건수를 응답에 담는다 — 화면이 세지 않는다)",
    "responses": { "200": { "description": "상세", "content": { "application/json": {
      "schema": { "$ref": "#/components/schemas/WarehouseDetail" } } } } }
  },
  "put": {
    "tags": ["mdm"],
    "summary": "창고 수정",
    "description": "근거: W-06-07 §5-3 · 공유계약 B-1(낙관적 잠금 — 덮어쓰기 강제는 제공하지 않는다)",
    "parameters": [
      { "$ref": "#/components/parameters/IdempotencyKey" },
      { "$ref": "#/components/parameters/IfMatchVersion" }
    ],
    "requestBody": { "required": true, "content": { "application/json": {
      "schema": { "$ref": "#/components/schemas/WarehouseUpdate" } } } },
    "responses": {
      "200": { "description": "수정됨", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/WarehouseDetail" } } } },
      "400": { "description": "검증 실패", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ErrorResponse" } } } },
      "409": { "description": "충돌", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ConflictResponse" } } } }
    }
  }
},
"/mdm/warehouses/{warehouseId}:deactivate": {
  "parameters": [{ "name": "warehouseId", "in": "path", "required": true,
    "schema": { "type": "integer", "format": "int64" } }],
  "post": {
    "tags": ["mdm"],
    "summary": "창고 사용 중지",
    "description": "물리 삭제는 제공하지 않는다. 근거: W-06-07 §5-1 · 공유계약 B-4",
    "parameters": [{ "$ref": "#/components/parameters/IdempotencyKey" }],
    "responses": {
      "200": { "description": "중지됨", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/WarehouseDetail" } } } },
      "409": { "description": "충돌", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ConflictResponse" } } } }
    }
  }
}
```

`components.schemas` 에 추가

```json
"Warehouse": {
  "x-source-table": "mdm.warehouse",
  "type": "object",
  "required": ["warehouseId", "plantId", "businessUnitId", "warehouseCode",
               "warehouseName", "warehouseTypeCode", "managementLevelCode",
               "isExternal", "isActive"],
  "description": "창고. 미착지: 다국어(한/베) 명칭 — #64",
  "properties": {
    "warehouseId": { "x-source-column": "warehouse_id", "type": "integer", "format": "int64" },
    "plantId": { "x-source-column": "plant_id", "type": "integer", "format": "int64" },
    "businessUnitId": { "x-source-column": "business_unit_id", "type": "integer", "format": "int64" },
    "warehouseCode": { "x-source-column": "warehouse_code", "type": "string", "maxLength": 50 },
    "warehouseName": { "x-source-column": "warehouse_name", "type": "string", "maxLength": 200 },
    "warehouseTypeCode": { "x-source-column": "warehouse_type_code", "type": "string", "maxLength": 50 },
    "managementLevelCode": { "x-source-column": "management_level_code", "type": "string", "maxLength": 50 },
    "isExternal": { "x-source-column": "is_external", "type": "boolean", "default": false },
    "partnerId": { "x-source-column": "partner_id", "type": ["integer", "null"], "format": "int64",
      "description": "ck_external_warehouse_partner — isExternal 이 참이면 필수" },
    "isActive": { "x-source-column": "is_active", "type": "boolean", "default": true }
  }
},
"WarehouseDetail": {
  "x-source-table": "mdm.warehouse",
  "type": "object",
  "required": ["warehouseId", "plantId", "businessUnitId", "warehouseCode",
               "warehouseName", "warehouseTypeCode", "managementLevelCode",
               "isExternal", "isActive"],
  "description": "창고 상세. editability 는 공유계약 B-4.",
  "properties": {
    "warehouseId": { "x-source-column": "warehouse_id", "type": "integer", "format": "int64" },
    "plantId": { "x-source-column": "plant_id", "type": "integer", "format": "int64" },
    "businessUnitId": { "x-source-column": "business_unit_id", "type": "integer", "format": "int64" },
    "warehouseCode": { "x-source-column": "warehouse_code", "type": "string", "maxLength": 50 },
    "warehouseName": { "x-source-column": "warehouse_name", "type": "string", "maxLength": 200 },
    "warehouseTypeCode": { "x-source-column": "warehouse_type_code", "type": "string", "maxLength": 50 },
    "managementLevelCode": { "x-source-column": "management_level_code", "type": "string", "maxLength": 50 },
    "isExternal": { "x-source-column": "is_external", "type": "boolean" },
    "partnerId": { "x-source-column": "partner_id", "type": ["integer", "null"], "format": "int64" },
    "isActive": { "x-source-column": "is_active", "type": "boolean" }
  }
}
```

⚠ **`editability` 는 `WarehouseDetail` 의 프로퍼티가 아니라 응답 래퍼로 둔다.** 물리 모델에 대응 컬럼이 없는 화면용 메타이므로 **엔티티 필드와 섞지 않는다** — 섞으면 「이 필드가 DB 에서 오는가 서버가 계산하는가」가 흐려진다.

- [ ] **Step 2: 응답 래퍼 스키마를 추가한다**

`mdm-기준정보.json` 에 추가

```json
"WarehouseDetailResponse": {
  "type": "object",
  "required": ["warehouse", "editability"],
  "properties": {
    "warehouse": { "$ref": "#/components/schemas/WarehouseDetail" },
    "editability": { "$ref": "#/components/schemas/Editability" }
  }
}
```

그리고 `GET /mdm/warehouses/{warehouseId}` 의 200 응답 `$ref` 를 `WarehouseDetailResponse` 로 바꾼다.

- [ ] **Step 3: Location 을 같은 패턴으로 추가한다**

`mdm.location`(SQL 333~) 의 비감사 컬럼 12개를 그대로 옮긴다 — `location_id` · `warehouse_id` · `parent_location_id` · `location_code` · `location_name` · `location_type_code` · `quality_zone_code` · `storage_condition_code` · `allow_mixed_item` · `allow_mixed_lot` · `capacity_qty` · `capacity_uom_id` · `is_active`.

`required` = NOT NULL 인 것 — `location_id`·`warehouse_id`·`location_code`·`location_name`·`location_type_code`·`allow_mixed_item`·`allow_mixed_lot`·`is_active`.

경로는 창고와 동형으로 `/mdm/locations` · `/mdm/locations/{locationId}` · `/mdm/locations/{locationId}:deactivate`. 목록에 `warehouseId` 필터를 둔다(근거: `W-06-07` §3 — Location 은 창고를 고른 뒤에 본다).

`Location` 스키마 `description` 에 다음을 넣는다:
`"계층 구조(parent_location_id 자기참조). ck_location_capacity — capacityQty 와 capacityUomId 는 함께 있거나 함께 비어야 한다. 근거: W-06-07 §4-B · 공유계약 A-2"`

- [ ] **Step 4: 검산과 테스트를 돌린다**

```bash
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
```

그리고 **`openapi/ui-적합성-점검표.md` 를 열어 이 태스크가 다룬 화면에 대해 5항목을 자문한다.** ❌ 가 나오면 지금 고친다 — 뒤로 미루면 굳는다.
기대: `JSON 유효 — 스키마 12 · 경로 6`

- [ ] **Step 5: 커밋**

```bash
git add deliverables/openapi/mdm-기준정보.json
git commit -m "docs: OpenAPI 창고·위치 — 마스터 형 패턴 확립

마스터 형(version_no·is_active 보유)의 여섯 경로를 확립했다 — 목록·
상세·등록·수정·사용중지·이력. 나머지 마스터가 이 패턴을 따른다.

상세 응답은 래퍼로 감쌌다. editability(공유계약 B-4)는 물리 모델에
대응 컬럼이 없어 스키마 프로퍼티로 두면 검산이 잡기 때문이다 —
Response 접미 스키마를 면제 대상으로 넓혔다."
```

---

## Task 5: 사용자·권한 — 부여·회수 형 패턴 확립

**파일**
- 수정: `deliverables/openapi/mdm-기준정보.json`

**인터페이스**
- 소비: 마스터 형 패턴(Task 4)
- 생산: **부여·회수 형 패턴** — 전체 치환 `PUT` 경로. Task 6이 `worker_qualification`·`item_uom_conversion`·`item_external_code`·`routing_operation_dependency` 에 적용한다

**대상 테이블**: `app.app_user`(229~) · `app.role`(244~) · `app.role_permission`(258~) · `app.user_role`(267~) · `app.user_data_scope`(277~)
**근거 화면**: `W-CO-02` §4-A~§4-E·§5-1

- [ ] **Step 1: 마스터 2종을 Task 4 패턴으로 추가한다**

`app_user` 비감사 컬럼 6개 — `app_user_id`·`login_id`·`user_name`·`department_id`·`email`·`status_code`·`is_active`(7개).
`role` 비감사 컬럼 5개 — `role_id`·`role_code`·`role_name`·`description`·`is_active`.

경로: `/app/users` · `/app/users/{appUserId}` · `/app/users/{appUserId}:deactivate` · `/app/roles` · `/app/roles/{roleId}` · `/app/roles/{roleId}:deactivate`.

`AppUser` 스키마 `description` 에 넣는다:
`"미착지: 인증 수단(password_hash·last_login_at·failed_login_count) 전무 — #69. 비밀번호 초기화 액션을 정의할 수 없다. loginId 는 참조를 셀 수 없어(created_by 류가 의도적 비-FK) editability.reason=NOT_COUNTABLE 로 무조건 잠근다 — 공유계약 B-4"`

- [ ] **Step 2: 부여·회수 3종을 전체 치환으로 추가한다**

```json
"/app/users/{appUserId}/roles": {
  "parameters": [{ "name": "appUserId", "in": "path", "required": true,
    "schema": { "type": "integer", "format": "int64" } }],
  "get": {
    "tags": ["app"],
    "summary": "사용자에게 부여된 역할",
    "description": "근거: W-CO-02 §4-C",
    "responses": { "200": { "description": "부여분", "content": { "application/json": {
      "schema": { "$ref": "#/components/schemas/UserRoleListResponse" } } } } }
  },
  "put": {
    "tags": ["app"],
    "summary": "역할 전체 치환",
    "description": "개별 부여·회수가 아니라 최종 상태를 통째로 보낸다. 체크박스 화면이 서버와 어긋날 여지가 없다. 서버는 한 트랜잭션으로 반영한다. 근거: W-CO-02 §4-C·§5-1 · 공유계약 B-6(부여·회수 테이블은 INSERT/DELETE 이며 물리 삭제 금지 규칙을 적용하지 않는다)",
    "parameters": [{ "$ref": "#/components/parameters/IdempotencyKey" }],
    "requestBody": { "required": true, "content": { "application/json": { "schema": {
      "type": "object",
      "required": ["roleIds"],
      "properties": { "roleIds": { "type": "array", "items": { "type": "integer", "format": "int64" } } }
    } } } },
    "responses": {
      "200": { "description": "치환됨", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/UserRoleListResponse" } } } },
      "400": { "description": "검증 실패", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ErrorResponse" } } } }
    }
  }
}
```

```json
"UserRole": {
  "x-source-table": "app.user_role",
  "type": "object",
  "required": ["userRoleId", "appUserId", "roleId"],
  "properties": {
    "userRoleId": { "x-source-column": "user_role_id", "type": "integer", "format": "int64" },
    "appUserId": { "x-source-column": "app_user_id", "type": "integer", "format": "int64" },
    "roleId": { "x-source-column": "role_id", "type": "integer", "format": "int64" }
  }
},
"UserRoleListResponse": {
  "type": "object",
  "required": ["items"],
  "properties": {
    "items": { "type": "array", "items": { "$ref": "#/components/schemas/UserRole" } }
  }
}
```

같은 형태로 둘을 더 만든다.

- `/app/roles/{roleId}/permissions` — `role_permission`. `permissionCodes: string[]` 를 받는다. `description` 에 **`"미착지: permission_code 값 목록이 회신 E-9 대기 — 화면은 비활성+사유로 둔다(공유계약 G-2·G-6)"`**
- `/app/users/{appUserId}/data-scopes` — `user_data_scope`. `businessUnitId`·`plantId` 쌍의 배열을 받는다. `description` 에 **`"ck_user_data_scope_target — 둘 중 하나 이상 필수. uq_user_data_scope 가 COALESCE(...,0) 으로 NULL 을 접어 유일 판정하므로 화면은 빈 축을 (전체)로 표기한다 — 공유계약 A-7. 미착지: 법인 축(legal_entity_id) 부재 — #69"`**

- [ ] **Step 3: 검산과 테스트를 돌린다**

```bash
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
```

그리고 **`openapi/ui-적합성-점검표.md` 를 열어 이 태스크가 다룬 화면에 대해 5항목을 자문한다.** ❌ 가 나오면 지금 고친다 — 뒤로 미루면 굳는다.
기대: `JSON 유효 — 스키마 21 · 경로 15`

- [ ] **Step 4: 커밋**

```bash
git add deliverables/openapi/mdm-기준정보.json
git commit -m "docs: OpenAPI 사용자·권한 — 부여·회수 형 패턴 확립

부여·회수 테이블(role_permission·user_role·user_data_scope)은 개별
POST/DELETE 가 아니라 전체 치환 PUT 으로 뒀다. 체크박스 화면이 최종
상태만 보내면 되고 서버와 어긋날 여지가 없다.

미착지 둘을 description 에 남겼다 — 인증 수단 전무(#69)로 비밀번호
초기화를 정의할 수 없고, 법인 축 부재(#69)로 다법인 격리를 사업부
나열로만 표현할 수 있다. loginId 는 참조를 셀 수 없어 무조건 잠근다."
```

---

## Task 6: 공정 순서 — 버전 마스터 형 패턴 확립 + A-5

**파일**
- 수정: `deliverables/openapi/mdm-기준정보.json`

**인터페이스**
- 소비: 마스터 형(Task 4) · 부여·회수 형(Task 5)
- 생산: **버전 마스터 형 패턴** — `:new-revision`·`:confirm`·`:obsolete` 오퍼레이션과 **라인 전체 치환**. Task 7이 `inspection_plan`·`bom` 에 적용한다

**대상 테이블**: `planning.routing`(617~) · `planning.routing_operation`(635~) · `planning.routing_operation_dependency`(660~)
**근거 화면**: `W-06-01` §4-A·§4-B·§4-B-1·§4-C·§5-1·§5-4

- [ ] **Step 1: 헤더와 상태 전이 오퍼레이션을 쓴다**

`routing` 비감사 컬럼 6개 — `routing_id`·`item_id`·`routing_code`·`routing_version`·`status_code`·`effective_from`·`effective_to`(7개, `effective_to` 만 NULL 허용).

경로 — `/planning/routings`(GET, `itemId` 필터) · `/planning/routings/{routingId}`(GET·PUT) · `:new-revision` · `:confirm` · `:obsolete`.

`:confirm` 의 `description`:
`"작성중 → 확정. 이후 in-place 수정은 불가하며 변경은 신규 Rev 로만 한다. 라인이 1건 이상이어야 한다. 근거: W-06-01 §5-4 · 설계 결정 07 · 공유계약 B-2·B-7"`

`Routing` 스키마 `description`:
`"미착지: 기본(default) Rev 플래그 부재 — #64. planning.bom 에는 is_default 와 부분 유일 인덱스가 있는데 여기엔 둘 다 없어 :set-default 오퍼레이션이 성립하지 않는다. 유효기간이 겹치는 Rev 가 둘일 때 어느 것을 쓸지 정할 수 없다. effective_from 은 결정 07 이 「선택」이라 했으나 하류가 NOT NULL 이라 하류를 따른다 — W-06-01 §8-3"`

- [ ] **Step 2: 라인 전체 치환을 쓴다 — A-5**

```json
"/planning/routings/{routingId}/operations": {
  "parameters": [{ "name": "routingId", "in": "path", "required": true,
    "schema": { "type": "integer", "format": "int64" } }],
  "get": {
    "tags": ["planning"],
    "summary": "공정 라인 목록",
    "description": "operationSeq 오름차순. 화면은 이 값을 그대로 보여주지 않고 목록 내 위치로 1부터 연속 표시한다 — 근거: 00-설계 §2-1-다. 채번 방식(연번/간격)은 데이터 모델링 재량이다",
    "responses": { "200": { "description": "라인", "content": { "application/json": {
      "schema": { "$ref": "#/components/schemas/RoutingOperationListResponse" } } } } }
  },
  "put": {
    "tags": ["planning"],
    "summary": "공정 라인 전체 치환 (순서 포함)",
    "description": "화면이 최종 순서를 통째로 보내고 서버가 한 트랜잭션으로 반영한다. 순서 컬럼에 유일 제약(uq_routing_operation)이 걸려 있어 두 행을 맞바꿀 때 중간 상태가 반드시 제약을 위반하는데, 전체 치환이면 그 상태가 화면에 노출되지 않는다 — 공유계약 A-5.\n\n⚠ 전체 치환은 「행 교체」가 아니다. routing_operation 을 참조하는 곳이 넷이고(routing_operation_dependency 선행·후행 2컬럼 · bom_component.routing_operation_id · production.work_order.routing_operation_id) 특히 work_order 는 NOT NULL 이라 DELETE 후 INSERT 로 구현하면 진행 중 작업지시가 무너진다. 서버는 기존 행을 유지하며 순서만 갱신해야 한다.\n\n상태가 작성중일 때만 허용한다. 근거: W-06-01 §5-1·§5-4·§8-8",
    "parameters": [{ "$ref": "#/components/parameters/IdempotencyKey" }],
    "requestBody": { "required": true, "content": { "application/json": { "schema": {
      "type": "object",
      "required": ["operations"],
      "properties": { "operations": { "type": "array",
        "items": { "$ref": "#/components/schemas/RoutingOperationUpsert" } } }
    } } } },
    "responses": {
      "200": { "description": "치환됨", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/RoutingOperationListResponse" } } } },
      "400": { "description": "검증 실패", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ErrorResponse" } } } },
      "409": { "description": "충돌 또는 확정 상태", "content": { "application/json": { "schema": {
        "$ref": "#/components/schemas/ConflictResponse" } } } }
    }
  }
}
```

`RoutingOperation` 은 `routing_operation` 의 비감사 컬럼 13개를 그대로 옮긴다 — `routing_operation_id`·`routing_id`·`operation_seq`·`process_id`·`operation_name`·`mes_managed`·`material_input_managed`·`production_result_managed`·`inspection_managed`·`output_lot_required`·`equipment_required`·`mold_required`·`standard_cycle_time_sec`·`standard_yield_rate`(14개).

`description` 에 넣는다:
`"미착지: 외주 공정 구분(is_outsourced boolean) — 요청 형태 확정, #64. L/T·UPH 부재 — 목표관리 화면이 인벤토리에 없어 범위 밖 가능성. standardYieldRate 는 0~1 비율이며 퍼센트가 아니다 — 화면이 %로 보이면서 저장을 비율로 하면 100배 오입력이 조용히 통과한다(공유계약 A-8)"`

`RoutingOperationUpsert` 는 `RoutingOperation` 에서 `routing_operation_id` 를 **선택**으로 바꾼 것이다(신규 행은 없음). `x-source-table` 은 동일하게 단다.

- [ ] **Step 3: 선후행을 별도 부여·회수 경로로 둔다**

`/planning/routings/{routingId}/operation-dependencies` — `PUT` 전체 치환. `description`:
`"선후행은 라인 행을 가리키는 별개 관계라 라인 전체 치환에 포함하지 않는다. 라인이 확정된 뒤에 건다. ck_routing_dependency_self — 자기 자신 금지. 순환(A→B→A)은 DB 가 막지 않으므로 서버가 검사한다(공유계약 A-9 ⓐ 차단). 근거: W-06-01 §4-C·§8-6"`

- [ ] **Step 4: 검산과 테스트를 돌린다**

```bash
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
```

그리고 **`openapi/ui-적합성-점검표.md` 를 열어 이 태스크가 다룬 화면에 대해 5항목을 자문한다.** ❌ 가 나오면 지금 고친다 — 뒤로 미루면 굳는다.
기대: 테스트 `OK` · 검산 통과

- [ ] **Step 5: 커밋**

```bash
git add deliverables/openapi/mdm-기준정보.json
git commit -m "docs: OpenAPI 공정 순서 — 버전 마스터 형 + A-5 전체 치환

헤더-버전-라인 3계층과 상태 전이(new-revision/confirm/obsolete)를
확립했다. 검사기준·BOM 이 같은 골격을 쓴다(공유계약 B-7).

라인은 전체 치환으로 뒀다. 순서 컬럼에 유일 제약이 걸려 있어 두 행을
맞바꾸면 중간 상태가 반드시 위반하는데, 최종 상태를 통째로 받으면 그
상태가 화면에 노출되지 않는다(A-5).

전체 치환이 「행 교체」가 아님을 description 에 못박았다 —
routing_operation 참조가 넷이고 production.work_order 는 NOT NULL 이라
DELETE+INSERT 로 구현하면 진행 중 작업지시가 무너진다.

선후행은 라인 치환에 넣지 않고 별도 부여·회수 경로로 뺐다."
```

---

## Task 7: 나머지 리소스 일괄

**파일**
- 수정: `deliverables/openapi/mdm-기준정보.json`

**인터페이스**
- 소비: 세 패턴(Task 4·5·6)
- 생산: 남은 리소스 전부. 이후 요구서가 이 경로 목록을 그대로 인용한다

각 군을 **확립된 패턴 그대로** 적용한다. 새 패턴을 만들지 않는다.

- [ ] **Step 1: 코드·조직·작업자** (`W-06-06`·`W-06-04`)

| 테이블 | 패턴 | `description` 에 넣을 것 |
| --- | --- | --- |
| `mdm.code_group` | 마스터 | — |
| `mdm.code_value` | 마스터 | `"미착지: 다국어(한/베) 명칭 — #64. ck_code_value_dates 짝 제약. display_order 에는 유일 제약이 없어 순서 재배치에 전체 치환이 불필요하다 — 공유계약 A-5 대상 아님"` |
| `mdm.department` | 마스터 | `"계층(parent_department_id). ck_department_parent 는 자기 자신만 막고 순환(A→B→A)은 막지 않으므로 서버가 검사한다. 미착지: 수신본 식별 플래그 — #65. ERP 수신본이라 읽기 전용이어야 하는데 판정할 컬럼이 없다"` |
| `mdm.worker` | **읽기 전용**(GET만) | `"ERP 수신본. 미착지: 수신본 식별 플래그 — #65"` |
| `mdm.worker_qualification` | 부여·회수 | `"uq_worker_qualification 이 COALESCE(process_id,0) 으로 접는다 — 빈 축은 (전체 공정)으로 표기(A-7). certified_by 는 FK 없는 bigint 라 무엇을 가리키는지 하류에 근거가 없다 — #64. valid_to 만료는 검사 확정을 막는다(공유계약 B-2)"` |

- [ ] **Step 2: 품목·BOM 확장** (`W-06-05`)

| 테이블 | 패턴 | 비고 |
| --- | --- | --- |
| `mdm.item` | 마스터 — 단 **MES 확장 속성만 PUT** | `description`: `"ERP 수신본. 원본 필드(itemCode·itemName·itemTypeCode·baseUomId)는 읽기 전용이고 MES 확장 속성만 편집한다 — QA #34. 미착지 넷: 자재LOT 보관단위·생산LOT 기본크기·개발품 플래그·재생재 하위코드 — #64. 그리고 수신본 식별 플래그 부재로 읽기 전용 판정 자체가 불가 — #65"` |
| `mdm.item_uom_conversion` | 부여·회수 | `ck_item_uom_distinct`·`ck_item_uom_dates` 를 `description` 에 |
| `mdm.item_external_code` | 부여·회수 | `uq_item_external_code` 의 `COALESCE(partner_id,0)` — A-7 |
| `mdm.item_bu_item_map` | 부여·회수 | `ck_item_bu_map_distinct` |
| `planning.bom` | **읽기 전용**(GET만) | `"ERP 정본 — QA #3. uq_bom_default 는 부분 유일 인덱스라 동시에 둘을 막을 뿐 자동 전환은 하지 않는다. :set-default 는 서버가 한 트랜잭션으로 처리해야 한다 — 공유계약 A-6"` |
| `planning.bom_component` | **MES 확장 4열만 PUT** | `"편집 가능한 것은 routingOperationId·actualUseProcessId·lotTraceRequired·backflushAllowed 넷뿐이다. scrapRate 는 0~1 비율이며 퍼센트가 아니다 — A-8"` |

`bom` 에 `:set-default` 오퍼레이션을 둔다.

- [ ] **Step 3: 품질 기준** (`W-06-02`·`W-06-03`)

| 테이블 | 패턴 | `description` |
| --- | --- | --- |
| `quality.inspection_plan` | 버전 마스터 헤더 | `"approved_by 와 approved_at 은 짝이어야 하나 CHECK 제약이 없다 — 서버가 검사한다(A-9). item_id·process_id·routing_id 가 모두 NULL 허용이라 적용 우선순위 규칙이 필요하다 — W-06-02 §8-4"` |
| `quality.inspection_plan_version` | 버전 | `"미착지 셋: PQC 생략 허용·생략 근거·검사기준 미비 시 단순 선택 허용 — #64. samplingQty 는 확정 문구가 「샘플 비율(%)」인데 컬럼은 수량(app.qty_t)이다 — 규약 없이 개발하면 30%가 30개로 저장된다(A-8·#64)"` |
| `quality.inspection_item_spec` | **라인 전체 치환** | `"ck_inspection_limits — 상한 ≥ 하한. 목표값이 범위 밖인 것은 DB 가 막지 않으므로 서버가 경고한다(A-9 ⓑ). 항목코드에 버전 내 유일 제약이 없어 중복 입력이 가능하다 — W-06-02 §8-3"` |
| `quality.defect_code` | 마스터 | `"2계층(parent_defect_code_id). ck_*_parent 자기참조 CHECK 가 없다 — department·location 에는 있는데 여기엔 없다(비대칭·#64). 3계층 이상은 결정 12 위반이라 서버가 차단한다. 미착지: 처분구분·다국어(ko/vi)·공정 N:M 매핑 — #64. N:M 테이블이 없어 공정 매핑 엔드포인트가 성립하지 않는다"` |
| `quality.cause_code` | 마스터 | `"불량 현상과 분리 관리(SQL 1906 주석). W-06-03 이 흡수한다 — 화면명 「불량·원인코드 2계층 마스터」"` |

- [ ] **Step 4: 연계·이력** (`W-06-10`·`W-06-11`)

| 테이블 | 패턴 | 비고 |
| --- | --- | --- |
| `integration.integration_message` | **조회 + `:retry`** | 기간 필수. `description`: `"message_key 가 UNIQUE 라 재처리는 중복 전송이 아니다. lockedBy 가 있으면 워커가 처리 중이므로 재처리를 거부하고 409 conflictCause=workerLease 로 응답한다. version_no 가 없어 낙관적 잠금 대신 리스 충돌이다 — 공유계약 B-1·C-4. payload 는 대외비를 담을 수 있어 노출 범위가 미결이다 — W-06-10 §8-4"` |
| `audit.audit_event` | **조회 전용** | **기간 필수**. `description`: `"occurred_at RANGE 파티션이라 기간 조건이 없으면 전 파티션을 훑는다 — 공유계약 B-5. 미착지: target_id 가 FK 가 아니라 유형별로 다른 테이블을 가리키고 before/after jsonb 의 키 규약이 없어 응답을 사람이 읽게 만들 수 없다 — #68"` |

- [ ] **Step 5: 조회 전용 8종**

`mdm.uom` · `mdm.partner` · `mdm.plant` · `mdm.legal_entity` · `mdm.business_unit` · `mdm.production_line` · `mdm.process` · `mdm.equipment` — **GET 목록만.**

공통 `description`: `"선택 목록용. 관리 화면이 인벤토리 108건에 없다. 유효성 판정은 서버가 하며 기본은 유효한 것만 내린다 — 공유계약 G-8. 과거 데이터 표시용은 includeInactive=true 로 켜고 isActive 표식을 함께 본다"`

`tags` 는 `["lookup"]` 을 함께 단다.

- [ ] **Step 6: 검산과 테스트를 돌린다**

```bash
cd deliverables && python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
```

그리고 **`openapi/ui-적합성-점검표.md` 를 열어 이 태스크가 다룬 화면에 대해 5항목을 자문한다.** ❌ 가 나오면 지금 고친다 — 뒤로 미루면 굳는다.
기대: 테스트 `OK` · 검산 통과

- [ ] **Step 7: 커밋**

```bash
git add deliverables/openapi/mdm-기준정보.json
git commit -m "docs: OpenAPI 나머지 리소스 — 코드·품목·품질·연계·조회 8종

확립된 세 패턴을 그대로 적용했다. 새 패턴을 만들지 않았다.

읽기 전용으로 둔 것 셋 — worker·item 원본 필드·bom(전부 ERP 수신본).
다만 수신본 식별 플래그가 없어(#65) 읽기 전용 판정 자체가 현재
불가능하다는 것을 각 description 에 남겼다.

조회 전용 8종은 GET 목록만 두고 lookup 태그를 달았다. 관리 화면이
인벤토리에 없으나 확대 1차 화면 전부가 선택 목록으로 쓴다."
```

---

## Task 8: 요구서

**파일**
- 생성: `deliverables/06-API-요구서.md`

**인터페이스**
- 소비: `mdm-기준정보.json` 의 경로 목록
- 생산: `deliverables/build-doc-html.py` 가 읽을 md

- [ ] **Step 1: 요구서를 쓴다**

절 구성 — **필드표를 쓰지 않는다**(설계 §3-2 규칙 1).

| 절 | 내용 |
| --- | --- |
| §1 개요 | 이 문서가 무엇인가 · 정본은 물리 모델 · WBS P0.4 초안 입력 · 계약 소유는 통합 Agent |
| §2 읽는 법 | 세 파일의 역할 · `x-source-table` 매핑 선언 · 검산 스크립트 사용법 |
| §3 **화면 → API 매핑** | **`openapi/ui-요구목록.md` 의 액션을 전건 소화**한다. 각 행에 엔드포인트와 근거 절 번호. 엔드포인트가 없으면 **「없음 + 이유」** — 빈칸은 누락이다 |
| §4 **UI 적합성 판정** | **화면 10개 × 5항목**(`ui-적합성-점검표.md` 기준). ❌ 는 API 설계 결함 — 고치고 다시 온다 |
| §4-1 공통 규약 대조표 | 공유계약 8건 × 어느 스키마·필드로 구현됐는가 |
| §5 **미착지가 무엇을 막는가** | 설계 §4-5 표를 그대로. 이슈 번호 포함 |
| §6 **API 를 정의할 수 없는 화면 3건** | `W-06-08`·`W-06-09`·`W-06-12` · 이유와 이슈 |
| §7 범위 밖 | 설계 §6 그대로 |
| §8 변경 이력 | |

§3 표의 형태 — 화면 10개 전건에 대해 이렇게 쓴다:

```markdown
### W-06-07 창고·Location 마스터

| 화면 액션(§5-1) | 엔드포인트 | 근거 |
| --- | --- | --- |
| 창고 추가 | `POST /mdm/warehouses` | §5-1 |
| 저장 | `PUT /mdm/warehouses/{id}` | §5-1 · 공유계약 B-1 |
| 사용 중지 | `POST /mdm/warehouses/{id}:deactivate` | §5-1 · B-4 |
| 변경 이력 | `GET /mdm/warehouses/{id}/history` | §5-1 · B-5 |
| Location 추가 | `POST /mdm/locations` | §5-1 |
| 라벨 이미지 생성 | **없음** — 서버 렌더링 호출은 라벨 도메인 소관(범위 밖) | §5-1 |
```

**「없음」인 액션을 반드시 적는다.** 빈칸으로 두면 누락으로 읽힌다.

- [ ] **Step 2: 커버리지 누락을 확인한다**

`openapi/ui-요구목록.md` 의 액션 전건이 §3 매핑표에 있는지 대조한다.

```bash
cd deliverables && python3 verify-ui-coverage.py && grep -c '^| ' openapi/ui-요구목록.md
```

목록의 액션 수와 §3 표의 데이터 행 수를 비교한다. **§3이 적으면 누락이다.** 엔드포인트가 없는 액션도 「없음 + 이유」로 **한 행을 차지해야** 한다.

- [ ] **Step 3: 적합성 5항목을 화면별로 판정한다**

`openapi/ui-적합성-점검표.md` 를 열고 화면 10개 각각에 대해 다섯 항목을 판정해 §4 에 표로 적는다. **❌ 가 나오면 그 자리에서 OpenAPI 를 고친다** — 요구서에 「결함 있음」이라고만 적고 넘어가지 마라.

- [ ] **Step 4: 커밋**

```bash
git add deliverables/06-API-요구서.md deliverables/openapi/mdm-기준정보.json
git commit -m "docs: 06 API 요구서 — 화면→API 매핑·규약 대조·미착지

필드표를 쓰지 않았다. 정본은 물리 모델이고 필드 정의는 OpenAPI 에만
둔다 — 같은 것이 여러 곳에 있어 어긋나는 것이 §I 14건의 원인이었다.

화면 10개의 액션을 전건 대조했고, 엔드포인트가 없는 액션은 「없음 +
이유」로 명시했다. 빈칸으로 두면 누락으로 읽힌다."
```

---

## Task 9: HTML 생성 + 최종 검산

**파일**
- 수정: `deliverables/build-doc-html.py`
- 생성: `deliverables/06-API-요구서.html`

- [ ] **Step 1: `CONFIG` 에 06 을 더한다**

`build-doc-html.py` 상단 `CONFIG` 딕셔너리에 `"06"` 항목을 추가한다. 기존 `"05"` 항목을 그대로 본떠 제목·부제·KPI 수치만 바꾼다. KPI 는 **자동 파싱이 아니므로** 손으로 맞춘다 — 엔드포인트 수·스키마 수·미착지 건수.

```bash
cd deliverables && grep -n '"05"' build-doc-html.py
```
로 기존 형태를 먼저 확인한다.

- [ ] **Step 2: 생성한다**

```bash
cd deliverables && python3 build-doc-html.py 06
```
기대: `생성: .../06-API-요구서.html`

- [ ] **Step 3: 브라우저로 연다**

파일을 열어 표지·본문·표·각주가 정상인지 본다. 기존 편람과 같은 모양이어야 한다.

- [ ] **Step 4: 최종 검산 — UI 요구 충족 2축**

**축 1 — 커버리지.** 화면이 요구하는 액션이 요구서 매핑표에서 하나도 빠짐없이 다뤄졌는가.

```bash
cd deliverables
python3 verify-ui-coverage.py                 # 요구 목록 재생성
python3 test-verify-ui-coverage.py            # 추출기 단위 테스트
```

그리고 **`openapi/ui-요구목록.md` 의 액션 전건**을 `06-API-요구서.md` §3 매핑표와 **손으로 대조**한다. 목록의 액션 하나하나가 매핑표에 있어야 하고, 엔드포인트가 없는 것은 **「없음 + 이유」**로 적혀 있어야 한다. **빈칸이 하나라도 있으면 누락이다.**

**축 2 — 적합성.** 요구서 §4 에 화면 10개 × 5항목 판정이 채워져 있고 **❌ 가 0건**인가. ❌ 가 남아 있으면 API 설계 결함이므로 고치고 다시 온다.

**부수.**

```bash
python3 -c "import json,io; d=json.load(io.open('openapi/mdm-기준정보.json',encoding='utf-8')); print('JSON 유효 — 스키마 %d · 경로 %d' % (len(d['components']['schemas']), len(d['paths'])))"
grep -c '"x-source-table"' openapi/mdm-기준정보.json   # 근거 추적 선언 수
```

- [ ] **Step 5: 커밋**

```bash
git add deliverables/build-doc-html.py deliverables/06-API-요구서.html
git commit -m "docs: 06 API 요구서 HTML 생성 + 최종 검산

build-doc-html.py CONFIG 에 06 을 추가했다. KPI 수치는 자동 파싱이
아니므로 손으로 맞춘다 — md 집계가 바뀌면 여기도 고친다."
```

---

## Task 10: 이 설계에서 파생된 정정 2건

**파일**
- 수정: `uiux/화면상세스펙-공통/공유계약.md`
- 수정: `uiux/2026-08-03-화면상세스펙-확대1차/W-06-01-Routing공정등록관리.md`
- 수정: `uiux/2026-08-03-화면상세스펙-확대1차/W-06-02-검사기준등록.md`

산출물 작성과 별개로 **반드시 반영해야 한다.** API 설계 과정에서 기존 문서의 오류가 드러났다.

- [ ] **Step 1: 공유계약 A-5 를 고친다**

현재 A-5 의 「해당(실측 4곳)」 목록에서 `mdm.code_value.display_order` 를 **뺀다.** 유일 제약이 없어(`integer NOT NULL DEFAULT 0`) 중복이 허용되므로 대상이 아니다. `ds-gap` G-7(행 순서 UI 가 필요한 곳 4곳)과 혼동한 것이며, **UI 가 필요한 곳과 서버 트랜잭션이 필요한 곳은 다르다**는 문장을 함께 넣는다.

A-5 에 **조항 2건을 더한다.**

1. 「전체 치환은 **행 교체가 아니다** — 서버는 기존 행을 유지하며 순서만 갱신한다. `routing_operation` 참조가 넷이고 `production.work_order.routing_operation_id` 는 `NOT NULL` 이라 `DELETE+INSERT` 로 구현하면 진행 중 작업지시가 무너진다.」
2. 「**순서 표시는 서버 저장 키가 아니라 목록 내 위치**로 매긴다. 채번 방식(연번/간격)은 데이터 모델링 재량이며, 화면이 저장 키를 보여주지 않으면 채번이 바뀌어도 화면이 흔들리지 않는다.」

- [ ] **Step 2: 화면 스펙 2장의 `Seq` 열 표기를 고친다**

`W-06-01` §3 레이아웃의 `│Seq│공정│…` 과 `│ 10│사출│…` 를 목록 내 위치 표기로 바꾸고, 아래 설명에 「표시 번호는 목록 내 위치이며 `operation_seq` 를 그대로 보이지 않는다」를 넣는다.

`W-06-02` §3 의 검사 항목 표도 같게 고친다.

- [ ] **Step 3: 커밋**

```bash
git add uiux/화면상세스펙-공통/공유계약.md uiux/2026-08-03-화면상세스펙-확대1차/W-06-01-*.md uiux/2026-08-03-화면상세스펙-확대1차/W-06-02-*.md
git commit -m "uiux: 공유계약 A-5 정정 + 순서 표시 규약 신설

API 설계 과정에서 A-5 의 대상이 4곳이 아니라 3곳임이 드러났다.
code_value.display_order 에는 유일 제약이 없어 중복이 허용되므로
대상이 아니다 — ds-gap G-7 의 「행 순서 UI 가 필요한 곳」과 혼동한
것이며 UI 가 필요한 곳과 서버 트랜잭션이 필요한 곳은 다르다.

조항 둘을 더했다 — 전체 치환은 행 교체가 아니라는 것(work_order 가
NOT NULL 이라 DELETE+INSERT 면 진행 중 작업지시가 무너진다), 그리고
순서 표시는 저장 키가 아니라 목록 내 위치로 매긴다는 것(채번은
데이터 모델링 재량이며 화면이 흔들리지 않게 한다)."
```

---

## 계획 자체 점검

**1. 설계 커버리지**

| 설계 절 | 태스크 |
| --- | --- |
| §3-1 산출물 4파일 | Task 1·2(검산) · 3~7(OpenAPI) · 8(요구서) · 9(HTML) |
| §3-2 중복 금지 4규칙 | Task 8 Step 1(필드표 없음) · 전역 제약(매핑 선언·근거 한 줄) |
| §4-1 리소스 목록 | Task 4~7 전건 |
| §4-2 URL 규약 3패턴 | Task 4(마스터)·5(부여·회수)·6(버전 마스터) |
| §4-3 A-5 | Task 6 Step 2 · Task 10 Step 1 |
| §4-4 응답 규약 8건 | Task 3(공통 스키마) · Task 8 §4(대조표) |
| §4-5 미착지 | Task 4~7 각 `description` · Task 8 §5 |
| §5 검증 4축 | Task 1·2(1번) · Task 8 Step 2(2번) · Task 8 §4(3번) · Task 9 Step 4(4번) |
| §6 범위 밖 | Task 8 §7 |
| §7 파생 정정 2건 | Task 10 |

**빠진 것 없음.**

**2. 미완 표기** — 없음. 모든 단계에 실제 코드·명령·기대 출력이 있다.

**3. 타입 일관성** — `extract_actions` → `extract_all` → `render`(전부 Task 1)이 같은 자료 구조(`{screen, action, condition, note}`)를 쓴다. 공통 응답 스키마 7종은 Task 3에서 정의하고 Task 4~7이 `$ref` 로만 참조한다. `x-source-table`·`x-source-column` 은 전역 제약에 선언하며 **근거 추적용**이라 강제 대상이 아니다.

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-03 | 초안 — 10 태스크. **설계 §3-1의 `.yaml` → `.json` 변경**(PyYAML 미설치·표준 라이브러리 관행). 검산 스크립트를 먼저 만들어 이후 전건이 그것을 통과시키며 진행하는 구조. 패턴 확립 3태스크(마스터·부여회수·버전마스터) 뒤 나머지 일괄. |
