# P-02-01 · 작업 시작 (작업지시 선택)

> 작성일: 2026-08-11 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 15차 1/2.** DR-001 §7 **5단계**의 첫 번째. **DR-001 이 마지막으로 미룬 두 장**이다 — 「**점검 기록의 형태가 정해진 뒤**에야 「무엇을 보고 막을지」를 쓸 수 있다」(§7 주). **12차가 그 형태를 확정**해 조건이 충족됐다.
> ⚠ **05 화면이 아니다** — 02 생산실행 소속인데 **설비 점검 데이터에 의존**해 DR-001 범위에 들어왔다.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `P-02-01` · 작업 시작 (작업지시 선택) |
| 프로그램 · 도메인 | **POP**(Electron 키오스크 · 1024×768 터치) · **02 생산실행** |
| 유형 | 실행 시작 |
| 행위자 | 현장 오퍼레이터 (도식 U2) |
| 신뢰도 | 확정 |
| IA 위치 | POP 태스크 모드 > 작업 시작 |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | **WF05 S3**(작업 전 점검 통제) — Trigger 「**작업(W/O) 시작 시점** — 현장 오퍼레이터가 설비에서 작업 착수」 · Actor 「현장반장·오퍼레이터 · **로그인 없이 사번만 입력**」<br>**도식스펙-05 §1** — **U2 작업자** → **B5 사번입력**(Mobile) → **A4 작업전통제** → H2 → (통과) **⊕-1** → **A5 생산진행**(파랑 · 「생산(②) 도메인 C2/C3로 인계」) | 실측 |
| **화면·IA** | `deliverables/04-통합-IA.md` §3 `P-02-01` 행 · 인벤토리 v1.6 | 실측 |
| **요구** | ✅ **REQ-PR-0023** — 「**로그인 없이 사번만 입력**」(POP 로그인 생략)<br>✓설계확정 **결정 14** — W/O 상태 8종 `편성 → 확정 → **배포** → 진행 → 완료 → 마감` · **중단↕재개 반복** · 「**재시작은 상태가 아니라 중단→진행 전이 이벤트**」<br>✓확정 **QA #9** — 점검 통제 3단계 · **긴급 W/O 우회 허용** | 실측 |
| **데이터** | ⭐ **전부 실재한다** — `production.work_order`(SQL 773 · `status_code`·`work_order_type_code`·`priority_no`·`planned_equipment_id`·`planned_mold_id`·`operation_settings_snapshot`) · `production.work_session`(847 · `session_no`·`started_at`·`terminal_id`) · **`mdm.terminal_process.can_start_work`**(SQL 577) | 실측 |

## §3. ⭐ 이 화면이 **테이블 신설을 요구하지 않는다**

05 의 17장 중 **테이블 신설을 요구하지 않은 것이 3장**이었다(11차 2 · 14차 1). **이 화면이 넷째**이고, 이유가 다르다 — **02 생산실행은 물리 모델의 선언 Scope 안**이라 애초에 갖춰져 있다.

| 필요한 것 | 실재 |
| --- | :-: |
| 작업지시 목록·상태 | `work_order.status_code` ✅ |
| **긴급 구분** | ⭐ **`work_order_type_code`**(기본 `'NORMAL'`) ✅ — QA #9 우회 판정의 근거 |
| 우선순위 | `priority_no`(기본 100) ✅ |
| 계획 설비·금형 | `planned_equipment_id`·`planned_mold_id` ✅ |
| **작업 세션 시작** | `work_session.session_no`·`started_at`·`terminal_id` ✅ |
| **단말이 시작할 수 있는가** | ⭐ **`terminal_process.can_start_work`** ✅ |

⭐ **`operation_settings_snapshot jsonb` 가 있다** — 주석이 「**기준정보 in-place 변경이 기존 지시에 소급되지 않도록 고정**」이다. **14차 A-18**(판정 근거가 움직이면 스냅샷)이 **하류에 이미 있던 선례**다.

## §4. 레이아웃 (POP 1024×768 — 세로 예산 검산)

```
┌ 헤더 64 ──────────────────────────────────────────────────────────────┐
│ 작업 시작                    PRS-01 프레스 1호기      사번 [    ] ●온 │
├ 본문 616 (패딩 16×2 제외 584) ────────────────────────────────────────┤
│ ┌ ① 사번 · 96 ─────────────────────────────────────────────────────┐ │
│ │   사번  [ 3 3 9 1 ]      김작업                [ 7 ][ 8 ][ 9 ]    │ │
│ │                                                [ 4 ][ 5 ][ 6 ]    │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│ ┌ ② 작업지시 · 328 ────────────────────────────────────────────────┐ │
│ │  이 설비 · 배포됨            [ 전체 보기 ]                        │ │
│ │  ┌──────────────────────────────────────────────────────────┐    │ │
│ │  │ ● WO-2026-0412  HSG-100 하우징      500 EA   08-11 08:00 │    │ │
│ │  │   프레스 · MLD-0207 · 우선 100                            │    │ │
│ │  ├──────────────────────────────────────────────────────────┤    │ │
│ │  │ ⚡ WO-2026-0418  CVR-220 커버       120 EA   08-11 13:00 │    │ │
│ │  │   긴급 · 프레스 · MLD-0104 · 우선 10                      │    │ │
│ │  ├──────────────────────────────────────────────────────────┤    │ │
│ │  │ ⏸ WO-2026-0405  HSG-100 하우징      300 EA   중단 중      │    │ │
│ │  │   설비 고장 · 09:40부터  → [ 재개 ]                       │    │ │
│ │  └──────────────────────────────────────────────────────────┘    │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│ ┌ ③ 선택 확인 · 160 ───────────────────────────────────────────────┐ │
│ │   WO-2026-0412  HSG-100 하우징 500 EA                             │ │
│ │   설비 PRS-01 · 금형 MLD-0207 · 교대 주간                          │ │
│ │   ─────────────────────────────────────────                       │ │
│ │   ✅ 작업 전 점검  08-11 07:12 · 사번 3391 · 합격                  │ │
│ └───────────────────────────────────────────────────────────────────┘ │
├ 액션바 88 ────────────────────────────────────────────────────────────┤
│                            [ 다시 선택 ]        [ 작업 시작 ]          │
└───────────────────────────────────────────────────────────────────────┘
```

### §4-1. 세로 예산 검산 — **E-1 슬랙 0**

```
헤더        64
본문       616   ← ① 96 + ② 328 + ③ 160 = 584 + 패딩 16×2 = 616  ✅
액션바      88
──────────────
합계       768   슬랙 0
```

⚠ **E-4 적용** — **스크롤 구획은 ② 하나**다(지시가 여럿이다). **③은 접지 않는다** — 점검 통제 결과가 여기 나오므로 **접히면 못 본다**.

⚠ **G-5** — 지시 카드는 **주요 64px↑**, 액션바는 **72px 급**(E-3). DS `xl` 60px 로 **12px 부족** — 임시 규약(제품 지정 · DS #67).

⭐ **G-14** — POP 진입 시 **미확인 공지를 셸이 먼저 띄운다.** 화면이 그리지 않는다.

## §5. 필드·규칙

### §5-A. 작업 세션 — `production.work_session`

| 라벨(한 / 베) | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| 작업지시 | `work_order_id` | ✅ | ②에서 선택 |
| 세션 번호 | `session_no` | ✅ | ⭐ **`uq_work_session (work_order_id, session_no)`** — ~~중단·재개마다 는다~~ → **세션을 새로 열 때마다** 는다(2026-08-23 정정) §5-4 |
| 교대 | `shift_id` | ✅ | `mdm.shift` — 시작 시각으로 판정 |
| 설비 · 금형 | `equipment_id` · `mold_id` | — | 헤더 설비 + W/O 계획 금형 |
| 단말 | `terminal_id` | ✅ | **POP 단말 등록분**(`M-CO-01`) |
| 시작 시각 | `started_at` | ✅ | ⚠ **단말 시각**(C-12 — 오차는 함께 보내지 않는다 · v2.7) |
| 종료 시각 | `ended_at` | — | ⭐ **비면 진행 중**(G-16) — 이 화면은 안 넣는다 |
| 상태 | `status_code` | ✅ | ⚠ **G-16 보완 대상** §8-2 |
| 작업자 | `work_session_worker`(SQL 870) | ✅ | **사번 귀속** — REQ-PR-0023 |

### §5-1. ⭐ **F-1 이 정확히 걸리는 첫 자리** — `can_start_work`

공유계약 **F-1**(`terminal_process` 8플래그를 액션 활성 조건으로 쓴다)이 v0.9 에서 「**생산·품질 단말 한정**」으로 범위 정정됐다. **이 화면이 그 한정의 정중앙**이다.

```
[ 작업 시작 ]  활성 조건
   terminal_process (이 단말 × 이 W/O 의 공정) . can_start_work = true
```

⚠ **12차·13차가 05 액션을 `can_input_result` 에 「잠정 포섭」**했던 것과 다르다 — **여기는 전용 플래그가 있다.**

| 없으면 | 처리 |
| --- | --- |
| 이 단말×공정 행이 없다 | ⛔ **시작 불가** + 「이 단말은 이 공정의 작업을 시작할 수 없습니다」(**G-3** — 「단말 설정에서 권한을 부여하세요」 + `W-CO-06` 안내) |
| 행은 있고 `can_start_work=false` | 동상 |

### §5-2. ⭐⭐ 작업 전 점검 통제 — **이 화면은 「보이고」 `P-02-02` 가 「막는다」**

**F-5**(게이트를 만드는 화면과 집행하는 화면을 나눈다)를 그대로 쓴다.

| 화면 | 책임 |
| --- | --- |
| **`M-05-01`** 점검 입력 | **근거를 만든다**(12차) |
| **`P-02-02`** 작업 전 점검 통제 | ⭐ **판정하고 막는다** |
| **이 화면** | **판정 결과를 보이고** 통과 시 세션을 연다 |

⭐ **③ 구획에 결과를 한 줄로 보인다** — 「✅ 작업 전 점검 08-11 07:12 · 사번 3391 · 합격」.

⛔ **이 화면이 판정 규칙을 다시 쓰지 않는다** — F-5 규칙 3(근거 화면은 집행 문구를 쓰지 않는다)의 역방향이다. **판정은 `P-02-02` 가 하고 결과 문자열을 받는다.**

### §5-3. ⚡ 긴급 W/O 는 **표시로 구분되고 우회는 `P-02-02` 가 판정한다**

QA #9 가 「**긴급 W/O 는 점검 통제 우회 허용**」을 확정했고 도식이 **회색 경로**로 그렸다.

| 이 화면 | |
| --- | --- |
| `work_order_type_code` 로 **⚡ 긴급 배지**를 보인다 | ✅ |
| 우회 **판정** | ⛔ **`P-02-02` 가 한다** — 이 화면은 결과만 받는다 |

⚠ **우회했다는 사실이 세션에 남아야 한다** — 「우회·경고 진행도 기록」(도식 MES-2). → §8-3

### §5-4. 중단된 W/O 는 **「재개」이지 새 시작이 아니다**

**결정 14** 가 「**재시작은 상태가 아니라 중단→진행 전이 이벤트**」로 확정했고, `uq_work_session (work_order_id, session_no)` 가 **세션이 여러 번 열림**을 전제한다.

⛔ **정정(2026-08-23) — 「재개하면 새 세션」은 틀렸다.** v0.1 이 아래처럼 적었으나 **실측 다섯 곳이 반대**였다.

> ~~`WO-2026-0405` `session_no 1` 08:00\~09:40 (중단) / `session_no 2` ← **재개하면 새 세션**~~

**재개는 같은 세션 안의 사건이다.** 중단해도 세션은 **열려 있고**(`ended_at` 이 비어 있다) `status_code` 만 「중단」으로 바뀐다. 근거·판정 전문은 공유계약 **`G-16` 보완**(구간을 «닫는» 사건과 구간 «안의» 사건을 가른다) — 요약하면 ① 계약에 `:resume` 액션이 없다(재개를 표현할 길은 사건 적재뿐) ② 논리 모델 §9.7 이 사건 표를 「시작·중지·**재개**·종료 이력」으로 정의했다 ③ `status_code` 가 「중단」을 담는다 ④ `P-02-10` §3 목업이 **한 세션에** 시작→중단→재개를 그린다 ⑤ **아래 §8 미결 2 자신이** 「`ended_at` 과 `status_code` 를 둘 다 쓴다」고 적었다.

⚠ **결정 14 인용이 결론을 받쳐 주지 않았다** — 그 문장은 **작업지시 상태 그래프**에 대한 것이고, 「전이 이벤트」는 **같은 것의 상태가 바뀐다**는 뜻이라 오히려 반대쪽이다.

```
WO-2026-0405   session_no 1  08:00~17:00   ← 세션은 하나
                 08:00  START
                 09:40  STOP   (사유: 설비 고장)
                 11:20  RESUME
                 17:00  END    ← :end 로 닫는다
```

| 버튼 | 언제 | 무엇 |
| --- | --- | --- |
| **작업 시작** | 상태 = **배포** | 새 세션을 연다 — `session_no = 직전 + 1`(그 작업지시의 첫 세션이면 `1`) · ⭐ **`START` 사건도 같은 트랜잭션으로 만들어진다** — 화면이 따로 보내지 않는다 |
| **재개** | 상태 = **중단** | ⛔ **세션을 새로 열지 않는다** — 같은 세션에 `RESUME` 사건을 적재하고 `status_code` 를 「진행」으로 되돌린다 · ⚠ **작업 홀드 이력의 종료는 생산 도메인**(`P-02-10`)이 찍는다 |

⚠ **그래도 `session_no` 는 는다** — 세션을 **새로 열 때** 증가한다(작업 완료·교대 마감으로 `:end` 한 뒤 다시 시작하는 경우). 「중단·재개마다」가 아니다.

⚠ **중단 사유가 「설비 고장」이면 그 고장이 끝났는지 보인다** — 13차 `W-05-04` §5-6 이 「**고장 완료가 W/O 를 자동 재개시키지 않는다**」로 정했다. **사람이 판단하도록 정보만 준다** — 「설비 고장 · 09:40부터」.

### §5-5. 액션·상태

| 액션 | 활성 조건 |
| --- | --- |
| 사번 입력 | 항상 — **숫자 키패드**(D-4 · ds-gap G-6) |
| 지시 선택 | 사번 확인됨 |
| **작업 시작** | 사번 + 지시 + **`can_start_work`** + **`P-02-02` 통과** |
| **재개** | 상태 = 중단 · 위와 같은 조건 |
| 전체 보기 | 이 설비 밖 지시도 본다 — ⚠ **경고**(§6) |

**상태**: `사번 대기` → `지시 목록` → `선택됨` → **`통제 판정 중`** → `시작 중` → `성공(작업 화면으로)` / `차단` / `실패`

## §6. 예외·오프라인

| 상황 | 처리 |
| --- | --- |
| 사번이 목록에 없다 | 인라인 — 「등록되지 않은 사번입니다」 · ⚠ **재직 여부는 캐시해도 된다**(C-13 보강 기준) |
| `can_start_work = false` | ⛔ **시작 불가** + 사유·경로(G-3 · §5-1) |
| **점검 통제 = 차단** | ⛔ **시작 불가** — `P-02-02` 가 판정 문구를 준다 · **점검 화면으로 안내** |
| 점검 통제 = 경고 | ⚠ **확인 다이얼로그 후 진행** — **우회 기록이 남음을 명시** |
| 이 설비에 배포된 지시가 없다 | `EmptyState` — 「이 설비에 배포된 작업지시가 없습니다」 + **전체 보기** |
| **전체 보기로 다른 설비 지시 선택** | ⚠ **경고** — 「이 지시의 계획 설비는 PRS-03 입니다」 · ⛔ 막지 않는다(현장이 바꿔 돌릴 수 있다) |
| 이미 진행 중인 세션이 있다 | ⛔ **새 시작 불가** — 「진행 중인 작업이 있습니다」 + 그 세션으로 이동 |
| 미확인 공지 | ⭐ **G-14** — 셸이 먼저 띄운다 |
| **오프라인** | §6-1 |

### §6-1. 오프라인 — ⛔ **허용하지 않는다** · C-14 가 그렇게 말한다

**C-14**(오프라인 가부는 **판정값을 쓰는가**로 정한다) 기준으로 **금지**다.

| 이 화면이 읽는 판정값 | 캐시하면 |
| --- | --- |
| **`can_start_work`** | 권한 없는 단말이 작업을 연다 |
| **점검 이력**(통제 판정) | ⛔ **차단해야 할 작업이 열린다** |
| W/O 상태(배포/중단) | 취소된 지시로 작업한다 |

⭐ **12차 `M-05-01` 이 「오프라인 점검을 허용하되 미전송 경고」로 간 것과 짝**이다 — **만드는 쪽은 허용하고 판정하는 쪽은 막는다**(C-15 ↔ C-14).

⚠ **결정 17 의 오프라인 시나리오 2건에 이 화면이 없다** — 「W/O 선배포 생산」·「입출고 단일 단말 스캔」뿐이다. **선배포는 W/O 데이터를 미리 받는 것**이고 **작업 시작 판정을 오프라인으로 하라는 뜻이 아니다.** → §8-4

**연결이 끊기면** — 화면이 **그 사실을 말하고 대기**한다(「연결이 끊겨 작업을 시작할 수 없습니다 · 복구되면 자동으로 목록을 불러옵니다」). ⛔ **회색 버튼만 두지 않는다**(G-3).

## §7. DS 매핑 (`a554d11` · 판정 절차 ① 수행 · 실재 29종)

| 요소 | 유형 | 컴포넌트 |
| --- | :-: | --- |
| 셸·헤더 | **a** | `AppShell` · `PageHeader` |
| 지시 카드 목록 | **a** | `Card` 반복 (⚠ **`Table` 아님** — 터치 타겟 64px↑) |
| 긴급·중단 배지 | **a** | `Chip` |
| 선택 확인 · 점검 결과 | **a** | `Card` · `Chip` |
| 확인 다이얼로그 | **a** | `Dialog` |
| 배너·빈 상태 | **a** | `AlertBanner` · `EmptyState` |
| **숫자 키패드**(사번) | **d** | **ds-gap G-6** — **7곳째** · ⚠ **사번형**(정수·고정 자릿수) |
| 읽기 전용 + 사유 | **c** | (**ds-gap G-10** · 20곳째) |
| **터치 타겟 72px 급** | **ds-gap G-5** | ⛔ DS `xl` 60px 로 부족 — DS #67 |

⭐ **G-6 이 일곱 곳** — 사번형이 `P-CO-01`·`M-CO-01`·**이 화면** 셋이 됐다.

**신규 갭 0건 — 15차 연속.**

## §8. 미결

| # | 항목 | 성격 | 처리 |
| :-: | --- | --- | --- |
| **1** | ⭐ **테이블 신설 요구 0건** | — | 02 는 물리 모델 Scope 안이라 갖춰져 있다 |
| **2** | ⚠ **`work_session` 이 `ended_at` 과 `status_code` 를 둘 다 갖는다** | **정합** | **G-16 보완**(14차)이 이미 다뤘다 — 「**이미 있는 것은 고치라는 뜻이 아니다** · 화면은 실재 구조를 쓴다」. **둘 다 쓰되 어긋나지 않게 서버가 보장**한다(결정 14 「시스템이 보장」) |
| **3** | **우회·경고 진행 기록** — 도식 MES-2 「우회·경고 진행도 기록」 | **설계 결정** | ⚠ **`work_session` 에 담을 자리가 없다.** **`P-02-02` 가 통제 판정 이력을 만든다** — 그 장에서 정한다 |
| **4** | **결정 17 오프라인 시나리오에 이 화면이 없다** | **정합** | ✅ **오프라인 금지가 맞다**(§6-1). 「W/O 선배포」는 **데이터를 미리 받는 것**이지 **판정을 오프라인으로 하라는 뜻이 아니다** |
| **5** | **전체 보기로 계획 밖 설비의 지시를 시작** | **설계 결정** | ⚠ **경고만 하고 막지 않는다** — 현장이 설비를 바꿔 돌릴 수 있다. **세션에 실제 설비가 기록**되므로 사후 추적된다 |
| **6** | ⭐ **`status_code` 의 «코드 문자열»이 없다** (2026-08-23 신설) | **설계 결정** | 세션 모델을 가르면서 이 칸이 **「진행·중단·종료」를 담는다는 뜻은 확정**됐다(`START`·`RESUME`→진행 · `STOP`→중단 · `END`→종료 · 공유계약 `A-25`). ⛔ **그런데 담을 코드 문자열이 어디에도 없다** — 계약에도 설명이 없던 칸이라 미확정 코드 계수기(`enum` 부재로 뽑는다)에도 안 걸렸다. ⬜ **지금 만들지 않는다** — 「뜻과 식별자는 다른 산출물」(`G-32` v3.4)이고, 지어내면 근거 없는 값이 상태 기계에 박힌다. 계약의 `statusCode` 설명에 「아직 확정되지 않았다」로 표면화해 뒀다 · 잔여 결정 분류표 「분류표가 놓친 자리」 셋째 줄 |

## §9. 이 장에서 나온 공유 계약 후보

### 9-1. ⭐ **판정하는 화면은 오프라인을 금지하고, 만드는 화면은 허용한다** — C-14 ↔ C-15 의 짝

12차가 **C-15**(내가 만든 것이 남의 판정 근거면 오프라인 허용 + 미전송 경고)를 세웠다. **그 반대쪽이 이 화면**이다.

```
M-05-01 점검 입력   만든다  →  오프라인 ✅ 허용 (C-15)
P-02-01 작업 시작   읽는다  →  오프라인 ⛔ 금지 (C-14)
```

**규칙안**: 한 데이터의 **생산자와 소비자가 다른 화면**일 때 오프라인 정책이 **반대**가 되는 것이 정상이다.

| | 생산자 | 소비자 |
| --- | :-: | :-: |
| 오프라인 | ✅ 허용 | ⛔ 금지 |
| 왜 | 막으면 현장이 종이로 돌아간다 | **틀린 판정으로 통과시킨다** |
| 화면이 말할 것 | 「**미전송 N건 — 작업 통제에 반영되지 않습니다**」 | 「**연결이 끊겨 시작할 수 없습니다**」 |

⚠ **두 문구가 짝이어야 한다** — 점검자는 「반영 안 됨」을 보고, 작업자는 「시작 못 함」을 본다. **같은 원인의 두 얼굴이다.**

### 9-2. **연결이 끊겼을 때 회색 버튼만 두지 않는다**

**G-3**(비활성 사유는 「왜」가 아니라 「어떻게 풀 것인가」)의 **오프라인 판**이다.

**규칙안**: 오프라인을 금지하는 화면은

1. **왜 못 하는지** — 「연결이 끊겼습니다」
2. **무엇을 하면 되는지** — 「복구되면 자동으로 불러옵니다」 · 대안 경로가 있으면 안내
3. ⛔ **회색 버튼만 두지 않는다** — 작업자는 **단말이 고장 난 줄 안다**

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.4 | 2026-08-23 | **2차 리뷰 반영(`PR #211`)** — **미결 6 신설**: `status_code` 의 **코드 문자열**이 없다. 세션 모델을 가르면서 이 칸이 「진행·중단·종료」를 담는다는 **뜻은 확정**됐는데 식별자가 없고, 계약에 설명이 없던 칸이라 **미확정 코드 계수기에도 안 걸렸다.** ⭐ **이 판이 표면화한 자리를 이 판이 등재한다** — 「놓친 것을 적어 두지 않으면 다음에 또 같은 방식으로 놓친다」는 이 트랙이 세운 원칙이 자신에게 걸린다. ⬜ 값은 지금 만들지 않는다(`G-32` v3.4 — 뜻과 식별자는 다른 산출물). |
| v0.3 | 2026-08-23 | **리뷰 반영(`PR #211`)** — §5-4 액션 표의 **작업 시작** 행에 「`START` 사건도 같은 트랜잭션으로 만들어진다」를 적었다. `START` 를 값 목록에 더해 놓고 **적재되는 자리를 어디에도 안 적어** 두면, 이력 화면이 「`08:00 세션 시작`」 줄을 그릴 원천이 없어 **`START` 를 더한 이유가 그대로 되돌아온다**(공유계약 `A-25` 규칙 6 — 사건 유형이 늘면 계약에 먼저 등재한다). 계약 쪽에도 events 오퍼레이션에 사건 유형 다섯과 적재 주체를 적었다. |
| v0.2 | 2026-08-23 | ⛔ **§5-4 정정 — 「재개하면 새 세션」이 틀렸다.** v0.1 이 `session_no` 를 「중단·재개마다 는다」로 적었으나 **실측 다섯 곳이 반대**였다(계약에 `:resume` 부재 · 논리 모델 §9.7 이 「재개」를 사건으로 정의 · `status_code` 가 「중단」을 담음 · `P-02-10` 목업이 한 세션에 시작→중단→재개를 그림 · **이 문서 자신의 §8 미결 2**). **재개는 같은 세션 안의 사건**이고 `session_no` 는 **세션을 새로 열 때** 는다. ⚠ **결정 14 인용이 결론을 받쳐 주지 않았다** — 그 문장은 작업지시 상태 그래프에 대한 것이고 「전이 이벤트」는 오히려 반대쪽을 지지한다. 판정 전문은 공유계약 **`G-16` 보완**(v3.9). ⭐ **어긋난 채 12일 있었다** — `P-02-10`(2026-08-04)과 이 문서(2026-08-11)가 같은 사건을 반대로 설계해 뒀는데, 두 화면이 서로를 인용하지 않아 아무도 못 봤다. |
| v0.1 | 2026-08-11 | 초안 — 확대 15차 1/2. **DR-001 이 마지막으로 미룬 두 장** 중 첫째이고, **12차가 점검 기록 형태를 확정**해 조건이 충족됐다. ⭐ **테이블 신설 요구 0건** — 02 는 물리 모델 Scope 안이라 `work_order`·`work_session`·`terminal_process` 가 다 갖춰져 있다. ⭐ **F-1 이 정확히 걸리는 첫 자리** — `can_start_work` 전용 플래그가 있다(12·13차가 `can_input_result` 에 「잠정 포섭」한 것과 다르다). ⭐ **F-5 대로 「보이고」만 하고 「막는 것」은 `P-02-02`** 에 넘긴다 — 판정 규칙을 두 화면에 복제하지 않는다. **중단된 W/O 는 「재개」이고 새 세션**(결정 14 「재시작은 상태가 아니라 전이 이벤트」 · `uq_work_session` 이 전제). ⛔ **오프라인 금지** — C-14 기준(판정값을 읽는다). ⭐ **12차 C-15 와 짝**이다: **만드는 화면은 허용 · 읽는 화면은 금지.** ⭐ `operation_settings_snapshot` 주석이 **A-18 의 하류 선례**임을 발견. DS 신규 갭 0건(15차 연속) · G-6 7곳째. 공유계약 후보 2건. |
