# M-04-03 · 포장 재구성 스캔

> 작성일: 2026-08-07 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 9차 5/6** — ⭐ **2단말 스택의 하나**다. 모바일에서 스캔하고 **POP(`P-04-04`)에서 발번·인쇄**한다 — 04 에서 유일하다.
> ⛔ **충실도 「부분」** — **재구성 이벤트 테이블이 0건**이다(결정 13 이 「이벤트 헤더–라인 계보」를 확정했는데).

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `M-04-03` · 포장 재구성 스캔 |
| 프로그램 · 도메인 | **모바일**(PDA) · 제품출하(04) |
| 유형 | **스캔** |
| 행위자 | 완제품창고담당 |
| 신뢰도 | **확정** — 04-S-D · ✓확정 QA #17 |
| IA 위치 | 모바일 > 스캔 타일 > 포장 재구성 |
| 범위 | **원 포장 스캔 + 재구성 유형 선택 + 구성 편집**. **발번·라벨 인쇄는 `P-04-04`**(2단말 스택) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` — **04-S-D** Mobile **원 포장 LOT 스캔** · **H10 합병/분할/재구성 유형 선택** · **배지 = 모바일 + POP 스택** |
| **화면·IA** | 인벤토리 v1.9 `M-04-03` — 유형 스캔 · 충실도 **부분** · ✓확정 **QA #17** |
| **요구** | ✓설계확정 **결정 13**(⭐ **이벤트 헤더–라인 계보** · **신규 생성분 = 신규 발번 + 새 라벨** · **분할 잔량 = 원 단위 유지** · 수량 변경은 **이벤트 한정 + 이력** · 라벨 재출력 = 사용자 선택) · 공유계약 **§C 오프라인** · **§D 스캔** |
| **데이터** | `inventory.handling_unit`(**`parent_handling_unit_id` 자기참조** · `status_code`) · `handling_unit_content`(**`uq(HU, 품목, LOT)`**) · `trace.lot_relation`(**`relation_type_code`·`source_event_type_code`+`source_event_id`·`allocation_method_code`·`trace_accuracy_code`** · `uq_lot_relation`) · ⛔ **재구성 이벤트 테이블 0건** — 전건 실측 |

> ⛔ **결정 13 이 「이벤트 헤더–라인」을 확정했는데 그 테이블이 없다.** `lot_relation.source_event_type_code` + `source_event_id` 가 **가리킬 대상이 없다**(§5-2).

## §3. 레이아웃 (모바일 — 360×720dp · 스크롤 단위 표시)

```
┌ 상단 ───────────────────────────────┐
│ 포장 재구성            박창고 ▾  ●온 │  ← 귀속 상시 표시(D-5)
├─────────────────────────────────────┤
│ 《① 원 포장 스캔》                   │  ─ 스크롤 1
│  ┌─────────────────────────────┐    │
│  │  [ 포장 QR 을 비추세요   ]  │    │
│  └─────────────────────────────┘    │
│  스캔됨  CTN-2026-0091               │
│   └ FG-1001  FG-…-0311  120          │
│   └ FG-1001  FG-…-0305   60          │
├─────────────────────────────────────┤
│ 《② 재구성 유형》                    │  ─ 스크롤 2
│  ( ) 합병 — 여러 포장을 하나로       │
│  (•) 분할 — 하나를 여러 개로         │
│  ( ) 재구성 — 내용을 바꾼다          │
├─────────────────────────────────────┤
│ 《③ 새 구성》                        │  ─ 스크롤 3
│  새 포장 1  FG-…-0311   80           │
│  ┌ 남는 것 ──────────────────────┐  │
│  │ CTN-2026-0091 (원 포장 유지)   │  │
│  │  └ FG-…-0311  40               │  │
│  │  └ FG-…-0305  60               │  │
│  │ ⓘ 분할 잔량은 원 포장 번호를   │  │
│  │   그대로 씁니다 (결정 13)      │  │
│  └────────────────────────────────┘  │
├─────────────────────────────────────┤
│  ⛔ 재구성 이력을 저장할 자리가 없습니다│
│     — 지금은 포장 구성만 바뀝니다     │
├─────────────────────────────────────┤
│      [ 재구성 확정 → 라벨 발행 ]     │
│      ⓘ 발번·인쇄는 POP 에서          │
└─────────────────────────────────────┘
```

## §4. 필드

### §4-A. 포장 단위 `inventory.handling_unit`

| 라벨(한/베) | 출처 컬럼 | 비고 |
| --- | --- | --- |
| 포장 번호 | `handling_unit_no` | UNIQUE · ⭐ **분할 잔량은 원 번호 유지**(결정 13) |
| 유형 | `handling_unit_type_code` | 카톤 · 팔레트 |
| 상위 포장 | `parent_handling_unit_id` | 자기참조 · `ck` 는 자기 자신만 막는다 |
| 상태 | `status_code` | ⚠ 값 목록 미정 |

### §4-B. 내용물 `inventory.handling_unit_content`

| 라벨 | 출처 컬럼 | 검증 |
| --- | --- | --- |
| 품목 · LOT · 수량 | `item_id` · `lot_id` · `qty` | 셋 다 NOT NULL · `qty CHECK > 0` |
| — | — | **`uq(HU, 품목, LOT)`** — 같은 LOT 두 행 불가(`P-04-01` §5-3 과 같은 규칙) |

### §4-C. ⛔ 재구성 이벤트 — **담을 곳이 없다**

| 결정 13 이 요구한 것 | 실측 |
| --- | :-: |
| 이벤트 **헤더** | ⛔ **테이블 없음** |
| 이벤트 **라인** | ⛔ **없음** |
| 수량 변경 이력 | ⛔ **없음** |

⚠ `trace.lot_relation` 은 **LOT 관계**를 담지 **포장 재구성**을 담지 않는다 — 그리고 **재구성은 LOT 이 안 갈린다**(§5-3).

## §5. 액션·상태

### §5-1. ⭐ 2단말 스택 — **04 에서 유일하다**

```
M-04-03 (모바일)          P-04-04 (POP)
원 포장 스캔 · 구성 편집  →  신규 발번 · 새 라벨 인쇄
```

**한 업무가 두 셸에 걸쳐 있다.** 통합 IA §7-4 가 「**셸 간 레코드 공유 설계가 필요하다**」를 파생 확인 사항으로 남겼다.

**화면 규칙** — 모바일이 **확정하면 POP 대기 목록에 올라간다.** 모바일에서 인쇄하지 않는다(프린터가 POP 스테이션에 있다 — 결정 18).

⚠ **중간 상태가 생긴다** — 「구성은 바꿨는데 라벨은 아직」. 그 상태를 **어디에 저장하나**가 §5-2 의 문제와 같은 뿌리다.

### §5-2. ⛔ 재구성 이벤트 테이블이 **0건**이다

**결정 13 확정**(2026-07-06 CTO · 절충안 채택):

> **이벤트 헤더–라인 계보** · 신규 생성분 = 신규 발번 + 새 라벨 · **분할 잔량 = 원 단위 유지** · **수량 변경은 이벤트 한정 + 이력** · 라벨 재출력 = 사용자 선택

**「이벤트」가 데이터에 없다.**

| 무엇이 막히나 | |
| --- | --- |
| **누가 언제 왜 재구성했는가** | ⛔ 안 남는다 |
| **수량 변경 이력** | ⛔ 결정 13 규칙 2(파괴검사 수량 변동 기록)가 성립하지 않는다 |
| `lot_relation.source_event_id` 가 가리킬 대상 | ⛔ 없다 |
| **모바일 → POP 중간 상태** | ⛔ 담을 곳이 없다(§5-1) |

**A-11 로 물러난다** — **포장 구성만 바꾸고**(`handling_unit_content` 갱신) **이력은 남기지 않는다.** 화면이 그 사실을 적는다.

```
⛔ 재구성 이력을 저장할 자리가 없습니다 — 지금은 포장 구성만 바뀝니다
```

⚠ **중간 상태는 `handling_unit.status_code` 로 물러난다** — 「라벨 발행 대기」 값을 두면 POP 이 그것을 찾는다. **값 정의만으로 되지만 이벤트 이력은 여전히 없다.**

### §5-3. ⭐ 재구성은 **LOT 을 가르지 않는다** — 포장만 바뀐다

```
handling_unit_content   handling_unit_id · item_id · lot_id · qty
```

**LOT 은 그대로이고 어느 포장에 들었는지만 바뀐다.** 그래서 **`trace.lot_relation` 이 생기지 않는다.**

| 행위 | LOT | 관계 |
| --- | :-: | :-: |
| **재구성**(이 화면) | 그대로 | ⛔ 없다 |
| 선별(`sorting_result`) | **갈린다** — `good_lot_id`·`defect_lot_id` | ✅ 생긴다 |
| 생산(투입 → 산출) | 새로 생긴다 | ✅ 생긴다 |

⭐ **`P-04-03` §5-4 가 세운 것과 같은 형태** — **모델의 컬럼이 「LOT 이 갈리는가」를 이미 말한다.**

### §5-4. 분할 잔량은 **원 번호를 유지한다** — 결정 13 절충안

```
CTN-2026-0091 (180)  →  분할  →  새 포장 (80) + CTN-2026-0091 (100)
                                    ↑ 신규 발번      ↑ 원 번호 유지
```

**둘 다 새로 발번하는 안**과 **잔량이 원 번호를 쓰는 안** 중 **후자가 채택**됐다(절충안).

**화면이 그것을 명시한다** — 「분할 잔량은 원 포장 번호를 그대로 씁니다」. 안 적으면 **작업자가 잔량 라벨도 새로 뽑으려 한다.**

⚠ **그래서 라벨 재출력이 사용자 선택이다**(결정 13) — 잔량 라벨은 **원래 것이 아직 유효**하다. 다만 **수량이 바뀌었으므로** 라벨에 수량이 찍혀 있으면 다시 뽑아야 한다 → `P-04-04` 가 선택하게 한다.

### §5-5. 합병에서 **같은 LOT 이 만나면 합친다**

`P-04-01` §5-3 과 같은 규칙이다 — `uq_handling_unit_content` 가 같은 LOT 두 행을 막는다.

```
CTN-A: FG-…-0311  80     +   CTN-B: FG-…-0311  60
                    ↓ 합병
CTN-C: FG-…-0311  140  (한 행)
```

⚠ **합쳤다고 말한다** — 조용히 합치면 **어느 포장에서 얼마가 왔는지**를 못 본다. 그리고 **그 정보가 이벤트 테이블이 없어 안 남는다**(§5-2).

### §5-6. 오프라인 — **판정값을 쓰지 않으므로 허용**

**C-14** 판정 기준을 적용한다.

| | |
| --- | --- |
| Lot Status 판정 | ⛔ **쓰지 않는다** — 포장 구성만 바꾼다 |
| 결론 | ✅ **오프라인 허용** — `M-04-04`(제품 입고)와 같은 갈래 |

⚠ **다만 POP 으로 넘기는 것이 온라인을 요구한다** — 오프라인에서 확정하면 **POP 대기 목록에 안 올라간다.** 동기화 후에 올라간다는 것을 화면이 적는다(C-7 미확정 표식).

### §5-7. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 원 포장 스캔 | 항상 — **D-2** |
| 직접 입력 | 항상 — **D-3 대체 경로 의무** |
| 포장 추가 스캔 | **합병 유형**일 때 — 여러 원 포장 |
| 재구성 유형 선택 | 원 포장 스캔됨 |
| 구성 편집 | 유형 선택됨 — **숫자 키패드**(D-4) |
| **재구성 확정** | 새 구성 유효 AND 수량 합 = 원 수량 합 |
| 라벨 발행 | ⛔ **여기서 하지 않는다** — `P-04-04`(§5-1) |
| 재구성 취소 | 확정 전 — 화면 이탈 |
| 확정 후 되돌리기 | ⛔ **두지 않는다** — 이벤트 이력이 없어 되돌릴 근거가 없다(§5-2) |

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 수량 합 불일치 | ⛔ 「원 포장 180 EA 와 새 구성 합이 다릅니다」 |
| **같은 LOT 이 만남**(합병) | ⚠ **합친다** + 「80 에 60 을 더해 140 이 됩니다」(§5-5) |
| 분할 잔량 0 | ⚠ **허용** — 전량을 새 포장으로 옮기는 것도 재구성이다 |
| 원 포장이 이미 출하 배분됨 | ⛔ 「이 포장은 출하에 배분됐습니다」 |
| 재구성 이력 조회 시도 | ⛔ **없다** + 사유 표시(§5-2) |
| **오프라인 확정** | ⚠ 「동기화 후 POP 대기 목록에 올라갑니다」(C-7) |
| 스캔 실패 | **직접 입력**으로 물러난다(D-3) |

## §7. DS 매핑

> ✅ **판정 절차 ① 수행** — 고정 커밋 `a554d11` `src/components/` 전수 조회(29개).

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 스캔 입력 | `TextField` + 포커스 관리 | **c** |
| 카메라 QR | Capacitor 네이티브 | **c** — 플랫폼 |
| 재구성 유형 선택 | **`Radio`** | a |
| 구성 편집 | `Table` + `TableColumn.render` | **c** |
| 수량 입력 | `TextField` + **숫자 키패드** | **d** — **G-6** |
| 원 포장·새 구성 카드 | `Card` | a |
| 결손 안내 | `AlertBanner`(warning) | a — G-1 |
| 확정 버튼 | `Button` — ⚠ **터치 타겟** | **b** — **G-5** |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ⛔ **재구성 이벤트 테이블 0건** — 결정 13 이 「이벤트 헤더–라인 계보」를 확정했는데 자리가 없다(§5-2) | 상류↔하류 불일치 | **조정** | **`[docs→데이터모델]` 이슈** · **A-11 로 구성만 바꾸고 이력 없음** |
| 2 | **모바일 → POP 중간 상태** — `handling_unit.status_code` 값으로 물러남 | 공통코드 | **조정** | `W-06-06` · 1번과 같은 뿌리 |
| 3 | **포장 유형·상태 값 목록** | 공통코드 | **조정** | `W-06-06` · #64 |
| 4 | **셸 간 레코드 공유 설계** — 2단말 스택 | 프로세스 | **표시** | 통합 IA §7-4 파생 확인 사항 |
| 5 | **G-5·G-6** | DS | **조정** | 모바일 확산 시 |

**차단 0건 — 이 화면은 지금 지어도 된다.** 단 **이력 조회는 그리지 않는다**(A-11).

## §9. 이 장에서 나온 공유 계약 후보

1. ⭐ **확정된 「이벤트」에 테이블이 없으면 화면은 상태만 바꾸고 이력을 그리지 않는다** — 결정 13 이 「이벤트 헤더–라인 계보」를 확정했는데 실측 0건이다. **이력 화면을 그려 두고 비워 두면** 사용자는 「기록이 안 됐다」가 아니라 「아직 안 했다」로 읽는다. A-11 의 **이력 판** (§5-2)
2. **2단말 스택은 중간 상태를 어디에 두는지를 스펙이 정한다** — 모바일이 확정하고 POP 이 발행하는데, **그 사이 상태를 담을 곳**이 정해지지 않으면 두 화면이 서로를 못 찾는다. 이벤트 테이블이 없어 **`status_code` 값으로 물러났고**, 그 사실을 양쪽 스펙에 적는다 (§5-1)
3. **오프라인 허용 화면이라도 「다음 단말로 넘기는 것」은 온라인을 요구한다** — C-14 로 판정하면 이 화면은 오프라인 허용인데, **POP 대기 목록에 올리는 것**은 동기화 뒤다. **「저장은 됐지만 다음 단계로 안 넘어갔다」**를 화면이 말해야 작업자가 POP 앞에서 헤매지 않는다 (§5-6)

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-07 | 초안 — 확대 9차 5/6. ⭐ **04 유일의 2단말 스택** — 모바일 스캔 → POP 발번·인쇄. ⛔ **재구성 이벤트 테이블이 0건**이다(결정 13 이 「이벤트 헤더–라인 계보」를 확정했는데) — **포장 구성만 바꾸고 이력은 남기지 않는다**(A-11). ⭐ **재구성은 LOT 을 가르지 않는다** — `handling_unit_content` 만 바뀌고 `lot_relation` 이 안 생긴다. **모델의 컬럼이 「LOT 이 갈리는가」를 이미 말한다**(`P-04-03` §5-4 와 같은 형태). ⭐ **분할 잔량은 원 번호를 유지**(결정 13 절충안) — 안 적으면 작업자가 잔량 라벨도 새로 뽑으려 한다. **C-14 로 오프라인 허용**이나 **POP 으로 넘기는 것은 동기화 뒤**다. 미결 5건(**차단 0**) · 계약 후보 3건 · 신규 DS 갭 0건. |
