# W-05-12 · 설비·설비그룹 마스터

> 작성일: 2026-08-10 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 11차 1/3 — 도메인 05 설비·툴 관리의 첫 화면.** DR-001 확정(설비·툴 19장 착수)으로 동결이 풀린 뒤 처음 쓰는 장이며, DR-001 §7 이 정한 진행 순서 **1단계(마스터 3장)**의 첫 번째다.
> ⭐ **이 장은 「데이터 모델이 설계를 따라온다」는 원칙 1)이 실제로 적용되는 첫 사례다.** 같은 상황을 다룬 `W-06-08`(예비품 마스터, 2026-08-03)은 **「없으니 이슈로 넘긴다」**로 끝냈으나, 이 장은 **「없으니 우리가 정의한다」**로 간다. 차이는 §3-3에 적었다.

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-05-12` · 설비·설비그룹 마스터 |
| 프로그램 · 도메인 | **관리웹** · 05 설비툴관리 |
| 유형 | 설정·마스터 |
| 행위자 | 설비담당 · 전산담당 |
| 신뢰도 | 추정 (근거 등급 4 — 엔티티 실재) |
| IA 위치 | 설비/툴 > 설비 보전 > 설비·설비그룹 마스터 |

## §2. 근거 4면

| 면 | 근거 | 강도 |
| --- | --- | :-: |
| **프로세스** | **WF05 S1**(설비·툴 자산 등록 / 기준정보 설정) — Actor 「설비담당 (+전산담당: 설비(기계)의 Legacy 연계 여부는 미확정)」 · Action 「설비/툴 자산을 마스터에 등록(설비코드/툴코드, **설비그룹**, 호기, Set·Cavity), **점검/보전 항목·주기** 정의」 · Result 「(신규) **설비 위치(레이아웃) 속성**」 · Rule 「점검·보전·사용실적·PM은 **이 기준정보가 있어야** 트리거·판정 가능」<br>**도식스펙-05 §0 특칙 3** — 「S1(자산 등록·기준정보)·S9(폐기)는 **도식 범위 외** — 기준정보는 Wallet(목록) 참조로만 등장」 | 실측 |
| **화면·IA** | `deliverables/04-통합-IA.md` §3 `W-05-12` 행 · 인벤토리 v1.6 — 「WF05 S1 §0 특칙3(도식 범위 외·Wallet 참조)·데이터모델 §1 설비·설비그룹·점검/보전 항목·주기·REQ-PR-0036 위치」 | 실측 |
| **요구** | ✓확정 **QA #10** — 설비 보전 = **MES 경량 보유**, 범위 = 점검(Daily/Monthly) + 고장 수집 + 보전 계획·지시·실적. 「**공백 ≠ 불요**」<br>✓확정 **QA #9** — 점검 통제 = 차단/경고/미적용 **3단계 정책 옵션(설정형)**<br>✓설계확정 **결정 03** — WorkCalendar 적용 = 계층형. 「**공장 기본 1건 필수 + 설비그룹 단위 override(선택)**. 해석 규칙: 설비 → 소속 설비그룹 → 공장 순으로 **가장 가까운 캘린더 적용**」<br>✅ **REQ-PR-0036** — 「대시보드 - 일일생산실적 및 **알람발생 설비 위치 표시**」 → **DR-004 확정(2026-08-10)**: 알람 위치는 **계층 텍스트**로 보인다. 평면 배치도는 **창고 적재 위치 확인용**이지 설비 알람용이 아니다 | 실측 |
| **데이터** | **일부 실재.** `mdm.equipment`(SQL 456) · `mdm.production_line`(SQL 164) 실재 · **점검·보전 항목·주기 테이블 없음**(검색 0건) · 「설비그룹」이라는 이름의 테이블 없음 → **§3에서 4단 확인** | 실측 |

## §3. 「설비그룹이 없다」는 틀렸다 — A-13 4단을 돌린 결과

공유계약 **A-13**은 「없다」고 적기 전에 ① 한 층 아래 ② 다른 파일 ③ 다른 스키마 ④ **다른 이름**을 보라고 정한다. 넷째에서 걸렸다.

| 단 | 확인한 것 | 결과 |
| :-: | --- | :-: |
| ① 한 층 아래 | `mdm.equipment` 안에 그룹 컬럼이 있나 | 없음 (`equipment_type_code`·`production_line_id` 만 있음) |
| ② 다른 파일 | 물리 모델이 여러 파일인가 | 단일 파일 |
| ③ 다른 스키마 | `mdm` 밖에 있나 | 없음 |
| ④ **다른 이름** | **`mdm.production_line` 이 그것이다** | ⭐ **걸렸다** |

### §3-1. 형태가 같다

```
개념모델 §1 「설비그룹」          물리모델 mdm.production_line (SQL 164)
─────────────────────────       ──────────────────────────────────────
설비그룹코드 (키)          ↔     line_code   + UNIQUE (plant_id, line_code)
그룹명                    ↔     line_name
상위그룹                  ↔     parent_line_id  (자기참조 FK)
설비그룹-설비 1:N          ↔     equipment.production_line_id  (SQL 463)
                          +     line_type_code = LINE | WORK_AREA
```

**상류와 하류가 같은 결론에 이름만 다르게 도달했다.**

- **상류(결정 03)**: 「라인 개념 필요 시 **별도 라인 마스터 대신 설비그룹 유형에 '라인' 추가로 흡수**」
- **하류(H-4)**: 라인 마스터를 만들고 **작업구역을 `line_type_code` 로 흡수**했다

둘 다 **「계층형 테이블 하나 + 유형 코드」**다. 담는 그릇이 같고 부르는 이름이 다르다.

### §3-2. 그래서 화면은 새 테이블을 요구하지 않는다

**이 화면이 다루는 「설비 그룹」의 저장처 = `mdm.production_line`.** `line_type_code` 가 그 층이 무엇인지 말한다(라인 · 작업구역 · 그 밖의 분류축).

⚠ **필요한 것은 테이블이 아니라 값 목록 하나**다 — `line_type_code` 에 분류축을 추가할 것인지. 이는 공통코드이므로 **웹-공통코드조직작업자-마스터(`W-06-06`)** 소관이다(§8-3).

### §3-3. ⭐ 같은 상황을 다룬 앞 장과 이 장이 다른 점

**웹-예비품-마스터(`W-06-08`, 2026-08-03)**도 「데이터 면이 비어 있다」를 만났고, 그때는 **§4를 「신설 요청 명세」로 남기고 이슈로 넘겼다.** 지금은 다르다.

| | `W-06-08` (2026-08-03) | **이 장 (2026-08-10)** |
| --- | --- | --- |
| 근거 | 물리 모델이 정본 | **원칙 1) — 데이터 모델이 UI/UX 결정을 따라온다** |
| 없는 항목을 어떻게 | **요청**으로 적고 대기 | **정의**하고 넘긴다 |
| 넘기는 시점 | 즉시 이슈 | **17장을 다 쓴 뒤**(DR-001 §5-2) |

**§4-C(점검·보전 항목·주기)가 그 차이가 드러나는 자리다.** 「지어내지 않는다」(서식 원칙 2)는 여전히 지킨다 — **근거 없이 필드를 늘리지 않되, 근거가 있으면 모델에 자리가 없어도 정의한다.**

### §3-4. 「설비 위치」도 새 컬럼이 아니다

REQ-PR-0036 이 요구한 「알람발생 설비 위치 표시」는 **DR-004 확정으로 계층 텍스트가 됐다.** 계층 텍스트의 재료가 이미 다 있다.

```
공장(mdm.plant) > 상위 그룹(production_line) > 하위 그룹(production_line) > 설비(equipment)
```

⛔ **설비 마스터에 좌표·평면 위치 필드를 두지 않는다.** DR-004 가 「알람발생 설비 위치 표시에서 평면 배치 불필요」로 확정했다.

⚠ **`mdm.location` 을 쓸 수 없다** — `warehouse_id NOT NULL`(SQL 335)이라 **창고에 소속된 위치**만 담는다. 설비는 창고에 있지 않다.

## §4. 필드

감사 컬럼과 `version_no`는 표에서 제외(공유계약 A-4).

### §4-A. 설비 그룹 `mdm.production_line`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 공장 / *(미정)* | `plant_id` | FK → `mdm.plant` | ✅ | 유일키 구성 | 선택 목록 | 신규만 |
| 그룹코드 / *(미정)* | `line_code` | `code_t` varchar(50) | ✅ | ✅ **(공장 내)** | 「이 공장에 이미 있는 그룹코드입니다」 — 유일 범위를 문구에(A-1) | ⚠ B-4 |
| 그룹명 / *(미정)* | `line_name` | `name_t` varchar(200) | ✅ | | 공백만 불가 | ✅ |
| 그룹유형 / *(미정)* | `line_type_code` | `code_t` 기본 `'LINE'` | ✅ | | **값 목록 미정** → G-2 비활성 + 사유 §8-3 | ✅ |
| 상위그룹 / *(미정)* | `parent_line_id` | FK → `mdm.production_line` (자기참조) | — | | `ck_production_line_parent` — **자기 자신만 금지.** ⚠ **순환(A→B→A)은 DB가 막지 않는다** → 화면·서버 책임(A-9 등급 2) §8-4 | ✅ |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | B-4 물리 삭제 금지 | ✅ |

### §4-B. 설비 `mdm.equipment`

| 라벨(한 / 베) | 출처 컬럼 | 타입 | 필수 | 유일 | 검증·비고 | 편집 |
| --- | --- | --- | :-: | :-: | --- | --- |
| 공장 / *(미정)* | `plant_id` | FK → `mdm.plant` | ✅ | 유일키 구성 | 좌측 선택 그룹의 공장으로 고정 | 신규만 |
| 설비코드 / *(미정)* | `equipment_code` | `code_t` varchar(50) | ✅ | ✅ **(공장 내)** | 「이 공장에 이미 있는 설비코드입니다」(A-1) | ⚠ B-4 |
| 설비명 / *(미정)* | `equipment_name` | `name_t` varchar(200) | ✅ | | 공백만 불가 | ✅ |
| 설비유형 / *(미정)* | `equipment_type_code` | `code_t` varchar(50) | ✅ | | ⚠ **같은 컬럼이 계측기도 가른다**(`W-05-11` §3-2)<br>⭐ **그룹은 계열마다 다르다**(2026-08-24 `#219`) — 이 화면은 **`EQUIPMENT_TYPE`**(설비 계열), 계측기 화면은 **`INSTRUMENT_TYPE`**. ⛔ **한 그룹으로 묶지 않는다** — 묶으면 이 화면의 선택칸에 캘리퍼스가 뜬다(`G-32`)<br>⭐⭐ **값 확정 2026-08-25**(팀 리더) — **`INJECTION_MOLDING`(사출기) · `PRESS`(프레스) · `WATER_HEATER`(온수기)** 3값. **더는 `G-2` 자리표시가 아니다.** 근거·제외 판단은 `uiux/2026-08-25-설비유형-제안안/00-제안안.md` | ✅ |
| 소속그룹 / *(미정)* | `production_line_id` | FK → `mdm.production_line` | — | | 선택 목록(같은 공장 내) · **비어 있으면 계층 텍스트가 공장까지만 나온다** §5-3 | ✅ |
| 소속공정 / *(미정)* | `process_id` | FK → `mdm.process` | — | | 선택 목록 | ✅ |
| 운용상태 / *(미정)* | `status_code` | `code_t` varchar(50) | ✅ | | **2값으로 정한다 — §5-2(A-14)** | ✅ |
| 검교정 대상 / *(미정)* | `calibration_required` | boolean 기본 `false` | ✅ | | 켜면 검교정 유효 게이트의 판정 대상이 된다(`W-05-11` §5-2) | ✅ |
| 최근 검교정일 / *(미정)* | `last_calibration_date` | date | — | | ⛔ **읽기 전용** — 웹-계측기검교정이력-등록(`W-05-10`)이 정한다(B-13) · G-10 | ⛔ |
| 차기 검교정 예정일 / *(미정)* | `calibration_due_date` | date | — | | ⛔ **읽기 전용** — 동상(B-13) · G-10 | ⛔ |
| 사용 / *(미정)* | `is_active` | boolean 기본 `true` | ✅ | | B-4 | ✅ |

**⚠ `equipment_code` 는 「호기」를 따로 담지 않는다.** WF05 S1 Action 이 「설비코드, 설비그룹, **호기**」를 나란히 적었으나 물리 모델에 호기 컬럼이 없다. **설비코드가 호기를 포함하는 채번 규약으로 흡수**한다(예: `PRS-01`) — 별도 컬럼을 요구하지 않는다. 근거: 호기는 같은 기종을 구분하는 일련번호이고, 그 구분은 **공장 내 유일한 설비코드가 이미 보장**한다. → §8-5

### §4-C. 점검·보전 항목과 주기 — **저장처가 없다 · 정의한다**

⛔ **물리 모델에 자리가 없다**(검색 0건 — 점검·보전·`maint`·`inspection_item` 중 설비용 없음). **원칙 1)에 따라 화면이 정의하고 모델이 따라온다**(§3-3).

#### C-1. 점검·보전 항목 마스터 — 근거와 강도

| 항목 | 근거 | 강도 |
| --- | --- | :-: |
| 항목코드 · 항목명(한/베) | 마스터의 최소 구성 · WF05 S1 Action 「점검/보전 **항목**·주기 정의」 · ✓확정 QA #33 다국어 전범위 | 실측(요구) |
| **점검 유형** — 일상(Daily) / 정기(Monthly) / 보전 | ✓확정 **QA #10** 범위 문구가 「점검(**Daily/Monthly**)」으로 못박음 · WF05 S2 Result 「점검유형 Daily/Monthly」<br>⭐ **코드 확정 2026-08-21**(`#186`) — `EQUIPMENT_INSPECTION_TYPE` = `DAILY`·`MONTHLY`·`MAINTENANCE`. ⚠ **품질 검사의 `inspectionTypeCode`(IQC·PQC·OQC)와 「같은 이름 다른 값」**이라 그룹을 가른다. ⭐ **보전이 셋째인 근거** — `W-05-05`(보전지시발행) §4 가 이 마스터를 FK 로 참조한다 | 실측 |
| **판정 방식** — 육안(합/NG) / 측정값 | WF05 S2 Action 「항목별 점검 수행(점검부위·기준 대비 **측정/육안**) → **합/NG 판정**」<br>⭐ **코드 확정 2026-08-21**(`#186`) — `EQUIPMENT_INSPECTION_JUDGMENT_METHOD` = `VISUAL`·`MEASUREMENT`. ⛔ **이 값을 모르면 아래 짝 제약을 화면이 걸 수 없다** — 등록·수정이 반드시 실패하는 경로가 된다(구현팀 실측) | 실측 |
| 측정 단위 · 하한 · 상한 | 판정 방식이 「측정값」일 때의 짝 · **`quality.inspection_item_spec` 과 동형**(`uom_id`·`lower_limit`·`upper_limit`·`ck_inspection_limits` — SQL 1784) | 실측(동형) |
| 필수 여부 | 동형(`required_flag`) | 실측(동형) |
| 점검부위 | WF05 S2 Input 「점검 항목·**점검부위**·기준」 | 실측 |
| 표시 순서 | 점검표는 순서대로 도는 것이 통상 · 동형(`sequence_no`) | `[추정]` — 검증: **현행 일상점검표 실물 수집**(WF05 S2 [Check]) |

⛔ **이 칸에 유일 제약을 두지 않는다** **[2026-08-24 확정 · 구현팀 확인 `#222`]**

**모양을 빌린 `quality.inspection_item_spec.sequence_no` 에는 유일 제약이 있고 그래서 `A-5`(순서 재배치는 서버가 한 트랜잭션으로) 대상 2곳 중 하나다.** 위 표가 「동형」이라고만 적어 두어 **제약까지 따라오는지가 열려 있었다.** 따라오지 않는다.

| | |
| --- | --- |
| **왜 안 거나** | 표시 순서는 **「순서대로 도는」 용도**다. 두 항목이 같은 번호를 가져도 점검이 틀리지 않는다. ⚠ 그리고 **이 칸이 필요한지 자체가 아직 미결이다**(§8-8) — 쓸지도 모르는 칸에 제약부터 걸지 않는다(서식 원칙 2) |
| **그래서 무엇이 달라지나** | **재배치가 행마다 개별 갱신으로 성립한다.** 유일 제약이 있으면 10↔20 을 맞바꾸는 **중간 상태가 반드시 제약을 위반**해 서버 일괄 경로가 따로 필요하다(`A-5`) |
| **선례** | `mdm.code_value.display_order` 가 같은 형태다 — `A-5` 2026-08-03 정정 ①이 **바로 이 이유로** 대상에서 뺐다 |

⚠ **이름은 `sequence_no` 다** — `display_order` 가 아니다(`#222` 에서 우리 요구서 오기가 드러났다). **`G-7`(행 순서 재배치 UI)과 `A-5`(서버 일괄 트랜잭션)는 다른 집합**이고, 이 화면은 **G-7 이되 A-5 는 아니다.**

⭐ **`quality.inspection_item_spec` 을 그대로 쓰지 않는 이유** — 형태는 같지만 **셋이 다르다**.

| | 품질 검사 항목 | **설비 점검 항목** |
| --- | --- | --- |
| 소유 | 품질담당 | **설비담당** |
| 대상 | 자재·제품 LOT | **설비** |
| 게이트 | 검사 판정 → LOT 상태 | **작업 시작 통제**(QA #9) |

**WF05 의 핵심 전제가 「점검 ≠ 보전」과 함께 「점검(설비) ≠ 검사(품질)」를 세운다.** 인벤토리 §5 미결 4도 같은 근거로 「점검≠검사(품질)」를 적었다. **모양을 빌리되 테이블은 나눈다.**

#### C-2. 부여 — 어느 설비가 어느 항목을 언제 도는가

공유계약 **B-6**(마스터 테이블과 부여 테이블을 구분한다)을 적용한다. 부여가 갖는 것:

| 항목 | 근거 | 강도 |
| --- | --- | :-: |
| 부여 대상 — **설비 그룹** 또는 **설비** | 아래 해석 규칙 | 실측(결정 03 동형) |
| 점검 항목 FK | C-1 | — |
| **주기** — 유형 + 간격 + 기준일 | WF05 S1 Action 「점검/보전 항목·**주기** 정의」 · S2 Trigger 「점검 **주기 도래**(일상=Daily, 정기=Monthly)」<br>⭐ **유형 코드 확정 2026-08-22**(`#188`) — `CYCLE_TYPE` = `DAY`·`WEEK`·`MONTH`·`YEAR`. ⚠ **검교정 주기(`W-05-11`)와 「같은 그룹」이다** — 둘 다 기간 단위다(`G-32`). ⚠ 여기 적혀 있던 「일/주/월」은 확정이 아니라 예시였다 | 실측 |
| 사용 여부 | B-4 | 확정 |

⭐ **해석 규칙은 새로 만들지 않는다 — 결정 03 이 이미 정한 것을 그대로 쓴다.**

> **결정 03(WorkCalendar)**: 「해석 규칙: **설비 → 소속 설비그룹 → 공장** 순으로 **가장 가까운** 캘린더 적용」

점검 항목 부여도 같은 규칙을 쓴다 — **설비에 부여가 있으면 그것, 없으면 소속 그룹의 것, 둘 다 없으면 점검 대상 아님.**

**왜 같은 규칙을 쓰는가**: 같은 화면군에서 **캘린더는 「설비→그룹→공장」, 점검 항목은 다른 순서**로 돌면 설비담당이 두 규칙을 외워야 한다. 한 규칙이면 「가장 가까운 것이 이긴다」 한 문장으로 끝난다. → §9-2

⚠ **공장 층은 두지 않는다.** 캘린더는 공장 기본 1건이 필수지만(가동일은 공장 전체가 공유한다), **점검 항목은 설비 종류마다 다르므로 공장 단위 기본값이 성립하지 않는다.** 2층(설비 → 그룹)으로 둔다.

## §5. 액션·상태

### §5-1. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 그룹 추가 · 설비 추가 | 편집 권한 |
| 저장 / 취소 | 변경분 존재 · 필수 충족 |
| **사용 중지** | `is_active=true` · **물리 삭제 버튼을 두지 않는다**(B-4) |
| **점검 항목 추가** | 편집 권한 — ⭐ **이 화면이 마스터를 만든다** §5-1-1 |
| **점검 항목 저장 / 취소** | 변경분 존재 · 필수 충족 · ⛔ **짝 제약** — 판정 방식이 「측정값」이면 단위·상하한이 함께 필요(§4-C-1) |
| **점검 항목 사용 중지** | `is_active=true` · 물리 삭제 없음(B-4) |
| **점검 항목 순서 재배치** | 목록 2건 이상 — **`sequence_no`**(§4-C-1 표시 순서 · G-7) · ⭐ **행마다 개별 갱신**(§4-C-1 주 · A-5 대상 아님) |
| 점검 항목 부여 편집 | 그룹 또는 설비 선택 상태 |
| 변경 이력 | 항상 — `audit.audit_event`(`target_type_code`+`target_id`, B-5) · 기간 필수(파티션) |
| 계층 텍스트 확인 | 설비 선택 시 §5-3 |

#### §5-1-1. ⭐⭐ 점검 항목 마스터를 **만드는 자리가 이 화면이다** **[2026-08-24 `#220` 확정 — 표를 덜 적었던 것을 고친다]**

**부여만 적혀 있고 「항목 자체를 만드는 액션」이 빠져 있었다.** 마스터의 정의는 §4-C-1 이 이미 갖고 있었고 DS 매핑(§7)도 「점검 항목 목록」과 「순서 재배치」를 이 화면에 걸어 두었는데, **이 표만 부여에서 멈춰 있었다.** 계약도 네 경로를 이 화면 몫으로 판정해 두었다(`06-API-요구서-05설비툴.md` §4).

⭐ **자리는 부여 창 «안»이 아니라 그룹·설비와 «나란한 셋째 목록»이다.**

```
W-05-12
   ├── 설비 그룹        (목록 + 편집)
   ├── 설비             (목록 + 편집)
   └── 점검 항목        (목록 + 편집)   ← 마스터. 여기서 만든다
          ↑
       부여 창은 이 목록에서 «고르기»만 한다
```

⛔ **부여 창 안에 「새 항목 만들기」를 두지 않는다.** 창 안에 창이 생기고, 만들다 만 항목이 부여와 한 트랜잭션에 얽힌다. **만드는 것과 고르는 것을 나눈다**(B-6 — 마스터 테이블과 부여 테이블을 구분한다 · 그 화면 판).

⚠ **부여 창이 빈 목록을 만나면 「어디서 만드는지」를 함께 말한다** — 「등록된 점검 항목이 없습니다」에서 멈추지 않고 **점검 항목 목록으로 가는 길**을 붙인다(`G-3` — 「어떻게 풀 것인가」를 함께 적는다).

⛔ **엑셀 업로드를 두지 않는다.** 다른 마스터(검사기준·예비품·툴)와 다른 판단이며 근거가 있다 — **WF05 S1 Medium 이 설비(기계) 대장의 매체를 🟡 미검증으로 남겼다**(「엑셀 설비대장+종이 사양서 **추정**, **매체 미검증**」). 마이그레이션 원천이 무엇인지 모르는 상태에서 업로드 양식을 만들면 두 번 만든다. 툴(`W-05-13`)은 「엑셀 관리·ERP 미사용」이 **확정**(REQ-PR-0003 현행)이라 반대다. → §8-6

### §5-2. ⭐ 운용 상태를 2값으로 정한다 — A-14 적용

`status_code` 는 `NOT NULL` 인데 값 목록이 없다. 늘리기 쉬운 자리다 — 「가동중 / 정지 / 고장 / 보전중 / 비가동 / 폐기」로 벌어질 수 있다.

공유계약 **A-14**가 이것을 막는다.

> **A-14**: 같은 상태에 이르는 길이 여럿이면 **상태 값을 늘리지 말고 조건을 따로 관리**한다. **값 목록이 자꾸 늘면 조건을 값에 밀어 넣고 있는지 의심한다.**

**확정** — 마스터가 갖는 것은 **자산 수명주기 축**뿐이다.

| 값 | 뜻 | 근거 |
| --- | --- | --- |
| **운용** | 자산으로 살아 있다 | WF05 S1 Result 「**상태**: 자산 '가동(운용)'」 |
| **폐기** | 자산 처분됨 | WF05 S9 Result 「**상태변화**: 자산 '운용→폐기'」 |

**「지금 쓸 수 있는가」는 마스터에 적지 않는다. 판정한다.**

```
사용 가능 = is_active
          AND status_code = 운용
          AND (검교정 대상이면 → 검교정 유효기간 내)
          AND 열린 고장·보전 건이 없다
```

**왜 이렇게 하는가** — 「고장」·「보전중」·「비가동」은 **트랜잭션이 만드는 조건**이고 각각 주인이 있다.

| 조건 | 만드는 곳 |
| --- | --- |
| 고장 | 모바일-설비고장-현장보고(`M-05-02`) → 웹-설비고장-상세처리(`W-05-04`) |
| 보전중 | 웹-보전지시-발행(`W-05-05`) → 웹-보전실적-예비품출고등록(`W-05-06`) |
| 비가동 | POP-비가동실적-입력(`P-05-02`) → 웹-비가동집계-조회(`W-05-08`) |
| 검교정 만료 | 웹-계측기검교정이력-등록(`W-05-10`) |

⛔ **이 넷을 마스터 상태로 올리면 네 화면이 같은 컬럼 하나를 두고 경합한다** — 공유계약 **B-13**(한 컬럼을 여러 경로가 바꾸면 「누가 정하고 누가 반영하는가」를 가른다)이 정면으로 걸린다. **상태를 2값으로 묶으면 경합 자체가 생기지 않는다.**

⚠ **이 판단은 이 화면 하나의 것이 아니다** — 툴 마스터(`W-05-13`)·계측기(`W-05-11`)에 같은 형태가 있다. → §9-1

### §5-3. 계층 텍스트를 이 화면이 보인다

DR-004 확정으로 설비 위치 = 계층 텍스트다. **그 텍스트가 맞게 나오는지 확인하는 자리가 여기**다 — 통합 대시보드(`W-CO-05`)가 그리는 문자열의 재료를 만드는 곳이기 때문이다.

```
설비 선택 시 상단에 표시
   호치민공장 > 프레스라인 A > 1구역 > 프레스 1호기
```

⚠ **소속 그룹이 비어 있으면 공장까지만 나온다.** 그 상태를 감추지 않는다 — 공유계약 **G-9**(모르는 값과 없는 값을 같은 모양으로 그리지 않는다)에 따라 **「소속 그룹 없음」을 명시**한다. 알람 화면에서 위치가 「호치민공장」으로만 나오면 찾아갈 수 없으므로, **여기서 비어 있음이 보여야 채운다.** → G-12(통제의 근거가 되는 마스터는 「비어 있음」을 함께 보인다)와 같은 취지.

### §5-4. 화면 상태

`초기(EmptyState)` → `조회` → `편집` → `저장 중` → `성공(Toast)` / `실패(인라인·배너)` / `충돌(B-1)`

**낙관적 잠금**: `production_line`·`equipment` 둘 다 `version_no` 실재 → **B-1 적용**. 저장 충돌과 상태 잠김은 배너 위치가 같고 문구·다음 행동이 다르다(G-1).

**B-4 참조 건수 — 둘 다 산출 가능하다**(전부 FK · 실측)

| 마스터 | 참조처 | 곳 |
| --- | --- | :-: |
| `mdm.equipment` | `work_order.planned_equipment_id`(796) · `work_session.equipment_id`(852) · `production_result.equipment_id`(1645) · `inspection_item_spec.default_inspection_equipment_id`(1799) · `inspection_measurement.inspection_equipment_id`(1882) · `defect_record.equipment_id`(1942) · `equipment_calibration.equipment_id`(2063) | **7** |
| `mdm.production_line` | `production_line.parent_line_id`(167 · 자기참조) · `equipment.production_line_id`(463) · `production_order.planned_line_id`(760) · `work_order.production_line_id`(790) | **4** |

→ **B-4 「참조 0일 때만 코드 수정」을 그대로 적용**한다. B-4 §「참조 건수를 셀 수 없는 대상」에 해당하지 않는다.

⚠ **상세 조회 응답에 참조 건수(또는 수정 가능 여부 + 사유)가 포함돼야 한다**(B-4 API 파급). 화면이 따로 세면 화면마다 달라진다.

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| 권한 없음 | 편집 컨트롤 비활성 + 사유 — 「관리자에게 권한을 요청하세요」(G-3) |
| 유일 제약 위반 | 인라인 · **유일 범위를 문구에**(A-1) — 「이 공장에 이미 있는 설비코드입니다」 |
| 참조가 있어 코드 수정 불가 | 코드 입력란 비활성 + 「이 설비를 참조하는 작업지시·검사 기록이 N건 있어 코드를 바꿀 수 없습니다. 사용 중지하고 새로 만드세요」(B-4·G-3) |
| **상위 그룹 순환** | 저장 거부 + 인라인 — 「이 그룹을 상위로 지정하면 순환이 생깁니다」. ⚠ **DB가 막지 않는다**(A-9 등급 2) §8-4 |
| 참조 중인 그룹 사용 중지 | 확인 다이얼로그 + 소속 설비 건수 — 「이 그룹에 설비 N대가 소속돼 있습니다」 |
| 저장 충돌 | 배너 + 「다시 불러오기」(B-1) |
| 폐기된 설비 편집 | 배너 + **상태 잠김** 문구 — 다시 불러와도 풀리지 않음을 명시(G-1 단서) |
| 그룹유형 값 목록 비어 있음 | 필드를 감추지 않고 **비활성 + 사유**(G-2) |
| 오프라인 | **해당 없음** — 관리웹은 온라인 전제(결정 17 · C-5) |

## §7. DS 매핑 (`@crefle/web-ui` · 고정 커밋 `a554d11`)

**판정 절차 ①을 명령으로 수행했다**(ds-gap §판정 절차) — `git -C $DS ls-tree --name-only a554d11 src/components/` 실행. 실재 컴포넌트 **29종** 확인.

| 요소 | 유형 | 컴포넌트 |
| --- | :-: | --- |
| 셸·헤더·탐색 | **a** | `AppShell` · `PageHeader` · `Breadcrumb` · `Navigation` |
| 그룹 목록 · 설비 목록 · 점검 항목 목록 | **a** | `Table` |
| 입력·선택·토글 | **a** | `TextField` · `Select` · `Switch` · `Checkbox` |
| 상태 표시 | **a** | `Chip`(운용/폐기) · `AlertBanner` · `Toast` · `EmptyState` · `Skeleton` |
| 확인·이력 | **a** | `Dialog` + **`DiffRow`**(변경 이력 diff — G-3 실재) |
| 검색·필터 바 | **c** | `SearchInput` + `Select` + `Chip` (G-2 · 기등재) |
| 읽기 전용 + 사유 | **c** | `TextField(disabled)` + `AlertBanner` (G-10 · **9곳째**) |
| 페이지 이동 | **c** | `Button` + `Select` + 상태 (G-12) |
| 날짜 입력(주기 기준일) | **c** | `TextField type="date"` (G-8) |
| 점검 항목 순서 재배치 | **a** | `Table` 의 `reorderable`·`onRowReorder` (G-7 · **7곳째**) |

### §7-1. 그룹 계층은 **갭이 아니다** — 2단이기 때문

**`parent_line_id` 는 무제한 자기참조**라 다단 계층이 가능해 보인다. 그러나 **확정된 `line_type_code` 값은 둘(LINE · WORK_AREA)**이므로 실제 계층은 **2단**이다.

ds-gap **G-1** 이 이 경우를 이미 정해 뒀다.

> **G-1 ⚠ 선행 확인**: 실제 계층이 **2단 이내면 갭 불성립**(`W-06-07` §8-5 · `W-06-06` §8-6)

→ **`Table` + 들여쓰기로 성립(c).** G-1 사용처에 추가하지 않는다.

⚠ **`line_type_code` 값 목록이 3단 이상으로 확정되면 재판정**한다(§8-3).

**신규 갭 0건 — 11차 연속.**

## §8. 미결

| # | 항목 | 성격 | 처리 |
| :-: | --- | --- | --- |
| **1** | **「설비그룹」과 `production_line` 의 이름이 다르다** — 상류(개념모델 §1·결정 03)는 설비그룹, 하류(H-4)는 생산라인. **형태는 같다**(§3-1) | **설계 결정** | ✅ **정했다** — 화면은 `mdm.production_line` 을 「설비 그룹」으로 다룬다. **테이블 신설을 요구하지 않는다.** 17장 완료 후 **#67 회신에 명칭 정리를 포함**한다 |
| **2** | **점검·보전 항목 마스터와 부여 테이블이 없다** — 검색 0건 | **설계 결정**(구 상류↔하류 불일치) | ✅ **정의했다**(§4-C) — 항목 마스터 + 부여(2층 해석) 2종. ⚠ **원칙 1) 적용 — 모델이 따라온다.** 넘기는 시점은 **17장 완료 후**(DR-001 §5-2) |
| **3** | `line_type_code` **값 목록 미정** · ~~`equipment_type_code`(설비 계열)~~ ✅ **해소 2026-08-25** · ~~`status_code`~~ ✅ **해소 2026-08-21** | **공통코드** | `W-06-06` 소관. 화면은 **G-2**(비활성 + 사유)로 만든다.<br>⭐ **`status_code` 는 확정했다**(`#185`) — `EQUIPMENT_STATUS` = **`IN_SERVICE`**(운용)·**`DISPOSED`**(폐기). ⛔ **`ACTIVE` 를 쓰지 않는다** — 사용 여부(`is_active`)와 **다른 축**인데 같은 낱말을 쓰면 혼동한다. ⚠ **여기 「등록 요청한다」고 적어 놓고 넉 달을 안 했다** — 구현팀이 실제로 쓰려다 막혀서 드러났다. **뜻을 정한 것과 식별자를 만든 것은 다른 산출물이다**(공유계약 `G-32`)<br>✅ **`equipment_type_code` 값이 섰다**(2026-08-25 팀 리더 확정 — `INJECTION_MOLDING`·`PRESS`·`WATER_HEATER`). 이 화면도 **자기 그룹 값 전체를 목록 조건에 실어** 계측기를 걸러 낸다 — `#219` 의 거울상 결함이 닫힌다. ⚠ **구 서술**: 값이 설 때까지 이 화면은 계측기까지 함께 보였다. 계측기 쪽은 `INSTRUMENT_TYPE` 3값이 확정돼 조건을 실을 수 있으나(`W-05-11`), 설비 계열은 실을 값이 없다. **감추지 말고 밝힌다**(`G-2`) · 값이 서면 같은 방법으로 풀린다 — 후속 제안안 필요 |
| **4** | **상위 그룹 순환을 DB가 막지 않는다** — `ck_production_line_parent` 는 **직계 자기참조만** 막는다(SQL 178) | **설계 결정** | A-9 **등급 2**(DB가 안 막으니 화면·서버가 진다). ⚠ `mdm.department` 도 **같은 형태**(`W-06-06` §8-6) — **두 곳이 같은 문제** → 서버 공통 검증으로 한 번에 |
| **5** | **「호기」를 담을 컬럼이 없다** — WF05 S1 Action 명시 항목 | **설계 결정** | ✅ **정했다** — **설비코드 채번 규약으로 흡수**(§4-B 주). 별도 컬럼을 요구하지 않는다. ⚠ 채번 규약 자체는 `app.numbering_rule`(SQL 2576) 소관 |
| **6** | **설비(기계) 대장의 매체가 미검증** — WF05 S1 Medium 🟡 「엑셀 설비대장+종이 사양서 **추정**」 | **수집 대기** | 엑셀 업로드를 **탑재하지 않는 근거**(§5-1). 실물이 확인되면 툴 마스터(`W-05-13`)와 같은 형태로 추가한다 |
| **7** | **작업 통제 3단계(QA #9)의 저장처 — `app.operation_policy`(SQL 2541)에 설비·설비그룹 축이 없다** | **설계 결정** | ⚠ **저장처가 있다는 것은 새 발견이 아니다** — **확대 6차 완료보고**(2026-08-06)가 「모델이 앞선 것」 2번으로 이미 적었다(「`app.operation_policy` 가 3단계 설정형의 자리를 이미 갖고 있다 — 정책코드 + BU/공장/품목/공정 스코프 + 유효기간」). **이 장이 더하는 것은 하나다 — 그 4축에 설비·설비그룹이 없다.** 「설비그룹별 통제 수준」이 필요하면 축이 하나 는다. ⛔ **이 화면에는 두지 않는다**(통제 수준은 설비 속성이 아니다 — 인벤토리 판정 유지). ⚠ **인벤토리 §5 미결 4 는 「어느 화면이 소유하나」를 물은 것이고 그 질문은 아직 열려 있다** — 115화면 중 이 테이블을 소유한 화면이 **0건**이다. **소유 화면은 2단계(POP-작업전점검-이력확인통제 `P-02-02`)에서 정한다** |
| **8** | **점검 항목 표시 순서**가 필요한지 | **수집 대기** | `[추정]` 강도로만 적었다. **현행 일상점검표 실물**(WF05 S2 [Check])을 보면 확정된다. 필요하면 G-7 그대로 |

## §9. 이 장에서 나온 공유 계약 후보

### 9-1. ⭐ 자산 마스터의 상태는 **수명주기 축만** 갖는다 — 「지금 쓸 수 있는가」는 파생 판정이다

A-14(상태와 조건을 분리한다)의 **자산 마스터 판**이다. §5-2 가 실증했다.

**규칙안**

1. 설비·툴·계측기 같은 **자산 마스터의 `status_code` 는 「운용 / 폐기」 2값**으로 둔다
2. 「고장」·「보전중」·「비가동」·「검교정 만료」는 **트랜잭션이 만드는 조건**이며 **마스터에 쓰지 않는다**
3. **「지금 쓸 수 있는가」는 화면·서버가 판정**한다 — 판정식을 계약이 갖는다
4. 판정 결과를 보일 때는 **어느 조건 때문에 못 쓰는지**를 함께 보인다(G-3 — 「어떻게 풀 것인가」)

**왜 계약인가**: 이 규칙이 없으면 **네 화면이 같은 컬럼을 두고 경합**한다(B-13). 그리고 **세 마스터(설비·툴·계측기)에 같은 형태가 있다** — 한 곳에서 정하지 않으면 셋이 갈라진다.

### 9-2. **계층 해석 규칙은 도메인 안에서 하나만 쓴다**

결정 03 이 캘린더에 대해 「설비 → 소속 설비그룹 → 공장 순으로 **가장 가까운** 것을 적용」을 확정했다. **점검 항목 부여도 같은 규칙을 쓴다**(§4-C-2).

**규칙안**: 한 도메인 안에서 **계층을 거슬러 올라가며 값을 찾는 규칙이 둘 이상 생기면 합친다.** 층의 개수는 대상마다 다를 수 있으나(캘린더 3층 · 점검 항목 2층) **「가장 가까운 것이 이긴다」는 한 문장을 공유**한다.

**왜 계약인가**: 사용자가 규칙을 외우는 비용이 규칙 수에 비례한다. 그리고 **구현이 같은 탐색 함수를 재사용**할 수 있다.

### 9-3. 「없다」의 4단 뒤에 **5단째가 붙는다** — 「그러면 우리가 정의한다」

A-13 은 「없다」고 적기 전에 4단을 확인하게 한다. **2026-08-10 원칙 전환으로 5단째가 생겼다.**

```
① 한 층 아래  ② 다른 파일  ③ 다른 스키마  ④ 다른 이름
                                              ↓ 네 단 다 없으면
                                    ⑤ 원칙 1) — 화면이 정의하고 모델이 따라온다
```

⚠ **⑤가 ①~④를 건너뛰는 면허가 아니다.** 이 장이 그 증거다 — **④에서 걸려 테이블 신설이 불필요**해졌고(§3), ⑤로 간 것은 점검 항목 하나뿐이다(§4-C). **먼저 찾고, 없을 때만 정의한다.**

그리고 **⑤로 갈 때도 서식 원칙 2(「있으면 좋을 것 같은 필드는 쓰지 않는다」)는 그대로**다 — §4-C 가 **항목마다 근거와 강도를 붙인** 이유다.

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-10 | 초안 — 확대 11차 1/3. **도메인 05 첫 화면.** ⭐ **A-13 ④(다른 이름)가 걸려 「설비그룹 테이블 신설」이 불필요해졌다** — `mdm.production_line` 이 개념모델 §1 설비그룹과 형태가 같다(상위그룹=`parent_line_id`·1:N=`equipment.production_line_id`). 상류(결정 03 「라인을 설비그룹에 흡수」)와 하류(H-4 「작업구역을 라인에 흡수」)가 **같은 결론에 이름만 다르게** 도달. **설비 위치도 새 컬럼 불요** — DR-004 확정(계층 텍스트)으로 `plant > production_line > equipment` 로 성립. ⭐ **A-14 를 자산 마스터에 적용해 `status_code` 를 2값(운용/폐기)으로 확정** — 고장·보전중·비가동·검교정만료는 트랜잭션 조건이며, 마스터에 올리면 네 화면이 한 컬럼을 두고 경합한다(B-13). 점검·보전 항목은 **원칙 1)로 정의**(항목 마스터 + 2층 부여 · 해석 규칙은 결정 03 재사용). 엑셀 업로드는 **매체 미검증(WF05 S1 🟡)을 근거로 미탑재.** **작업 통제 3단계의 저장처에 설비·설비그룹 축이 없다**(⚠ 저장처 실재 자체는 **확대 6차가 이미 짚은 것**이다 · 소유 화면은 여전히 미정 — 115화면 중 0건). DS 신규 갭 0건(11차 연속) · G-1 계층 갭은 **2단이라 불성립**. 공유계약 후보 3건. |
| v0.2 | 2026-08-24 | **구현팀 질의 `#220`·`#219` 회신 반영.** ⭐⭐ **§5-1 액션 표가 「점검 항목을 만드는 것」을 빠뜨리고 있었다** — 마스터 정의(§4-C-1)·DS 매핑(§7 「점검 항목 목록」·「순서 재배치」)·계약 판정(요구서 05 §4)이 셋 다 이 화면 몫으로 서 있는데 **액션 표만 부여에서 멈춰** 있었다. 추가·저장·사용 중지·순서 재배치 **4행을 더하고 §5-1-1 을 신설**했다 — 자리는 **부여 창 안이 아니라 그룹·설비와 나란한 셋째 목록**이며, ⛔ 부여 창 안에 「새 항목 만들기」를 두지 않는다(B-6 의 화면 판). ⭐ **설비유형 공통코드 그룹을 계열마다 가른다**(`#219`) — 이 화면은 `EQUIPMENT_TYPE`, 계측기는 `INSTRUMENT_TYPE`. §4-B·§8-3 갱신. ⚠ **값 목록이 설 때까지 이 화면 목록에 계측기가 함께 보인다** — 밝히고 후속 제안안으로 넘긴다. |
| v0.3 | 2026-08-25 | **구현팀 확인 `#222` 반영 — 두 가지다.** ⛔ **①필드 이름이 틀렸다** — v0.2 가 순서 칸을 `display_order` 로 적었으나 **한 쪽 위 §4-C-1 이 `sequence_no`(동형)라 적어 두었고 계약도 `sequenceNo`** 다. `code_value.display_order` 를 관성으로 쓴 오기다(요구서 §3-1 · 생성물도 함께 고쳤다). ⭐⭐ **②그 옆에 판단이 통째로 빠져 있었다** — **유일 제약을 걸 것인가.** 모양을 빌린 `quality.inspection_item_spec.sequence_no` 는 **유일 제약이 있어 `A-5` 대상**인데, 「동형」이라고만 적어 두어 **제약까지 따라오는지가 열려 있었다.** ⇒ **걸지 않는다**(§4-C-1 주 신설) — 순서 중복이 점검을 틀리게 하지 않고 **칸의 필요 여부 자체가 §8-8 로 열려 있다.** 그래서 **재배치는 행마다 개별 갱신으로 성립**하고 구현팀이 만든 것이 그대로 맞다. 공유계약 `A-5` 에도 소절을 더했다 — 「**동형은 제약까지 빌리지 않는다**」. |
