# 비도식 근거 화면 도출 규칙 — 적용 결과 (1~3단계)

> 작성일: 2026-07-29 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 적용 규칙: `비도식근거-화면도출-규칙.md` **v0.2** · 입력: `screen-inventory-ia.md` v1.2 (113건)
> **4단계 사람 확정 완료 (2026-07-29~30) — `screen-inventory-ia.md` v1.3에 반영됨.**
> 1~3단계 산출과 4단계 결정의 차이는 §7에 기록했다.

## 1. 결과 요약

| | 건수 | |
| --- | ---: | --- |
| **존치 판정 대상** | **28** | 도식 앵커 없는 현행 화면 (도메인 23 + 공통 5) |
| ├ 존치 | **20** | 등급 1~4 근거 확보 |
| ├ **삭제** | **6** | 등급 5~6 — 근거가 화면을 정당화하지 못한다 |
| └ 통합 검토 | 2 | 사람 판단 |
| **신설 도출** | **5** | 기준 A·C 적용 + 적대 검증 누락 후보 판정 |
| **화면 수** | **113 → 112** | −6 +5 (통합 2건 반영 시 110) |

**성공 기준 통과.** 규칙 §5가 「28건 전부 존치되면 규칙이 일을 안 한 것」이라 했는데, **6건이 삭제로 갈렸다.** 등급 6(역산)으로 남은 화면은 **0건**이다.

## 2. 최대 발견 — 대시보드 6건 중 근거가 있는 것은 1건

기준 B-2(대시보드는 REQ 직접 요구만)를 적용한 결과다. **REQ 원문을 직접 열어** 확인했다.

| 화면 | 인용한 근거 | 원문이 실제로 요구한 것 | 판정 |
| --- | --- | --- | --- |
| `W-CO-05` 통합 대시보드(경영·생산) | REQ-PR-0036 | 「**대시보드** — 일일생산실적 및 알람발생 설비 위치 표시」 | **존치** — 유일한 직접 근거 |
| `W-05-14` 설비·툴 대시보드 | REQ-PR-0036·0016 | 0036의 「알람발생 **설비 위치**」가 이 화면 몫이나, **REQ 하나가 화면 둘을 정당화하지 못한다** | **`W-CO-05` 통합** |
| `W-02-09` 생산 현황·비가동 알람 | REQ-PR-0016 | 「특정 이벤트에 대한 **알람** 기능」 — 알람이지 대시보드가 아니다 | **삭제** — 생산 현황은 `W-CO-05`(0036 「일일생산실적」)·`W-02-08`, 알람은 `W-CO-03` |
| `W-03-08` 품질 대시보드·불량 알람 | REQ-PR-0016 | 동상 | **삭제** — 알람은 `W-CO-03`, 불량률·코드 분포 집계는 `W-03-05` |
| `W-01-08` 위치 확인 모니터링 | REQ-PR-0005 | 「자재 LOT의 적재(보관) **위치 정보 관리**」 — 관리지 모니터링 화면이 아니다 | **삭제** — `W-01-07` 재고 현황·상태 조회로 충분 |
| `W-04-09` 출하 현황 모니터링 | 없음 (QA #21) | QA #21 = 「현행 모니터링 없음, **추후** 고객 요구」 — 아직 요구가 아니다 | **삭제** — 1차 제외 |

**적대 검증이 「대시보드 5건 난립」이라 지적한 것의 원인이 확인됐다.** 「알람」 요구(REQ-PR-0016)를 대시보드로 바꿔 읽었고, WF의 「모니터링」 서술에서 역산했다. 알람 채널은 이미 SNS(Zalo·카카오톡)+알림센터로 확정돼 있다(QA #24).

> **알람을 받을 곳은 `W-CO-03` 알림센터다.** 도메인마다 대시보드를 세울 이유가 아니었다.

## 3. 판정 상세

### 3-1 · 마스터·설정 9건 → 존치 8 · 삭제 1

| 화면 | 등급 | 판정 |
| --- | --- | --- |
| `W-05-07` 수집채널 매핑 관리 | **3** 결정 11이 「수집채널매핑(신설, 마스터)」 명시 + 엔티티 실재 | 존치 |
| `W-05-11` 계측기 마스터 관리 | **4** 개념모델 §4 계측기 엔티티 실재(v2.2) | 존치 — `W-05-10`(계측기이력)과 저장 엔티티가 다르다 |
| `W-05-12` 설비·설비그룹 마스터 | **4** 개념모델 §1 설비·설비그룹 | 존치 |
| `W-05-13` 툴/금형/지그 마스터·QR 발급 | **1** REQ-PR-0003 「금형, 지그를 포함한 **모든 도구의 정보 관리를 MES에서** 필요」 | 존치 — ⚠ **「QR 발급」은 분리**(§3-4 기준 C) |
| `W-06-03` 불량코드 2계층 마스터 | **3** 결정 12(2계층·N:M 매핑 확정) | 존치 |
| `W-06-04` 판정유형 코드 마스터 | **1** REQ-PR-0027 「판정 유형 — 한도승인, 특채, 폐기 등」 | 존치 — E3는 **값 목록** 대기지 화면 존재가 아니다 |
| `W-06-06` 공통코드·조직 마스터(다국어) | **4** MES 저장분 = 다국어·확장 속성 | **통합 검토** — 아래 |
| `W-CO-06` 단말기-공정 매핑 설정 | **4** 데이터모델에 `mdm.terminal`·`mdm.terminal_process` 실재(DDL 검증 완료) | 존치 |
| `W-CO-07` 다국어(한/베) 환경설정 | **6 역산** | **삭제 → 전역 컨트롤** |

**`W-CO-07` 삭제 근거** — REQ-PR-0012는 「한국어, 베트남어 **지원**」이지 설정 화면 요구가 아니다. QA #33이 「언어 = 사용자별 설정」이라 했으나 **개념모델에 「사용자 환경설정」 엔티티가 없다**(실측 0건). v1.2에서 POP의 한/베 토글을 **셸 전역 컨트롤**로 처리한 것(N7)과 같은 판정을 관리웹에도 적용한다 — 헤더의 언어 선택기지 화면이 아니다.

### 3-2 · 조회·목록 8건 → 존치 7 · 통합 검토 1

| 화면 | 등급 | 판정 |
| --- | --- | --- |
| `W-01-07` 재고 현황·상태 조회 | **2** WF01 S6 재고 보관·상태(가용/Hold·유효기간) | 존치 |
| `W-01-09` 입하 예정 조회 | **2** WF01 S1 「입하 예정을 **파악·공유**」·Actor=전산·물류담당 명시 | 존치 |
| `W-02-08` W/O 진행현황 조회 | **3** 결정 14 W/O 상태 전이 그래프 | 존치 |
| `W-03-04` Lot Status 변경이력 조회 | **3** 결정 10 변경이력 1:N 전수 | **통합 검토** — `W-03-01` 현황 조회와 **같은 사용자·같은 엔티티 집합** |
| `W-03-05` 검사실적·검사결과 조회 | **3** 결정 01 3계층 통합 조회는 03 소관 | 존치 |
| `W-04-08` 완제품 재고·Lot Status 조회 | **2·3** WF04 S2·결정 10 | 존치 — `W-01-07`(자재)과 사용자·엔티티가 다르다 |
| `W-06-10` 연계 동기화 현황·실패 재처리 | **2** WF06 S7 예외 SA2~3 | 존치 — **조회가 아니라 조작**(재처리)이 있다 |
| `W-CO-03` 알림센터 | **1** REQ-PR-0016 이벤트 알람 | 존치 — **삭제된 대시보드들의 알람이 여기로 모인다** |

### 3-3 · 판정·입력 5건 → 전건 존치

| 화면 | 등급 | 근거 |
| --- | --- | --- |
| `W-01-02` 긴급 IQC 생략 한도승인 | **1** | REQ-PR-0027이 「한도승인」을 판정 유형으로 명시 |
| `W-04-07` 재작업/폐기 판정 의뢰 | **1** | REQ-PR-0025 「재작업 가능 불량과 폐기 불량 구분 필요」 — ⚠ SC4 도식 결함(이슈 #44) 대기 |
| `W-04-10` 제품 폐기 품의 | **1** | REQ-PR-0028 「제품 폐기 시 **내부 품의 후 기타 출고**」 — E4는 계정 항목 대기 |
| `W-02-10` 추가 자재 출고 요청(수동) | **2** | WF02 S4 문답 #4·#8 |
| `W-CO-01` 계정 로그인 | — | REQ 밖이나 **시스템 기반**(규칙 §6) — 결정 16·WBS §7 폼팩터① |

### 3-4 · 신설 도출 5건

#### 기준 C(출력) — 라벨 8종 전수 대조에서 **3종이 인쇄 화면 없음**

하드웨어 스펙 §3의 라벨/코드 8종과 현행 POP 화면을 맞춰봤다.

| 라벨/코드 | 발행 지점 | 인쇄 화면 |
| --- | --- | --- |
| 자재LOT 라벨 · 출고 QR · 인식표 · 포장라벨 · 납품라벨 | 창고·라인·출하 | `P-01-01`·`P-01-02`·`P-02-05`·`P-02-07`·`P-04-02` ✔ |
| **금형·툴 QR** | 기준정보 등록 시 발급 | ⚠ **없음** — `W-05-13`(**관리웹**)에 「QR 발급」이 붙어 있으나 인쇄는 POP 소관(결정 18) |
| **Location(보관 위치) 코드** | 창고 선반/구역 부착(고정 표지) | ⚠ **없음** — `W-06-07`은 마스터 등록만 |
| 고객 바코드 | 출하 포장 라인 | 하드웨어 스펙이 **[가설]** 표기 → 신설 보류 |

→ ~~`P-05-03`·`P-01-03` 신설~~ **철회 (리더 확정 2026-07-29)** — **라벨은 두 종류이고 이 둘은 마스터 라벨이다.**

결정 18의 POP 인쇄는 「**프린터가 배치되는 스테이션**(조합 1·3)의 패널 PC」가 전제인데, 금형·툴 QR(기준정보 등록 시·**내구성 라벨 스톡**)과 Location 코드(창고 고정 표지)는 **그 스테이션에서 발행되지 않는다.** 반면 결정 18의 **렌더링은 서버 소관**이라 프로그램과 무관하다.

→ **마스터 화면이 QR 라벨 이미지 생성까지 담당**하고 물리 인쇄는 기능 개발 범위 밖으로 둔다. `W-05-13`(화면명에서 「QR 발급」 제거)·`W-06-07`(Location 코드 라벨 이미지 생성 명시)에 반영. **POP 화면 신설 0건** — 규칙 기준 C도 v0.2로 개정했다(적대 검증 N6·N8은 이 방식으로 해소).

> 이건 개별 누락이 아니라 **한 종류의 구멍**이었다 — 마스터 등록 화면은 빠짐없이 세웠는데 그 마스터가 발행하는 물리 라벨을 인쇄할 POP 화면만 체계적으로 빠졌다.

#### 기준 A·B — 적대 검증 누락 후보 판정

| 후보 | 판정 | 근거 |
| --- | --- | --- |
| **N2** 04 제품입고 | **신설** | WF04 S1이 「제품입고(생산완료/외주 → 완제품 창고 · **제품LOT 형성**)」로 실재·Actor 명시. 제품LOT genealogy의 시작점 |
| **N3** 06 검사정책 설정 | **신설** | 개념모델 §4에 **「검사정책」 엔티티 실재**(v2.2 신설). 전수/샘플·샘플 비율·합격판정개수를 설정할 곳 |
| 공통 공장/창고 레이아웃 마스터 | **신설** | 개념모델 v2.0 신규 엔티티로 **실재**(8건 언급) + REQ-PR-0036이 「현장 공장 및 창고 **레이아웃 배치** 요청」 명시 |
| **N4** 06 작업자 마스터 | **신설 아님** | WF06 S7 확정 「수신 연계대상 = 품목·BOM·공통코드·조직·**작업자**·P/O」 — **ERP 수신본**이라 기준 A 제외 ①. `W-06-05`/`W-06-06` 통합 대상 |
| **N6·N8** | 위 기준 C에서 신설 | |
| 04 외주 제품 입고검사 | N2 흡수 | WF04 S1이 「외주분은 품질담당 동석」으로 같은 단계에 포함 |
| 04 출하성적서(CoA/CoC) 발행 | `W-04-03` 흡수 | 이미 `W-04-03`의 엔티티에 「검사성적서(CoA/CoC)」가 있다 |
| 05 설비 점검 계획·점검이력 | 신설 안 함 | **「점검계획」 엔티티 없음**(실측 0건). 점검이력은 트랜잭션(`M-05-01`이 생성)·조회는 `W-05-04` |
| 02 작업 통제 수준 3단계 설정 | **확인 필요** | QA #9가 「차단/경고/미적용 3단계 **설정형**」 확정(등급 3) — 설정할 곳이 필요하나 **귀속이 불명**(공정 마스터? 검사기준?) |
| 06 작업조(Shift/Crew) 마스터 | **확인 필요** | 개념모델에 언급은 있으나 ERP 수신 「조직」 포함 여부 미확인 |
| 04 고객사별 바코드 체계 마스터 | 보류 | 하드웨어 스펙 §3이 「고객별 양식 상이는 **[가설]**」 |

## 4. 화면 수 변동

| 구분 | 건수 | 화면 |
| --- | ---: | --- |
| **삭제** | **6** | `W-CO-07` 다국어 환경설정 · `W-05-14` 설비·툴 대시보드 · `W-02-09` 생산 현황 대시보드 · `W-03-08` 품질 대시보드 · `W-01-08` 위치 확인 모니터링 · `W-04-09` 출하 현황 대시보드 |
| **신설** | **5** | `P-05-03` 툴/금형 QR 인쇄 · `P-01-03` Location 코드 인쇄 · 04 제품입고 · `W-06-13` 검사정책 설정 · `W-CO-08` 공장/창고 레이아웃 마스터 |
| **통합 검토** | 2 | `W-03-04` → `W-03-01` · `W-06-06` → `W-06-05`(+작업자) |

**113 − 6 + 5 = 112건.** 통합 2건까지 반영하면 **110건**.

| 프로그램 | v1.2 | 적용 후 |
| --- | ---: | ---: |
| 관리웹 | 73 | **69** (−6 +2) |
| POP | 22 | **24** (+2) |
| 모바일 | 18 | 18 |
| (미배정) | — | 1 — 04 제품입고 프로그램 미정 |

## 5. 사람 확정 결과 (2026-07-29~30) — **7건 전건 결정**

| # | 항목 | 결정 |
| --- | --- | --- |
| 1 | 대시보드 삭제 5건 | **A — 삭제 + 조회 화면 3곳에 집계 뷰 요건 명시** |
| 2 | `W-03-04` → `W-03-01` | **A — 통합 + 이력 검색 모드·권한 분리 요건 명시** |
| 3 | `W-06-06` 통합 검토 | **A — 2화면 유지 + `W-06-06`에 작업자 귀속**(N4 해소·변동 0) |
| 4 | 04 제품입고 프로그램 | **A — `M-04-04` 모바일 신설** |
| 5 | `W-05-13` QR 발급 분리 | **리더 대안 채택 — 마스터 라벨은 이미지 생성까지, POP 신설 없음**(§3-4 참조) |
| 6 | 확인 필요 2건 | **A — [확인 필요]로 남기고 마감**(인벤토리 §4-6 #4·#5) |
| 7 | `W-CO-07` 다국어 환경설정 | **A — 삭제 → 관리웹 셸 전역 컨트롤**(POP·모바일과 일관) |

**최종 화면 수 109** (관리웹 68 · POP 22 · 모바일 19).

## 6. 1~3단계 예측 vs 4단계 실제

| | 1~3단계 예측 | 4단계 실제 | 차이 |
| --- | ---: | ---: | --- |
| 삭제 | 6 | **6** | 동일 |
| 통합 | 2(검토) | **1** | `W-06-06`은 존치·확대로 결정 |
| 신설 | 5 | **3** | `P-05-03`·`P-01-03` **철회** — 마스터 라벨은 이미지 생성까지(§3-4) |
| 화면 수 | 112(통합 시 110) | **109** | |

**규칙이 틀린 곳이 하나 있었다** — 기준 C가 라벨을 한 종류로 보고 POP 인쇄 화면 2건을 신설 후보로 잘못 도출했다. 리더 지적으로 **트랜잭션 라벨 / 마스터 라벨** 2종 분리로 교정하고 규칙을 v0.2로 올렸다. 이건 규칙의 실패가 아니라 **적용이 규칙을 교정한 정상 경로**다.

## 7. 다음 단계

1. ~~[사람] 6건 결정~~ → **완료 (7건)**
2. ~~`screen-inventory-ia.md` v1.3 · HTML 2종 재생성~~ → **완료**
3. **규칙 v0.2 → v1.0 확정** — 적용 실적(28건 전건 판정·등급 6 잔존 0)을 §5 성공 기준에 기재
4. 남는 것 = 인벤토리 §4-6 확인 사항 **5건** + 고객 바코드 체계 마스터 보류 1건

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| **4단계** | **2026-07-30** | **사람 확정 7건 완료 → 인벤토리 v1.3 반영.** 화면 **109**(관리웹 68·POP 22·모바일 19). 예측 대비 차이 = 신설 5→3(마스터 라벨 판정 교정)·통합 2→1(`W-06-06` 존치). 규칙 기준 C를 v0.2로 개정. |
| 1~3단계 | 2026-07-29 | 규칙 v0.1을 28건에 적용. **존치 20 · 삭제 6 · 통합 검토 2**, 신설 5건 도출. 등급 6(역산)으로 남은 화면 **0건** — 성공 기준 통과. 최대 발견 = **대시보드 6건 중 REQ 직접 근거는 1건뿐**(REQ-PR-0016은 알람 요구·REQ-PR-0005는 위치 관리 요구로 원문 확인). 기준 C 라벨 8종 전수 대조로 인쇄 화면 2건 신설(N6·N8). N4 작업자는 **ERP 수신본**이라 신설 아님을 WF06 S7로 확인. 화면 113 → 112(통합 반영 시 110). |
