# 도식 노드 대장 — 총괄 (v1.2)

> 작성일: 2026-07-28 (v1.2 — 6도메인 캔버스 완전성 보강·승격 79·분류 유보 0 / v1.1 — 04·05·06 캔버스 ID 전환·해상도 비대칭 해소 / v1.0 — 최초 전사) · 작성 주체: CREFLE OMF 팀 (UI/UX)
> 기준선: main `3dce876` · 도식스펙 01 v2.12 / 02 v2.18 / 03 v2.1 / 04 v1.4 / 05 v1.4 / 06 v1.5 · FigJam 보드 `Sgy5IKD4aWuzpKtJAbMyWz` 페이지 `219:4509`(2026-07-28 조회)
> 성격: **1계층 산출물** — 판단 없는 기계적 전사. 화면 매핑(2계층)은 아직 하지 않았다.
> **식별 체계: 6개 도메인 전부 FigJam 캔버스 노드 ID.** 674행 중 668행(99.1%)이 캔버스 ID를 보유한다.

## 0. 왜 만들었나

화면 목록 v1.0의 적대 검증(`../verification-report-r1.md`)에서 **분해 기준이 없다는 방법론 결함(M8)**이 나왔다. 같은 문법 요소(Wallet=목록)를 04에서는 독립 화면으로 뽑고 05·06에서는 다른 화면에 흡수해, 결과적으로 「화면 112건이 WBS 추정보다 많으니 누락 없음」이라는 커버리지 근거가 성립하지 않게 됐다.

이 대장은 그 결함을 구조적으로 막기 위한 **1계층**이다. 2계층 구조는 이렇다.

- **1계층(이 문서)** — 도식 노드를 하나도 빠짐없이 나열한다. 판단하지 않는다.
- **2계층(다음 단계)** — 각 노드가 어느 화면에 속하는지 명시적 규칙으로 매핑한다. **화면이 없는 노드는 왜 없는지를 반드시 적는다**(시스템 자동 / 실물 작업 / 다른 화면에 흡수).

이러면 양방향 검산이 가능해진다. 매핑 없는 노드가 있으면 그게 곧 누락이고, 근거 노드 없는 화면이 있으면 그게 과잉이다. 검증에서 발견된 「정상품 입하 처리」 누락은 이 대장이 있었으면 자동으로 걸렸을 것이다.

## 1. 방법

도메인별 독립 에이전트 6개가 **자기 도메인의 도식스펙만** 읽고 전사했다. 화면 목록 파일 열람을 금지해 역방향 오염을 차단했다. 각 에이전트에게 스펙의 규모(집계) 절과 대조하는 검산을 강제하고, **숫자를 맞추려고 노드를 지어내거나 빼는 것을 금지**했다.

전사 후 리더가 인용된 노드 ID가 원천 스펙에 실재하는지 전수 대조했다 — **6개 도메인 전부 날조 0건**.

## 2. 집계

**v1.2 확정치 (캔버스 완전성 보강 후)**

| 도메인 | 행위 | 시스템 | 사람 | 게이트 | 참조 | 표기 | 상태 | 전이 | 계 | (v1.0 대비) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 01 자재창고관리 | 64 | 16 | 15 | 10 | 24 | 86 | – | – | **215** | +2 |
| 02 생산실행 | 55 | 17 | 7 | 8 | 26 | 87 | – | – | **200** | +2 |
| 03 품질관리 | 3 | 1 | 0 | 1 | 2 | 9 | 7 | 16 | **39** | – |
| 04 제품출하 | 36 | 10 | 10 | 8 | 8 | 33 | – | – | **105** | +22 |
| 05 설비·툴 관리 | 33 | 13 | 6 | 7 | 9 | 31 | – | – | **99** | +25 |
| 06 기준정보·연계 | 29 | 13 | 6 | 2 | 8 | 37 | – | – | **95** | +28 |
| **합계** | **220** | **70** | **44** | **36** | **77** | **283** | **7** | **16** | **753** | **+79** |

분류 총합 753 = 행 합계 753 (검산 일치) · **유보 0** (v1.0의 「기타」 13건 전량 정규화).

**분류 정의** — 행위(Wallet·Mobile·Desktop·File·Archive) / 시스템(Server·Database) / 사람(User) / 게이트(OR) / 참조(헥사곤·DOCUMENT_SINGLE) / 표기(스티키·Email·태그·인라인·TEXT·OR 라벨칩) / 상태·전이(03 전용).

### ⚠ 승격 79건 중 행위 노드는 0건이다

이번 보강에서 늘어난 79행은 전부 **시스템 +19 · 참조 +21 · 표기 +52**이고 **행위는 220개 그대로**다. 캔버스에서 새로 찾아낸 것들이 Server 아이콘·리셉션 헥사곤·스티키·캡션이라 **화면 후보가 하나도 늘지 않았다.**

이건 2계층에 중요한 함의가 있다. 적대 검증이 지적한 **누락 15건은 「대장에 없던 행위 노드」로 설명되지 않는다.** 도식에 그려지지 않은 화면(WF·REQ 근거)이거나, 기존 행위 노드를 잘못 흡수한 결과라는 뜻이다. 2계층에서 누락을 다시 볼 때 이 점을 전제로 삼아야 한다.

2계층에서 화면과 대응할 1차 모수는 **행위 220개**다. 현행 화면 112건과 대비하면 약 2:1 압축이다.

## 3. 검산 결과

| 도메인 | 판정 | 내용 |
| --- | --- | --- |
| 01 | 불일치 1 | 메인 인라인 조건 라벨 스펙 18 vs 실측 19 — **원인은 스펙 오류**(§4-1) |
| 02 | 불일치 1 | 아이콘 인스턴스 스펙 50 vs 실측 51 — Email 스펙 1 vs 실측 2(`297:5414` U2·`297:5459` U6). **두 노드 모두 스펙 §미정의 마커 표에 등재돼 있어 집계 쪽이 낡았다** |
| 03 | **완전 일치** | 39행 = 스펙 「섹션 584:6758 자식 39」와 정확히 일치. 상태 7·전이 16·노드 23 전항목 대조 |
| 04 | **완전 일치** | 축 11 · 행위 24 · OR 8 · Database 2 · 스티키 8 · Email 4 전항목 |
| 05 | 불일치 2 | 회색 리셉션 hex 3건 전사 불가(§4-2) · M8 마커가 공식 표에서 누락(§4-3) |
| 06 | 불일치 1 | 회색 리셉션·경계 hex 4건 전사 불가(§4-2) |

## 4. 발견된 스펙 결함 — [uiux→docs] 이슈 **발행 완료(2026-07-28)**

대장을 만드는 과정에서 원천 문서의 오류가 드러났다. 경계 규칙상 직접 수정하지 않고 이슈로 넘겼다.

| 이슈 | 대상 | 내용 |
| --- | --- | --- |
| **#22** | #17 Task3 감사 산출물 | `766:7740` 삭제 금지 — 감사 결론 번복(§5-2) |
| **#23** | 도식스펙-01 | §1.2 인라인 조건 라벨 표 오류 2건(§4-1) |
| **#24** | 도식스펙-05 | 노드ID 오기 5 · 리셉션 hex 3 미기재 · 송출카드 4 미등재 · M8 표 누락(§4-2·4-3·5-2) |
| **#25** | 도식스펙-06 | 회색 hex 4 미기재 · Server 9 미등재(체인 서술 오류) · Email 집계 과소(§4-2·5-1·5-2) |
| **#26** | 도식스펙-02·04 | 02 Email 집계 과소 · 04 `H7` 캔버스 미작화(§4-4·5-2) |

### 4-1 · 도식스펙-01 §1.2 인라인 조건 라벨 표 — 오류 2건 (개수가 상쇄돼 은폐됨)

- **`220:12448`이 인라인 라벨로 오등재**. 표는 「적치 불가(포화)·기본」으로 한 행을 주지만, 같은 문서 §1.2 본문과 §5 관찰은 **「커넥터 220:12448 — 조건 라벨 없음」·「진입 조건 미표기」**라고 명시한다. 실제 문구는 사이드 진입 헥사곤(「적치 불가(포화) ← 메인」) 소속이다. **문서 내부 자기모순.**
- **`914:9058`「생략·긴급승인→한도승인」이 표에서 누락**. v2.10에서 IQC 생략을 2분기로 쪼개며 신설됐고 §1.2-B 본문에는 서술돼 있으나 표에 행이 없다.
- 부수: 표 캡션이 아직 「메인 15개」이고 #16 신규 3건(`946:6827`·`946:6793`·`946:6806`)도 표에 없다. **규모 절(18)은 갱신됐는데 §1.2 표는 v2.10·v2.12 두 차례 갱신이 반영되지 않았다.**
- **왜 지금까지 안 잡혔나**: 잘못 들어간 1건과 빠진 1건이 맞물려 개수가 우연히 맞는다. 노드를 하나씩 대조하지 않으면 발견될 수 없는 유형이다.

### 4-2 · 도식스펙-05·06 — 집계에만 존재하고 본문에 식별자가 없는 노드 7건

05의 회색 리셉션 hex 3건, 06의 회색 리셉션·경계 hex 4건이 **§0 수량 집계에는 잡혀 있으나 본문 어느 표에도 개별 노드 ID·위치·라벨이 없다.** 스펙 스스로 "캔버스에 있다"고 세어놓고 그게 무엇인지는 적지 않은 상태라 대장에 옮길 수 없었다.

> **✅ 대장 측 해소(v1.1)** — 캔버스 수확으로 7건 전부 정체를 밝혀 대장에 반영했다(§5-1). 다만 **원천 스펙의 결함은 그대로 남아 있다** — 스펙 본문에 이 노드들의 ID·라벨을 기재하는 건 docs 몫이므로 이슈 대상으로 존치한다.

### 4-3 · 도식스펙-05 §4.1 미정의 마커 표 — M8 누락

M8이 §0 집계(8개)와 §2.5 괄호 주석에는 등장하나 **§4.1 공식 미정의 마커 표에는 행이 없다**(M1~M7만 등재). 개수는 맞지만 공식 목록이 1건을 빠뜨렸다.

### 4-4 · 도식스펙-02 아이콘 집계 — Email 1 vs 실측 2

`297:5414`(U2)·`297:5459`(U6) 둘 다 §미정의 마커 표에 등재돼 있는데 집계는 Email을 1로 센다. 01·05와 같은 **집계 절 대 본문 표 불일치** 유형이다.

## 5. 해상도 비대칭 — 캔버스 수확으로 **해소**(2026-07-28)

v1.0에서 이 대장의 최대 제약으로 기록했던 문제다. 04·05·06 스펙에는 개별 노드 ID가 없어(각 7·12·12개가 전부 섹션 껍데기) 대장이 `M1`·`A1`·`B7` 같은 **문서용 마커 번호**로 작성됐고, 그 결과 완전성이 「캔버스에 무엇이 있는가」가 아니라 「스펙이 무엇을 라벨링했는가」에 묶여 있었다.

**FigJam 캔버스에서 직접 수확해 세 도메인을 캔버스 ID로 전환했다.** 이제 여섯 도메인이 같은 식별 체계를 쓴다.

| 도메인 | 캔버스 ID 보유 행 | 비고 |
| --- | --- | --- |
| 01 · 02 · 03 | 213/213 · 198/198 · 39/39 | 원래부터 캔버스 ID |
| 04 | **79/83** | 미확정 1(H7 — 캔버스 미작화) · 커넥터 ID 행 3 |
| 05 | **74/74** | 전건 확정 |
| 06 | **65/67** | 커넥터 라벨 2건은 커넥터 ID로 기재 |
| **합계** | **668 / 674 (99.1%)** | |

원래 마커 번호는 비고 열에 `[M4]` 형태로 보존해 도식스펙과의 상호 참조를 유지했다. 확신이 없는 행은 치환하지 않고 `(캔버스ID 미확정)`으로 남기도록 지시했다 — **틀린 ID는 빈칸보다 나쁘기 때문**이다.

### 5-1 · 수확이 답을 준 미해결 질문 3건

| 질문 | 결과 |
| --- | --- |
| 05 회색 리셉션 hex 3건의 정체(§4-2) | **규명** — `780:7746`(05-S-A 진입)·`784:7907`(05-S-B 진입)·`791:8038`(05-S-C 진입). 전부 **사이드 진입 리셉션**이다. 스펙 §0 집계 5(리셉션 3+경계 1+게이트 1)와 정확히 일치 |
| 06 회색 hex 4건의 정체(§4-2) | **규명** — `688:7231`(A6→02 경계)·`710:7242`·`712:7321`·`717:7462`(리셉션 A/B/D). 06-S-C에는 리셉션이 없다는 것까지 확인 |
| 06 「판정유형 E3 빨간 Email」의 정체 | **독립 Email 노드 `686:7203`으로 판정**(스티키 임베드 아님). 따라서 캔버스 Email은 8개이고 **스펙 §0의 "7개"는 1개 과소 집계**다 |

## 5-2. 캔버스 수확이 새로 찾아낸 것

스펙만 읽어서는 발견할 수 없었던 항목들이다.

### ⚠ 최우선 — `766:7740`은 삭제하면 안 된다 (#17 판단 번복)

#17 표현오류 감사는 이 도형을 「SQUARE·fill/stroke/text 전무 = 화면상 아무것도 렌더 안 됨(비가시 잔여도형) … **불필요 시 삭제(비가시라 시각 영향 0 — 선택 사항)**」으로 판정했다.

**실제로는 커넥터 `766:7744`「다음 주기」의 시작점이다.** 리더가 캔버스를 직접 조회해 확인했다.

```
<shape-with-text id="766:7740" x="2858" y="152" width="24" height="96" name="SQUARE"></shape-with-text>
<connector id="766:7744" connectorStart="766:7740" connectorEnd="746:7524">다음 주기</connector>
```

이 커넥터가 05 도식을 **주기 루프**로 성립시킨다(A5 작업실적 → A1 점검 재개). 삭제하면 루프가 끊긴다. #17이 "선택 사항"으로 남긴 정리 항목이므로 **누군가 실행하기 전에 막아야 한다.**

부수 사실: 대장이 「A5→A1 다음 주기」로 적은 체인의 실제 기점은 A5(`746:7605`)가 아니라 이 비가시 스텁이다. 논리적 기점이 A5라는 건 스펙에만 있고 캔버스 구조에는 없다.

### 05 · 대장·스펙의 노드 ID 오기 5건

`174:4531`(캠데코)→실제 `790:8036` · MES-8 `792:8073`→`792:8063` · MES-10 `792:8090`→`792:8080` · MES-11 `797:8219`→`797:8209` · MES-12 `797:8236`→`797:8226`.

뒤 4건은 전부 **초록 스티키의 ID를 Server 노드 ID로 잘못 적은 동일 패턴**이다. 스펙에서 온 값이므로 원천 문서 오류로 보인다. 치환본에서 정정하고 비고에 ⚠ 표기했다.

### 05 · 메인측 사이드 송출 카드 4건 미등재

`752:7708`(05-S-B#1) · `752:7711`(05-S-A) · `752:7714`(05-S-C) · `752:7717`(05-S-B#2). 리셉션 hex의 짝이 되는 송신점인데 스펙·대장 어디에도 없다. 대장의 「A5→사이드A(회색)」 류 서술은 실제로 이 카드들을 경유한다.

### 06 · Server 아이콘 9건 대장 누락

01·02·05 대장은 Server를 노드로 등재하는데 06만 스티키만 등재했다. 그 결과 06-S-D의 체인 서술(`B13·B14 → MES-10 → DB-D`)이 실제 구조(`→ Server 717:7506 → DB-D`)와 어긋난다.

### 04 · 역방향 누락 — 대장에 있는데 캔버스에 없다

`H7`(출하 1:N P&P 반복)에 대응하는 캔버스 객체가 **없다**(미작화). 지금까지 발견한 것과 반대 방향의 결함이라 별도로 추적한다.

### 04 · 미매칭 22건 (배지 제외)

Server 8 + OR 라벨칩 8 + 대장에 서술조차 없는 객체 6.

### 정밀도가 올라간 것

05 사이드 제목 TEXT의 A~E 매핑이 「순번 추정」에서 **`9057`=A·`9058`=B·`9059`=C·`9060`=D·`9061`=E 1:1 확정**으로 바뀌었다. 06의 #17 TEXT도 「개별 미분리」에서 `974:9062~9065` 개별 확정으로 해소됐다. 04의 제목 TEXT 순차 추정도 실측과 부합함을 확인했다.

### 배지 칩은 누락이 아니다

캔버스에는 프로그램 배지 칩이 도메인마다 붙어 있다(04 11 · 05 13 · 06 8, 03도 4개 확인). #10-2 결정으로 도식스펙에 정본화하지 않고 `placement-plan.md`에만 두기로 했으므로 대장 미등재가 정상이다. 미매칭 집계에서 별도 분류했다.

## 5-3. 승격 규칙 (v1.2 — 무엇을 대장 행으로 삼는가)

캔버스 수확으로 「대장에 없는 캔버스 노드」가 드러나면서, **무엇을 대장 행으로 올릴지**를 규칙으로 못박을 필요가 생겼다. 도메인마다 다르게 처리하면 v1.0에서 겪은 M8(분해 기준 부재)이 재발한다.

### 승격한다 — 캔버스에 실재하는 비커넥터 객체 전부

| 캔버스 객체 | 분류 |
| --- | --- |
| Wallet · Mobile · Desktop · File · Archive | `행위` |
| Server · Database | `시스템` |
| User | `사람` |
| OR | `게이트` |
| HEXAGON (리셉션 · 경계 · 참조) · DOCUMENT_SINGLE (사이드 송출/수신 카드) | `참조` |
| 스티키 · Email 마커 · 캡션 TEXT · OR 라벨칩 · 태그 · 인라인 라벨 | `표기` |

원칙은 **「캔버스에 있으면 대장에 있다」**이다. 화면과 무관해 보여도 일단 등재하고, 2계층에서 「화면 없음 + 사유」를 붙여 처리한다. 등재하지 않으면 사유를 남길 자리조차 없어져 조용히 사라진다.

### 승격하지 않는다 — 단 총괄에 개수를 기록한다

- **프로그램 배지 칩** (52×40 안팎 ROUNDED_RECTANGLE · 「웹」·「POP」·「모바일」·「?」 라벨). **#10-2 결정으로 도식스펙 정본화 제외** — 정본은 `../../2026-07-24-프로그램배지/placement-plan.md`다. 대장에 넣으면 정본이 둘이 된다. 캔버스 실측: 03 4 · 04 11 · 05 13 · 06 8.
- **커넥터**. 노드가 아니다. 단 **03은 예외** — Lot Status 전이도라 전이(커넥터) 자체가 콘텐츠이므로 기존대로 `전이` 분류로 등재한다.

### 분류 유보 표기 금지

v1.0에서 `표기(추정)`·`시스템(추정)`·`분류규칙 미명시·최근접` 같은 유보값이 13건 있었다. 분류는 **유형에서 기계적으로 파생**되므로 유보할 대상이 아니다. 표준 6분류(행위·시스템·사람·게이트·참조·표기) 중 하나로 확정하고, 망설인 이유는 비고로 옮긴다.

## 5-4. 캔버스 완전성 검산 (v1.2)

6개 도메인 전부 캔버스와 양방향 대조를 마쳤다. **각 도메인의 합계 검산이 맞는다.**

| 도메인 | 캔버스 총 객체 | 매칭 | 승격 | 배지(제외) | 커넥터(제외) | 검산 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 01 | 339 | 212 | 2 | 17 | 108 | ✔ |
| 02 | 303 | 196 | 2 | 13 | 92 | ✔ |
| 03 | 43 | 39 | 0 | 4 | (전이 16은 등재) | ✔ |
| 04·05·06 | — | — | 22·25·28 | 11·13·8 | 83·79·53 | ✔ |

배지 칩 실측 합계 **66개**(01 17 · 02 13 · 03 4 · 04 11 · 05 13 · 06 8)로 `placement-plan.md` §합계의 **66칩과 정확히 일치**한다. 배지 정본이 캔버스와 어긋나지 않음을 독립 경로로 재확인한 셈이다.

### 역방향 누락 3건 — 스펙에는 있는데 캔버스에 없다

| 대상 | 도메인 | 상태 |
| --- | --- | --- |
| `297:5414` (U2 Email) | 02 | 캔버스 부재 — 이슈 **#26**(정정 코멘트 참조) |
| `H7` (출하 1:N P&P 반복) | 04 | 캔버스 부재 — 이슈 **#26** |
| `223:5562` (범례 분기규약 문구) | 01 | 01 두 섹션 밖. 삭제됐거나 페이지 공통 범례(`219:4610`)에 소재 — 판별 보류 |

앞의 둘은 **「작화할 것인가, 스펙에서 뺄 것인가」의 같은 판단**이다.

> **이슈 #26 정정 이력**: 최초 발행 시 02 Email을 「집계 1 → 2로 정정」해달라고 요청했으나, 캔버스 실측 결과 **집계 1이 맞고 마커 표에 미작화 노드가 등재된 것**이 결함이었다. 요청 방향이 반대여서 코멘트로 철회·정정했다. 스펙 본문만 근거로 판정하면 어느 쪽이 정답인지 가릴 수 없다는 사례다.

## 6. 다음 단계

1. **매핑 규칙 확정** — 초안 = `../screen-mapping-rule.md`. 리뷰 후 확정한다. 사유 코드 11종·분류별 기본 처분 8종·병합 조건(나누기 S1~S4 / 묶기 4조건)·프로그램별 보정.
2. **2계층 매핑** — 753행 각각에 화면 ID 또는 `화면없음:<사유코드>`를 부여한다. 빈칸을 허용하지 않는다.
3. **누락·과잉 자동 도출** — 매핑 결과를 현행 화면 112건과 대조한다. 적대 검증의 누락 15·과잉 7이 이 규칙 아래에서도 살아남는지 재검증한다. **단 §2에서 확인했듯 누락 15는 「대장에 없던 행위 노드」로 설명되지 않으므로**, 도식 밖 근거(WF·REQ)나 흡수 판단 오류 쪽에서 원인을 찾아야 한다.
4. HTML 정리본 재생성 · 이슈 #22~#28 처리 추적.

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v1.0 | 2026-07-28 | 최초 — 6개 도메인 674행 전사(행위 220·시스템 51·사람 44·게이트 36·참조 56·표기 231·상태 7·전이 16·기타 13). 노드 ID 날조 0 검증. 검산 결과 03·04 완전 일치, 01·02·05·06 불일치 5건은 전부 원천 스펙 결함으로 판명(§4). 해상도 비대칭 한계 명시(§5). |
| v1.2 | 2026-07-28 | **6도메인 캔버스 완전성 보강 — 「캔버스에 있으면 대장에 있다」 달성.** 승격 규칙 확정(§5-3: 배지 칩·커넥터만 제외, 분류 유보 금지). 01·02는 캔버스 재조회로 양방향 대조(승격 각 2 — 스펙 전사가 실제로 거의 완전했음이 확인됨), 04·05·06은 확보된 미매칭 목록 승격(22·25·28). **총 674 → 753행(+79)**, 분류 유보 13 → **0**. **행위 노드는 220 불변** — 승격분이 전부 시스템·참조·표기라 화면 후보가 늘지 않았고, 이는 적대 검증의 누락 15건이 「대장에 없던 행위 노드」로 설명되지 않음을 뜻한다. 배지 칩 실측 66 = placement-plan §합계 66칩 **독립 일치 확인**. 역방향 누락 3건 등재(02 `297:5414` · 04 `H7` · 01 `223:5562`). **이슈 #26 요청 방향 오류를 캔버스 실측으로 발견해 코멘트 정정**(집계가 아니라 마커 표가 결함). 매핑 규칙 초안 = `../screen-mapping-rule.md`. |
| v1.1 | 2026-07-28 | **04·05·06 캔버스 ID 전환 — 해상도 비대칭 해소.** FigJam 직접 조회로 마커 번호(M1·A1·B7)를 실제 노드 ID로 치환, 6도메인 식별 체계 통일(668/674 = 99.1%). 원 마커는 비고에 `[M4]`로 보존. **미해결 질문 3건 전부 규명**(05 리셉션 hex 3 · 06 회색 hex 4 · 06 E3=독립 Email `686:7203`, 스펙 §0 1개 과소집계 확인). **신규 발견**: `766:7740`이 「다음 주기」 루프 커넥터 기점 → **#17 삭제 권고 번복**(리더 캔버스 직접 검증) · 05 노드ID 오기 5건(스티키 ID↔Server 노드 ID 혼동 4 + 캠데코 1) · 05 송출 카드 4건 미등재 · 06 Server 9건 누락으로 체인 서술 오류 · 04 H7 캔버스 미작화(역방향 누락) · 04 미매칭 22. 정밀도 상향: 05 제목 TEXT A~E 1:1 확정 · 06 #17 TEXT 개별 ID 확정. |
