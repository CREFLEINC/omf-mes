# M-01-11 · 실물 카운트

> 작성일: 2026-08-05 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 4차 8/11** — **C-1 규칙 3(발생 시각 ≠ 서버 수신 시각)이 데이터로 뒷받침되는 첫 화면**이다. 동시에 **「아직 세지 않았다」와 「세었더니 0이다」를 구분할 수 없다.**

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `M-01-11` · 실물 카운트 |
| 프로그램 · 도메인 | **모바일**(360×720dp + BLE 스캐너) · 01 자재창고관리 |
| 유형 | **스캔** |
| 행위자 | 물류(창고)담당 |
| 신뢰도 | **확정** |
| IA 위치 | 모바일 스캔 퍼스트 홈 > **실사 타일** |
| 범위 | **카운트만.** 조정은 관리웹 `W-01-04`(인벤토리 명시) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §3 **S9**(01-S-H) 태스크 2 — 「장부 재고 조회(219:5550) + **실물 카운트**(219:5551 — **모바일 병렬**)」 → 태스크 3 「장부 대비 대조 분기(OR 219:5552)」 |
| **화면·IA** | 인벤토리 v1.5 `M-01-11` — 유형 스캔 · 데이터=재고실사·재고·자재LOT · 배지=모바일 · **「창고 순회 실물 카운트·수량 입력(조정은 관리웹 `W-01-04`)」** |
| **요구** | ✓설계확정 **결정 08** 「조정=**수불 원장 트랜잭션**으로 기록(잔량 직접 덮어쓰기 금지)」 · 예외 **E-8** ① 「실사 중 입출고 발생(**동결 안 됨**) → 카운트 왜곡 — 동결 여부 **(U-D1)**」 |
| **데이터** | `inventory.inventory_count`(11열 — **`blind_count`**) · `inventory_count_line`(14열 — **`counted_at` NOT NULL · `variance_qty` GENERATED · `uq_inventory_count_line`**) — 실측 |

## §3. 레이아웃 — 360×720dp

```
360 × 720 dp
┌ 앱바 56 ─────────────────────────────┐
│ ←  실물 카운트    ○ 오프라인  미동기12 │
├──────────────────────────────────────┤
│ 본문 592                              │
│  ┌ 실사 CNT-2026-08 ────────────┐     │
│  │ 1공장 자재창고 · 정기         │ 56  │
│  │ 블라인드 실사 ⓘ               │     │
│  └───────────────────────────────┘    │
│   진행  38 / 120                 40   │
│  ─────────────────────────────        │
│   위치 스캔                      32   │
│  ┌────────────────────────────┐       │
│  │ ▌  A-01-03                  │ 64   │
│  └────────────────────────────┘       │
│  ─────────────────────────────        │
│   ▸ ABC-123  …0007               56   │
│     장부  ▪▪▪ (가림)                   │
│     실물 [    120    ] EA              │
│   ▸ DEF-456  …0012               56   │
│     실물 [           ] EA  미실사      │
│  ─────────────────────────────        │
│   [ + 목록에 없는 재고 ]          48  │
│   ⚠ 계획 라인만 담을 수 있습니다  40  │
├──────────────────────────────────────┤
│ 액션 72   [ 이 위치 완료 ]            │
└──────────────────────────────────────┘
```

**세로 검산**: 56+40+32+64+56+56+48+40 = 392 / 592 — 통과(목록 스크롤).

## §4. 필드

### §4-A. 실사 헤더 `inventory.inventory_count` (읽기)

| 라벨 | 출처 컬럼 | 이 화면이 쓰는 방식 |
| --- | --- | --- |
| 실사번호 | `inventory_count_no` | UNIQUE |
| 유형 | `count_type_code` | 정기 / 수시 — ⚠ 값 목록 §8-3 |
| 창고 | `warehouse_id` | FK NOT NULL |
| 계획일 | `planned_date` | date NOT NULL |
| **블라인드** | `blind_count` | **boolean NOT NULL DEFAULT false** — §5-1 |
| 상태 | `status_code` | ⚠ 값 목록 |

### §4-B. 카운트 라인 `inventory.inventory_count_line`

| 라벨(한/베) | 출처 컬럼 | 타입 | 필수 | 검증·비고 |
| --- | --- | --- | :-: | --- |
| 라인 번호 | `line_no` | integer CHECK > 0 | ✅ | `UNIQUE(count_id, line_no)` — §5-3 |
| 위치 | `location_id` | FK NOT NULL | ✅ | |
| 품목 | `item_id` | FK NOT NULL | ✅ | |
| LOT | `lot_id` | FK | — | **nullable** — LOT 무관 재고 |
| **장부 수량** | `system_qty` | `qty_t` NOT NULL | ✅ | **블라인드면 가린다** §5-1 |
| **실물 수량** | `counted_qty` | `qty_t` **NOT NULL** | ✅ | ⛔ **미실사를 표현할 수 없다** §5-2 |
| **차이** | `variance_qty` | **GENERATED** `(counted - system)` | — | 화면이 계산하지 않는다 |
| 차이 사유 | `variance_reason_code` | `code_t` | — | nullable |
| **카운트 시각** | `counted_at` | timestamptz **NOT NULL** | ✅ | ★ **C-1 규칙 3** §5-4 |
| 카운트 담당 | `counted_by` | bigint | — | |

## §5. 액션·상태

### §5-1. 블라인드 실사 — 장부를 감춘다

```
inventory_count.blind_count   boolean NOT NULL DEFAULT false
```

**확정이 데이터에 있다.** 참이면 **`system_qty`를 화면에 보이지 않는다** — 작업자가 장부 수를 보고 그대로 적는 것을 막는 실사 기법이다.

| | `blind_count = false` | `blind_count = true` |
| --- | --- | --- |
| 장부 수량 | `120 EA` | **`▪▪▪`** (가림) |
| 차이 | 실시간 표시 | **보이지 않음** |
| 차이 사유 | 즉시 입력 | ⚠ **입력할 근거가 없다** §8-1 |

⚠ **블라인드에서 차이 사유를 어떻게 받나** — 차이를 모르는데 사유를 물을 수 없다. **사유는 `W-01-04`(관리웹)에서 받는 것**으로 설계한다. `variance_reason_code`가 nullable이라 성립한다.

⚠ **API가 `system_qty`를 아예 내려보내지 않아야 한다.** 화면에서 가리는 것으로는 부족하다 — 응답 본문에 있으면 개발자 도구로 보인다. **블라인드는 서버가 지킨다** → §9-1.

### §5-2. ⛔ 「아직 안 셌다」와 「세었더니 0이다」가 같은 값이다

```
counted_qty   app.qty_t NOT NULL     -- qty_t 는 ≥ 0
```

**NOT NULL이라 비워 둘 수 없고, `qty_t`가 0을 허용하므로 0이 유효값이다.**

| 상태 | 저장 |
| --- | --- |
| 아직 세지 않음 | ⛔ **표현 불가** — 행을 안 만들거나 0을 넣는 수밖에 |
| 세었더니 0개 | `counted_qty = 0` |

**창고를 순회하는 실사에서 이것이 문제가 된다** — 120 라인 중 38개를 센 상태에서 전송하면, 나머지 82개가 「0개로 확인됨」인지 「아직 안 감」인지 서버가 모른다. **`W-01-04`가 조정을 만들 때 82건을 전량 손실로 잡을 수 있다.**

**세 안**:

| 안 | 동작 | 대가 |
| :-: | --- | --- |
| **① 센 라인만 전송** *(권고)* | 미실사 라인은 아예 안 보낸다 | 서버가 「없음 = 미실사」로 해석해야 한다 |
| ② 별도 상태 컬럼 | `line_status_code` 신설 | **컬럼이 없다** |
| ③ 헤더 상태로 | 실사 완료 전엔 조정 금지 | 부분 실사가 안 된다 |

**①로 설계**하고 **「없는 라인 = 미실사」를 API 규약으로 못박는다.** 그렇지 않으면 오프라인 부분 전송이 데이터를 망친다.

→ **§8-2 · §9-2.**

### §5-3. 계획에 없던 재고를 셀 수 있나

```
uq_inventory_count_line  UNIQUE (inventory_count_id, line_no)
line_no  integer NOT NULL CHECK (line_no > 0)
```

라인은 **번호를 갖는 계획 항목**이다. 실사 계획을 만들 때 장부 기준으로 라인이 생성된다(`system_qty NOT NULL`이 그 증거 — 계획 시점에 이미 채워진다).

**그런데 순회하다 보면 장부에 없는 물건이 나온다.**

| 상황 | 처리 |
| --- | --- |
| 장부에 있고 실물도 있다 | ✅ 계획 라인에 입력 |
| 장부에 있는데 실물이 없다 | ✅ `counted_qty = 0` |
| **장부에 없는데 실물이 있다** | ⚠ **라인이 없다** — `system_qty`에 무엇을 넣나 |

**`system_qty = 0`으로 라인을 추가하면 성립한다**(`qty_t`가 0 허용). `line_no`는 서버가 다음 번호로 채번한다.

⚠ **모바일이 라인을 추가할 수 있는가**가 미정이다. 화면에는 버튼을 두되 **오프라인에서는 `line_no` 채번을 할 수 없으므로**(서버 채번) **온라인일 때만 활성**한다 → §8-4.

### §5-4. ★ `counted_at`이 C-1 규칙 3을 데이터로 뒷받침한다

**C-1 규칙 3**: 「`occurred_at`(발생)과 `recorded_at`(서버 수신)을 **분리** — 오프라인 지연이 실적 시각을 왜곡하지 않는다」

```
inventory_count_line.counted_at   timestamptz NOT NULL      ← 발생 (작업자가 센 시각)
inventory_count_line.created_at   timestamptz NOT NULL      ← 서버 수신
                                  DEFAULT clock_timestamp()
```

**두 컬럼이 실재하고 의미가 정확히 갈린다.** 3차까지는 `production_result`·`work_session_event`에서 봤고, **이 화면이 재고 계열에서 첫 사례**다.

**규칙**: 화면은 **`counted_at`을 로컬 시각으로 채워 큐에 담는다.** 서버가 받은 시각으로 덮어쓰면 안 된다.

⚠ **PDA 시계가 틀어져 있으면 어떻게 하나** — 오프라인이라 서버 시각으로 보정할 수 없다. **연결 시 서버 시각과의 차이를 보정**하는 것이 일반적 처리이나 **규약이 없다** → §8-5.

### §5-5. 실사 동결 — (U-D1) 미정이고 오프라인이 악화시킨다

예외 E-8 ①: 「실사 중 입출고 발생(**동결 안 됨**) → 카운트 왜곡 — 동결 여부 **(U-D1)**」

**오프라인이면 문제가 더 커진다.**

```
09:00  A-01-03 카운트 (120개)         ← 오프라인 큐
10:30  다른 단말이 A-01-03 에서 출고 (20개)
11:00  연결 복구 → 카운트 전송
       → 서버의 system_qty 는 이미 100
       → variance = 120 - 100 = +20  (실제로는 차이가 없었다)
```

**`system_qty`가 언제 기준인가가 정해지지 않았다.**

| 안 | `system_qty` |
| :-: | --- |
| **① 계획 시점 고정** *(권고)* | 실사 계획 생성 시 스냅샷. **`counted_at`과 짝이 맞는다** |
| ② 전송 시점 | 오프라인 지연이 그대로 차이가 된다 |

**①이 데이터와 맞다** — `system_qty`가 라인에 저장되어 있고 계획 시점에 채워지므로 이미 스냅샷이다. 화면은 그것을 전제한다.

⚠ **동결 여부는 여전히 미정**이다. 동결하지 않으면 ①이어도 실사 기간 중 이동분이 차이로 잡힌다 → `W-01-04`가 판단할 몫으로 넘긴다.

### §5-6. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 실사 선택 | 진행 중 실사 목록 |
| 위치 스캔 | 실사 선택됨 — 포커스 고정 |
| 수량 입력 | 라인 선택됨 — 키패드(G-6) |
| 목록에 없는 재고 추가 | **온라인일 때만**(§5-3) |
| 직접 입력 | 항상(D-3) |
| **이 위치 완료** | 그 위치의 라인 1개 이상 입력 — C-3 |

## §6. 예외·오프라인

| 상황 | 처리 |
| --- | --- |
| 위치에 라인이 없음 | `EmptyState` + 「목록에 없는 재고 추가」 안내 |
| **블라인드** | 장부·차이 숨김 · **서버가 안 내려보낸다**(§5-1) |
| 실물 0개 | ✅ `counted_qty = 0` 입력 — 미실사와 다르다(§5-2) |
| **미실사 라인** | ⚠ **전송하지 않는다**(§5-2 ①안) |
| 이미 카운트된 라인 | 덮어쓰기 허용 + 「이전 값 120 EA」 표시 |
| **오프라인** | 큐잉(C-1) · `counted_at` 로컬 시각(§5-4) · `business_date` 동봉 |
| 실사가 마감됨 | `400 STATE_LOCKED` — 다시 불러와도 안 풀린다(G-1) |
| 시계 오차 | ⚠ **규약 없음** §8-5 |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 실사 정보 | `Card` + 읽기 전용 | **c** — G-10 |
| 진행률 | `Progress` | a |
| 스캔 입력 | `TextField` + 포커스 관리 | **c** |
| 라인 목록 + 수량 입력 | `Table`의 `TableColumn.render` | **c** — 편집 그리드 |
| **수량 키패드** | 키패드 | **d** — **G-6 모바일 5번째** |
| 장부 가림 | 텍스트 마스킹 | **c** |
| 미실사 표식 | `Chip` | a |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | **블라인드 실사에서 차이 사유를 어디서 받나** — 차이를 모르는데 물을 수 없다 | 설계 | **조정** | `W-01-04`에서 받는 것으로 |
| 2 | ⛔ **미실사와 0 카운트를 구분할 수 없다** — `counted_qty NOT NULL`. 부분 전송이 위험하다 | 데이터 | **조정** | **①안(센 라인만 전송) + API 규약** |
| 3 | `count_type_code`·`status_code`·`variance_reason_code` 값 목록 | 공통코드 | **조정** | `W-06-06` · #64 |
| 4 | **모바일이 계획에 없던 라인을 추가할 수 있나** — `line_no` 서버 채번이라 오프라인 불가 | 설계 | **조정** | 온라인 한정으로 시작 |
| 5 | **PDA 시계 오차 보정 규약이 없다** — `counted_at`을 로컬이 채우는데 검증 수단이 없다 | 설계 | **조정** | §9-3 계약 승격 후 |
| 6 | **실사 동결 여부** — (U-D1). 오프라인이 악화시킨다 | 회신 대기 | **조정** | `W-01-04` 소관 |

**차단 0건 — 이 화면은 지금 지어도 된다.**

## §9. 이 장에서 나온 공유 계약 후보

1. **감춰야 하는 값은 서버가 내려보내지 않는다** — 블라인드 실사의 `system_qty`를 화면에서만 가리면 응답 본문에 남는다. **「표시하지 않는다」와 「전송하지 않는다」를 계약이 구분**한다 (A-4는 표시 규칙이고 전송은 막지 않는다고 확정했다 — **이 조항은 그 반대 방향이라 별개다**) (§5-1)
2. **부분 전송이 있는 화면은 「보내지 않은 것」의 의미를 API 규약이 정한다** — 미실사와 0을 구분할 컬럼이 없으므로 **「없는 라인 = 미실사」**를 규약으로 못박아야 한다. 그러지 않으면 오프라인 부분 전송이 조정을 망친다 (§5-2)
3. **오프라인이 채우는 발생 시각은 단말 시계에 의존한다** — C-1 규칙 3이 `counted_at`으로 뒷받침되나, **시계 오차 보정 규약이 없다.** 연결 시 서버 시각과의 차이를 어떻게 다루는지 계약이 정한다 (§5-4)

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| v0.1 | 2026-08-05 | 초안 — 확대 4차 8/11. **`counted_at` NOT NULL + `created_at`이 C-1 규칙 3(발생≠수신)을 재고 계열에서 처음 뒷받침**함을 확인. **`counted_qty NOT NULL`이라 미실사와 0 카운트를 구분할 수 없음**을 발견 — 오프라인 부분 전송에서 위험하므로 「센 라인만 전송 + 없는 라인=미실사」 API 규약으로 정리. **`blind_count` 실재** 확인 — 다만 **가림은 화면이 아니라 서버가 해야** 성립. 실사 동결(U-D1) 미정이 **오프라인에서 악화**되나 `system_qty`가 계획 시점 스냅샷이라 완화됨을 정리. G-6 모바일 **5번째 사용처**. 미결 6건(**차단 0**) · 공유 계약 후보 3건 · 신규 DS 갭 0건. |
