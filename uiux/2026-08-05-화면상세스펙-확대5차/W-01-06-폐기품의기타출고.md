# W-01-06 · 폐기 품의·기타출고

> 작성일: 2026-08-05 · 서식: `00-서식.md` v1.0 · 작성 주체: CREFLE OMF 팀 (UI/UX)
> **확대 5차 11/13** — **§J 승인 워크플로우 2차 검증.** #72 회신(「담당부서 관리자 · 권한 설정으로 처리」)과 **J-3(요청 시점에 승인자를 사람으로 확정)이 부딪친다.**

## §1. 식별

| 항목 | 값 |
| --- | --- |
| 화면 ID · 명 | `W-01-06` · 폐기 품의·기타출고 |
| 프로그램 · 도메인 | **관리웹** · 01 자재창고관리 |
| 유형 | **판정·승인** |
| 행위자 | 물류담당(+품질담당, **품의 승인권자**) |
| 신뢰도 | **확정** |
| IA 위치 | 관리웹 > 자재창고관리 > 반품·폐기 |
| 범위 | 품의 발의 → 승인 → 기타출고. **`W-01-05`와 분리 유지**(4단계 확정 — **01 유일 사례**) |

## §2. 근거 4면

| 면 | 근거 |
| --- | --- |
| **프로세스** | `05` §3 **S11**(01-S-I 폐기 분기) — 「1. [판정 대기 시] **불량창고 입고**(219:5586) 2. 판정 → 03 전이도 3. **[폐기 판정 시] 내부 품의**(219:5587) 4. **기타출고 처리**(219:5588) → MES → 폐기 완료 · **(U-E1) 폐기 계정**(219:5590)」 · 설계 메모 「폐기 = 별도 엔티티보다 **출고 트랜잭션 유형 확장**(투입/이동/**기타출고(폐기)**) + 사유·**품의 참조**·계정 속성 통합 방향 우선」 |
| **화면·IA** | 인벤토리 v1.5 `W-01-06` — 유형 **판정·승인** · 데이터=폐기(기타출고)·자재LOT·재고·수불원장 · REQ-PR-0025·0028 · **「W-01-05·06 분리 유지: 결재선·ERP 전표 종류가 다름 — 규칙 기본값을 사람이 뒤집은 01 유일 사례」** |
| **요구** | ✅**REQ-PR-0025** 재작업 가능 불량 vs 폐기 구분 — **판정은 불량창고 입고 후** · ✅**REQ-PR-0028** 폐기 = **내부 품의 승인 후 기타 출고** · ⚠ **(U-E1) 폐기 계정 = 회신 E-4 대기** |
| **데이터** | `app.approval_request`·`approval_step`·`approval_route`·`approval_route_step`(**§J 4테이블**) · `logistics.goods_issue`(`reason_code`) · `mdm.department` · `app.role` — 실측 |

## §3. 레이아웃 (관리웹 — 구조 기술)

```
┌ 헤더 ─────────────────────────────────────────────────────────────────┐
│ 폐기 품의·기타출고                                                     │
├───────────────────────────────────────────────────────────────────────┤
│ 탭  [ 품의 발의 ] [ 처리 이력 ]        ⓘ 결재는 결재함(W-CO-09)에서    │
├───────────────────────────────────────────────────────────────────────┤
│ 《① 폐기 대상》  ← 불량창고 입고분만                                   │
│  ☑ LOT       품목      불량창고 보유  원천        입고일               │
│  ☑ …0088    ABC-123        320       현장 불량   08-02                │
│  ☑ …0091    DEF-456        140       IQC 불합격  08-01                │
├───────────────────────────────────────────────────────────────────────┤
│ 《② 품의 발의》                                                        │
│   사유   [ 재작업 불가 — 변형 손상                       ] ✅ 필수     │
│   수량   460 EA   금액환산  ⚠ 산정 근거 없음 (회신 E-4)               │
│   결재선                                                               │
│    1단계  담당부서 관리자   →  ⚠ 누구로 확정되나 (§5-2)               │
│    2단계  품질책임자        →  ⚠ 동                                    │
│   ⚠ 결재선 정의 화면이 인벤토리에 없습니다 (J-1 ⬜)                    │
│   폐기 계정  [ 선택지 준비 중 ]  ⓘ 회신 E-4 대기                      │
├───────────────────────────────────────────────────────────────────────┤
│ 《③ 승인 후 — 기타출고》  (승인 완료 시 활성)                          │
│   유형   기타출고(폐기) 고정                                           │
│   ⚠ 승인 없이 출고할 수 없습니다 (✅REQ-PR-0028)                       │
├───────────────────────────────────────────────────────────────────────┤
│ 액션   [ 품의 상신 ]                       [ 기타출고 처리 ]           │
└───────────────────────────────────────────────────────────────────────┘
```

> ⛔ **2026-08-07 정정 — 「결재 대기」 탭과 `[승인] [반려]`를 제거했다.** 결재는 대상이 무엇이든 행위가 같아서 **`W-CO-09` 결재함**이 모아 소유한다(v1.7 신설). 이 화면은 **상신**과 **승인 후 처리**만 갖는다. **착수 통지가 아직 나가지 않아 재작업 0이다.** — `W-CO-09` §5-1

## §4. 필드

### §4-A. 승인 요청 `app.approval_request`

| 라벨(한/베) | 출처 컬럼 | 타입 | 필수 | 검증·비고 |
| --- | --- | --- | :-: | --- |
| 품의번호 | `approval_request_no` | `business_no_t` | ✅ UNIQUE | |
| **승인 유형** | `approval_type_code` | `code_t` | ✅ | **폐기 품의** — 결재선 선택 키(J-2) |
| **대상** | `target_type_code` + `target_id` | `code_t` + bigint | ✅ | **A-10 다형** — ⚠ **무엇을 가리키나** §5-4 |
| 요청자 | `requested_by` | FK NOT NULL | ✅ | |
| 요청 시각 | `requested_at` | timestamptz NOT NULL | ✅ | |
| **사유** | `reason` | **text NOT NULL** | ✅ | ✅ **J-4가 DB로 강제된다** |
| 상태 | `status_code` | `code_t` | ✅ | ⚠ 값 목록 §8-3 |

### §4-B. 결재 단계 `app.approval_step`

| 라벨 | 출처 컬럼 | 필수 | 검증·비고 |
| --- | --- | :-: | --- |
| 단계 | `step_no` | ✅ | `UNIQUE(요청, step_no)` · CHECK > 0 · **A-5 3번째 후보** |
| **승인자** | `approver_id` | ✅ **NOT NULL** | ⛔ **사람이어야 한다** §5-2 |
| 결정 | `decision_code` · `decision_at` · `decision_comment` | — | 미결재면 NULL |

**기록 전용**(`created_at`만) — **J-6 결재 번복 불가가 구조로 강제된다.**

### §4-C. 결재선 정의 `app.approval_route` + `approval_route_step` (읽기)

| 컬럼 | 이 화면이 쓰는 방식 |
| --- | --- |
| `approval_type_code` · `business_unit_id` | 결재선 선택 키 |
| **`min_value` · `max_value`** | **J-2 「한도」** — ⚠ **무엇의 값인가** §5-3 |
| `approver_type_code` | **`USER` / `ROLE` / `DEPARTMENT`** — §5-2 |
| `approver_user_id` · `approver_role_id` · `approver_department_id` | `CHECK (num_nonnulls(...) = 1)` |

## §5. 액션·상태

### §5-1. §J 2차 검증 — **6조항 중 4건이 그대로 선다**

2차 `W-01-02`(긴급 IQC 생략 한도승인)에서 세운 §J를 두 번째 화면으로 검증한다.

| 조항 | 이 화면에서 | 판정 |
| :-: | --- | :-: |
| **J-1** 4테이블 한 벌 | 정의(`route`+`route_step`) / 실행(`request`+`step`) 그대로 | ✅ |
| **J-2** 「한도」=`min_value`/`max_value` | ⚠ **무엇의 값인지 미정** §5-3 | ⚠ |
| **J-3** 요청 시점에 승인자를 **사람으로 확정** | ⛔ **#72 회신과 부딪친다** §5-2 | ⛔ |
| **J-4** 사유 필수 | `reason text NOT NULL` — **DB가 강제** | ✅ |
| **J-5** 「일어나지 않는 것」 명시 | 폐기 승인은 **재고를 차감하지 않는다** §5-5 | ✅ |
| **J-6** 결재 번복 불가 | `approval_step`이 기록 전용 | ✅ |

**J-2·J-3 둘이 남는다.**

### §5-2. ⛔ J-3 ↔ #72 회신이 부딪친다

**J-3**(2차 신설): 「**요청 시점에 승인자를 사람으로 확정**」 — 근거는 `approval_step.approver_id bigint **NOT NULL** REFERENCES app_user`.

**#72 회신**(사용자 확정): 「승인 주체는 **담당부서 관리자**」 · 「명확히 고정된 것이 아니라 **관리자의 권한 설정으로 처리**」

**권한 설정으로 처리한다 = `approver_type_code`가 `ROLE` 또는 `DEPARTMENT`다.** 그런데 **`approval_step`은 사람만 받는다.**

```
approval_route_step.approver_type_code = 'DEPARTMENT'
    → approver_department_id = 품질부
        → 품질부에 관리자가 3명이면?
            → approval_step.approver_id 에 누구를 넣나  ⛔
```

**해법 3안**

| 안 | 방법 | 판단 |
| :-: | --- | --- |
| **① 상신 시 한 사람으로 확정** | 규칙(최상위 1명·순번 등)으로 골라 `approver_id`에 넣는다 | ⚠ **그 사람이 부재하면 결재가 멈춘다** |
| **② 후보 전원에게 단계 생성** | `step_no`를 나눠 여러 행 | ⛔ **`UNIQUE(요청, step_no)`라 같은 단계에 둘을 못 넣는다** |
| **③ 대리 결재로 우회** | ①로 넣되 대리자가 처리 | ⛔ **대리 컬럼이 없다** |

**①을 권고**하되 **부재 시 경로가 필요**하다. `approval_step`에 행을 **바꿔 넣는 것**(승인자 교체)은 기록 전용이라 감사상 문제가 된다.

⚠ **J-3을 세운 근거가 「데이터가 사람만 받는다」였는데, 업무 확정은 「역할로 처리한다」다.** **둘 중 하나가 바뀌어야 한다.**

→ **§I 신규 후보** §9-1 · **#72에 코멘트**로 회신 반영 결과를 알린다 §8-1.

### §5-3. J-2 「한도」가 무엇의 값인지 정해지지 않았다

```
approval_route.min_value  numeric(20,6)
approval_route.max_value  numeric(20,6)
ck_approval_route_range   CHECK (max >= min)
```

주석: 「업무유형·사업부·**수량(금액)** 구간별 승인단계 설정」

**「수량(금액)」이라 둘 다 가능하다.** 2차 `W-01-02`(한도승인)에서는 **수량**으로 읽었는데, **폐기는 금액이 자연스럽다**(폐기 손실이 회계 사안이므로).

| 화면 | 무엇의 구간인가 |
| --- | --- |
| `W-01-02` 긴급 IQC 생략 한도승인 | **수량** — 몇 개까지 검사 없이 |
| **`W-01-06` 폐기 품의** | ⚠ **금액?** — 얼마어치까지 부서장 전결 |

**금액이면 산정 근거가 필요한데 MES에 단가가 없다.** `mdm.item`에 원가·단가 컬럼이 **없다**(실측).

**A-11 물러난 수준**: **수량 기준으로 운용**하고 금액 구간은 쓰지 않는다. **회신 E-4(폐기 계정)와 함께 정해질 사안** → §8-2.

⚠ **`approval_type_code`별로 의미가 갈릴 수 있다** — 같은 컬럼이 화면마다 다른 것을 재면 결재선 설정이 혼란스럽다. **계약이 유형별 의미를 표로 갖는다** → §9-2.

### §5-4. 승인 대상이 무엇인가 — A-10

```
approval_request.target_type_code + target_id
```

**폐기 품의의 대상 후보**:

| 후보 | 판단 |
| --- | --- |
| **`trace.lot`** | ⚠ **여러 LOT을 한 품의로** 올리면 하나만 가리킨다 |
| **`logistics.goods_issue`** *(권고)* | 출고 전표를 **먼저 만들고**(미확정 상태) 그것을 승인 대상으로 |
| 별도 폐기 헤더 | ⛔ 없다(설계 메모가 「별도 엔티티보다 출고 유형 확장」) |

**②를 권고**한다 — 설계 메모의 「**품의 참조**」가 `goods_issue`에 붙는다는 뜻으로 읽히고, 여러 LOT을 라인으로 담을 수 있다.

⚠ **`goods_issue`에 품의 참조 컬럼이 없다**(실측 — `reason_code`·`replacement_expected`·`remarks`뿐). **설계 메모가 「사유·품의 참조·계정 속성 통합」을 말했는데 품의 참조와 계정이 없다.**

→ **§I 신규 후보 2번째** §9-1.

### §5-5. J-5 — 폐기 승인은 **재고를 차감하지 않는다**

**J-5**(2차 신설): 승인 시 「**일어나지 않는 것**」을 명시한다.

| 승인 시 | |
| --- | :-: |
| 재고 차감 | ⛔ **일어나지 않는다** |
| 기타출고 전표 확정 | ⛔ 일어나지 않는다 |
| **승인 상태 전이만** | ✅ |

**출고는 별도 액션**이다(§3 ③). REQ-PR-0028이 「승인 **후** 기타 출고」라고 순서를 못박았다.

**화면 문구**:

```
승인되었습니다. 재고는 아직 차감되지 않았습니다 — 「기타출고 처리」를 진행하세요.
```

⚠ **승인 후 출고를 안 하면 어떻게 되나** — 승인만 되고 방치된 품의가 남는다. **촉구 수단이 없다** → §8-4.

### §5-6. 폐기 계정 — G-2로 비워 둔다

**(U-E1) 폐기 계정 = 회신 E-4 대기**(219:5590). `goods_issue`에 계정 컬럼도 없다(§5-4).

**G-2 적용** — 필드를 감추지 않고 비활성 + 사유:

```
폐기 계정  [ 선택지 준비 중 ]  ⓘ 계정 항목은 고객 회신 대기 중입니다 (회신 E-4)
```

### §5-7. 액션

| 액션 | 활성 조건 |
| --- | --- |
| 대상 선택 | **불량창고 입고분만**(REQ-PR-0025 — 판정은 입고 후) |
| 사유 입력 | **필수**(J-4 — DB가 강제) |
| 결재선 확인 | 자동 — `approval_type_code`+사업부로 조회 |
| **품의 상신** | 대상 1건 이상 AND 사유 입력 AND **결재선 존재** |
| **기타출고 처리** | **승인 완료 후에만**(§5-5) |

> ⛔ **2026-08-07 — 「승인 · 반려」를 이 표에서 뺐다.** `W-CO-09` 결재함으로 이관했다(§3 정정 주). **취소선으로 남기지 않는다** — `verify-ui-coverage.py`가 표를 읽어 요구 목록을 만들므로 **취소선 행도 살아 있는 요구로 나간다.**

## §6. 예외

| 상황 | 처리 |
| --- | --- |
| **결재선이 없다** | ⛔ 상신 차단 — 「결재선이 설정되지 않았습니다」 ⚠ **설정 화면이 없다**(J-1 ⬜) |
| **승인자가 확정되지 않는다** | ⛔ **§5-2** — ①안 채택 전까지 미해결 |
| 반려됨 | ⚠ **재상신 가능** — 새 `approval_request`(J-6 — 번복이 아니라 새 건) |
| 승인 후 미출고 | ⚠ **촉구 수단 없음** §8-4 |
| 승인 없이 출고 시도 | ⛔ 차단(REQ-PR-0028) |
| 폐기 계정 미선택 | ⚠ **막지 않는다** — 값 목록 자체가 없다 |
| **저장 충돌**(B-1) | `409` + 다시 불러오기 |
| **오프라인** | ⛔ 해당 없음 — 관리웹(C-5) |

## §7. DS 매핑

| 용도 | 컴포넌트 | 유형 |
| --- | --- | --- |
| 탭 | `Tabs` | a |
| 대상 목록 + 다중 선택 | `Table` | a |
| **결재선 진행 표시** | **`Stepper`** | a — 2차 `W-01-02`에서 실재 확인 |
| 사유 | `TextField`(multiline) | a |
| 폐기 계정 | `Select`(비활성) | a · **G-2** |
| 승인·반려 | `Button` | a |
| 「일어나지 않는 것」 안내 | `AlertBanner`(info) | a |

**신규 DS 갭 0건.**

## §8. 미결

| # | 항목 | 성격 | 등급 | 처리 |
| --- | --- | --- | :-: | --- |
| 1 | ⛔ **J-3(사람으로 확정) ↔ #72 회신(권한 설정으로 처리)** — `approval_step.approver_id NOT NULL`이 역할 기반 결재를 못 받는다 | **상류↔하류 불일치** | **조정** | **§I 등재 · #72에 코멘트** |
| 2 | **J-2 「한도」가 수량인가 금액인가** — 폐기는 금액이 자연스러운데 **MES에 단가가 없다** | 설계 | **조정** | 수량으로 운용 · 회신 E-4와 함께 |
| 3 | ⛔ **`goods_issue`에 품의 참조·계정 컬럼이 없다** — 설계 메모가 「사유·품의 참조·계정 속성 통합」을 말했는데 사유만 있다 | **상류↔하류 불일치** | **조정** | **§I 등재 · 1번과 묶음** |
| 4 | **승인 후 미출고 품의를 촉구할 수단이 없다** | 프로세스 | **조정** | 목록 표식으로 시작 |
| 5 | `approval_type_code`·`approval_request.status_code`·**폐기 계정** 값 목록 | 공통코드 | **조정** | `W-06-06` · **회신 E-4** |
| 6 | **결재선 정의 화면이 인벤토리 108건에 없다**(J-1 ⬜) — `approval_route_step.step_no`가 **A-5 3번째 후보**인 것도 이 때문 | 프로세스 | **조정** | 완료 보고에서 종합 |

**차단 0건 — 이 화면은 지금 지어도 된다**(①안 전제로).

## §9. 이 장에서 나온 공유 계약 후보

1. **⛔ J-3을 세운 근거(데이터가 사람만 받는다)와 업무 확정(역할로 처리한다)이 부딪친다** — #72 회신이 「담당부서 관리자·권한 설정으로 처리」인데 `approval_step.approver_id`는 `app_user` FK다. **결재선 정의는 역할·부서를 받는데 실행은 사람만 받는** 비대칭이다. **J-3을 「상신 시 규칙으로 한 사람을 고른다」로 고쳐 쓰고, 부재 시 경로를 함께 정한다** (§5-2)
2. **§J-2의 「한도」가 무엇의 값인지는 승인 유형마다 다르다** — `W-01-02`는 수량, 폐기는 금액이 자연스럽다. **한 컬럼이 화면마다 다른 것을 재면 결재선 설정이 혼란스럽다** — 계약이 **유형별 의미 표**를 갖는다 (§5-3)
3. **설계 메모가 「통합 방향」을 말한 것은 확정이 아니라 방향이다 — 그래도 대조한다** — 「출고 트랜잭션 유형 확장 + 사유·품의 참조·계정 속성 통합」에서 **사유만 착지**했다. **방향성 문구도 §I 대조 대상**으로 삼되 확정과 무게를 달리 적는다 (§5-4)

## 변경 이력

| 버전 | 날짜 | 변경 요지 |
| --- | --- | --- |
| **v0.2** | **2026-08-07** | ⛔ **「결재 대기」 탭과 `[승인] [반려]` 제거 — `W-CO-09` 결재함으로 이관.** 결재는 대상이 무엇이든 행위가 같아 모을 수 있다는 판정(`W-CO-09` §5-1)에 따른다. 이 화면은 **상신**과 **승인 후 기타출고 처리**만 갖는다. ✅ **착수 통지 미발행이라 재작업 0.** 아울러 **§J-3의 미해결이 닫혔다** — 이 장이 발견한 「J-3 ↔ #72 회신 충돌」은 **① 업무를 데이터에 맞춘다**로 확정됐고(2026-08-07), 실측 결과 `DEPARTMENT`·`ROLE`은 상신 시 사람을 고를 입력이 모델에 없어 **1차 결재선은 `USER`만 연다**(`W-06-15` §5-2). **J-2도 닫혔다** — **1차에는 값 구간을 쓰지 않는다**(금액 컬럼 0건 · 값 성격은 수량). |
| v0.1 | 2026-08-05 | 초안 — 확대 5차 11/13. **§J 2차 검증** — 6조항 중 J-1·J-4·J-5·J-6은 그대로 서고 **J-2·J-3이 남았다.** **J-3(요청 시점에 승인자를 사람으로 확정)이 #72 회신(「권한 설정으로 처리」)과 정면으로 부딪침**을 발견 — `approval_step.approver_id`가 `app_user` FK라 역할 기반 결재를 못 받는다(**§I 신규 후보** · #72에 코멘트). **J-2 「한도」가 수량인가 금액인가**가 승인 유형마다 갈리고, 금액이면 **MES에 단가가 없어** 수량으로 물러남. 설계 메모의 「사유·품의 참조·계정 속성 통합」 중 **사유만 착지**했음을 확인(**§I 신규 후보 2번째**). J-5(「일어나지 않는 것」)를 「승인은 재고를 차감하지 않는다」로 실증. 미결 6건(**차단 0**) · 공유 계약 후보 3건 · 신규 DS 갭 0건. |
